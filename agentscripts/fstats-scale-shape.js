export const meta = {
  name: 'fstats-scale-shape',
  description: 'Make the standalone f-stats (f4/f3/f4-ratio/qpDstat A+B) PRODUCTION-SCALE + proper GPU shape by INHERITING the precompute architecture wholesale — invent NOTHING new, reuse everything. Root cause (measured, docs/perf/fstats-sweep.md): f4/f3/qpdstat-A borrow the fit GLS jackknife_cov which builds a DENSE N×N covariance Q + Cholesky inverse Qinv but f4 reads ONLY diag(Q) and NEVER inverts (f4.cpp:12) -> O(N^2) mem, OOM at ~36k items (below the C(60,4)~487k envelope); f4-ratio/dstat use a HOST long-double per-item jackknife (CPU-shape violation). THE FIX (5 parts, all REUSE): (1) DIAGONAL-only on-device jackknife (delete the dense Q + the inverse; the per-item variance off the cancellation-formed xtau), on-device native-FP64 cancellation carve-out via the EXISTING engage_f2_precision/emulation_honorable/MathModeScope seam; (2) ITEM-BATCH TILING + the M5 device->host->disk TIERING — REUSE tier_select.hpp (OutputTier enum, the HostRam/Disk sinks, free_host_ram_bytes(), resolve_output_tier, the STEPPE_FORCE_TIER override) + stream_f2_blocks_impl, applied to the quartet/triple/quadruple batch (a select_*_tier analogue sized for the f-stat working set) so it NEVER OOMs at any N (spills exactly like the precompute, PARITY-NEUTRAL); (3) qpDstat Part B genotype front-end: REUSE compute_f2_blocks_streamed (SNP-tile stream) instead of the full-materialize, for large pop-unions; (4) f4-ratio/dstat jackknife ON-DEVICE (native-FP64 cancellation carve-out, NOT host long-double; the FP64 goldens confirm FP64 suffices); (5) PRECISION = EmulatedFp64{40} default (matmul-heavy: the contraction/gather) / native-FP64 fallback+carve-out / NEVER the CPU. NO GOLDEN REGEN — gate against the EXISTING FP64 goldens (golden_fit0_{f4,f3,f4ratio,dstat_geno}_*.csv; the diagonal IS the same number the full jackknife already produces, so they re-pass at 1e-6 by construction). Discipline: design-gate -> implement -> verify. FAIL-PROTOCOL: NEVER git checkout/clean; on failure git stash push -u + HALT; verdict classifies severity (minor=move-on / bad=defer). SINGLE-GPU; REAL data; commit-on-green.',
  phases: [ { title: 'Design: verify reuse seams + diagonal correctness (GATE)' }, { title: 'Implement diagonal jackknife + tiering + streaming + build' }, { title: 'Verify goldens + no-OOM scaling + GPU-shape + commit-or-stash' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -30; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ de27475. The standalone f-stats just landed (f4.cpp/f3.cpp/f4ratio.cpp + src/core/stats/dstat.cpp + dstat_kernel.cu + cmd_qpdstat.cpp). They are GOLDEN-CORRECT but NOT production-scale-shaped (docs/perf/fstats-sweep.md: f4/f3 OOM ~36k items; f4-ratio/dstat host-jackknife).',
  'THE PRECISION POLICY (verified docs/design/fit-engine.md:227-261, cuda_backend.cu:1800/1851, handles.hpp:623, commit 8d4f22f): EmulatedFp64{40} (Ozaki fixed-slice tensor-core) is the DEFAULT for well-conditioned matmul-heavy work (the f2 GEMMs + the covariance SYRK); native FP64 is the FALLBACK (via the single emulation_honorable predicate) PLUS the fixed carve-out for cancellation-sensitive / ill-conditioned ops (the f4-combine/xtau numerator fit-engine.md:232; the Cholesky inverse). NEVER the CPU for a reported number (CpuBackend is the oracle only). The new kernels MUST conform: emulated{40} default where matmul-heavy, native-FP64 carve-out for the xtau/ratio cancellation, NOTHING on the host. Reuse engage_f2_precision / emulation_honorable / MathModeScope — no new precision machinery.',
  'THE M5 TIERING (verified tier_select.hpp): OutputTier {Resident, HostRam, Disk}; select_output_tier picks the fastest tier the result FITS from RUNTIME free VRAM / free_host_ram_bytes(); resolve_output_tier honors config.force_tier > STEPPE_FORCE_TIER env > auto; the streamed tiers run stream_f2_blocks_impl at O(P·tile) (the triple-buffered host sink / the disk staging). PARITY-NEUTRAL (tiering moves WHERE a slab lands, never bits). The f-stats MUST reuse this (the OutputTier enum, the HostRam/Disk sinks, free_host_ram_bytes, resolve_output_tier, the STEPPE_FORCE_TIER override) for the item-batch so they NEVER OOM at any N — spilling device->host->disk exactly like the precompute. A select_*_tier analogue SIZED for the f-stat working set (N items, nb blocks); NOT new sink machinery.',
  'THE FIX (all REUSE, nothing invented): (1) DIAGONAL-only on-device jackknife — f4/f3/qpdstat-A delete the dense N×N Q + Cholesky inverse (f4.cpp:12 documents f4 never inverts Q; correctness-neutral), keep only the per-item variance off the cancellation-formed xtau (native-FP64 carve-out). (2) item-batch tiling + the device/host/disk tiering (reuse above) -> no OOM at any N. (3) qpDstat-B genotype front-end: reuse compute_f2_blocks_streamed (SNP-tile stream) instead of the full-materialize read_tile(part,0,M0)+full [P×M] decode, for large pop-unions. (4) f4-ratio/dstat jackknife ON-DEVICE (native-FP64 cancellation carve-out; the host long-double is a policy/GPU-only deviation; the FP64 AT2 goldens confirm FP64 suffices — long-double was over-precision). (5) precision per the policy above.',
  'NO GOLDEN REGEN. The existing FP64 goldens (tests/reference/goldens/at2/csv/golden_fit0_{f4,f3,f4ratio,qpdstat,dstat_geno}*.csv) ARE the oracle. The diagonal IS the same number the current full jackknife produces (it reads diag(Q) today), so they re-pass at est/se/z/p rtol 1e-6 BY CONSTRUCTION; if any golden shifts, the refactor changed a number and that is a BUG.',
  'FAIL-PROTOCOL (USER-MANDATED): NEVER git checkout -- . / git clean -fd. On ANY failure ' + STASH + ' "wip:fstats-scale-FAILED-<reason>" + HALT. NON-trivial/structural blocker -> STOP + report (defer). Classify minor (move-on) vs bad (defer).',
  'SINGLE-GPU (--device 0; multi-gpu PARKED). RELEASE build-rel. REAL AADR; no synthetic. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering (the device kernels in .cu; tier policy host-pure CUDA-free like tier_select.hpp). nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); if (r === null) { log(opts.label + ': transient null — retry'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) } return r }

phase('Design: verify reuse seams + diagonal correctness (GATE)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','diagonal_plan','tiering_reuse','precision_map','geno_stream_plan','ratio_dstat_plan','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff all 5 parts can be done by REUSING the existing machinery with the existing goldens unchanged; false -> structural blocker, defer' },
    diagonal_plan: { type: 'string', description: 'how f4/f3/qpdstat-A get a diagonal-only on-device variance (delete the dense Q + Cholesky inverse) — PROVE correctness-neutral (f4 reads only diag(Q), never inverts: f4.cpp:12) so the existing goldens re-pass at 1e-6 unchanged; the native-FP64 carve-out via engage_f2_precision; file:line' },
    tiering_reuse: { type: 'string', description: 'how the item-batch reuses tier_select.hpp (OutputTier, the HostRam/Disk sinks, free_host_ram_bytes, resolve_output_tier, STEPPE_FORCE_TIER) + stream_f2_blocks_impl — a select_*_tier analogue sized for the f-stat working set (N items, nb); what is reuse vs a minimal new sizing helper; file:line' },
    precision_map: { type: 'string', description: 'per new sub-step: emulated{40} (matmul-heavy) vs native-FP64 carve-out (cancellation) vs reused — conforming to the fit-engine.md policy; NOTHING on the host' },
    geno_stream_plan: { type: 'string', description: 'how qpDstat-B reuses compute_f2_blocks_streamed (SNP-tile) instead of full-materialize; file:line' },
    ratio_dstat_plan: { type: 'string', description: 'how the f4-ratio/dstat jackknife moves on-device (native-FP64 carve-out); proof FP64 (not host long-double) reproduces the existing golden' },
    blocker: { type: 'string', description: 'if NOT feasible: the structural gap + options for the user' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing the f-stats production-scale + GPU-shape fix by REUSING the precompute architecture (verify-before-implement; NO code changes). READ: tier_select.hpp (the FULL device/host/disk tiering — OutputTier, select_output_tier, resolve_output_tier, free_host_ram_bytes, the sinks) + stream_f2_blocks.hpp + shard_plan.hpp + host_ram.cpp (the sink/resolve impls); src/core/qpadm/f4.cpp + f3.cpp + f4ratio.cpp + src/core/stats/dstat.cpp (the current jackknife flow); src/device/cuda/cuda_backend.cu jackknife_cov (~1792-1930: the dense Q SYRK + the Cholesky potrf/potri inverse — what to delete for the diagonal) + the xtau kernel + engage_f2_precision/emulation_honorable/MathModeScope (~1361-1457); docs/design/fit-engine.md:227-261 (the precision policy); docs/perf/fstats-sweep.md (the measured OOM/host-jackknife). VERIFY: (a) the diagonal-only jackknife is correctness-neutral (f4 reads only diag(Q), never inverts — confirm f4.cpp:12) so the EXISTING goldens re-pass at 1e-6 with NO regen; (b) the item-batch can REUSE tier_select/sinks/streaming for device->host->disk OOM-immunity; (c) the precision per-substep map (emulated{40}/native-carve-out, nothing host); (d) qpDstat-B can reuse compute_f2_blocks_streamed; (e) f4-ratio/dstat jackknife on-device FP64 (not host long-double) reproduces the golden.', STD, '',
  'Return the structured design (feasible + the 5 plans + the precision map, file:line). If something CANNOT be done by reuse / would shift a golden / needs a real decision, set feasible=false + blocker. Do NOT implement.',
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:scale', phase: 'Design: verify reuse seams + diagonal correctness (GATE)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- f-stats scale STRUCTURAL (defer): ' + design.blocker); return { halted: true, deferred: true, design } }
log('feasible by reuse; diagonal correctness-neutral: ' + String(design.diagonal_plan).slice(0,120))

phase('Implement diagonal jackknife + tiering + streaming + build')
const fixer = await tryAgent([
  'You are implementing the f-stats production-scale + GPU-shape fix per this design (REUSE everything; invent nothing):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced). Work from HEAD.', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT all 5 parts REUSING the existing machinery: (1) the diagonal-only on-device jackknife for f4/f3/qpdstat-A (delete the dense Q + Cholesky inverse; native-FP64 carve-out via engage_f2_precision) — the existing goldens MUST re-pass UNCHANGED at 1e-6 (no regen). (2) item-batch tiling + the device/host/disk tiering REUSING tier_select (OutputTier/resolve_output_tier/free_host_ram_bytes/STEPPE_FORCE_TIER) + the sinks + stream_f2_blocks_impl. (3) qpDstat-B genotype front-end via compute_f2_blocks_streamed. (4) f4-ratio/dstat jackknife on-device (native-FP64 carve-out). (5) precision per the policy. ADD: a large-N no-OOM scaling test (e.g. 487k all-quartets over the 60-pop f2 + 1M, proving NO OOM + linear) and a force-tier (resident/host/disk via STEPPE_FORCE_TIER) f-stat test. Build + full STEPPE_THOROUGH ctest. SANITY: ALL existing f4/f3/f4ratio/qpdstat/dstat_geno goldens re-pass at 1e-6 UNCHANGED; the new large-N test does NOT OOM. Report every file changed, the reuse (file:line), the gate + the new scaling test, the FULL ctest. Do NOT commit. NON-trivial blocker -> STOP + report.',
].join('\n'), { label: 'implement:scale', phase: 'Implement diagonal jackknife + tiering + streaming + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for the f-stats scale fix. Accumulated edits (do NOT clean/revert/destroy; git stash only if forced). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON) + green ctest, patching only trivial -Werror / CMake wiring. DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 5x on trivial errors. NON-trivial -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement diagonal jackknife + tiering + streaming + build' })

phase('Verify goldens + no-OOM scaling + GPU-shape + commit-or-stash')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','goldens_unchanged','no_oom_at_scale','diagonal_correctness_neutral','tiering_reused','nothing_on_cpu','precision_conformant','build_clean','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if ALL existing goldens re-pass at 1e-6 UNCHANGED (no regen), the large-N test (>=487k items) does NOT OOM + is ~linear, f4/f3/qpdstat-A use diagonal-only (no dense Q/inverse), the device/host/disk tiering is reused (force-tier test passes), the f4-ratio/dstat jackknife is on-device (nothing on host), precision conforms (emulated{40}/native-carve-out), full ctest green, build clean, single-GPU' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (empty if pass)' },
    goldens_unchanged: { type: 'boolean', description: 'the EXISTING f4/f3/f4ratio/qpdstat/dstat_geno goldens re-pass at 1e-6 with NO regen (the diagonal is the same number)' },
    no_oom_at_scale: { type: 'boolean', description: 'a >=487k-item f4/f3 run does NOT OOM (the old wall was ~36k); spills device->host->disk if needed' },
    diagonal_correctness_neutral: { type: 'boolean' }, tiering_reused: { type: 'boolean', description: 'reuses tier_select OutputTier/resolve_output_tier/sinks/STEPPE_FORCE_TIER (not new machinery)' },
    nothing_on_cpu: { type: 'boolean', description: 'the f4-ratio/dstat jackknife moved on-device; no host long-double / CPU compute for a reported number' },
    precision_conformant: { type: 'boolean', description: 'emulated{40} default / native-FP64 carve-out per the fit-engine.md policy' },
    build_clean: { type: 'boolean' }, commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the before/after scaling (OOM gone, linear), the reuse, the GPU util; on FAIL the blocker + minor-vs-bad' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for the f-stats scale + GPU-shape fix (adversarial). design:\n<<<\n' + JSON.stringify(design) + '\n>>>\nimplementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — diagonal-only (the dense Q + Cholesky inverse DELETED for f4/f3/qpdstat-A), the tiering REUSES tier_select (no new sink machinery), the f4-ratio/dstat jackknife is ON-DEVICE (no host long-double), precision conforms (emulated{40}/native carve-out), nothing new invented. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; the EXISTING goldens re-pass at 1e-6 UNCHANGED (independently confirm a golden value did NOT shift); the large-N (>=487k) test does NOT OOM + is ~linear; the force-tier (resident/host/disk) test passes. (3) confirm single-GPU, real AADR, no synthetic, no golden regen. PASS only if all hold. ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed source/test/cmake/doc (NEVER git add dot; never aadr/ atlas_results/ handoff-*.md; NO golden files should change), commit (perf(f-stats scale): diagonal-only on-device jackknife + M5 device/host/disk tiering + item-tiling + genotype streaming [all REUSE] -> f4/f3/qpDstat production-scale, OOM removed, GPU-shaped, nothing on CPU; goldens unchanged) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs (docs/perf/fstats-sweep.md: the OOM-removed + linear after-numbers; TODO/REORIENT: f-stats production-scale-shaped). ',
  'ON FAIL: DO NOT git checkout/clean. ' + STASH + ' "wip:fstats-scale-FAILED" (capture the ref). Classify fail_severity. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:scale', phase: 'Verify goldens + no-OOM scaling + GPU-shape + commit-or-stash' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ f-stats scale ' + verdict.commit_hash + ' — ' + verdict.note)
else log('--- f-stats scale FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — ' + verdict.note)
return { design, verdict }
