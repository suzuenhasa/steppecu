export const meta = {
  name: 'm-fit-6-rotation',
  description: 'M(fit-6) S8: the qpAdm model-space ROTATION on the GPU, designed SCALE-FIRST. The production workload: enumerate a rotation of MANY (large) candidate models and fit+rank-test each BATCHED on the GPU (cuSOLVER gesvdjBatched + pointer-array batched potrf/potri/getrf across models, the batched SYRK/GEMMs), sharded across BOTH RTX 5090s (each model fits wholly on one device — zero inter-GPU traffic — results re-sorted by model index host-side, deterministic). The design center is the production envelope (large nl/nr x thousands of models x multi-GPU), NOT the smallest golden. Validated vs a REAL AT2 rotation golden (pinned via admixtools on real AADR) for the model set; the 9-pop golden is only a parity spot-check. Reads f2 RESIDENT (DeviceF2Blocks replicated per device). Design (scale-first) -> Goldens (real AT2 rotation) -> GPU (batched + multi-GPU) -> INDEPENDENT verdict. commit-green / revert+HALT.',
  phases: [
    { title: 'Design', detail: 'freeze the SCALE-FIRST contract: batched cuSOLVER (gesvdjBatched + pointer-array batched solves across models) + multi-GPU model-shard sharding + the rotation API + result table; read-only' },
    { title: 'Goldens', detail: 'pin a REAL AT2 rotation golden on box5090 (admixtools qpadm across a meaningful rotation on real AADR) — per-model est/p/f4rank/feasible; full §12 metadata' },
    { title: 'GPU', detail: 'the batched multi-GPU rotation on box5090: many large models fit+rank-tested batched, sharded across both 5090s, f2 resident; to green vs the rotation golden + throughput at scale' },
    { title: 'Verify', detail: 'independent verdict: the rotation RUNS BATCHED on the GPU(s) (not per-model host loop, not CpuBackend), matches the AT2 rotation golden, scales (throughput reported); commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -30 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const QPTEST = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ctest --test-dir build-rel -R qpadm --output-on-failure 2>&1 | tail -90'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120, 2x RTX 5090) reimplementation of ADMIXTOOLS 2. Phase-2 qpAdm fit engine. Branch phase2-fit-engine @ 01a94d5 (== main). DONE on the GPU: M(fit-4) the qpAdm fit, M(fit-2) the rank test/qpWave (rank sweep + rankdrop + popdrop), and ARBITRARY-SIZE single models (cuSOLVER large_svd_V gesvdj/gesvd + dynamic VRAM workspace; the nr=39 NRBIG model runs on the GPU vs golden_fit1_NRBIG). The fit is fully model-parameterized (QpAdmModel = {target, left[], right[]}; dims derived at runtime). This milestone is S8: the model-space ROTATION.',
  'HARD RULE — SCALE-FIRST (the user, emphatic; memory design-for-scale-not-smallest-model + build-the-real-gpu-implementation): DO NOT anchor on the smallest golden. The production workload is THOUSANDS of (often large) models rotated over a source/right pool, fit+rank-tested, ranked by feasibility. Design and build for THAT envelope from the first line: BATCHED across models on the GPU + sharded across BOTH GPUs. The small golden is ONLY a cheap parity spot-check, never the design center. A per-model host loop calling the single-model path is NOT acceptable as the deliverable (that is the CPU-shaped anti-pattern) — the rotation must be GPU-batched.',
  'WHAT S8 IS (architecture.md §5 S8; fit-engine.md M(fit-6); the embarrassingly-parallel multi-GPU payoff): given a target + a POOL of candidate left sources + a right set (and/or a right-rotation), ENUMERATE the candidate models (e.g. all valid k-subsets of the source pool, the AT2 "rotation"), and for EACH model run the qpAdm fit + rank test (weights, p, f4rank, feasible) — producing a RANKED feasibility table. The existing API anticipates this: include/steppe/qpadm.hpp references run_qpadm_search over std::span<const QpAdmModel> (M(fit-6)). Build it.',
  'BATCHING (the core scale primitive — GPU-first, not a host loop): fit MANY models in one batched dispatch. Use cuSOLVER BATCHED routines across the model axis — gesvdjBatched for the SVD (requires dims<=32; the common rotation case), per-model gesvd for the >32 tail; pointer-array BATCHED potrf/potri/getrfBatched/getrsBatched for the Qinv + weight solves across models; strided/pointer-array batched GEMM/SYRK for the covariance + f4 assembly across models. Reuse the M(fit-2)/cap-lift kernels but lift them to a model-batch axis (the *_large kernels are one-model-per-launch — batch them: one block/grid-stride per model, or the cuSOLVER batched APIs). Precision: the unified policy (batched SYRK/GEMM EmulatedFp64{40} default, SVD/Qinv/chi^2 native).',
  'MULTI-GPU (this is multi-GPUs proper home — zero inter-GPU traffic): replicate the resident f2_blocks on every device in Resources::gpus (DeviceF2Blocks per device; the precompute already broadcasts), then SHARD THE MODEL LIST across the G devices (round-robin / contiguous ranges). Each model is fit WHOLLY on one device (its internal reductions stay single-GPU, inheriting single-GPU parity); NO cross-device communication during the search. Re-sort results by model index HOST-SIDE so the output order is deterministic regardless of which GPU produced which model (architecture.md §5/§11.4/§12). Report the 1-GPU vs 2-GPU throughput (models/sec).',
  'GOLDENS (REAL AADR only, §12/§13 — NO synthetic data for any accuracy claim): pin a REAL AT2 rotation golden on box5090 (R + admixtools were installed for M(fit-0)). Pick a target + a source POOL (e.g. ~6-12 candidate sources) + a right set over pops present in the AADR .ind; run AT2 qpadm across the rotation (the AT2 rotation idiom / qpadm per model, boot=FALSE deterministic), capturing PER-MODEL est (weights), p (tail), f4rank, and the feasibility flag, with full §12 metadata (R ver, admixtools ver, blgsize, maxmiss, the pop lists). This is the rotation acceptance oracle. Also reuse the existing 9-pop + NRBIG goldens as single-model spot-checks. If a large rotation golden is heavy, pin a meaningful set (tens-to-hundreds of models) for accuracy + demonstrate THROUGHPUT at larger N (thousands) honestly (validated subset + throughput-demonstrated full; do NOT fabricate per-model goldens you did not compute).',
  'GATE (must-pass, the SCALE bar): the rotation runs BATCHED on the GPU(s) (cuSOLVER batched + multi-GPU model-shard, f2 resident, NOT a per-model host loop, NOT the CpuBackend) AND the per-model results (weights, p, f4rank, feasible) match the AT2 rotation golden within tier (weights rtol~1e-6, p/feasibility loose/decision, f4rank exact) for the validated model set; the 9-pop + NRBIG single-model goldens still pass (no regression); deterministic host-side result order across GPU count; build clean + full ctest green. Report the validated-set parity + the 1-GPU and 2-GPU throughput (models/sec) at scale.',
  'BOX = box5090 (vast 2x RTX 5090, UP, sm_120, CUDA 13, 2 DEVICES — use BOTH). ' + SSH + ' (alias); nvcc -> ' + PATHENV + ' . build-rel exists. RELEASE only. NOTHING builds locally. R/admixtools + AADR + the existing f2 fixtures on the box.',
  'KEY FILES: include/steppe/qpadm.hpp (run_qpadm_search over span<QpAdmModel> — the rotation API + the per-model result table), src/core/qpadm/ (a NEW rotation/search orchestrator: enumerate models, dispatch the batch through the backend, multi-GPU shard + host re-sort), src/device/backend.hpp (a batched search virtual if warranted), src/device/cuda/cuda_backend.cu + qpadm_fit_kernels.cu/.cuh (the BATCHED rotation: gesvdjBatched + batched solves + model-batch kernels; multi-GPU via Resources::gpus), src/device/resources.hpp (PerGpuResources per device — already multi-GPU capable from the precompute), tests/reference/test_qpadm_parity.cu or a NEW test_qpadm_rotation.cu (the rotation vs the AT2 golden, on the GPU(s), throughput), tests/reference/goldens/at2/ (the rotation golden), docs/design/fit-engine.md (the S8 row).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); the qpAdm/rotation test (' + QPTEST + '). Iterate until build clean + full ctest green + the rotation passes BATCHED on the GPU(s) vs the AT2 rotation golden + no single-model regression. Do NOT commit (the verdict commits). Clean revert: ' + CLEAN + '.'

phase('Design')
const design = await agent([
  'You are the lead CUDA architect freezing the M(fit-6) S8 contract: the qpAdm model-space ROTATION on the GPU, SCALE-FIRST (batched across many large models + sharded across both GPUs). READ-ONLY (read include/steppe/qpadm.hpp incl. the run_qpadm_search stub, src/core/qpadm/* the fit orchestrators, src/device/backend.hpp the qpadm virtuals, src/device/cuda/cuda_backend.cu the fit + rank_sweep + large_svd_V + the *_large kernels, qpadm_fit_kernels.cu/.cuh, src/device/resources.hpp the multi-GPU PerGpuResources + how the precompute shards/replicates, device_f2_blocks.hpp, the goldens; do NOT edit, do NOT touch the box).', STD, '',
  'Specify with file:line + signatures: (1) the rotation/search API (run_qpadm_search over std::span<const QpAdmModel> -> a ranked per-model result table {model, weights, p, f4rank, feasible}); (2) the BATCHED GPU path — cuSOLVER gesvdjBatched (dims<=32) + per-model gesvd tail for the SVD ACROSS models, pointer-array batched potrf/potri/getrfBatched/getrsBatched for Qinv + weights across models, strided/pointer-array batched GEMM/SYRK for covariance/f4 across models; how the M(fit-2)/cap-lift one-model kernels lift to a model-batch axis (grid-stride per model or the cuSOLVER batched APIs); the per-model dynamic workspace as a batched arena; (3) the MULTI-GPU shard — replicate resident f2 per device (Resources::gpus), shard the model list across G devices, each model wholly on one device (zero inter-GPU traffic), host-side re-sort by model index for determinism; the 1-GPU/2-GPU throughput plan; (4) the precision map (batched SYRK/GEMM EmulatedFp64{40} default, SVD/Qinv/chi^2 native); (5) the GOLDEN/validation plan — what AT2 rotation golden to pin (target + source pool + right set over real AADR pops), how to validate the batched GPU rotation against it + the single-model no-regression, and the throughput-at-scale measurement; (6) memory/throughput envelope (thousands of models x large nl/nr; VRAM budget per device). The design center is the PRODUCTION envelope, NOT the small golden. The implementers make NO design decisions. GPU-first, batched, multi-GPU, resident.',
].join('\n'), { label: 'design:s8-rotation', phase: 'Design' })

phase('Goldens')
const goldens = await agent([
  'You are a pop-gen-aware engineer pinning the REAL AT2 ROTATION golden on box5090 (R + admixtools already installed for M(fit-0)). REAL AADR only — NO synthetic data. EDIT locally (write the golden + a generate script into tests/reference/goldens/at2/) + run R on the box; do NOT commit.', STD, '',
  'THE FROZEN DESIGN (use its golden/validation plan):\n<<<\n' + (design || '(missing — pin a rotation golden: a target + ~6-12 candidate sources + a right set over real AADR pops; AT2 qpadm per model, boot=FALSE; capture per-model weights/p/f4rank/feasible + §12 metadata)') + '\n>>>', '', DEVLOOP, '',
  'STEPS: confirm the chosen target/source-pool/right pops EXIST in the AADR .ind (grep); extract_f2 over that pop set (blgsize=0.05, maxmiss=0); run AT2 qpadm across the rotation (per candidate model, boot=FALSE deterministic) capturing PER-MODEL est (weights), p, f4rank, and feasibility; ALSO dump a co-matching f2 fixture (.bin) the GPU rotation test loads as a DeviceF2Blocks (like f2_fit1_NRBIG.bin). Write the rotation golden (JSON: per-model rows + the model definitions) + a generate .R script + the §12 metadata into tests/reference/goldens/at2/. Run the R DETACHED + poll a /tmp log (flaky network). Report: the rotation golden path + a few spot per-model values (weights/p/f4rank), the model set (target/pool/right), the §12 metadata, and the f2 fixture path. Do NOT commit. Do NOT fabricate any golden number — if a model is infeasible/degenerate, record it as AT2 reports it.',
].join('\n'), { label: 'goldens:s8-rotation', phase: 'Goldens' })

phase('GPU')
const gpu = await agent([
  'You are a senior CUDA engineer building THE DELIVERABLE: the qpAdm model-space ROTATION on the GPU — BATCHED across many large models + sharded across BOTH RTX 5090s, reading f2 resident. The CpuBackend is the diff oracle only. Build per the FROZEN design + validate vs the REAL AT2 rotation golden. EDIT locally + dev-loop on box5090; do NOT commit.', STD, '',
  'THE FROZEN DESIGN:\n<<<\n' + (design || '(missing)') + '\n>>>', '',
  'THE ROTATION GOLDEN (from the Goldens phase):\n<<<\n' + (goldens || '(goldens phase died — pin/locate the rotation golden + f2 fixture before validating; do NOT fabricate)') + '\n>>>', '', DEVLOOP, '',
  'Build the batched multi-GPU rotation: run_qpadm_search enumerates the models and dispatches a BATCHED GPU fit+rank-test (cuSOLVER gesvdjBatched + batched solves + model-batch kernels), sharded across Resources::gpus (f2 replicated per device, model list split, host re-sort by model index). Add a rotation test (load the rotation f2 fixture resident, run run_qpadm_search on the GPU(s), assert per-model weights/p/f4rank/feasible vs the AT2 rotation golden, assert it ran BATCHED on the GPU not a host loop, measure 1-GPU vs 2-GPU throughput models/sec). Iterate until: build clean (warnings-as-errors), full ctest green, the rotation matches the AT2 golden within tier for the validated set, the 9-pop + NRBIG single-model goldens still pass (no regression), and the result order is deterministic across GPU count. Report: git diff --stat; the batched + multi-GPU code; the rotation-vs-golden per-model results; the 1-GPU and 2-GPU throughput (models/sec); confirmation it ran BATCHED on the GPU(s) resident (cuSOLVER batched, not a per-model host loop, not the CpuBackend). Do NOT commit. Do NOT fabricate; do NOT loosen tiers; do NOT report a host-loop or CPU result as the batched-GPU deliverable.',
].join('\n'), { label: 'impl:s8-rotation', phase: 'GPU' })

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','diff_is_real','batched_on_gpu','multi_gpu_sharded','rotation_matches_golden','no_single_model_regression','deterministic_order','throughput','build_green','rotation_results','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: real diff implementing the BATCHED multi-GPU rotation (cuSOLVER batched SVD/solves across models + model-shard across Resources::gpus) + Release build clean + full ctest green + the rotation matches the AT2 rotation golden within tier for the validated model set + the rotation runs BATCHED on the GPU(s) resident (NOT a per-model host loop, NOT the CpuBackend) + the 9-pop & NRBIG single-model goldens still pass + deterministic result order across GPU count. NO tier loosened, NO fabricated golden.' },
    diff_is_real: { type: 'boolean', description: 'you re-ran git diff: the batched rotation is genuinely present (cuSOLVER batched routines + model-batch kernels + multi-GPU shard), not a per-model host loop dressed up, not a stub' },
    batched_on_gpu: { type: 'boolean', description: 'the fit+rank-test across models is GPU-BATCHED (cuSOLVER batched / model-batch kernels), confirmed NOT a host loop calling the single-model path and NOT the CpuBackend' },
    multi_gpu_sharded: { type: 'boolean', description: 'the model list is sharded across both GPUs (f2 replicated per device), each model wholly on one device, zero inter-GPU traffic; you confirmed both devices are used' },
    rotation_matches_golden: { type: 'boolean', description: 'per-model weights/p/f4rank/feasible match the AT2 rotation golden within tier for the validated set' },
    no_single_model_regression: { type: 'boolean', description: 'the 9-pop golden_fit0 + the NRBIG golden_fit1 single-model paths still pass (you re-ran)' },
    deterministic_order: { type: 'boolean', description: 'results are re-sorted by model index host-side; output order is identical on 1 GPU vs 2 GPUs' },
    throughput: { type: 'string', description: 'the measured 1-GPU and 2-GPU rotation throughput (models/sec) at scale + the model count' },
    build_green: { type: 'boolean', description: 'Release build clean (warnings-as-errors) + full ctest green (you re-ran)' },
    rotation_results: { type: 'string', description: 'the YOU-measured per-model rotation results vs the golden (spot values) + the validated model count' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'what landed; the rotation-vs-golden parity + throughput; confirmation it ran batched multi-GPU resident; for FAIL exactly what diverged / where it fell to a host loop or CPU' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (maximally adversarial — the user is emphatic this must be SCALE-FIRST: GPU-BATCHED across many large models + MULTI-GPU, not a per-model host loop and not anchored on the small golden; prior milestones shipped small-first / deferred scale). The GPU implementer reported:\n<<<\n' + (gpu || '(gpu phase died)') + '\n>>>\n\nThe rotation golden:\n<<<\n' + (goldens || '(goldens phase died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself: (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM the rotation is GPU-BATCHED (cuSOLVER gesvdjBatched / batched solves / model-batch kernels) and MULTI-GPU sharded (Resources::gpus, f2 per device, host re-sort) — NOT a per-model host loop over the single-model path, NOT the CpuBackend dressed up. If the "rotation" is secretly a host for-loop or runs on CPU => pass=false. (2) RE-RUN yourself: ' + BUILD + ' ; ' + QPTEST + ' . (3) PASS only if ALL: diff real (batched + multi-GPU); Release build clean; full ctest green; the rotation matches the AT2 rotation golden within tier for the validated set; the 9-pop + NRBIG single-model goldens still pass; the result order is deterministic across GPU count; AND you confirm it ran BATCHED on the GPU(s) resident. Capture the 1-GPU/2-GPU throughput. NO tier loosened, NO fabricated golden. \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source+test+doc+golden files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (M(fit-6) S8 the qpAdm model-space rotation — batched multi-GPU on the GPU, vs the AT2 rotation golden + the measured throughput) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly what diverged or where it fell to a host loop / CPU.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:s8-rotation', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ M(fit-6) S8 ROTATION LANDED ' + verdict.commit_hash + ' — batched_gpu=' + verdict.batched_on_gpu + ' multi_gpu=' + verdict.multi_gpu_sharded + ' matches=' + verdict.rotation_matches_golden + ' throughput=' + verdict.throughput)
else log('--- M(fit-6) S8 FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { design, goldens, gpu, verdict }
