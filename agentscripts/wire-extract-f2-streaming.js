export const meta = {
  name: 'wire-extract-f2-streaming',
  description: 'Wire the M5 adaptive tiered/streamed f2 path into extract-f2 PROPERLY (as a first-class feature, NO code duplication). Today cmd_extract_f2.cpp:429 calls compute_f2_blocks_multigpu_device (the resident-only entry; on G==1 -> run_f2_blocks_resident, the all-M 7*P*M feeder) which OOMs at full-autosome P>=768. The unified tiered entry compute_f2_blocks_multigpu_tiered (f2_blocks_multigpu.cpp:440) ALREADY handles all tiers: resolve_output_tier (config.force_tier > STEPPE_FORCE_TIER > auto select_output_tier) then switch{Resident -> the EXISTING resident path UNCHANGED; HostRam/Disk -> stream_f2_blocks_impl, the SNP-tile INPUT streaming that makes GPU footprint O(P*tile+P^2*nb), independent of M}. It is VALIDATED single-GPU bit-identical to resident (c65179f memcmp gate) and completed derived_2500 (M=584131) to P=2500 single-GPU. But its only callers are a bench + a test (zero production). THE WIRING (proper, no dup): (1) route cmd_extract_f2 compute through compute_f2_blocks_multigpu_tiered (the ONE unified entry — it picks Resident for small inputs so the small goldens stay bit-identical, and auto-streams for big P); consume its F2BlocksOut return (.to_host() -> F2BlockTensor, the SAME downstream the writer already uses; DeviceF2Blocks->F2BlocksOut at the seam only, no duplicated compute). (2) Make the tier override a FIRST-CLASS CLI flag: add --tier auto|resident|host|disk wired to DeviceConfig::ForceTier / config.force_tier (which currently has NO setter), default auto (= select_output_tier). Keep STEPPE_FORCE_TIER as the env fallback. (3) Make the --dry-run tier estimate consistent with the real run. GOLDEN-GATE (bit-identical): the small goldens use the Resident tier -> must stay byte-exact; ADD/extend a test that runs extract-f2 with --tier disk on a small set and memcmp the f2.bin == the Resident output (the streamed==resident parity guarantee, for extract-f2 specifically). Then RE-MEASURE on 1240K single-GPU: 700 pops (and up toward 1000-2500) now COMPLETES via streaming (auto-select AND --tier disk); correct docs/perf/1240k-sweep.md (the ~250-pop ceiling was the un-streamed resident path; streaming lifts it). fixer -> build-repair -> verdict(+re-measure+doc+commit). SINGLE-GPU (multi-gpu PARKED); RELEASE; REAL data; HALT-on-fail; resumable on 529.',
  phases: [ { title: 'Wire the tiered entry + --tier flag (no dup)' }, { title: 'Bit-identical gate + big-pop re-measure + doc + commit' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -55; " + CORECLEAN + "'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'
const PREFIX = '/workspace/data/aadr/1240k/v66.p1_1240K.aadr.patch.PUB'
const BIN = 'LD_LIBRARY_PATH=/usr/local/cuda/lib64 /workspace/steppe/build-rel/bin/steppe'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ b45a76b. extract-f2 (src/app/cmd_extract_f2.cpp) builds an f2 dir from a genotype prefix. The M5 streamed tier is built + validated single-GPU but UNREACHABLE from extract-f2 (it calls the resident-only entry).',
  'THE EVIDENCE (c65179f, verified on box5090 2x RTX 5090, Release, SINGLE-GPU): the streamed path (stream_f2_blocks_impl, SNP-tile INPUT streaming) is memcmp BIT-IDENTICAL to the single-GPU resident reference, GPU peak ~26GB INDEPENDENT of M, and completed full-autosome derived_2500 (M=584131) at P=1000/1500/2000/2500 single-GPU where the resident 7*P*M feeder OOMd at P>=768. multi-GPU is PARKED (memory multi-gpu-parked, 2a0c020 "single-GPU is the path") — this is purely the SINGLE-GPU streamed path; nothing P2P.',
  'THE UNIFIED ENTRY (use it; do NOT duplicate): compute_f2_blocks_multigpu_tiered (src/core/fstats/f2_blocks_multigpu.cpp:440) does resolve_output_tier (config.force_tier > STEPPE_FORCE_TIER env > auto select_output_tier) then switch{ Resident -> the EXISTING resident compute UNCHANGED (so small inputs are byte-identical to today); HostRam/Disk -> the streamed SNP-tile input path }. Returns device::F2BlocksOut (f2_blocks_multigpu.hpp:43, exposes .to_host() -> F2BlockTensor, same as DeviceF2Blocks). The resident-only entry compute_f2_blocks_multigpu_device is what extract-f2 calls now (:429).',
  'THE WIRING (proper, minimal, no dup): (1) cmd_extract_f2 compute -> compute_f2_blocks_multigpu_tiered; change the seam type DeviceF2Blocks->F2BlocksOut and consume .to_host() the SAME way the writer already does (NO duplicated compute, NO if-big-else-small branch in extract-f2 — the tiered entry IS the unified path). (2) add a FIRST-CLASS CLI flag --tier auto|resident|host|disk on extract-f2, plumbed CLI->cli_args->config_builder->DeviceConfig::ForceTier/config.force_tier (which has NO setter today — add it cleanly at the single ConfigBuilder merge site), default auto. Keep STEPPE_FORCE_TIER env as fallback (resolve_output_tier already honors precedence). (3) the --dry-run tier estimate must match the real run. (4) keep the host [P*M] decode footprint as-is (the input streaming keeps inputs host-side; fine on the 251GB box) — note it, do not try to also stream the decode.',
  'GOLDEN-GATE (the parity guarantee): small goldens (9-pop fixtures etc.) use the Resident tier -> their f2/qpadm output MUST stay byte-exact (qpadm_parity/rotation/qpwave/cli_extract_qpadm green). ADD/extend a test (cli e2e or a focused one) that runs extract-f2 on a SMALL real set with --tier disk (or host) and asserts the f2.bin is BIT-IDENTICAL (memcmp) to the default/Resident output — the streamed==resident guarantee, gated for extract-f2 specifically, so this feature stays correct. Do NOT weaken any fit golden.',
  'SINGLE-GPU only (--device 0); RELEASE -DSTEPPE_BUILD_CLI=ON; REAL data; nothing builds locally; clear core dumps. NAMING-STYLE-STANDARD + the CLI/meta schema (docs/design/cli-bindings.md). Verify CUDA/API claims. Box ' + SSH + '; nvcc -> ' + PATHENV + '.',
].join('\n')

const DEVLOOP = 'DEV LOOP: clean HEAD (' + CLEAN + '). Edit locally; rsync (' + RSYNC + '); build+thorough-ctest (' + BUILD + '). Do NOT commit (the verdict commits). NO synthetic. SINGLE-GPU.'

async function tryAgent(p, opts) { let r = await agent(p, opts); if (r === null) { log(opts.label + ': transient null — retry'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) } return r }

phase('Wire the tiered entry + --tier flag (no dup)')
const fixer = [
  'You are a senior engineer wiring the M5 tiered/streamed f2 path into extract-f2 as a FIRST-CLASS feature, NO code duplication. Do NOT commit. Start clean: ' + CLEAN + '. READ src/app/cmd_extract_f2.cpp (esp :318 decode, :429 the compute call, the writer hand-off), src/core/fstats/f2_blocks_multigpu.{hpp,cpp} (the _device vs _tiered entries, F2BlocksOut, .to_host()), src/device/host_ram.cpp (resolve_output_tier), src/device/tier_select.hpp, the CLI plumbing (cli_parse.cpp/cli_args.hpp/config_builder.cpp/run_config.hpp, DeviceConfig::ForceTier), and tests/cli/test_cli_extract_qpadm.cpp.', STD, '', DEVLOOP, '',
  'IMPLEMENT exactly the 4-point wiring in STD: route extract-f2 through compute_f2_blocks_multigpu_tiered (the unified entry; F2BlocksOut seam + .to_host(), no duplicated compute, no big/small branch in extract-f2); add --tier auto|resident|host|disk wired to config.force_tier (add the missing setter at the single ConfigBuilder merge site; default auto; STEPPE_FORCE_TIER stays the env fallback); make --dry-run consistent. ADD the streamed==resident bit-identical test for extract-f2 (small set, --tier disk vs default, memcmp the f2.bin). Build + full STEPPE_THOROUGH ctest (small goldens MUST stay byte-exact). SANITY (no commit): on the 1240K single-GPU, run extract-f2 at 700 pops --maxmiss 0.5 with (a) DEFAULT (expect auto-select streams -> COMPLETES, was OOM) and (b) --tier disk (COMPLETES) — report wall + tier chosen + f2.bin size. Report every file changed, the wiring, the test, the 700-pop result, and the FULL ctest. Do NOT commit. If 700 still OOMs, report exactly where (the host decode? the result tier?).',
].join('\n')
const fix = await tryAgent(fixer, { label: 'fix:wire-streaming', phase: 'Wire the tiered entry + --tier flag (no dup)' })
if (fix === null) { log('--- fixer died — HALT'); return { halted: true } }

await tryAgent(['BUILD-REPAIR for the extract-f2 streaming wiring. Accumulated edits (do NOT clean/revert). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON) + green ctest, patching only trivial -Werror / the F2BlocksOut type propagation mechanics. DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 4x on trivial errors. NON-trivial (a real parity break or a logic error) -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Wire the tiered entry + --tier flag (no dup)' })

phase('Bit-identical gate + big-pop re-measure + doc + commit')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','no_duplication','tier_flag_firstclass','streamed_eq_resident','small_goldens_byte_exact','p700_completes','p700_detail','build_clean','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: extract-f2 routed through the UNIFIED tiered entry (no duplicated compute), --tier is a first-class flag wired to config.force_tier (default auto), streamed==resident is BIT-IDENTICAL (memcmp gated), small goldens byte-exact, 700 pops COMPLETES single-GPU via streaming (auto AND --tier disk), THOROUGH ctest green, Release build clean, single-GPU, no synthetic, no fit-golden weakening' },
    no_duplication: { type: 'boolean', description: 'extract-f2 calls the ONE tiered entry; no copied resident-compute / no if-big-else-small branch in extract-f2' },
    tier_flag_firstclass: { type: 'boolean', description: '--tier auto|resident|host|disk wired to config.force_tier (the missing setter added at the merge site); default auto; env fallback kept' },
    streamed_eq_resident: { type: 'boolean', description: 'extract-f2 f2.bin under --tier disk == default/Resident output, memcmp bit-identical, gated by a test' },
    small_goldens_byte_exact: { type: 'boolean' },
    p700_completes: { type: 'boolean', description: '700 pops single-GPU now COMPLETES via streaming (was OOM)' },
    p700_detail: { type: 'string', description: 'the 700-pop (and any higher) result: tier auto-chosen, wall, peak VRAM, f2.bin; default-auto vs --tier disk' },
    build_clean: { type: 'boolean' }, commit_hash: { type: 'string' },
    note: { type: 'string', description: 'the seam change + the --tier flag + the parity test; the big-pop numbers; for FAIL the exact issue' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for the extract-f2 streaming wiring (adversarial; this touches the f2-output path). The fixer reported:\n<<<\n' + fix + '\n>>>', STD, '',
  'DO: (1) cd ' + R + ' && git --no-pager diff — confirm: extract-f2 calls the UNIFIED compute_f2_blocks_multigpu_tiered (NO duplicated compute, NO big/small branch); --tier wired to config.force_tier with the setter added at the single merge site (default auto, env fallback kept); the F2BlocksOut seam is clean. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; small goldens byte-exact. (3) THE PARITY GATE: confirm extract-f2 on a small real set with --tier disk produces an f2.bin BIT-IDENTICAL (memcmp) to the default/Resident output (run it; the new test should assert this). (4) THE UNLOCK: run extract-f2 at 700 pops (1240K, --maxmiss 0.5, --device 0) with DEFAULT (must auto-select a streamed tier and COMPLETE, was OOM) and with --tier disk (COMPLETE); report the tier chosen, wall, peak VRAM, f2.bin size. PASS only if all hold. ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed source/test/doc files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md), commit with a ROADMAP §6 message (extract-f2: wire the M5 adaptive tiered/streamed f2 path [unified entry, no dup] + first-class --tier flag -> big-P f2 streams single-GPU; 700 pops completes [was OOM]; streamed==resident bit-identical gated) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . ALSO correct docs/perf/1240k-sweep.md (the ~250-pop ceiling was the un-streamed RESIDENT path; with streaming wired, single-GPU extract-f2 completes 700 pops [give the number] and scales toward ~2500 as validated in c65179f; auto-select or --tier disk). Capture the hash + the 700-pop numbers.',
  'ON FAIL: ' + CLEAN + ' and report the exact residual (parity break? still OOM? where?). Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verdict:wire-streaming', phase: 'Bit-identical gate + big-pop re-measure + doc + commit' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true } }
if (verdict.pass) log('+++ STREAMING WIRED ' + verdict.commit_hash + ' — 700p: ' + verdict.p700_detail)
else log('--- FAILED (' + verdict.note + ')')
return { verdict }
