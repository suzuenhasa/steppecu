export const meta = {
  name: 'scope-cli-bindings',
  description: 'SCOPE step 2 (productization): the CLI + Python bindings that expose the now-FINISHED qpAdm fit backend (qpAdm fit / qpWave / model rotation / f2 precompute) so steppe is actually usable. READ-ONLY scoping/design — find out WHAT EXISTS (confirmed: no app/ or bindings/ dir, no CLI11/nanobind in CMake, no main() — greenfield, but the architecture has the design intent: app/bindings->api->core->device layering, CLI11, nanobind+scikit-build-core ADR-0002, C-ABI ADR-0008), CATALOG the public C++ API surface the CLI/bindings wrap (include/steppe/qpadm.hpp run_qpadm/run_qpadm_search/run_qpwave + QpAdmModel/QpAdmOptions/QpAdmResult/JackknifePolicy; fstats.hpp; config.hpp DeviceConfig/RunConfig/Precision; the f2 precompute entry + DeviceF2Blocks; error.hpp Status), and the I/O formats (EIGENSTRAT/PACKEDANCESTRYMAP in; CSV/JSON/Parquet out per the desirable-features survey). 4 parallel design lenses (existing-surface + CLI command design + Python/nanobind design + packaging/layering/testing) -> a synthesis design doc + a milestone build-order. NO box, NO source edits — the deliverable is docs/design/cli-bindings.md for the user to APPROVE before building step 2.',
  phases: [
    { title: 'Scope', detail: '4 parallel read-only lenses: existing API surface / CLI command design / Python nanobind design / packaging+layering+testing' },
    { title: 'Synthesize', detail: 'CLI+bindings design doc + milestone build-order, for user approval' },
  ],
}

const R = '/home/suzunik/steppe'
const OUT = R + '/docs/design/cli-bindings.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell C++20 reimplementation of ADMIXTOOLS 2 f-statistics + qpAdm. main @ (post-docs-refresh). The qpAdm fit BACKEND is now FINISHED: precompute (f2_blocks / DeviceF2Blocks), qpAdm fit (run_qpadm), qpWave (run_qpwave), model-space rotation (run_qpadm_search), rank test/popdrop, jackknife SE + JackknifePolicy, domain outcomes (Status incl ChisqUndefined), NA/missing-block handling — all golden-gated on real AADR, GPU + CpuBackend. The USER build sequence (memory build-sequence-backend-first): step 1 finish backend (DONE), step 2 = CLI + Python bindings for what exists (THIS scoping), step 3 = new standalone stats each with its own CLI/bindings.',
  'WHAT EXISTS (confirmed greenfield for the access layer): NO app/ or bindings/ or python/ dir; NO CLI11/nanobind/pybind/scikit in any CMakeLists; NO main(). But the DESIGN INTENT is in architecture.md: §4/§7 layering app/bindings->api->core->device (CUDA is PRIVATE to steppe_device; core is CUDA-free), the dependency table lists CLI11 2.4.x + nanobind >=2.x, ADR-0002 (nanobind over pybind11), ADR-0008 (C ABI at the public boundary), §9 the ConfigBuilder/RunConfig (compiled<TOML<env STEPPE_*<CLI precedence), the source-tree app/ "(planned, P3) CLI (extract-f2, qpadm, qpdstat)". The bindings note: thin bindings/module.cpp converting numpy<->span_view, no compute logic.',
  'THE PUBLIC API TO WRAP (read it): include/steppe/qpadm.hpp (run_qpadm(DeviceF2Blocks/F2BlockTensor, QpAdmModel, QpAdmOptions, Resources)->QpAdmResult; run_qpwave; run_qpadm_search(span<QpAdmModel>)->vector<QpAdmResult>; QpAdmModel{target,left[],right[]}, QpAdmOptions{rank,fudge,als_iterations,rank_alpha,jackknife policy,...}, QpAdmResult{weight/se/z/p/chisq/dof/f4rank/feasible/rankdrop_*/popdrop_*/status/precision_tag}); include/steppe/fstats.hpp (F2BlockTensor); include/steppe/config.hpp (DeviceConfig/RunConfig/Precision/FilterConfig); include/steppe/error.hpp (Status); the precompute entry (how f2_blocks is produced from EIGENSTRAT/PACKEDANCESTRYMAP -> DeviceF2Blocks) + how it caches to disk (f2_disk_format / the ADMIXTOOLS-compatible f2_blocks interchange).',
  'SCOPE ONLY — NO implementation, NO box, NO source edits, READ-ONLY. The deliverable is a DESIGN doc + a milestone build-order for the user to approve. Be concrete + honest; cite the real API signatures + the architecture ADRs. Match ADMIXTOOLS 2 + admixr ergonomics where it makes sense (the f2_blocks dir, the extract_f2/qpadm command shape, tidy CSV output) so the tool is familiar to AT2 users.',
].join('\n')

phase('Scope')
const LENSES = [
  { key: 'existing-surface', focus: 'Catalog the EXISTING public C++ API + the precompute/IO surface the CLI/bindings will wrap, and confirm what access-layer scaffolding exists (expect none). Read include/steppe/*.hpp (qpadm.hpp, fstats.hpp, config.hpp, error.hpp) — list every public entry point + its args/returns the CLI/bindings must expose. Read how f2_blocks is produced + cached (the precompute entry, f2_disk_format, DeviceF2Blocks.to_host/upload, the ADMIXTOOLS-compatible interchange) + the io readers (eigenstrat/snp/ind). Read the ConfigBuilder/RunConfig design (architecture §9) + the CMake layering (the steppe_core/steppe_device/steppe_io targets, the PRIVATE-CUDA rule) so the app/ target wiring is correct. Report: the API surface table (entry -> args -> what the CLI/binding passes), the precompute->fit data flow, and the exact layering constraints the app/ + bindings/ targets must obey.' },
  { key: 'cli-design', focus: 'Design the CLI command surface (CLI11, architecture app/ = extract-f2/qpadm/qpdstat planned). Propose the commands that expose the FINISHED backend: e.g. `steppe extract-f2` (precompute f2_blocks from EIGENSTRAT/PACKEDANCESTRYMAP -> a cacheable f2_blocks dir, AT2-compatible), `steppe qpadm` (target/left/right -> weights/se/z/p/rankdrop/popdrop), `steppe qpwave` (left/right cladality), `steppe qpadm-rotate` (a source POOL -> the ranked feasibility table, the JackknifePolicy --jackknife=0/1/2 knob). For EACH: the args (mapping to QpAdmModel/QpAdmOptions — pops by name resolved against the .ind/the f2_blocks pop list, --rank, --fudge, --jackknife, --rank-alpha, --precision, --device), the inputs (an f2_blocks dir OR raw geno+ -> extract first), the OUTPUT format (tidy CSV + optional JSON, like admixr/AT2 — the result fields), and the config precedence (compiled<TOML<env STEPPE_*<CLI, §9). Mirror ADMIXTOOLS 2 / admixr ergonomics so AT2 users are at home. Note SINGLE-GPU default (multi-GPU rotation deferred). Cite the API args it maps to.' },
  { key: 'python-design', focus: 'Design the Python bindings (nanobind + scikit-build-core, ADR-0002; thin bindings/module.cpp numpy<->span, no compute logic). Propose the Python API that mirrors the C++ public API ergonomically for the popgen/numpy/pandas user: e.g. steppe.extract_f2(...) -> an f2_blocks handle (or path), steppe.qpadm(f2, target, left, right, **opts) -> a result (a dict / dataclass / pandas DataFrame of weights/se/z/p + the rankdrop/popdrop tables), steppe.qpwave(...), steppe.qpadm_rotate(pool...) -> a DataFrame of the ranked models. Decide the data exchange: numpy arrays in/out (the f2_blocks as a numpy tensor or an opaque handle?), the AT2-compatible f2 dir interchange, results as pandas. Honor the C-ABI boundary (ADR-0008) if it informs the binding seam. Address GPU-optional (the wheel must import without a GPU? graceful CpuBackend fallback). Cite the API + nanobind/scikit-build patterns (web-search nanobind/scikit-build-core if useful via ToolSearch select:WebSearch,WebFetch).' },
  { key: 'packaging-testing', focus: 'Design the packaging, layering, and TESTING for the access layer. (1) The CMake wiring: a steppe_app executable target (CLI11) + a nanobind_add_module bindings target, both at the app/bindings layer (depend on the public api/core, NEVER on a CUDA header — the PRIVATE-CUDA rule, §4/§7); scikit-build-core for the wheel (pyproject.toml). (2) The build-order milestones M(cli-N)/M(py-N) (smallest-first: a thin `steppe qpadm` on an existing f2_blocks dir first, then extract-f2, then rotate, then the Python module). (3) TESTING per the existing pattern: the CLI/bindings must be validated against the SAME real-AADR AT2 goldens (golden_fit0/qpwave/rot/fitNA) — a CLI/python invocation reproducing the golden qpAdm result; NO synthetic data, NO smoke tests; follow the existing golden-test structure. (4) the I/O formats + how a CLI run is reproducible (the §12 metadata, the f2_blocks provenance). Report the target wiring, the milestone order, and the golden-validated test plan.' },
]
const scoped = await parallel(LENSES.map(L => () => agent([
  'You are scoping the steppe CLI/Python access layer, READ-ONLY (design only, no implementation, no box, no edits): ' + L.focus, '', STD,
  '', 'Read the actual headers/CMake/architecture (git + file reads); web-search nanobind/scikit-build-core/CLI11 patterns only if useful. Be concrete + cite real API signatures + the architecture ADRs. Return your design for this lens (commands/args/formats/targets/milestones as applicable), honest about trade-offs. NO fabrication.',
].join('\n'), { label: 'scope:' + L.key, phase: 'Scope' })))

phase('Synthesize')
const doc = await agent([
  'You are the lead architect synthesizing the steppe CLI + Python bindings DESIGN doc (step 2 productization). Combine the 4 scoping lenses into one approvable design + build-order.', '', STD,
  '', 'THE LENSES:\n' + scoped.filter(Boolean).map((s,i)=>'### '+LENSES[i].key+'\n'+s).join('\n\n'),
  '', 'WRITE ' + OUT + ' with: (1) GOAL + scope (expose the FINISHED backend; what exists = nothing yet; the layering/ADR constraints). (2) THE CLI — the command set (extract-f2 / qpadm / qpwave / qpadm-rotate) with args, inputs (f2_blocks dir or raw), tidy CSV/JSON output (the result fields), config precedence, single-GPU default; each mapped to the real API entry. (3) THE PYTHON API — the nanobind module surface (functions, numpy/pandas exchange, the f2 interchange, GPU-optional), mirroring the C++ API. (4) PACKAGING + LAYERING — the app/ executable + bindings/ nanobind target CMake wiring (PRIVATE-CUDA layering), scikit-build-core wheel. (5) TESTING — golden-validated against the existing real-AADR AT2 goldens (NO synthetic), the existing test pattern. (6) MILESTONE BUILD-ORDER — M(cli-1).. / M(py-1).. smallest-first, each golden-gated, with deps. (7) OPEN QUESTIONS for the user (output format default, pop-name vs index resolution, GPU-optional wheel policy, whether bindings wrap the C-ABI or C++ directly). Use the Write tool. Return a 1-screen summary (the command set + the python surface + the milestone order + the open questions) so the user can approve/scope at a glance. Cite real API + ADRs; honest; no fabrication.',
].join('\n'), { label: 'synthesize:cli-bindings', phase: 'Synthesize' })

log('cli-bindings scope: ' + String(doc).slice(0, 200))
return { scoped, doc }
