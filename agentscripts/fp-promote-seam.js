export const meta = {
  name: 'fp-promote-seam',
  description: 'FUTURE-PROOF the fit precision: native FP64 is fine for one model, but at S8 rotation (thousands of models x ~708 LOO blocks = millions of small solves) native FP64 is a Blackwell throughput wall. M(fit-4) landed the GPU qpAdm fit with the cuSOLVER solves HARDCODED native (CusolverDnHandle has set_stream/get only, no math-mode engage). This fix-pass adds the PROMOTION SEAM so the SVD/Cholesky/GLS solves CAN run on the emulated-FP64 (Ozaki) tensor-core path — WITHOUT changing the default (native stays the default => the af6a8c2 golden parity is unchanged). Mirrors the existing cuBLAS MathModeScope/engage_f2_precision. ALSO MEASURES (exploratory, NOT a gate) whether emulated-FP64{40} solves still match the golden at THIS model conditioning — forward evidence for S8. ALSO softens docs/design/fit-engine.md §1.4 "solves pinned native regardless of Precision" -> "default native, promotable under a validated per-stage policy". Fixer on box5090 -> INDEPENDENT verdict. commit-green / revert+HALT.',
  phases: [
    { title: 'Implement', detail: 'add CusolverDnHandle emulated-math-mode seam (default native, promotable) + measure emulated-40 solves vs golden + soften §1.4 doc; dev-loop on box5090' },
    { title: 'Verify', detail: 'independent verdict: seam real + DEFAULT parity unchanged (golden re-passes) + build/ctest green; promoted-emulated result REPORTED not gated; commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -30 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const QPTEST = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ctest --test-dir build-rel -R qpadm --output-on-failure 2>&1 | tail -70'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120) reimplementation of ADMIXTOOLS 2. Phase-2 qpAdm fit engine. Branch phase2-fit-engine @ 3e78c79 — M(fit-4) just landed the qpAdm fit ON THE GPU (CUDA backend): f4-gather from the resident f2 tensor, jackknife Q (cublasDsyrk), rank SVD + ALS, GLS weights, batched LOO SE, via cuSOLVER+cuBLAS+kernels, validated bit-tolerance vs the af6a8c2 AT2 golden (weights 0.5589/0.4411, chisq 4.635, p 0.3268).',
  'WHY THIS FIX-PASS (the user, emphatic; memory fit-precision-future-proof-emulated): native FP64 on Blackwell runs at a small fraction of the tensor-core (emulated-FP64/Ozaki) throughput. For ONE model the fit solves are tiny so native is free — but S8 model ROTATION is thousands of models x a rank sweep x ~708 leave-one-block-out SE re-fits = MILLIONS of small SVD/Cholesky/GLS solves; native FP64 there is a throughput wall. M(fit-4) added CusolverDnHandle with ONLY set_stream/get — the solves are HARDCODED native with NO promotion seam. We must NOT cement that. Emulated FP64 is a TUNABLE-precision tensor-core path (Ozaki slice count / mantissa_bits), and cuSOLVER 13.x exposes it: cusolverDnSetMathMode(handle, CUSOLVER_FP64_EMULATED_FIXEDPOINT_MATH).',
  'WHAT TO BUILD (additive, behavior-preserving): (1) Give CusolverDnHandle (src/device/cuda/handles.hpp) a SCOPED emulated-math-mode capability MIRRORING the existing cuBLAS pattern (MathModeScope + engage_f2_precision in handles.hpp / f2_block_kernel.cuh): a scoped RAII engage that records the previous cuSOLVER math mode (cusolverDnGetMathMode if available, else track our own), sets CUSOLVER_FP64_EMULATED_FIXEDPOINT_MATH for an HONORABLE EmulatedFp64 request (route through the SAME emulation_honorable predicate the f2 path uses — degrade to native + logged tag if not honorable) else the native/pedantic mode, and RESTORES the previous mode at scope exit (warn-on-failure, never throw — like the cuBLAS scope). VERIFY the exact enum/signature against /usr/local/cuda/include/cusolverDn.h ON THE BOX before coding. (2) Wire the per-stage Precision param (already flows into the qpAdm virtuals) so a solve stage CAN engage it — but KEEP THE DEFAULT NATIVE: the qpAdm virtuals still pass native for the ill-conditioned solves, so the af6a8c2 golden parity is UNCHANGED. The deliverable is the CAPABILITY + the wiring, not a default flip. (3) MEASURE (exploratory): add a test path (a tagged/optional test, or a documented one-off in test_qpadm_parity that does NOT gate the default) that runs the GPU qpAdm fit with the SOLVE-stage precision promoted to EmulatedFp64{40} and reports the vs-golden delta per quantity — forward evidence on whether emulated-40 solves hold parity at this Q/X conditioning. (4) DOC: soften docs/design/fit-engine.md §1.4 — the line "the solves are pinned native FP64 internally regardless of the passed Precision" -> "default native; PROMOTABLE to EmulatedFp64 under a validated per-stage policy (the CusolverDnHandle exposes cusolverDnSetMathMode); native FP64 remains the oracle/fallback, and the promotion is validated against it per stage at S8 scale". Keep the table; add a short note that the seam exists as of this commit.',
  'PRECISION/PARITY (§12): the DEFAULT remains exactly M(fit-4): cancellation-sensitive f-stat diffs + the ill-conditioned SVD/Cholesky/GLS in native FP64; the well-conditioned covariance SYRK may be EmulatedFp64{40}. This fix-pass does NOT change any default precision — it only adds the ABILITY to promote the solves later. The af6a8c2 golden MUST still pass on the default path, byte-for-byte the same as 3e78c79.',
  'GATE (must-pass): (a) the seam is REAL — CusolverDnHandle genuinely calls cusolverDnSetMathMode with the emulated/native modes via a scoped RAII (not dead code, not a no-op stub); routed through emulation_honorable; (b) the DEFAULT path is UNCHANGED — the existing qpAdm parity test still passes vs the af6a8c2 golden (weights rtol~1e-6 to 0.5589/0.4411, chisq, dof=4, p) on the GPU; (c) Release build clean (warnings-as-errors) + full ctest green; (d) the §1.4 doc softened. The PROMOTED-emulated measurement (3) is REPORTED in the verdict note, NOT a pass condition — if emulated-40 solves do NOT hold tier at this conditioning, that is a documented finding (native required here; dial mantissa up at S8), NOT a failure of this fix-pass.',
  'BOX = box5090 (vast 2x RTX 5090, UP, sm_120, CUDA 13). ' + SSH + ' (alias); nvcc -> ' + PATHENV + ' . build-rel exists. RELEASE only. NOTHING builds locally. Golden fixture + AADR on the box.',
  'KEY FILES: src/device/cuda/handles.hpp (CusolverDnHandle — ADD the scoped engage; mirror the cuBLAS MathModeScope already in this file), src/device/cuda/f2_block_kernel.cuh / .cu (engage_f2_precision + emulation_honorable — REUSE the predicate), src/device/cuda/cuda_backend.cu (the qpAdm virtual solve sites — wire the precision param to the solver engage, default native), src/device/cuda/qpadm_fit_kernels.cu/.cuh (the on-device small-LA), tests/reference/test_qpadm_parity.cu (add the exploratory promoted-emulated measurement, non-gating), docs/design/fit-engine.md §1.4 (soften the pin language).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); the qpAdm test (' + QPTEST + '). Iterate until: build clean, full ctest green, the DEFAULT qpAdm parity test STILL passes vs the af6a8c2 golden (unchanged), the seam is real, and the promoted-emulated measurement is captured. Do NOT commit (the verdict commits). Clean revert: ' + CLEAN + '.'

phase('Implement')
const impl = await agent([
  'You are a senior CUDA engineer future-proofing the fit precision. Add the cuSOLVER emulated-math-mode PROMOTION SEAM (default native, promotable) mirroring the existing cuBLAS MathModeScope, measure emulated-40 solves vs the golden, and soften the §1.4 doc. EDIT locally + dev-loop on box5090; do NOT commit. ADDITIVE + behavior-preserving: the default precision and the golden parity must be UNCHANGED.', STD, '', DEVLOOP, '',
  'STEPS: (1) ' + SSH + " 'grep -n \"MathMode\\|EMULATED\\|FIXEDPOINT\" /usr/local/cuda/include/cusolverDn.h | head -40' — confirm the exact cuSOLVER math-mode enum/signature on the box. (2) Read the cuBLAS MathModeScope + engage_f2_precision + emulation_honorable in handles.hpp / f2_block_kernel.cuh, then add the cuSOLVER analogue to CusolverDnHandle (scoped RAII engage + restore, routed through emulation_honorable). (3) Wire the per-stage Precision into the solve sites in cuda_backend.cu, DEFAULT NATIVE (golden parity unchanged). (4) Add the non-gating promoted-emulated measurement to test_qpadm_parity.cu. (5) Soften docs/design/fit-engine.md §1.4. (6) Build Release + full ctest; run the qpAdm test. Iterate to green.",
  'Report: git diff --stat; the new CusolverDnHandle engage code; proof the DEFAULT golden parity is UNCHANGED (the qpAdm parity test output, weights 0.5589/0.4411 etc.); the PROMOTED-emulated measurement (per-quantity vs-golden delta when the solves run EmulatedFp64{40} — does it hold the 1e-6/1e-3 tiers or not, with numbers); build/ctest status; the §1.4 doc diff. Do NOT commit. Do NOT fabricate — if emulated-40 solves diverge, report the real deltas.',
].join('\n'), { label: 'impl:fp-promote-seam', phase: 'Implement' })

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','seam_is_real','default_parity_unchanged','build_green','doc_softened','emulated_solves_hold','promoted_measurement','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if ALL must-pass: the cuSOLVER emulated-math-mode seam is REAL (scoped cusolverDnSetMathMode via emulation_honorable, not dead/no-op) + the DEFAULT path parity vs the af6a8c2 golden is UNCHANGED (weights rtol~1e-6, chisq, dof=4, p) on the GPU + Release build clean + full ctest green + §1.4 doc softened. The promoted-emulated MEASUREMENT is NOT a pass condition.' },
    seam_is_real: { type: 'boolean', description: 'you re-read the diff: CusolverDnHandle genuinely sets CUSOLVER_FP64_EMULATED_FIXEDPOINT_MATH (and restores) via a scoped RAII routed through emulation_honorable — capability, not a stub' },
    default_parity_unchanged: { type: 'boolean', description: 'the DEFAULT (native-solve) qpAdm parity test still passes vs the golden, same as 3e78c79 (you re-ran it)' },
    build_green: { type: 'boolean', description: 'Release build clean (warnings-as-errors) + full ctest green (you re-ran)' },
    doc_softened: { type: 'boolean', description: 'docs/design/fit-engine.md §1.4 "pinned native regardless" softened to default-native-promotable' },
    emulated_solves_hold: { type: 'boolean', description: 'the EXPLORATORY result: do the solves promoted to EmulatedFp64{40} still match the golden within tier at this conditioning? (informational — does NOT affect pass)' },
    promoted_measurement: { type: 'string', description: 'the per-quantity vs-golden deltas when the solves run EmulatedFp64{40} (the forward-looking evidence for S8)' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'what landed + the seam location; the promoted-emulated finding; for FAIL exactly what broke (default parity? seam not real? build?)' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (adversarial — prior fix-passes have shammed; the user wants a REAL future-proofing seam that does NOT change the default/parity). The implementer reported:\n<<<\n' + (impl || '(impl died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself: (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM the cuSOLVER seam is REAL (scoped cusolverDnSetMathMode with the emulated + native modes, routed through emulation_honorable, with restore-at-scope-exit; NOT a no-op stub, NOT dead code) AND that no DEFAULT precision was flipped (the solves still default native). Confirm the §1.4 doc is softened. (2) RE-RUN yourself: ' + BUILD + ' ; ' + QPTEST + ' . (3) PASS only if ALL must-pass hold: seam real; the DEFAULT golden parity UNCHANGED (weights rtol~1e-6 to 0.5589/0.4411, chisq, dof=4, p) on the GPU; Release build clean; full ctest green; §1.4 softened. The promoted-emulated measurement is REPORTED (emulated_solves_hold + promoted_measurement), NOT a pass gate. \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source+test+doc files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (the cuSOLVER emulated-FP64 promotion seam — future-proofing the fit solves for S8 scale; default native, golden parity unchanged; + the promoted-emulated measurement) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly what broke.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:fp-promote-seam', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ PROMOTION SEAM LANDED ' + verdict.commit_hash + ' — seam_real=' + verdict.seam_is_real + ' default_parity_unchanged=' + verdict.default_parity_unchanged + ' emulated_solves_hold=' + verdict.emulated_solves_hold + ' | ' + verdict.promoted_measurement)
else log('--- PROMOTION SEAM FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { impl, verdict }
