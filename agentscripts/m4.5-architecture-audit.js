export const meta = {
  name: 'm4.5-architecture-audit',
  description: 'ADVERSARIAL architecture audit of the M4.5 multi-GPU f2 precompute: is it genuinely the IDEAL design, or are the scaling results (single-GPU OOM at only ~50GB-needed P=1536; multi-GPU speedup only 1.07-1.29x and SHRINKING with P; 2500 pops OOMing everywhere) symptoms of a wasteful/wrong architecture? READ-ONLY (code + design docs + the measured sweep), four independent critical lenses — data-layout/memory (incl. f2 symmetry 2x-storage waste + how many full [P^2*n_block] tensors are held at once), parallelism/scaling (why <1.3x and shrinking; Amdahl on the serial D2H tail; sharding optimality), algorithm/formulation (3-GEMM + full-resident-tensor vs ADMIXTOOLS2 / on-demand / on-disk / symmetric / lower-precision-storage), and system-fit (the seam + what the qpAdm fit engine actually needs from precompute) — then an adversarial synthesis with an honest IDEAL? verdict + ranked levers -> docs/cleanup/m4.5/architecture-audit.md.',
  phases: [
    { title: 'Audit', detail: 'four independent critical lenses on the multi-GPU architecture (read-only)' },
    { title: 'Synthesize', detail: 'adversarial holistic verdict: ideal or not + ranked levers + the better design' },
  ],
}

const R = '/home/suzunik/steppe'

const STD = [
  'PROJECT: steppe = CUDA-13/Blackwell (sm_120) reimpl of ADMIXTOOLS 2 f-statistics, precompute-once / fit-many. M4.5 = single-node multi-GPU f2 precompute (block-jackknife tensor f2_blocks[P x P x n_block], the per-block f2 for the leave-one-block-out jackknife). The canonical spec is docs/architecture.md (read §5 the pipeline, §9 Resources, §11.2 the resident pair / budget, §11.4 SPMG + combine, §12 PARITY LAW). Branch m4.5-scaling-sweep (off main with M4.5 merged).',
  'YOUR JOB IS ADVERSARIAL: the user is (rightly) skeptical that this is the ideal architecture — "these results don\'t make sense." Do NOT rubber-stamp it. Actively look for where the design is wasteful, mis-shaped, or solving the wrong problem. Ground EVERY claim in the actual code (file:line) and the measured numbers; quantify waste in GB / ms / x. Where the design IS sound, say so with the reason. End with a blunt IDEAL? = yes / partly / no for your lens.',
  'THE MEASURED SCALING SWEEP (docs/cleanup/m4.5/scaling-sweep.md, rtxbox 2x RTX PRO 6000, 95.6GB/GPU, 169GB host, Release, EmuFp64{40}, n_block=757):',
  '  P     result_GB  single-GPU   G2-device-resident  G2-host-staged   (G2res/G1)',
  '  256   0.79       458ms        355ms               463ms            1.29x',
  '  512   3.18       1213ms       1045ms              1479ms           1.16x',
  '  768   7.14       2334ms       2117ms              3087ms           1.10x',
  '  1024  12.70      3802ms       3570ms              5298ms           1.07x',
  '  1536  28.58      OOM          7843ms              11746ms          enabling',
  '  2000  48.45      OOM          15100ms             21829ms          enabling',
  '  2500  75.70      OOM          OOM                 OOM              -',
  'THE SUSPICIOUS SIGNALS to explain (confirm or refute, with evidence): (a) single-GPU OOMs at P=1536 where the RESULT is only 28.6GB and inputs ~21GB (~50GB) on a 95.6GB card — what is the TRUE device footprint, and is it wasteful (e.g. multiple full [P^2*n_block] intermediate tensors G/Vpair/R held simultaneously, non-chunked, + fragmentation)? (b) the multi-GPU speedup is only 1.07-1.29x AND SHRINKS as P grows — for 2 GPUs on an O(P^2*M) compute that is suspicious; is it Amdahl on a serial combine/final-D2H tail (the pageable result D2H grows as P^2), poor overlap, or load imbalance? (c) f2 is SYMMETRIC (f2[i,j]==f2[j,i], diag 0) and so is Vpair — we store the FULL dense P x P; is that a 2x storage (and compute) waste that alone would move the OOM ceilings? (d) is holding the ENTIRE [P^2*n_block] tensor RESIDENT the right memory model at all, or should the result be tiled/streamed?',
  'KEY FILES (read, cite file:line): include/steppe/fstats.hpp (F2BlockTensor layout: f2/vpair/block_sizes), src/device/cuda/cuda_backend.cu (the per-device compute_f2_blocks — EVERY device allocation: dQ/dV/dS, dF2_all/dVpair_all, the per-block G/Vpair/R GEMM intermediates, chunking, cuBLAS workspace, the D2H), src/device/cuda/f2_block_kernel.cu (the 3-GEMM kernel / formulation + the fixed-slice Ozaki emulated-FP64), src/core/fstats/f2_blocks_multigpu.cpp + f2_blocks_multigpu_core.cpp (sharding + fan-out), src/device/cuda/p2p_combine.cu + src/core/fstats/f2_combine.cpp (combine), src/device/shard_plan.cpp (the block->device mapping), tests/reference/bench_f2_multigpu.cu (what the bench measures + how it allocates), docs/architecture.md (the spec + its rationale), docs/cleanup/m4.5/why-multigpu-slow.md (the D2H-tail root cause), docs/cleanup/m4.5/scaling-sweep.md.',
  'CONTEXT on the real use case: the precompute feeds a qpAdm FIT engine (Phase 2, not yet built) that runs MANY models over the f2_blocks (leave-one-block-out jackknife std errors). Consider what the fit ACTUALLY needs from the precompute when judging whether a full dense resident FP64 [P^2*n_block] tensor is the right deliverable.',
].join('\n')

phase('Audit')
const LENSES = [
  { key: 'data-layout-memory', focus: 'THE MEMORY MODEL + the OOM anomaly. Forensically account EVERY device allocation in compute_f2_blocks (cuda_backend.cu) at P=1536: list each buffer (dQ/dV/dS inputs, dF2_all, dVpair_all, the per-block G/Vpair/R GEMM intermediates, chunk slabs, cuBLAS workspace) with its size in GB as a function of P/M/n_block, and SUM the true peak footprint — does it explain the P=1536 OOM (and is it wasteful: are multiple full [P^2*n_block] tensors resident at once)? Then the SYMMETRY question: f2 and Vpair are symmetric (f2[i,j]==f2[j,i], diag 0); we store full dense P x P (fstats.hpp) — quantify the 2x storage waste and what triangular/packed storage would do to every OOM ceiling (recompute the P=2500 budget: triangular f2+vpair ~38GB — does 2500 then FIT the device-resident root?). And: is holding the ENTIRE [P^2*n_block] tensor resident the right model, or should it be block-tiled/streamed? IDEAL? verdict for the memory model.' },
  { key: 'parallelism-scaling', focus: 'WHY ONLY ~1.1x AND SHRINKING. Decompose the multi-GPU critical path (per why-multigpu-slow.md: ~74% compute overlap, then a serial combine + final pageable D2H tail that grows as P^2). Compute the Amdahl ceiling: what serial fraction explains 1.29x@256 -> 1.07x@1024, and does the shrink match the result-D2H growing as P^2 while compute is O(P^2*M) (M fixed)? Is the final result D2H PAGEABLE + serial (the obvious cap)? Is block-aligned SNP sharding load-balanced and is it even the right axis (vs 2D population-pair tiling, or sharding the OUTPUT so each GPU owns disjoint result tiles and there is NO gather)? What would a near-2x design look like? IDEAL? verdict for the parallelization + sharding.' },
  { key: 'algorithm-formulation', focus: 'THE FORMULATION + DELIVERABLE. Is the 3-GEMM f2 reformulation (G=Q*Qt, Vpair=V*Vt, R=...) optimal — could a symmetric kernel (syrk-style, half the FLOPs) compute the symmetric outputs? Is the fixed-slice Ozaki emulated-FP64 the right precision for STORAGE (FP64 dense [P^2*n_block] is the memory wall — does the result need FP64 stored, or could storage be FP32/compressed while compute stays emulated-FP64)? Most importantly: is "precompute ALL blocks into one full resident tensor" the right model vs what ADMIXTOOLS 2 actually does (does AT2 hold all f2_blocks in RAM, or stream/cache to disk?) and vs compute-on-demand / only-needed-pairs? Does precompute-once/fit-many justify a 76GB-at-P2500 resident tensor? IDEAL? verdict for the algorithm + deliverable shape.' },
  { key: 'system-fit', focus: 'THE ARCHITECTURE FIT TO THE REAL NEED. Is multi-GPU even the right scaling axis here, or is the real ceiling a memory problem that out-of-core single-GPU streaming (M5) solves better/sooner (the sweep shows multi-GPU only ENABLES P1536-2000 then OOMs — a narrow window)? Examine the seam/layering (the CUDA-free core ComputeBackend, the resident-handle we just added, host-staged vs device-resident combine): does the abstraction force inefficiency or leave performance on the table? Crucially, what does the qpAdm FIT engine actually CONSUME from f2_blocks (it does leave-one-block-out jackknife over many models) — does it need the full dense FP64 tensor materialized at once, or could the precompute produce a streamed/on-disk/triangular artifact the fit consumes block-by-block, dissolving the OOM wall entirely? Is the current architecture optimizing the wrong thing (a resident full tensor) for the downstream consumer? IDEAL? verdict for the system architecture vs the actual workload.' },
]
const audits = await parallel(LENSES.map((l) => () => agent([
  'You are a senior GPU/HPC architect doing an ADVERSARIAL, READ-ONLY audit of ONE lens of the steppe M4.5 multi-GPU architecture. Do NOT edit code. Cite file:line + measured numbers for every claim; quantify waste in GB/ms/x. Be blunt.', STD, '',
  'YOUR LENS: ' + l.key + '\n' + l.focus, '',
  'Return: (1) what the current design ACTUALLY does in your lens, file:line; (2) the QUANTIFIED inefficiencies/mis-shapes you found (each: what, how much GB/ms/x wasted, why avoidable, evidence) — and explicitly which of the suspicious sweep signals your lens explains; (3) what the IDEAL design for your lens would be (concrete, with the expected effect on the OOM ceilings / speedup); (4) a one-line IDEAL? = yes / partly / no with the single most important change. Be adversarial — find the real flaws; but where the design is genuinely sound, say so and why.',
].join('\n'), { label: 'audit:' + l.key, phase: 'Audit' }))).then(a => a.filter(Boolean))

phase('Synthesize')
const synth = await agent([
  'You are the lead architect writing the adversarial verdict on whether the M4.5 multi-GPU architecture is IDEAL. The four lens audits are below. Be honest and blunt — the user does not want reassurance, they want the truth. WRITE the verdict to ' + R + '/docs/cleanup/m4.5/architecture-audit.md (Write tool). READ-ONLY on code.', STD, '',
  'THE FOUR LENS AUDITS:\n<<<\n' + audits.map((a,i)=>'### '+LENSES[i].key+'\n'+a).join('\n\n') + '\n>>>', '',
  'Cross-examine the lenses against each other and against the measured sweep — where they conflict, the NUMBERS win; call out any lens claim the data refutes. The doc must contain: (1) THE VERDICT — is the M4.5 multi-GPU architecture the ideal design for this problem? A blunt yes / partly / no, in one paragraph, with the 2-3 load-bearing reasons; (2) the RANKED list of genuine architectural flaws (each: what, quantified cost incl. which sweep signal it explains — e.g. the symmetric-matrix 2x storage waste, multiple full intermediate tensors causing the early OOM, the serial P^2 D2H tail capping the speedup, full-resident-tensor vs streamed — with file:line + GB/ms/x + PARITY-safety of the fix); (3) THE IDEAL ARCHITECTURE — concretely what it should be (e.g. triangular/packed symmetric storage; tiled/streamed result that dissolves the OOM wall; output-sharding to kill the gather; possibly out-of-core single-GPU (M5) being the RIGHT axis instead of/before multi-GPU; FP32-or-compressed storage), and what it would do to the ceilings (P=2500 fit? near-2x speedup?); (4) the HONEST framing of whether multi-GPU was even the right thing to build first vs M5 out-of-core, given the narrow P1536-2000 enabling window; (5) a pragmatic recommendation — what to change, in priority order, and what to leave. Tag every claim to a file:line or a measured number.',
  'Return a tight 8-12 line executive summary: the verdict (ideal? y/n), the top 3 flaws with their quantified cost, the single highest-leverage architectural change, and whether M5 out-of-core should leapfrog further multi-GPU perf work.',
].join('\n'), { label: 'synthesize-verdict', phase: 'Synthesize' })

return { audits, synth }
