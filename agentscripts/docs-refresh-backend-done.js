export const meta = {
  name: 'docs-refresh-backend-done',
  description: 'Refresh ALL docs to reflect that the fit-engine BACKEND is now FINISHED (the 6 FINISH-NOW punch-list items F1-F6 landed on main @ 2496a14, golden-gated on real AADR): F5 removed dead opts.constrained; F3 added Status::ChisqUndefined + dof<=0 guard (host+CUDA); F2 the M(fit-5) domain-outcome test (new test_qpadm_domain.cu); F6 widened the G1==G2 determinism memcmp; F4 pinned a REAL AT2 qpwave() golden (golden_qpwave.json) + run_qpwave test (test_qpwave_parity.cu); F1 missing-block/NA handling = AT2 read_f2(remove_na=TRUE) NA-aware block-DROP on BOTH backends (shared core::pair_block_is_missing predicate; new golden_fitNA.json + test_qpadm_missing_block.cu, real-AADR maxmiss=0.99). 5 parallel doc owners update ROADMAP.md, RESUME.md, TODO.md, architecture.md, docs/design/fit-engine.md + the punch-list (mark F1-F6 DONE), each from the SAME git-verified ground truth; then an adversarial verdict cross-checks every claim vs git, fixes overclaims, and commits. NO box, docs only. Honest: the backend is DONE; NEXT is step-2 productization (CLI + Python bindings) — NOT yet built.',
  phases: [ { title: 'Write', detail: '5 parallel doc owners: ROADMAP / RESUME / TODO / architecture / fit-engine + punch-list status' }, { title: 'Verify', detail: 'adversarial accuracy cross-check vs git, fix, commit' } ],
}

const R = '/home/suzunik/steppe'

const GROUND = [
  'GROUND TRUTH (verify against git ' + R + ' before writing; do NOT contradict it):',
  '- main == phase2-fit-engine @ 2496a14. Phase-1 precompute DONE thru M5. Phase-2 qpAdm FIT ENGINE (S3-S8) BUILT + golden-gated. AND NOW the BACKEND-FINISH (step 1 of the backend-first sequence, memory build-sequence-backend-first) is COMPLETE: the 6 FINISH-NOW items from docs/design/fit-engine-finish-punchlist.md all landed:',
  '  * F5 (e8430a2): removed the dead public QpAdmOptions::constrained field (API hygiene; non-negative constrained-weights is a deferred step-3 feature).',
  '  * F3 (ffdcba2): added Status::ChisqUndefined (include/steppe/error.hpp) + a dof<=0 => ChisqUndefined guard on the HOST (qpadm_fit.cpp) AND the CUDA model-batched path (cuda_backend.cu) — was leaking NaN p with status=Ok.',
  '  * F2 (c8fe397): the M(fit-5) domain-outcome acceptance test — NEW tests/reference/test_qpadm_domain.cu — degenerate REAL-AADR models (collinear left -> RankDeficient; fudge=0 singular Q -> NonSpdCovariance; over-parameterized dof<=0 -> ChisqUndefined) asserted as STATUS VALUES (no crash/NaN) on BOTH CpuBackend + CudaBackend. ctest is now 40 tests (qpadm_domain #19).',
  '  * F6 (360e386): widened the G1==G2 determinism memcmp in test_qpadm_rotation.cu to the FULL QpAdmResult (z/dof/est_rank/rank_*/rankdrop_*/popdrop_* — previously a subset).',
  '  * F4 (6481dfa): pinned a REAL AT2 qpwave() golden tests/reference/goldens/at2/golden_qpwave.json (admixtools 2.0.10 / R 4.3.3, real AADR v66.p1_HO) + NEW tests/reference/test_qpwave_parity.cu gating the first-class run_qpwave entry on BOTH backends.',
  '  * F1 (2496a14): missing-block / NA handling (OQ-12). steppe is pairwise-complete (NOT AT2 maxmiss=0 global-intersection), so a pair-block Vpair==0 CAN occur on sparse AADR and was silently imputed f2=0 (bias-toward-0). Now implements AT2 read_f2(remove_na=TRUE): DROP any block with a non-finite/Vpair==0 pair before the LOO/jackknife (not impute-0), via a single shared host/device predicate core::pair_block_is_missing (f2_estimator.hpp) — CpuBackend oracle + GPU (f2_block_keep_kernel; single-model AND S8 model-batched survivor-compaction). Legacy maxmiss=0 goldens stay BYTE-IDENTICAL (no-drop identity arm). NEW golden_fitNA.json + test_qpadm_missing_block.cu (real-AADR maxmiss=0.99, a sparse right pop -> 1 real dropped block, real Vpair==0). NO synthetic data.',
  '- All 6 golden-gated on the REAL-AADR AT2 goldens (golden_fit0/fit1_NRBIG/rot/qpwave/fitNA), default ctest + STEPPE_THOROUGH oracle green, CPU/GPU consistent, deterministic, WALLCLOCK unchanged.',
  '- NOT YET BUILT (the honest NEXT = step 2 productization): NO CLI, NO Python bindings (confirmed: no app/ or bindings/ dir, no nanobind/CLI11 in CMake, no main()). architecture.md keeps app/ + standalone f3/f4/D-stat/qpDstat "(planned)". After step 2 comes step 3 (standalone f-stats each WITH its own CLI/bindings). See docs/research/desirable-features-survey.md + memory build-sequence-backend-first.',
  '- MULTI-GPU rotation remains DEFERRED (TODO(multigpu-host-bounce), run single-GPU). Do NOT claim multi-GPU speedup.',
  'HONESTY: the BACKEND is now finished; do NOT claim the CLI/Python/standalone-f-stats exist. Every "DONE/BUILT" must be git-verifiable. NO synthetic-data claims (F1/F2/F4 goldens are REAL AADR). Cite commit hashes / file paths.',
].join('\n')

phase('Write')
const DOCS = [
  { key: 'roadmap', file: 'docs/ROADMAP.md', task: 'Update ROADMAP.md: under Phase 2, mark the BACKEND-FINISH complete (the F1-F6 items + the new goldens/tests + NA handling/qpwave/ChisqUndefined/domain-outcome/determinism-widen/dead-flag). The fit engine backend is now DONE. Update Phase 2.5 / "what is next" so step 2 = CLI + Python bindings (productization of the finished backend) is THE next work, then step 3 = standalone f-stats each with its CLI/bindings (per memory build-sequence-backend-first + the desirable-features survey). Keep the precompute + multi-GPU-deferred sections accurate. Preserve voice.' },
  { key: 'resume', file: 'docs/RESUME.md', task: 'Update the RESUME.md handoff card: main @ 2496a14; the fit-engine BACKEND is FINISHED (list F1-F6 one-liners + the new goldens golden_qpwave/golden_fitNA + tests test_qpadm_domain/test_qpwave_parity/test_qpadm_missing_block; ctest now 40). Update "open threads / pick up here" -> the next work is step 2 (CLI + Python bindings — NOT built), then step 3 (standalone f-stats). Keep the build/test one-liners + the pointer to docs/RUN-GUIDE.md. Fast re-orientation card.' },
  { key: 'todo', file: 'docs/TODO.md', task: 'TODO.md is BADLY STALE (its big-picture says "the fit engine (Phase 2 / S3-S8) does not exist yet"). Rewrite the big-picture + add a "Phase 2 fit engine — DONE" section: the precompute is done (keep), the fit engine S3-S8 + backend-finish (F1-F6) are DONE (move to the Done section with the commit hashes + the goldens/tests), and the NEXT open items are step 2 (CLI + Python bindings — break into granular checkboxes: CLI commands extract-f2/qpadm/qpwave/qpadm-rotate, the nanobind module, packaging) and step 3 (standalone f-stats). Mark the precompute M6/M7 as remaining. Keep it the granular checkable companion to ROADMAP; preserve the Done/upcoming structure.' },
  { key: 'architecture', file: 'docs/architecture.md', task: 'architecture.md surgical updates: in §5 + the source-tree, note the fit engine backend is FINISHED (M(fit-5) domain outcomes now BUILT + tested; run_qpwave a tested first-class entry; NA/missing-block handling via core::pair_block_is_missing; Status::ChisqUndefined added to the §10 taxonomy). The error.hpp §10 domain-outcome list should reflect THREE outcomes incl ChisqUndefined. KEEP app/ CLI + standalone f3/f4/D-stat "(planned)" (still true). Do NOT rewrite the whole spec; preserve §-numbering + §12 PARITY LAW. Add the new tests to the test listing if one exists.' },
  { key: 'fit-engine', file: 'docs/design/fit-engine.md', task: 'Update docs/design/fit-engine.md: the STATUS banner -> note the backend is FINISHED (S3-S8 + the F1-F6 finish items). In the milestone table, mark M(fit-5) domain outcomes BUILT (the F2 test) and add the finish items (F1 NA handling/OQ-12 CLOSED via PATH B drop-on-both-backends; F3 ChisqUndefined; F4 qpwave golden+test; F5 dead-flag; F6 determinism-widen) with their commits. ALSO update docs/design/fit-engine-finish-punchlist.md: mark all 6 FINISH-NOW items DONE (with commit hashes); the DEFER + NEW-FEATURE sections stay. Keep the design body.' },
]
const written = await parallel(DOCS.map(D => () => agent([
  'You are the doc owner for ' + D.file + ' in the steppe backend-finish docs refresh. READ the current file + verify the ground truth against git (git -C ' + R + ' log/show), then update it. NO box, NO source edits — docs only.', '', GROUND, '',
  'YOUR DOC: ' + D.task, '',
  'Use Write/Edit on ' + R + '/' + D.file + ' (the punch-list owner also edits ' + R + '/docs/design/fit-engine-finish-punchlist.md). Keep the doc voice/structure; surgical for existing docs. Every claim must match git + the ground truth. Return a 2-3 line summary of changes.',
].join('\n'), { label: 'write:' + D.key, phase: 'Write' })))

phase('Verify')
const verdict = await agent([
  'You are the ADVERSARIAL accuracy verdict for the steppe backend-finish docs refresh (overclaiming is worse than stale). The owners updated: ' + DOCS.map(d=>d.file).join(', ') + ' + the punch-list.', '', GROUND, '',
  'OWNER REPORTS:\n' + written.filter(Boolean).map((w,i)=>'### '+DOCS[i].file+'\n'+w).join('\n\n'),
  '', 'DO: (1) read each updated doc. (2) CROSS-CHECK every "DONE/BUILT" claim vs git -C ' + R + ' (commits e8430a2/ffdcba2/c8fe397/360e386/6481dfa/2496a14 exist; the new goldens golden_qpwave.json/golden_fitNA.json + tests test_qpadm_domain.cu/test_qpwave_parity.cu/test_qpadm_missing_block.cu exist; Status::ChisqUndefined in error.hpp; core::pair_block_is_missing exists) — FIX any overclaim, ESPECIALLY: nothing may claim a CLI/Python/standalone-f-stat is built (no app/ or bindings/ exists); no multi-GPU rotation speedup. (3) ensure cross-doc CONSISTENCY (ROADMAP/RESUME/TODO/architecture/fit-engine agree: backend DONE, step-2 CLI is next + not built). Fix directly with Edit. ',
  'THEN commit: cd ' + R + ' && git add ONLY the changed docs (docs/ROADMAP.md docs/RESUME.md docs/TODO.md docs/architecture.md docs/design/fit-engine.md docs/design/fit-engine-finish-punchlist.md; NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md) && commit with a ROADMAP §6 message (docs: fit-engine backend FINISHED — F1-F6 + new goldens/tests; next = CLI/bindings) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Return: the commit hash + any overclaims caught/fixed + a 1-line accuracy confirmation per doc.',
].join('\n'), { label: 'verify:docs', phase: 'Verify' })
log('backend-finish docs refresh: ' + String(verdict).slice(0, 200))
return { written, verdict }
