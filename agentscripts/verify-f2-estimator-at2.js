export const meta = {
  name: 'verify-f2-estimator-at2',
  description: 'Chase the LAST steppe-vs-AT2 parity residual: the ~0.2% f2-VALUE difference that remains after blocks + SNPs were made element-wise identical. It is now isolated to the f2 ESTIMATOR (the het/bias correction, pseudo-haploid adjustment, apply_corr/afprod, the f2 formula) OR the emulated-FP64 floor. KEY (user insight): a het-correction / pseudo-haploid convention difference can be ~0.2% on a MIXED set but BLOW UP on a different regime (pure pseudo-haploid ancients vs all-diploid moderns) — so characterize the MECHANISM + its worst-case across regimes, not just the Haak magnitude. READ-ONLY verify + EMPIRICAL compare (comparing against AT/AT2 is the explicit ask here). 3 lenses: (A) AT2 f2-estimator FROM SOURCE — the original DReichLab AdmixTools C (/workspace/AdmixTools_src/src) + admixtools R 2.0.10 cpp (cpp_aftable_to_f2 / the f2 formula): the exact estimator incl. the n/(n-1) heterozygosity bias correction, apply_corr, afprod, pseudo-haploid (adjust_pseudohaploid) handling. (B) steppe f2-estimator FROM CODE — decode_af finalize_af (Q/V/N, ploidy, het correction), src/core/internal/f2_estimator + the 3-GEMM f2 (the bias/het terms, emulated-FP64). (C) EMPIRICAL ACROSS REGIMES — on the box, steppe f2 vs AT2 f2 (convertf-PA) element-wise on (1) all-DIPLOID-modern pops, (2) all-pseudo-haploid-ANCIENT pops, (3) the mixed Haak set; blocks+SNPs are now identical so any diff is pure estimator -> measure the relative f2 diff per regime -> does it blow up on pseudo-haploid? Synthesis -> spec: the exact estimator difference, the mechanism, the worst-case regime + magnitude, whether it is a FIXABLE convention (het/apply_corr/pseudo-haploid) or the irreducible emulated-FP64 floor, + the reconciliation. Writes /tmp/f2-estimator-at2.md. NO code changes, NO commits.',
  phases: [ { title: 'Verify + characterize across regimes' }, { title: 'Synthesize spec' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const OUT = '/tmp/f2-estimator-at2.md'
const RAW = '/workspace/data/aadr/raw/v66.p1_HO.aadr.patch.PUB'
const PA = '/workspace/data/aadr/converted_pa/v66_HO_pa'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm; parity target = ADMIXTOOLS (original DReichLab C + admixtools R 2.0.10). After fixing blgsize(Morgans), monomorphic-drop, and the block-partition (SNP-anchored walk -> element-wise identical 709 blocks / 264544 SNPs on the Haak union), steppe still differs from AT2 by ~0.2% on f2-derived weights and ~1% on SE. Blocks + SNPs are now byte-identical, so the residual is the f2 ESTIMATOR itself (the per-SNP/per-pair f2 VALUES) OR the emulated-FP64 floor.',
  'THE HYPOTHESIS TO TEST (user): the residual is a het-correction / pseudo-haploid / bias-correction (apply_corr) CONVENTION difference that is small on the mixed Haak set but could BLOW UP on a different regime — e.g. all-pseudo-haploid ancient pops (where the within-population heterozygosity term, and its (n/(n-1)) bias correction, dominate) vs all-diploid modern pops. Characterize the MECHANISM + the WORST CASE, do not just match the one Haak number.',
  'KEY FACTS: AT2 cache_metadata for our goldens had apply_corr=true, afprod=true, adjust_pseudohaploid=true. f2(A,B) = E[(p_A - p_B)^2] - het_A/(2 n_A ...) - het_B/(...) (the unbiased estimator subtracts the within-pop sampling variance; AT2 uses a specific n/(n-1)-style correction + a pseudo-haploid path where N is alleles not individuals). steppe: decode_af finalize_af builds Q (alt-allele freq), V (validity 0/1), N (allele/individual count with the het correction hc=q(1-q)/max(N-1,1)); the 3-GEMM forms f2 from Q/V/N. The exact correction terms + the pseudo-haploid N convention are what must match.',
  'SOURCES: AT2 C /workspace/AdmixTools_src/src (the f2 / aftable / dofstats code); admixtools R 2.0.10 (github uqrmaie1/admixtools, R/ + src/cpp_*.cpp — cpp_aftable_to_f2 / aftable_to_f2 / the f2 + correction); steppe src/core/internal/decode_af.hpp (finalize_af, ploidy, hc), src/core/internal/f2_estimator*, the f2 GEMM (src/device/cuda/f2_block_kernel / f2_blocks_kernel). DATA: steppe reads raw TGENO ' + RAW + '; AT2 reads convertf-PA ' + PA + ' (faithful conversion -> same genotypes). The .ind pop counts are known (Han 153, French 91, Sardinian 58, Papuan 46, Karitiana 28, Mbuti 25 = modern diploid; Russia_Samara_EBA_Yamnaya 46, Czechia_EBA_CordedWare 48, Turkey_N 68, Serbia_IronGates_Mesolithic 73, Israel_Natufian 17, Iran_GanjDareh_N 17 = ancient, many pseudo-haploid).',
  'READ-ONLY: web (admixtools R source) + read box source/files (' + SSH + ') + run steppe extract-f2 + AT2 extract_f2 for the EMPIRICAL regime comparison (comparing against AT2 IS the ask here). NO steppe code changes, NO commits. Deliverable /tmp/f2-estimator-at2.md. Cite exact file:line + the per-regime measured f2 diffs. No fabrication; flag uncertainty + emulated-FP64-vs-native as a candidate floor.',
].join('\n')

phase('Verify + characterize across regimes')
const LENSES = [
  { key: 'at2-f2-source', focus: 'The EXACT AT2 f2 estimator FROM SOURCE. Read the DReichLab AdmixTools C (/workspace/AdmixTools_src/src — grep for the f2 / "aftable" / "dofstats" / the per-pair f2 formula + the heterozygosity/bias correction + the pseudo-haploid path) AND admixtools R 2.0.10 (web-fetch github uqrmaie1/admixtools src/cpp_*.cpp cpp_aftable_to_f2 / aftable_to_f2 + R/ — the f2 with apply_corr, afprod, adjust_pseudohaploid). State the EXACT estimator math: the f2(A,B) formula, the within-pop heterozygosity term + its bias correction (n/(n-1)? 1/(n-1)? the exact denominator), how apply_corr/afprod change it, and the pseudo-haploid convention (N = #alleles vs #individuals; does the het correction differ / vanish for pseudo-haploid?). Pseudocode + file:line.' },
  { key: 'steppe-f2-code', focus: 'steppe f2 estimator FROM CODE. Read src/core/internal/decode_af.hpp finalize_af (the Q alt-freq, V validity, N count, the het correction hc=q(1-q)/max(N-1,1), the ploidy handling for pseudo-haploid), src/core/internal/f2_estimator* (the f2 assembly), and the 3-GEMM f2 kernel (src/device/cuda/f2_block_kernel.cu / f2_blocks_kernel.cu — the Σp^2 - 2Σpq + Σq^2 + the correction terms). State steppe EXACT f2 math: the bias/het correction term + denominator, the pseudo-haploid N convention, and where emulated-FP64 enters. Then DIFF against AT2 (lens A): which term differs (the het denominator? the pseudo-haploid N? apply_corr/afprod presence?). Cite file:line.' },
  { key: 'empirical-regimes', focus: 'EMPIRICALLY measure the steppe-vs-AT2 f2 difference ACROSS REGIMES (blocks+SNPs are now identical, so any diff is the estimator). On the box: pick 3 pop sets — (1) MODERN all-diploid: Han, French, Sardinian, Papuan, Karitiana, Mbuti; (2) ANCIENT all-pseudo-haploid: Russia_Samara_EBA_Yamnaya, Czechia_EBA_CordedWare, Turkey_N, Serbia_IronGates_Mesolithic, Israel_Natufian, Iran_GanjDareh_N; (3) MIXED = the Haak 15-pop union. For EACH: run steppe extract-f2 (raw TGENO, --blgsize 0.05 --maxmiss 0 --auto-only) -> read f2.bin f2 values; run AT2 extract_f2 (convertf-PA, same params) -> read_f2 the f2 array; compute the per-pop-pair RELATIVE f2 difference (max + median). REPORT: is the diff ~uniform ~0.2% (=> emulated-FP/method floor) or does it BLOW UP on the pseudo-haploid ANCIENT set / specific pseudo-haploid pairs (=> a fixable het/pseudo-haploid convention bug)? This is the decisive test of the user worry. Tabulate the f2-diff per regime.' },
]
const found = await parallel(LENSES.map(L => () => agent([
  'You are a popgen-statistics + numerics analyst chasing the last steppe-vs-AT2 f2 parity residual, PRECISE + sourced + empirical: ' + L.focus, '', STD,
  '', 'Use web tools and/or read box source + run the box compares as your lens needs. Cite exact file:line + measured per-regime numbers. Return the estimator math / diff / per-regime table with a clear conclusion (fixable convention vs FP floor; worst-case regime). No fabrication; flag uncertainty.',
].join('\n'), { label: 'f2:' + L.key, phase: 'Verify + characterize across regimes' })))

phase('Synthesize spec')
const spec = await agent([
  'You are the lead synthesizer of the f2-estimator AT2-parity chase for steppe. Combine the 3 lenses into the diagnosis + reconciliation spec.', '', STD,
  '', 'THE LENSES:\n' + found.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'WRITE ' + OUT + ' with: (1) THE EXACT ESTIMATOR DIFFERENCE — AT2 f2 math vs steppe f2 math, the SPECIFIC term that differs (het/bias-correction denominator? pseudo-haploid N convention? apply_corr/afprod?), file:line for both. (2) THE EMPIRICAL CHARACTERIZATION — the steppe-vs-AT2 f2 relative-diff table across the 3 regimes (modern-diploid / ancient-pseudo-haploid / mixed); the WORST-CASE regime + magnitude; whether it BLOWS UP on pseudo-haploid (the user worry) or is uniform. (3) THE VERDICT — is the residual a FIXABLE convention difference (name the exact steppe change to match AT2) or the IRREDUCIBLE emulated-FP64-vs-native floor (if so, quantify it + recommend documenting it as the stated §12 tolerance)? (4) IF FIXABLE — the precise reconciliation (the steppe finalize_af / f2-estimator change) + the blast radius (does it change the precompute goldens? note they are AT2-fixtures, unaffected by the fit tests, but the extract-f2 e2e would tighten). (5) A go/no-go + which it is. Use the Write tool. Return the headline: the differing term + does-it-blow-up-on-pseudo-haploid + fixable-or-FP-floor. Cite sources + measured numbers; no fabrication.',
].join('\n'), { label: 'synthesize:f2spec', phase: 'Synthesize spec' })
log('f2-estimator AT2 chase -> /tmp/f2-estimator-at2.md: ' + String(spec).slice(0, 200))
return { spec }
