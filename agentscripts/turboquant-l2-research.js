export const meta = {
  name: 'turboquant-l2-research',
  description: 'RESEARCH (read-only, web + steppe internals): could a TurboQuant-like quantization methodology — with the angle of keeping the hot working set RESIDENT IN L2 CACHE — fit into steppe and speed something up? steppe is precision-CRITICAL (Ozaki emulated-FP64, bit-identical to ADMIXTOOLS2, parity law §12), so quantization is lossy and the real question is WHERE (if anywhere) it can apply without breaking bit-identity. Three parallel lenses — (1) what TurboQuant actually IS (web; be honest about confidence, do NOT invent its mechanism), (2) steppe precision internals + where precision is sacred vs where there is slack, (3) GPU L2-cache facts (Blackwell sm_120) + which steppe data structures could be kept L2-resident and the bandwidth win — then a synthesis that designs a concrete, measurable EXPERIMENT (what to quantize, what to keep in L2, the accuracy/parity gate, expected payoff, risks, go/no-go). Writes docs/research/turboquant-l2-experiment.md.',
  phases: [
    { title: 'Research', detail: 'parallel: what TurboQuant is (web) + steppe precision internals + L2-cache facts/fit' },
    { title: 'Design', detail: 'synthesize: where it fits + a concrete experiment with the parity gate -> doc' },
  ],
}

const R = '/home/suzunik/steppe'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120) reimplementation of ADMIXTOOLS 2 f-statistics (qpAdm). Pipeline: precompute f2_blocks[P x P x n_block] FP64 ONCE (a 3-GEMM reformulation: G=Q*Qt, Vpair=V*Vt, R=[Q^2;Hc]*Vt, over millions of SNPs) then fit MANY qpAdm models (the "rotation" model search) reading small pair-subsets + a per-block jackknife.',
  'THE PRECISION CONSTRAINT (the crux for any quantization idea): steppe computes the GEMMs with EMULATED-FP64 (Ozaki fixed-slice scheme via cuBLAS emulation, EmulatedFp64{40}) and the cancellation-prone assemble in NATIVE FP64, specifically to be BIT-IDENTICAL to ADMIXTOOLS 2 / a native-FP64 oracle (architecture.md §12 PARITY LAW, memcmp). The whole value proposition is FP64-accurate f-stats. So a LOSSY quantization cannot touch the values that must stay bit-identical. The research question is WHERE a TurboQuant-like (lossy/quantized) method could apply WITHOUT breaking that: e.g. a fast APPROXIMATE pre-screen of the rotation model search (rank/prune candidate models, then EXACT recompute of survivors), or operand encoding the Ozaki slicing already does, or a non-parity-critical path. Be rigorous and honest about the precision tension; do NOT propose breaking §12.',
  'THE L2-CACHE ANGLE (the user’s specific interest): "kept in the L2 cache" — keep the hot working set resident in the GPU L2 (on-chip, ~tens of MB on Blackwell) instead of round-tripping VRAM, so the compute is cache-bandwidth-bound not VRAM-bound. Candidate steppe structures: GEMM operand tiles (Q/V columns for a block), the per-block f2/Vpair slabs (P^2 doubles; e.g. P=256 -> 512 KB), the small pair-subsets the rotation fit reads (low-tens pops). Quantization shrinks these -> more fits in L2 -> potential speedup. Establish the ACTUAL L2 size on RTX 5090 / RTX PRO 6000 (sm_120) and what would fit.',
  'CONTEXT we just established (relevant): the precompute for small P is now DEVICE-RESIDENT (result stays in VRAM, 3.9x win); large P needs streaming (M5); the FIT/ROTATION (many independent models) is the embarrassingly-parallel, multi-GPU-friendly phase. A cache-residency/quantization win is most plausible in (a) the GEMM inner loop (operand tiles in L2) or (b) the rotation fit (quantized f2 subsets resident in L2 for fast approximate model screening).',
  'KEY STEPPE FILES (read for the precision/structure lens): docs/architecture.md (the precision policy / §12 parity law / §5 the 3-GEMM + Ozaki / §11 budgets), src/device/cuda/f2_block_kernel.cu (the 3-GEMM + emulated-FP64 kernel), include/steppe/config.hpp (Precision / EmulatedFp64), docs/cleanup or any emulated-fp64 spike doc, include/steppe/fstats.hpp (F2BlockTensor layout). The agentscripts/steppe-f2-emulated-fp64-spike.js was the Ozaki precision/throughput spike.',
].join('\n')

phase('Research')
const lenses = await parallel([
  () => agent([
    'You are a research analyst. Establish what "TurboQuant" actually IS, from authoritative sources. Use web search/fetch (they are available via ToolSearch: search "select:WebSearch,WebFetch" then call them). READ-ONLY.', STD, '',
    'FIND: what TurboQuant is (paper / library / method?), its quantization METHODOLOGY (what it quantizes, the scheme — e.g. format, rounding, the math/algorithm), its claimed benefits + target domain (you said "inference"), and ANY notion of cache/L2 residency or memory-bandwidth reduction in it. CRITICAL HONESTY: if you cannot find authoritative info, SAY SO and report your confidence — do NOT invent or guess its mechanism. Distinguish what you VERIFIED (with source URLs) from what you are inferring. Return: the method summary (sourced), the core quantization idea, target use, and an explicit confidence level + the sources.',
  ].join('\n'), { label: 'research:turboquant', phase: 'Research' }),
  () => agent([
    'You are a senior numerical/GPU engineer. Map steppe’s precision-critical structure: where is FP64 bit-identity SACRED vs where is there genuine numerical SLACK that a lossy/quantized method could exploit? READ-ONLY; cite file:line / architecture.md section.', STD, '',
    'Read the precision policy + the 3-GEMM/Ozaki kernel. Answer: (1) exactly which quantities MUST stay bit-identical (the f2/Vpair values, the §12 parity) and why; (2) what the Ozaki emulated-FP64 already does (it is itself an integer/fixed-point "quantized" matmul of FP64 — describe the slicing, EmulatedFp64{40}); (3) where in the pipeline there is SLACK a lossy method could use without violating §12 — e.g. an APPROXIMATE first pass of the rotation model search (rank/prune candidates in low precision, then EXACTLY recompute the survivors in emulated-FP64), or pre-filtering, or a coarse screening statistic — name the concrete spots + why parity is preserved (because the EXACT recompute is the source of truth); (4) any spot where quantization would CATASTROPHICALLY break things (the cancellation-prone f2 difference). Return the sacred-vs-slack map with file:line.',
  ].join('\n'), { label: 'research:precision-map', phase: 'Research' }),
  () => agent([
    'You are a GPU performance engineer. Establish the L2-cache facts for the target hardware and which steppe data structures could be kept L2-resident. Use web search for the HW specs (ToolSearch "select:WebSearch,WebFetch"). READ-ONLY on code.', STD, '',
    'FIND/REASON: (1) the ACTUAL L2 cache size of the NVIDIA RTX 5090 and RTX PRO 6000 Blackwell (sm_120) — sourced; (2) the cache-residency / cache-blocking optimization idea (keep operand tiles or the hot working set in L2 so compute is L2-bandwidth-bound not VRAM-bound; CUDA L2 persistence / access-policy windows on Blackwell); (3) which steppe structures could plausibly fit/benefit: GEMM operand tiles (Q/V block-columns), per-block f2/Vpair slabs (P^2 FP64 = P=256 -> 512KB, P=512 -> 2MB, P=1024 -> 8MB), the rotation fit’s small pair-subsets — and how much QUANTIZATION would shrink each (FP64->FP32->INT8 etc.) and whether that brings a working tile under the L2 size; (4) the realistic speedup mechanism (avoided VRAM round-trips) and where it would NOT help (already compute-bound, or the data is too big for L2 regardless). Return the L2 sizes (sourced) + the fit/benefit analysis per structure.',
  ].join('\n'), { label: 'research:l2-cache', phase: 'Research' }),
]).then(a => a.filter(Boolean))

phase('Design')
const design = await agent([
  'You are the lead researcher. Synthesize the three lenses into an honest assessment + a concrete EXPERIMENT design for whether a TurboQuant-like, L2-resident quantization could help steppe. WRITE it to ' + R + '/docs/research/turboquant-l2-experiment.md (Write tool). READ-ONLY on code.', STD, '',
  'THE THREE LENSES:\n<<<\n' + lenses.map((a,i)=>'### lens '+(i+1)+'\n'+a).join('\n\n') + '\n>>>', '',
  'The doc must contain: (1) HONEST FRAMING — what TurboQuant actually is (with the confidence level from lens 1; if it could not be pinned down, say so and proceed on the GENERAL methodology = quantize-to-shrink-for-cache-residency); (2) the PRECISION VERDICT — can a lossy method touch steppe at all, and WHERE (the sacred-vs-slack map): the honest answer is likely NO for the f2 values (§12) but YES for an approximate rotation-model-SCREEN that prunes candidates then exact-recomputes survivors — make this the central idea if the lenses support it; (3) THE L2 FIT — the measured L2 size and which structure (operand tile / f2 slab / fit pair-subset) a quantized version would make L2-resident, with the numbers; (4) THE EXPERIMENT — a concrete, minimal, measurable experiment: what to quantize, what to keep in L2, the throughput metric, AND the accuracy/parity gate (the exact-recompute must reproduce the §12 bit-identical result; the screen only changes WHICH models are recomputed, never the reported numbers), expected payoff, the failure modes, and a clear GO/NO-GO criterion; (5) BLUNT BOTTOM LINE — is this promising or a distraction, and how it ranks vs the known next levers (M5 streaming, the multi-GPU rotation fit). Do NOT oversell; if it is a distraction, say so.',
  'Return a tight 8-12 line executive summary: what TurboQuant is (+ confidence), whether/where it could fit in steppe given §12, the L2-residency target + numbers, the proposed experiment + its parity gate, and the blunt go/no-go vs the other levers.',
].join('\n'), { label: 'design:experiment', phase: 'Design' })

return { lenses, design }
