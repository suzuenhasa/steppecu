export const meta = {
  name: 'research-tgeno-at2',
  description: 'Settle the TGENO question definitively (the M(cli-4) extract-f2 golden-fail root cause). The v66 AADR .geno has magic "TGENO" (transposed/individual-major), NOT classic PACKEDANCESTRYMAP. The M(cli-4) verdict "proved" steppe decode wrong by having AT2 read that .geno via packedancestrymap_to_afs — but if AT2 does NOT support TGENO, AT2 was MISREADING it (the 0xFF->2 AND 0xFF->0 contradiction is the signature), so that root cause is invalid. AND the goldens (golden_fit0/rot/fitNA/...) were generated by AT2 extract_f2 on the SAME TGENO prefix — so their validity hinges on whether AT2 reads TGENO correctly. 3 parallel lenses: (A web) does admixtools/AT2 support the v66 TGENO format yet (repo/issues/releases/AADR v66 notes/timeline)? (B box+web) the AUTHORITATIVE TGENO byte-layout spec from the AADR v66 release artifacts on the box (aadr_v66.p1__README.docx, mtdna_uncompress_v66.p1.py at /workspace/data/aadr/1240k/, any build_tgeno_matrix.py oracle) + any Dataverse/AADR format doc; (C code) does steppe io decode (src/io/eigenstrat_format.*, geno_reader.cpp read_tile, src/core/internal/decode_af.hpp) match that authoritative TGENO layout? Synthesis -> the verdict: (1) AT2 TGENO support yes/no/version, (2) the authoritative TGENO spec, (3) is steppe decode correct vs spec, (4) are the goldens VALID or AT2-misread-corrupted, (5) the fix path. READ-ONLY: web + read box files + read steppe code. NO box compute, NO repo edits, NO git. Writes /tmp/tgeno-at2.md.',
  phases: [ { title: 'Research', detail: '3 parallel lenses: AT2-TGENO-support (web) / TGENO-spec (AADR python+README) / steppe-decode-vs-spec (code)' }, { title: 'Synthesize', detail: 'verdict: AT2 support? spec? steppe decode correct? goldens valid? fix path -> /tmp/tgeno-at2.md' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const OUT = '/tmp/tgeno-at2.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm; validated against AT2 (admixtools R, v2.0.10) goldens on REAL AADR v66. The raw genotype file /workspace/data/aadr/raw/v66.p1_HO.aadr.patch.PUB.geno has header magic "TGENO" (transposed/individual-major packed format, 27594 inds, 584131 SNPs) — this is a NEWER AADR format, NOT classic PACKEDANCESTRYMAP (SNP-major). steppe io::GenoReader::read_tile is built FOR TGENO. The .ind/.snp are standard.',
  'THE SITUATION: M(cli-4) `steppe extract-f2` on this .geno kept 391333 SNPs / weights [0.866,0.134]; the AT2 golden_fit0 (made by AT2 extract_f2 on the SAME prefix) has 500848 SNPs / [0.559,0.441]. The M(cli-4) verdict attributed this to a steppe decode bug — but its EVIDENCE was AT2 reading the TGENO file via packedancestrymap_to_afs / cpp_read_packedancestrymap (a low-level reader), which hit IMPOSSIBLE results (a 0xFF byte decoding to genotype 2 for one sample and 0 for another at the same SNP) — the classic signature of reading individual-major TGENO bytes AS IF SNP-major packed = transposed garbage. So the verdict mechanism is suspect. The user suspects AT2 does NOT support TGENO yet. CRUCIAL KNOCK-ON: every golden (golden_fit0/golden_rot/golden_fitNA/golden_qpwave/golden_fit1) was generated by AT2 extract_f2(pref="/workspace/data/aadr/raw/v66.p1_HO.aadr.patch.PUB", ...) reading THIS TGENO file — so if AT2 cannot read TGENO, the goldens themselves are built on an AT2 misread and steppe parity was measured against a corrupted reference.',
  'THE QUESTIONS TO SETTLE: (1) Does AT2/admixtools (any version incl GitHub dev) support the v66 "TGENO" transposed format yet — and if so since which version; if not, what does extract_f2/read_packedancestrymap DO when handed a TGENO file (error? silent misread?)? (2) What is the AUTHORITATIVE TGENO byte layout (header, individual-major transpose, 2-bit packing, bit order, missing/NA code, the n_ind/n_snp + stride) — from the AADR v66 release artifacts (the README.docx + mtdna_uncompress python on the box) and any AADR/Dataverse format doc? (3) Does steppe io decode MATCH that authoritative layout? (4) Therefore: is steppe decode correct, or are the goldens AT2-misread-corrupted, or both wrong? (5) The fix path.',
  'READ-ONLY: web research (ToolSearch select:WebSearch,WebFetch) + read box files via ' + SSH + ' (cat the .py; for the .docx unzip+extract word/document.xml text: `' + SSH + ' "cd /workspace/data/aadr/1240k && unzip -p aadr_v66.p1__README.docx word/document.xml | sed -e \\"s/<[^>]*>/ /g\\""`) + read steppe source. NO box compute, NO repo edits, NO git. Deliverable /tmp/tgeno-at2.md. Cite sources (URLs + the box file lines). No fabrication; flag uncertainty.',
].join('\n')

phase('Research')
const LENSES = [
  { key: 'at2-tgeno-support', focus: 'WEB: does ADMIXTOOLS 2 / the admixtools R package support the AADR v66 "TGENO" (transposed geno) format yet? Search the admixtools GitHub repo (uqrmaie1/admixtools) — issues, commits, NEWS/changelog, the read_packedancestrymap / packedancestrymap_to_afs / extract_f2 source — for "TGENO", "transposed", "tgeno", v66 format support. Search the AADR v66 release notes / the Reich lab / the Dataverse v66 page / countingchromosomes for the format change announcement (did v66 switch the .geno to a transposed/compressed format?) and whether downstream tools (admixtools, eigensoft/convertf) read it. Determine: (a) is TGENO a NEW v66 AADR format? (b) does AT2 read it (which version) or NOT? (c) if NOT, what happens when extract_f2 is handed a TGENO file — hard error, or silent misread-as-packedancestrymap? Cite repo/issue/release URLs + dates. This is the load-bearing fact.' },
  { key: 'tgeno-spec', focus: 'Establish the AUTHORITATIVE TGENO byte-layout from the AADR v66 release artifacts on the box. Read (via ssh box5090): /workspace/data/aadr/1240k/aadr_v66.p1__README.docx and aadr_v66.p1__README_MT.docx (unzip -p ...docx word/document.xml | strip tags) — these should document the v66 .geno format + how to read/convert it; /workspace/data/aadr/1240k/mtdna_uncompress_v66.p1.py (cat it — the AADR-provided decoder, reveals the compression/packing scheme + bit order); and find + read any build_tgeno_matrix.py / tgeno decoder on the box (find /workspace -iname "*tgeno*" -o -iname "build_tgeno*"). Also web-search AADR "TGENO" / "transposed geno" / v66 format documentation. EXTRACT the exact layout: the header (magic "TGENO", how n_ind/n_snp are stored, header size), individual-major vs SNP-major, the 2-bit genotype packing (which bits, MSB-first or LSB-first), the missing/NA code, the per-record stride, and how AADR says to decode/convert it to EIGENSTRAT. Cite the README lines + the python code.' },
  { key: 'steppe-decode-vs-spec', focus: 'Read steppe io decode + compare to the TGENO format. Read src/io/eigenstrat_format.{hpp,cpp} (the TGENO header parse + format detection), src/io/geno_reader.cpp (read_tile: the per-record seek `header_bytes + row*bytes_per_record`, the byte/bit extraction), src/core/internal/decode_af.hpp (the 2-bit code -> Q/V/N + missing handling + ploidy/pseudo-haploid), and any on-box build_tgeno_matrix.py oracle steppe was validated against. Document steppe EXACT decode assumptions: header_bytes, bytes_per_record, individual-major mapping (.ind row i <-> record i), the 2-bit code values (which code = missing, which = hom/het), bit order. Flag the SPECIFIC assumptions that must be checked against the authoritative spec (from lens B): bit order (MSB vs LSB), the missing code, the transpose, the header size, pseudo-haploid. Note steppe was validated against build_tgeno_matrix.py (its own oracle) not AT2 — so if BOTH share an assumption, a shared error would be invisible. Cite file:line.' },
]
const found = await parallel(LENSES.map(L => () => agent([
  'You are a bioinformatics-format + tooling researcher settling the TGENO/AT2 question for steppe, HONEST + precise: ' + L.focus, '', STD,
  '', 'Use web tools (ToolSearch select:WebSearch,WebFetch) and/or read the box files (' + SSH + ') and steppe source as your lens requires. Cite real URLs / file:line. Return your findings + a clear per-lens conclusion. Flag uncertainty explicitly; NO fabrication.',
].join('\n'), { label: 'research:' + L.key, phase: 'Research' })))

phase('Synthesize')
const report = await agent([
  'You are the lead synthesizer settling the TGENO/AT2 question for steppe. Combine the 3 lenses into a definitive verdict + fix path.', '', STD,
  '', 'THE LENSES:\n' + found.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'WRITE ' + OUT + ' (/tmp, NOT the repo) with: (1) DOES AT2 SUPPORT v66 TGENO? — yes/no, since which version, and (if no) what extract_f2 DOES with a TGENO file (error vs silent misread) — cited. (2) THE AUTHORITATIVE TGENO SPEC — the exact byte layout (header/magic/n_ind/n_snp, individual-major, 2-bit packing, bit order, missing code, stride), from the AADR python/README. (3) IS STEPPE DECODE CORRECT vs that spec? (point-by-point: header_bytes, transpose, bit order, missing code — match or mismatch, file:line). (4) THE VERDICT ON THE DISCREPANCY — which of these is true: [a] steppe decode bug; [b] the GOLDENS are AT2-misread-corrupted (AT2 cannot read TGENO) and steppe is right; [c] a missingness-criterion difference; [d] still ambiguous — with the EVIDENCE. (5) ARE THE GOLDENS VALID? — the critical knock-on: if AT2 cannot read TGENO, every golden made from this prefix is suspect and must be regenerated from a format AT2 supports (eigenstrat/packedancestrymap). (6) THE FIX PATH — concrete (e.g. convert the v66 TGENO -> packedancestrymap with the AADR python/convertf and regenerate the goldens; or fix steppe decode; or both) + the cheapest decisive next experiment to confirm. Use Write. Return the headline (AT2 supports TGENO? + which of a/b/c/d + are the goldens valid) so the user sees it at a glance. Cite sources; no fabrication.',
].join('\n'), { label: 'synthesize:tgeno', phase: 'Synthesize' })
log('tgeno-at2 research -> /tmp/tgeno-at2.md: ' + String(report).slice(0, 200))
return { report }
