export const meta = {
  name: 'qpgraph-build',
  description: 'BUILD qpGraph single-graph fit (STEP 3, tool 2 of 3 — the roadmap, per docs/research/qpgraph-gpu-design.md + the SETTLED optimizer decision 932d108). qpGraph = fit an admixture-graph topology to the observed f-statistics: given a graph (drift edges >=0, admixture nodes with mixture weights in [0,1]) + the f2 data, find the edge lengths + mixture proportions minimizing the GLS residual ||f_obs - f_pred(theta)||^2_{Qinv} under the block-jackknife covariance; report the fitted weights, the fit score/chisq, worst-f-stat residual, + block-jackknife SE. SCOPE THIS STEP: SINGLE-GRAPH FIT given a fixed topology (topology SEARCH over graph space is DEFERRED to a later step, per the roadmap). HEAVILY REUSE WHAT WE BUILT (verified, no parallel bolt-on): (1) the OPTIMIZER SPIKE tests/reference/bench_optimizers.cu — it already has the DE-RISKED IDEA-1 batched-sequential fleet (projected-gradient/L-BFGS-B per (graph,restart) thread, multistart, on-device, GPU-bound — the SETTLED qpGraph optimizer) AND a path-algebra prototype (fill_pwts/ppwts mapping theta -> predicted f-stats for nadmix=1/2/3) AND the GLS objective (dev_chisq_of_core) AND the host-pinned-optimum oracle machinery — PRODUCTIZE this, do NOT reinvent; (2) the f2 cache F2BlockTensor + read_f2_dir + DeviceF2Blocks (the f2 INPUT; qpgraph takes an f2 object, so the committed 9-pop fixture works — NO genotype-regime issue); (3) the f-stat basis (the outgroup-f3 identity assemble_f3_triples, qpadm_fit_kernels.cu:663, the SAME the spike used); (4) jackknife_cov (backend.hpp:791) for the GLS Qinv + the block-jackknife SE; (5) small_linalg.hpp as the CpuBackend oracle; (6) the CLI/binding scaffold. NEW = the general graph/topology DATA MODEL (parse an edge-list/admixture-graph -> the path-weight matrix generalizing the spike fixed-nadmix fill_pwts) + run_qpgraph (the production fit entry) + cmd_qpgraph + the binding. REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND (the fleet optimizer + the GLS objective ON-DEVICE, the f-stat basis + Qinv RESIDENT, the host NEVER calls back a host objective per iteration — the CPU-bound trap the design doc designed out; reformulate, do NOT transliterate the AT2 host optim() loop); VERIFY (real code file:line + admixtools qpgraph source); REUSE no parallel bolt-on; EmulatedFp64{40} default (the matmul-heavy objective evals: the path-weight GEMM + the GLS quadratic form) + native carve-out (cancellation / the SPD solve); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+ (verify cuSOLVER/cuBLAS/CUB vs docs); KEEP ON-DEVICE no warts/bouncing. ORACLE = admixtools-R qpgraph() (the project standard; no-AT2 lifted for golden GENERATION only): regenerate a fixture-matched golden via admixtools::qpgraph(read_f2(the 9-pop fixture f2 == f2_fit0_9pop.bin), a FIXED well-identified topology on the 9 pops) -> the converged fit (edge weights + score/chisq + worst residual); steppe`s fleet fit reaches the SAME minimum (weights + score) at the tier. DELIVER: run_qpgraph + `steppe qpgraph` CLI + steppe.qpgraph binding + a CpuBackend oracle + the golden gate. Discipline: design+regen-golden (GATE) -> implement (CpuBackend oracle fleet first, then on-device) -> verify (golden + GPU-bound fleet + emulated + ctest) -> commit. FAIL-PROTOCOL: verify->commit on green; on BAD HALT + report + defer (NEVER silently fail/revert); on failure git stash push -u + HALT.',
  phases: [ { title: 'Design + regen the qpgraph golden (GATE: topology model + fleet reuse + oracle)' }, { title: 'Implement run_qpgraph (productize the spike fleet + path algebra) + CLI + binding + build' }, { title: 'Verify golden (weights+score) + GPU-bound fleet + emulated + commit-or-HALT' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const PULL = 'rsync -az -e ssh box5090:/workspace/steppe/tests/reference/goldens/at2/ ' + R + '/tests/reference/goldens/at2/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -25; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ f32ce89. BUILT: the fit engine + f4/f3/f4-ratio/qpDstat + the sweep + bindings + the wheel + qpfstats (dd8f99a). The OPTIMIZER SPIKE (tests/reference/bench_optimizers.cu, comparison docs) already DE-RISKED + prototyped the qpGraph optimizer; the SETTLED decision (932d108, docs/research/optimizer-comparison.md + qpgraph-gpu-design.md) is the IDEA-1 batched-sequential fleet (CMA-ES IDEA-2 PARKED).',
  'qpGraph = fit an admixture graph to the observed f-statistics. INPUT: an f2 object (read_f2 — the committed 9-pop fixture WORKS; qpgraph is f2-path, NOT genotype) + a graph topology (drift edges >=0, admixture nodes, mixture weights in [0,1]). FIT: minimize the GLS residual ||f_obs - f_pred(theta)||^2_{Qinv} over theta (the mixture weights; the inner edge lengths by the GLS-optimal solve), Qinv = the f-stat block-jackknife covariance. OUTPUT: fitted edge lengths + mixture proportions + the fit score/chisq + worst-f-stat residual z + block-jackknife SE. SCOPE: SINGLE-GRAPH FIT (a fixed topology); topology SEARCH is DEFERRED.',
  'HEAVILY REUSE (verify file:line, NO parallel bolt-on): (1) tests/reference/bench_optimizers.cu — the IDEA-1 fleet (one thread per (graph,restart), projected-gradient/L-BFGS-B, multistart, on-device) + the path-algebra prototype (fill_pwts/ppwts: theta -> ppwts_2d [npair x nedge] -> f_pred) + the GLS objective (dev_chisq_of_core, qpadm_fit_kernels.cu:370) + the host-pinned-optimum oracle. PRODUCTIZE this into src/ (the spike proved the shape; lift + generalize, do NOT reinvent). (2) the f2 cache F2BlockTensor + read_f2_dir + DeviceF2Blocks. (3) the outgroup-f3 basis assemble_f3_triples (qpadm_fit_kernels.cu:663). (4) jackknife_cov (backend.hpp:791) — the GLS Qinv + the SE. (5) small_linalg.hpp — the CpuBackend oracle. (6) the cuSOLVER/cuBLAS batched primitives (potrfBatched/gemmStridedBatched) + the solve/precision seam. (7) the CLI flag helpers + cmd_* pattern + the nanobind module.',
  'NEW (the only genuinely-new code): the general graph/topology DATA MODEL — parse an admixture-graph spec (edge list + admixture nodes, the AT2 graph format) into the path-weight mapping that GENERALIZES the spike fixed-nadmix fill_pwts to an arbitrary topology (the leaf->root path products through admixture nodes); + run_qpgraph (the production fit entry wiring the model -> the resident basis -> the fleet -> the result); + cmd_qpgraph + the binding.',
  'STANDING REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND — the fleet optimizer + the GLS objective evals ON-DEVICE, the f-stat basis + Qinv RESIDENT, the host NEVER calls a host objective per optimizer iteration (the AT2 optim() host-loop CPU-bound trap, designed out in qpgraph-gpu-design.md §0); VERIFY (file:line + admixtools qpgraph source — the box has the AdmixTools C qpGraph.c + admixtools-R qpgraph); REUSE no parallel bolt-on; EmulatedFp64{40} default (the path-weight GEMM + the GLS quadratic form) + native carve-out (cancellation / the SPD edge solve); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+ (verify vs docs); KEEP ON-DEVICE no warts/bouncing.',
  'ORACLE = admixtools-R qpgraph() (the project standard; no-AT2 lifted for golden GENERATION only). On box5090: pick a FIXED, well-identified admixture-graph topology on the 9 fit0 pops (e.g. the spike topology: base=Mbuti, sources Turkey_N/Iran_GanjDareh_N, an admixed CordedWare — a clean non-degenerate fit), run admixtools::qpgraph(read_f2(the 9-pop fixture f2), the graph) -> the converged fit (edge weights + score/chisq + worst residual) -> golden_qpgraph_fit. steppe`s fleet fit over the SAME f2 + topology reaches the SAME minimum (weights + score) at the tier. Note: the golden is the CONVERGED OPTIMUM (any sound optimizer reaching the same minimum matches; do NOT require step-identical L-BFGS-B). If the AT2 qpgraph fit is non-identifiable/degenerate on the chosen topology (multiple optima), pick a better-identified topology OR HALT + defer (do NOT gate on a degenerate fit).',
  'FAIL-PROTOCOL (USER-MANDATED, STRICT): NEVER git checkout -- . / git clean -fd. verify->commit ONLY on green. On a BAD result (golden mismatch beyond tier, CPU-bound optimizer, structural blocker, non-identifiable oracle) HALT + report + DEFER — do NOT silently fail, do NOT revert without asking. On any failure ' + STASH + ' "wip:qpgraph-FAILED-<reason>" + HALT.',
  'SINGLE-GPU --device 0. RELEASE build-rel. REAL AADR; no synthetic. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering (cmd/binding TUs CUDA-free; kernels in .cu). nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); for (let i=0;i<2&&r===null;i++){ log(opts.label+': transient null/500 — retry '+(i+1)); r = await agent(p, {...opts, label: opts.label+':retry'+(i+1)}) } return r }

phase('Design + regen the qpgraph golden (GATE: topology model + fleet reuse + oracle)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','reuse_spike_fleet','topology_model','gpu_bound_fit','oracle_golden','identifiability_check','emulated_plan','cuda13_apis','design','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff single-graph qpGraph is buildable GPU-bound by PRODUCTIZING the spike fleet + path algebra + the f2 cache + GLS, with a reproducible, WELL-IDENTIFIED admixtools-R qpgraph golden. false -> HALT + defer' },
    reuse_spike_fleet: { type: 'string', description: 'EXACTLY how the bench_optimizers.cu IDEA-1 fleet + path-algebra prototype + GLS objective are productized (file:line) — confirm reuse not reinvent' },
    topology_model: { type: 'string', description: 'the general graph/topology data model: how an admixture-graph spec parses to the path-weight mapping generalizing the spike fixed-nadmix fill_pwts' },
    gpu_bound_fit: { type: 'string', description: 'the GPU-bound fit: the fleet + GLS objective on-device, basis+Qinv resident, NO host objective per iteration (the designed-out CPU trap)' },
    oracle_golden: { type: 'string', description: 'the regenerated golden_qpgraph_fit (path + the chosen topology + the converged weights/score) via admixtools-R qpgraph(read_f2(fixture), graph)' },
    identifiability_check: { type: 'string', description: 'PROOF the chosen topology is well-identified on the fixture f2 (a unique optimum; the AT2 fit is stable/reproducible) — NOT degenerate' },
    emulated_plan: { type: 'string', description: 'EmulatedFp64{40} (the path GEMM + GLS quadratic form) vs native carve-out (cancellation / SPD edge solve)' },
    cuda13_apis: { type: 'string', description: 'the cuSOLVER/cuBLAS/CUB APIs VERIFIED vs CUDA 13.x docs' },
    design: { type: 'string', description: 'the run_qpgraph plan (model -> resident basis -> fleet -> result; CLI + binding)' },
    blocker: { type: 'string', description: 'if NOT feasible: the structural blocker / non-identifiability + options (HALT + defer)' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing qpGraph single-graph fit GPU-first + regenerating its golden (verify-before-implement; NO steppe code changes). FIRST confirm the reuse: READ tests/reference/bench_optimizers.cu (the IDEA-1 fleet + the path-algebra prototype fill_pwts/ppwts + the GLS objective + the host-pinned optimum) + docs/research/qpgraph-gpu-design.md + optimizer-comparison.md (the SETTLED fleet decision). READ the admixtools qpgraph method (web github uqrmaie1/admixtools qpgraph + the box AdmixTools C qpGraph.c) — esp. the graph format + how f_pred is built from the topology + the score. READ the reuse seams: F2BlockTensor/read_f2_dir/DeviceF2Blocks, assemble_f3_triples (qpadm_fit_kernels.cu:663), jackknife_cov (backend.hpp:791), small_linalg.hpp, the cuSOLVER batched primitives, the CLI/binding scaffold. VERIFY the cuSOLVER/cuBLAS/CUB APIs vs the CUDA 13.x docs. On box5090 (AT2/R, lifted no-AT2 for GENERATION): pick a FIXED well-identified topology on the 9 fit0 pops, run admixtools::qpgraph(read_f2(the 9-pop fixture), the graph) -> golden_qpgraph_fit (weights + score); PROVE it is well-identified + reproducible; pull ' + PULL + ' . Design run_qpgraph GPU-bound (productize the spike fleet, no host objective loop). Return the structured design. If single-graph qpGraph cannot be GPU-bound by reuse OR no well-identified topology gives a reproducible AT2 golden, feasible=false + blocker (HALT + defer, do NOT guess).', STD,
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:qpgraph', phase: 'Design + regen the qpgraph golden (GATE: topology model + fleet reuse + oracle)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- qpGraph STRUCTURAL/non-identifiable (HALT + defer): ' + design.blocker); return { halted: true, deferred: true, design } }
log('qpGraph feasible (productizes the spike fleet); golden: ' + String(design.identifiability_check).slice(0,120))

phase('Implement run_qpgraph (productize the spike fleet + path algebra) + CLI + binding + build')
const fixer = await tryAgent([
  'You are implementing qpGraph single-graph fit per this design (PRODUCTIZE the spike fleet + path algebra; GPU-bound; emulated-first; on-device; reuse-no-bolt-on):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced).', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT: a CpuBackend oracle run_qpgraph (small_linalg, native — the reference fleet/objective) FIRST, then the GPU-bound path: PRODUCTIZE the bench_optimizers.cu IDEA-1 fleet + path-algebra into src/ as the production optimizer + the general topology data model (parse a graph spec -> the path-weight mapping); wire the resident f-stat basis (assemble_f3_triples) + Qinv (jackknife_cov) -> the on-device fleet (NO host objective per iteration) -> the fitted weights + score + worst residual + block-jackknife SE; emulated-FP64 for the matmul evals + native carve-out. Add a `steppe qpgraph` CLI (--f2-dir + --graph -> the fit) + a steppe.qpgraph binding. Gate: a cli_qpgraph ctest + a pytest reproducing golden_qpgraph_fit (weights + score) at the tier. Build + full STEPPE_THOROUGH ctest. SANITY: cli_qpgraph reproduces the golden weights/score; CpuBackend==CudaBackend; the fleet converges to the AT2 optimum; existing goldens/ctest green. Report files, the spike-fleet REUSE (file:line), the GPU-bound fit (no host objective loop), the gate, the FULL ctest. Do NOT commit. BAD/NON-trivial blocker -> STOP + report (do NOT revert).',
].join('\n'), { label: 'implement:qpgraph', phase: 'Implement run_qpgraph (productize the spike fleet + path algebra) + CLI + binding + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for qpGraph. Accumulated edits (do NOT clean/revert/destroy; git stash only if forced). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON) + green ctest, patching only trivial -Werror / CMake / cuSOLVER-cuBLAS-CUB API misuse (VERIFY vs CUDA 13 docs). DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 6x on trivial errors. NON-trivial -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement run_qpgraph (productize the spike fleet + path algebra) + CLI + binding + build' })

phase('Verify golden (weights+score) + GPU-bound fleet + emulated + commit-or-HALT')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','golden_reproduced','reuses_spike_fleet','gpu_bound','no_host_objective_loop','emulated_fp64','goldens_green','build_clean','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if steppe qpgraph reproduces the admixtools-R qpgraph golden (weights + score) at the tier (cli + pytest), PRODUCTIZES the spike fleet (not reinvented), the fit is GPU-bound (fleet + objective on-device, NO host objective per iteration), emulated-FP64 default, full ctest green, single-GPU, no synthetic, CpuBackend==CudaBackend' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (HALT + defer)' },
    golden_reproduced: { type: 'boolean', description: 'the fitted weights + score match the AT2 qpgraph converged optimum at the tier' },
    reuses_spike_fleet: { type: 'boolean', description: 'productizes the bench_optimizers.cu IDEA-1 fleet + path algebra, not a parallel rebuild' },
    gpu_bound: { type: 'boolean' }, no_host_objective_loop: { type: 'boolean', description: 'verified: the host does NOT call the objective per optimizer iteration (the designed-out CPU trap)' },
    emulated_fp64: { type: 'boolean' }, goldens_green: { type: 'boolean' }, build_clean: { type: 'boolean' },
    commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the golden reproduction (weights+score) + the fleet reuse + the GPU-bound fit; on FAIL the blocker + minor-vs-bad' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for qpGraph (adversarial; hold the bars: PRODUCTIZES the spike fleet [not reinvented], GPU-bound [NO host objective per iteration], emulated-first, on-device, well-identified golden). design:\n<<<\n' + JSON.stringify(design) + '\n>>>\nimplementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — run_qpgraph PRODUCTIZES the bench_optimizers.cu fleet + path algebra (not a parallel rebuild); the fit runs the fleet + GLS objective ON-DEVICE with the basis+Qinv resident (NO host objective per optimizer iteration); emulated-FP64 + native carve-out; CpuBackend oracle present. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; cli_qpgraph + pytest reproduce the admixtools-R qpgraph golden (fitted weights + score) at the tier; CpuBackend==CudaBackend; the fleet converges to the AT2 optimum. (3) GPU-bound, single-GPU, no synthetic. PASS only if all hold. ',
  'ON PASS: cd ' + R + ' && git add ONLY the new golden + new/changed source/test/cmake/binding/doc (NEVER git add dot; never aadr/ atlas_results/), commit (STEP3 qpGraph: single-graph fit `steppe qpgraph` + steppe.qpgraph binding — productizes the de-risked IDEA-1 fleet + path algebra [932d108], GPU-bound on-device fit [no host objective loop], emulated-FP64; reproduces the admixtools-R qpgraph converged optimum; topology search deferred) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs (RESUME/TODO: qpGraph single-graph fit done; next = DATES; topology search deferred). ',
  'ON BAD: DO NOT git checkout/clean, DO NOT revert. ' + STASH + ' "wip:qpgraph-FAILED" (capture the ref). Classify fail_severity=bad + the blocker for the user. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:qpgraph', phase: 'Verify golden (weights+score) + GPU-bound fleet + emulated + commit-or-HALT' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ qpGraph ' + verdict.commit_hash + ' — ' + verdict.note)
else log('--- qpGraph FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — HALT+defer — ' + verdict.note)
return { design, verdict }
