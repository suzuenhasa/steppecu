export const meta = {
  name: 'fit-precision-policy',
  description: 'Make the qpAdm FIT use the SAME precision policy as the f2 precompute: DEFAULT EmulatedFp64{40}, FALLBACK native — use the default for ALL stages unless the stage genuinely cannot honor it. Today the fit is native FP64 EVERYWHERE (qpadm_fit.cpp hardcodes Precision::Kind::Fp64; the covariance SYRK does (void)precision + native). f2 instead defaults emulated and falls back via emulation_honorable, with a fixed native carve-out for the catastrophic-cancellation numerator. This fix-pass mirrors that: (1) the qpAdm entrypoint DEFAULTS to EmulatedFp64{kDefaultMantissaBits}; (2) the well-conditioned covariance SYRK (xtau*xtau^T, cublasDsyrk) ENGAGES that precision via engage_f2_precision/emulation_honorable (default emulated, fallback native) instead of hardcoded native; (3) the cancellation-sensitive ops (f4 4-slab combine, xtau centering) STAY native (the carve-out, same as f2 numerator); (4) the cuSOLVER solves STAY native (they FALL BACK — cuSOLVER 13.0/12.0 has NO FP64-emulated mode; the d6d3cbb seam auto-promotes later); (5) result.precision_tag reflects what ACTUALLY ran. GATE: the af6a8c2 golden STILL passes on the GPU with the SYRK now emulated{40} (weights rtol~1e-6, chisq, dof, p) + GPU-vs-CPU-oracle seam holds + build/ctest green. This yields the FIRST real emulated-vs-golden measurement in the fit (the cuSOLVER probe degraded to native, so was uninformative). Fixer on box5090 -> INDEPENDENT verdict. commit-green / revert+HALT.',
  phases: [
    { title: 'Implement', detail: 'default the fit to EmulatedFp64{40}; engage it on the covariance SYRK (fallback native); keep cancellation + solves native; honest precision_tag; dev-loop on box5090' },
    { title: 'Verify', detail: 'independent verdict: SYRK genuinely emulated, golden STILL passes on GPU, GPU-vs-CPU-oracle seam holds, carve-outs/solves still native, build/ctest green; commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -30 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const QPTEST = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ctest --test-dir build-rel -R qpadm --output-on-failure 2>&1 | tail -70'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120) reimplementation of ADMIXTOOLS 2. Phase-2 qpAdm fit engine. Branch phase2-fit-engine @ d6d3cbb. M(fit-4) landed the qpAdm fit ON THE GPU (CUDA backend) validated vs the af6a8c2 AT2 golden (weights 0.5589/0.4411, chisq 4.635, dof 4, p 0.3268). d6d3cbb added the cuSOLVER solve PROMOTION SEAM (default native; auto-promotes when a future toolkit exposes FP64-emulated cuSOLVER).',
  'WHY (the user, explicit): "take the same policy with f4/etc as we do with f2 re: fp64 — there should be a default and a fallback; use the default for all things unless it falls back." f2 (the precompute) DEFAULTS to EmulatedFp64{40} (Ozaki, ~8-17x over native FP64 at ~2.2e-11 accuracy) and FALLS BACK to native via the emulation_honorable predicate, with a FIXED native carve-out for the catastrophic-cancellation numerator. The FIT, by contrast, is currently native FP64 EVERYWHERE: qpadm_fit.cpp hardcodes `const Precision prec{Precision::Kind::Fp64}` (lines ~88/139/151) and `precision_tag = Fp64` (~80); the covariance SYRK in jackknife_cov does `(void)precision;` + native. Make the fit MIRROR f2.',
  'WHAT TO CHANGE (additive policy unification; the golden parity MUST still hold): (1) src/core/qpadm/qpadm_fit.cpp — change the default Precision the entrypoint passes to the backend virtuals from `Precision::Kind::Fp64` to `Precision{Precision::Kind::EmulatedFp64, steppe::kDefaultMantissaBits}` (the SAME default f2 uses; include steppe/config.hpp for kDefaultMantissaBits). The DEFAULT for all fit stages is now emulated; stages that cannot honor it fall back internally (below). (2) src/device/cuda/cuda_backend.cu jackknife_cov — the well-conditioned covariance SYRK (`cublasDsyrk` for Q = xtau*xtau^T) must ENGAGE the passed `precision` via the SAME `engage_f2_precision(blas_.get(), precision)` + `emulation_honorable` path the f2 GEMMs use (handles.hpp / f2_block_kernel.cuh) — default emulated{40}, automatic native fallback if not honorable. Remove the `(void)precision;` for the SYRK; the precision now governs the SYRK. (Restore the cuBLAS math mode after the SYRK if needed for the determinism/workspace contract — follow the existing f2 scoped pattern.) (3) The CANCELLATION-sensitive ops STAY native ALWAYS, regardless of `precision` — the f4 4-slab combine (assemble_f4, the §OQ-5 `(void)precision; // native FP64` site ~1229) and the xtau centering kernel — exactly the f2-numerator carve-out (emulation faithfully computes a product but cannot recover bits annihilated by a prior subtraction). Leave these native and KEEP/clarify the carve-out comment. (4) The cuSOLVER SOLVES (potrf/potri SPD inverse, any gesvd) STAY native — they FALL BACK because cuSOLVER 13.0/12.0 has NO FP64-emulated mode (the d6d3cbb seam already handles future promotion via solve_precision_; do NOT change that default). (5) result.precision_tag (qpadm_fit.cpp ~80) must reflect what ACTUALLY ran: if the SYRK ran emulated (honorable) tag EmulatedFp64, else Fp64 — query the backend honorability (the caps.emulated_fp64_honorable probe already exists) rather than hardcoding Fp64.',
  'PARITY (§12, the GATE): with the covariance SYRK now EmulatedFp64{40} by default, the af6a8c2 golden MUST STILL pass on the GPU within tier — weights rtol~1e-6 to 0.5589062489/0.4410937511, chisq 4.6352 rtol~1e-6, dof=4 exact, p 0.3268 rtol~1e-3. The SYRK is well-conditioned (accumulate-many), and emulated{40} is ~2.2e-11 (the f2 precompute proves this), so it WILL hold — but VERIFY, do not assume. ALSO confirm the GPU(emulated)-vs-CpuBackend(native oracle) seam still holds tight (the CpuBackend ignores precision => always native => it is the native oracle the emulated GPU path is diffed against). REPORT the REAL per-quantity emulated-SYRK-vs-golden AND emulated-vs-native-oracle deltas (this is the first informative emulated measurement in the fit; the d6d3cbb cuSOLVER probe degraded to native and was uninformative). If the SYRK-emulated golden delta EXCEEDS tier, that is a real finding — report it; do NOT loosen the tier to pass.',
  'NOTE ON SCOPE/PERF (be honest): at SINGLE-MODEL scale this is a POLICY/consistency change, not a speedup — the SYRK is tiny (xtau is n_block x m = 708 x 10), so emulation gives ~no single-model benefit (possibly tiny overhead). The payoff is (a) the fit policy now MATCHES f2 (one policy, not two), and (b) S8 INHERITS it — when the SYRK is batched across thousands of rotation models it is already on the tensor-core emulated path. Do NOT claim a single-model speedup.',
  'BOX = box5090 (vast 2x RTX 5090, UP, sm_120, CUDA 13). ' + SSH + ' (alias); nvcc -> ' + PATHENV + ' . build-rel exists. RELEASE only. NOTHING builds locally. Golden fixture + AADR on the box.',
  'KEY FILES: src/core/qpadm/qpadm_fit.cpp (the entrypoint default precision + precision_tag), src/device/cuda/cuda_backend.cu (jackknife_cov SYRK engage; assemble_f4 carve-out stays native), src/device/cuda/f2_block_kernel.cuh + handles.hpp (engage_f2_precision/emulation_honorable/MathModeScope — REUSE), tests/reference/test_qpadm_parity.cu (confirm the default path is now emulated-SYRK and STILL matches the golden + the native oracle; report deltas), docs/design/fit-engine.md §1.4 (update the SYRK row / note: the fit now DEFAULTS emulated like f2, falls back native; reflects what landed).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); the qpAdm test (' + QPTEST + '). Iterate until: build clean (warnings-as-errors), full ctest green, the qpAdm parity test STILL passes vs the af6a8c2 golden with the SYRK now emulated{40} by default, and the GPU-vs-native-oracle seam holds. Do NOT commit (the verdict commits). Clean revert: ' + CLEAN + '.'

phase('Implement')
const impl = await agent([
  'You are a senior CUDA engineer unifying the fit precision policy with the f2 precompute policy (default EmulatedFp64{40}, fallback native, cancellation carve-out native). EDIT locally + dev-loop on box5090; do NOT commit. The golden parity MUST hold with the SYRK now emulated by default.', STD, '', DEVLOOP, '',
  'STEPS: (1) read the f2 policy: engage_f2_precision + emulation_honorable (f2_block_kernel.cuh / .cu) + the cuBLAS MathModeScope (handles.hpp) + how run_f2_blocks_resident engages precision on its GEMMs. (2) flip the qpadm_fit.cpp default precision to EmulatedFp64{kDefaultMantissaBits} and make precision_tag honest. (3) engage `precision` on the covariance SYRK in jackknife_cov (mirror the f2 GEMM engage + scope-restore); keep the f4 combine + xtau centering native (carve-out); keep the cuSOLVER solves native (fallback). (4) build Release + full ctest; run the qpadm parity test; capture the REAL emulated-SYRK-vs-golden + emulated-vs-native-oracle deltas. (5) update fit-engine.md §1.4 to reflect the landed policy. Iterate to green.',
  'Report: git diff --stat; the SYRK engage code (showing it now uses `precision`/emulation_honorable, with restore); PROOF the golden STILL passes on the GPU with emulated SYRK (the qpadm parity output, weights 0.5589/0.4411 etc., per-quantity deltas); the GPU(emulated)-vs-CPU(native oracle) seam deltas; build/ctest status; the precision_tag now reported (EmulatedFp64 when honorable); the §1.4 doc diff. Do NOT commit. Do NOT loosen any tier. If emulated SYRK breaks tier, report the real deltas honestly.',
].join('\n'), { label: 'impl:fit-precision-policy', phase: 'Implement' })

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','syrk_now_emulated','golden_still_passes','oracle_seam_holds','carveouts_and_solves_native','tag_is_honest','build_green','emulated_syrk_measurement','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if ALL: the covariance SYRK genuinely DEFAULTS to EmulatedFp64{40} via engage_f2_precision/emulation_honorable (not a no-op) + the af6a8c2 golden STILL passes on the GPU within tier with the SYRK emulated (weights rtol~1e-6, chisq, dof=4, p) + the GPU(emulated)-vs-CpuBackend(native oracle) seam holds + the cancellation ops + cuSOLVER solves remain native + precision_tag is honest (EmulatedFp64 when honorable) + Release build clean + full ctest green. NO tier was loosened.' },
    syrk_now_emulated: { type: 'boolean', description: 'you re-read the diff: the SYRK uses the passed precision via engage_f2_precision/emulation_honorable (default emulated{40}), with the cuBLAS math-mode restore — a real engage, not (void)precision' },
    golden_still_passes: { type: 'boolean', description: 'the af6a8c2 golden passes on the GPU with the emulated SYRK default (you re-ran), within the UNCHANGED tiers' },
    oracle_seam_holds: { type: 'boolean', description: 'GPU(emulated) vs CpuBackend(native oracle) still agree at the X/Q/w/chisq seams within tight tolerance' },
    carveouts_and_solves_native: { type: 'boolean', description: 'the f4 4-slab combine + xtau centering stay native (carve-out) and the cuSOLVER solves stay native (fallback) — you confirmed in the diff' },
    tag_is_honest: { type: 'boolean', description: 'result.precision_tag reflects what actually ran (EmulatedFp64 when honorable), not hardcoded Fp64' },
    build_green: { type: 'boolean', description: 'Release build clean (warnings-as-errors) + full ctest green (you re-ran)' },
    emulated_syrk_measurement: { type: 'string', description: 'the REAL per-quantity emulated-SYRK-vs-golden AND emulated-vs-native-oracle deltas (the first informative emulated measurement in the fit)' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'what landed; the emulated-SYRK parity result; for FAIL exactly what broke or diverged (and by how much)' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (adversarial — prior fix-passes have shammed; the user wants the fit precision policy to GENUINELY match f2: default emulated, fallback native). The implementer reported:\n<<<\n' + (impl || '(impl died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself: (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM: the covariance SYRK now ENGAGES the passed precision (default emulated{40}) via engage_f2_precision/emulation_honorable WITH the math-mode restore (a REAL engage, NOT (void)precision, NOT a no-op); the qpadm entrypoint default precision is EmulatedFp64{kDefaultMantissaBits}; the f4 combine + xtau centering STAY native (carve-out); the cuSOLVER solves STAY native (fallback); precision_tag is honest. CONFIRM no tier/tolerance was loosened to pass. (2) RE-RUN yourself: ' + BUILD + ' ; ' + QPTEST + ' . (3) PASS only if ALL gates hold: SYRK genuinely emulated by default; the af6a8c2 golden STILL passes on the GPU within the UNCHANGED tiers; the GPU-vs-native-oracle seam holds; carve-outs + solves native; tag honest; build clean; ctest green. \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source+test+doc files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (the fit now uses f2\'s precision policy: default EmulatedFp64{40} + native fallback, cancellation carve-out + cuSOLVER solves native; golden parity preserved; + the emulated-SYRK measurement) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly what broke or diverged.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:fit-precision-policy', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ FIT PRECISION POLICY UNIFIED ' + verdict.commit_hash + ' — syrk_emulated=' + verdict.syrk_now_emulated + ' golden_holds=' + verdict.golden_still_passes + ' oracle_seam=' + verdict.oracle_seam_holds + ' | ' + verdict.emulated_syrk_measurement)
else log('--- FIT PRECISION POLICY FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { impl, verdict }
