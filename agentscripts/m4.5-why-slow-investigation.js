export const meta = {
  name: 'm4.5-why-slow-investigation',
  description: 'DEFINITIVELY answer why M4.5 multi-GPU (G==2) is SLOWER than single-GPU (G==1) at P=768 (3471ms vs 2416ms, 0.70x) — as an ARCHITECTURE flaw, not "physics". Parallel read-only architecture map (orchestrator/threading + per-device compute + both combine paths + the bench harness) + an on-box nsys per-phase profile of G1 vs G2 (reusing the existing build-rel — NO rebuild) to get a hard ms breakdown AND whether the two device streams actually overlap, then an adversarial synthesis naming the exact flaw + the corrected design. Writes docs/cleanup/m4.5/why-multigpu-slow.md.',
  phases: [
    { title: 'Read', detail: 'parallel read-only: map the real multi-GPU data flow + memory traffic, file:line' },
    { title: 'Measure', detail: 'on-box nsys profile G1 vs G2 @ P=768 — per-phase ms + stream overlap (reuse build-rel)' },
    { title: 'Diagnose', detail: 'adversarial synthesis: the exact architectural flaw + the corrected design -> doc' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh rtxbox'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'

const STD = [
  'PROJECT: steppe = CUDA-13/Blackwell (sm_120) reimpl of ADMIXTOOLS 2 f-statistics, branch m4.5-multigpu. M4.5 = single-node multi-GPU f2 precompute. It is CORRECT + bit-identical to single-GPU (parity proven), but MEASURED SLOWER: bench_f2_multigpu @ P=768 gives G==1 (1 GPU) = 2416 ms, G==2 (2 GPU) = 3471 ms = 0.70x. The user is (rightly) certain this is an ARCHITECTURE/implementation flaw, not inherent. Find it with evidence; do NOT hand-wave "memory-bound physics".',
  'KEY KNOWN FACTS (from prior measurement + a CODE READ, treat as leads to CONFIRM/QUANTIFY, not gospel — a prior "P1 = 1.4s combine zeroing" hypothesis was REFUTED on-box: removing the host assign(0.0) just relocated the same first-touch page-fault cost into the D2H copy; cold full-tensor D2H ~849ms, warm ~101ms): (a) the SNP shard is BLOCK-ALIGNED + DISJOINT — each whole block owned by exactly ONE device — so the combine is a PLACEMENT, NOT a sum; (b) the per-device fan-out (one std::jthread per device, f2_blocks_multigpu_core.cpp:107-162) was measured at only ~18% GPU overlap — suspected to serialize on per-call device allocation (cudaMalloc takes the driver global lock + is device-wide-synchronizing, audit L4); (c) the bench at P=768 on rtxbox runs the P2P device-resident combine path (prefer_p2p_combine default true, can_access_peer true) — last_combine_path=P2pDeviceResident — so p2p_combine.cu is the HOT combine path.',
  '*** THE PRIME SUSPECT — THE DATA-BOUNCE WART (the user explicitly named this and the CODE ADMITS IT, p2p_combine.cu:18-25): the per-device compute_f2_blocks (cuda_backend.cu) ALWAYS returns a HOST F2BlockTensor — it D2H-copies its partial to host and FREES its device buffers before returning. So the "device-resident P2P combine" is forced to RE-UPLOAD each host partial back to device: H2D into root staging for the root\'s own partial (p2p_combine.cu:215-218), and for a PEER, an H2D onto the peer device (cuda_backend frees, so it is gone) then cudaMemcpyPeer peer->root (p2p_combine.cu:251-267), then a place-add kernel, then ONE final D2H of the full accumulator (p2p_combine.cu:308-313). NET G2 path per partial = compute-on-device -> D2H-to-host -> H2D-re-upload -> [peer copy] -> place-add -> D2H-result = a TRIPLE host bounce of multi-GB tensors. Single-GPU does ONE D2H. The bounce moves the full ~7GB pair across PCIe ~3x more than necessary. The code comment punts the fix ("a device-resident per-device compute that skips it ... is OUT OF SCOPE for M4.5") — that punt IS the architectural flaw to fix: the partials are already resident in device memory at the end of the GEMM and should STAY there for the combine (no D2H, no H2D re-upload), with only ONE final D2H of the combined result. Also note p2p_combine.cu:283 does a full cudaDeviceSynchronize PER PEER partial (serializes the combine). CONFIRM + QUANTIFY this bounce in the measurement (how many H2D/D2H bytes + ms does G2 spend that G1 does not), and design the device-resident fix.***',
  'THE RESULT TENSOR is f2 + vpair, each [P^2 * n_block] FP64. At P=768, n_block~757 => each array ~3.57 GB, ~7.14 GB the pair. So host/device allocation + first-touch faulting + PCIe D2H of multi-GB tensors dominates wall-clock; the GEMMs are a small slice.',
  'BOX = rtxbox (2x RTX PRO 6000 Blackwell, sm_120, CUDA 13, 96 GB, REAL P2P). ' + SSH + '; nvcc/nsys not on PATH -> ' + PATHENV + ' . The RELEASE build ALREADY EXISTS at /workspace/steppe/build-rel (freshly clean-built; bin/bench_f2_multigpu present) — REUSE it, do NOT rebuild unless a source change is required. Data /workspace/data/aadr/{raw, derived_acc, derived_full P=768}. NOTHING builds locally.',
  'KEY FILES (read these, cite file:line): src/core/fstats/f2_blocks_multigpu.cpp (the SPMG entry + the §4 combine gate), src/core/fstats/f2_blocks_multigpu_core.{hpp,cpp} (plan_multigpu_shards + compute_multigpu_partials = the jthread fan-out), src/device/cuda/cuda_backend.cu (per-device compute_f2_blocks: H2D / GEMM / D2H / per-call allocations), src/device/cuda/p2p_combine.cu (the device-resident P2P combine = the bench hot path), src/core/fstats/f2_combine.cpp (host-staged combine), tests/reference/bench_f2_multigpu.cu (the bench harness — what region it TIMES, whether the combine is inside the timed region, how G1 vs G2 are driven), include/steppe/fstats.hpp (F2BlockTensor), src/device/resources.hpp / shard_plan.hpp.',
].join('\n')

phase('Read')
const READS = [
  { key: 'orchestrator-threads', focus: 'The fan-out + threading. Read f2_blocks_multigpu.cpp + f2_blocks_multigpu_core.{hpp,cpp}. Map EXACTLY: how the G devices are driven (std::jthread per device?), whether the per-device H2D+GEMM+D2H actually run CONCURRENTLY or are serialized (by a join barrier, a shared mutex, lazy cuBLAS/context init, or per-call cudaMalloc taking the driver global lock). Does each device get its own stream/context? Is there anything that forces the two device threads to run one-after-another? Quantify the critical path: is it max(device times)+combine (good overlap) or sum(device times)+combine (serialized)?' },
  { key: 'per-device-compute', focus: 'The per-device hot path. Read src/device/cuda/cuda_backend.cu compute_f2_blocks end-to-end. Enumerate EVERY device/host allocation per call (cudaMalloc/cudaMallocAsync/cudaMallocHost/vector growth) and every cudaMemcpy (H2D + D2H) — sizes, sync vs async, pinned vs pageable, which stream. Flag any per-call cudaMalloc/cudaFree on the hot path (device-wide-synchronizing + global driver lock => serializes concurrent device threads, audit L4). Does the D2H land in a fresh (faulted) host buffer each call? Is the result buffer reused or re-allocated every call?' },
  { key: 'combine-paths', focus: 'BOTH combine paths + the redundant-materialization question. Read src/device/cuda/p2p_combine.cu (the bench HOT path) AND src/core/fstats/f2_combine.cpp. For the P2P path: exactly what it allocates (a full device tensor on the root? a full host tensor?), the cudaMemcpyPeer traffic (how many bytes move device->device), the zero-fill, and the final D2H (how many bytes, pinned/pageable, sync). For host-staged: the full-tensor zero + the copy_n. THE CORE QUESTION: does the architecture materialize the full ~7GB result MORE THAN ONCE (per-device compact partial buffers + a separate full combine tensor + a host copy), where single-GPU materializes it once? Account the redundant bytes. Could each device D2H DIRECTLY into its disjoint block-slice of ONE pre-allocated result tensor (no separate partials, no combine alloc, no copy_n)?' },
  { key: 'bench-harness', focus: 'What is actually being MEASURED. Read tests/reference/bench_f2_multigpu.cu. Determine: exactly what wall-clock region is timed for G1 and for G2 (is the combine INSIDE the timed region? the result allocation? warm-up runs? how many repetitions, min/median/mean?); how G1 vs G2 are constructed (same data? G2 repacks derived_full subsets?); whether the timer includes one-time setup (cuBLAS init, peer enable, pinned-buffer alloc) that should be amortized out. Is the comparison APPLES-TO-APPLES, or does G2 pay one-time costs G1 does not (or vice versa)? This determines whether the 0.70x is even measuring the steady-state algorithm.' },
]
const reads = await parallel(READS.map((r) => () => agent([
  'You are a senior CUDA/C++ architect doing a READ-ONLY forensic read of ONE area of the steppe M4.5 multi-GPU data flow. Do NOT edit, do NOT touch the box. Cite file:line for every claim.', STD, '',
  'YOUR AREA: ' + r.key + '\n' + r.focus, '',
  'Return: (1) a precise step-by-step data-flow / memory-traffic account for your area with file:line; (2) every place the architecture does MORE work than single-GPU needs (redundant allocation, redundant full-tensor materialization, lost overlap, non-amortized one-time cost), each tagged with a rough byte/ms cost and WHY it is avoidable; (3) the concrete architectural fix for each (what to change, which seam). Be specific and adversarial — the user is certain there is a real flaw here.',
].join('\n'), { label: 'read:' + r.key, phase: 'Read' }))).then(a => a.filter(Boolean))

phase('Measure')
const MEASURE_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['method', 'g1_768_ms', 'g2_768_ms', 'g1_buckets', 'g2_buckets', 'stream_overlap_pct', 'combine_path', 'biggest_g2_extra_cost', 'evidence', 'note'],
  properties: {
    method: { type: 'string', description: 'nsys | cudaEvent host-timer | bench-internal — how you got the per-phase numbers' },
    g1_768_ms: { type: 'number' }, g2_768_ms: { type: 'number' },
    g1_buckets: { type: 'object', description: 'per-phase ms for G1: keys like h2d, gemm, d2h, alloc, combine, host (whatever the profile resolves)', additionalProperties: { type: 'number' } },
    g2_buckets: { type: 'object', description: 'per-phase ms for G2 incl. combine + per-device. Same keys where possible.', additionalProperties: { type: 'number' } },
    stream_overlap_pct: { type: 'number', description: 'how much the TWO device streams/threads overlap in wall-clock at G2 (0 = fully serialized, 100 = perfect). From the nsys timeline (per-device kernel/memcpy start-end) or host timers.' },
    combine_path: { type: 'string', description: 'which combine path the bench actually exercised (P2pDeviceResident expected on rtxbox)' },
    biggest_g2_extra_cost: { type: 'string', description: 'the SINGLE biggest bucket of extra time G2 spends that G1 does not — the prime suspect for the +1055ms, with its measured ms' },
    evidence: { type: 'string', description: 'the raw nsys stats / timeline lines (cuda_gpu_kern_sum, cuda_gpu_mem_time_sum, gpu_trace concurrency) backing the buckets — paste the actual numbers' },
    note: { type: 'string' },
  },
}
const measure = await agent([
  'You are a senior CUDA performance engineer. MEASURE — on rtxbox — exactly where the wall-clock goes for G==1 vs G==2 at P=768, to a per-phase ms breakdown, and DETERMINE whether the two device streams actually overlap. This is the crux: produce HARD NUMBERS, not theory.', STD, '',
  'PLAN (adapt as needed): (1) confirm the binary: ' + SSH + " 'ls -la /workspace/steppe/build-rel/bin/bench_f2_multigpu'. (2) Find nsys: " + SSH + " '" + PATHENV + " && which nsys || ls /usr/local/cuda/bin/nsys /opt/nvidia/nsight*/*/nsys 2>/dev/null'. (3) Profile JUST P=768 (the bench takes [root] [P...]; pass a single P to limit trace size): " + SSH + " '" + PATHENV + " && cd /workspace/steppe && nsys profile --trace=cuda,nvtx,osrt --force-overwrite=true -o /tmp/bench768 ./build-rel/bin/bench_f2_multigpu /workspace/data/aadr 768 2>&1 | tail -30'. (4) Extract: " + SSH + " '" + PATHENV + " && nsys stats --report cuda_gpu_kern_sum --report cuda_gpu_mem_time_sum --report cuda_api_sum /tmp/bench768.nsys-rep 2>&1 | tail -80' AND a gpu_trace/timeline report to compute per-DEVICE concurrency (do the two devices' kernels/memcpys run in the SAME wall-clock window = overlap, or back-to-back = serialized?). If nsys is missing or the bench mixes G1+G2 in one process so the trace is ambiguous, FALL BACK: read bench_f2_multigpu.cu, and if needed add coarse cudaEvent/std::chrono host timers around H2D / per-device compute / D2H / combine in a throwaway edit (rebuild only that target), run, then REVERT the edit (git checkout). Whatever it takes to get the bucket breakdown + the overlap fraction.",
  'CRITICAL OUTPUTS: per-phase ms for G1 and G2 (H2D, GEMM/kernel, D2H, allocation/fault, combine incl. cudaMemcpyPeer); the stream-overlap % at G2 (are the 2 devices concurrent?); and the SINGLE biggest extra-cost bucket in G2 vs G1 accounting for the ~1055ms gap. Paste the raw nsys numbers as evidence. Do NOT leave the box in a dirty state (revert any timing edit; ' + 'cd ' + R + ' && git status must be clean of source changes). Return the structured verdict.',
].join('\n'), { schema: MEASURE_SCHEMA, label: 'measure:nsys-g1-vs-g2', phase: 'Measure' })

phase('Diagnose')
const diag = await agent([
  'You are the lead architect. Synthesize the architecture read + the on-box measurement into the DEFINITIVE answer to "why is M4.5 multi-GPU slower than single-GPU, and what did we architect wrong" — then WRITE it to ' + R + '/docs/cleanup/m4.5/why-multigpu-slow.md (Write tool).', STD, '',
  'THE ARCHITECTURE READS:\n<<<\n' + reads.join('\n\n---\n\n') + '\n>>>', '',
  'THE ON-BOX MEASUREMENT (structured):\n<<<\n' + JSON.stringify(measure, null, 2) + '\n>>>', '',
  'Be adversarial with yourself: the measured buckets are GROUND TRUTH — if they contradict a reading-phase hypothesis (e.g. the redundant-materialization story, or the overlap story), the NUMBERS win; say which hypotheses the data REFUTES. Then the doc must contain: (1) THE HEADLINE — the single root cause of the +1055ms, in one sentence, backed by the measured bucket; (2) the measured per-phase G1-vs-G2 table + the overlap %; (3) the architectural diagnosis — WHAT was architected wrong (the redundant full-tensor materialization? the non-overlapping fan-out? a non-apples-to-apples bench? something the numbers revealed?), cited to file:line; (4) THE CORRECTED DESIGN — concretely what to change so G2 BEATS G1 (e.g. D2H each device partial directly into disjoint slices of one pre-allocated/pinned result tensor to kill the redundant materialization; persistent per-device buffer pool / cudaMallocAsync to stop the malloc-lock serialization; per-device streams for real overlap; fix the bench if it is unfair), each as an ordered, parity-SAFE work item with the seam it touches and the expected ms it recovers; (5) whether each fix risks the §12 bit-identity (must stay memcmp-identical) and how to keep it. Tag every claim to a measured number or a file:line.',
  'Return a tight 6-10 line executive summary: the root cause (with the measured ms), what was architected wrong, and the ordered fix list with expected payoff — the honest answer the user has been asking for.',
].join('\n'), { label: 'diagnose-synthesis', phase: 'Diagnose' })

return { reads, measure, diag }
