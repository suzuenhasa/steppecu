export const meta = {
  name: 'jackknife-se-policy',
  description: 'Implement + explore the OPT-IN jackknife-SE policy for the qpAdm rotation — the real speedup lever from the TurboQuant/L2 study: the block-jackknife SE (~757 LOO refits/model) DOMINATES the per-model cost, and ~70-99% of rotation models are infeasible, so skip the SE for models you would reject. A policy knob (the user: "--jackknife=0/1/2 ... all, or only some, or none") on the search: NONE (no SE for any model — pure screen), FEASIBLE-ONLY (cheap point-estimate + feasibility for ALL, then the expensive SE ONLY for the survivors), ALL (SE for every model = current behavior, the DEFAULT so it stays additive/parity-safe). The Design phase EXPLORES the best implementation (API/enum shape, the survivor criterion + threshold, where to filter, two-pass GPU mechanics, whether to also short-circuit rank-sweep/popdrop). GPU is the deliverable (single-GPU path; multi-GPU is deferred). Validated: ALL mode reproduces the goldens UNCHANGED; FEASIBLE-ONLY/NONE give identical results for the models they DO compute; REAL-DATA speedup measured (All vs Feasible-only vs None + the feasible fraction). Design -> GPU implement -> INDEPENDENT verdict. commit-green / revert+HALT.',
  phases: [
    { title: 'Design', detail: 'EXPLORE + freeze: the SE-policy API (enum/default), the survivor criterion + threshold, the two-pass GPU mechanics (cheap-all -> filter -> SE-survivors), parity plan; read-only' },
    { title: 'GPU', detail: 'implement the opt-in SE policy on the single-GPU rotation (two-pass: point-estimate all -> SE survivors only) on box5090, to green' },
    { title: 'Verify', detail: 'independent verdict: ALL reproduces goldens unchanged; Feasible-only/None identical for computed models; REAL-data speedup + feasible fraction measured; commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -30 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const QPTEST = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ctest --test-dir build-rel -R qpadm --output-on-failure 2>&1 | tail -90'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120, RTX 5090) reimplementation of ADMIXTOOLS 2. Branch phase2-fit-engine @ 653557f (== ahead of main; multi-GPU DEFERRED, SINGLE-GPU is the supported path). The qpAdm fit + S8 model-space ROTATION are BUILT: run_qpadm_search(span<QpAdmModel>) -> CudaBackend::fit_models_batched buckets same-shape models and per bucket runs: f4 gather (resident f2) -> jackknife Q (cublasDgemmStridedBatched) -> Qinv (cusolverDnDpotrfBatched/potrsBatched) -> rank sweep + constrained weights + chisq + popdrop (qpadm_fit_models_kernel, one thread/model) -> the LOO SE (qpadm_loo_models_kernel, B*nb threads, ~757 per-model LOO refits) + a deterministic variance reduction (qpadm_se_from_wmat_kernel).',
  'THE LEVER (from docs/research/turboquant-l2-feasibility.md): the per-model cost is DOMINATED by the LOO SE (qpadm_loo_models_kernel: ~757 leave-one-block-out refits per model, each a Jacobi-SVD seed + ALS + weight solve). The cheap point estimate (weights/chisq/p/f4rank/feasible) is a small fraction. The qpAdm literature: ~70-99% of rotation models are INFEASIBLE. So: compute the cheap point estimate for ALL models, but pay the expensive SE ONLY for the models worth reporting (the survivors). This is a COMPUTE/algorithmic win, fully parity-safe (survivors get the IDENTICAL full FP64 SE).',
  'WHAT TO BUILD: an OPT-IN jackknife-SE policy on the search (the user: "--jackknife=0/1/2"). Three modes (Design names them + picks the int mapping + the default): (NONE) no SE for any model — every result has empty/NaN se/z, just the point estimate (the fastest pure screen); (FEASIBLE-ONLY / "some") cheap point-estimate + feasibility for ALL models, then the expensive SE ONLY for the SURVIVORS that pass the criterion; (ALL) SE for every model = the CURRENT behavior and the DEFAULT (so existing callers + goldens are UNCHANGED — the feature is purely additive/opt-in). Plumb the policy through QpAdmOptions (or a search-specific options struct) -> run_qpadm_search -> the backend batched path.',
  'THE TWO-PASS GPU MECHANICS (the deliverable; single-GPU): restructure fit_models_batched so the SE (qpadm_loo_models_kernel + qpadm_se_from_wmat_kernel) is a SEPARATE pass over a SUBSET of models. Pass 1: the cheap point-estimate (gather -> Q -> Qinv -> rank sweep -> weights -> chisq -> popdrop -> feasibility) for ALL models in the bucket/chunk. Filter: select the survivor model indices per the criterion (NONE -> empty; FEASIBLE-ONLY -> those passing; ALL -> all). Pass 2: run the LOO SE kernels ONLY over the survivor index set (a compacted batched dispatch). Keep it batched (NOT a per-model host loop), reading f2 RESIDENT. The DESIGN decides where the filter runs (host after a small D2H of the cheap results, or on-device compaction) and whether the rank-sweep/popdrop also short-circuit.',
  'SURVIVOR CRITERION (Design explores + picks, make it configurable): the natural qpAdm screen is FEASIBLE (all weights in [0,1], the existing feasibility flag) optionally AND p >= a threshold (e.g. p_se_threshold default ~0.01, configurable; or feasibility alone). Justify the default. The criterion must be computable from the CHEAP pass outputs (weights/p/f4rank/feasible) ONLY.',
  'PARITY (NON-NEGOTIABLE, §12): ALL mode must be BIT/TOLERANCE-IDENTICAL to today — the existing goldens (84-model real-AADR rotation golden_rot, 9-pop golden_fit0, NRBIG golden_fit1) MUST still pass unchanged. FEASIBLE-ONLY/NONE must produce, for every model they DO compute the SE on, the IDENTICAL se/z as ALL mode (the SE math is unchanged — only WHICH models get it changes); models without SE get a clearly-marked empty/NaN se/z (NOT a fake 0). The cheap point estimate (weights/p/f4rank/feasible) is identical across all three modes.',
  'MEASURE THE SPEEDUP — REAL DATA ONLY (memory real-data-only-all-results; NO synthetic f2, NO synthetic models): on a REAL rotation (reuse the real-AADR rotation fixture fixtures/f2_rot.bin + its model set, or a larger real source pool over a real precomputed f2) measure wall-time for ALL vs FEASIBLE-ONLY vs NONE + the FEASIBLE FRACTION (how many survivors) + the per-model SE-vs-point-estimate cost split. Report the real speedup (it scales with the infeasible fraction). Every number REAL.',
  'GATE: the opt-in SE policy is plumbed + works on the GPU (single-GPU rotation, f2 resident, batched two-pass — NOT a host loop); ALL mode reproduces the goldens UNCHANGED; FEASIBLE-ONLY/NONE give identical computed-SE results + correctly omit the rest; the REAL-data speedup + feasible fraction are measured; build clean + full ctest green. NO synthetic perf numbers; NO loosened tiers; NO parity change in ALL mode.',
  'BOX = box5090 (vast 2x RTX 5090, UP, sm_120, CUDA 13). ' + SSH + ' (alias); nvcc -> ' + PATHENV + ' . build-rel exists. RELEASE only. NOTHING builds locally. Real rotation fixture + goldens + AADR on the box. SINGLE-GPU focus (multi-GPU deferred).',
  'KEY FILES: include/steppe/qpadm.hpp (QpAdmOptions / a search-options struct — ADD the jackknife policy enum + threshold; QpAdmResult.se/z empty-marking), src/core/qpadm/qpadm_fit.cpp + model_search.cpp (plumb the policy; the host-side survivor filter if chosen), src/device/backend.hpp (fit_models_batched signature — carry the policy/survivor set), src/device/cuda/cuda_backend.cu fit_models_batched (the TWO-PASS restructure: point-estimate pass for all + SE pass for survivors), src/device/cuda/qpadm_fit_kernels.cu/.cuh (qpadm_loo_models_kernel / qpadm_se_from_wmat_kernel run over a survivor index set), tests/reference/test_qpadm_rotation.cu + test_qpadm_parity.cu (assert ALL==golden, Feasible-only/None==All on computed models, + the real speedup harness), docs/design/fit-engine.md (the SE-policy note).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); the qpAdm/rotation test (' + QPTEST + '). Iterate until build clean + full ctest green + ALL mode matches the goldens + the real-data speedup is measured. Do NOT commit (the verdict commits). Clean revert: ' + CLEAN + '.'

phase('Design')
const design = await agent([
  'You are the lead architect. EXPLORE the design space for an opt-in jackknife-SE policy on the qpAdm rotation and freeze the BEST contract. READ-ONLY (read include/steppe/qpadm.hpp QpAdmOptions/QpAdmResult, src/core/qpadm/{qpadm_fit,model_search}.cpp, src/device/backend.hpp fit_models_batched, src/device/cuda/cuda_backend.cu fit_models_batched + qpadm_fit_kernels.cu the loo/se kernels, the rotation test + goldens, docs/research/turboquant-l2-feasibility.md; do NOT edit, do NOT touch the box).', STD, '',
  'EXPLORE + decide with file:line + signatures: (1) the API — an enum (e.g. JackknifePolicy{None,FeasibleOnly,All}) + int mapping (the user said --jackknife=0/1/2) + a configurable survivor threshold, on QpAdmOptions or a search-options struct; the DEFAULT (must be ALL = current behavior so goldens/callers are unchanged); how se/z is empty-marked for un-computed models (NaN vs std::optional vs empty vector — pick, justify). (2) the SURVIVOR CRITERION (feasible-only vs feasible+p>=thr vs top-K) computable from the cheap pass; the default + why. (3) the TWO-PASS GPU mechanics — how to split fit_models_batched into the point-estimate pass (all models) + the SE pass (survivors only): where the survivor filter runs (host after a tiny D2H of the cheap results, or on-device stream-compaction), how the SE kernels (qpadm_loo_models_kernel/qpadm_se_from_wmat_kernel) take a survivor index set, whether rank-sweep/popdrop also short-circuit, and how the bucketing/chunking interacts. (4) the PARITY plan (ALL == today exactly; survivors get identical SE). (5) the REAL-data speedup harness (All vs Feasible-only vs None on the real rotation fixture + feasible fraction). Weigh trade-offs (host-filter simplicity vs device-compaction throughput; per-bucket vs global survivor compaction) and RECOMMEND the cleanest. The implementer makes NO design decisions. Single-GPU; GPU is the deliverable.',
].join('\n'), { label: 'design:jackknife-policy', phase: 'Design' })

phase('GPU')
const gpu = await agent([
  'You are a senior CUDA engineer building THE DELIVERABLE: the opt-in jackknife-SE policy on the single-GPU qpAdm rotation, two-pass (point-estimate all -> SE survivors only) ON THE GPU. EDIT locally + dev-loop on box5090; do NOT commit. ALL mode must stay bit/tolerance-identical to today.', STD, '',
  'THE FROZEN DESIGN:\n<<<\n' + (design || '(missing — add a JackknifePolicy{None,FeasibleOnly,All} enum (default All) + survivor=feasible criterion; split fit_models_batched into a point-estimate pass for all + an SE pass over the survivor index set; ALL==today; measure real speedup)') + '\n>>>', '', DEVLOOP, '',
  'Implement the policy end to end: plumb it through QpAdmOptions -> run_qpadm_search -> fit_models_batched; restructure the batched path so the LOO SE runs only over the survivor set (NONE: none; FEASIBLE-ONLY: the passing models; ALL: all); empty-mark se/z for un-computed models. Add the test assertions (ALL==goldens; Feasible-only/None == All on the models they compute; the real-data speedup + feasible fraction on the real rotation fixture). Iterate until: build clean (warnings-as-errors), full ctest green, ALL mode reproduces golden_rot + golden_fit0 + golden_fit1 UNCHANGED, the two opt-in modes are parity-correct, and the REAL speedup is measured. Report: git diff --stat; the policy API + the two-pass code; proof ALL==goldens; the Feasible-only/None correctness; the REAL All-vs-Feasible-vs-None wall-times + feasible fraction; confirmation it ran batched on the GPU (not a host loop). Do NOT commit. Do NOT use synthetic data for the speedup; do NOT change ALL-mode parity; do NOT fake-0 the omitted se/z.',
].join('\n'), { label: 'impl:jackknife-policy', phase: 'GPU' })

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','diff_is_real','default_is_all_unchanged','all_matches_goldens','optin_modes_parity','omitted_se_marked','runs_batched_gpu','real_speedup','feasible_fraction','build_green','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: a real diff plumbing an opt-in jackknife-SE policy (None/FeasibleOnly/All) with a TWO-PASS batched GPU restructure (SE only over survivors) + DEFAULT=All keeps current behavior + ALL mode reproduces ALL goldens unchanged + the opt-in modes are parity-correct (identical SE on computed models, omitted se/z clearly empty-marked not fake-0) + the rotation still runs BATCHED on the GPU (not a host loop) + the REAL-data speedup + feasible fraction are measured + Release build clean + full ctest green. NO synthetic perf, NO ALL-mode parity change.' },
    diff_is_real: { type: 'boolean', description: 'you re-read the diff: a genuine two-pass SE-policy (point-estimate pass for all + SE pass over a survivor index set), not a stub, not a per-model host loop' },
    default_is_all_unchanged: { type: 'boolean', description: 'the DEFAULT policy is ALL and existing callers/goldens behave EXACTLY as before (purely additive opt-in)' },
    all_matches_goldens: { type: 'boolean', description: 'ALL mode reproduces golden_rot (84-model) + golden_fit0 (9-pop) + golden_fit1 (NRBIG) unchanged (you re-ran)' },
    optin_modes_parity: { type: 'boolean', description: 'FeasibleOnly/None produce IDENTICAL se/z to ALL for every model they compute the SE on; the cheap point estimate (weights/p/f4rank/feasible) is identical across all three modes' },
    omitted_se_marked: { type: 'boolean', description: 'models with no SE get a clearly-marked empty/NaN se/z (NOT a fake 0)' },
    runs_batched_gpu: { type: 'boolean', description: 'the rotation still runs batched on the CUDA backend, f2 resident — NOT degraded to a host loop' },
    real_speedup: { type: 'string', description: 'the YOU-measured REAL (real AADR) wall-times: ALL vs FeasibleOnly vs None on the real rotation fixture + the speedup factor (real data only)' },
    feasible_fraction: { type: 'string', description: 'the measured feasible/survivor fraction on the real rotation (drives the speedup)' },
    build_green: { type: 'boolean', description: 'Release build clean (warnings-as-errors) + full ctest green (you re-ran)' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'what landed + the real speedup + feasible fraction; for FAIL exactly what broke / where ALL-mode parity shifted / where synthetic crept in' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (adversarial — the user wants a parity-safe opt-in lever, real-data speedup only; ALL mode must NOT change goldens). The implementer reported:\n<<<\n' + (gpu || '(gpu phase died)') + '\n>>>\n\nThe frozen design:\n<<<\n' + (design || '(design died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself: (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM: a real TWO-PASS SE policy (point-estimate pass for ALL + SE pass over a SURVIVOR subset), DEFAULT=All (additive), the SE math unchanged, omitted se/z empty-marked (NOT fake-0), still batched on the GPU (not a host loop). (2) RE-RUN yourself: ' + BUILD + ' ; ' + QPTEST + ' ; and measure the REAL All-vs-FeasibleOnly-vs-None wall-times + feasible fraction on the real rotation fixture (nvidia-smi quiet; REAL data only — reject any synthetic). (3) PASS only if ALL: diff real + two-pass + batched; DEFAULT=All unchanged; ALL mode reproduces golden_rot + golden_fit0 + golden_fit1; FeasibleOnly/None parity-correct on computed models + omitted se/z empty-marked; Release build clean; full ctest green; real speedup measured. NO synthetic perf, NO ALL-mode parity change, NO fake-0 SE. \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source+test+doc files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (the opt-in jackknife-SE policy: None/FeasibleOnly/All, two-pass GPU, skip the ~757-refit SE for infeasible models; ALL-mode parity preserved; + the measured REAL speedup + feasible fraction) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly what broke.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:jackknife-policy', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ JACKKNIFE-SE POLICY LANDED ' + verdict.commit_hash + ' — all==goldens=' + verdict.all_matches_goldens + ' optin_parity=' + verdict.optin_modes_parity + ' feasible_frac=' + verdict.feasible_fraction + ' | speedup ' + verdict.real_speedup)
else log('--- JACKKNIFE-SE POLICY FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { design, gpu, verdict }
