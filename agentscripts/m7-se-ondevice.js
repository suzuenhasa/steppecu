export const meta = {
  name: 'm7-se-ondevice',
  description: 'M7 (the LAST host-compute violation) — move the single-model qpAdm jackknife SE on-device (USER-APPROVED). src/core/qpadm/nested_models.cpp sample_cov_diag (the S7 SE variance reduction diag(cov(wmat)), host LONG DOUBLE) runs on the HOST in the single-model/large-tail path (se_from_loo, the >kQpMax models: nl>5 / nr>10 / r>4). The on-device native-FP64 kernel ALREADY EXISTS (qpadm_se_from_wmat_kernel via launch_qpadm_se_from_wmat_batched, used by the batched S8 path; the wmat is already device-resident as dWmat in gls_weights_loo_batched). THE FIX: add a backend se_from_wmat virtual (base default = host sample_cov_diag, the CpuBackend long-double ORACLE — UNCHANGED; CUDA override = route se_from_loo through the EXISTING launch_qpadm_se_from_wmat_batched on the resident dWmat, native FP64 — the cancellation carve-out, NOT emulated [Ozaki is matmul-only; this is a reduction]). NO new kernel. THE PRECISION/GOLDEN PICTURE (settled with the user): the SE is correctly NATIVE FP64 on the GPU (cancellation carve-out per the LANDED policy); the host long double is the CpuBackend test ORACLE only. The CORRECTNESS GUARD is the AT2 parity gate: test_qpadm_parity.cu se[CordedWare]/se[Turkey_N] vs g_se=0.0248167 at rtol=1e-3 (lines 276-277 + the GPU ones 771-772) — this MUST hold (FP64 is well inside 1e-3) and is the REAL check. The kNrbigPreSe0/1 anchor (test_qpadm_parity.cu:79-80, the nr=39 large-model SE asserted bit-identically rtol=atol=0) is a steppe-INTERNAL DETERMINISM/regression anchor (it captured the PRE-FIX host long-double value; NOT an AT2 golden — no AT2 SE reference exists for that large model). It is RE-CAPTURED to the new on-device FP64 deterministic value — VALID ONLY BECAUSE the AT2 rtol-1e-3 correctness gate independently HOLDS (this is NOT re-anchoring to hide a failure; it is updating a determinism constant for an intentional, AT2-correctness-preserving on-device move). The new FP64 value MUST be bit-deterministic (re-run twice, identical) so the re-captured bit-identity anchor is meaningful. REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND (the SE reduction on-device); VERIFY (real code file:line); REUSE the EXISTING kernel (NO new kernel); native FP64 (cancellation carve-out, NOT emulated); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+; KEEP ON-DEVICE no warts/bouncing. GOLDEN GATE: the AT2 se gate (rtol 1e-3) + ALL other AT2 parity goldens MUST hold; CpuBackend(long-double)==CudaBackend(FP64) SE at a TOLERANCE (~1e-9, NOT bit-identity — different precisions); the NRBIG anchor re-captured + bit-deterministic; full STEPPE_THOROUGH ctest green. ROLLBACK: on BAD (the AT2 rtol-1e-3 gate breaks, OR the SE is not actually on-device, OR the on-device value is non-deterministic) HALT + git stash push -u + defer — NOTHING commits, main stays clean. NEVER touch an AT2 parity golden; NEVER silently revert. Discipline: diagnose (GATE — the wiring + the AT2-gate-as-guard + the determinism-anchor-recapture plan) -> implement (the virtual + route + re-capture) -> verify (AT2 gate holds + on-device + deterministic + ctest) -> commit-or-HALT.',
  phases: [ { title: 'Diagnose the se_from_wmat wiring + the AT2-gate guard + anchor-recapture plan (GATE)' }, { title: 'Implement the se_from_wmat virtual (route the existing kernel) + re-capture the determinism anchor + build' }, { title: 'Verify AT2 gate holds + on-device + deterministic + ctest + commit-or-HALT' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -22; " + CORECLEAN + "'"
const NRBIG2X = SSH + " 'cd /workspace/steppe && " + PATHENV + " && echo RUN1: && STEPPE_THOROUGH=1 ./build-rel/bin/test_qpadm_parity 2>&1 | grep -iE \"NRBIG|se\\[|bit-identical|PASS|FAIL\" | head; echo RUN2: && STEPPE_THOROUGH=1 ./build-rel/bin/test_qpadm_parity 2>&1 | grep -iE \"NRBIG|se\\[|bit-identical|PASS|FAIL\" | head'"
const STASH = 'cd ' + R + ' && git stash push -u -m'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm; a GPU product. Branch phase2-fit-engine == main @ 29c48df. The host-compute audit campaign is COMPLETE except M7 (the 2 CRITICALs + jackknife family M1/M2 + qpfstats reduce + M5/M6 all landed). M7 is the LAST host-compute violation. USER APPROVED moving it on-device incl re-capturing the steppe-internal determinism anchor.',
  'M7: src/core/qpadm/nested_models.cpp sample_cov_diag (16-45, host `long double acc`) = the S7 SE variance reduction, called from se_from_loo (~72) in the single-model/large-tail path. The on-device native-FP64 kernel EXISTS: qpadm_se_from_wmat_kernel (qpadm_fit_kernels.cu:1623-1643, `double` accumulators) via launch_qpadm_se_from_wmat_batched (1783-1791), already used by the batched S8 path; dWmat is resident in gls_weights_loo_batched (cuda_backend.cu:4360, populated :4459, currently D2Hd :4461). Single-model path is UNSCALED (qpadm_fit_kernels.cu:1292).',
  'THE FIX: add a backend se_from_wmat virtual (backend.hpp, the gls_weights_loo_batched pattern). CpuBackend override = the EXISTING host sample_cov_diag (long-double ORACLE, UNCHANGED). CudaBackend override = route the resident dWmat through the EXISTING launch_qpadm_se_from_wmat_batched (n_models=1, native FP64), D2H only the nl-length se; drop the dWmat D2H + the host sample_cov_diag call on the CUDA path. se_from_loo calls be.se_from_wmat. NO new kernel.',
  'PRECISION (settled): the GPU SE = NATIVE FP64 (cancellation carve-out per the LANDED policy; Ozaki/emulated-FP64 is MATMUL-only + would not help a cancellation-sensitive reduction). The host long double = the CpuBackend test ORACLE only. This is policy-correct, NOT a violation.',
  'GOLDEN GATE — the distinction is LOAD-BEARING: (1) the AT2 PARITY gate (test_qpadm_parity.cu se[CordedWare]/se[Turkey_N] vs g_se=0.0248167 at rtol=1e-3, lines 276-277 + GPU 771-772) is the REAL CORRECTNESS GUARD — it MUST hold (FP64 is well inside 1e-3). ALL other AT2 parity goldens MUST hold. (2) the kNrbigPreSe0/1 anchor (test_qpadm_parity.cu:79-80, bit-identity rtol=atol=0) is a steppe-INTERNAL DETERMINISM anchor (captured the pre-fix host long-double value; NOT an AT2 golden — no AT2 SE reference for the nr=39 model). RE-CAPTURE it to the new on-device FP64 value — VALID ONLY BECAUSE the AT2 rtol-1e-3 gate independently HOLDS (intentional, correctness-preserving on-device move; NOT hiding a failure). The new FP64 value MUST be bit-DETERMINISTIC (re-run test_qpadm_parity twice -> identical NRBIG se bits) so the re-captured bit-identity anchor is meaningful. (3) CpuBackend(long-double) vs CudaBackend(FP64) SE compared at a TOLERANCE (~1e-9), NOT bit-identity (different precisions).',
  'STANDING REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND; VERIFY (file:line); REUSE the EXISTING kernel (NO new kernel); native FP64 carve-out (NOT emulated); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+; KEEP ON-DEVICE no warts/bouncing.',
  'ROLLBACK / FAIL-PROTOCOL (USER-CONFIRMED): NEVER git checkout -- . / git clean -fd. NEVER touch an AT2 parity golden. verify->commit ONLY on green (AT2 gate holds + on-device + deterministic + ctest green). On BAD (the AT2 rtol-1e-3 gate breaks, OR not actually on-device, OR non-deterministic) HALT + ' + STASH + ' "wip:m7-FAILED-<reason>" + defer — nothing commits, main stays clean.',
  'SINGLE-GPU --device 0. RELEASE build-rel. REAL AADR; no synthetic. Box ' + SSH + '; nvcc -> ' + PATHENV + '. nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); for (let i=0;i<2&&r===null;i++){ log(opts.label+': transient null/500 — retry '+(i+1)); r = await agent(p, {...opts, label: opts.label+':retry'+(i+1)}) } return r }

phase('Diagnose the se_from_wmat wiring + the AT2-gate guard + anchor-recapture plan (GATE)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','wiring','single_model_scaling','at2_guard','anchor_recapture','tolerance_gate','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff se_from_loo can route through the existing FP64 kernel on the resident dWmat, the AT2 rtol-1e-3 gate holds, and the determinism anchor can be re-captured deterministically. false -> HALT + defer' },
    wiring: { type: 'string', description: 'the se_from_wmat virtual + the CUDA override routing the resident dWmat through the existing launch_qpadm_se_from_wmat_batched; the CpuBackend oracle unchanged; file:line' },
    single_model_scaling: { type: 'string', description: 'the single-model UNSCALED vs batched-scaled handling (qpadm_fit_kernels.cu:1292) so the SE matches the host sample_cov_diag scaling' },
    at2_guard: { type: 'string', description: 'the AT2 rtol-1e-3 se gate (the REAL correctness guard) + confirmation FP64 is inside it' },
    anchor_recapture: { type: 'string', description: 'the kNrbigPreSe0/1 re-capture plan (to the new on-device FP64 value) + why it is a determinism anchor not an AT2 golden + the bit-determinism check' },
    tolerance_gate: { type: 'string', description: 'the CpuBackend(long-double) vs CudaBackend(FP64) SE tolerance comparison (~1e-9, not bit-identity)' },
    blocker: { type: 'string', description: 'if NOT feasible: the blocker (HALT + defer)' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing M7 on-device (verify-before-implement; NO code changes). READ src/core/qpadm/nested_models.cpp (sample_cov_diag + se_from_loo), the EXISTING qpadm_se_from_wmat_kernel + launch_qpadm_se_from_wmat_batched (qpadm_fit_kernels.cu ~1623/1783) + the batched callers (cuda_backend.cu ~4885/4917) + dWmat residency (cuda_backend.cu ~4360-4461) + the single-model UNSCALED note (qpadm_fit_kernels.cu:1292) + backend.hpp (the gls_weights_loo_batched virtual pattern) + test_qpadm_parity.cu (the AT2 se gate :276-277/:771-772 + the kNrbigPreSe0/1 anchor :79-80 + the NRBIG gate :594-613). Plan the se_from_wmat virtual + the route + the determinism-anchor re-capture. Confirm the AT2 rtol-1e-3 gate is the real guard + holds for FP64. Return the structured plan. If se_from_loo cannot route through the existing kernel preserving the AT2 gate, feasible=false + blocker (HALT + defer).', STD,
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:m7', phase: 'Diagnose the se_from_wmat wiring + the AT2-gate guard + anchor-recapture plan (GATE)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- M7 STRUCTURAL (HALT + defer): ' + design.blocker); return { halted: true, deferred: true, design } }
log('M7 feasible; wiring: ' + String(design.wiring).slice(0,140))

phase('Implement the se_from_wmat virtual (route the existing kernel) + re-capture the determinism anchor + build')
const fixer = await tryAgent([
  'You are implementing M7 on-device per this plan (route the EXISTING kernel; re-capture the determinism anchor; AT2 gate is the guard):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced).', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT: the se_from_wmat backend virtual (CpuBackend = the existing host sample_cov_diag oracle UNCHANGED; CudaBackend = route the resident dWmat through the EXISTING launch_qpadm_se_from_wmat_batched, native FP64, drop the dWmat D2H + host sample_cov_diag on the CUDA path); se_from_loo calls be.se_from_wmat; handle the single-model UNSCALED case. NO new kernel. Then RE-CAPTURE kNrbigPreSe0/1 (test_qpadm_parity.cu:79-80) to the new on-device FP64 value — ONLY after confirming the AT2 rtol-1e-3 se gate independently HOLDS + the new value is bit-deterministic (run test_qpadm_parity TWICE, identical NRBIG bits). Build + full STEPPE_THOROUGH ctest. SANITY: the AT2 se gate (rtol 1e-3) holds; CpuBackend==CudaBackend SE at ~1e-9; the NRBIG anchor (re-captured) passes bit-identically + is deterministic across 2 runs; the host sample_cov_diag no longer runs on the CUDA path; all goldens green. Report files, the wiring (file:line), the re-captured anchor values, the AT2-gate result, the 2-run determinism, the FULL ctest. Do NOT commit. BAD (AT2 gate breaks / not on-device / non-deterministic) -> STOP + report (do NOT revert).',
].join('\n'), { label: 'implement:m7', phase: 'Implement the se_from_wmat virtual (route the existing kernel) + re-capture the determinism anchor + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for M7. Accumulated edits (do NOT clean/revert/destroy; git stash only if forced). Reach a CLEAN Release build + green STEPPE_THOROUGH ctest (the AT2 se gate rtol-1e-3 + all AT2 goldens MUST hold; the re-captured NRBIG anchor must pass bit-identically + be deterministic), patching only trivial errors. DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 4x. NON-trivial / AT2 gate breaks -> STOP + report. Report build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement the se_from_wmat virtual (route the existing kernel) + re-capture the determinism anchor + build' })

phase('Verify AT2 gate holds + on-device + deterministic + ctest + commit-or-HALT')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','at2_gate_held','se_on_device','reuses_existing_kernel','nrbig_deterministic','anchor_recaptured','cpu_gpu_tolerance','goldens_green','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if the AT2 se gate (rtol 1e-3) + all AT2 goldens hold, the single-model SE runs on-device via the EXISTING FP64 kernel (no new kernel, host sample_cov_diag gone from the CUDA path), the re-captured NRBIG anchor passes bit-identically + is deterministic across 2 runs, CpuBackend==CudaBackend at ~1e-9, ctest green, single-GPU' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (HALT + defer); the AT2 gate breaking = bad' },
    at2_gate_held: { type: 'boolean', description: 'THE GUARD: the AT2 se vs g_se rtol-1e-3 gate holds' },
    se_on_device: { type: 'boolean', description: 'the single-model SE reduction runs on the GPU (host sample_cov_diag gone from the CUDA path)' },
    reuses_existing_kernel: { type: 'boolean', description: 'routed the EXISTING launch_qpadm_se_from_wmat_batched, NO new kernel' },
    nrbig_deterministic: { type: 'boolean', description: 'the new on-device NRBIG SE is bit-identical across 2 runs (the re-captured anchor is meaningful)' },
    anchor_recaptured: { type: 'string', description: 'the re-captured kNrbigPreSe0/1 values' },
    cpu_gpu_tolerance: { type: 'string', description: 'the CpuBackend(long-double) vs CudaBackend(FP64) SE delta (should be ~1e-9)' },
    goldens_green: { type: 'boolean' },
    commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the AT2 gate + on-device + determinism; on FAIL the blocker + minor-vs-bad' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for M7 (adversarial; the AT2 rtol-1e-3 se gate is the CORRECTNESS GUARD and MUST hold; the NRBIG anchor re-capture is determinism-only and valid ONLY if the AT2 gate independently holds + the value is deterministic). design:\n<<<\n' + JSON.stringify(design) + '\n>>>\nimplementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — se_from_loo routes through the EXISTING launch_qpadm_se_from_wmat_batched (NO new kernel), the host sample_cov_diag is gone from the CUDA path (only the CpuBackend oracle keeps it), the kNrbigPreSe0/1 re-capture is determinism-only + NO AT2 parity golden touched. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; THE GUARD: the AT2 se gate (se[CordedWare]/se[Turkey_N] vs g_se rtol 1e-3) HOLDS; CpuBackend==CudaBackend SE ~1e-9. (3) DETERMINISM: ' + NRBIG2X + ' — the NRBIG on-device se bits IDENTICAL across the 2 runs (so the re-captured bit-identity anchor is real). (4) single-GPU, real AADR. PASS ONLY if the AT2 gate held AND the SE is on-device AND it is deterministic. ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed source/test/cmake (NEVER git add dot; never aadr/ atlas_results/), commit (perf(host-cleanup M7): single-model qpAdm SE on-device via the existing FP64 se_from_wmat kernel — the LAST host-compute violation closed; AT2 se gate (rtol 1e-3) held, NRBIG determinism anchor re-captured to the on-device value (not an AT2 golden), CpuBackend==CudaBackend ~1e-9; the host-compute audit campaign complete bar L1-L4) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs/research/host-compute-audit.md (M7 done; campaign complete except the deferred L1-L4) + RESUME/TODO. ',
  'ON BAD (AT2 gate breaks / not on-device / non-deterministic): DO NOT git checkout/clean, DO NOT revert, DO NOT touch an AT2 golden. ' + STASH + ' "wip:m7-FAILED" (capture the ref). Classify fail_severity=bad + the blocker for the user. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:m7', phase: 'Verify AT2 gate holds + on-device + deterministic + ctest + commit-or-HALT' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ M7 ' + verdict.commit_hash + ' — AT2 held, SE on-device, deterministic — ' + verdict.note)
else log('--- M7 FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — HALT+defer — ' + verdict.note)
return { design, verdict }
