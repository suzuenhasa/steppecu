export const meta = {
  name: 'research-turboquant-l2',
  description: 'Research fan-out: determine what TurboQuant is and whether keeping a quantized working set L2-RESIDENT on the RTX 5090 (GB202) would speed up steppe — especially the now-BUILT qpAdm fit / S8 model-space rotation. Parallel angle agents: (1) TurboQuant the METHOD (the paper(s), the actual algorithm, distortion-rate/online claims, any GPU impls), (2) GPU L2 PERSISTENCE mechanics (cudaAccessPolicyWindow / persisting-L2 carve-out, the 5090/GB202 L2 size + HBM-vs-L2 bandwidth, when L2-residency actually wins), (3) STEPPE bottleneck reality (read the code + the perf we have: is the single-GPU rotation memory-bound where L2 helps, or compute/launch-bound where it does not?), (4) the SCREEN-then-VERIFY pattern (quantized L2-resident f2 → fast feasibility screen of thousands of models → FP64 recompute of survivors; the §12 parity law: quantized NEVER emits a reported est/se/z/p), (5) HONEST cost/benefit + a concrete minimal experiment. Adversarial completeness critic + synthesis with a quantified speedup ceiling and a go/no-go. Builds on docs/research/turboquant-l2-experiment.md. No box, no repo edits — returns the report.',
  phases: [
    { title: 'Sweep', detail: 'parallel angles: TurboQuant method, GPU L2 persistence on the 5090, steppe bottleneck, screen-then-verify, cost/benefit+experiment' },
    { title: 'Synthesize', detail: 'adversarial critic + a quantified, honest report: will it speed steppe up, how it would work, go/no-go + experiment design' },
  ],
}

const STEPPE = [
  'steppe = a GPU/CUDA-13/Blackwell reimplementation of ADMIXTOOLS 2 f-statistics + qpAdm. BUILT: the f2 precompute (device-resident f2_blocks [P x P x n_block] FP64, O(P^2 * n_block) — MB-scale at tens of pops, GB-scale at hundreds+ pops; ~76 GB at P=2500), and the qpAdm fit engine S3-S8 ON THE GPU — GLS fit, rank test/qpWave, arbitrary model sizes (cuSOLVER), and the model-space ROTATION (run_qpadm_search: many models batched via cuSOLVER gesvdjBatched + cublasDgemmStridedBatched + cusolverDnDpotrfBatched, reading the RESIDENT f2). Parity-validated vs ADMIXTOOLS 2 goldens on real AADR.',
  'MEASURED (real AADR, P=600, n_block=757): single-GPU rotation ~530 models/sec at N=9086 (~1.9 ms/model); the per-model fit gathers a small pop-subset (target+sources+right, ~12 pops) from the resident f2 across all ~757 jackknife blocks, then does small batched SVD/SYRK/Cholesky. Multi-GPU is host-bounce-capped on the 5090s (no P2P) and DEFERRED — so SINGLE-GPU throughput is what matters.',
  'HARDWARE: RTX 5090 = GB202, sm_120, 32 GB GDDR7, a LARGE L2 cache (find the exact size — order ~88-128 MB). CUDA 13 supports persisting-L2 (cudaAccessPolicyWindow / cudaStreamSetAttribute / the L2 set-aside carve-out via cudaDeviceSetLimit(cudaLimitPersistingL2CacheSize)).',
  'PARITY LAW (§12, NON-NEGOTIABLE): a quantized/low-precision value may ONLY be used for SCREENING/ranking — it NEVER produces a reported est/se/z/p; the final number is always recomputed in EmulatedFp64{40}/native FP64 and bit/tolerance-checked vs AT2. So TurboQuant can at most be a SCREEN, with FP64 verify of survivors.',
  'The idea under test (the user\'s): use TurboQuant (cheap near-optimal quantization) to shrink the f2 working set enough to be L2-RESIDENT on the 5090, making a fast first-pass model-space SCREEN HBM-traffic-free, then recompute survivors in FP64. EXISTING starting doc: docs/research/turboquant-l2-experiment.md.',
].join('\n')

const ANGLES = [
  { key: 'turboquant-method', focus: 'TurboQuant THE METHOD: find the actual paper(s) and any reference/GPU implementations. What exactly does TurboQuant quantize (vectors? KV-cache? embeddings?), what is the algorithm (the "online/streaming, near-optimal distortion-rate" claim — rotation/random-projection + scalar quant? product quant?), what bit-widths + what measured distortion/accuracy, and what speed/throughput + memory-reduction it reports. Is there CUDA code? How fast is the QUANTIZE step itself (does it amortize)? Cite primary sources (arXiv, repos).' },
  { key: 'gpu-l2-persistence', focus: 'GPU L2 RESIDENCY on Blackwell/GB202 (RTX 5090): the EXACT L2 cache size of the 5090 and its L2-vs-HBM bandwidth ratio (how much faster is an L2 hit than a GDDR7 read). How CUDA pins data in L2: cudaDeviceSetLimit(cudaLimitPersistingL2CacheSize) carve-out + cudaAccessPolicyWindow (hitRatio, num_bytes) via cudaStreamSetAttribute / CUDA-graph node attrs. The PRACTICAL rules: a persisting window must be <= the set-aside (which is a fraction of total L2); when does L2-residency actually win (memory-bound, data-reused-across-many-launches) vs not (compute-bound or streamed-once). Prior art: L2-aware tiling, FlashAttention-style residency, GEMM L2 blocking. Cite NVIDIA docs / blogs.' },
  { key: 'steppe-bottleneck', focus: 'WHERE is steppe\'s single-GPU rotation actually bottlenecked — memory-bandwidth (where L2 helps) or compute/launch (where it does not)? READ the code: src/device/cuda/cuda_backend.cu fit_models_batched + qpadm_fit_kernels.cu (the f4 gather reading the resident f2, the batched SYRK/cuSOLVER), src/core/qpadm/model_search.cpp. Reason from the measured ~1.9 ms/model at P=600/n_block=757: per model the f4 gather touches ~(subset^2 * n_block) doubles of the resident f2 — estimate the bytes read per model and the arithmetic intensity. Is the rotation HBM-read-bound on the f2 gather (=> L2-residency of the hot f2 region or a quantized f2 would help), or dominated by the small cuSOLVER/SYRK launches + ALS (=> L2 won\'t help much, the fix is batching/launch)? Load web tools only if needed; this angle is mostly codebase + arithmetic.' },
  { key: 'screen-then-verify', focus: 'The SCREEN-then-VERIFY architecture for a model-space search: a fast low-precision/quantized first pass that REJECTS the obviously-infeasible models (keeps the f2 working set tiny + L2-resident), then a precise FP64 recompute of the survivors. How big is the rejection ratio typically (what fraction of a rotation is infeasible)? Precedents in popgen / vector-search / ANN (coarse-quantize-then-rerank, IVF-PQ). The §12 parity constraint: quantized screening can shift the accept/reject boundary — what error budget keeps a screen SAFE (no false-negatives that drop a truly-feasible model)? Cite ANN/quantization-rerank literature + the qpAdm feasibility-threshold practice.' },
  { key: 'costbenefit-experiment', focus: 'HONEST cost/benefit + a concrete minimal experiment. Quantify: the 5090 L2 size vs steppe\'s f2 working-set size (at what P / model-subset does a quantized f2 fit in the L2 set-aside?); the best-case speedup (L2-vs-HBM bandwidth ratio x the memory-bound fraction of the rotation — Amdahl); the QUANTIZE overhead (is it one-time or per-screen?); the accuracy/parity risk. Is this a REAL win for steppe\'s actual sizes, or a solution looking for a problem (e.g. if the rotation is launch-bound, or if the per-model f2 footprint already fits L2 without quantization)? Propose the smallest experiment that would DECIDE it (e.g. an ncu memory-workload-analysis of fit_models_batched on the box to get the real HBM-read fraction + L2 hit rate). NO synthetic-data perf claims (real AADR only).' },
]

phase('Sweep')
const findings = await parallel(ANGLES.map(a => () => agent([
  'You are a CUDA-perf-and-quantization-literate research analyst. Investigate THIS angle for whether TurboQuant + L2-residency speeds up steppe on the RTX 5090: ' + a.focus,
  '', 'CONTEXT — ' + STEPPE,
  '', 'Load web tools if needed: ToolSearch query "select:WebSearch,WebFetch", then run targeted searches + FETCH primary sources (arXiv, NVIDIA docs/blogs, repos). For the steppe-bottleneck angle, READ the named source files and reason from the measured numbers. Be QUANTITATIVE (sizes in MB, bandwidth ratios, bytes/model, Amdahl fractions) and HONEST — flag inferences vs cited facts, and call out if the idea does NOT help. No fabricated URLs/numbers. NO synthetic-data perf claims.',
  'Return a structured findings list for this angle: each item = {claim, the number/mechanism, evidence (URL or code file:line or the arithmetic), confidence, and what it implies for "does TurboQuant-to-L2 speed up steppe on the 5090"}.',
].join('\n'), { label: 'sweep:' + a.key, phase: 'Sweep' })))

phase('Synthesize')
const critic = await agent([
  'You are an adversarial critic of a "TurboQuant + L2-residency for steppe on the 5090" feasibility study. The user has been BURNED by over-optimistic perf claims (a synthetic sweep that inverted reality) — your job is to find where this analysis is hand-wavy or hype. Below are 5 angle reports. Attack: is the rotation even memory-bound (if launch/compute-bound, L2 is moot)? does the per-model f2 footprint already fit L2 WITHOUT quantization (then TurboQuant is pointless)? is the quantize cost amortized or paid per screen? does the screen risk false-negatives (dropping feasible models — a §12 violation)? are the bandwidth/size numbers real for GB202 or guessed? Do 3-6 of your OWN web searches (ToolSearch select:WebSearch,WebFetch) to settle the biggest uncertainties.',
  '', 'CONTEXT — ' + STEPPE,
  '', 'THE ANGLE REPORTS:\n' + findings.filter(Boolean).map((f,i)=>'### '+ANGLES[i].key+'\n'+f).join('\n\n'),
  '', 'Return: the weakest claims + your corrected/added sourced findings, and a blunt take on whether this is a real win or a solution looking for a problem.',
].join('\n'), { label: 'synthesize:critic', phase: 'Synthesize' })

const report = await agent([
  'You are the lead synthesizer. Produce an HONEST, QUANTIFIED feasibility report: does TurboQuant + L2-residency speed up steppe on the RTX 5090, how would it work, and is it worth doing. Use the angle reports + the critic. Be concrete with numbers; give a clear GO / NO-GO / EXPERIMENT-FIRST verdict; respect the §12 parity law (quantized = screen-only, FP64-verify survivors). No fabrication; flag inference vs cited.',
  '', 'CONTEXT — ' + STEPPE,
  '', 'ANGLE REPORTS:\n' + findings.filter(Boolean).map((f,i)=>'### '+ANGLES[i].key+'\n'+f).join('\n\n'),
  '', 'CRITIC:\n' + (critic || '(none)'),
  '', 'Return a clean markdown report: (1) WHAT TurboQuant is (1 paragraph, cited); (2) HOW L2-residency works on the 5090 (the L2 size, the persisting-L2 mechanism, the bandwidth win) cited; (3) WOULD it speed up steppe — the honest bottleneck analysis (memory-bound vs launch-bound), the best-case Amdahl speedup with numbers, and where it helps vs doesn\'t; (4) HOW it would work in steppe IF worth it (the screen-then-FP64-verify design, the parity-safe error budget); (5) VERDICT: GO / NO-GO / EXPERIMENT-FIRST + the single smallest experiment (likely an ncu memory-workload-analysis on box5090) that decides it; (6) sources (real URLs). This report is the deliverable.',
].join('\n'), { label: 'synthesize:report', phase: 'Synthesize' })

log('research-turboquant-l2: synthesis complete')
return { report, critic, findings }
