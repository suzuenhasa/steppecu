export const meta = {
  name: 'fstats-scale-finish',
  description: 'FINISH the f-stats production-scale fix PROPERLY for BOTH realistic AND MASSIVE scale (the massive sets — all-quartets/triples over 1000+ pops, BILLIONS of items, multi-TB result tables — are a FIRST-CLASS target, NOT a hypothetical). The diagonal-only on-device jackknife CORE is ALREADY in the working tree (restored from stash 880e929: OOM gone, linear to 1M, goldens UNCHANGED, ctest 54/54, correctness-neutral — KEEP IT). Three parts remain, all REUSE, all designed for the MASSIVE envelope: (A) MOVE the f4-ratio/dstat jackknife ON-DEVICE (native-FP64 cancellation carve-out; the host long-double loops f4ratio.cpp:79-155 + dstat.cpp:70-157 are a GPU-only/precision-policy violation — NOTHING on the CPU for a reported number); (B) UPGRADE the interim STEPPE_FSTAT_CHUNK lever to the REAL device->host->disk TIERING — REUSE OutputTier / resolve_output_tier / free_host_ram_bytes / the HostRam+Disk SINKS / STEPPE_FORCE_TIER (tier_select.hpp + host_ram.cpp + stream_f2_blocks.hpp), sizing for the f-stat item-batch AND the RESULT-SPILL so a billions-item [N×4] table spills device->host->disk EXACTLY like the precompute spills the f2 tensor (PARITY-NEUTRAL); (C) qpDstat Part B genotype front-end: a TILED run_dstat (SNP-tile decode->reduce->accumulate, reusing the SNP-tile-streaming PATTERN of run_f2_blocks_resident) instead of the full [P×M] materialize, so a LARGE pop-union streams. PRECISION = EmulatedFp64{40} default (matmul-heavy) / native-FP64 carve-out (cancellation/ill-conditioned) / NEVER the CPU. NO GOLDEN REGEN (the existing FP64 goldens are the oracle; the diagonal IS the same number). VALIDATE BOTH SCALES ON REAL AADR — NO synthetic data EVER (even for scaling/throughput): goldens unchanged at small N; force-tier resident==host==disk bit-correctness on REAL data; and a GENUINELY MASSIVE real run — extract a LARGE real f2 cache (several hundred pops at 1240K) and run all-quartets at a scale that AUTO-SPILLS to disk, confirming completion + spot-checked correctness; qpDstat-B on a large real pop-union exercising the SNP-tile stream. Discipline: design-gate -> implement -> build-repair -> verify. FAIL-PROTOCOL: NEVER git checkout/clean; on failure git stash push -u + HALT; verdict classifies severity (minor=move-on / bad=defer). SINGLE-GPU; commit-on-green.',
  phases: [ { title: 'Design the finishing parts for MASSIVE scale (GATE)' }, { title: 'Implement on-device jackknife + real tiering + geno stream + build' }, { title: 'Verify BOTH scales on real AADR + commit-or-stash' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -30; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'
const PA = '/workspace/data/aadr/converted_pa/v66_HO_pa'
const TGENO = '/workspace/data/aadr/raw/v66.p1_HO.aadr.patch.PUB'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine, HEAD 40f56ae (the workflow agentscript ONLY). The DIAGONAL-jackknife CORE is in the UNCOMMITTED WORKING TREE (restored from stash): f4.cpp/f3.cpp use a diagonal-only on-device variance (f4_diag_var_kernel = (1/nb)Σ_b xtau[k,b]², reusing launch_f4_xtau), the dense Q SYRK + Cholesky inverse are DELETED for f4/f3/qpdstat-A, an interim STEPPE_FSTAT_CHUNK VRAM chunk-lever (tier_select.hpp) exists, tests/reference/test_fstats_scale.cu added. This CORE is verified (OOM gone, linear to 1M, goldens unchanged, ctest 54/54) — KEEP it; do NOT revert it.',
  'DESIGN-FOR-SCALE (the user mandate, [[design-for-scale-not-smallest-model]]): the MASSIVE sets are FIRST-CLASS — all-quartets/triples over 1000+ pops = BILLIONS of items, the [N×4] est/se/z/p result reaching MULTI-TB and MUST spill device->host->disk. Do NOT dismiss the spill as "overkill because the small result is tiny" — that is the exact anti-pattern. Both realistic AND massive scale, PROPERLY.',
  'THE 3 REMAINING PARTS (all REUSE, all massive-scale): (A) f4-ratio/dstat jackknife ON-DEVICE — replace the host long-double loops (f4ratio.cpp:79-155, dstat.cpp:70-157) with an on-device native-FP64 kernel (the ratio/num-den cancellation = native carve-out per the precision policy); NOTHING on the CPU for a reported number (CpuBackend = oracle only). (B) REAL device->host->disk TIERING — upgrade the interim STEPPE_FSTAT_CHUNK lever to REUSE OutputTier{Resident,HostRam,Disk} + resolve_output_tier (config.force_tier > STEPPE_FORCE_TIER > auto) + free_host_ram_bytes() + the HostRam triple-buffered sink + the Disk staging sink (tier_select.hpp / host_ram.cpp / stream_f2_blocks.hpp), sizing BOTH the device item-batch working set AND the [N×4] RESULT so billions-of-items spill device->host->disk PARITY-NEUTRAL, exactly like the precompute f2 tensor. (C) qpDstat-B TILED genotype front-end — replace the full read_tile(part,0,M0)+full [P×M] decode (dstat.cpp:203,238-255) with a SNP-tile decode->dstat_block_reduce->accumulate loop reusing the run_f2_blocks_resident SNP-tile-streaming PATTERN, so a large pop-union streams at O(P·tile).',
  'PRECISION POLICY (verified docs/design/fit-engine.md:227-261, commit 8d4f22f): EmulatedFp64{40} (Ozaki tensor-core) is the DEFAULT for well-conditioned matmul-heavy work; native FP64 is the FALLBACK (emulation_honorable) + the carve-out for cancellation (the ratio/num-den/xtau numerators) + ill-conditioned. NEVER the CPU. Reuse engage_f2_precision / emulation_honorable / MathModeScope — no new precision machinery.',
  'REAL DATA ONLY — NO synthetic, EVER, including scaling/throughput ([[real-data-only-all-results]]). convertf-PA at ' + PA + '; raw TGENO at ' + TGENO + '. The 60-pop real f2 cache (f2_60) exists from the perf run; for the MASSIVE test EXTRACT a LARGER real f2 cache (several hundred pops at 1240K) so all-quartets genuinely exceeds VRAM and auto-spills to disk.',
  'NO GOLDEN REGEN — the existing FP64 goldens (golden_fit0_{f4,f3,f4ratio,qpdstat,dstat_geno}_*.csv) ARE the oracle; the diagonal + the on-device ratio/dstat jackknife reproduce them at 1e-6 by construction (if any golden shifts, that is a BUG).',
  'FAIL-PROTOCOL (USER-MANDATED): NEVER git checkout -- . / git clean -fd. On ANY failure ' + STASH + ' "wip:fstats-finish-FAILED-<reason>" + HALT. NON-trivial/structural blocker -> STOP + report (defer). Classify minor (move-on) vs bad (defer).',
  'SINGLE-GPU (--device 0; multi-gpu PARKED). RELEASE build-rel. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering (tier policy host-pure CUDA-free like tier_select.hpp; kernels in .cu). nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); if (r === null) { log(opts.label + ': transient null — retry'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) } return r }

phase('Design the finishing parts for MASSIVE scale (GATE)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','core_intact','ondevice_jackknife_plan','tiering_plan','geno_stream_plan','massive_test_plan','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff all 3 parts are achievable by reuse, massive-scale-correct, with the existing goldens unchanged; false -> structural blocker, defer' },
    core_intact: { type: 'string', description: 'confirm the working-tree diagonal core is present + correct (f4_diag_var_kernel, dense Q deleted, goldens-neutral) — to BUILD ON, not redo; file:line' },
    ondevice_jackknife_plan: { type: 'string', description: 'how f4ratio.cpp:79-155 + dstat.cpp:70-157 host long-double loops become an on-device native-FP64 kernel (one thread per tuple/quadruple; the loo_num/loo_den ratio jackknife); proof native-FP64 reproduces the existing golden (the host long-double was over-precision); file:line' },
    tiering_plan: { type: 'string', description: 'how the interim STEPPE_FSTAT_CHUNK lever is UPGRADED to REUSE OutputTier/resolve_output_tier/free_host_ram_bytes/the HostRam+Disk sinks/STEPPE_FORCE_TIER, sizing the device item-batch AND the [N×4] result-spill so billions-of-items spill device->host->disk; what is verbatim reuse vs a thin f-stat sizing/sink-adapter; file:line' },
    geno_stream_plan: { type: 'string', description: 'how run_dstat becomes a SNP-tile decode->reduce->accumulate loop (reusing the run_f2_blocks_resident streaming pattern) for large pop-unions; file:line' },
    massive_test_plan: { type: 'string', description: 'the REAL-AADR massive-scale validation: which large real f2 cache to extract (pop count, ~SNPs), the all-quartets N that auto-spills to disk, the force-tier resident==host==disk correctness check, the qpDstat-B large-union stream test — ALL real data, NO synthetic' },
    blocker: { type: 'string', description: 'if NOT feasible: the structural gap + options for the user' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing the FINISHING of the f-stats massive-scale fix (verify-before-implement; NO code changes). The diagonal CORE is already in the working tree — confirm it + BUILD ON it. READ: the current working-tree f4.cpp/f3.cpp (the diagonal kernel) + tier_select.hpp (the interim STEPPE_FSTAT_CHUNK lever AND the real OutputTier/select_output_tier/resolve_output_tier) + host_ram.cpp (free_host_ram_bytes + the resolve + the HostRam/Disk sinks) + stream_f2_blocks.hpp + the f2-precompute orchestrator (run_f2_blocks_resident / the streamed path — the SNP-tile pattern to mirror for qpDstat-B); f4ratio.cpp:79-155 + dstat.cpp:70-157 (the host long-double jackknife loops to move on-device); dstat.cpp:203,238-255 (the full-materialize to tile); docs/design/fit-engine.md:227-261 (precision). VERIFY all 3 parts are reuse + massive-scale-correct + golden-neutral.', STD, '',
  'Return the structured design (feasible + core_intact + the 3 plans + the REAL-AADR massive_test_plan, file:line). If anything cannot be done by reuse / would shift a golden / needs synthetic to test / needs a real decision, set feasible=false + blocker. Do NOT implement.',
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:finish', phase: 'Design the finishing parts for MASSIVE scale (GATE)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- finishing STRUCTURAL (defer): ' + design.blocker); return { halted: true, deferred: true, design } }
log('finishing feasible; core intact: ' + String(design.core_intact).slice(0,100))

phase('Implement on-device jackknife + real tiering + geno stream + build')
const fixer = await tryAgent([
  'You are implementing the f-stats massive-scale FINISHING per this design (BUILD ON the working-tree diagonal core; REUSE everything; massive sets are first-class):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced). Do NOT revert the diagonal core.', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT the 3 parts: (A) the f4-ratio/dstat jackknife ON-DEVICE (native-FP64; delete the host long-double loops; nothing on CPU). (B) UPGRADE the chunk-lever to REUSE OutputTier/resolve_output_tier/free_host_ram_bytes/the HostRam+Disk sinks/STEPPE_FORCE_TIER for the device item-batch AND the [N×4] result-spill (billions-of-items -> device->host->disk). (C) the TILED run_dstat genotype front-end (SNP-tile decode->reduce->accumulate). Keep precision per policy; goldens UNCHANGED. ADD/extend tests: the existing goldens re-pass at 1e-6; a force-tier (STEPPE_FORCE_TIER=resident/host/disk) f-stat correctness test on REAL data (the 3 tiers bit-agree); the massive real run (extract a several-hundred-pop real f2 cache, all-quartets auto-spills to disk, completes + spot-check correct); qpDstat-B large-union real stream. NO synthetic. Build + full STEPPE_THOROUGH ctest. Report every file changed, the reuse (file:line), the on-device jackknife, the tiering wiring, the massive real-data result, the FULL ctest. Do NOT commit. NON-trivial blocker -> STOP + report.',
].join('\n'), { label: 'implement:finish', phase: 'Implement on-device jackknife + real tiering + geno stream + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for the f-stats massive-scale finishing. Accumulated edits incl the diagonal core (do NOT clean/revert/destroy the core; git stash only if forced). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON) + green ctest, patching only trivial -Werror / CMake wiring of the new kernels/sinks/tests. DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 5x on trivial errors. NON-trivial -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement on-device jackknife + real tiering + geno stream + build' })

phase('Verify BOTH scales on real AADR + commit-or-stash')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','goldens_unchanged','nothing_on_cpu','tiering_reused_real','spills_to_disk_on_real_massive','geno_streamed','no_synthetic','build_clean','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: existing goldens re-pass 1e-6 UNCHANGED (no regen); the f4-ratio/dstat jackknife is ON-DEVICE (no host long-double / nothing on CPU for a reported number); the device->host->disk tiering REUSES OutputTier/resolve_output_tier/sinks/STEPPE_FORCE_TIER (force-tier resident==host==disk bit-agree on REAL data); a GENUINELY MASSIVE real run (several-hundred-pop real f2 cache, billions of items) AUTO-SPILLS to disk + completes correctly; qpDstat-B large-union streams; NO synthetic anywhere; full ctest green; build clean; single-GPU' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (empty if pass)' },
    goldens_unchanged: { type: 'boolean' },
    nothing_on_cpu: { type: 'boolean', description: 'f4ratio.cpp/dstat.cpp host long-double loops GONE; the jackknife is on-device; no CPU compute for a reported number' },
    tiering_reused_real: { type: 'boolean', description: 'reuses OutputTier/resolve_output_tier/free_host_ram_bytes/HostRam+Disk sinks/STEPPE_FORCE_TIER (NOT a parallel chunk lever); force-tier resident==host==disk bit-agree on real data' },
    spills_to_disk_on_real_massive: { type: 'boolean', description: 'a real several-hundred-pop all-quartets run genuinely auto-spills device->host->disk + completes correct (the massive envelope, real AADR)' },
    geno_streamed: { type: 'boolean', description: 'run_dstat SNP-tile streams for a large pop-union (no full [P×M] materialize)' },
    no_synthetic: { type: 'boolean', description: 'every test/number is real AADR; no synthetic data anywhere' },
    build_clean: { type: 'boolean' }, commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the both-scales results (small goldens + massive disk-spill), the reuse, the on-device jackknife, the geno stream; on FAIL the blocker + minor-vs-bad' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for the f-stats massive-scale finishing (adversarial; the user has corrected the design-for-scale + nothing-on-CPU + no-synthetic + reuse bars REPEATEDLY — hold them HARD). design:\n<<<\n' + JSON.stringify(design) + '\n>>>\nimplementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — the diagonal core intact; the f4-ratio/dstat host long-double loops DELETED + replaced on-device (grep "long double" in f4ratio.cpp/dstat.cpp = NONE for a reported number); the tiering REUSES OutputTier/resolve_output_tier/free_host_ram_bytes/the sinks/STEPPE_FORCE_TIER (NOT a parallel chunk lever); the run_dstat is SNP-tile streamed. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; the EXISTING goldens re-pass 1e-6 UNCHANGED (independently confirm no golden shifted); the force-tier resident==host==disk test bit-agrees on REAL data; the MASSIVE real run (several-hundred-pop real f2 cache, all-quartets) AUTO-SPILLS to disk + completes correct; qpDstat-B large-union streams. (3) confirm NO synthetic anywhere, single-GPU, no golden regen. PASS only if ALL hold (do NOT pass a deferred part this time — the brief requires all 3). ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed source/test/cmake/doc (NEVER git add dot; never aadr/ atlas_results/ handoff-*.md; NO golden files change), commit (perf(f-stats scale): diagonal on-device jackknife + on-device f4-ratio/dstat jackknife + REAL device/host/disk result-tiering (OutputTier reuse) + tiled qpDstat-B genotype stream -> f4/f3/f4-ratio/qpDstat production-scale to BILLIONS of items, OOM removed, nothing on CPU; validated on real AADR at both small (goldens) and massive (disk-spill) scale; goldens unchanged) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs (fstats-sweep.md: the both-scales after-numbers incl the massive disk-spill; TODO/REORIENT: f-stats production-scale-shaped to billions, nothing on CPU). ',
  'ON FAIL: DO NOT git checkout/clean. ' + STASH + ' "wip:fstats-finish-FAILED" (capture the ref; the diagonal core is in there too — preserved). Classify fail_severity. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:finish', phase: 'Verify BOTH scales on real AADR + commit-or-stash' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ f-stats massive-scale ' + verdict.commit_hash + ' — ' + verdict.note)
else log('--- f-stats finishing FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — ' + verdict.note)
return { design, verdict }
