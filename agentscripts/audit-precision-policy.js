export const meta = {
  name: 'audit-precision-policy',
  description: 'AUDIT: is the Ozaki emulated-FP64 / native-FP64 precision policy applied CONSISTENTLY at every matmul/solve site? The intended policy: matmul-heavy WELL-CONDITIONED ops (the f2 3-GEMMs, the covariance SYRK, the f4/rotation GEMMs) DEFAULT to EmulatedFp64{40} and FALL BACK to native FP64 via the ONE `emulation_honorable` predicate (engage_f2_precision); the cancellation-sensitive elementwise diffs (f2 numerator, f4 combine, xtau centering) are native ALWAYS (carve-out); the cuSOLVER solves (SVD/Cholesky/GLS) are native (no FP64-emulated cuSOLVER mode on the toolkit; the CusolverMathModeScope seam auto-promotes when one appears); the CpuBackend is always native (the oracle). The RISK to find: a GEMM/SYRK hardcoded native in ONE place while another engages the emulated path; a `(void)precision` that is actually a MISSED matmul engagement (not a legit carve-out); an entrypoint defaulting native instead of EmulatedFp64; a site that skips the honorable-fallback (assumes emulated unconditionally OR hardcodes native without the predicate); an inconsistent precision_tag. 5 parallel read-only lenses -> a synthesis verdict (CONSISTENT vs DEVIATIONS, each deviation file:line + the fix). Writes docs/cleanup/bigrefactor/precision-policy-consistency.md. NO box, NO build, NO source edits.',
  phases: [
    { title: 'Lenses', detail: '5 parallel read-only lenses: matmul-engage / void-precision classification / entrypoint defaults / honorable-fallback / CpuBackend-oracle' },
    { title: 'Synthesize', detail: 'cross-check the lenses -> consistency verdict + deviation list + the policy-map doc' },
  ],
}

const R = '/home/suzunik/steppe'
const OUT = R + '/docs/cleanup/bigrefactor/precision-policy-consistency.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell reimplementation of ADMIXTOOLS 2. The PRECISION POLICY (architecture.md §12; fit-engine.md §1.4; landed across commits d6d3cbb the cuSOLVER seam + 8ace724 the unified fit policy):',
  '  DEFAULT = EmulatedFp64{mantissa_bits=40} (Ozaki fixed-slice, cuBLAS CUBLAS_FP64_EMULATED_FIXEDPOINT_MATH) for the WELL-CONDITIONED matmul-heavy stages: the f2 3-GEMMs (precompute), the covariance SYRK (Q = xtau·xtauᵀ), the f4/rotation batched GEMMs.',
  '  FALLBACK = native FP64, decided by the ONE predicate `emulation_honorable(precision)` (f2_block_kernel.cuh/.cu): an EmulatedFp64 request the build cannot honor degrades to CUBLAS_PEDANTIC_MATH (native) with a logged tag. `engage_f2_precision(handle, precision)` is the single cuBLAS engage that routes through it; `MathModeScope` is the scoped save/restore.',
  '  NATIVE-ALWAYS CARVE-OUTS (correct to ignore precision): the cancellation-sensitive elementwise math — the f2 numerator (Σp²−2Σpq+Σq²), the f4 4-slab combine, the xtau centering — because emulation cannot recover bits annihilated by a prior subtraction. cuSOLVER SOLVES (potrf/potri/gesvd/gesvdj/getrf/potrfBatched/potrsBatched: SVD/Cholesky/GLS) are native because CUDA 13.0/cuSOLVER has NO FP64-emulated math mode (STEPPE_HAVE_CUSOLVER_FP64_EMULATED==0); the CusolverMathModeScope/engage_solver_precision seam auto-promotes WHEN a toolkit exposes it. The CpuBackend is the parity ORACLE — ALWAYS native (long double / FP64), ignores precision by design.',
  'THE QUESTION (the user): are we CONSISTENT everywhere — emulated-FP64 default with native fallback for the matmul-heavy ops — or did we hardcode native in one place and engage emulated in another? Find any INCONSISTENCY.',
  'OUTPUT RULES: read-only, NO edits, NO box. Cite REAL file:line. For each matmul/solve/precision site, classify it as ONE of: ENGAGES (correctly engages the policy: emulated-default + honorable-fallback) | NATIVE-CARVE-OUT-OK (legitimately native: cancellation-elementwise OR cuSOLVER-solve OR CpuBackend-oracle) | DEVIATION (a matmul-heavy op hardcoded native / not engaging when it should; OR a site skipping the honorable predicate; OR an entrypoint defaulting native; OR an inconsistent tag). Be precise; a clean "consistent" is a valid, valuable result. NO fabrication.',
].join('\n')

const LENSES = [
  { key: 'matmul-engage', focus: 'Enumerate EVERY cuBLAS matmul-heavy call in src/ (cublasDgemm, cublasDsyrk, cublasDgemmStridedBatched, cublasDgemmBatched, any GEMM/SYRK) — grep + read each call site. For EACH: does it ride the emulated-FP64 policy (is engage_f2_precision / a MathModeScope engaging CUBLAS_FP64_EMULATED_FIXEDPOINT_MATH applied to the handle BEFORE this call, with the math mode restored after)? Trace where the math mode is set relative to the call. Sites to nail: the f2 3-GEMMs (run_f2_gemms / run_f2_gemms_group in cuda_backend.cu + f2_blocks_kernel.cu), the single-model covariance SYRK (cuda_backend.cu ~1411), the batched rotation covariance GEMM (cuda_backend.cu ~2228). Report each as ENGAGES / DEVIATION (a GEMM/SYRK with NO precision engagement = hardcoded native = deviation). Flag any matmul that engages in one path but is hardcoded native in a sibling path.' },
  { key: 'void-precision', focus: 'Enumerate EVERY `(void)precision` (and any site that takes a Precision param but never uses it) in src/ — grep gives ~14 sites (backend.hpp base no-ops, cuda_backend.cu, cpu_backend.cpp, qpadm_fit.cpp). CLASSIFY each: NATIVE-CARVE-OUT-OK (the op is cancellation-sensitive elementwise — f2 numerator / f4 combine / xtau centering — OR a cuSOLVER solve wrapper OR the CpuBackend oracle OR a pure base-class no-op stub) vs DEVIATION (the op is actually matmul-heavy / well-conditioned and SHOULD engage the emulated path but silently ignores precision). Read the surrounding code to judge what each function actually computes. The cuda_backend.cu sites at ~1247 (f-stat diff), ~1648, ~1734, ~1873, ~1951 each need a verdict: legit carve-out or missed engagement?' },
  { key: 'entrypoint-defaults', focus: 'Check that EVERY entrypoint that constructs a Precision and dispatches into the backend DEFAULTS to EmulatedFp64{kDefaultMantissaBits}, consistently. Sites: src/core/qpadm/qpadm_fit.cpp (run_impl + run_qpadm overloads + run_qpwave), src/core/qpadm/model_search.cpp (fit_one_model + run_qpadm_search + the host overload), and the f2 PRECOMPUTE entry (where run_f2_blocks_resident / compute_f2_blocks gets its Precision — trace from include/steppe + cuda_backend.cu; what does the precompute default to?). Also QpAdmOptions / DeviceConfig default precision in include/steppe/config.hpp + qpadm.hpp. Report any entrypoint that defaults to native Fp64 instead of EmulatedFp64 (a deviation) OR that is inconsistent with its siblings. Confirm kDefaultMantissaBits=40 is the single shared source.' },
  { key: 'honorable-fallback', focus: 'Verify the FALLBACK mechanism is the ONE predicate `emulation_honorable` everywhere, applied uniformly. Read f2_block_kernel.cu (emulation_honorable + engage_f2_precision bodies) + handles.hpp (MathModeScope, CusolverMathModeScope, engage_solver_precision, STEPPE_HAVE_CUSOLVER_FP64_EMULATED). Questions: does engage_f2_precision ALWAYS route the emulated-vs-native decision through emulation_honorable (never a raw unconditional CUBLAS_FP64_EMULATED set that skips the honorability check)? Is there any site that sets the emulated math mode DIRECTLY (bypassing engage_f2_precision / the predicate)? Is the precision_tag computed via the SAME emulated_fp64_honorable capability (qpadm_fit.cpp ~64) so a degraded-to-native run is tagged Fp64, not falsely EmulatedFp64? Is the cuSOLVER seam (engage_solver_precision) consistently gated on STEPPE_HAVE_CUSOLVER_FP64_EMULATED (so it degrades to native today, not a broken emulated call)? Report any site that hardcodes the math mode or skips the predicate.' },
  { key: 'cpubackend-and-tags', focus: 'Confirm the CpuBackend (src/device/cpu/cpu_backend.cpp) is CONSISTENTLY native (long double / FP64) and ignores precision BY DESIGN as the parity oracle (every (void)precision there should be the oracle carve-out, not a missed GPU engagement — it has no cuBLAS). AND audit the precision_tag / result-precision reporting end to end: do QpAdmResult.precision_tag (qpadm.hpp ~159/~220) and the f2/precompute result honestly reflect what ACTUALLY ran (emulated iff honorable-and-requested, else native)? Is the tag default (Fp64 in the header) overridden correctly by the GPU path? Any place where a result is tagged EmulatedFp64 but actually ran native, or vice versa (a lying tag is a §12 reproducibility hazard)? Report inconsistencies between what-ran and what-is-tagged.' },
]

phase('Lenses')
const findings = await parallel(LENSES.map(L => () => agent([
  'You are a CUDA precision-policy auditor for steppe. Investigate THIS lens of the emulated-FP64/native-FP64 consistency question, READ-ONLY: ' + L.focus, '', STD,
  '', 'Use grep + read the actual call sites (no box, no build). For every site in your lens, give: file:line, what it computes, and the classification (ENGAGES / NATIVE-CARVE-OUT-OK / DEVIATION) with a one-line justification. End with your lens verdict: CONSISTENT, or the list of DEVIATIONS (each with file:line + the one-line fix). NO fabrication; a clean lens is valid.',
].join('\n'), { label: 'lens:' + L.key, phase: 'Lenses' })))

phase('Synthesize')
const report = await agent([
  'You are the lead synthesizer for the steppe precision-policy consistency audit. Cross-check the 5 lens reports, resolve overlaps, and produce the verdict + the policy-map doc.', '', STD,
  '', 'THE 5 LENS REPORTS:\n' + findings.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'DO: WRITE ' + OUT + ' with: (1) HEADLINE VERDICT — is the emulated-FP64-default + native-fallback policy applied CONSISTENTLY across all matmul/solve sites? (CONSISTENT / DEVIATIONS FOUND: N). (2) THE POLICY MAP — a table of every matmul/solve/precision site [file:line | what it computes | classification ENGAGES/NATIVE-CARVE-OUT-OK/DEVIATION | note], grouped (f2 GEMMs, covariance SYRK, f4/rotation GEMMs, cuSOLVER solves, cancellation carve-outs, entrypoint defaults, CpuBackend oracle). (3) DEVIATIONS — for each, the file:line, why it is inconsistent, and the precise one-line fix (identify-only; do NOT edit). (4) CONFIRMATIONS — the things that ARE consistent (so we know they were checked). Use the Write tool. Then return the headline verdict + the deviation count so the human sees it at a glance.',
].join('\n'), { label: 'synthesize:precision', phase: 'Synthesize' })

log('precision-policy audit complete: ' + String(report).slice(0, 200))
return { findings, report }
