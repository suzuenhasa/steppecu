export const meta = {
  name: 'm4.5-device-resident-combine',
  description: 'FIX the multi-GPU data-bounce wart (the measured +1017ms = a redundant 2nd full 7.14GB D2H). Implement the device-resident combine: per-device compute LEAVES its partial resident on the GPU (no D2H, no free), the P2P combine cudaMemcpyPeers each resident partial straight into the disjoint result slice on the root, then ONE final D2H (drop the re-upload H2D + the accumulator/memset/place-add — disjoint shards = placement). + Item2 (event fence not per-peer cudaDeviceSynchronize) + Item4 (bench median/p10/p90 + out-of-band phase timers). Design -> coupled-core implement (on-box to GREEN parity) + parallel bench -> INDEPENDENT verdict (memcmp parity BIT-IDENTICAL on BOTH host-staged AND P2P paths + measured G2 drop). Release build on rtxbox. HALT-on-fail. Spec: docs/cleanup/m4.5/why-multigpu-slow.md §4.',
  phases: [
    { title: 'Design', detail: 'freeze the resident-handle seam + the orchestrator gate-move + lifetime/memory plan' },
    { title: 'Implement', detail: 'coupled core (seam+backend+combine+orchestrator) to green parity, + bench honesty in parallel' },
    { title: 'Verify', detail: 'independent verdict: memcmp parity bit-identical (both paths) + re-bench G1 vs G2; commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh rtxbox'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ rtxbox:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const PARITY = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ./build-rel/bin/test_f2_multigpu_parity 2>&1 | tail -55'"
const BENCH = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ./build-rel/bin/bench_f2_multigpu /workspace/data/aadr 400 768 2>&1 | grep -vE \"P2P combine unavailable\"'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include'

const STD = [
  'PROJECT: steppe = CUDA-13/Blackwell (sm_120) reimpl of ADMIXTOOLS 2 f-statistics, branch m4.5-multigpu. M4.5 = single-node multi-GPU f2 precompute, CORRECT + bit-identical but SLOWER than single-GPU (G2=3446ms vs G1=2429ms @ P=768, 0.70x).',
  'ROOT CAUSE (measured by nsys, committed doc docs/cleanup/m4.5/why-multigpu-slow.md — READ IT, esp. §3 diagnosis + §4 Item 1 the design + §5 the parity table): the +1017ms is a redundant SECOND full 7.14GB Device->Host copy. compute_f2_blocks (cuda_backend.cu:483-489) D2H-copies each device partial to host and FREES its device buffers (dF2_all/dVpair_all, cuda_backend.cu:326) — so combine_f2_partials_p2p (p2p_combine.cu) must RE-UPLOAD each partial H2D (:215-218 root, :251-257 peer), cudaMemcpyPeer, place-add into a zeroed full accumulator, then a 2nd full D2H (:308-313). The partials are ALREADY resident at end of GEMM; the host bounce throws that away. NOTE the trace REFUTED two red herrings: GPU overlap is 74% (NOT 18% — do not touch the fan-out threading), and the host zero-fill / P1 allocator is NOT the cost (memset 5ms; do NOT reintroduce the default-init allocator). The ONLY fix is killing the bounce.',
  'THE FIX (parity-SAFE by construction; doc §4): (Item1) add a DEVICE-RESIDENT per-device compute path that computes the partial and LEAVES dF2_all/dVpair_all resident (no D2H, no free), returning a device HANDLE (the owning DeviceBuffer<double> pair + shape {P, n_block_local, b0} + the device_id it lives on). The combine consumes handles: allocate ONE full [2*P^2*n_block] device result on the root; D2D-copy the root\'s own resident partial into its disjoint slice [slab*b0, slab*b1); cudaMemcpyPeer each PEER\'s resident partial straight into its disjoint slice; ONE final D2H of the full result. Because block-aligned shards are DISJOINT and TILE [0,n_block) exactly (validate_f2_partials checks covered==n_block_full), every slab is written exactly once => NO cudaMemset, NO place-add (+=), NO staging buffers, NO re-upload H2D. Keep the fixed g=0..G-1 order for block_sizes placement (host int). (Item2) replace the per-peer full cudaDeviceSynchronize (p2p_combine.cu:283) with an Event fence / a single sync before the final D2H (under Item1 the resident peer buffers are not freed mid-loop, so the fence collapses). (Item4, bench only) tests/reference/bench_f2_multigpu.cu: raise ITERS 2->>=10, report MEDIAN + p10/p90 (not min-of-2), and add OUT-OF-BAND phase timers (a debug timing struct on device::Resources, mirroring last_combine_path — NEVER on the numeric F2BlockTensor) breaking out {compute-wall, partial/result D2H, peer, etc}; print measured H2D/D2H byte totals per G.',
  'CRITICAL ORCHESTRATION POINT: the gate decision (use_p2p, f2_blocks_multigpu.cpp:171-173) currently runs AFTER the fan-out. MOVE it BEFORE the fan-out (it depends only on config + caps + G, all known up front). IF use_p2p: fan-out the RESIDENT compute variant -> the device-resident P2P combine. ELSE (no P2P / prefer_p2p_combine=false / can_access_peer=false): KEEP the EXACT existing host path unchanged (host partials via compute_f2_blocks -> combine_f2_partials_host). The host-staged fallback MUST stay byte-for-byte as-is — the parity test exercises BOTH paths and both must remain bit-identical to single-GPU. The G==1 fast path (f2_blocks_multigpu.cpp:87-90) is UNTOUCHED.',
  'PARITY LAW (architecture.md §12, NON-NEGOTIABLE): test_f2_multigpu_parity does memcmp of G==2 (host-staged AND P2P) vs single-GPU on derived_acc (P=50) AND derived_full (P=768), production EmulatedFp64{40}. The resident combine moves the SAME doubles (resident-to-resident DMA is byte-exact) into the SAME disjoint offsets in the SAME fixed g-order, so it is bit-identical — dropping the `+=0.0` onto a zeroed acc is exact (x written directly == x+0.0). If ANY slab is mis-placed/missed, parity FAILS — that is the guard. No recompute, no FP reassociation, no precision/order change.',
  'MEMORY/LIFETIME: keeping all partials resident simultaneously fits 96GB (each device holds ~half the 7.14GB pair; root also holds the 7.14GB full result + pulls peers). The resident handle\'s DeviceBuffer was allocated on device g by worker g; it must MOVE out of the worker into the returned vector (survive the jthread join) and be freed AFTER the combine (cudaFree is pointer-device-aware, frees correctly from any context). Exception-safety: a worker fault still surfaces on join (keep the exception_ptr pattern).',
  'BOX = rtxbox (2x RTX PRO 6000 Blackwell sm_120, CUDA 13, 96GB, REAL P2P). ' + SSH + '; nvcc not on PATH -> ' + PATHENV + ' . build-rel already exists (reuse incrementally; only a clean rebuild if needed). Data /workspace/data/aadr/{derived_acc P=50, derived_full P=768}. RELEASE build only. NOTHING builds locally.',
  'KEY FILES: include/steppe/backend.hpp (the ComputeBackend seam — add the resident method decl, CUDA-FREE), src/device/cuda/cuda_backend.cu (per-device compute; the resident variant + keep the existing host one), src/device/cuda/p2p_combine.cu (rewrite to consume resident handles), src/core/fstats/f2_blocks_multigpu.cpp + f2_blocks_multigpu_core.{hpp,cpp} (orchestrator: move gate, fork resident-vs-host fan-out), include/steppe/fstats.hpp (F2BlockTensor), src/device/resources.hpp (last_combine_path + the new debug-timing struct for Item4), src/device/shard_plan.hpp, src/device/p2p_combine.hpp (CUDA-free decl — update signature for handles), src/device/cuda/device_buffer.cuh (DeviceBuffer<double>), tests/reference/bench_f2_multigpu.cu (Item4), tests/reference/test_f2_multigpu_parity.cu (the gate — do not weaken it).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); parity (' + PARITY + '); bench (' + BENCH + '). Iterate until BOTH parity paths are memcmp BIT-IDENTICAL. Do NOT commit (the verdict commits). Clean revert if needed: ' + CLEAN + '.'

phase('Design')
const design = await agent([
  'You are the lead CUDA/C++ architect. Produce the EXACT, FROZEN implementation contract for the device-resident combine fix — the spec the implementer follows verbatim. READ-ONLY (read the doc + the key files; do NOT edit, do NOT touch the box).', STD, '',
  'Read docs/cleanup/m4.5/why-multigpu-slow.md and the key files. Then specify PRECISELY, with file:line and exact C++ signatures: (1) the resident HANDLE type (where it lives — a new header? device/resources.hpp? — its members: the DeviceBuffer<double> f2/vpair owners, P, n_block_local, b0, device_id; move-only); (2) the new ComputeBackend seam method signature (CUDA-free decl in backend.hpp) for resident compute, and how it shares code with the existing compute_f2_blocks (factor the GEMM body so the resident + host variants do not duplicate it); (3) the EXACT cuda_backend.cu change (where the current D2H+free is, what to return instead, how the buffers escape the call); (4) the EXACT combine_f2_partials_p2p rewrite (new signature taking handles, allocate one root result, D2D root slice + cudaMemcpyPeer peer slices into disjoint offsets, one D2H; what to DELETE — accumulator/memset/staging/place-add/re-upload; the Item2 fence); (5) the orchestrator change (move the use_p2p gate before the fan-out; the resident-vs-host fork; keep the host path EXACTLY as-is; p2p_combine.hpp decl update); (6) the Item4 bench + the out-of-band Resources timing struct; (7) the lifetime/exception-safety plan across the jthread join. Flag every spot where the host-staged fallback must stay byte-identical. Be concrete enough that the implementer makes NO design decisions. Return the full contract.',
].join('\n'), { label: 'design:resident-combine', phase: 'Design' })

phase('Implement')
const impl = await parallel([
  () => agent([
    'You are a senior CUDA/C++ engineer. Implement the COUPLED CORE of the device-resident combine fix (the seam + backend resident compute + p2p_combine rewrite + orchestrator gate-move + Item2 fence) per the FROZEN design, and dev-loop on rtxbox until BOTH parity paths are memcmp BIT-IDENTICAL. Do the bench file ONLY if the bench agent has not (coordinate: the OTHER agent owns tests/reference/bench_f2_multigpu.cu + the Resources timing struct — you own everything else).', STD, '',
    'THE FROZEN DESIGN CONTRACT:\n<<<\n' + (design || '(design missing — derive from the doc §4)') + '\n>>>', '', DEVLOOP, '',
    'Implement Items 1 + 2 (NOT the bench — that is the parallel agent). Build Release + ctest + run test_f2_multigpu_parity on the box; iterate until BUILD clean (warnings-as-errors) AND parity BIT-IDENTICAL on BOTH host-staged and P2P, BOTH datasets. Then run the bench once to confirm G2 dropped. Report: git --no-pager diff --stat; every file changed + what; the FULL build result; ctest summary; the parity output (the EmuFp64 G==2 host-staged == single-GPU AND G==2 P2P == single-GPU bit-identical lines + last_combine_path); the bench G1/G2 @ 400/768. Do NOT commit. If blocked, report exactly what blocked it — do NOT fabricate.',
  ].join('\n'), { label: 'impl:core', phase: 'Implement' }),
  () => agent([
    'You are a senior C++ engineer. Implement ITEM 4 ONLY (bench honesty) per the FROZEN design — you own tests/reference/bench_f2_multigpu.cu AND the out-of-band debug-timing struct on src/device/resources.hpp (mirroring last_combine_path; NEVER on the numeric F2BlockTensor). The core agent owns ALL other files — do NOT edit them beyond the agreed Resources timing struct fields the design specifies.', STD, '',
    'THE FROZEN DESIGN CONTRACT:\n<<<\n' + (design || '(design missing)') + '\n>>>', '',
    'Raise ITERS from 2 to >=10; report MEDIAN + p10/p90 per (P,G) instead of min-of-2; add the out-of-band phase timers (compute-wall / partial-D2H / re-upload / peer / final-D2H, whichever the timing struct exposes) and print measured H2D/D2H byte totals per G. Keep the numeric result untouched (timing strictly out-of-band). The bench must still compile against the NEW combine signatures (coordinate via the design contract). Return your diff --stat + the changed bench/Resources hunks. Do NOT commit. (Build/verify happens in the core agent + the verdict.)',
  ].join('\n'), { label: 'impl:bench', phase: 'Implement' }),
]).then(a => a)

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass', 'diff_is_real', 'parity_bit_identical_hoststaged', 'parity_bit_identical_p2p', 'g1_768_ms', 'g2_768_ms', 'g2_beats_g1', 'g2_dropped_vs_baseline', 'commit_hash', 'note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: real non-empty diff implementing the device-resident combine (resident handle + no D2H/free in the resident compute + cudaMemcpyPeer-into-disjoint-slice combine + one final D2H + gate moved + host path unchanged) + Release build clean (warnings-as-errors) + ctest green + parity memcmp BIT-IDENTICAL on BOTH host-staged AND P2P (both datasets) + the measured G2@768 dropped substantially vs the 3446ms baseline (>~700ms)' },
    diff_is_real: { type: 'boolean', description: 'you re-ran git diff yourself: the resident path + the combine rewrite (re-upload H2D + accumulator memset + place-add DELETED) are genuinely present, host-staged path untouched' },
    parity_bit_identical_hoststaged: { type: 'boolean' },
    parity_bit_identical_p2p: { type: 'boolean', description: 'the device-resident P2P path == single-GPU, memcmp bit-identical (last_combine_path=P2pDeviceResident actually ran)' },
    g1_768_ms: { type: 'number', description: 'YOU-measured single-GPU wall @ P=768' },
    g2_768_ms: { type: 'number', description: 'YOU-measured 2-GPU wall @ P=768 (re-run yourself)' },
    g2_beats_g1: { type: 'boolean', description: 'true if g2_768_ms < g1_768_ms — the goal (multi-GPU finally faster)' },
    g2_dropped_vs_baseline: { type: 'boolean', description: 'true if g2_768_ms dropped >~700ms below the ~3446ms pre-fix baseline — the sham-proof gate (the bounce removal is real)' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'before/after G1+G2@768 + speedup; if g2 dropped hugely but still ~ties g1, note the remaining lever (pageable final D2H pinning). For FAIL: exactly why (parity break / no drop / sham).' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (maximally adversarial — prior fix-passes on this project have SHAMMED: empty commits + fabricated reports). The implementers reported:\n<<< CORE:\n' + (impl[0] || '(core agent died)') + '\n>>>\n<<< BENCH:\n' + (impl[1] || '(bench agent died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself (do NOT trust the reports): (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM non-empty AND genuinely contains: the resident handle + the resident compute path (no D2H/free of the partial), the combine_f2_partials_p2p rewrite (cudaMemcpyPeer straight into disjoint result slices, ONE final D2H, with the re-upload H2D + accumulator cudaMemset + place_add_f2_kernel + staging buffers DELETED), the gate moved before the fan-out, the host-staged path UNCHANGED, and the Item4 bench. If missing/empty => SHAM => pass=false. (2) RE-RUN the box yourself: ' + BUILD + ' ; ' + PARITY + ' ; ' + BENCH + ' . (3) PASS only if ALL: diff real; Release build clean; ctest green; parity test_f2_multigpu_parity memcmp BIT-IDENTICAL on BOTH host-staged AND P2P (both datasets — confirm last_combine_path=P2pDeviceResident actually ran); AND the YOU-measured G2@768 dropped >~700ms below ~3446ms (ideally g2 < g1). \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source/test files (NEVER git add dot; do not add aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (the measured before/after G1+G2@768 + speedup) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly why.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:resident-combine', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ DEVICE-RESIDENT COMBINE LANDED ' + verdict.commit_hash + ' — G2@768 ' + verdict.g2_768_ms + 'ms vs G1 ' + verdict.g1_768_ms + 'ms (beats_g1=' + verdict.g2_beats_g1 + ', parity host=' + verdict.parity_bit_identical_hoststaged + ' p2p=' + verdict.parity_bit_identical_p2p + ')')
else log('--- FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { design, impl, verdict }
