export const meta = {
  name: 'fstats-gpu-sweep',
  description: 'Fix the f-stat scale problem PROPERLY and GPU-FIRST (the prior CPU-bound over-build was rolled back to 81d5f51 + preserved on branch wip/fstats-massive-overbuild for mining). REQUIREMENTS (user, emphatic): (1) NO OOM. (2) REUSE the pre-existing batched machinery (assemble_f4_quartets / the rotation engine run_qpadm_search/fit_models_batched) — or VERIFY + REPORT exactly why it cannot be reused (file:line), do NOT silently fork. (3) cap + USER-CHOSEN filter (do not dump a multi-TB file; --min-z/--top-k, --sure to override the cap). (4) GPU GPU GPU — everything on-device; EMULATED-FP64 FIRST (the matmul-heavy parts; native-FP64 cancellation carve-out per fit-engine.md/8d4f22f); MULTI-GPU PARKED (single-GPU --device 0, 5090s have no P2P); CUDA 13+ stack — VERIFY every CUB/cuBLAS/cuSOLVER API against the CUDA 13.x docs; ZERO hidden CPU work for a reported number (CpuBackend = oracle only) and PROVE the GPU is actually saturated (nvidia-smi). THE DESIGN: (a) the OOM fix = the diagonal-only jackknife for f4/f3/qpDstat-A (delete the dense NxN covariance + Cholesky inverse; f4 reads only diag(Q)) — MINE it from wip/fstats-massive-overbuild (f4_diag_var_kernel + jackknife_diag), golden-EXACT. (b) the SWEEP = ON-DEVICE: a combinatorial-unranking kernel maps thread->quartet (NO host enumeration), feed the device-resident quartet list to the EXISTING batched assemble_f4_quartets (emulated-FP64) + the diagonal jackknife, filter |z| on-device, CUB DeviceSelect stream-compact the survivors on-device, D2H ONLY survivors; chunk the index range to bound VRAM; cap on the enumeration count (refuse > kFstatMaxComb unless --sure). The HOST only drives the chunk loop + receives the small filtered survivor set. NO host enumeration, NO host filter, NO host per-item loop. Discipline: VERIFY/design-gate (incl. CUDA-13 doc check + the existing-batching-reuse verdict) -> implement GPU-only -> build-repair -> verify GPU-BOUND (nvidia-smi util HIGH, not 0%; FAIL if CPU-bound). NO golden regen (existing f4/f3/f4ratio/qpdstat/dstat_geno goldens stay EXACT). REAL AADR, no synthetic. FAIL-PROTOCOL: NEVER git checkout/clean; on failure git stash push -u + HALT; verdict classifies severity (minor=move-on / bad=defer). If the design finds a STRUCTURAL blocker (existing batching unusable + the GPU design infeasible), HALT + report — do NOT YOLO a CPU fallback.',
  phases: [ { title: 'VERIFY + design the GPU-only sweep (GATE: reuse + CUDA-13 docs)' }, { title: 'Implement on-device sweep + diagonal fix (GPU-only) + build' }, { title: 'Verify GPU-BOUND + no-OOM + goldens-exact + commit-or-stash' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -30; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'
const PA = '/workspace/data/aadr/converted_pa/v66_HO_pa'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimpl of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ 81d5f51 (the CPU-bound over-build was ROLLED BACK; it is preserved on branch wip/fstats-massive-overbuild for MINING the good kernels). The standalone f-stats f4/f3/f4-ratio/qpDstat A+B are intact at 81d5f51 BUT f4/f3 SE is back on jackknife_cov (the dense NxN covariance that OOMs ~36k items).',
  'NON-NEGOTIABLE REQUIREMENTS (user, emphatic — do NOT violate): GPU-FIRST, everything on-device, ZERO hidden CPU for a reported number (CpuBackend = oracle only); EMULATED-FP64 FIRST (EmulatedFp64{40} default for matmul-heavy work, native-FP64 only as the fallback + the cancellation carve-out — fit-engine.md, commit 8d4f22f; NOT the reverse); MULTI-GPU PARKED (single-GPU --device 0; the 5090s have no P2P); CUDA 13.x stack — VERIFY every CUB/cuBLAS/cuSOLVER API you use against the CUDA 13.x docs (do not guess an API). NO OOM. cap + USER-CHOSEN filter (no multi-TB dump). PROVE the GPU is saturated (nvidia-smi) — if it is CPU-bound / GPU-idle that is a FAIL, not a caveat.',
  'THE GPU-ONLY SWEEP DESIGN (the fix for the CPU-bound disaster): (1) on-device combinatorial UNRANKING — a kernel maps thread-id -> its quartet (a<b<c<d) by unranking the lex index (closed-form combinatorial number system); NO host enumeration. (2) feed the device-resident quartet list to the EXISTING batched assemble_f4_quartets (emulated-FP64) + the diagonal jackknife (on-device). (3) on-device |z| FILTER (each thread flags survivor = |z|>=min_z; or a device top-K). (4) CUB DeviceSelect::Flagged STREAM-COMPACT survivors on-device. (5) D2H ONLY the (small, filtered) survivors. (6) CHUNK the index range to bound VRAM (the chunk lever). (7) maxcomb CAP on the enumeration count (refuse > kFstatMaxComb unless --sure). The HOST only drives the chunk loop + receives survivors — NO host enumeration, NO host filter, NO host per-item loop.',
  'THE OOM FIX (mine from wip/fstats-massive-overbuild): the diagonal-only jackknife for f4/f3/qpDstat-A — delete the dense NxN covariance + Cholesky inverse from their SE (f4 reads only diag(Q), never inverts); reuse f4_diag_var_kernel + jackknife_diag from the preserved branch (git show wip/fstats-massive-overbuild:src/device/cuda/qpadm_fit_kernels.cu etc.). Golden-EXACT (the f4/f3 goldens must not move).',
  'REUSE-OR-EXPLAIN (user mandate Q2): the existing batched machinery is assemble_f4_quartets (src/device/backend.hpp + cuda_backend.cu — the m-axis batched four-slab f4 over the resident f2) and the rotation engine run_qpadm_search / fit_models_batched (src/core/qpadm/model_search.cpp — thousands of items batched + ranked on the GPU, multi-GPU-sharded but PARKED). The sweep MUST reuse the batched f4 compute; if any part genuinely cannot reuse it, REPORT exactly why (file:line) — do not silently fork a parallel path.',
  'NO golden regen (the existing FP64 goldens are the oracle; explicit-list mode unchanged -> exact). REAL AADR only, no synthetic. convertf-PA at ' + PA + '; f2_60 (real 60-pop cache) + f2_500 (real 500-pop cache) exist on the box.',
  'FAIL-PROTOCOL (USER-MANDATED): NEVER git checkout -- . / git clean -fd. On ANY failure ' + STASH + ' "wip:gpu-sweep-FAILED-<reason>" + HALT. If the design finds a STRUCTURAL blocker (existing batching unusable AND the on-device design infeasible), HALT + report — do NOT YOLO a CPU fallback. Classify minor vs bad.',
  'SINGLE-GPU --device 0. RELEASE build-rel. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering. nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); for (let i=0;i<2&&r===null;i++){ log(opts.label+': transient null/500 — retry '+(i+1)); r = await agent(p, {...opts, label: opts.label+':retry'+(i+1)}) } return r }

phase('VERIFY + design the GPU-only sweep (GATE: reuse + CUDA-13 docs)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','batching_reuse_verdict','ondevice_design','cuda13_api_check','emulated_fp64_plan','diagonal_fix_mining','cap_filter_output','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff a fully GPU-only sweep (on-device unrank+compute+filter+compact, no host hot loop) is achievable reusing assemble_f4_quartets, emulated-FP64, with the existing goldens exact. false -> structural blocker, HALT (NO CPU fallback)' },
    batching_reuse_verdict: { type: 'string', description: 'CAN the existing batched assemble_f4_quartets / rotation engine be reused for the sweep compute? EXACTLY how (the device quartet list feeds it), or EXACTLY why not (file:line). The user demands this answer.' },
    ondevice_design: { type: 'string', description: 'the on-device pipeline: the unranking kernel (thread->quartet, closed-form) + batched f4 + diagonal jackknife + on-device |z| filter + CUB DeviceSelect compaction + chunked index range + survivors-only D2H. Confirm NO host enumeration/filter/per-item-loop. file:line for the reuse.' },
    cuda13_api_check: { type: 'string', description: 'the CUB/cuBLAS APIs used (CUB DeviceSelect::Flagged for compaction; the cuBLAS emulated-FP64 mode) VERIFIED against the CUDA 13.x docs — exact API names/signatures + a doc reference. Flag anything not confirmed.' },
    emulated_fp64_plan: { type: 'string', description: 'where EmulatedFp64{40} is used (the matmul-heavy f4 assemble) vs the native-FP64 cancellation carve-out (xtau), reusing engage_f2_precision/emulation_honorable — per the policy (emulated DEFAULT, native FALLBACK)' },
    diagonal_fix_mining: { type: 'string', description: 'how the diagonal jackknife (f4_diag_var_kernel + jackknife_diag) is mined from wip/fstats-massive-overbuild + applied cleanly (golden-exact)' },
    cap_filter_output: { type: 'string', description: 'the maxcomb cap (enumeration-count, --sure) + the user filter (--min-z/--top-k) + the (small filtered) output (no multi-TB dump)' },
    blocker: { type: 'string', description: 'if NOT feasible: the structural blocker + the options for the user (NO CPU fallback)' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing the GPU-ONLY f-stat sweep (verify-before-implement; NO code changes). VERIFY, do not guess. READ: src/device/backend.hpp + src/device/cuda/cuda_backend.cu (assemble_f4_quartets — the batched f4; the engage_f2_precision/emulation_honorable seam ~1361-1457; jackknife_cov ~1792-1930), src/core/qpadm/model_search.cpp (the rotation engine — the existing batched+ranked GPU machinery), src/core/qpadm/f4.cpp/f3.cpp (the current SE on jackknife_cov), the preserved diagonal fix (git show wip/fstats-massive-overbuild:src/device/cuda/qpadm_fit_kernels.cu + :src/core/qpadm/jackknife.hpp for f4_diag_var_kernel/jackknife_diag), docs/design/fit-engine.md:227-261 (the emulated-FP64 policy). VERIFY the CUDA 13.x CUB DeviceSelect::Flagged API + the cuBLAS emulated-FP64 mode against the docs (WebSearch/WebFetch the CUDA 13 CUB + cuBLAS docs). ANSWER the user mandate: can the existing batched assemble_f4_quartets be reused for the sweep (feed it a DEVICE-resident unranked quartet list)? if not, exactly why?', STD, '',
  'Return the structured design: feasible + the batching-reuse verdict + the on-device pipeline (NO host hot loop) + the CUDA-13 API check + the emulated-FP64 plan + the diagonal-fix mining + cap/filter/output. If a fully-GPU design is infeasible, set feasible=false + blocker (NO CPU fallback). Cite file:line + doc refs. Do NOT implement.',
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:gpu-sweep', phase: 'VERIFY + design the GPU-only sweep (GATE: reuse + CUDA-13 docs)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- GPU sweep STRUCTURAL (defer, NO cpu fallback): ' + design.blocker); return { halted: true, deferred: true, design } }
log('GPU-only sweep feasible; batching reuse: ' + String(design.batching_reuse_verdict).slice(0,140))

phase('Implement on-device sweep + diagonal fix (GPU-only) + build')
const fixer = await tryAgent([
  'You are implementing the GPU-ONLY f-stat sweep + the diagonal OOM fix per this design (mine the diagonal kernels from wip/fstats-massive-overbuild; REUSE assemble_f4_quartets; EVERYTHING on-device):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced).', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT: (1) the diagonal jackknife (mine f4_diag_var_kernel + jackknife_diag from wip/fstats-massive-overbuild; wire f4/f3/qpdstat-A to it; golden-EXACT). (2) the GPU-ONLY sweep: an on-device unranking kernel (thread->quartet) feeding the device quartet list to assemble_f4_quartets (emulated-FP64) + the diagonal jackknife + an on-device |z| filter + CUB DeviceSelect::Flagged compaction + chunked index range + survivors-only D2H + the maxcomb cap (enumeration-count, --sure) + the user filter (--min-z/--top-k). NO host enumeration/filter/per-item-loop. (3) the small filtered output (no multi-TB dump). Build + full STEPPE_THOROUGH ctest. SANITY: the existing f4/f3/f4ratio/qpdstat/dstat_geno goldens stay EXACT; a real sweep runs GPU-bound (you SHOULD see high GPU util) + no OOM. Report every file, the reuse (file:line), the on-device pipeline, the FULL ctest, and your own nvidia-smi observation during a sweep. Do NOT commit. NON-trivial/structural blocker -> STOP + report (NO cpu fallback).',
].join('\n'), { label: 'implement:gpu-sweep', phase: 'Implement on-device sweep + diagonal fix (GPU-only) + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for the GPU-only f-stat sweep. Accumulated edits (do NOT clean/revert/destroy; git stash only if forced). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON) + green ctest, patching only trivial -Werror / CMake wiring + any CUB/emulated-FP64 API misuse (VERIFY against the CUDA 13 docs, do not guess). DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 5x on trivial errors. NON-trivial -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement on-device sweep + diagonal fix (GPU-only) + build' })

phase('Verify GPU-BOUND + no-OOM + goldens-exact + commit-or-stash')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','gpu_bound_proven','gpu_util_pct','no_cpu_hotloop','no_oom','goldens_exact','reuses_batching','emulated_fp64','cap_filter_ok','build_clean','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if a real sweep is PROVEN GPU-BOUND (nvidia-smi GPU util HIGH/sustained during the sweep, NOT ~0%; no host per-item hot loop), no OOM, the existing goldens are EXACT, the sweep reuses assemble_f4_quartets, emulated-FP64 is the default matmul precision, cap+filter work, full ctest green, single-GPU, no synthetic, NO hidden CPU compute' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (empty if pass)' },
    gpu_bound_proven: { type: 'boolean', description: 'a real sweep was sampled with nvidia-smi and the GPU is SATURATED (the headline anti-CPU-hiding gate). If GPU-idle/CPU-bound -> FALSE -> FAIL' },
    gpu_util_pct: { type: 'string', description: 'the measured GPU utilization during the sweep (sampled nvidia-smi) + the CPU usage, honestly' },
    no_cpu_hotloop: { type: 'boolean', description: 'git-diff-verified: no host enumeration/filter/per-item loop on the reported-number path' },
    no_oom: { type: 'boolean' }, goldens_exact: { type: 'boolean' }, reuses_batching: { type: 'boolean', description: 'the sweep reuses assemble_f4_quartets (not a forked parallel compute)' },
    emulated_fp64: { type: 'boolean', description: 'EmulatedFp64{40} is the default for the matmul-heavy f4; native only fallback/carve-out' },
    cap_filter_ok: { type: 'boolean' }, build_clean: { type: 'boolean' },
    commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the GPU-util numbers (the proof), no-OOM, goldens-exact, the reuse; on FAIL the blocker + minor-vs-bad. If CPU-bound anywhere, SAY SO explicitly.' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for the GPU-only f-stat sweep (adversarial; the user is FURIOUS about a prior CPU-bound build — hold the GPU-BOUND bar HARD; if it is CPU-bound/GPU-idle anywhere, that is a FAIL, not a caveat). The implementer reported:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — the sweep enumerates+filters+compacts ON-DEVICE (CUB DeviceSelect), reuses assemble_f4_quartets, NO host per-item/enumeration/filter loop; emulated-FP64 default; diagonal jackknife for the SE. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; existing goldens EXACT (independently confirm no golden shifted). (3) GPU-BOUND PROOF (the headline gate): run a REAL sweep on the box while SAMPLING nvidia-smi (e.g. `nvidia-smi dmon -s u -c 30` or --query-gpu=utilization.gpu in a loop) — the GPU MUST be saturated (high sustained util), the run MUST NOT be CPU-bound. Report the actual util numbers. If GPU-idle/CPU-bound -> FAIL. (4) no OOM on a large real sweep; cap fires; filter works. (5) single-GPU, no synthetic, no golden regen. PASS only if ALL hold + GPU-bound is PROVEN. ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed/new source/test/cmake/binding/doc (NEVER git add dot; never aadr/ atlas_results/ handoff-*.md; NO golden files change), commit (feat(f-stats): GPU-only sweep — on-device unrank-enumerate + assemble_f4_quartets (emulated-FP64) + on-device |z| filter + CUB stream-compact, survivors-only D2H; diagonal jackknife OOM fix; reuses the existing batching; cap + user filter; PROVEN GPU-bound (util X%); goldens exact; single-GPU) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs (the GPU-bound numbers). ',
  'ON FAIL: DO NOT git checkout/clean. ' + STASH + ' "wip:gpu-sweep-FAILED" (capture the ref). Classify fail_severity (bad if CPU-bound/GPU-idle or a golden shifted). Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:gpu-sweep', phase: 'Verify GPU-BOUND + no-OOM + goldens-exact + commit-or-stash' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ GPU-only sweep ' + verdict.commit_hash + ' — GPU util ' + verdict.gpu_util_pct + ' — ' + verdict.note)
else log('--- GPU sweep FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — ' + verdict.note)
return { design, verdict }
