export const meta = {
  name: 'm4.5-why-d2h',
  description: 'Answer the architectural question: WHY is there a Device->Host copy of f2_blocks at all? Is it fundamental (the consumer is host/disk-side: precompute-once/fit-many, on-disk cache, AT2-compat, CPU fit) or incidental (an artifact of the precompute/fit phase boundary that a fused on-GPU pipeline keeping f2_blocks resident in VRAM would eliminate)? READ-ONLY, three lenses — the deliverable contract + its design rationale, the actual consumer/data-flow after precompute, and the could-it-be-eliminated/fused analysis — then a synthesis that gives a clear honest answer + the conditions under which the D2H goes away. Writes docs/cleanup/m4.5/why-d2h.md.',
  phases: [
    { title: 'Investigate', detail: 'three read-only lenses on the deliverable contract, the consumer, and the fused-pipeline alternative' },
    { title: 'Answer', detail: 'synthesis: why the D2H exists + whether/when it can be removed -> doc' },
  ],
}

const R = '/home/suzunik/steppe'

const STD = [
  'PROJECT: steppe = CUDA-13/Blackwell reimpl of ADMIXTOOLS 2 f-statistics, precompute-once / fit-many. The PRECOMPUTE produces f2_blocks[P x P x n_block] FP64 (per-block jackknife tensor). It is computed on the GPU, then COPIED Device->Host (D2H) into a host-resident F2BlockTensor (std::vector<double> f2/vpair, include/steppe/fstats.hpp). The recent perf work showed that D2H is the dominant cost / the speedup wall. The user asks the DEEPER question: WHY is there a D2H at all — why does the result leave the GPU?',
  'YOUR JOB: answer it from the DESIGN + CODE, not assertion. Is the D2H FUNDAMENTAL (the deliverable is contractually a host/on-disk artifact consumed by a host-side or persisted fit-many phase) or INCIDENTAL (an artifact of the current precompute/fit phase split + the fit engine not existing yet, which a fused on-GPU precompute+fit keeping f2_blocks resident in VRAM would eliminate)? Quantify/cite. Be honest about what the design actually says vs what is just current convenience.',
  'KEY SOURCES (read, cite file:line / section): docs/architecture.md (THE spec — find the rationale for the precompute-once/fit-many split, where f2_blocks is contractually supposed to LIVE, the §11 VRAM/budget reasoning, the on-disk cache / M7 milestone, ADMIXTOOLS-2 compatibility of the f2_blocks artifact, whether a device-resident or fused pipeline is contemplated or excluded), docs/ROADMAP.md (the M0-M7 phases + where the FIT engine sits — Phase 2 — and the precompute/fit boundary), docs/TODO.md, include/steppe/fstats.hpp (F2BlockTensor = host vectors — the deliverable shape), src/core/fstats/f2_blocks_multigpu.cpp + src/device/cuda/cuda_backend.cu (where + why the D2H happens), the API/CLI layer (what RETURNS/CONSUMES the host F2BlockTensor — grep for f2 consumers, qpadm, fit, f3, f4), docs/cleanup/m4.5/architecture-audit.md (Flaw 4 already argues the full-resident-host model is wrong for the block-by-block consumer) and why-multigpu-slow.md / scaling-sweep.md (the D2H is the measured wall).',
].join('\n')

phase('Investigate')
const LENSES = [
  { key: 'deliverable-contract', focus: 'WHAT the precompute deliverable contractually IS and WHY it lives on host/disk. From architecture.md + ROADMAP: is f2_blocks DESIGNED to be a host-resident (then on-disk, AT2-compatible, cacheable) artifact? Why — precompute-once/fit-many persistence, reuse across many qpAdm models, the M7 on-disk cache, the assumption that "f2_blocks is tiny/MB-scale and cacheable" (true at AT2 P~tens, false at P=2500)? Quote the design rationale for the precompute/fit SPLIT and where the result is supposed to live. Is the D2H mandated by the contract, or just the current return type?' },
  { key: 'consumer-dataflow', focus: 'WHAT actually consumes the host F2BlockTensor after precompute, and on which side (host/device). Does a fit engine exist or is it only designed (Phase 2)? Trace: the D2H site -> the host F2BlockTensor -> who reads f2/vpair (grep f3/f4/qpadm/fit/jackknife consumers). Does anything keep or want f2_blocks on the DEVICE? Is the consumer CPU-side (=> needs host), GPU-side (=> could stay resident), or unbuilt (=> the host return is a placeholder default, not a requirement)? Is the D2H feeding a real host consumer, a disk write, or just "the API returns a host vector because that is the current seam"?' },
  { key: 'eliminate-or-fuse', focus: 'COULD the D2H be eliminated, and under what model. Describe the fused on-GPU precompute+fit pipeline that keeps f2_blocks RESIDENT in VRAM and runs the qpAdm fit (which reads small pair-subsets, block-by-block jackknife) directly from device memory — no D2H. What makes that viable (the fit reads tiny subsets; f2_blocks fits VRAM at moderate P) vs what breaks it (VRAM ceiling at large P — the scaling sweep; disk persistence/caching across sessions; a CPU-side fit; multi-dataset reuse). Is the D2H FUNDAMENTAL (persistence + host/CPU consumer + scale-beyond-VRAM) or INCIDENTAL (phase boundary + unbuilt fit)? What is the IDEAL data flow — fused-resident for the fits-in-VRAM case, streamed/on-disk only when it must persist or exceed VRAM? Connect to audit Flaw 4 (block-by-block consumer) + the measured D2H wall.' },
]
const lenses = await parallel(LENSES.map((l) => () => agent([
  'You are a senior GPU/HPC architect answering, READ-ONLY and from EVIDENCE, ONE lens of "why is there a D2H of f2_blocks at all." Cite file:line / architecture.md section for every claim. Do NOT edit code. Distinguish what the DESIGN mandates from what is merely current convenience.', STD, '',
  'YOUR LENS: ' + l.key + '\n' + l.focus, '',
  'Return: (1) the evidence-cited findings for your lens; (2) your lens\'s answer to "is the D2H fundamental or incidental, and why"; (3) what the ideal data flow would be for your lens. Be concrete and honest.',
].join('\n'), { label: 'lens:' + l.key, phase: 'Investigate' }))).then(a => a.filter(Boolean))

phase('Answer')
const answer = await agent([
  'You are the lead architect. Answer the user\'s question — WHY is there a D2H of f2_blocks at all — clearly, honestly, and from the evidence. WRITE it to ' + R + '/docs/cleanup/m4.5/why-d2h.md (Write tool). READ-ONLY on code.', STD, '',
  'THE THREE LENS FINDINGS:\n<<<\n' + lenses.map((a,i)=>'### '+LENSES[i].key+'\n'+a).join('\n\n') + '\n>>>', '',
  'The doc must answer, in plain terms: (1) THE DIRECT ANSWER — why the D2H exists today, in 2-3 sentences (the deliverable is a host-resident F2BlockTensor because <reason from evidence>); (2) is it FUNDAMENTAL or INCIDENTAL — cite the design: which parts of the D2H are mandated (persistence to an on-disk AT2-compatible cache, a host/CPU-side or cross-session fit-many, scale beyond VRAM) vs which are just the current phase boundary / unbuilt fit engine / default return type; (3) the FUSED alternative — when f2_blocks fits VRAM and the fit runs on-GPU reading small pair-subsets block-by-block, there need be NO D2H (the fit reads device memory directly); what that pipeline looks like and what it would save (the measured D2H wall); (4) when the D2H is genuinely UNAVOIDABLE (persist/cache, CPU consumer, P beyond VRAM -> stream to host/disk, ideally GDS device->disk skipping host); (5) the recommendation — does this change the roadmap (e.g. design the fit engine to consume f2_blocks device-resident for the in-VRAM case; treat host/disk as the persistence path, not the default)? Tie to audit Flaw 4 + the measured numbers. Be honest where the design is silent vs explicit.',
  'Return a tight 6-10 line executive summary that directly answers the user: why the D2H is there, whether it is fundamental or an artifact, and when it could be removed (fused on-GPU pipeline).',
].join('\n'), { label: 'answer-why-d2h', phase: 'Answer' })

return { lenses, answer }
