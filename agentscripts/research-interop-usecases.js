export const meta = {
  name: 'research-interop-usecases',
  description: 'Research the PRACTICAL use cases for a numpy/CuPy/PyTorch/DLPack array-interop seam in steppe\'s Python bindings (the PyCUDA-verdict follow-up: nanobind + a DLPack/__cuda_array_interface__ seam — but WHAT is it actually for?). Map every interop avenue to steppe\'s THREE real data objects — genotype INPUT, the f2_blocks INTERCHANGE (DeviceF2Blocks in VRAM / F2BlockTensor on host), and RESULTS OUT (QpAdmResult weights/se/z/p + rankdrop/popdrop) — and tie each to a CONCRETE popgen workflow. Explore the whole ecosystem: numpy + pandas (host), CuPy + PyTorch (GPU, DLPack/CAI zero-copy), JAX/numba/cuDF, and popgen-specific (scikit-allel, tskit, anndata, admixr/AT2 conventions). HONEST/adversarial: separate REAL value (results->pandas, f2 host numpy, genotype-array input) from speculative (GPU zero-copy f2 for power users), rank by value x effort, and recommend exactly what the M(py-1) bindings should expose. 4 parallel lenses -> a synthesis use-case+recommendation doc. Writes /tmp/interop-usecases.md (NOT the repo — a concurrent CLI build CLEANs the tree; the orchestrator commits it to docs/research/ afterward). NO box, NO repo edits, NO git.',
  phases: [ { title: 'Research', detail: '4 parallel lenses: results-out / f2-interchange / genotype-in / protocols+ecosystem' }, { title: 'Synthesize', detail: 'practical use-case + value×effort ranking + M(py-1) recommendation -> /tmp/interop-usecases.md' } ],
}

const OUT = '/tmp/interop-usecases.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell C++20 reimplementation of ADMIXTOOLS 2 + qpAdm. The fit backend is FINISHED; step 2 = a CLI (building now) + Python bindings (nanobind, ADR-0002). The PyCUDA research concluded: nanobind-only binding + a DLPack/__cuda_array_interface__ (CAI) INTEROP SEAM realized ON the nanobind module (nb::ndarray / nb::device::cuda, zero new deps) — NO PyCUDA. THIS research answers: what is that interop seam practically FOR, across the Python array ecosystem, and what should M(py-1) actually expose.',
  'steppe\'s THREE data objects the interop touches: (1) GENOTYPE INPUT — currently read from EIGENSTRAT/PACKEDANCESTRYMAP/PLINK files by the io leaf, decoded to per-pop Q/V/N, fed to the f2 precompute; (2) the f2_blocks INTERCHANGE — DeviceF2Blocks (resident in VRAM, P x P x n_block FP64, the precompute-once/fit-many artifact) and F2BlockTensor (the host mirror, opt-in to_host()); plus the on-disk STPF2BK1 cache + the AT2-compatible f2 dir; (3) RESULTS OUT — QpAdmResult{weight,se,z,p,chisq,dof,f4rank,feasible,rankdrop_*,popdrop_*,status} and the rotation\'s vector of them. The precompute-once/fit-many design (ADR-0005): one expensive f2, then thousands of cheap fits.',
  'THE QUESTION: what are the PRACTICAL, real-popgen-workflow use cases for an array-interop seam at each of those three objects, across numpy/pandas/CuPy/PyTorch/JAX/numba/cuDF + the popgen-specific stack (scikit-allel, tskit, anndata, admixr/ADMIXTOOLS conventions)? Be HONEST/adversarial — distinguish genuine value from "zero-copy because we can". Rank by value x implementation effort. Recommend what M(py-1) ships (must / should / nice-to-have) and what is YAGNI.',
  'READ-ONLY: web research (ToolSearch select:WebSearch,WebFetch) for the ecosystem + popgen-tool conventions; you MAY skim the steppe headers (include/steppe/{qpadm,fstats}.hpp, src/device/device_f2_blocks.hpp) for the exact data shapes. NO box, NO repo edits, NO git. Deliverable = /tmp/interop-usecases.md. Cite real sources; no fabrication; flag the FP64 caveat (PyTorch/JAX default fp32; steppe is fp64) where relevant.',
].join('\n')

phase('Research')
const LENSES = [
  { key: 'results-out', focus: 'RESULTS interop (the everyday, highest-frequency use). qpAdm/qpWave/rotation results -> pandas DataFrame / numpy arrays / a typed object. The concrete workflows: a researcher runs a rotation of thousands of models and wants the ranked feasibility table as a pandas DataFrame to sort/filter/plot (admixr returns tibbles for exactly this); weights/se/z/p as columns; the rankdrop/popdrop tables as frames. Compare to admixr (R) + the AT2 output ergonomics users expect. Is this the #1 must-have for a usable Python package? What is the right return shape (DataFrame per result-type vs a dataclass vs dict)? Value + effort (host-side, numpy/pandas, easy). Cite admixr/AT2 result conventions.' },
  { key: 'f2-interchange', focus: 'The f2_blocks INTERCHANGE interop — the most interesting + most debatable. (a) HOST: F2BlockTensor as a numpy array (P,P,n_block) — use cases: inspect/QC the f2 tensor, load a precomputed f2 from another tool/format, compute custom downstream stats in numpy, serialize to npz/zarr. (b) GPU zero-copy: DeviceF2Blocks (VRAM) exposed as a DLPack / __cuda_array_interface__ tensor consumable by CuPy/PyTorch WITHOUT a host round-trip — use cases: keep the (possibly 10s of GB) f2 RESIDENT and run many fits from a notebook (precompute-once/fit-many driven from Python), or let a power user run their OWN custom f-stat / GPU linear algebra on the resident f2 in CuPy without copying to host (real for large P where to_host is GBs). HONESTLY assess: does the typical user ever need the raw f2 tensor, or just call qpadm/rotate? Is the GPU zero-copy a power-user feature or YAGNI? The stream-sync + ownership + FP64 caveats. Rank both (host-numpy vs GPU-DLPack) by value x effort.' },
  { key: 'genotype-in', focus: 'GENOTYPE INPUT interop — feed the precompute (extract_f2) from an IN-MEMORY array instead of EIGENSTRAT/PLINK files. Use cases: a user whose genotypes are already in numpy/CuPy/PyTorch from another pipeline (QC, imputation, simulation) wants extract_f2 directly without writing files; integration with the popgen Python stack — scikit-allel (GenotypeArray), tskit (tree-sequence genotypes / simulated data from msprime), anndata/scanpy-style matrices, sgkit/xarray. The concrete workflows: simulate with msprime/tskit -> steppe f2 -> qpAdm (a methods-paper / power-analysis loop); or an existing scikit-allel QC pipeline -> steppe. HONESTLY assess value (real integration win vs file IO is already fine) + effort (steppe io is file-oriented; an array-input path is new) + the format/ploidy/missing-data mapping (steppe needs Q/V/N with ploidy + validity — does a raw genotype array carry enough?). Cite scikit-allel/tskit/sgkit genotype representations.' },
  { key: 'protocols-ecosystem', focus: 'The PROTOCOLS + ecosystem mechanics. DLPack vs __cuda_array_interface__ (CAI) vs the numpy buffer protocol: which Python libs SPEAK which (numpy=buffer; CuPy=CAI+DLPack; PyTorch=DLPack; JAX=DLPack; numba=CAI; cuDF/RAPIDS=DLPack/CAI), so ONE DLPack seam covers most GPU frameworks vs per-lib code. How nanobind exposes these: nb::ndarray<> with the framework tag (numpy/pytorch/cupy/jax) + nb::device::cpu/cuda, and the import/export (consuming a foreign array; producing a steppe-owned one with a custom deleter — the recipe the PyCUDA report said to SPIKE first). The hard gotchas: device-pointer ownership/lifetime (who frees the VRAM), STREAM synchronization across frameworks (the validation target), dtype (steppe FP64 vs torch/jax fp32 defaults), contiguity/layout (column-major f2 vs numpy C-order). Recommend the protocol(s) to support + the must-spike risks. Cite the DLPack/CAI specs + nanobind ndarray docs.' },
]
const found = await parallel(LENSES.map(L => () => agent([
  'You are a scientific-Python + GPU-interop research analyst evaluating practical interop for steppe, HONEST + adversarial (separate real value from zero-copy-for-its-own-sake): ' + L.focus, '', STD,
  '', 'Load web tools (ToolSearch select:WebSearch,WebFetch), search + FETCH primary sources (nanobind ndarray docs, DLPack/CAI specs, CuPy/PyTorch/JAX/numba interop docs, scikit-allel/tskit/sgkit genotype docs, admixr). You MAY skim the steppe headers for shapes. Return: the concrete use cases for this lens (each tied to a real popgen workflow), value (high/med/low) x effort (S/M/L), the caveats, and what M(py-1) should expose for it. Cite real URLs. No fabrication.',
].join('\n'), { label: 'research:' + L.key, phase: 'Research' })))

phase('Synthesize')
const report = await agent([
  'You are the lead synthesizer of the steppe Python-interop USE-CASE + recommendation report. Combine the 4 lenses into one practical, ranked answer.', '', STD,
  '', 'THE LENSES:\n' + found.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'WRITE ' + OUT + ' (in /tmp, NOT the repo) with: (1) the MENTAL MODEL — interop at steppe\'s 3 data objects (genotype IN / f2 INTERCHANGE / results OUT) x the ecosystem (numpy/pandas/CuPy/PyTorch/JAX/numba/cuDF + scikit-allel/tskit/sgkit). (2) THE PRACTICAL USE CASES — a table: avenue | data object | concrete popgen workflow | library | value (H/M/L) | effort (S/M/L) | verdict (M(py-1) must / should / nice / YAGNI). (3) The honest split: what is genuinely valuable (likely: results->pandas [must], f2 host numpy [must/should], genotype-array input incl scikit-allel/tskit [should — real pipeline integration], DLPack f2 GPU zero-copy [should/nice — power users + large P]) vs speculative. (4) The PROTOCOL recommendation (DLPack as the primary GPU seam + numpy buffer for host; CAI where needed) + the must-SPIKE risks (VRAM ownership/deleter, stream sync, FP64). (5) The concrete M(py-1) interop scope + what to defer. Use the Write tool to write ' + OUT + '. Do NOT git-commit / touch the repo. Return the headline (the must-haves + the one or two real GPU-interop use cases) so the user sees the value at a glance.',
].join('\n'), { label: 'synthesize:interop', phase: 'Synthesize' })
log('interop use-cases -> /tmp/interop-usecases.md: ' + String(report).slice(0, 200))
return { report }
