export const meta = {
  name: 'gpu-large-models',
  description: 'Make the GPU qpAdm fit + rank test run ARBITRARY model sizes — lift the on-device kernels cap (kQpMaxNl=5, kQpMaxNr=10 in qpadm_fit_kernels.cu: one-thread-per-model with compile-time-fixed local arrays Vfull[MAXNR*MAXNR] etc.) that currently makes models with nl>5 or nr>10 UNRUNNABLE on the GPU. The 9-pop golden (nr=5) runs on GPU today; the nr=39 NRBIG fixture (and any real-world >10-outgroup model) does NOT — it overflows the fixed per-thread arrays, so it was only CPU-oracle-validated. FIX: route the SVD through cuSOLVER (gesvdj/gesvdjBatched for nl,nr<=32; gesvd for >32; the CusolverDnHandle already on CudaBackend from d6d3cbb) and size the ALS/weight working set dynamically (device workspace, not per-thread local arrays) so arbitrary nl/nr runs ON THE GPU resident. GATE: BOTH fixtures run on the GPU (CUDA backend, f2 resident) and match their AT2 goldens — the 9-pop golden_fit0 (nr=5, NO regression) AND golden_fit1_NRBIG (nr=39). The CpuBackend is the diff oracle only. Design -> GPU implement -> INDEPENDENT verdict. commit-green / revert+HALT.',
  phases: [
    { title: 'Design', detail: 'freeze how to lift the cap: cuSOLVER SVD dispatch (gesvdj/gesvd) + dynamic ALS/weight device workspace; keep the 9-pop bit-parity (small fast-path or unify); read-only' },
    { title: 'GPU', detail: 'implement arbitrary-size GPU rank-sweep/fit on box5090: cuSOLVER SVD + dynamic workspace; run BOTH fixtures resident; to green vs both goldens' },
    { title: 'Verify', detail: 'independent verdict: BOTH the 9-pop (no regression) and the nr=39 NRBIG run ON THE GPU resident and match their AT2 goldens; build/ctest green; commit-green / revert+HALT' },
  ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -30 && echo === CTEST === && ctest --test-dir build-rel --output-on-failure 2>&1 | tail -45'"
const QPTEST = SSH + " 'cd /workspace/steppe && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0 && ctest --test-dir build-rel -R qpadm --output-on-failure 2>&1 | tail -90'"
const CLEAN = 'cd ' + R + ' && git checkout -- . && git clean -fd src tests include docs'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell (sm_120) reimplementation of ADMIXTOOLS 2. Phase-2 qpAdm fit engine. Branch phase2-fit-engine @ da126b1 (M(fit-2) rank test on GPU just landed). The GPU qpAdm fit (M(fit-4)) + rank sweep (M(fit-2)) are ON THE GPU and match the AT2 9-pop goldens — but ONLY for SMALL models.',
  'THE PROBLEM (the real reason large models are not executed on the GPU): src/device/cuda/qpadm_fit_kernels.cu uses ONE-THREAD-PER-MODEL kernels with COMPILE-TIME-FIXED per-thread local arrays — kQpMaxNl=5, kQpMaxNr=10, kQpMaxM=50, kQpMaxR=4 (lines 41-44); dev_jacobi_svd_V / seed_AB / als / weights_chisq are templated on MAXNL/MAXNR/MAXR and declare double Vfull[MAXNR*MAXNR], W[MAXNL*MAXNR], sigma[MAXNR], etc. in LOCAL memory. A model with nl>5 or nr>10 does not fit (e.g. nr=39 => Vfull is 39*39=1521 doubles/thread) and the templates are not even instantiated that big. So the nr=39 NRBIG fixture — and ANY real-world model with >10 outgroups — CANNOT run on the current GPU kernels; it was only CpuBackend-oracle-validated. The code comment (line ~56) says the properly-resident large path needs the kernels extended. cuSOLVER is the path to arbitrary sizes (real device workspaces).',
  'HARD RULE (the user, emphatic; memory build-the-real-gpu-implementation + fit-phase2-progress): the milestone is COMPLETE only when the GPU version actually RUNS the work. CPU-oracle validation is just the parity step. So the deliverable here is: the GPU (CUDA backend) actually EXECUTES the rank test/fit for the large model (nr=39) resident and matches the golden — not a CPU result, not a "pending seam".',
  'WHAT TO BUILD: lift the small-size cap so the GPU qpAdm fit + rank_sweep handle ARBITRARY nl/nr. (1) Route the SVD through cuSOLVER: the CusolverDnHandle (solver_) is ALREADY on CudaBackend (d6d3cbb); use cusolverDnDgesvdj / gesvdjBatched for nl,nr<=32 and cusolverDnDgesvd for >32 (orient X rows>=cols; deterministic; single statistic stream) IN PLACE OF the fixed-size dev_jacobi_svd_V for models that exceed the small cap (or uniformly — design decides, see precision/parity). (2) Size the ALS opt_A/opt_B + weight-solve + chisq working set DYNAMICALLY — a device workspace buffer sized at runtime from nl/nr/m/r (cudaMalloc/the existing workspace_ pool or a per-call DeviceBuffer), NOT per-thread fixed local arrays — so the rank-r ALS + the Qinv quadratic form run for any m=nl*nr. (3) Keep it RESIDENT (reads DeviceF2Blocks in VRAM) and batched-capable (the design should not preclude S8 batching). The small models must STILL work.',
  'PARITY / PRECISION (§12, the unified policy 8ace724): the SVD + Q^{-1} + chi^2 quadratic form are native FP64 (cuSOLVER has no FP64-emulated mode; ill-conditioned). The well-conditioned covariance SYRK stays EmulatedFp64{40} default (unchanged). CRITICAL: the 9-pop golden (golden_fit0) must STILL pass on the GPU within the UNCHANGED tiers (weights rtol~1e-6, chisq, dof, p, rankdrop/popdrop). If switching the 9-pop SVD from the on-device Jacobi to cuSOLVER shifts bits, it must still hold the golden tier — OR keep the small on-device fast-path for nl<=5,nr<=10 (bit-parity preserved) and use cuSOLVER ONLY for the large path. The design picks; either way NO tier is loosened.',
  'GATE (must-pass, the completeness bar): BOTH fixtures RUN ON THE GPU (CUDA backend, f2 RESIDENT, no host round-trip) and match their AT2 goldens: (a) the 9-pop golden_fit0 (nl=2, nr=5) — f4rank=1, rankdrop rank1 {dof=4, chisq 4.6352 rtol~1e-6, p 0.327}, weights 0.5589/0.4411, popdrop tables — NO REGRESSION; (b) golden_fit1_NRBIG (nl=2, nr=39, P=43, nb=701) — f4rank=1, the rankdrop dof 38/78 + dofdiff 40 + chisq (52.7043 / 190.836 rtol~1e-6) + chisqdiff, the popdrop 00/01/10 — MATCHED RUNNING ON THE GPU (not the CpuBackend oracle). test_qpadm_parity.cu must EXECUTE the CUDA backend rank_sweep on the NRBIG fixture (today it only runs the CpuBackend oracle on it — lines ~338-355; add the GPU run + assertions) and assert the GPU path (f2 resident, real GPU).',
  'BOX = box5090 (vast 2x RTX 5090, UP, sm_120, CUDA 13). ' + SSH + ' (alias); nvcc -> ' + PATHENV + ' . build-rel exists. RELEASE only. NOTHING builds locally. BOTH fixtures on the box: tests/reference/goldens/at2/fixtures/f2_fit0_9pop.bin (nr=5) + f2_fit1_NRBIG.bin (nr=39) + the goldens golden_fit0.json + golden_fit1_NRBIG.json.',
  'KEY FILES: src/device/cuda/qpadm_fit_kernels.cu/.cuh (the kQpMax* caps + the fixed-local-array kernels — the thing to lift: cuSOLVER SVD + dynamic workspace), src/device/cuda/cuda_backend.cu (rank_sweep @ ~1540 + the fit virtuals + the solver_ handle + workspace_; the SVD dispatch), src/device/cuda/handles.hpp (CusolverDnHandle — add gesvdj/gesvd buffer helpers if needed), src/core/qpadm/ranktest.* (host orchestrator — unchanged dispatch through the backend), tests/reference/test_qpadm_parity.cu (ADD the CUDA-backend run on the NRBIG fixture + assertions vs golden_fit1_NRBIG), docs/design/fit-engine.md (note the GPU now runs arbitrary sizes via cuSOLVER).',
].join('\n')

const DEVLOOP = 'DEV LOOP: edit locally; rsync (' + RSYNC + '); RELEASE build+ctest (' + BUILD + '); the qpAdm test (' + QPTEST + '). Iterate until build clean + full ctest green + BOTH fixtures pass ON THE GPU vs their goldens. Do NOT commit (the verdict commits). Clean revert: ' + CLEAN + '.'

phase('Design')
const design = await agent([
  'You are the lead CUDA architect. Freeze the contract to lift the GPU qpAdm small-size cap so the fit + rank_sweep run ARBITRARY nl/nr on the GPU resident, via cuSOLVER SVD + dynamic device workspace. READ-ONLY (read qpadm_fit_kernels.cu/.cuh incl. the kQpMax* caps + the templated fixed-local-array kernels, cuda_backend.cu rank_sweep + the fit virtuals + solver_/workspace_, handles.hpp CusolverDnHandle, ranktest.*, test_qpadm_parity.cu incl. the NRBIG block ~338-355, the goldens; do NOT edit, do NOT touch the box).', STD, '',
  'Specify with file:line + signatures: (1) the cuSOLVER SVD dispatch to REPLACE/augment dev_jacobi_svd_V — cusolverDnDgesvdj/gesvdjBatched (nl,nr<=32) vs cusolverDnDgesvd (>32), the workspace query (gesvdj_bufferSize), X orientation rows>=cols, determinism + single stream; (2) the DYNAMIC device workspace for the ALS opt_A/opt_B + weight solve + chisq (sized at runtime from nl/nr/m/r) replacing the per-thread fixed local arrays — where it allocates (workspace_ pool vs a per-call DeviceBuffer), the kernel restructure (still one-model-per-launch is fine for now, but the arrays move to global memory; preserve batched-capability for S8); (3) the parity strategy: EITHER keep the small on-device Jacobi fast-path for nl<=5,nr<=10 (bit-parity preserved) + cuSOLVER for larger, OR unify on cuSOLVER (then PROVE the 9-pop golden still holds tier) — pick ONE and justify; (4) the precision map (SVD/Qinv/chi^2 native; SYRK emulated{40} default — unchanged); (5) the test extension: run the CUDA-backend rank_sweep on the NRBIG fixture (resident) + assert vs golden_fit1_NRBIG, plus the 9-pop no-regression. The implementer makes NO design decisions. GPU-first, resident, batched-capable; the CpuBackend is the oracle, NOT the deliverable.',
].join('\n'), { label: 'design:gpu-large', phase: 'Design' })

phase('GPU')
const gpu = await agent([
  'You are a senior CUDA engineer. Lift the GPU qpAdm small-size cap per the FROZEN design so the fit + rank_sweep run arbitrary nl/nr ON THE GPU resident (cuSOLVER SVD + dynamic device workspace), and dev-loop on box5090 until BOTH fixtures pass on the GPU vs their goldens. EDIT locally + dev-loop on the box; do NOT commit.', STD, '',
  'THE FROZEN DESIGN:\n<<<\n' + (design || '(missing — derive: route SVD through cusolverDnDgesvdj/gesvd, move the ALS/weight/chisq working arrays from per-thread local to a runtime-sized device workspace; keep the 9-pop bit/tier parity; run the NRBIG fixture on the CUDA backend)') + '\n>>>', '', DEVLOOP, '',
  'Build it: the cuSOLVER SVD dispatch + the dynamic device workspace so models with nl>5 / nr>10 (incl. nr=39) RUN ON THE GPU; extend test_qpadm_parity.cu to EXECUTE the CUDA-backend rank_sweep on f2_fit1_NRBIG.bin (resident) and assert vs golden_fit1_NRBIG.json. Iterate until: build clean (warnings-as-errors), full ctest green, AND BOTH fixtures pass ON THE GPU (f2 resident) vs their goldens — the 9-pop (no regression: weights 0.5589/0.4411, f4rank=1, rankdrop/popdrop) AND the nr=39 NRBIG (f4rank=1, dof 38/78, chisq 52.7043/190.836 rtol~1e-6, popdrop 00/01/10). Report: git diff --stat; the cuSOLVER SVD + dynamic-workspace code; the GPU results for BOTH fixtures vs their goldens (per quantity); confirmation BOTH ran on the GPU resident (cuSOLVER/kernels, no host round-trip). Do NOT commit. If the large fixture diverges on the GPU, localize with the CPU oracle + report the real delta — do NOT fabricate, do NOT loosen tiers, do NOT fall back to reporting the CPU result as the GPU result.',
].join('\n'), { label: 'impl:gpu-large', phase: 'GPU' })

const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','diff_is_real','cap_lifted','nrbig_runs_on_gpu','nrbig_matches_golden','small_no_regression','resident_no_roundtrip','build_green','gpu_results','commit_hash','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if: real diff lifting the cap (cuSOLVER SVD dispatch + dynamic device workspace, not the fixed kQpMax* local arrays) + Release build clean + full ctest green + BOTH fixtures RUN ON THE GPU resident and match their AT2 goldens — the 9-pop (NO regression) AND golden_fit1_NRBIG (nr=39) — with the nr=39 rank test genuinely EXECUTED on the CUDA backend (NOT the CpuBackend oracle). NO tier loosened.' },
    diff_is_real: { type: 'boolean', description: 'you re-ran git diff: real cuSOLVER gesvdj/gesvd SVD + runtime-sized device workspace replacing the per-thread fixed local arrays; not a stub' },
    cap_lifted: { type: 'boolean', description: 'the kQpMaxNl=5/kQpMaxNr=10 fixed-local-array limitation is genuinely removed for the GPU path (the large model no longer overflows)' },
    nrbig_runs_on_gpu: { type: 'boolean', description: 'the nr=39 NRBIG rank test EXECUTES on the CUDA backend (GPU cuSOLVER/kernels, f2 resident) — you confirmed it is NOT the CpuBackend oracle' },
    nrbig_matches_golden: { type: 'boolean', description: 'the GPU nr=39 result matches golden_fit1_NRBIG (f4rank exact, dof/dofdiff exact, chisq/chisqdiff rtol~1e-6, popdrop rows) within tier' },
    small_no_regression: { type: 'boolean', description: 'the 9-pop golden_fit0 STILL passes on the GPU within the UNCHANGED tiers (weights/f4rank/rankdrop/popdrop)' },
    resident_no_roundtrip: { type: 'boolean', description: 'both fits read f2 RESIDENT (DeviceF2Blocks); no per-model host round-trip of f2' },
    build_green: { type: 'boolean', description: 'Release build clean (warnings-as-errors) + full ctest green (you re-ran)' },
    gpu_results: { type: 'string', description: 'the YOU-measured GPU-path results for BOTH fixtures vs their goldens (per quantity)' },
    commit_hash: { type: 'string' },
    note: { type: 'string', description: 'what landed; both GPU-vs-golden results; confirmation the nr=39 ran on the GPU resident; for FAIL exactly what diverged or where it fell back to CPU' },
  },
}
phase('Verify')
const verdictPrompt =
  'You are the INDEPENDENT VERDICT (maximally adversarial — the user is emphatic the GPU must ACTUALLY EXECUTE the large model, not defer to the CPU oracle; prior milestones shipped the large path as "pending"). The GPU implementer reported:\n<<<\n' + (gpu || '(gpu phase died)') + '\n>>>\n\n' + STD + '\n\n' +
  'DO, yourself: (1) cd ' + R + ' && git --no-pager diff --stat && git --no-pager diff — CONFIRM the cap is genuinely lifted: real cuSOLVER gesvdj/gesvd SVD + a runtime-sized device workspace replacing the per-thread fixed local arrays (kQpMax*); the test EXECUTES the CUDA-backend rank_sweep on the NRBIG fixture (resident), NOT just the CpuBackend oracle. If the nr=39 result is secretly the CpuBackend oracle dressed up as GPU => pass=false. (2) RE-RUN yourself: ' + BUILD + ' ; ' + QPTEST + ' . (3) PASS only if ALL: diff real (cuSOLVER + dynamic workspace); Release build clean; full ctest green; the 9-pop golden STILL passes on the GPU (no regression, unchanged tiers); AND the nr=39 NRBIG rank test EXECUTES ON THE GPU resident and matches golden_fit1_NRBIG (f4rank exact, dof/dofdiff exact, chisq/chisqdiff rtol~1e-6, popdrop rows). NO tier loosened. \n\nON PASS: cd ' + R + ' && git add ONLY the genuinely-changed source+test+doc files (NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md); commit with a ROADMAP §6 message (the GPU qpAdm fit/rank-test now runs ARBITRARY model sizes via cuSOLVER + dynamic workspace; the nr=39 model executed on the GPU vs golden_fit1_NRBIG; 9-pop no regression) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash.\nON FAIL: ' + CLEAN + ' ; report exactly what diverged or where it fell back to CPU.\nReturn the structured verdict.'

async function tryAgent(p, opts) {
  let r = await agent(p, opts)
  if (r === null) { log(opts.label + ': transient null — retry once'); r = await agent(p, { ...opts, label: opts.label + ':retry' }) }
  return r
}
const verdict = await tryAgent(verdictPrompt, { schema: VERDICT_SCHEMA, label: 'verdict:gpu-large', phase: 'Verify' })

if (verdict && verdict.pass) log('+++ GPU ARBITRARY-SIZE LANDED ' + verdict.commit_hash + ' — nrbig_on_gpu=' + verdict.nrbig_runs_on_gpu + ' nrbig_matches=' + verdict.nrbig_matches_golden + ' no_regression=' + verdict.small_no_regression + ' | ' + verdict.gpu_results)
else log('--- GPU ARBITRARY-SIZE FAILED (' + (verdict ? verdict.note : 'agent died') + ') — HALT; human takes over')
return { design, gpu, verdict }
