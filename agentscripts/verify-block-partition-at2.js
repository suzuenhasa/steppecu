export const meta = {
  name: 'verify-block-partition-at2',
  description: 'VERIFY the EXACT ADMIXTOOLS block-jackknife partition algorithm and pin precisely how steppe assign_blocks differs — so we can reconcile steppe to AT2 parity (709 blocks, not 718, on the Haak v66 union). READ-ONLY: do NOT change code. Three lenses: (A) SOURCE — read the ORIGINAL DReichLab/AdmixTools C block code (on the box at /workspace/AdmixTools_src/src — we built convertf from it; the canonical Patterson convention) + the admixtools R block code (web github uqrmaie1/admixtools, the version 2.0.10 that built our goldens) and state the EXACT rule (how a block boundary is decided: cumulative genetic distance from block start? chromosome reset? partial-block handling? the precise cut condition + units). (B) EMPIRICAL — on the box, get AT2 v2.0.10 actual block structure for the Haak union (the f2 dir block_lengths / per-block SNP counts at /workspace/data/haak/at2_f2, or have admixtools emit its block assignment) AND steppe block IDs for the SAME SNP set, and DIFF them: where exactly do the 9 extra steppe blocks come from (chromosome ends? gaps? grid vs sequential)? (C) STEPPE + BLAST RADIUS — read src/core/domain/block_partition_rule.* assign_blocks (the floor-bin-per-chrom rule) and grep EVERYTHING that depends on the block convention (the M3 block golden / block_partition_unit test counts 757/719, the precompute fixtures, any committed block counts) so we know what must change if we match AT2. Synthesis -> a RECONCILIATION SPEC: the AT2 rule (verified+empirical), steppe rule, the exact diff, the blast radius, and the precise change to assign_blocks to match AT2 + which goldens/tests update. Writes /tmp/block-partition-at2.md. NO code changes, NO commits.',
  phases: [ { title: 'Verify (source + empirical + steppe/blast)' }, { title: 'Synthesize spec' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const OUT = '/tmp/block-partition-at2.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm; the parity target is ADMIXTOOLS (the accepted field reference). steppe computes the block jackknife over genome blocks defined on the GENETIC map (Morgans). The Haak v66 study showed steppe = 718 jackknife blocks vs AT2 = 709 (same 264544 SNPs, same blgsize 0.05 Morgans, same maxmiss=0) — a block-BOUNDARY convention difference that drives the residual SE/p + a ~0.5% qpAdm weight wobble (Q feeds the GLS). steppe floor-bins SNP genetic positions into fixed 0.05-Morgan grid cells per chromosome; AT2 walks SNPs sequentially and cuts a new block when the cumulative genetic distance from the block start reaches blgsize. GOAL: verify AT2 EXACTLY + spec steppe to match (parity).',
  'KEY SOURCES: (1) the ORIGINAL DReichLab AdmixTools C source is ON THE BOX at /workspace/AdmixTools_src/src (we built convertf v8621 from it — the canonical Patterson block-jackknife convention lives here, e.g. the block/jackknife code in qpAdm/dofstats/the block-definition helpers). (2) admixtools R v2.0.10 (uqrmaie1/admixtools on GitHub; the version that generated our goldens) — its extract_f2 / f2-block definition (the function that assigns SNPs to blocks from cM). (3) steppe: src/core/domain/block_partition_rule.{hpp,cpp} (assign_blocks). (4) the AT2 Haak f2 dir on the box /workspace/data/haak/at2_f2 (has per-pop pairwise + cache_metadata.json with n_blocks=709 + the block_lengths).',
  'READ-ONLY: web (ToolSearch select:WebSearch,WebFetch for the admixtools R source) + read box files (' + SSH + ') + read steppe source. NO code changes, NO commits, NO box compute beyond reading/light R probes. Cite exact source file:line + the empirical block boundaries. The deliverable is the reconciliation SPEC at /tmp/block-partition-at2.md — enough that a follow-up can change assign_blocks to match AT2 bit-tight. No fabrication; flag uncertainty.',
].join('\n')

phase('Verify (source + empirical + steppe/blast)')
const LENSES = [
  { key: 'at2-source', focus: 'The EXACT AT2 / original-ADMIXTOOLS block-partition rule FROM SOURCE. (1) On the box, read the DReichLab AdmixTools C source at /workspace/AdmixTools_src/src — find the block-jackknife block-DEFINITION code (grep for "block", "jackknife", "wjack", "blgsize", "chrom", the function that turns SNP genetic positions -> block ids; likely in a file like mcio/admutils/qpsubs or the jackknife helper). State the EXACT cut rule: does it start a new block when (genpos - block_start_genpos) > blgsize? does it reset at chromosome boundaries? how is the FIRST block / a partial trailing block handled? what genetic-distance field (the .snp col-3 Morgans)? (2) Web-fetch the admixtools R v2.0.10 source (github uqrmaie1/admixtools — the f2-block / extract_f2 block assignment, e.g. R/ or src/) and confirm it matches the C convention. Report the precise algorithm in pseudocode with file:line citations.' },
  { key: 'empirical-diff', focus: 'EMPIRICALLY pin where steppe 718 diverges from AT2 709. On the box: (1) read AT2 block structure for the Haak union — cat /workspace/data/haak/at2_f2/cache_metadata.json (n_blocks, block_lengths if present) and/or have admixtools R emit its block id per SNP for the Haak SNP set (block_lengths / the af block index). (2) get steppe block ids for the SAME SNPs — rerun or read steppe extract-f2 meta / a steppe block dump for the Haak union (steppe_f2_fixed dir meta.json + block_sizes), OR reason from assign_blocks on the .snp genetic positions. (3) DIFF: tabulate per-chromosome block counts steppe vs AT2 — where are the 9 extra steppe blocks (chromosome-end partial blocks? empty-grid-cell fragmentation? the first-block offset?). Pin the 9-block excess to specific chromosomes/boundaries. This makes the fix concrete.' },
  { key: 'steppe-blast', focus: 'steppe assign_blocks + the BLAST RADIUS of changing it. Read src/core/domain/block_partition_rule.{hpp,cpp} — the EXACT assign_blocks rule (floor((genpos-chrom_start)/blocksize)? per-chrom reset? dense renumber? the 757/719 counts). Then grep EVERYTHING that depends on the block convention / would change if assign_blocks matches AT2: the block_partition_unit test (tests/.../block_partition*; the 757/719 expected counts), any precompute golden / fixture with a baked block count, the M3 block-rule golden, docs that cite 757/719, and whether the committed f2 FIXTURES (AT2-generated, 709/710) are affected (they are NOT — they read AT2 blocks; only steppe-COMPUTED block counts change). Report: the steppe rule, the full list of tests/goldens/docs that must update to the AT2 convention, and whether the change is CONTAINED (just assign_blocks + its unit test + re-derive the steppe-computed counts) or RIPPLES (touches validated precompute goldens).' },
]
const found = await parallel(LENSES.map(L => () => agent([
  'You are a bioinformatics-methods + source-archaeology analyst verifying the ADMIXTOOLS block-jackknife partition for steppe parity, PRECISE + sourced: ' + L.focus, '', STD,
  '', 'Use web tools and/or read the box source/files and steppe source as your lens needs. Cite exact file:line + empirical numbers. Return the algorithm/diff/blast-radius for this lens with a clear conclusion. Flag uncertainty; NO fabrication.',
].join('\n'), { label: 'verify:' + L.key, phase: 'Verify (source + empirical + steppe/blast)' })))

phase('Synthesize spec')
const spec = await agent([
  'You are the lead synthesizer producing the block-partition AT2-parity RECONCILIATION SPEC for steppe. Combine the 3 lenses.', '', STD,
  '', 'THE LENSES:\n' + found.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'WRITE ' + OUT + ' with: (1) THE AT2 ALGORITHM — the exact block-cut rule (pseudocode), verified from the DReichLab C source AND admixtools R, with file:line; note any C-vs-R difference. (2) STEPPE'S ALGORITHM — the exact assign_blocks rule (file:line). (3) THE PRECISE DIFFERENCE — what makes 718 vs 709 (the empirical per-chromosome breakdown; the boundary/partial/gap mechanism). (4) THE RECONCILIATION SPEC — the exact change to src/core/domain/block_partition_rule assign_blocks to match AT2 (the new cut rule), in enough detail to implement bit-tight. (5) THE BLAST RADIUS — every test/golden/doc that must update (the block_partition_unit 757/719 counts, any steppe-computed block count; confirm the AT2-generated f2 fixtures are unaffected), and whether the change is CONTAINED or RIPPLES into validated precompute goldens. (6) A go/no-go: is matching AT2 a clean bounded change (recommend proceed) or does it ripple enough to need the user's sign-off on regenerating precompute goldens? Use the Write tool. Return the headline: the AT2 rule in one line + the exact steppe change + contained-or-ripples. Cite sources; no fabrication.',
].join('\n'), { label: 'synthesize:blockspec', phase: 'Synthesize spec' })
log('block-partition AT2 spec -> /tmp/block-partition-at2.md: ' + String(spec).slice(0, 200))
return { spec }
