export const meta = {
  name: 'toposearch-parity-harden',
  description: 'HARDEN the qpGraph topology-search per-candidate parity (the one honest test-strength gap from the v1 verdict 5fe4c2c): the automated gate was argmin-anchored (best + gap-to-2nd vs AT2), NOT an all-candidate per-score check, and the committed 4-candidate spot-check golden (tests/reference/goldens/at2/golden_qpgraph_toposearch_spotcheck.csv) is not yet READ by any test. THIS TASK: (1) expose the search`s PER-CANDIDATE scored vector on QpGraphSearchResult (include/steppe/qpgraph_search.hpp — it already has a ScoredCandidate {score, edges} struct + keeps the best/top-set; add/retain the full per-candidate (canonical-id/edges + score) vector so a test can look up a specific topology`s score); (2) add a ctest that READS the EXISTING committed golden (the 4 candidates: tree_21 nadmix0 7.36683912797386, tree_1 38.9072106917856, tree_105 39.233342780732, admix1_871 nadmix1 1.65355245107521 — with their edge lists + the 5 pops Mbuti;Han;Iran_GanjDareh_N;Israel_Natufian;Czechia_EBA_CordedWare), and for EACH asserts steppe`s per-candidate score (from the search`s exposed vector, matched by the canonical edge-list/graph-hash) == the golden AT2 score at the clean fit tier (rtol ~1e-6, atol ~1e-9 — the cc9ff69 fit tier). CRITICAL — DO NOT REGENERATE ANY GOLDEN: reuse the committed golden_qpgraph_toposearch_spotcheck.csv VERBATIM (it is already on disk + tracked); NO admixtools/R runs, NO re-running the full exhaustive search-golden, NO CPU-oracle sweeps — the user does not want to wait hours. The change is additive (expose a vector + read an existing golden in a new test); it must NOT alter the fit/score math (the search`s d_qpgraph_score is unchanged + already cc9ff69-anchored). REQUIREMENTS: GPU-FIRST (the search still runs on the heterogeneous fleet, unchanged); REUSE no bolt-on; SINGLE-GPU --device 0; CUDA 13+; REAL AADR (the search runs on the real 9-pop/5-pop f2 fixture, the same the search uses). VERIFY scope: build + a TARGETED ctest (the NEW parity test + qpgraph_parity + qpgraph_search to confirm no regression) — do NOT run a full golden-regen; the full STEPPE_THOROUGH suite is ~176s if a final no-regression sweep is wanted but is OPTIONAL (targeted is enough for this additive change). Discipline: implement (expose vector + the golden-reading test) + build + targeted ctest -> verify (the 4 candidates match the golden at the fit tier, no regression) -> commit. FAIL-PROTOCOL: commit ONLY on green (the 4 per-candidate scores match the golden); on BAD (a per-candidate score is OFF the golden beyond the fit tier — a real correctness finding) HALT + report + defer; NEVER silently fail/revert; on failure git stash push -u + HALT. NEVER touch the golden file or any AT2 parity golden.',
  phases: [ { title: 'Expose the per-candidate vector + the golden-reading parity test + build + targeted ctest' }, { title: 'Verify the 4 candidates match the existing golden + no regression + commit-or-HALT' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
// TARGETED build + ctest (the qpgraph + new parity tests only — NOT the full thorough golden-regen)
const BUILD_TARGETED = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -18 && echo === TARGETED CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel -R \"qpgraph\" --output-on-failure 2>&1 | tail -22; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ 5fe4c2c. qpGraph topology search v1 (oracle C, exhaustive enumeration on the heterogeneous-topology fleet) is built + green (ctest 64/64). The v1 verdict named ONE honest test-strength gap: the per-candidate parity gate is argmin-anchored, and the committed 4-candidate spot-check golden tests/reference/goldens/at2/golden_qpgraph_toposearch_spotcheck.csv is not READ by any test. This task closes that gap.',
  'THE GOLDEN ALREADY EXISTS + is committed (tests/reference/goldens/at2/golden_qpgraph_toposearch_spotcheck.csv): 4 candidates with AT2 qpgraph fit scores + edge lists + the 5 pops. REUSE IT VERBATIM — DO NOT REGENERATE (no admixtools/R, no re-running the exhaustive search-golden, no CPU-oracle sweep; the user does not want to wait hours).',
  'THE CHANGE (additive, fit/score math UNCHANGED): (1) expose the search`s per-candidate scored vector on QpGraphSearchResult (include/steppe/qpgraph_search.hpp; it already has a ScoredCandidate {score,edges} + a best/top-set — retain/expose the full per-(topology) vector so a test can look up a candidate by its canonical edge-list/graph-hash). (2) a new ctest reads the committed golden, parses each candidate`s edge list, looks up steppe`s search score for that topology (by canonical hash/edges), and asserts == the golden AT2 score at the clean fit tier (rtol 1e-6, atol 1e-9). The search`s d_qpgraph_score is the cc9ff69-anchored fit inner loop — unchanged.',
  'REQUIREMENTS: GPU-FIRST (the search still runs on the heterogeneous fleet on-device, unchanged); REUSE no bolt-on; SINGLE-GPU --device 0; CUDA 13+; REAL AADR (the search runs on the real f2 fixture). NO synthetic. The change must NOT alter the fit/score/enumeration math — only EXPOSE the per-candidate scores + READ the existing golden in a test.',
  'BUILD/VERIFY scope (FAST — the user does not want hours): build-rel + a TARGETED ctest -R "qpgraph" (the new parity test + qpgraph_parity + qpgraph_search + cli_qpgraph — confirms no regression in the qpGraph area). Do NOT run a full golden-regen. The full STEPPE_THOROUGH suite (~176s) is OPTIONAL and only if a final broad no-regression sweep is explicitly wanted — targeted is sufficient for this additive change.',
  'FAIL-PROTOCOL (USER-MANDATED): NEVER git checkout -- . / git clean -fd; NEVER touch the golden file. commit ONLY on green (the 4 per-candidate scores match the golden at the fit tier + no qpgraph regression). On BAD (a per-candidate score is OFF the golden beyond the fit tier — a REAL correctness finding worth surfacing) HALT + report + DEFER. On any failure ' + STASH + ' "wip:toposearch-parity-FAILED-<reason>" + HALT.',
  'SINGLE-GPU --device 0. RELEASE build-rel. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering. nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); for (let i=0;i<2&&r===null;i++){ log(opts.label+': transient null/500 — retry '+(i+1)); r = await agent(p, {...opts, label: opts.label+':retry'+(i+1)}) } return r }

phase('Expose the per-candidate vector + the golden-reading parity test + build + targeted ctest')
const fixer = await tryAgent([
  'You are hardening the topology-search per-candidate parity (additive; REUSE the existing golden, NO regen). Do NOT commit. Do NOT git checkout/clean (git stash if forced).', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+targeted-ctest (' + BUILD_TARGETED + ').', '',
  'READ include/steppe/qpgraph_search.hpp (QpGraphSearchResult + ScoredCandidate) + src/core/qpadm/qpgraph_search* + tests/reference/test_qpgraph_search.cu + the committed golden tests/reference/goldens/at2/golden_qpgraph_toposearch_spotcheck.csv. IMPLEMENT: (1) expose the per-candidate scored vector on QpGraphSearchResult (retain all enumerated (canonical edges/hash + best-of-restarts score), or at minimum a lookup by canonical topology) — additive, do not change the fit/enumeration math; (2) a new ctest (e.g. test_qpgraph_search_parity, or extend test_qpgraph_search) that READS the committed golden, parses each of the 4 candidate edge lists, finds steppe`s search score for that topology by canonical hash/edges, and asserts == the golden AT2 score at rtol 1e-6 / atol 1e-9; register it in tests/CMakeLists.txt. Build + the TARGETED ctest (-R qpgraph). SANITY: the 4 candidates each match the golden at the fit tier; qpgraph_parity/qpgraph_search/cli_qpgraph still pass; the golden file is UNCHANGED (git diff shows no golden edit). Report files changed, how the per-candidate vector is exposed, the 4 per-candidate match deltas, the targeted ctest result. Do NOT commit. BAD (a candidate off the golden beyond tier) -> STOP + report (do NOT revert, do NOT touch the golden).',
].join('\n'), { label: 'implement:harden', phase: 'Expose the per-candidate vector + the golden-reading parity test + build + targeted ctest' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true } }
await tryAgent(['BUILD-REPAIR for the toposearch parity hardening. Accumulated edits (do NOT clean/revert/destroy/touch-the-golden; git stash only if forced). Reach a CLEAN Release build + green TARGETED ctest -R qpgraph (the new parity test + qpgraph_parity/qpgraph_search/cli_qpgraph), patching only trivial errors. DO: ' + RSYNC + ' then ' + BUILD_TARGETED + '. Loop up to 4x. NON-trivial / a candidate off the golden -> STOP + report. Report build + targeted ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Expose the per-candidate vector + the golden-reading parity test + build + targeted ctest' })

phase('Verify the 4 candidates match the existing golden + no regression + commit-or-HALT')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','four_candidates_match','golden_unchanged','per_candidate_vector_exposed','no_qpgraph_regression','no_golden_regen','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if the new test READS the committed golden + all 4 candidates match steppe`s search per-candidate score at the fit tier (rtol 1e-6), the golden file is UNCHANGED (no regen), the per-candidate vector is exposed, qpgraph_parity/qpgraph_search/cli_qpgraph unregressed, single-GPU' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (a candidate off the golden = bad correctness finding -> HALT+defer)' },
    four_candidates_match: { type: 'string', description: 'the per-candidate deltas: tree_21/tree_1/tree_105/admix1_871 steppe-score vs golden (should be <=1e-6 rel)' },
    golden_unchanged: { type: 'boolean', description: 'git diff confirms golden_qpgraph_toposearch_spotcheck.csv NOT modified (reused, not regenerated)' },
    per_candidate_vector_exposed: { type: 'boolean' },
    no_qpgraph_regression: { type: 'boolean', description: 'qpgraph_parity + qpgraph_search + cli_qpgraph still pass' },
    no_golden_regen: { type: 'boolean', description: 'no AT2/R run, no golden regenerated (the existing one reused)' },
    commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the 4-candidate match + the exposure; on FAIL the blocker' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for the toposearch parity hardening (adversarial; confirm the golden is REUSED not regenerated + the 4 candidates genuinely match). implementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — the per-candidate vector is exposed (additive), the new test READS the committed golden (no golden edit — git diff shows golden_qpgraph_toposearch_spotcheck.csv UNCHANGED), the fit/score/enumeration math is unchanged. (2) ' + BUILD_TARGETED + ' — the new parity test passes (all 4 candidates within the fit tier), qpgraph_parity/qpgraph_search/cli_qpgraph still pass. (3) confirm NO golden was regenerated (no AT2/R run). PASS only if the 4 match + the golden is untouched + no regression. ',
  'ON PASS: cd ' + R + ' && git add ONLY the changed header/source/test/cmake (NEVER git add dot; NEVER the golden; never aadr/ atlas_results/ tests/tools/), commit (test(qpGraph topology search): per-candidate parity — read the committed spot-check golden, assert all 4 candidate scores == AT2 qpgraph-fit at rtol 1e-6 via the exposed per-candidate vector; closes the v1 argmin-anchored test-strength gap; golden reused not regenerated) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. ',
  'ON BAD (a candidate off the golden / golden was modified / regression): DO NOT git checkout/clean, DO NOT revert, DO NOT touch the golden. ' + STASH + ' "wip:toposearch-parity-FAILED" (capture the ref). Classify fail_severity. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:harden', phase: 'Verify the 4 candidates match the existing golden + no regression + commit-or-HALT' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true } }
if (verdict.pass) log('+++ toposearch parity hardened ' + verdict.commit_hash + ' — ' + verdict.four_candidates_match)
else log('--- toposearch parity FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — ' + verdict.note)
return { verdict }
