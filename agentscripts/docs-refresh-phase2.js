export const meta = {
  name: 'docs-refresh-phase2',
  description: 'Refresh ALL the docs to the real state: Phase-2 qpAdm fit engine (S3-S8) is BUILT + golden-gated on GPU, and the big-refactor (3 HIGH + 14 MED + 9 LOW groups + 2 device dedups, all bit-identical, wallclock unchanged) is complete — all on main @ 25c882a. The docs are STALE (fit-engine.md says "Status: DESIGN / Nothing is built"; ROADMAP says Phase 2 "does not exist yet"). 5 parallel doc-owner agents (each reads the SAME ground-truth: git log + the fit-engine.md milestone BUILT annotations + the VERIFIED build/test commands) update: ROADMAP.md (Phase 2 COMPLETE; next = productization/new features), RESUME.md (the handoff card -> current state + whats next), architecture.md (mark the fit engine BUILT; §5 S3-S8 + the file-tree planned->BUILT; keep the run-single-GPU multi-GPU note), docs/design/fit-engine.md (fix the stale DESIGN/Nothing-built header), and a NEW docs/RUN-GUIDE.md (HOW TO RUN EVERYTHING: build, default vs STEPPE_THOROUGH ctest, run a qpAdm fit / rotation / precompute via the test harness + C++ API, the box dev loop; HONEST that there is no CLI/Python yet). Then a VERDICT agent cross-checks every BUILT/DONE claim vs git + every command vs the verified set, fixes inconsistencies, and commits. NO box, NO source edits (docs only). The build/test commands are ALREADY VERIFIED working (qpadm_parity 0.86s default / 27.3s thorough at HEAD).',
  phases: [
    { title: 'Write', detail: '5 parallel doc owners: ROADMAP / RESUME / architecture / fit-engine status / NEW RUN-GUIDE' },
    { title: 'Verify', detail: 'adversarial accuracy cross-check vs git + verified commands, fix inconsistencies, commit' },
  ],
}

const R = '/home/suzunik/steppe'

const GROUND = [
  'GROUND TRUTH (verify against git ' + R + ' before writing; do NOT contradict it):',
  '- Branch phase2-fit-engine == main @ 25c882a. Phase-1 precompute (S0-S2) DONE through M5 (device-resident output + streaming; full-autosome P=2500 ~51.5s single-GPU). Phase-2 qpAdm FIT ENGINE (S3-S8) is BUILT + golden-gated on the GPU.',
  '- Fit milestones (all BUILT, see docs/design/fit-engine.md milestone table + git): M(fit-0) AT2 goldens; M(fit-1) f4+GLS weight fit; M(fit-2) rank test/qpWave (rankdrop/popdrop, nr<=32 9-pop + nr=39 NRBIG large path via cuSOLVER gesvd); M(fit-3) block-jackknife SE + the opt-in JackknifePolicy{None,FeasibleOnly,All}; M(fit-4) CUDA backend; M(fit-5) domain outcomes (RankDeficient/NonSpdCovariance as per-model status values); M(fit-6) S8 model-space ROTATION (run_qpadm_search, batched fit_models_batched + multi-GPU shard).',
  '- Validation: REAL-AADR AT2 goldens — tests/reference/goldens/at2/golden_fit0.json (9-pop), golden_fit1_NRBIG.json (nr=39), golden_rot.json (84-model rotation), all admixtools 2.0.10 / R 4.3.3 / v66.p1_HO. The GPU path matches them bit/tolerance; the CpuBackend is the native oracle (run under STEPPE_THOROUGH).',
  '- MULTI-GPU rotation is DEFERRED (TODO(multigpu-host-bounce)): on the consumer 5090s the one-time f2 replication is a ~8.72 GB / ~3.8 s HOST BOUNCE (no GeForce P2P) -> only ~1.21x at 9086 real models -> RUN SINGLE-GPU; the payoff needs P2P hardware (RTX PRO 6000) or per-device precompute. (Do NOT claim multi-GPU rotation speedup.)',
  '- BIG REFACTOR COMPLETE (docs/cleanup/bigrefactor/): 3 HIGH (block_sink silent-corruption fail-fast 9dbc610, kQpMax single-source 3beff6d, opt_A/opt_B als_ridge_solve ed6cc44) + 14 MED groups + 9 LOW groups (held to docs/cleanup/bigrefactor/NAMING-STYLE-STANDARD.md) + 2 device dedups (block_sink StagingRing b1bd620, qpadm_fit_kernels twin-collapse 25c882a) — ALL golden-gated on real AADR, bit-identical, WALLCLOCK UNCHANGED (qpadm_parity 0.86s default / 27.3s thorough; rotation 3.42s; no perf regression). The precision policy is consistent (emulated-FP64 default + native fallback via the ONE emulation_honorable predicate; precision-policy-consistency.md = 0 deviations). Skipped/deferred by decision: the host/device pointer-wrapper tag-type, blanket const __restrict__.',
  '- NOT YET BUILT (the honest "what is left"): no CLI, no Python bindings (architecture app/ = planned); no standalone f4/f3/D-stat/f4-ratio/qpDstat entry points (the f4 math is internal to the fit); qpfstats / DATES / qpGraph not built; precompute M6 (multi-dataset merge) / M7 (on-disk cache) pending. (See docs/research/desirable-features-survey.md.)',
  '- THE VERIFIED BUILD/TEST COMMANDS (these WORK at HEAD — do not invent variants):',
  '  * box: box5090 (2x RTX 5090, sm_120, CUDA 13). Alias `ssh box5090`. nvcc not on PATH -> prefix: export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0',
  '  * sync from local: rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr -e ssh ' + R + '/ box5090:/workspace/steppe/',
  '  * build (RELEASE, required for perf): cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release && cmake --build build-rel',
  '  * default ctest (GPU-vs-AT2-golden, fast ~1 min): ctest --test-dir build-rel --output-on-failure  (qpadm_parity 0.86s, qpadm_rotation 3.42s)',
  '  * THOROUGH (adds the CpuBackend oracle + NRBIG full SE; for CI-without-GPU + localization): STEPPE_THOROUGH=1 ctest --test-dir build-rel -R qpadm  (~56s)',
  '  * the qpAdm fit / rank test / rotation run via the test harness binaries: build-rel/bin/test_qpadm_parity <goldens-dir>, build-rel/bin/test_qpadm_rotation <goldens-dir> (these load the real f2 fixtures + run the GPU fit/rotation). The C++ API: steppe::run_qpadm(DeviceF2Blocks, QpAdmModel, QpAdmOptions, Resources) + steppe::run_qpadm_search(...) in include/steppe/qpadm.hpp.',
  '  * core dumps: the box accumulates ~404 MB cores on abort() — clear with rm -f /var/lib/vastai_kaalia/data/core-* (the workflows do this per build).',
  'HONESTY: this is the real-state refresh. Do NOT claim anything is built that is not (no CLI/Python/standalone-f-stats). Do NOT claim multi-GPU rotation speedup. Every "BUILT"/command must be verifiable. NO synthetic-data claims. Cite commit hashes / file paths.',
].join('\n')

phase('Write')
const DOCS = [
  { key: 'roadmap', file: 'docs/ROADMAP.md', task: 'Update ROADMAP.md: mark Phase-2 qpAdm FIT engine (S3-S8) as BUILT/COMPLETE (it currently says "does not exist yet / THE NEXT REAL WORK") with the M(fit-0..6) milestones + the real-AADR golden gate; note multi-GPU rotation DEFERRED (host-bounce, single-GPU). Add a "Phase 2.5 / what is next" pointing at the HONEST remaining work (productization: CLI + Python bindings; standalone f-stats f4/D/f3/qpDstat; qpfstats; precompute M6/M7) per the desirable-features survey. Note the big-refactor is complete (link docs/cleanup/bigrefactor/). Keep the existing precompute M0-M5 section accurate. Preserve the doc style.' },
  { key: 'resume', file: 'docs/RESUME.md', task: 'Rewrite the RESUME.md state/handoff card to the CURRENT reality: main @ 25c882a; Phase-1 precompute DONE thru M5; Phase-2 fit engine S3-S8 BUILT + golden-gated (list the milestones + the goldens); the big-refactor COMPLETE (HIGH+MED+LOW+device dedups, bit-identical, wallclock unchanged); multi-GPU rotation DEFERRED (run single-GPU). Update the "open threads / pick up here" to the honest next work (productization + new f-stats features). Update the build/test one-liners to the VERIFIED build-rel + default/THOROUGH ctest commands. Keep it a fast re-orientation card; point to docs/RUN-GUIDE.md for full commands.' },
  { key: 'architecture', file: 'docs/architecture.md', task: 'Update architecture.md to mark the fit engine BUILT: the §5 S3-S8 stage table (these now exist — note BUILT + the owning files src/core/qpadm/* + the CudaBackend overrides), the source-tree listing where qpadm/fit files are marked "(planned, P2)" -> "[BUILT]" (f4_matrix, gls_solve, jackknife, ranktest, nested_models, model_search, qpadm_fit + src/device/cuda/qpadm_fit_kernels.cu), and the app/ CLI/qpdstat lines stay "(planned)". Keep the §12 precision + the multi-GPU run-single-GPU notes (already corrected). Do NOT rewrite the whole spec — surgical status updates only; preserve §-numbering + the §12 PARITY LAW.' },
  { key: 'fit-engine', file: 'docs/design/fit-engine.md', task: 'Fix the STALE status in docs/design/fit-engine.md: line 3 "Status: DESIGN (Phase 2, S3-S8). Nothing here is built." -> BUILT (S3-S8 implemented + golden-gated on GPU; the milestone table below already carries the per-milestone BUILT annotations); and line ~384 "Nothing below is built." -> a BUILT summary. Keep the milestone table (it is accurate). Add a short "STATUS (as of 25c882a): the fit engine is BUILT — M(fit-0..6) complete; multi-GPU rotation deferred; the big-refactor is complete" banner near the top. Do NOT rewrite the design body.' },
  { key: 'run-guide', file: 'docs/RUN-GUIDE.md', task: 'CREATE docs/RUN-GUIDE.md = "How to run everything" using ONLY the VERIFIED commands in the ground truth. Sections: (0) the box + nvcc-PATH prefix + rsync-from-local; (1) BUILD (Release build-rel); (2) TEST — the default GPU-vs-golden ctest (what it covers, ~1min) vs STEPPE_THOROUGH=1 (adds the CpuBackend oracle; the CI-without-GPU lane); (3) RUN A qpAdm FIT / RANK TEST / ROTATION — via the test_qpadm_parity / test_qpadm_rotation binaries (what they load + assert) AND the C++ API entry points (run_qpadm / run_qpadm_search in include/steppe/qpadm.hpp, the QpAdmModel/QpAdmOptions/JackknifePolicy knobs); (4) THE PRECOMPUTE (how f2_blocks is produced, DeviceF2Blocks resident); (5) the dev loop (edit local -> rsync -> build -> ctest); (6) gotchas (RELEASE-only for perf, clear core dumps, run SINGLE-GPU for the rotation on the 5090s). BE HONEST: there is NO CLI/Python yet — running = the test harness + the C++ library API; note where the CLI would land (app/, planned). Every command copy-pasteable + verified.' },
]
const written = await parallel(DOCS.map(D => () => agent([
  'You are the doc owner for ' + D.file + ' in the steppe big-refactor docs refresh. READ the current file (if it exists) + verify the ground truth against git (git -C ' + R + ' log/show), then WRITE the updated file. NO box, NO source edits — docs only.', '', GROUND, '',
  'YOUR DOC: ' + D.task, '',
  'Use the Write/Edit tools on ' + R + '/' + D.file + '. Keep the existing doc voice/structure (surgical updates for existing docs; a clean new doc for RUN-GUIDE). Every factual claim must match the ground truth + git. Return a 2-3 line summary of what you changed.',
].join('\n'), { label: 'write:' + D.key, phase: 'Write' })))

phase('Verify')
const verdict = await agent([
  'You are the ADVERSARIAL accuracy verdict for the steppe docs refresh. The doc owners updated: ' + DOCS.map(d=>d.file).join(', ') + '. Be skeptical: docs that overclaim are worse than stale.', '', GROUND, '',
  'THE OWNER REPORTS:\n' + written.filter(Boolean).map((w,i)=>'### '+DOCS[i].file+'\n'+w).join('\n\n'),
  '', 'DO: (1) read each updated doc (' + DOCS.map(d=>R+'/'+d.file).join(', ') + '). (2) CROSS-CHECK every "BUILT/DONE/COMPLETE" claim against git -C ' + R + ' (the commits 9dbc610/3beff6d/ed6cc44/b1bd620/25c882a/the MED+LOW chain exist; the milestone files exist) — flag + FIX any overclaim (esp: nothing should claim a CLI/Python/standalone-f-stats exists; nothing should claim multi-GPU rotation speedup; the goldens named must exist under tests/reference/goldens/at2/). (3) VERIFY the RUN-GUIDE commands are exactly the verified set (build-rel Release, default ctest, STEPPE_THOROUGH, the test binaries, the rsync) — fix any invented/wrong command. (4) ensure cross-doc CONSISTENCY (ROADMAP/RESUME/architecture/fit-engine agree on what is built). Fix inconsistencies directly with Edit. ',
  'THEN commit: cd ' + R + ' && git add ONLY the changed/new docs (docs/ROADMAP.md docs/RESUME.md docs/architecture.md docs/design/fit-engine.md docs/RUN-GUIDE.md; NEVER git add dot; never aadr/ build_run.sh f2_emu_spike.cu handoff-*.md) && commit with a ROADMAP §6 message (docs refreshed to the real state: Phase-2 fit engine BUILT + big-refactor complete + the RUN-GUIDE) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Return: the commit hash + any overclaims you caught & fixed + a 1-line confirmation each doc is accurate.',
].join('\n'), { label: 'verify:docs', phase: 'Verify' })

log('docs refresh: ' + String(verdict).slice(0, 200))
return { written, verdict }
