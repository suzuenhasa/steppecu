export const meta = {
  name: 'm5-docs-refresh',
  description: 'Refresh ALL docs to the current reality (post device-resident output + M5 tiered/input streaming) before merging the branch chain to main. Parallel doc-updaters (each owns disjoint files, one authoritative fact-sheet) + a consistency critic + a NEW M5 results doc (the repo-verified home for the full-autosome numbers). Key updates: precompute now COMPLETE through M5 (full-autosome P=2500 on a 32GB card, 51.5s); the honest multi-GPU story (real wins = device-resident + streaming + off-CPU, NOT multi-GPU per se; multi-GPU belongs on the fit/rotation); the stale scaling-sweep.md (says P=2500 OOM) marked superseded; the AT2 timing comparison; and the NEXT PHASE = the qpAdm fit engine (Phase 2). EDIT-ONLY (no git) — the human commits + merges.',
  phases: [
    { title: 'Update', detail: 'parallel: each agent rewrites its docs to the post-M5 reality + write the new M5 results doc' },
    { title: 'Consistency', detail: 'grep the repo for leftover stale claims (P=2500 OOM, multi-GPU-is-the-speedup) + fix; verify next-phase coherence' },
  ],
}

const R = '/home/suzunik/steppe'

const FACTS = [
  'AUTHORITATIVE CURRENT STATE (branch m5-input-streaming, HEAD ~60b0332, about to merge to main). Rewrite every doc claim to match; preserve each doc structure/tone/unrelated content. EDIT IN PLACE (Edit/Write). Do NOT touch git (no add/commit) — the human commits + merges.',
  '1. PHASE-1 PRECOMPUTE IS NOW SUBSTANTIALLY COMPLETE THROUGH M5. M0-M4 (3-GEMM f2 kernel + real-AADR loader + filters + per-block f2_blocks tensor) + before-M4.5 cleanup (B1-B27): on main. M4.5 single-node multi-GPU (block-aligned shard, host-staged + device-resident P2P combine, bit-identical): on main.',
  '2. DEVICE-RESIDENT OUTPUT (commit 1f80c0c): the precompute now returns a DEVICE-RESIDENT handle (DeviceF2Blocks; result stays in VRAM); the host F2BlockTensor is an opt-in .to_host(). MEASURED: P=512 device-resident ~673ms vs ~2879ms bulk-to-host = ~4.3x. THE KEY LESSON: the precompute was HOST-RESULT-BOUND — ~80% of the old wall was copying the 6.36GB+ result to CPU; getting it OFF the CPU was the real win, NOT multi-GPU.',
  '3. M5 OUT-OF-CORE STREAMING — DONE (tiered output 176a07d + SNP-tile input streaming c65179f). (a) ADAPTIVE TIERED OUTPUT, not mandatory: result goes to the fastest tier it FITS — VRAM-resident (small P, keeps the 4.3x) -> host RAM (big box) -> disk (laptop), auto-selected from runtime free VRAM/RAM. (b) SNP-tile INPUT streaming: per-block decode, GPU footprint O(P*tile + P^2) INDEPENDENT of M (no 7*P*M feeder wall). RESULT: full-autosome (M=584131, n_block=757) P=2500 COMPLETES on a SINGLE 32GB RTX 5090 in ~51.5s (76GB result streamed, GPU peak ~26GB bounded), parity memcmp BIT-IDENTICAL. Measured sweep (one 5090, streamed): P=512 ~3.6s, P=1000 ~10.4s, P=1500 ~20.2s, P=2000 ~34.0s, P=2500 ~51.5s.',
  '4. THE HONEST MULTI-GPU STORY (correct the older docs): M4.5 multi-GPU was built, but the REAL perf wins came from device-resident output + streaming (getting off the CPU), NOT multi-GPU per se. On the precompute, multi-GPU is a modest throughput layer (and was measured slower than single-GPU until the data-bounce was fixed; nsys showed ~22-74% overlap, a serial D2H/host tail). Multi-GPU genuinely SHINES on the FIT/ROTATION phase (thousands of INDEPENDENT models, no combine) — that is its proper home, not the precompute. (See architecture-audit.md, parallelism-check.md, why-d2h.md, why-multigpu-slow.md.)',
  '5. vs ADMIXTOOLS 2 (docs/research/at2-timing-comparison.md, sourced + verified-vs-estimated): the f2 precompute is plausibly ~2-3 ORDERS OF MAGNITUDE faster than AT2 extract_f2 at thousands-of-pops scale — but AT2 published NO extract_f2-vs-P timing, so that ratio is an ESTIMATE (complexity O(P^2*M) + the one "~a day -> 20min" anecdote), anchored to steppe MEASURED P=2000=15.1s (PRO6000) / P=2500=51.5s (one 5090). Do NOT state a single clean "Nx" as fact; it is an estimate.',
  '6. THE NEXT PHASE = the qpAdm FIT ENGINE (Phase 2, S3-S8), which DOES NOT EXIST YET. It reads f2_blocks (DEVICE-RESIDENT for the in-VRAM case, per why-d2h.md; streamed tiles for large P), runs the GLS solve + rank test + the MODEL ROTATION (many independent qpAdm models = the embarrassingly-parallel, multi-GPU-friendly phase). AT2 goldens (install R + admixtools, pin extract_f2/qpadm goldens per §12) are the validation gate. Deferred/optional: multi-GPU block-sharding on the stream (throughput); the device-resident final-D2H pinning (rtxbox); the TurboQuant-L2 rotation screen (AFTER the fit exists, docs/research/turboquant-l2-experiment.md).',
  '7. DOC GAP TO FIX: docs/cleanup/m4.5/scaling-sweep.md is STALE/PRE-M5 — it says P=2500 OOMs on every path. That was true BEFORE M5; it is now FALSE (M5 streaming completes P=2500). Mark it SUPERSEDED-BY the new M5 results doc; do not delete it (historical).',
].join('\n')

phase('Update')
const GROUPS = [
  { key: 'm5-results-doc', files: 'NEW docs/cleanup/m5/00-results.md (create it)', note: 'WRITE the repo-verified M5 results doc: the device-resident win (4.3x @P=512), the adaptive tiering (Resident/HostRam/Disk + how it auto-selects), the SNP-tile input streaming (per-block decode, O(P*tile+P^2)), and THE HEADLINE measured full-autosome sweep (one 5090, M=584131, n_block=757: P=512 ~3.6s ... P=2500 ~51.5s completes, GPU peak ~26GB, parity bit-identical), with the commits (1f80c0c, 176a07d, c65179f) and the honest caveats (disk-tier ENOSPC on a small overlay; parity proven at P=50 + by-construction). This is the doc that makes the 51.5s repo-verified.' },
  { key: 'scaling-sweep-supersede', files: 'docs/cleanup/m4.5/scaling-sweep.md', note: 'Add a prominent SUPERSEDED banner at the TOP: this pre-M5 sweep showed P>=2000/2500 OOM; M5 streaming (c65179f) now completes full-autosome P=2500 on a 32GB card — see docs/cleanup/m5/00-results.md. KEEP the historical table; do not delete.' },
  { key: 'workflow-map', files: 'agentscripts/README.md', note: 'Update WHERE WE ARE to post-M5 (precompute complete through M5; next = fit engine). Add ALL the new workflows to the EXISTING table: m4.5-perf-discovery, m4.5-why-slow-investigation, m4.5-device-resident-combine, m4.5-perf-fix-pass, m4.5-hoststaged-direct-d2h, m4.5-persistent-pin-d2h, m4.5-parallelism-check, m4.5-architecture-audit, m4.5-gpu-resident-output, m5-tiered-streaming, m5-input-streaming, turboquant-l2-research, at2-timing-comparison, m4.5-docs-refresh, m5-docs-refresh (mark each done + its commit). In WORKFLOWS WE STILL NEED: the qpAdm fit engine (HIGH, the next phase); AT2 goldens; multi-GPU block-sharding on the stream.' },
  { key: 'todo-roadmap', files: 'docs/TODO.md AND docs/ROADMAP.md', note: 'Mark M5 (out-of-core streaming) DONE (tiered output + input streaming, full-autosome P=2500 on 32GB, 51.5s). Update the precompute status to complete-through-M5. Surface the NEXT PHASE = the qpAdm fit engine (Phase 2 / S3-S8) prominently. Note AT2 goldens as the fit validation gate. Keep the milestone ordering + rationale; move finished items to Done.' },
  { key: 'handoff-resume', files: 'handoff-ba37d95.md AND docs/RESUME.md', note: 'Update the cold-start handoff + the resume card to the post-M5 reality: precompute complete through M5 (the measured numbers), the honest multi-GPU story (real wins = device-resident + streaming), current HEAD, and the next phase = the fit engine. Keep them faithful entry points.' },
  { key: 'm45-perf-docs', files: 'docs/cleanup/m4.5/why-multigpu-slow.md AND perf-discovery.md AND parallelism-check.md AND architecture-audit.md AND why-d2h.md', note: 'These are the multi-GPU/perf analysis saga. ADD a short "RESOLVED / OUTCOME" note at the top of each tying it to what actually landed: why-d2h -> device-resident output IMPLEMENTED (1f80c0c) + M5 streaming; architecture-audit + parallelism-check -> the real lever was off-CPU device-resident + streaming, multi-GPU belongs on the fit; why-multigpu-slow/perf-discovery -> historical, the data-bounce was fixed and then superseded by the device-resident/streaming model. Keep the analyses; just add the outcome + that multi-GPU per se is not the precompute speedup.' },
  { key: 'overviews', files: 'docs/cleanup/00-overview.md AND docs/cleanup/m4.5/00-overview.md', note: 'Update any row about multi-GPU perf / streaming / the deliverable to the post-M5 resolved state (device-resident output + M5 streaming done; full-autosome P=2500 on 32GB). Keep the backlog structure; only touch the now-stale claims.' },
]
const updates = await parallel(GROUPS.map((g) => () => agent([
  'You are a senior engineer + technical writer refreshing steppe docs to the post-M5 reality. EDIT the file(s) in place (Edit/Write). Do NOT touch git. Preserve structure, tone, and UNRELATED content — change only what the facts make stale; cite commits (1f80c0c device-resident, 176a07d tiered, c65179f input-streaming) where relevant.', FACTS, '',
  'YOUR FILE(S): ' + g.files + '\nFOCUS: ' + g.note, '',
  'Read your file(s) first (for the NEW doc, create it). Find every stale claim (search: "P=2500 ... OOM", "SLOWER", "multi-GPU is the/a speedup", "L4 pool", "0.70x", "does NOT tile", "input feeder", "next = M4.5", any "streaming not built/needed") and rewrite to the authoritative state. Be precise; do NOT invent facts beyond the fact-sheet (esp. the AT2 ratio is an ESTIMATE). Return a short list of every edit you made.',
].join('\n'), { label: 'update:' + g.key, phase: 'Update' }))).then(a => a)

phase('Consistency')
const critic = await agent([
  'You are the consistency critic. The doc-updaters just refreshed steppe docs to the post-M5 reality (precompute complete through M5; full-autosome P=2500 on a 32GB card; next = the qpAdm fit engine; multi-GPU belongs on the fit not the precompute). Find + fix any LEFTOVER stale claim in the repo docs and verify coherence. EDIT in place; do NOT touch git.', FACTS, '',
  'WHAT THE UPDATERS REPORTED:\n<<<\n' + updates.map((u,i)=>'['+GROUPS[i].key+'] '+(u||'(agent died — CHECK its files)')).join('\n\n') + '\n>>>', '',
  'DO: (1) grep the repo for residual stale claims: cd ' + R + ' && grep -rinE "P=?2500.*OOM|OOMs (on )?(every|all)|multi-?GPU is (the|a) speedup|input feeder.*(wall|OOM)|does NOT tile|next ?= ?M4.5|streaming (is )?not (built|done)|0\\.70x slow" docs/ agentscripts/README.md handoff-*.md . Inspect EACH: if it is a genuinely stale claim, FIX it (or confirm it is correctly framed as historical/superseded); if unrelated, leave it + note why. (2) Confirm the NEW docs/cleanup/m5/00-results.md exists with the measured full-autosome numbers, scaling-sweep.md carries its SUPERSEDED banner, and the workflow map + TODO/ROADMAP name the qpAdm fit engine as the next phase. (3) Make final fix-up edits. (4) Confirm the AT2 ratio is everywhere framed as an ESTIMATE, never a measured fact.',
  'Return: the grep results + per-hit verdict (FIXED / left-historical / unrelated), and a 4-6 line confirmation that the docs now consistently reflect "precompute complete through M5, full-autosome P=2500 on a 32GB card, next = the qpAdm fit engine" with no surviving "P=2500 OOM / multi-GPU-is-the-speedup" claims.',
].join('\n'), { label: 'consistency-critic', phase: 'Consistency' })

return { updates, critic }
