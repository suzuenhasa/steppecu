export const meta = {
  name: 'qpfstats-build',
  description: 'BUILD qpfstats (STEP 3, tool 1 of 3) — CORRECTED SPEC (the first run HALTed on a mis-specified oracle; the genotype+allsnps machinery it claimed "new" ALREADY EXISTS in qpDstat Part B). qpfstats = the admixtools genotype-path JOINT f2 SMOOTHER: from genotypes (allsnps=TRUE) it jointly estimates + shrinkage-smooths the FULL pairwise f2 basis, emitting a smoothed f2 tensor that downstream qpadm/f4/qpGraph consume (a better f2 object, esp. with missing data). IT IS A GENOTYPE-PATH TOOL (NOT an f2-cache stat) outputting an f2 TENSOR (NOT est/se/z/p) — that is why qpfstats(read_f2) is impossible and the est/se/z/p golden was wrong. REUSE WHAT WE BUILT (verified, no bolt-on): the qpDstat Part-B genotype seam — src/core/stats/dstat.cpp (read_ind/read_snp/read_tile/decode_af/assign_blocks front-end) + src/device/cuda/dstat_kernel.cu (the allsnps=TRUE per-(item,block) finiteness segmented reduction, dstat_kernel.cu:65) — — this kernel computes the f4 NUMERATOR (a-b)(c-d) per-(item,block) from genotypes, allsnps, batched: it IS the genotype-f4 engine (the AT2 f4blockdat) qpfstats is built on, built in qpDstat-B as the deliberate on-ramp for the genotype-path tools. qpfstats DRIVES this engine over ALL the population combos AT2 qpfstats regresses; VERIFY against AT2 qpfstats source the EXACT per-block basis (the f4blockdat combos + the underlying f-stat basis it solves for) — do NOT assume pairwise-f2. Then the SMOOTHING REGRESSION on the GPU (the already-designed shape): A_shared = x.x + ridge*I via ONE cublasDsyrk; ONE cusolverDnDpotrf (shared factor); RHS = x.ymat via ONE cublasDgemm; the [npair x nblock] multi-column solve via a cublasDtrsm PAIR (lower then upper — NOT potrsBatched, which is nrhs=1); NaN/missing blocks grouped by a CUB segmented mask + per-group downdate + batched potrfBatched/potrsBatched; output scattered to a smoothed F2BlockTensor (device-resident, the existing f2-dir format). REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND (no host per-block loop; reformulate, do NOT transliterate the AT2 host block loop); VERIFY (real code file:line + admixtools qpfstats source); REUSE no parallel bolt-on; EmulatedFp64{40} default (matmul-heavy syrk/gemm) + native carve-out (cancellation); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+ (verify cuSOLVER/cuBLAS/CUB vs docs); KEEP ON-DEVICE no warts/bouncing (the f2 tensor + the long frame stay resident). ORACLE = admixtools-R qpfstats(GENOTYPES) -> the smoothed f2 TENSOR (R-ridge, the project standard; no-AT2 lifted for golden GENERATION only): on box5090 run admixtools::qpfstats(the 9-pop raw TGENO prefix we already use for qpDstat-B, pops) -> golden_qpfstats_geno (the smoothed f2 tensor); steppe reproduces it element-wise at the tier. DELIVER: run_qpfstats (genotype-path) + a `steppe qpfstats` CLI (outputs a smoothed f2 dir) + a steppe.qpfstats binding + a CpuBackend oracle + the f2-tensor golden gate. Discipline: design+regen-golden (GATE) -> implement (oracle-first then on-device) -> verify (golden + GPU-bound + emulated + ctest) -> commit. FAIL-PROTOCOL: verify->commit on green; on BAD HALT + report + defer (NEVER silently fail/revert); on failure git stash push -u + HALT.',
  phases: [ { title: 'Design + regen the genotype qpfstats golden (GATE: reuse qpDstat-B seam)' }, { title: 'Implement run_qpfstats (genotype-path, reuse the dstat seam) + CLI + binding + build' }, { title: 'Verify f2-tensor golden + GPU-bound + emulated + commit-or-HALT' } ],
}

const R = '/home/suzunik/steppe'
const SSH = 'ssh box5090'
const PATHENV = 'export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0'
const CORECLEAN = 'rm -f /var/lib/vastai_kaalia/data/core-* 2>/dev/null'
const RSYNC = 'rsync -az --delete-after --exclude .git --exclude build --exclude build-rel --exclude aadr --exclude atlas_results -e ssh ' + R + '/ box5090:/workspace/steppe/'
const PULL = 'rsync -az -e ssh box5090:/workspace/steppe/tests/reference/goldens/at2/ ' + R + '/tests/reference/goldens/at2/'
const BUILD = SSH + " 'cd /workspace/steppe && " + PATHENV + " && " + CORECLEAN + " && cmake -S . -B build-rel -GNinja -DCMAKE_BUILD_TYPE=Release -DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON >/tmp/cfg.log 2>&1 && cmake --build build-rel 2>&1 | tail -25 && echo === CTEST === && STEPPE_THOROUGH=1 ctest --test-dir build-rel --output-on-failure 2>&1 | tail -25; " + CORECLEAN + "'"
const STASH = 'cd ' + R + ' && git stash push -u -m'
const TGENO = '/workspace/data/aadr/raw/v66.p1_HO.aadr.patch.PUB'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. Branch phase2-fit-engine == main @ 41942ed. The qpfstats GPU-shape design is docs/research/qpfstats-gpu-design.md; the FIRST build run HALTed because the workflow mis-specified the oracle (f2-cache input + est/se/z/p golden) — qpfstats actually reads GENOTYPES (allsnps=TRUE) + outputs a smoothed f2 TENSOR.',
  'KEY FACT (VERIFIED this session): the genotype + allsnps machinery qpfstats needs ALREADY EXISTS in qpDstat Part B — REUSE it, do NOT build new: src/core/stats/dstat.cpp (run_dstat: read_ind(Explicit) + read_snp + read_tile + decode_af + assign_blocks — the genotype front-end) + src/device/cuda/dstat_kernel.cu (dstat_block_reduce_kernel: the allsnps=TRUE per-(item,block) finiteness segmented reduction, line ~65 "a SNP is used iff V==1 for all pops"; batched over the item AND n_block axes; reads resident Q/V [P x M]). THIS KERNEL COMPUTES THE f4 NUMERATOR (a-b)(c-d) per-(item,block) from genotypes — it IS the genotype-f4 engine (the AT2 f4blockdat), built in qpDstat-B as the deliberate on-ramp for the genotype-path tools. qpfstats DRIVES this engine over ALL the population combos AT2 qpfstats regresses; VERIFY against AT2 qpfstats source the EXACT per-block basis (the f4blockdat combos + the underlying f-stat basis it solves for) — do NOT assume pairwise-f2.',
  'qpfstats = a GENOTYPE-PATH JOINT f-stat SMOOTHER: genotypes (allsnps=TRUE) -> the per-block f-stat basis via the genotype-f4 engine (drive the dstat f4-numerator kernel over all the AT2 qpfstats combos; the EXACT basis VERIFIED vs AT2 source) -> the SMOOTHING REGRESSION -> a smoothed f-stat object (the F2BlockTensor / f2-dir format, so qpadm/f4/qpGraph consume it). NOT an f2-cache stat; NOT est/se/z/p.',
  'THE SMOOTHING REGRESSION (the already-designed GPU shape, on-device, NO host block loop): A_shared = x.x + ridge*I via ONE cublasDsyrk (cuda_backend.cu:2263); ONE cusolverDnDpotrf shared factor (:2299/:2302); RHS = x.ymat via ONE cublasDgemm (ymat = compact [npopcomb x nblock] = t(numer), device-resident, NOT the hundreds-of-GB host long frame); the [npair x nblock] multi-column back/forward solve via a cublasDtrsm PAIR (lower then upper) — NOTE potrsBatched is nrhs=1-ONLY (cuda_backend.cu:3323), so it is a Dtrsm pair, NOT a single potrsBatched; NaN/missing blocks grouped by a CUB segmented mask + per-group downdate A_i = A_shared - x[nan].x[nan] + a per-group batched potrfBatched/potrsBatched (:3342/:3370). Scatter b -> a smoothed [P x P x nblock] DeviceF2Blocks + recenter. jackknife_cov (backend.hpp:791) for the covariance pass.',
  'STANDING REQUIREMENTS (locked): GPU-FIRST + GPU-BOUND (no host per-jackknife-block loop; reformulate not transliterate); VERIFY (file:line + admixtools qpfstats source — the box has AdmixTools C qpfstats.c + admixtools-R); REUSE no parallel bolt-on; EmulatedFp64{40} default (the syrk/gemm) + native carve-out (the cancellation numerator); SINGLE-GPU --device 0 (multi-GPU PARKED); CUDA 13+ (verify cuSOLVER/cuBLAS/CUB vs docs); KEEP ON-DEVICE no warts/bouncing.',
  'ORACLE = admixtools-R qpfstats(GENOTYPES) -> the smoothed f2 TENSOR (R-ridge; the project standard; no-AT2 lifted for golden GENERATION only). On box5090: admixtools::qpfstats(the 9-pop raw TGENO prefix ' + TGENO + ' [the SAME genotypes qpDstat-B uses], the 9 fit0 pops) -> golden_qpfstats_geno.{csv/bin} (the smoothed f2 tensor, e.g. the global f2 table + per-block slices). steppe qpfstats over the SAME genotypes reproduces it element-wise at the tier. PROVE it. If the admixtools-R qpfstats output is genuinely ambiguous/irreproducible, HALT + defer (do NOT guess).',
  'FAIL-PROTOCOL (USER-MANDATED, STRICT): NEVER git checkout -- . / git clean -fd. verify->commit ONLY on green. On a BAD result (golden mismatch beyond tier, CPU-bound, structural blocker, oracle ambiguity) HALT + report + DEFER — do NOT silently fail, do NOT revert without asking. On any failure ' + STASH + ' "wip:qpfstats-FAILED-<reason>" + HALT.',
  'SINGLE-GPU --device 0. RELEASE build-rel. REAL AADR; no synthetic. The 9-pop raw TGENO at ' + TGENO + '. Box ' + SSH + '; nvcc -> ' + PATHENV + '. §4 layering. nothing builds locally.',
].join('\n')

async function tryAgent(p, opts) { let r = await agent(p, opts); for (let i=0;i<2&&r===null;i++){ log(opts.label+': transient null/500 — retry '+(i+1)); r = await agent(p, {...opts, label: opts.label+':retry'+(i+1)}) } return r }

phase('Design + regen the genotype qpfstats golden (GATE: reuse qpDstat-B seam)')
const DESIGN_SCHEMA = {
  type: 'object', additionalProperties: false, required: ['feasible','reuse_qpdstat_seam','pairwise_generalization','smoothing_gpu_shape','oracle_golden','fixture_match_check','emulated_plan','cuda13_apis','blocker','notes'],
  properties: {
    feasible: { type: 'boolean', description: 'true iff qpfstats is buildable GPU-bound by REUSING the qpDstat-B genotype seam (generalized pairwise) + the smoothing regression, with a reproducible admixtools-R genotype f2-tensor golden. false -> HALT + defer' },
    reuse_qpdstat_seam: { type: 'string', description: 'EXACTLY how the qpDstat-B genotype seam is reused (dstat.cpp front-end + dstat_kernel allsnps finiteness), file:line — confirm it is reused not rebuilt' },
    pairwise_generalization: { type: 'string', description: 'how the per-(quadruple,block) dstat reduction generalizes to per-(PAIR,block) for the full f2 basis (the qpfstats input)' },
    smoothing_gpu_shape: { type: 'string', description: 'the on-device smoothing regression (syrk + shared potrf + gemm RHS + Dtrsm pair + NaN-group batched), no host block loop, file:line for the reused primitives' },
    oracle_golden: { type: 'string', description: 'the regenerated golden_qpfstats_geno (path + format) via admixtools-R qpfstats(the 9-pop TGENO)' },
    fixture_match_check: { type: 'string', description: 'PROOF steppe qpfstats over the same genotypes reproduces the admixtools-R f2-tensor golden ~ the tier' },
    emulated_plan: { type: 'string', description: 'EmulatedFp64{40} (syrk/gemm) vs native carve-out (cancellation), via engage_*/emulation_honorable' },
    cuda13_apis: { type: 'string', description: 'the cuSOLVER/cuBLAS/CUB APIs VERIFIED vs CUDA 13.x docs (potrf/Dtrsm/potrfBatched/segmented-select)' },
    blocker: { type: 'string', description: 'if NOT feasible: the structural blocker / oracle ambiguity + options (HALT + defer)' },
    notes: { type: 'string' },
  },
}
const design = await tryAgent([
  'You are designing qpfstats GPU-first as a GENOTYPE-PATH f2 SMOOTHER (verify-before-implement; NO steppe code changes). FIRST confirm the reuse: READ src/core/stats/dstat.cpp (the genotype front-end) + src/device/cuda/dstat_kernel.cu (the allsnps per-(item,block) finiteness reduction) — qpfstats REUSES this, generalized per-(PAIR). READ docs/research/qpfstats-gpu-design.md + the admixtools qpfstats method (web github uqrmaie1/admixtools + the box AdmixTools C qpfstats.c), the smoothing-regression primitives (cuda_backend.cu cublasDsyrk:2263 / cusolverDnDpotrf:2299 / cublasDgemm / cublasDtrsm / potrfBatched:3342 + the nrhs=1 potrsBatched note :3323), F2BlockTensor (the smoothed-f2 output format), jackknife_cov, the CLI/binding scaffold. VERIFY the cuSOLVER/cuBLAS/CUB APIs vs the CUDA 13.x docs. On box5090 (AT2/R, lifted no-AT2 for GENERATION): admixtools::qpfstats(the 9-pop raw TGENO prefix ' + TGENO + ', the 9 fit0 pops) -> golden_qpfstats_geno (the smoothed f2 tensor + its format); PROVE steppe qpfstats-from-the-same-genotypes can match it; pull ' + PULL + ' . Design run_qpfstats GPU-bound (reuse the dstat seam pairwise + the smoothing regression, device-resident, emulated-FP64). Return the structured design. If reuse is NOT actually possible OR the admixtools-R qpfstats oracle is ambiguous/irreproducible, feasible=false + blocker (HALT + defer, do NOT guess).', STD,
].join('\n'), { schema: DESIGN_SCHEMA, label: 'design:qpfstats', phase: 'Design + regen the genotype qpfstats golden (GATE: reuse qpDstat-B seam)' })
if (design === null) { log('--- design died — HALT'); return { halted: true } }
if (!design.feasible) { log('--- qpfstats STRUCTURAL/oracle-ambiguous (HALT + defer): ' + design.blocker); return { halted: true, deferred: true, design } }
log('qpfstats feasible (reuses qpDstat-B seam): ' + String(design.fixture_match_check).slice(0,120))

phase('Implement run_qpfstats (genotype-path, reuse the dstat seam) + CLI + binding + build')
const fixer = await tryAgent([
  'You are implementing qpfstats per this design (GENOTYPE-PATH, REUSE the qpDstat-B seam generalized pairwise; GPU-bound; emulated-first; on-device):\n<<<\n' + JSON.stringify(design) + '\n>>>\n\nDo NOT commit. Do NOT git checkout/clean (git stash if forced).', STD, '', 'DEV LOOP: edit locally; ' + RSYNC + '; build+ctest (' + BUILD + ').', '',
  'IMPLEMENT: a CpuBackend oracle run_qpfstats (small_linalg, native — the reference) FIRST, then the GPU-bound path: REUSE the qpDstat-B genotype front-end (dstat.cpp) + the dstat allsnps finiteness reduction GENERALIZED to per-(PAIR,block) -> the full pairwise per-block f2 basis; then the on-device smoothing regression (syrk + shared potrf + gemm RHS + the Dtrsm pair + NaN-group batched; device-resident; emulated-FP64 matmul + native cancellation carve-out; NO host per-block loop, NO host long-frame); scatter to a smoothed F2BlockTensor; write it as an f2 dir (so qpadm/f4 consume it). Add a `steppe qpfstats` CLI (genotype --prefix + --pops -> smoothed f2 dir) + a steppe.qpfstats binding. Gate: a cli_qpfstats ctest + a pytest reproducing golden_qpfstats_geno (the f2 tensor element-wise) at the tier. Build + full STEPPE_THOROUGH ctest. SANITY: cli_qpfstats reproduces the golden; CpuBackend==CudaBackend; existing goldens/ctest green; the smoothed f2 dir is read_f2-able. Report files, the REUSE of the dstat seam (file:line), the GPU-bound smoothing (no host block loop), the gate, the FULL ctest. Do NOT commit. BAD/NON-trivial blocker -> STOP + report (do NOT revert).',
].join('\n'), { label: 'implement:qpfstats', phase: 'Implement run_qpfstats (genotype-path, reuse the dstat seam) + CLI + binding + build' })
if (fixer === null) { log('--- fixer died — HALT'); return { halted: true, design } }
await tryAgent(['BUILD-REPAIR for qpfstats. Accumulated edits (do NOT clean/revert/destroy; git stash only if forced). Reach a CLEAN Release build (-DSTEPPE_BUILD_CLI=ON -DSTEPPE_BUILD_PYTHON=ON) + green ctest, patching only trivial -Werror / CMake / cuSOLVER-cuBLAS-CUB API misuse (VERIFY vs CUDA 13 docs). DO: ' + RSYNC + ' then ' + BUILD + '. Loop up to 5x on trivial errors. NON-trivial -> STOP + report. Report final build + ctest + patches.', STD].join('\n'), { label: 'repair', phase: 'Implement run_qpfstats (genotype-path, reuse the dstat seam) + CLI + binding + build' })

phase('Verify f2-tensor golden + GPU-bound + emulated + commit-or-HALT')
const VERDICT_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['pass','fail_severity','golden_reproduced','reuses_dstat_seam','gpu_bound','no_host_block_loop','emulated_fp64','goldens_green','build_clean','commit_hash','stash_ref','note'],
  properties: {
    pass: { type: 'boolean', description: 'true ONLY if steppe qpfstats reproduces the admixtools-R genotype f2-tensor golden at the tier (cli + pytest), REUSES the qpDstat-B genotype seam (not rebuilt), the smoothing solve is GPU-bound (no host per-block loop, device-resident), emulated-FP64 default, full ctest green, single-GPU, no synthetic, CpuBackend==CudaBackend, the smoothed f2 dir is read_f2-able' },
    fail_severity: { type: 'string', description: 'if pass=false: minor or bad (HALT + defer)' },
    golden_reproduced: { type: 'boolean' }, reuses_dstat_seam: { type: 'boolean', description: 'reuses the qpDstat-B genotype front-end + allsnps finiteness (generalized pairwise), not a parallel rebuild' },
    gpu_bound: { type: 'boolean' }, no_host_block_loop: { type: 'boolean' }, emulated_fp64: { type: 'boolean' }, goldens_green: { type: 'boolean' }, build_clean: { type: 'boolean' },
    commit_hash: { type: 'string' }, stash_ref: { type: 'string' },
    note: { type: 'string', description: 'the golden reproduction + the seam reuse + the GPU-bound smoothing; on FAIL the blocker + minor-vs-bad' },
  },
}
const verdict = await tryAgent([
  'You are the INDEPENDENT VERDICT for qpfstats (adversarial; hold the bars: REUSES the qpDstat-B genotype seam [not rebuilt], GPU-bound [no host block loop], emulated-first, on-device). design:\n<<<\n' + JSON.stringify(design) + '\n>>>\nimplementer:\n<<<\n' + fixer + '\n>>>', STD, '',
  'DO: (1) git diff review — qpfstats REUSES the qpDstat-B genotype front-end + the allsnps finiteness reduction (generalized pairwise, NOT a parallel rebuild); the smoothing solve is the shared-factor batched cuSOLVER path (NO host per-block loop, NO host long-frame), device-resident, emulated-FP64 + native carve-out; CpuBackend oracle present; the output is a read_f2-able smoothed f2 dir. (2) ' + BUILD + ' — STEPPE_THOROUGH ctest green; cli_qpfstats + pytest reproduce the admixtools-R genotype f2-tensor golden at the tier; CpuBackend==CudaBackend. (3) GPU-bound, single-GPU, no synthetic. PASS only if all hold. ',
  'ON PASS: cd ' + R + ' && git add ONLY the new golden + new/changed source/test/cmake/binding/doc (NEVER git add dot; never aadr/ atlas_results/), commit (STEP3 qpfstats: genotype-path joint f2-smoother `steppe qpfstats` + steppe.qpfstats binding — REUSES the qpDstat-B genotype seam (allsnps, pairwise) + the GPU-bound shared-factor smoothing regression [no host block loop], emulated-FP64; outputs a smoothed f2 dir; admixtools-R genotype golden) ending: Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com> . Capture the hash. Update docs (RESUME/TODO: qpfstats done; next qpGraph). ',
  'ON BAD: DO NOT git checkout/clean, DO NOT revert. ' + STASH + ' "wip:qpfstats-FAILED" (capture the ref). Classify fail_severity=bad + the blocker for the user. Return the structured verdict.',
].join('\n'), { schema: VERDICT_SCHEMA, label: 'verify:qpfstats', phase: 'Verify f2-tensor golden + GPU-bound + emulated + commit-or-HALT' })
if (verdict === null) { log('--- verdict died — HALT'); return { halted: true, design } }
if (verdict.pass) log('+++ qpfstats ' + verdict.commit_hash + ' — ' + verdict.note)
else log('--- qpfstats FAILED [' + verdict.fail_severity + '] — stashed ' + verdict.stash_ref + ' — HALT+defer — ' + verdict.note)
return { design, verdict }
