export const meta = {
  name: 'architecture-md-refresh',
  description: 'Bring the CANONICAL spec docs/architecture.md current with what we built (device-resident output + M5 tiered/input streaming), WITHOUT touching the forward-looking spec (the unbuilt fit engine S3-S8, M6/M7, the precision policy §12). The spec already MANDATED out-of-core streaming (§11.1) + GPU-resident f2_blocks (§5/§11.1) — M5 implemented that — so this is mostly: (a) reconcile the "MB-scale tiny f2_blocks" premise (§0/§11.1) with the at-scale reality (76GB @P=2500); (b) add the M5 ADAPTIVE OUTPUT TIERING (Resident/HostRam/Disk) the spec lacks (it assumes always-resident); (c) describe the device-resident output type (DeviceF2Blocks + opt-in to_host) + note SNP-tile input streaming is BUILT; (d) correct §11.4 multi-GPU framing to the honest measured role (modest throughput on the precompute, real wins were off-CPU device-resident+streaming, home is the fit/rotation). Audit (read-only, find every stale/under-specified/tensioned claim with section+line) -> update -> consistency. EDIT-ONLY (no git); human commits + merges.',
  phases: [
    { title: 'Audit', detail: 'read-only: cross-check architecture.md sections vs the built reality, section+line discrepancies' },
    { title: 'Update', detail: 'apply the spec updates (preserve forward-looking + §12) + consistency pass' },
  ],
}

const R = '/home/suzunik/steppe'

const FACTS = [
  'TASK: bring docs/architecture.md (the CANONICAL design spec, 907 lines, last substantive touch predates the device-resident + M5 work) current with what we BUILT. PRESERVE the forward-looking spec exactly (the unbuilt FIT engine S3-S8, M6/M7, ADRs, the precision policy §12, the layering §4) — only update what we have now BUILT differently/beyond what the spec describes, and reconcile internal contradictions our work exposed. Do NOT mark unbuilt things (the fit engine) as done. EDIT IN PLACE; no git.',
  'WHAT WE BUILT (all merged to main @ 9518551; cite commits): (1) DEVICE-RESIDENT OUTPUT (1f80c0c) — the precompute returns a device-resident handle DeviceF2Blocks (result stays in VRAM); the host F2BlockTensor is now an OPT-IN .to_host() materialization, NOT the forced return. This ALIGNS with the spec mandate (§5:235 "keep f2_blocks GPU-resident"; §11.1:673 "stays on the device across the whole stream and the entire downstream model search") — the code now does what the spec said. ~4.3x at P=512 vs the old forced host round-trip. (2) M5 ADAPTIVE TIERED OUTPUT (176a07d) — when the result does NOT fit VRAM, it streams to the fastest tier it FITS: Resident (VRAM) -> HostRam -> Disk, auto-selected at runtime from cudaMemGetInfo + sysinfo (new src/device/tier_select.hpp, OutputTier enum). The spec currently assumes the output is ALWAYS small + resident — this output-does-not-fit case is unspecified. (3) M5 SNP-tile INPUT STREAMING (c65179f) — the exact mechanism §11.1 already specifies (tile over SNPs, per-block decode, tiny resident accumulator), now BUILT: GPU footprint O(P*tile + P^2) independent of M, no all-M feeder. (4) full-autosome (M=584131, n_block=757) P=2500 COMPLETES on a single 32GB GPU in ~51.5s (76GB result streamed), parity bit-identical (see docs/cleanup/m5/00-results.md).',
  'THE INTERNAL TENSION TO RECONCILE: §0:12 + §11.1:669 call f2_blocks "tiny / MB-scale", but §11.4:717 concedes it reaches ~220GB at P=4266. Both are right at different scales: MB-scale at AT2 typical P (tens of pops), tens-of-GB at thousands (76GB @P=2500). Fix the contradiction: f2_blocks is small at AT2-scale (where it stays resident) but O(P^2*n_block) and large at thousands of pops — which is exactly WHY M5 adaptive output tiering exists. Frame it as scale-dependent, not flatly "tiny".',
  'THE HONEST MULTI-GPU CORRECTION (§11.4, §0/§1 multi-GPU framing): §11.4 already describes the capability-tiered combine (host-staged + device-resident P2P) correctly. But the framing of multi-GPU as the day-one "parallelism + speedup" objective is optimistic vs what we MEASURED: on the PRECOMPUTE, multi-GPU is a MODEST throughput layer (it was measured SLOWER than single-GPU until the data-bounce was fixed; nsys ~22-74% overlap, a serial D2H/host tail; see docs/cleanup/m4.5/{why-multigpu-slow,parallelism-check,architecture-audit}.md). The REAL precompute wins were getting the result OFF the CPU (device-resident output) + streaming. Multi-GPU genuinely SHINES on the FIT/ROTATION phase (S8: thousands of INDEPENDENT models, zero inter-GPU traffic) — which the spec ALREADY says (§11.4:721, §1). So: keep the day-one multi-GPU scope, but add the honest note that on the precompute it is a modest layer (not the at-scale enabler — M5 streaming is), and its decisive payoff is S8. Do NOT overclaim; do NOT delete the existing (correct) combine description.',
  'KEY SPEC ANCHORS to update (verified): §0:12 (the MB-scale premise + the precompute/fit split), §1:37/41/46-47 (vision, out-of-core, the f2_blocks interchange artifact), the file map ~:126 (F2BlockTensor handle — note DeviceF2Blocks/tier_select/stream_f2_blocks now built), §5:208/229/235 (S2 + the GPU-resident corollary + multi-GPU S0-S2/S8), §11.1:667-674 (out-of-core streaming — now BUILT via M5; add that the OUTPUT also tiers when it does not fit), §11.2:693/701 (the resident pair budget — add the output-does-not-fit -> tiered streaming path), §11.4:713-721 (SPMG + the combine + the honest multi-GPU role). PRESERVE §12 (precision/parity) and the fit-engine spec.',
  'NEW SOURCE FILES (built, for the file map / references): src/device/tier_select.hpp (OutputTier + select), src/device/stream_f2_blocks.hpp, src/device/f2_disk_format.hpp, src/device/host_ram.cpp, src/device/device_f2_blocks.hpp (+ cuda/device_f2_blocks*.cu*). The measured results live in docs/cleanup/m5/00-results.md.',
].join('\n')

phase('Audit')
const audits = await parallel([
  () => agent([
    'You are a spec auditor. READ docs/architecture.md sections §0, §1, §5, and the file map, and the implemented reality (the FACTS + the M5 source + docs/cleanup/m5/00-results.md). Find EVERY claim that our built work makes stale, under-specified, or internally contradictory. READ-ONLY; cite section + line.', FACTS, '',
    'Focus: (1) the "tiny / MB-scale f2_blocks" premise (§0:12, §1:37/41) vs the at-scale reality + the §11.4 220GB concession — flag the contradiction + the precise fix wording; (2) the deliverable/output type (§1 interchange artifact, file map ~:126 F2BlockTensor) vs the device-resident DeviceF2Blocks + opt-in to_host; (3) §5:235 "keep f2_blocks GPU-resident" — confirm the device-resident output now satisfies it (alignment, not a change); (4) anything in §0/§1/§5 about multi-GPU that needs the honest-role caveat. Return a precise per-item list: section:line | current text (short) | what is stale/missing | the exact corrected wording (concise, in the spec voice) | PRESERVE-or-CHANGE.',
  ].join('\n'), { label: 'audit:vision-pipeline', phase: 'Audit' }),
  () => agent([
    'You are a spec auditor. READ docs/architecture.md §11 (especially §11.1 out-of-core streaming, §11.2 budgets, §11.4 SPMG/multi-GPU/combine) and the implemented reality. Find EVERY claim our M5 + device-resident + multi-GPU work makes stale, under-specified, or now-built. READ-ONLY; cite section + line.', FACTS, '',
    'Focus: (1) §11.1:667-674 — the spec specifies SNP-tile streaming + tiny resident accumulator; note it is now BUILT (M5 c65179f, per-block decode, O(P*tile+P^2)); AND add the missing case: the OUTPUT does not always fit resident -> M5 adaptive tiering (Resident/HostRam/Disk, tier_select.hpp) streams it; (2) §11.2:693/701 — the resident-pair VRAM budget assumes the result fits; add the tiered-streaming path for when it does not (the budget/tier-select decides); (3) §11.4:713-721 — keep the (correct) combine description, but correct the multi-GPU ROLE framing to the honest measured finding (modest throughput on the precompute; the real precompute wins were off-CPU device-resident + streaming; the decisive multi-GPU payoff is S8 fit/rotation, which the spec already says). Return a precise per-item list: section:line | current text (short) | stale/missing/now-built | the exact corrected/added wording (concise, spec voice) | PRESERVE-or-CHANGE. Do NOT touch §12.',
  ].join('\n'), { label: 'audit:budgets-multigpu', phase: 'Audit' }),
]).then(a => a.filter(Boolean))

phase('Update')
const update = await agent([
  'You are the lead architect/editor. Apply the audited updates to docs/architecture.md (Edit tool) so the canonical spec is current with what we built — PRESERVING the forward-looking spec (the unbuilt fit engine S3-S8, M6/M7, ADRs, §12 precision/parity, §4 layering) verbatim. EDIT IN PLACE; no git.', FACTS, '',
  'THE AUDITS (per-section discrepancy lists):\n<<<\n' + audits.map((a,i)=>'### audit '+(i+1)+'\n'+a).join('\n\n') + '\n>>>', '',
  'Apply, in the spec voice (concise, dense, matching the surrounding prose), citing commits where natural (1f80c0c device-resident, 176a07d tiered, c65179f input-streaming) and pointing to docs/cleanup/m5/00-results.md for the measured numbers: (1) RECONCILE the "tiny/MB-scale f2_blocks" premise -> scale-dependent (MB-scale at AT2 P; O(P^2*n_block), tens-of-GB at thousands -> 76GB @P=2500), which is WHY M5 output tiering exists; (2) ADD the M5 ADAPTIVE OUTPUT TIERING (Resident VRAM -> HostRam -> Disk, auto-selected from runtime free VRAM/RAM; tier_select.hpp) to §11.1/§11.2 as the path when the result does not fit resident; (3) NOTE the SNP-tile INPUT streaming is now BUILT (the §11.1 mechanism realized; per-block decode, O(P*tile+P^2) independent of M); (4) DESCRIBE the device-resident output (DeviceF2Blocks primary, host F2BlockTensor opt-in .to_host()) in §1/§5/the file map, framed as SATISFYING the §5:235/§11.1:673 residency mandate; (5) CORRECT §11.4 multi-GPU ROLE to the honest measured finding (modest throughput layer on the precompute; real precompute wins = off-CPU device-resident + streaming; decisive multi-GPU payoff is S8) — keep the existing combine description. Do NOT mark the fit engine as built. Do NOT touch §12. After editing, re-read the changed sections for internal consistency (no remaining "MB-scale/tiny" contradiction; the output-tiering and device-resident output coherent across §1/§5/§11).',
  'Return: the list of every edit made (section:line + what changed) + a 4-6 line confirmation that architecture.md is now current (device-resident output + M5 tiering/input-streaming described, the f2_blocks-size premise reconciled, the multi-GPU role honest) while the forward-looking fit-engine spec + §12 are preserved untouched.',
].join('\n'), { label: 'update-architecture-md', phase: 'Update' })

return { audits, update }
