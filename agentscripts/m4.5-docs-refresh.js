export const meta = {
  name: 'm4.5-docs-refresh',
  description: 'Refresh ALL docs to the post-fix reality: M4.5 single-node multi-GPU is DONE, bit-identical, AND now FASTER than single-GPU (1.10x @P=768, 1.22x @P=400) after the device-resident combine landed (867a4bf) — the data-bounce wart is fixed; the cudaMalloc/18%-overlap and P1-zeroing hypotheses were REFUTED. Parallel doc-updaters (each owns disjoint files, one authoritative fact-sheet) + a consistency critic that greps the whole repo for leftover "multi-GPU is slower" staleness. EDIT-ONLY (no git) — the human commits + merges m4.5-multigpu -> main after.',
  phases: [
    { title: 'Update', detail: 'parallel: each agent rewrites its docs stale parts to the new reality' },
    { title: 'Consistency', detail: 'grep the whole repo for leftover stale perf claims + fix; verify cross-refs' },
  ],
}

const R = '/home/suzunik/steppe'

const FACTS = [
  'AUTHORITATIVE CURRENT STATE (as of HEAD 867a4bf on branch m4.5-multigpu, about to be merged to main). Rewrite every doc claim to match this; preserve each doc\'s structure, tone, and unrelated content. EDIT IN PLACE (Edit/Write). Do NOT touch git (no add/commit) — the human commits + merges.',
  '1. M4.5 single-node multi-GPU f2 precompute is DONE: correct, bit-identical to single-GPU (memcmp parity proven on BOTH the host-staged AND the device-resident P2P combine paths, both datasets derived_acc P=50 + derived_full P=768, production EmulatedFp64{40}), AND NOW FASTER than single-GPU.',
  '2. MEASURED (rtxbox 2x RTX PRO 6000 Blackwell sm_120, Release, EmuFp64{40}, median of 10): P=768 G1=2342ms G2=2125ms = 1.10x; P=400 = 1.22x. This REPLACES the old "multi-GPU is SLOWER (0.70x / 0.75-0.97x)" finding — that was the PRE-FIX state and is now FALSE.',
  '3. ROOT CAUSE of the old slowdown (diagnosed by nsys, docs/cleanup/m4.5/why-multigpu-slow.md): the DATA-BOUNCE wart — a redundant SECOND full 7.14GB Device->Host copy. compute_f2_blocks D2H-copied each partial to host and freed its device buffers, forcing the P2P combine to re-upload (H2D) + place-add + a 2nd D2H. FIXED by the device-resident combine (commit 867a4bf): per-device compute leaves its partial RESIDENT (no D2H/no free, returns an opaque move-only DevicePartial handle); the combine allocates ONE root result, D2D-copies the root partial + cudaMemcpyPeers each peer partial straight into its DISJOINT block slice, then ONE final D2H. Deleted the re-upload H2D, the accumulator cudaMemset, the place-add kernel, the staging buffers, and the per-peer cudaDeviceSynchronize (now one fence before the final D2H).',
  '4. REFUTED hypotheses — do NOT carry these as open problems / next-steps anymore: (a) "the per-call cudaMalloc/cudaFree driver-lock serializes the device threads / only ~18% GPU overlap" — the nsys trace measured 74% overlap; the L4 pool-allocator is at most a few % and is NOT the speedup lever (demote to optional cleanup, not a blocker). (b) "the ~1.4s combine host-zeroing (P1 default-init allocator) is the dominant cost" — the accumulator memset is 5ms; the host assign just relocates its page-fault into the D2H; P1 was REVERTED and is not the cure. docs/cleanup/m4.5/perf-discovery.md\'s P1 premise was WRONG and is superseded by why-multigpu-slow.md.',
  '5. REMAINING OPTIONAL levers (NOT blockers, clearly tagged optional): pin the final pageable result D2H (the next available speedup, could push past 1.10x); the bench byte-traffic columns are observability-only (currently print 0.00). B2 (P2P transport rework, agentscripts/m4.5-b2-p2p-fix-pass.js) — its original motivation (the slow/bouncing combine) is now largely addressed by the resident combine; mark it as REASSESS / likely-subsumed rather than a pending speedup.',
  '6. Branch m4.5-multigpu (46 commits ahead of main) is being MERGED to main now — M4.5 milestone complete (correct AND faster). After merge: the real next work is the qpAdm FIT ENGINE (Phase 2, does not exist yet), then M5 out-of-core streaming, AT2 goldens (M7 gate), M6 merge. The perf rabbit-hole is CLOSED.',
  'KEY COMMITS to cite: 867a4bf (the device-resident combine fix), 165f655 (the nsys root-cause doc). The bench tool is tests/reference/bench_f2_multigpu.cu (now ITERS=10, median + p10/p90, out-of-band timers).',
].join('\n')

phase('Update')
const GROUPS = [
  { key: 'workflow-map', files: 'agentscripts/README.md', note: 'Update "WHERE WE ARE" (no longer slower — now 1.10x/1.22x, data-bounce fixed @867a4bf). In "WORKFLOWS WE STILL NEED": the L4 pool-allocator + nsys "measure-first" items are DONE/refuted (mark so); the why-slow investigation + device-resident-combine workflows are DONE (add rows for agentscripts/m4.5-why-slow-investigation.js and m4.5-device-resident-combine.js in the EXISTING WORKFLOWS table). B2 -> REASSESS/likely-subsumed. Update the bench-tool note (ITERS=10/median).' },
  { key: 'handoff', files: 'handoff-ba37d95.md', note: 'Update the TL;DR + "KEY OPEN FINDING" + the NEXT/forward map: multi-GPU is now FASTER (1.10x), the data-bounce wart is FIXED @867a4bf, the L4/B2 "next" items are refuted/subsumed. Keep it a faithful cold-start handoff but pointing at the CURRENT reality (note current HEAD 867a4bf). Do NOT rename the file.' },
  { key: 'todo', files: 'docs/TODO.md', note: 'Update the M4.5 section: remove/replace the "KEY OPEN FINDING: multi-GPU SLOWER" with the resolved state (1.10x, fixed @867a4bf); move the L4 pool-allocator / nsys-profile / "make multi-GPU faster" items to Done (or strike as refuted). Surface the real next milestone = the qpAdm fit engine (Phase 2). Keep the checklist format.' },
  { key: 'roadmap', files: 'docs/ROADMAP.md', note: 'Update the M4.5 status to DONE + bit-identical + FASTER (1.10x @768) on branch m4.5-multigpu, merged to main. Adjust any "next/perf" note that says multi-GPU needs a speedup — it has it. Keep the milestone ordering + rationale.' },
  { key: 'resume-runbook', files: 'docs/RESUME.md AND docs/BOX-RUNBOOK.md', note: 'docs/RESUME.md: update any reference to the multi-GPU slowdown / the pending perf work to the resolved state. docs/BOX-RUNBOOK.md: it is mostly setup mechanics — only touch if it makes a stale perf claim; keep the Release-build-for-perf guidance.' },
  { key: 'm45-cleanup-docs', files: 'docs/cleanup/m4.5/why-multigpu-slow.md AND docs/cleanup/m4.5/perf-discovery.md AND docs/cleanup/missing-fixes.md', note: 'why-multigpu-slow.md: ADD a "STATUS: RESOLVED" banner at the TOP — fixed by the device-resident combine @867a4bf, measured G2 1.10x (2125ms) vs G1 (2342ms) @P=768; KEEP the full diagnosis below (valuable history). perf-discovery.md: add a banner noting its P1/"~1.4s combine zeroing" premise was REFUTED on-box and SUPERSEDED by why-multigpu-slow.md (do not delete the doc; correct it). missing-fixes.md: P1 is now MOOT — it was refuted (not a speedup lever) and the real fix (device-resident combine) landed @867a4bf; update the P1 row + headline accordingly.' },
  { key: 'overviews', files: 'docs/cleanup/00-overview.md AND docs/cleanup/m4.5/00-overview.md', note: 'Update any row about the multi-GPU perf / B2 P2P transport / the slowdown to the resolved state (device-resident combine landed @867a4bf, multi-GPU now faster; B2 reassess/subsumed). Keep the per-file audit backlog structure intact; only touch the stale perf/B2 claims.' },
]
const updates = await parallel(GROUPS.map((g) => () => agent([
  'You are a senior engineer + technical writer refreshing steppe docs to the current reality. EDIT the file(s) in place (Edit/Write). Do NOT touch git. Preserve structure, tone, formatting, and all UNRELATED content — change ONLY what the facts below make stale.', FACTS, '',
  'YOUR FILE(S): ' + g.files + '\nFOCUS: ' + g.note, '',
  'Read your file(s) first, find every stale claim (search for: "slower", "0.70", "0.7x", "0.75", "SLOWER", "L4 pool", "cudaMalloc serial", "18%", "currently SLOWER", "NEXT: ... L4", "not a speedup", any "multi-GPU needs faster"), and rewrite each to the authoritative state. Be precise (cite 867a4bf / the measured 1.10x where relevant). Do NOT invent facts beyond the fact-sheet. Return a short list of every edit you made (file + what changed).',
].join('\n'), { label: 'update:' + g.key, phase: 'Update' }))).then(a => a)

phase('Consistency')
const critic = await agent([
  'You are the consistency critic. The doc-updaters just refreshed steppe docs to the post-fix reality (multi-GPU now 1.10x, data-bounce fixed @867a4bf). Your job: find + fix any LEFTOVER stale claim anywhere in the repo docs, and verify cross-references are coherent. EDIT in place; do NOT touch git.', FACTS, '',
  'WHAT THE UPDATERS REPORTED:\n<<<\n' + updates.map((u,i)=>'['+GROUPS[i].key+'] '+(u||'(agent died — its files may still be stale, CHECK them)')).join('\n\n') + '\n>>>', '',
  'DO: (1) grep the whole repo for residual stale perf claims: cd ' + R + ' && grep -rinE "slower than single|0\\.70x|0\\.7x|0\\.75|currently SLOWER|L4 pool[- ]?alloc|18% (GPU )?overlap|multi-?GPU is (currently )?slow" docs/ agentscripts/README.md handoff-*.md . Inspect EACH hit: if it is a stale M4.5 multi-GPU perf claim, FIX it to the new reality; if it is unrelated (e.g. docs/cleanup/io-filter-filter_decision.md may say "slower" about CPU vs GPU filtering, or docs/architecture.md may describe the algorithm/design not a perf result), LEAVE it and note why. (2) Spot-check docs/architecture.md for any M4.5 perf claim that is now wrong (the algorithm/design description is fine; only a stale "slower" measurement claim needs fixing). (3) Confirm the cross-references are coherent: why-multigpu-slow.md has its RESOLVED banner, perf-discovery.md is marked superseded, missing-fixes.md P1 is moot, the workflow map lists the two new workflows as done. (4) Make any final fix-up edits.',
  'Return: the grep results + a per-hit verdict (FIXED / left-unrelated-because), and a final 3-5 line confirmation that the docs now consistently reflect "M4.5 multi-GPU DONE + bit-identical + 1.10x faster, data-bounce fixed @867a4bf" with no surviving "slower" claims.',
].join('\n'), { label: 'consistency-critic', phase: 'Consistency' })

return { updates, critic }
