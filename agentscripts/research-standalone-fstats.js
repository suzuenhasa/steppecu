export const meta = {
  name: 'research-standalone-fstats',
  description: 'Research what it takes to build the STANDALONE f-statistics on top of steppe (f4, f3/outgroup-f3/admixture-f3, D-statistic/ABBA-BABA, f4-ratio, qpDstat-style batched D/f4; + the bigger qpfstats, DATES, qpGraph) — for EACH: the math it computes, what of steppe code is REUSABLE (the f4 contraction, f2_blocks, the block-jackknife SE, the GLS, the f2-dir reader, pop resolution, the rank test, the CLI/output/golden scaffolding), what is NEW machinery, and the effort/order. READ-ONLY (read steppe source + web for the AT2/ADMIXTOOLS f-stat definitions); NO code changes, NO box compute. HARD VERIFY MANDATE (the user lesson — comments/docs/memory have repeatedly LIED this project: corrupt goldens, multi-GPU "sucked" but rotation was fine, streaming built-but-unwired, qpwave golden metadata stale): EVERY claim about what exists / is internal-only / is parked / is missing / is reusable MUST be VERIFIED against the actual source with a file:line citation. Do NOT trust a comment, doc, ROADMAP, TODO, or memory at face value — if any says "X does not exist" or "X is parked" or "X is deferred", OPEN THE CODE and confirm the TRUTH (does X exist? is it wired? is it actually parked, or done, or never built?), and EXPLICITLY flag any comment-vs-code mismatch found. Concrete example the user gave: "a comment says feature X does not exist -> check that it actually does not exist and is not just parked/unwired/already-built." Synthesis -> a CODE-VERIFIED build plan per standalone (reusable vs new, file:line evidence, effort, recommended order) at /tmp/standalone-fstats.md. No fabrication; flag every uncertainty.',
  phases: [ { title: 'Verify steppe f-stat machinery + survey the standalones' }, { title: 'Synthesize the verified build plan' } ],
}

const R = '/home/suzunik/steppe'
const STD = [
  'PROJECT: steppe = GPU/CUDA-13 reimplementation of ADMIXTOOLS 2 + qpAdm. main @ a8cbd40. The qpAdm/qpWave FIT ENGINE is built + golden-gated; the precompute (f2_blocks) is built; the CLI has extract-f2/qpadm/qpadm-rotate wired (qpwave CLI is a scaffold). The f4/f3 contractions are used INSIDE the fit. STEP 3 of the roadmap (per memory build-sequence-backend-first) = standalone f-stats, each shipped WITH its own CLI/bindings — but NOTHING has verified what exists vs what is new.',
  'GOAL: research the STANDALONE f-statistics on top of steppe — f4(A,B;C,D), f3/outgroup-f3/admixture-f3, D-statistic (ABBA-BABA = normalized f4), f4-ratio (admixture proportion), qpDstat-style batched D/f4 over many quadruples; and the BIGGER tools qpfstats (f-stat covariance smoothing), DATES (admixture dating via ancestry-LD decay), qpGraph (admixture-graph fitting). For each: the math, what steppe code is REUSABLE, what is NEW, the effort + order.',
  'THE HARD VERIFY MANDATE (NON-NEGOTIABLE): every definitive claim — "X exists" / "X is internal-only" / "X is parked/deferred" / "X is missing" / "Y is reusable" — MUST be backed by reading the ACTUAL source + a file:line citation. Comments, docs, ROADMAP/TODO, and the memory files have REPEATEDLY been wrong on this project (corrupt goldens trusted; multi-GPU "sucked" yet the rotation path was fine; the M5 streaming was built but unwired; the qpwave golden metadata was stale). So: do NOT trust any comment/doc/memory at face value. If a source says "no standalone f4 exists" or "X is parked", OPEN THE CODE and determine the TRUTH (built? wired? parked? never built?) and EXPLICITLY flag the mismatch if the comment/doc was wrong. Cite file:line for everything load-bearing.',
  'READ-ONLY: read the steppe repo at ' + R + ' (esp. src/core/qpadm/*, src/core/fstats/*, src/device/*, include/steppe/*, the cmd_*.cpp CLI scaffolding, the goldens/tests) + web for the AT2/ADMIXTOOLS definitions of each stat. NO code changes, NO box runs. Deliverable: /tmp/standalone-fstats.md.',
].join('\n')

phase('Verify steppe f-stat machinery + survey the standalones')
const LENSES = [
  { key: 'steppe-machinery', focus: 'VERIFY (file:line) the INVENTORY of f-stat primitives steppe ALREADY has + their reusability for standalones. Open the code — do NOT trust comments: (1) the f4 contraction — is there a reusable assemble_f4 / f4 routine, where (src/core/qpadm/* or src/core/fstats/*), is it exposed or buried in the fit, what is its signature/inputs (f2_blocks? a quadruple?); (2) f2_blocks — the in-VRAM/host f2 tensor + the f2-dir reader (read_f2_dir / DeviceF2Blocks) — reusable as the input to any f-stat?; (3) the block-jackknife SE machinery (the per-block leave-one-out + the weighted jackknife) — is it generic or qpAdm-specific?; (4) the rank test / qpWave (run_qpwave); (5) pop resolution (PopResolver), the CLI/output emitters, the golden-gate test pattern. For EACH: where it lives, whether it is standalone-callable or internal-only, and whether anything is parked/unwired. State what a standalone f-stat could REUSE verbatim vs what would need exposing. Flag any comment/doc that misstated what exists.' },
  { key: 'near-stats', focus: 'The NEAR standalones: f4(A,B;C,D), f3(C;A,B) (outgroup-f3 + admixture-f3), D-statistic (ABBA-BABA), f4-ratio (alpha = f4(A,O;X,C)/f4(A,O;B,C)), and qpDstat-style BATCHED D/f4 over many quadruples. For EACH: (a) the exact math (web: the AT2/Patterson definitions — f3(C;A,B)=E[(c-a)(c-b)], f4(A,B;C,D)=E[(a-b)(c-d)], D = f4 normalized by the ABBA+BABA denominator, etc.); (b) what steppe code computes it ALREADY or NEARLY (verify against the lens-A inventory — does the f4 contraction + f2_blocks + jackknife already give f4/D/f3 with just a new entry+CLI? is f3 derivable from f2? cite the code); (c) the GAP (what is genuinely new: the batched-quadruple enumeration for qpDstat? the f4-ratio division + its SE? the f3 bias term?); (d) effort. These should be mostly "expose + wrap existing f2/f4 machinery + a new CLI/output + a golden" — VERIFY that claim against the code rather than asserting it.' },
  { key: 'far-stats', focus: 'The FAR standalones — the ones needing genuinely NEW machinery. (a) qpfstats: the f-statistic covariance estimation/smoothing (what AT2 qpfstats does) — what does it need beyond the f2/jackknife steppe has?; (b) DATES (and ALDER/ROLLOFF-family): admixture DATING via the decay of ancestry-LD / weighted covariance with genetic distance — does steppe have ANY LD / per-SNP-position / phased machinery (VERIFY — check the io/decode + the .snp genpos handling), or is this a whole new data pipeline?; (c) qpGraph: admixture-GRAPH fitting (fit f2/f3/f4 to a user-specified graph topology + optimize edge weights/admixture proportions, score the fit) — what optimizer/graph machinery would be new vs the f-stat + jackknife steppe reuses. For each: what steppe HAS (verify file:line) vs what is NEW, and a feasibility/effort call. Be honest about which are big (qpGraph/DATES) vs medium (qpfstats).' },
  { key: 'integration', focus: 'The INTEGRATION surface: what a NEW standalone stat costs to ship WITH its own CLI + bindings, given the EXISTING scaffolding. VERIFY against the actual pattern (read src/app/cmd_qpadm.cpp + cmd_rotate.cpp + cli_parse.cpp + the result emitters + a golden test like test_cli_qpadm.cpp + the f2-dir reader): the steps to add a stat = a run_<stat> core entry + a cmd_<stat>.cpp (reuse the f2-dir loader + PopResolver + the result emitter) + a cli_parse subcommand (reuse add_common_flags/add_output_flags) + a golden test. How much is boilerplate-reuse vs new per stat? Also the bindings surface (nanobind, per docs/research/interop-usecases.md + cli-bindings.md): what the numpy/pandas entry for a standalone stat needs. Quantify the per-stat integration cost (files touched, what is reused). VERIFY the reuse claims (do these helpers/loaders actually exist + are they reusable? cite file:line).' },
]
const found = await parallel(LENSES.map(L => () => agent([
  'You are a popgen-methods + steppe-codebase analyst, PRECISE + CODE-VERIFIED. Your lens: ' + L.focus, '', STD,
  '', 'Read the actual steppe source (cite file:line for every existence/reuse/parked claim) + web for the stat math. Return your findings with a clear conclusion. CRITICAL: verify, do not trust comments/docs/memory; flag any comment-vs-code mismatch you find. No fabrication.',
].join('\n'), { label: 'fstat:' + L.key, phase: 'Verify steppe f-stat machinery + survey the standalones' })))

phase('Synthesize the verified build plan')
const spec = await agent([
  'You are the lead synthesizer producing the CODE-VERIFIED standalone-f-stats build plan for steppe. Combine the 4 lenses.', '', STD,
  '', 'THE LENSES:\n' + found.filter(Boolean).map((f,i)=>'### '+LENSES[i].key+'\n'+f).join('\n\n'),
  '', 'WRITE /tmp/standalone-fstats.md: (1) the VERIFIED inventory of reusable steppe machinery (the f4 contraction, f2_blocks, jackknife SE, rank test, f2-dir reader, PopResolver, CLI/output/golden scaffolding) — each with file:line + "standalone-callable or internal-only" + parked/done status; CALL OUT any comment/doc/memory that was WRONG vs the code. (2) PER STANDALONE (f4, f3/outgroup-f3/admixture-f3, D-stat, f4-ratio, qpDstat, qpfstats, DATES, qpGraph): the math, what is REUSABLE (file:line), what is NEW, and an effort tier (small/medium/large). (3) The per-stat INTEGRATION cost (the cmd_+run_+cli+golden+binding pattern, how much is reuse). (4) A RECOMMENDED ORDER (the cheap-reuse f4/D/f3/f4-ratio/qpDstat first since they likely reuse the f4+f2+jackknife; then qpfstats; qpGraph/DATES last as new-machinery). (5) An explicit "VERIFIED, not assumed" note on every load-bearing claim + a list of any comment-vs-code mismatches found. Use the Write tool. Return the headline: which standalones are cheap-reuse vs need-new-machinery, the recommended first target, and any comment-vs-code mismatch discovered. Cite file:line; no fabrication.',
].join('\n'), { label: 'synthesize:fstats', phase: 'Synthesize the verified build plan' })
log('standalone-fstats build plan -> /tmp/standalone-fstats.md: ' + String(spec).slice(0, 240))
return { spec }
