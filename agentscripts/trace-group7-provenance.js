export const meta = {
  name: 'trace-group7-provenance',
  description: 'Provenance archaeology for the deferred group-7 device-side DUPLICATION: for each duplicated pair — (a) the small vs large qpAdm fit-kernel TWINS in src/device/cuda/qpadm_fit_kernels.cu (dev_opt_A/_large, dev_opt_B/_large, dev_chisq_of/_large, the single-model vs _models f4 gather/loo/xtau, the 4x constrained weight solve, the ALS loop), and (b) the HostRamSink vs DiskSink ring-buffer staging writer in src/device/cuda/block_sink.cu — determine WHICH copy ENTERED first, AT WHICH COMMIT, and WHY, by checking git history (git log/blame/-L/pickaxe -S) AND the agent workflow/session history (agentscripts/*.js + the commit messages + the workflow transcripts that produced each commit). Output a provenance report mapping each duplicated region -> introducing commit -> the workflow/milestone behind it -> the why, and the CANONICAL copy for the dedup. READ-ONLY: git + file/transcript reading only, NO box, NO source edits, NO commits.',
  phases: [
    { title: 'Dig', detail: 'parallel: git archaeology on qpadm_fit_kernels.cu twins / on block_sink.cu sinks / map commits->workflows via agentscripts + transcripts' },
    { title: 'Synthesize', detail: 'provenance report: which entered when + why + the canonical copy' },
  ],
}

const R = '/home/suzunik/steppe'
const OUT = R + '/docs/cleanup/bigrefactor/group7-provenance.md'
const TX = '/home/suzunik/.claude/projects/-home-suzunik-steppe/48ef1b13-abff-4ff9-b585-bc29ae04d1e7'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell reimplementation of ADMIXTOOLS 2, git repo at ' + R + ' (branch phase2-fit-engine == main @ facf73e). The qpAdm fit + precompute were built milestone-by-milestone via WORKFLOWS in ' + R + '/agentscripts/*.js (each commits with a ROADMAP §6 message naming the milestone). Relevant milestones (commit -> what): M(fit-4) 3e78c79 the GPU qpAdm fit (the SMALL fixed-local-array kernels); gpu-large-models 01a94d5 the arbitrary-size cuSOLVER path (introduced the LARGE VRAM-scratch kernels); M(fit-6) S8 rotation 7852404 the model-BATCHED path (the _models kernels + fit_models_batched); parallelize-large-loo-se 8f485b7 (the loo_large_batched parallel LOO SE); M5 streaming (tiered output ~176a07d / c65179f) created block_sink.cu (the HostRam + Disk staging sinks).',
  'THE QUESTION: for the deferred group-7 DUPLICATION, find out WHICH copy of each duplicated pair ENTERED first, AT WHAT POINT (commit + date + the workflow/milestone), and WHY (the motivation in the commit message / the workflow brief). This determines the CANONICAL copy for the in-progress dedup (fix-group7-device).',
  'METHOD — whichever is easier per region, prefer GIT first then the workflow/session history to fill the WHY: (1) GIT: `git -C ' + R + ' log --oneline --follow -- src/device/cuda/qpadm_fit_kernels.cu` (and block_sink.cu); `git -C ' + R + ' log -L :<function>:src/device/cuda/qpadm_fit_kernels.cu` to trace a specific function\'s birth; `git -C ' + R + ' log -S "<unique code snippet>" --oneline -- <file>` (pickaxe) to find the commit that INTRODUCED a specific duplicated block; `git -C ' + R + ' show <commit> --stat` + the commit message for the WHY. (2) WORKFLOW/SESSION: the introducing commit message names the milestone; cross-ref ' + R + '/agentscripts/*.js (the workflow that built it) for the design intent; if needed, the workflow transcripts under ' + TX + '/subagents/workflows/ or the session jsonl ' + TX + '.jsonl (grep for the milestone/commit). Cite real commit hashes + dates + file:function.',
  'READ-ONLY: NO box, NO source edits, NO commits. Do NOT run builds. Output is a provenance report only. NO fabrication — every claim cites a real commit/file/line or transcript.',
].join('\n')

phase('Dig')
const LENSES = [
  { key: 'kernels-git', focus: 'GIT archaeology on src/device/cuda/qpadm_fit_kernels.cu — for EACH duplicated twin pair, find the commit that introduced EACH copy and the date: dev_opt_A vs dev_opt_A_large, dev_opt_B vs dev_opt_B_large, dev_chisq_of vs dev_chisq_of_large, assemble_f4_gather_kernel vs assemble_f4_gather_models_kernel, f4_loo_total_kernel vs _models_kernel, f4_xtau_kernel vs _models_kernel, the constrained weight solve (dev_als_weights / weights_chisq_kernel / weights_chisq_large_kernel / loo_large_batched_kernel), the ALS loop. Use git log --follow, git log -L:func:file, and git log -S "<snippet>" pickaxe to pin the INTRODUCING commit of each. Report a table: region -> first-copy commit (hash+date+msg) -> second-copy commit (hash+date+msg) -> which entered first. Note the file may have been created by M(fit-4) 3e78c79; the large kernels by 01a94d5; the _models kernels by 7852404; loo_large_batched by 8f485b7 — VERIFY each against git, do not assume.' },
  { key: 'blocksink-git', focus: 'GIT archaeology on src/device/cuda/block_sink.cu — find when the file was created and when HostRamSink vs DiskSink were each added (the duplicated acquire_slot / writer_loop / spill_block / stop-join / event-teardown). Use git log --follow + git log -S pickaxe on a unique snippet from each sink. Did both sinks land in the SAME commit (created together as copy-paste) or did one come first and the second was copy-pasted later? Report: file-creation commit (hash+date+msg = which M5 streaming workflow), and per-sink introduction. Also note the HIGH fail-fast fix 9dbc610 (the recent edit) and any later edits (group-15/16 MED commits 8b7ef06/6945f74).' },
  { key: 'commit-to-workflow', focus: 'Map the introducing commits (from the other two lenses, plus the known set: 3e78c79 M(fit-4), 01a94d5 gpu-large-models, 7852404 M(fit-6) S8, 8f485b7 parallelize-large-loo-se, the M5 streaming commits) to the WORKFLOW/agent that produced each + the WHY (design intent). Read ' + R + '/agentscripts/*.js (m-fit-gpu.js, gpu-large-models.js, m-fit-6-rotation.js, parallelize-large-loo-se.js, the m5 streaming scripts) — each workflow\'s meta.description + the fixer/design prompts state WHY the code was added that way (e.g. WHY the large kernels are a separate copy: gpu-large-models added VRAM-scratch kernels because the small fixed-local-array kernels could not hold nl>5/nr>10; WHY the _models kernels: S8 batched across models). For the WHY, prefer the commit message + the workflow meta; only dig the transcripts under ' + TX + '/subagents/workflows/ if the why is unclear. Report: commit -> workflow file -> the WHY (the design reason the duplicate was created rather than sharing code), with a quote/cite.' },
]
const dug = await parallel(LENSES.map(L => () => agent([
  'You are a git+workflow archaeologist for steppe, READ-ONLY: ' + L.focus, '', STD,
  '', 'Run the git commands (git -C ' + R + ' ...) and read the cited files/transcripts. Report your findings with REAL commit hashes + dates + the why, as a clear table/list. No fabrication, no box, no edits.',
].join('\n'), { label: 'dig:' + L.key, phase: 'Dig' })))

phase('Synthesize')
const report = await agent([
  'You are the lead synthesizer of the group-7 duplication PROVENANCE report for steppe. Combine the 3 archaeology lenses into one clear answer to "which copy entered at what point and why".', '', STD,
  '', 'THE LENS FINDINGS:\n' + dug.filter(Boolean).map((d,i)=>'### '+LENSES[i].key+'\n'+d).join('\n\n'),
  '', 'WRITE ' + OUT + ' with: (1) a HEADLINE timeline — for each duplicated pair, "the <X> copy entered at <commit> (<date>, <workflow/milestone>) because <why>; the <Y> twin entered later at <commit> (<date>, <workflow>) because <why>". (2) A table: duplicated region | original copy (commit/date/why) | duplicate copy (commit/date/why) | CANONICAL (which should be the kept body in the dedup) + one-line rationale. (3) THE WHY narrative — the design tension that produced the twins (small fixed-local-array vs large VRAM-scratch; single-model vs model-batched; HostRam vs Disk) and whether the duplication was a deliberate copy-then-specialize (and why sharing was deferred). (4) A short note for the in-progress fix-group7-device dedup: which copy to treat as canonical and any provenance gotcha (e.g. the loo_large_batched weight-solve being the newest copy). Use the Write tool. Return the headline timeline so the user sees it at a glance. Cite real commits.',
].join('\n'), { label: 'synthesize:provenance', phase: 'Synthesize' })

log('group-7 provenance traced: ' + String(report).slice(0, 200))
return { dug, report }
