export const meta = {
  name: 'audit-fit-engine-finish',
  description: 'GAP AUDIT for "finish the fit-engine backend" (step 1 of the backend-first build sequence). READ-ONLY: audit the BUILT qpAdm/qpWave fit engine (src/core/qpadm/*, src/device/cuda/cuda_backend.cu + qpadm_fit_kernels.cu, include/steppe/qpadm.hpp, the tests + goldens) against the DESIGN intent (docs/design/fit-engine.md design body + OQ-1..OQ-13, architecture.md §5 S3-S8 + §12, and ADMIXTOOLS 2 qpAdm/qpWave feature surface) and produce a precise PUNCH-LIST of what is still INCOMPLETE in the backend BEFORE it gets exposed via CLI/bindings. 3 parallel lenses: (A) design-vs-built coverage per S3-S8 stage + per M(fit-0..6) milestone (BUILT/PARTIAL/MISSING); (B) the AT2 feature-surface gaps (allsnps=TRUE per-quartet SNP sets, boot=N bootstrap vs delete-1 jackknife, maxmiss, standalone qpWave exposure, the full popdrop/rankdrop semantics, f2 NA/missing-block handling, pseudo-haploid/ploidy) + the open OQ-* questions; (C) domain/correctness completeness (M(fit-5) status taxonomy + its test, error paths, edge cases, determinism). Synthesis -> a ranked punch-list: each item = what it is, FINISH-NOW vs DEFER vs NEW-FEATURE(step 3), effort, parity risk, design ref. NO box, NO source edits — the deliverable is the punch-list for the user to scope before we fix.',
  phases: [
    { title: 'Audit', detail: '3 parallel read-only lenses: stage/milestone coverage / AT2-feature + OQ gaps / domain-correctness completeness' },
    { title: 'Synthesize', detail: 'ranked fit-engine-finish punch-list (finish-now/defer/new-feature, effort, parity risk)' },
  ],
}

const R = '/home/suzunik/steppe'
const OUT = R + '/docs/design/fit-engine-finish-punchlist.md'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell reimplementation of ADMIXTOOLS 2 f-statistics + qpAdm. main @ 0db07be. The Phase-2 qpAdm fit engine (S3-S8) is BUILT + golden-gated on the GPU (M(fit-0..6)); the big refactor is complete. The USER strategy (memory build-sequence-backend-first): (1) FINISH the fit-engine BACKEND first, (2) then CLI + Python bindings for what exists, (3) then new standalone statistics (f4/D/f3/qpDstat...) each with its own CLI/bindings. THIS audit is step (1)\'s scoping: find what is still INCOMPLETE in the BACKEND.',
  'BUILT (do not re-flag as missing): S3 assemble_f4, S4 jackknife_cov (weighted Q via Vpair), S5 rank test/qpWave rank sweep (rankdrop + popdrop, nr<=32 small + nr=39 NRBIG cuSOLVER-gesvd large path), S6 GLS weights (opt_A/opt_B ALS + constrained solve), S7 block-jackknife SE + the opt-in JackknifePolicy{None,FeasibleOnly,All}, S8 model-space rotation (run_qpadm_search batched + multi-GPU shard, the latter DEFERRED to single-GPU). Domain status values RankDeficient/NonSpdCovariance exist. Real-AADR AT2 goldens golden_fit0/fit1_NRBIG/rot.',
  'THE QUESTION: what is STILL INCOMPLETE in the fit-engine BACKEND (S3-S8 + the qpAdm/qpWave domain) that should be FINISHED before exposing it via CLI/bindings — vs what is genuinely a NEW feature (step 3) or a defensible DEFERRAL. Be precise + honest: distinguish a real backend gap (e.g. an AT2 qpAdm behavior we cannot reproduce) from a nice-to-have.',
  'CANDIDATES TO ADJUDICATE (confirm each against the code + the design, do NOT assume): M(fit-5) domain-outcome taxonomy + its explicit test (does a collinear/under-determined model return a status value not a crash? is the STEPPE_ERR_* / Status taxonomy complete?); standalone qpWave (run_qpwave exists internally — is it a first-class, tested entry?); allsnps=TRUE (per-quartet maximal SNP sets — currently maxmiss=0 GLOBAL-intersection only; the more-power aDNA mode; needs deterministic-reduction care); boot=N bootstrap (vs the delete-1 jackknife we have); the f2 NA / missing-block handling (the design noted "assume no missing blocks for M(fit-1); add NA handling later"); pseudo-haploid/ploidy honoring in the fit path; the OQ-1..OQ-13 open questions in fit-engine.md (which are resolved vs open); any S3-S8 piece marked partial/seam-only; the precision-promotion seam (cuSOLVER emulated when a toolkit exposes it). For EACH: BUILT / PARTIAL / MISSING + finish-now vs defer vs new-feature.',
  'READ-ONLY: git + file reading only, NO box, NO source edits, NO commits, NO builds. The deliverable is the punch-list doc. NO fabrication — every claim cites a real file:line / design ref / AT2 behavior.',
].join('\n')

phase('Audit')
const LENSES = [
  { key: 'stage-coverage', focus: 'Per-stage + per-milestone coverage: for EACH of S3 (assemble_f4), S4 (jackknife_cov), S5 (rank_test/rank_sweep), S6 (gls_weights), S7 (SE), S8 (model_search/fit_models_batched), and each M(fit-0..6), read the actual code (src/core/qpadm/{f4_matrix,gls_solve,jackknife,ranktest,nested_models,model_search,qpadm_fit}.*, src/device/cuda/cuda_backend.cu + qpadm_fit_kernels.cu, include/steppe/qpadm.hpp) AND the fit-engine.md milestone table, and classify each sub-component BUILT / PARTIAL / MISSING with file:line evidence. Specifically check: is run_qpwave a complete first-class entry (not just an internal helper)? is M(fit-5) domain-outcomes fully done (status taxonomy + a domain-outcome TEST exists)? are there any "seam-only"/"stub"/"deferred"/"PENDING"/"TODO" markers in the fit code? Report a stage/milestone table with the gaps.' },
  { key: 'at2-feature-gaps', focus: 'AT2 qpAdm/qpWave FEATURE-SURFACE gaps — what does ADMIXTOOLS 2 qpadm()/qpwave() do that steppe does NOT yet? Read the design (fit-engine.md OQ-1..OQ-13 + the §-refs) and reason about AT2 (web-search the admixtools qpadm/qpwave API if helpful: ToolSearch select:WebSearch,WebFetch). Adjudicate each: (1) allsnps=TRUE (per-quartet maximal SNP intersection vs our maxmiss=0 global-intersection — the more-power aDNA path; check if f2_blocks/the fit can even express it + the deterministic-reduction concern); (2) boot=N bootstrap resampling vs our delete-1 block jackknife; (3) maxmiss / missing-data policy in the fit; (4) f2 NA / missing-block handling (design said "assume no missing blocks for M(fit-1); add later" — is it added?); (5) pseudo-haploid/ploidy correctness through the fit; (6) standalone qpWave/qpAdm options we lack (constraints, fixed weights, the dofdiff/rank options). For each: is it a BACKEND-FINISH item, a NEW-FEATURE (step 3), or a defensible DEFERRAL — with the AT2 reference + the steppe gap.' },
  { key: 'domain-correctness', focus: 'Domain/correctness COMPLETENESS of the backend: read the Status/error taxonomy (include/steppe/error.hpp, the QpAdmResult.status usage), the domain-outcome handling in cuda_backend.cu/cpu_backend.cpp (RankDeficient/NonSpdCovariance/chisq-undefined), and the tests (tests/reference/test_qpadm_*.cu + any unit tests). Check: is there an explicit M(fit-5) domain-outcome TEST (a deliberately collinear/under-determined/non-SPD model returns a status VALUE, not a crash/NaN)? Are all the failure modes covered (singular Q, rank-deficient X, nl>nr under-determined, empty/degenerate model, p undefined)? Is the determinism contract (G1==G2, single-stream, the emulated-FP64 workspace) complete + tested for the fit? Any edge case the design requires that has no test? Report the correctness gaps + missing tests with file:line.' },
]
const audited = await parallel(LENSES.map(L => () => agent([
  'You are auditing the steppe fit-engine backend for completeness, READ-ONLY: ' + L.focus, '', STD,
  '', 'Use git + read the actual code/design/tests (no box, no edits). For every item give file:line evidence + the BUILT/PARTIAL/MISSING verdict + whether it is finish-now / defer / new-feature. Honest + precise; cite real code + AT2 behavior. No fabrication.',
].join('\n'), { label: 'audit:' + L.key, phase: 'Audit' })))

phase('Synthesize')
const punch = await agent([
  'You are the lead synthesizer of the steppe FIT-ENGINE-FINISH punch-list (step 1 of backend-first). Combine the 3 audit lenses into ONE ranked, actionable punch-list.', '', STD,
  '', 'THE AUDIT LENSES:\n' + audited.filter(Boolean).map((a,i)=>'### '+LENSES[i].key+'\n'+a).join('\n\n'),
  '', 'WRITE ' + OUT + ' with: (1) HEADLINE — is the backend essentially complete, or are there real finish items? how many FINISH-NOW. (2) THE PUNCH-LIST table: item | what it is + file/design ref | classification [FINISH-NOW backend gap / DEFER / NEW-FEATURE(step 3)] | effort (S/M/L) | parity risk (low/med/high) | one-line approach. Ranked: FINISH-NOW first (the things that make the backend genuinely complete before CLI), then DEFER, then NEW-FEATURE (which belong to step 3 with their own CLI). (3) A short RECOMMENDED FINISH SET — the minimal set of FINISH-NOW items to call the backend "done" + ready for the CLI step, with rationale for what to defer. (4) Explicit notes on the big adjudications: allsnps=TRUE (finish vs new-feature?), bootstrap (finish vs defer?), qpWave standalone, M(fit-5) taxonomy+test, NA/missing-block handling. Use the Write tool. Return the headline + the FINISH-NOW list so the user can scope at a glance. Cite real refs; no fabrication; honest about what is genuinely done.',
].join('\n'), { label: 'synthesize:punchlist', phase: 'Synthesize' })

log('fit-engine-finish punch-list: ' + String(punch).slice(0, 200))
return { audited, punch }
