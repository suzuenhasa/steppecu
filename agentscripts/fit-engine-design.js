export const meta = {
  name: 'fit-engine-design',
  description: 'PHASE 2 KICKOFF (design/contracts, read-only): scope the qpAdm FIT ENGINE (S3-S8) before any code. It consumes the now-built device-resident f2_blocks (DeviceF2Blocks; streamed tiles at large P) and produces qpAdm est/se/z/p. Four design lenses — (A) the qpAdm/f3/f4 algebra + data-flow from f2_blocks (what each model reads, the jackknife S3/S4); (B) the numerics (S5 rank test/SVD, S6 GLS Cholesky+solve, S7 block-jackknife SE) + the precision policy (emulated-FP64 matmul / native-FP64 cancellation / Tf32-screen-then-recompute) + the batched-SVD limit + cuSOLVER/cuBLAS primitives; (C) the seam/layering (core/qpadm/, ComputeBackend dispatch), how it reads f2_blocks, the public API + result type, and the S8 model ROTATION (the embarrassingly-parallel, multi-GPU-friendly model-space search); (D) the AT2-goldens validation plan (install R+admixtools, pin extract_f2/qpadm goldens per §12, the parity gate) — then a synthesis: the fit-engine architecture + the milestone build-order + the FIRST-milestone frozen contract. Writes docs/design/fit-engine.md. EDIT-ONLY the design doc; no code, no git.',
  phases: [
    { title: 'Design', detail: 'four read-only lenses: qpAdm algebra/data-flow, numerics+precision, seam/rotation, AT2-goldens validation' },
    { title: 'Plan', detail: 'synthesize: fit-engine architecture + milestone build-order + first-milestone contract -> doc' },
  ],
}

const R = '/home/suzunik/steppe'

const STD = [
  'PROJECT: steppe = GPU/CUDA-13/Blackwell reimplementation of ADMIXTOOLS 2 f-statistics, precompute-once/fit-many. THE PRECOMPUTE IS DONE through M5 (on main @ ~1d57992): it produces f2_blocks[P x P x n_block] FP64 (per-block leave-one-out jackknife tensor) DEVICE-RESIDENT (steppe::device::DeviceF2Blocks, opt-in .to_host() to a host F2BlockTensor; src/device/device_f2_blocks.hpp), adaptively tiered (resident/host/disk) + streamed at large P, bit-identical to a native-FP64 oracle (Ozaki emulated-FP64). NOW: design PHASE 2 — the qpAdm FIT ENGINE (S3-S8) — which consumes f2_blocks and produces qpAdm results. It DOES NOT EXIST YET (no src/core/qpadm/, no src/app/).',
  'THE GOAL of this workflow: a DESIGN/CONTRACTS deliverable, NOT code. Scope the fit engine so the milestone-build (Contracts -> per-file Implement -> Build -> Verify) can start with a frozen first-milestone contract. Be concrete + grounded: cite the spec sections + the qpAdm/AT2 algebra (with sources) + the existing precompute seams. Do NOT write production code; the only artifact is docs/design/fit-engine.md.',
  'THE SPEC (read it — docs/architecture.md): §5 S3-S8 (the fit-engine stages: S3 f3/f4 from f2, S4 jackknife setup, S5 rank test, S6 GLS, S7 SE, S8 model-space search), §5:231 (the S5 batched-SVD limit: gesvdjBatched m,n<=32, the per-model gesvd fallback), §5:243 (S6 GLS = ONE Cholesky potrf + solve, NOT an iterative sweep), §9 (Precision/DeviceConfig: search_streams=4 for S8, the three precision modes), §11.4 (S8 model-space search shards across GPUs with ZERO inter-GPU traffic — multi-GPU s proper home), §12 (PARITY LAW: emulated-FP64 matmul vs native-FP64 cancellation; Tf32 = screening only, recomputed in EmulatedFp64/Fp64; the AT2-goldens acceptance gate). Also fstats.hpp (F2BlockTensor / the planned f2/f3/f4 entry points) + the ComputeBackend seam (backend.hpp) + DeviceF2Blocks (device_f2_blocks.hpp).',
  'qpAdm DOMAIN (ground it from sources — use web search/fetch via ToolSearch select:WebSearch,WebFetch for the AT2/Patterson/Harney formulation; cite): qpAdm/qpWave models a target as a mixture of source (left) pops relative to reference (right/outgroup) pops. The core object is the f4 matrix X = f4(Left_i, Left_0; Right_j, Right_0) (or the target-vs-left form), built from f2 (f4 is a linear combination of f2). qpWave = a RANK TEST on X (SVD); qpAdm = a GLS fit for the admixture weights w given X and its jackknife covariance, yielding weights + std errors + a tail/p-value + the rank-test stats. ALL of f3/f4/qpAdm reduce to linear combinations of f2 (the precompute-once premise). The block-jackknife (leave-one-block-out over n_block) gives the covariance + the SEs. Get the exact matrix definitions + the GLS/rank-test formulas right (cite AT2 qpadm() / qpwave() docs + the Patterson 2012 / Haak 2015 / Harney 2021 formulation).',
].join('\n')

phase('Design')
const LENSES = [
  { key: 'algebra-dataflow', focus: 'The qpAdm/f3/f4 ALGEBRA + the DATA-FLOW from f2_blocks. Establish (with sources): exactly how f3 and f4 are linear combinations of f2 (the formulas); the qpAdm f4 matrix X definition (indices over Left/Right pops); what a SINGLE model fit READS from f2_blocks (only the pop subset of its Left+Right+target — low-tens of pops, across all n_block blocks) and how that maps onto the device-resident DeviceF2Blocks (a small pair-subset gather per model, per block). The S3 (f3/f4 from f2) + S4 (jackknife: per-block X plus the leave-one-block-out replicates) data structures + shapes. Confirm the precompute-once/fit-many access pattern (each model reads a tiny f2 subset; the rotation reads many overlapping subsets). Return the precise algebra + the per-model data-flow + shapes, sourced.' },
  { key: 'numerics-precision', focus: 'The fit NUMERICS + the precision policy. S5 RANK TEST: SVD of the f4 matrix X ((n_L-1)x(n_R-1)); the rank/df, the test statistic; the gesvdjBatched m,n<=32 limit (§5:231) and the per-model gesvd fallback for n_R-1>32 (common). S6 GLS: a single Cholesky (potrf) of the covariance/weight matrix Q + solve the weighted normal equations for the admixture weights w and the residual chi^2 (NOT iterative, §5:243); the matrices Q/X are low-double-digit. S7 SE: block-jackknife over n_block (leave-one-out replicates -> weights -> jackknife variance). The PRECISION MAP (§12): the f4/covariance matmuls = EmulatedFp64; the cancellation-sensitive f-stat differences = native FP64; Tf32 = screening-rank only, recomputed in EmulatedFp64/Fp64 before any reported number. The cuSOLVER/cuBLAS primitives (potrf, gesvd/gesvdjBatched, gemm/syrk) + determinism (single statistic stream, the cublasSetWorkspace for emulated-FP64). Return the per-stage numerics + primitives + the precision assignment, cited to §12.' },
  { key: 'seam-rotation', focus: 'The SEAM/LAYERING + the S8 model ROTATION. Where the fit engine lives (core/qpadm/ orchestration + the ComputeBackend dispatch; core never issues a GEMM/SVD itself — §4); how it CONSUMES f2_blocks (the device-resident DeviceF2Blocks handle for in-VRAM; streamed tiles for large P; a per-model pair-subset gather). The PUBLIC API: the qpAdm entry point (inputs: target, left, right, the f2_blocks handle, config) + the result type (est weights / se / z / p / rank-test stats), CUDA-free at the seam. The S8 MODEL-SPACE SEARCH (the rotation): many INDEPENDENT models partitioned across devices via a dynamic work-queue (zero inter-GPU traffic, §11.4); per-model fit captured into a CUDA graph (§5/§11), search_streams lanes (§9); deterministic re-sort by model index. This is multi-GPU s PROPER home (embarrassingly parallel). Return the layering + API + result type + the rotation/scheduling design, cited to the spec.' },
  { key: 'at2-goldens-validation', focus: 'The AT2-GOLDENS VALIDATION plan (the Phase-2 acceptance gate, §12). What goldens to pin (extract_f2 -> f2_blocks parity, already covered; AND qpadm()/qpwave() est/se/z/p for a set of reference models); the EXACT reproducibility metadata to record per §12 (R version, RNGkind, ADMIXTOOLS-2 version, blgsize, bootstrap/jackknife setting, seed, the dataset + the L/R/target sets); how to GENERATE them (install R + admixtools on a box — which box: box5090 has R? or install; rtxbox; the commands); the PARITY gate (qpAdm est/se/z/p vs AT2 — memcmp is too strict for the fit since SVD/Cholesky differ in the last bits; define a TOLERANCE per §12 "match the oracle to all reported digits" — propose the tolerance + which quantities are bit-vs-tolerance). Use web search for the AT2 qpadm()/qpwave() API + golden-generation recipe. Return the goldens spec + generation recipe + the tolerance-based parity gate, sourced.' },
]
const lenses = await parallel(LENSES.map((l) => () => agent([
  'You are a senior numerical/GPU architect + pop-gen-aware engineer doing READ-ONLY design for ONE lens of the steppe qpAdm fit engine (Phase 2). Read the spec + the existing precompute seams; use web search for the qpAdm/AT2 algebra where needed (ToolSearch select:WebSearch,WebFetch) and CITE it. Do NOT write code. Be concrete + grounded; distinguish spec-mandated from your-proposal.', STD, '',
  'YOUR LENS: ' + l.key + '\n' + l.focus, '',
  'Return: (1) the grounded findings for your lens (formulas/shapes/primitives/API as applicable), cited to the spec section or a source URL; (2) the concrete design proposal for your lens; (3) the OPEN QUESTIONS / risks the synthesis must resolve; (4) what the FIRST minimal milestone needs from your lens (the smallest end-to-end slice).',
].join('\n'), { label: 'design:' + l.key, phase: 'Design' }))).then(a => a.filter(Boolean))

phase('Plan')
const plan = await agent([
  'You are the lead architect. Synthesize the four lenses into the qpAdm FIT-ENGINE design + the milestone build-order + the FIRST-milestone frozen contract, and WRITE it to ' + R + '/docs/design/fit-engine.md (Write tool; create docs/design/ if needed). READ-ONLY on code; this is a DESIGN doc, not implementation.', STD, '',
  'THE FOUR LENSES:\n<<<\n' + lenses.map((a,i)=>'### '+LENSES[i].key+'\n'+a).join('\n\n') + '\n>>>', '',
  'The doc must contain: (1) THE FIT-ENGINE ARCHITECTURE — the S3-S8 data flow from the device-resident f2_blocks to qpAdm est/se/z/p, the layering (core/qpadm/ + ComputeBackend), the public API + result type, the precision map (§12), the S8 rotation/multi-GPU model; (2) the MILESTONE BUILD-ORDER — break S3-S8 into buildable milestones with the dependency order, each with its acceptance gate (the smallest-first: e.g. M(fit-1) = f4-from-f2 + a single GLS fit for ONE model validated against an AT2 golden; then rank test; then jackknife SE; then the rotation/multi-GPU); (3) THE FIRST-MILESTONE FROZEN CONTRACT — concretely buildable: the files/types/signatures, what it reads from DeviceF2Blocks, the numerics + precision, and the AT2-golden it is validated against, with the parity tolerance; (4) the AT2-GOLDENS plan (what to pin, the generation recipe, which box, the §12 metadata, the tolerance gate); (5) OPEN QUESTIONS + risks (the batched-SVD fallback, the f4-matrix form, the device-resident-vs-streamed fit at large P, where multi-GPU sharding plugs in). Ground every claim in the spec or a cited source; be explicit spec-mandated-vs-proposed. Do NOT mark anything built.',
  'Return a tight 8-12 line executive summary: the fit-engine architecture in brief, the milestone build-order, the first-milestone contract (what to build first + its AT2-golden gate), and the top open questions.',
].join('\n'), { label: 'plan:fit-engine', phase: 'Plan' })

return { lenses, plan }
