# steppe — cold-start handoff @ `m4.5-multigpu` (current HEAD `867a4bf`)

Everything a fresh session needs to resume. **START AT `docs/RESUME.md` (the single entry point — read it first).** Canonical spec: `docs/architecture.md`; build order + status: `docs/ROADMAP.md`; living task list: `docs/TODO.md`; the per-file audit + master cleanup backlog: `docs/cleanup/00-overview.md` + `docs/cleanup/m4.5/00-overview.md`. **→ THE WORKFLOW MAP (where we are + every workflow's state/retrigger + the workflows still needed) is `agentscripts/README.md` — read it first. To stand up / refresh a remote box: `docs/BOX-RUNBOOK.md` (+ `scripts/box_bringup.sh`, `scripts/p2p_probe.cu`).** **Read these before writing code.**

---

## 0. TL;DR
`steppe` = GPU/CUDA-13/Blackwell (sm_120) reimplementation of ADMIXTOOLS 2 f-statistics (precompute-once / fit-many), senior-engineer quality bar. **Status: Phase 1 precompute M0–M4 + before-M4.5 cleanup (B1–B27) merged to `main` @ `ac0ca6f`; M4.5 single-node multi-GPU + its cleanup + perf fix-pass DONE on branch `m4.5-multigpu` @ `867a4bf` (current HEAD; 36/36 ctest green, bit-identity-proven on real AADR, NOW FASTER than single-GPU, BEING MERGED to `main` now — M4.5 milestone complete: correct AND faster).** The M4.5 work landed T1 (parity VRAM-gate), B1 (fan-out), B3–B9, and the perf fix-pass (`867a4bf` device-resident combine).
- **⚠ THE KEY FINDING — RESOLVED (bench `tests/reference/bench_f2_multigpu.cu`, ITERS=10, median + p10/p90, out-of-band timers; measured on rtxbox 2× RTX PRO 6000 Blackwell sm_120, Release, `EmulatedFp64{40}`): multi-GPU is now FASTER than single-GPU — P=768 G1=2342ms G2=2125ms = 1.10×; P=400 = 1.22×.** This SUPERSEDES the old PRE-FIX finding ("multi-GPU is SLOWER, 0.70× / 0.75–0.97×") which is now FALSE. ROOT CAUSE of the old slowdown (diagnosed by nsys, `docs/cleanup/m4.5/why-multigpu-slow.md` @ `165f655`): the **DATA-BOUNCE wart** — `compute_f2_blocks` D2H-copied each partial to host and freed its device buffers, forcing the P2P combine to re-upload (H2D) + place-add + a 2nd D2H = a redundant SECOND full 7.14 GB Device→Host copy. **FIXED @ `867a4bf` by the device-resident combine:** per-device compute leaves its partial RESIDENT (returns an opaque move-only `DevicePartial`), the combine allocates ONE root result, D2D-copies the root partial + `cudaMemcpyPeer`s each peer partial straight into its DISJOINT block slice, then ONE final D2H. Deleted the re-upload H2D, the accumulator memset, the place-add kernel, the staging buffers, and the per-peer `cudaDeviceSynchronize`. Single-GPU OOMs at P≥700 on a 32 GB 5090 (so there multi-GPU is *also enabling*, on top of being faster on the 96 GB PROs).
- **REFUTED — do NOT carry as open problems:** (a) "per-call `cudaMalloc`/`cudaFree` driver-lock serializes the device-threads / only ~18% overlap" — the nsys trace measured **74% overlap**; the **L4** pool-allocator is at most a few % and is NOT the speedup lever (optional cleanup, not a blocker). (b) "the ~1.4s combine host-zeroing (P1 default-init allocator) is the dominant cost" — the accumulator memset is 5ms; P1 was REVERTED and is not the cure (`perf-discovery.md`'s P1 premise was WRONG, superseded by `why-multigpu-slow.md`).
- **NEXT:** (a) **merge `m4.5-multigpu` → main now** (M4.5 complete — correct AND faster); then the real next work is the **qpAdm FIT ENGINE** (Phase 2, does not exist yet), then **M5** out-of-core streaming, **AT2 goldens** (M7 gate), **M6** merge. (b) **Optional levers, NOT blockers:** pin the final pageable result D2H (next available speedup, could push past 1.10×); the bench byte-traffic columns are observability-only (print 0.00). **B2** (`agentscripts/m4.5-b2-p2p-fix-pass.js`) — its original motivation (the slow/bouncing combine) is now largely subsumed by the resident combine; REASSESS / likely-subsumed, not a pending speedup. **The perf rabbit-hole is CLOSED.** See `agentscripts/README.md` for the full forward map.

## 1. Boxes
- **CAPABLE box = rtxbox (Verda):** (was `ssh rtxbox` → `root@31.22.104.224`; ephemeral — the alias IP may be STALE, update `~/.ssh/config` to the new instance before use). 2× **RTX PRO 6000 Blackwell** (sm_120, 96 GB ea), CUDA 13.0.88, driver 580, $3.78/h. **GOTCHA: `nvcc` not on PATH → `export PATH=/usr/local/cuda/bin:$PATH`.** **Real stock-driver P2P confirmed: `cudaMemcpyPeer` byte-exact 55.6 GB/s, `can_access_peer==true`.** This is where the M4.5 speedup was MEASURED (P=768 → 1.10×, P=400 → 1.22×, Release, `EmulatedFp64{40}`, median of 10) and where the nsys root-cause trace was run. Still needed for: AT2 goldens, GDS. See [[rtxbox]].
- **BUDGET box = box5090 (vast 2× RTX 5090, UP):** `ssh box5090` = `-i ~/.ssh/id_vastai -p 43215 root@78.92.24.57`. 32 GB ea, CONSUMER tier (`can_access_peer=false` → host-staged combine; the graceful-degrade target). **Flaky network — long ssh drops; run long build/ctest/bench DETACHED on the box + poll a `/tmp/*.log` (see `agentscripts/README.md` retrigger mechanics).** See [[steppebox5090]].
- **DATA strategy:** vast→rtx path is SLOW (~1 MB/s). `raw` came from LOCAL (~10 MB/s); `derived_*` REGENERATED on the box from raw via `python3 /workspace/data/aadr/build_tgeno_matrix.py --geno raw/...geno --ind raw/...ind --out <dir> <flags>` (needs `apt install python3-numpy`). `derived_acc` = `--auto-top 50 --snp-cap 100000`; `derived_full` = `--auto-top 768`.
- **DISK/cores:** run tests with `ulimit -c 0` (asserts abort → core dumps).

## 2. Working rules (hard)
NOTHING builds/runs locally — author locally → rsync to `/workspace/steppe` → build/test on the box. NO synthetic data for any precision/accuracy claim (real AADR only). git identity `suzuenhasa <suzu@enhasa.co>`. Commit only on green, per-milestone, ROADMAP §6 message + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Don't reuse the old MVP at `/home/suzunik/vahaduo/qpadm/steppe`.

## 3. Build & run (on rtxbox)
```
ssh rtxbox
cd /workspace/steppe
export PATH=/usr/local/cuda/bin:$PATH && export LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH && ulimit -c 0
cmake -S . -B build -GNinja && cmake --build build
ctest --test-dir build --output-on-failure
```
Dev loop: edit locally → `rsync -az --delete-after --exclude .git --exclude build --exclude aadr -e ssh /home/suzunik/steppe/ rtxbox:/workspace/steppe/` → build/test on box → commit locally on green. **30/30 ctest green at `18b7801`** (~140 s; `filter_oracle` ~45 s = the CPU long-double parity oracle).

## 4. The workflow PATTERN (this is how milestones get built — the user insists on it)
- **Implementation** (milestone/new code) = the `steppe-m0-scaffold-f2` shape: **Contracts/Design** (1 agent fixes interfaces) → **Implement** (1 devoted agent PER FILE/unit) → **Build agent** (rsync+build+ctest fix-loop) → **independent Verify agent** (adversarial diff + commit-green/revert). STRICTLY SEQUENTIAL (shared files + one box build dir), halt-on-fail, retry-once-on-transient-null. The two M4.5 workflows (`agentscripts/m4.5-scaffold.js`, `m4.5-multigpu.js`) are the current reference.
- **Fix/cleanup** = `agentscripts/fix-pass-phase2.js` (per-item fixer + verdict).
- **Research/review** = parallel read-only fan-out.
- All builds/tests on the box; commit-green/revert; never `git add .` (leave `aadr/`, `build_run.sh`, `f2_emu_spike.cu`, `handoff-*.md` untracked).

## 5. State — M4.5 (branch `m4.5-multigpu` @ `867a4bf`; the algorithm/scaffold commits below are the original landings)
- **Scaffold** (`d81d2a1`→`182d7cb`): `STEPPE_CUDA_WARN` (non-throwing tagged degrade, `device/cuda/check.cuh`) · CUDA-free `BackendCapabilities` POD + `ComputeBackend::capabilities()` (`device/backend.hpp`) · `DeviceConfig::prefer_p2p_combine` + override-knob banner (`config.hpp`) · `CublasHandle` device-ordinal + `MathModeScope` (`handles.hpp`) · `CudaBackend(int device_id)` + `cudaSetDevice` + the real capability probe (`cuda_backend.cu`).
- **Algorithm** (`dd24941`→`867a4bf`): `Resources`/`PerGpuResources` + `build_resources` (`include/steppe/resources.hpp` + `resources.cpp`, CUDA-free; one backend+probe per device, fixed `g=0..G-1` order) · **block-aligned shard** + per-device partials (`core::compute_f2_blocks_multigpu`; G==1 = exact single-GPU path; reuses `compute_f2_blocks` per device) · two fixed-order combine paths, both proven bit-identical to single-GPU: **host-staged** (the consumer/`can_access_peer==false` fallback) and the **device-resident `cudaMemcpyPeer` P2P combine** (`device/cuda/p2p_combine.cu`, gated on `can_access_peer && prefer_p2p_combine`, tagged degrade, `CombinePath` recorded out-of-band on `Resources`). **The perf fix @ `867a4bf` made per-device compute leave its partial RESIDENT (returns a move-only `DevicePartial`); the combine D2D-copies the root + `cudaMemcpyPeer`s each peer straight into its DISJOINT slice + ONE final D2H — deleting the old data-bounce (re-upload H2D / place-add kernel / accumulator memset / staging buffers / per-peer sync).**
- **Parity (`test_f2_multigpu_parity`, THE gate):** production `EmulatedFp64{40}` → G==2 host-staged AND P2P **bit-identical** to single-GPU on `derived_acc` + `derived_full`; P2P==host-staged bit-identical. Native `Fp64` at G≥2 → oracle tol (rel ≤2.87e-15) = §12 `[UNCERTAIN]` `cublasGemmStridedBatchedEx` batchCount-sensitivity, correctly scoped (EmulatedFp64 is the production f2 default; native Fp64 is only the oracle). **NEVER NCCL AllReduce on a parity path.**
- **The science:** f2 = 3-GEMM; **fixed-slice Ozaki** emulated-FP64 (40-bit ≈ native at 7–17×); dynamic mantissa = the rejected trap; native FP64 = oracle + numerator/divide.

## 6. NEXT
1. **Merge `m4.5-multigpu` → `main`** (IN PROGRESS NOW — M4.5 is green, proven bit-identical, AND faster @ `867a4bf`; 46 commits ahead of main). The perf rabbit-hole is CLOSED.
2. **qpAdm FIT ENGINE (Phase 2)** — the real next work; does not exist yet. Precompute (Phase 1, f2) is done; the fit-many half is unwritten.
3. **M5** (out-of-core SNP-tile streaming — pinned double-buffer ingest, the big CPU-offload win; never deferred, genotype input ≫ VRAM) · **AT2 goldens** (**install R+admixtools on a box**, the real parity gate, M7) · **M6** (multi-dataset merge).
4. **"5090 after" — consumer graceful-degrade validation** on the budget vast 5090 (if still up): the parity test's tier-assertions are currently **PRO-specific** (assert `can_access_peer==true` + P2P-ran) — add a **tier branch** so on a consumer box it asserts `can_access_peer==false` → host-staged path + the "P2P unavailable" degrade tag. Validates the fallback the capability-tier design promises.
5. **Optional perf levers (NOT blockers):** pin the final pageable result D2H (the next available speedup, could push past 1.10×); demote **L4** pool-allocator to optional cleanup (nsys showed 74% overlap — it is NOT the speedup lever); **B2** P2P transport rework — REASSESS / likely-subsumed by the resident combine (`867a4bf`), not a pending speedup.
6. **Deferred:** L1–L19 (`docs/cleanup/00-overview.md`, L4 demoted to optional); B5/B6 at-scale; GDS for M5/M7 (real on PRO 6000, dead on consumer).

## 7. Resume / git
- `main` @ `ac0ca6f` (M0–M4 + cleanup + docs/handoff/agentscripts). `m4.5-multigpu` @ `867a4bf` = M4.5 (above), branched off main, 46 commits ahead, **BEING MERGED now** (was unmerged).
- Merged feature branches `m0-f2-scaffold`/`m1-decode-af`/`m2-filters`/`m3-block-partition`/`m4-perblock-f2` = prunable.
- Untracked strays (leave): `aadr/`, `build_run.sh`, `f2_emu_spike.cu`. This handoff is committed for durability.
- Memory: `~/.claude/projects/-home-suzunik-steppe/memory/` ([[steppe-project]], [[rtxbox]], [[steppebox5090]], [[steppe-dev-process]], [[steppe-old-mvp-deprecated]]).
