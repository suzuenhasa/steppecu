# Review findings — src__device__cuda__decode_af_kernel

Files: /home/suzunik/steppe/src/device/cuda/decode_af_kernel.cu, /home/suzunik/steppe/src/device/cuda/decode_af_kernel.cuh

## Group 4 — Type & numeric

No Group 4 issues found.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

## Group 3 — Dead / commented-out code

No Group 3 issues found.

## Group 5 — Hardcoded values / magic numbers

- [5.3][MED] src/device/cuda/decode_af_kernel.cu:69 — `byte_in_rec = s / 4u` re-picks the 2-bit-packing radix (4 genotype codes per byte) as a bare literal. The project already single-homes this as `io::kCodesPerByte = 4` (src/io/eigenstrat_format.hpp:54), and mind_prepass.cpp routes its identical byte-index math through it (`s / kPerByte`). The kernel here and the CPU oracle (src/device/cpu/cpu_backend.cpp:336) hold a duplicated copy of the radix that can DRIFT from the io single-home; the byte index MUST match the packing the reader used. Note: decode_af.hpp deliberately avoids depending on `io` (lines 53-57), so the fix is a named packing-radix constant in the core decode primitive home (decode_af.hpp), not a direct `io::kCodesPerByte` use. Suggested: name the radix once in decode_af.hpp (e.g. `kCodesPerByte`, pinned equal to `io::kCodesPerByte` by the existing equivalence test) and have the kernel + CPU backend divide by it.
- [5.3][MED] src/device/cuda/decode_af_kernel.cu:70 — `pos_in_byte = s & 3` re-picks the same 2-bit-packing fact (`& (codes_per_byte - 1)`) as the bare mask `3`. This duplicates the in-byte-position convention that decode_af.hpp::genotype_code already embeds (`k & 3`, line 67) and that io::code_in_byte derives from `kCodesPerByte` (eigenstrat_format.hpp:105). The kernel computes the position itself then passes it to genotype_code, so the mask lives in two places with no shared name; drift between the `/4u` byte index and the `&3` position would silently mis-decode. Suggested: derive the position mask from the same named radix (`pos = s % kCodesPerByte` or `& (kCodesPerByte-1)`) used for the byte index, so both come from one constant.

## Group 6 — Naming

- [6.4][LOW] src/device/cuda/decode_af_kernel.cu:69 — `byte_in_rec` abbreviates "record" to "rec" while the very next-cited parameter in the same TU spells it out as `bytes_per_record` (line 57/91); an inconsistent abbreviation of the same word ("record") within one file. The local also reads as plausibly "bytes in record" rather than "byte index within the record". Suggested: spell it consistently — e.g. `byte_in_record` (and optionally clarify intent as `byte_index_in_record`) to match `bytes_per_record`.

## Group 7 — Duplication

No Group 7 issues found.

## Group 8 — Comments

- [8.1][LOW] src/device/cuda/decode_af_kernel.cu:46 — the inline comment on `using core::kDecodeBlockY;` reads `// population axis (32*8 = 256 threads/block)`, but `kDecodeBlockY` is 8 (launch_config.hpp:167); the `32*8 = 256` is the whole block's thread-count product (kDecodeBlockX·kDecodeBlockY), attached to the Y-dim alias line. A reader skimming can misread it as kDecodeBlockY = 256, and the "256 threads/block" arithmetic restates a value derivable from the two block edges. Contrast the sibling line 45 comment, which correctly annotates kDecodeBlockX's own value (`32 = one warp`). Suggested: state kDecodeBlockY's own value/role on this line (e.g. `// population axis (8; block is 32×8 = 256 threads)`) so the inline comment describes the symbol it annotates.

## Group 9 — Constants & configuration

No Group 9 issues found.

## Group 10 — Initialization

No Group 10 issues found.

## Group 11 — Qualifiers & const-correctness

No Group 11 issues found.

## Group 12 — Launch config & indexing

- [12.3][LOW] src/device/cuda/decode_af_kernel.cu:106 — the x-axis (SNP/`M`) grid extent is the ONLY launch axis with no fail-fast over-limit guard: y rides `grid_for(P, kDecodeBlockY)` (asserts ≤ kMaxGridY) and the M4 batch rides `grid_z_extent` (asserts ≤ kMaxGridZ), but `gridDim.x` is `static_cast<unsigned>(core::cdiv(M, kDecodeBlockX))` with no assert that the result ≤ kMaxGridX. For the documented M envelope (≤ ~584131 → ~18255 blocks) and even M = 2^31 (~2^26 blocks) the `long`→`unsigned` cast cannot truncate, so this is not a present-day bug — but the asymmetry means the one axis that is allowed to grow toward 2^31 is the one without the sibling fail-fast, and a future caller passing an out-of-spec M (≥ ~6.9e10) would silently truncate the cast instead of getting the precise "exceeds hardware grid limit" message its neighbors give. Suggested: route the x extent through `grid_for`'s `max_grid` parameter with `kMaxGridX` (or an analogous long-aware assert) so all three axes share the same fail-fast guarantee. NOTE: per §7 the long-cdiv-on-x orientation itself is correct and intentional; only the missing matching cap-assert is the gap.

The remaining Group 12 tasks are clean: 12.1 the block is `dim3(kDecodeBlockX=32, kDecodeBlockY=8)` = 256 threads, a multiple of 32 with the full warp deliberately spent on the SNP axis for coalescing (launch_config.hpp:155-167) — warp-aligned. 12.2 both grid dims are derived via `cdiv`/`grid_for` (lines 106-107), none hardcoded. 12.3 (coverage) the grid covers the whole [P × M] domain by construction (x = cdiv(M,32), y = grid_for(P,8)) with the `if (s >= M || i >= P) return;` tail guard (line 65), so a one-elem-per-thread map with no grid-stride loop is correct for the documented P/M envelope (both extents fit their axis caps); no missed-coverage bug. 12.4 the 32×8 block is a named, warp-coalescing-justified constexpr (launch_config.hpp:162/167), an intentional §11.3 bandwidth-bound design choice, not a baked-in value to be auto-tuned. 12.5 the grid-dimension limits (kMaxGridX/Y/Z) are architectural constants documented as holding on all compute capabilities incl. sm_120 (launch_config.hpp:50-73); they do not vary per device, so no `cudaGetDeviceProperties` query is warranted.

## Group 13 — Error handling

No Group 13 issues found.

13.1 (unchecked cuda* API return): this TU makes NO bare `cuda*` runtime API calls — the kernel body (decode_af_kernel.cu:56-86) is pure device code with no host-API calls, and the launch wrapper `launch_decode_af` (lines 90-112) only issues the `<<<>>>` launch plus the post-launch check. No `cudaMalloc`/`cudaMemcpy`/`cudaStreamCreate`/etc. live here (the stream is received as a parameter, not created), so there is nothing to wrap. 13.2 (unchecked launch — needs BOTH arms): the single launch (decode_af_kernel.cu:108-110) is immediately followed by `STEPPE_CUDA_CHECK_KERNEL()` (line 111), which does BOTH required things — `cudaGetLastError()` for the synchronous launch-config error (throwing via STEPPE_CUDA_CHECK) AND a `STEPPE_DEBUG_ONLY(cudaDeviceSynchronize())` to attribute async faults to this launch under compute-sanitizer (check.cuh:251-256); release deliberately defers the async-fault surfacing to the next runtime call (the documented §7 hot-path contract, check.cuh:248-250), so this is the correct both-arms pattern, not a missing async check. 13.3 (inconsistent checking): there is exactly one launch in the unit and it is checked, so there is no guarded/unguarded asymmetry. 13.4 (error-swallowing homegrown macro): the only check used, `STEPPE_CUDA_CHECK_KERNEL`, routes through the single sanctioned `STEPPE_CUDA_CHECK` → `cuda_check` which THROWS a typed `CudaError` carrying file:line:function (check.cuh:151-155, 207-256) — it never `exit()`s and never swallows the status, so no homegrown error-hiding macro is present.

## Group 14 — Memory: allocation & lifetime

No Group 14 issues found.

This TU owns no memory. The kernel body (decode_af_kernel.cu:56-86) is pure device code, and the launch wrapper `launch_decode_af` (lines 90-112) only forms `dim3` grid/block values, issues the `<<<>>>` launch, and calls the post-launch check — it makes NO allocation API calls (`cudaMalloc`/`cudaMallocAsync`/`new[]`/`malloc`) and NO free calls (`cudaFree`/`cudaFreeAsync`/`free`/`delete[]`). All device buffers (`d_packed`, `d_pop_offsets`, `d_Q`, `d_V`, `d_N`, line 90-94) and the `stream` (line 95) are received as caller-owned parameters. Therefore: 14.1 (alloc/free mismatch) — N/A, no alloc or free here. 14.2 (stream-ordered alloc on hot paths) — N/A, no allocation; the hot-path enqueue rides the caller's `stream` (line 108). 14.3 (async/sync free pairing) — N/A, no frees. 14.4 (free before async work completes) — N/A, the launch is enqueued on the passed stream and the wrapper frees/reuses no buffer, so no cross-stream use-after-free is introduced. 14.5 (missing frees on error paths) — N/A, the only throwing surface is `STEPPE_CUDA_CHECK_KERNEL()` (line 111) and this TU holds no owned allocation to leak when it throws.

## Group 15 — Memory: transfers

No Group 15 issues found.

This TU performs NO host<->device transfers. The kernel body (decode_af_kernel.cu:56-86) is pure device code: it reads device buffers `packed`/`pop_offsets` and writes device buffers `Q`/`V`/`N`, all already device-resident. The launch wrapper `launch_decode_af` (lines 90-112) makes NO `cudaMemcpy`/`cudaMemcpyAsync` calls — it only forms `dim3` grid/block, issues the `<<<>>>` launch on the caller's `stream` (line 108), and calls the post-launch check (line 111). All five buffers (`d_packed`, `d_pop_offsets`, `d_Q`, `d_V`, `d_N`, lines 90-94) arrive as caller-owned device pointers. Therefore: 15.1 (H<->D memcpy in a loop that should be hoisted/batched/kept-resident) — N/A, no memcpy of any kind; the Q/V/N outputs are written in place to VRAM and kept device-resident for the downstream f2 path, exactly as the kept-resident design requires. 15.2 (direction enum not matching the actual transfer) — N/A, no `cudaMemcpy*` calls and thus no `cudaMemcpyKind` enum to mismatch. 15.3 (pageable host memory for frequent transfers where pinned ~doubles bandwidth) — N/A, no host staging buffers and no transfers in this TU, so there is nothing to pin.

## Group 16 — RAII: ownership & wrapper hygiene

No Group 16 issues found.

This TU creates and owns NO resource of any kind. The kernel body (decode_af_kernel.cu:56-86) is pure device code; the launch wrapper `launch_decode_af` (lines 90-112) only forms `dim3` grid/block values, issues the `<<<>>>` launch, and calls the post-launch check. Therefore: 16.1 (wrap every resource) — N/A, there is NO resource to wrap: no `cudaStreamCreate`/event/graph/`cudaGraphExec`/texture/surface/`cudaMallocAsync`-pool/CUDA-array/`cudaMallocHost`-pinned/cuBLAS-or-cuSOLVER `*Create` call appears in either file; the only handle present, `cudaStream_t stream` (line 95), is RECEIVED as a caller-owned parameter and is correctly used as a non-owning view (passed straight to the launch on line 108, never created or `cudaStreamDestroy`'d here). 16.2 (move-only + null-on-move) and 16.3 (rule of five) — N/A, there is no class/struct with a freeing destructor in either the .cu or the .cuh (the .cuh declares only the free function `launch_decode_af`, lines 37-42); nothing to make move-only or give a rule-of-five. 16.4 (single clear ownership; raw kernel pointers are non-owning views, never freed; do not pass owning wrappers by value) — satisfied: all device pointers (`d_packed`, `d_pop_offsets`, `d_Q`, `d_V`, `d_N`, lines 90-94) and the stream are non-owning views passed by raw pointer/handle, and the kernel itself takes `__restrict__` raw pointers (lines 56-62) as non-owning views and frees nothing; no owning wrapper is passed by value. 16.5 (don't reinvent the wrapper) — N/A, since nothing is owned there is no wrapper, hand-rolled or otherwise.

## Group 17 — RAII: lifetime & deleter pitfalls (CUDA-specific)

No Group 17 issues found.

This TU defines NO destructor, NO custom deleter, and NO RAII owner of any kind. The .cuh (decode_af_kernel.cuh:37-42) declares only the free function `launch_decode_af` — no class/struct, no `unique_ptr`/`shared_ptr` alias, no deleter type. The .cu has a pure device kernel (decode_af_kernel.cu:56-86, host-API-free) and the launch wrapper `launch_decode_af` (lines 90-112) which only forms `dim3` grid/block, issues the `<<<>>>` launch on the caller's stream (line 108), and calls `STEPPE_CUDA_CHECK_KERNEL()` (line 111). Therefore: 17.1 (non-throwing dtor / teardown-order at exit) — N/A, no destructor exists in either file; nothing is freed at scope exit or program teardown here. 17.2 (deleter matches allocator) — N/A, there is no allocation and no free of any flavor (no `cudaFree`/`cudaFreeHost`/`cudaFreeAsync`/`cudaFreeArray`, and no matching alloc) — all five device buffers (`d_packed`, `d_pop_offsets`, `d_Q`, `d_V`, `d_N`, lines 90-94) and the `stream` (line 95) are caller-owned parameters used as non-owning views. 17.3 (`unique_ptr<T[]>` over `cudaMalloc` → UB `delete[]`) — N/A, no smart pointer or array-form owner appears; the only pointers are raw `__restrict__` kernel views (lines 56-62). 17.4 (RAII vs async lifetime / free-at-scope-exit before async work finishes) — N/A, the wrapper owns nothing and frees nothing, so it cannot drop a buffer while the launch it enqueued on `stream` is still in flight; output-buffer lifetime is the caller's responsibility. 17.5 (multi-GPU device-correct free: deleter must `cudaSetDevice` to the alloc device) — N/A, there is no deleter and no free; this TU issues no `cudaSetDevice` and performs no per-device allocation/teardown.

## Group 18 — Correctness traps (wrong numbers, not crashes)

No Group 18 issues found.

The kernel (decode_af_kernel.cu:56-86) is an embarrassingly-parallel one-thread-per-(population i, SNP s) map with a fully thread-local reduction — there is NO shared memory, NO warp coordination, and NO cross-thread data flow, so the entire class of barrier/warp/order traps is absent by construction. 18.1 (divergent `__syncthreads()`): there are NO `__syncthreads()` calls anywhere in the TU; the only divergent control flow is the early `return` at line 65 and the inner-loop bound (line 74), neither followed by any barrier — no thread can reach a barrier some sibling skips. 18.2 (missing `__syncthreads()` for shared RAW/WAR): no `__shared__` declaration exists; the accumulators `ac`/`an` (lines 72-73) are per-thread registers written and read only by their owning thread, so there is no shared write-then-read needing a barrier. 18.3 (warp-synchronous assumptions): each thread runs independently start-to-finish with no implicit-warp-lockstep reliance — no `__syncwarp()`/`_sync` is needed because no thread observes another's intermediate state. 18.4 (non-`_sync` warp intrinsics): no `__shfl`/`__ballot`/`__any`/`__all` (sync or otherwise) appears; the coalescing is purely a memory-layout property (adjacent threads read adjacent record bytes, line 75), not a warp-shuffle reduction. 18.5 (missing bounds guard): the guard `if (s >= M || i >= P) return;` (line 65) gates BOTH axes before any indexing; the subsequent `pop_offsets[i+1]` (line 68) is safe because `pop_offsets` is `[P+1]` and `i < P` is established, and the inner loop is bounded by `[seg_begin, seg_end)` (line 74) — no OOB read. 18.6 (cross-thread read without barrier/atomic): each thread reads only the read-only inputs `packed`/`pop_offsets` (which it never writes) and writes only its own disjoint output cell `off = i + P·s` (lines 81-85, a bijection over the covered (i,s) domain), so no thread reads another thread's output and no atomic is required. 18.7 (order-dependent float reduction): the segmented reduction accumulates INTEGER `ac`/`an` via `accumulate_genotype` (decode_af.hpp:87-94) — integer addition is associative and commutative, so the loop result is independent of any thread/iteration order; the single per-thread FP64 divide in `finalize_af` (decode_af.hpp:135) is not a cross-thread reduction at all. This is the §11.3 bandwidth-bound, bit-exact-oracle decode: Q is exact because AC/AN are integer-accumulated and divided ONCE, with no float-sum ordering to enforce.

## Group 19 — Performance: debug leftovers

No Group 19 issues found.

19.1 (stray `cudaDeviceSynchronize()`): the TU issues exactly one kernel launch (decode_af_kernel.cu:108-110) and has NO bare `cudaDeviceSynchronize()` in either file; the only sync is the NDEBUG-gated `STEPPE_DEBUG_ONLY(cudaDeviceSynchronize())` reached via `STEPPE_CUDA_CHECK_KERNEL()` (line 111, check.cuh:251-256), which the brief explicitly classes as the intentional debug-only fault-attribution sync, not a serializing leftover — release defers async-fault surfacing to the next runtime call. 19.2 (leftover `printf`/`#if 0`): no `printf`/`std::printf`/`#if 0`/`#if 1`/commented-out launch appears anywhere — the kernel body (lines 56-86) is pure compute writing only Q/V/N, and the launch wrapper (lines 90-112) only forms `dim3` and launches; the sole preprocessor directives are the .cuh include guard and the standard `#include`s. 19.3 (redundant `__syncthreads()`): there are NO `__syncthreads()`/`__syncwarp()`/`__threadfence*()` calls in the TU at all (the kernel is a thread-local one-per-(population,SNP) reduction with no shared memory — confirmed in Group 18), so there is no barrier that could be guarding nothing.

## Group 20 — Performance: memory access

- [20.1][MED] src/device/cuda/decode_af_kernel.cu:81-85 — the Q/V/N output writes are STRIDED/uncoalesced. Output is column-major [P×M] with `off = i + P·s` (line 82); within a warp `threadIdx.x` runs over `s` (the warp axis, lines 63/105) while `i` is fixed per warp-row, so consecutive lanes write addresses `i + P·s` separated by stride `P` (P up to ~2500). Each of the three stores (Q/V/N, lines 83-85) therefore scatters the warp's 32 lanes across 32 distinct cache lines — the opposite of the coalesced reads the header advertises (lines 20-23, which describe only the input side). At decode scale (P·M outputs) this triples the store traffic via uncoalesced sectors. The reads coalesce on the SNP axis but the writes anti-coalesce on the same axis because the output is column-major in P. Suggested: either write the [P×M] outputs in row-major-over-the-warp-axis (so adjacent lanes hit adjacent addresses), or stage a warp's worth of results in shared/registers and emit a coalesced burst; if the column-major (i + P·s) layout is a fixed downstream-f2 contract, document that the store stride is an accepted bandwidth cost.
- [20.3][MED] src/device/cuda/decode_af_kernel.cu:75 — within a warp the input byte read is 4× redundant. `byte_in_rec = s/4` (line 69) takes only 8 distinct values across the 32 lanes of a warp ({0,0,0,0,1,1,1,1,…,7,7,7,7}), so each record byte is fetched by FOUR consecutive lanes, and this 8-byte window is re-fetched on EVERY iteration of the individual loop (line 74, [seg_begin,seg_end) — up to n_individuals per segment). The reads are contiguous (L1/L2 will serve the duplicates) so this is not a correctness or coalescing bug, and the comment frames the within-record read as the intended bandwidth-bound design (lines 15-23); but the 4-way duplication across lanes plus the per-g re-fetch is the canonical "re-reading the same global value instead of caching" pattern — one warp reuses the same 8 bytes 4×32 ways per record. Suggested: have one lane-group cooperatively load the 8-byte record window into shared/registers per `g` and broadcast the 2-bit extraction across the 4 SNP lanes (warp shuffle or shared), rather than 4 independent global reads of the same byte; only worth it if profiling shows the L1 hit rate isn't already absorbing it.

20.2 (shared-memory bank conflicts): N/A — the kernel declares NO `__shared__` memory of any kind (confirmed in Groups 18/19); the segmented reduction uses only per-thread registers (`ac`/`an`, lines 72-73), so there is no shared-memory access pattern that could serialize on banks.

## Group 21 — Performance: occupancy & registers

- [21.4][LOW] src/device/cuda/decode_af_kernel.cu:56 — `decode_af_kernel` has a single, fixed launch geometry (block is always `dim3(kDecodeBlockX=32, kDecodeBlockY=8)` = 256 threads, lines 105/108) yet carries no `__launch_bounds__(256)`. Adding the bound lets the compiler cap per-thread register allocation to the count that guarantees the intended occupancy at exactly that block size, so it cannot silently regress (over-allocate registers → fewer resident blocks) under a future nvcc/sm_120 codegen change. This is a LOW because the kernel is bandwidth-bound (the §11.3 decode front-end) with a tiny register footprint (a couple of `std::int64_t` accumulators plus the 3-double `AfResult` POD and index locals — no spill risk, see 21.3), so register-driven occupancy is not the present binding constraint; the hint is a deliberate guard for the one always-256-thread kernel, not a reflexive add. Suggested: annotate the kernel `__launch_bounds__(kDecodeBlockX * kDecodeBlockY)` to pin the occupancy contract to its single fixed block size.

21.1 (warp divergence) — clean. The only data-dependent branch is `accumulate_genotype`'s `if (genotype_valid(code))` (decode_af.hpp:90), whose taken/not-taken body is a trivial `ac += code; ++an;` that predicates rather than serializes — no heavy divergent work. The inner-loop trip count `[seg_begin, seg_end)` (line 74) varies by population row `i = threadIdx.y` (line 64), but within a single warp `threadIdx.y` is constant (the warp spans threadIdx.x = the SNP axis, kDecodeBlockX=32), so all 32 lanes of a warp share one segment length — no intra-warp loop-count divergence. The early `return` (line 65) only diverges at the grid edge (a partial last block), negligible. 21.2 (excessive shared memory) — clean: the kernel declares NO `__shared__` and launches with 0 dynamic smem (line 108), so smem cannot crater occupancy; the segmented reduction is purely register-local (`ac`/`an`, lines 72-73). 21.3 (register spills / monolithic kernel) — clean: the kernel body (lines 56-86) is small and flat — two int64 accumulators, the 3-double `AfResult`, the index/offset locals, and an inner loop over inlined scalar primitives (genotype_code/accumulate_genotype/finalize_af, all tiny `inline` STEPPE_HD funcs in decode_af.hpp); register pressure is low with no monolithic-kernel spill-to-local candidate, so no split is warranted. 21.4 (other hints) — `#pragma unroll` on the inner loop (line 74) is correctly ABSENT: the trip count is a runtime segment length (up to ~n_individuals, thousands), not a small compile-time constant, so unroll buys nothing; the shared decode primitives are already `inline` and trivially inlined, so no `__forceinline__` is needed.

## Group 22 — Performance: compute & launch

No Group 22 issues found.

22.1 (atomics where a reduction/scan is cheaper) — clean: the kernel uses NO atomics. Each thread owns one disjoint output cell `off = i + P·s` (lines 81-85, a bijection over the covered (i,s) domain), and the segmented reduction over `[seg_begin, seg_end)` (line 74) folds into per-thread register accumulators `ac`/`an` (lines 72-73). There is no cross-thread accumulation that an `atomicAdd` could be papering over — the one-thread-per-(population, SNP) map already does each reduction privately, which is the cheapest possible form (no atomic, no shared-memory scan needed).

22.2 (integer div/mod in loops) — clean: the only div and mod in the TU, `byte_in_rec = s/4u` (line 69) and `pos_in_byte = s & 3` (line 70), are both computed ONCE per thread BEFORE the inner loop and are loop-invariant (they depend on `s`, fixed per thread), so neither executes inside the `for (g …)` body (lines 74-78). The `/4u` is already a compile-time power-of-two divide the compiler emits as a shift, and the position is already written as the mask `& 3`. The inner loop's address math `g * bytes_per_record + byte_in_rec` (line 75) is a multiply-add, not a div/mod. No per-iteration integer division.

22.3 (loop-invariant work / repeated index recomputation) — clean: every loop-invariant quantity is already hoisted above the inner loop — `seg_begin`/`seg_end` (lines 67-68), `byte_in_rec` (line 69), `pos_in_byte` (line 70), and the `ac`/`an` accumulators (lines 72-73). Inside the loop (lines 74-78) only `g`-dependent work remains: the per-record byte fetch `packed[g * bytes_per_record + byte_in_rec]` (line 75) and the shared inlined decode step (line 77). The lone non-invariant `g * bytes_per_record` is a single multiply the compiler will strength-reduce to a running pointer increment; nothing recomputed redundantly. The output offset `off` and the `finalize_af` divide (lines 80-85) are correctly computed once, after the loop.

22.4 (launch overhead / fuse-or-graph) — clean: `launch_decode_af` issues exactly ONE kernel launch (lines 108-110) covering the entire [P × M] decode domain in a single grid (x = cdiv(M, 32), y = grid_for(P, 8), lines 106-107). There is NO loop of many small per-tile/per-population/per-SNP launches to fuse, and a single launch has nothing to amortize via a CUDA graph. The per-call launch cost is already minimal (one launch per decode), so neither fusion nor graph capture applies here.
