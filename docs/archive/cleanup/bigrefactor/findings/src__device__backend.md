# Review findings — src__device__backend

Files: /home/suzunik/steppe/src/device/backend.hpp

## Group 4 — Type & numeric

- [4.7][LOW] backend.hpp:485-494 — `compute_f2_blocks_into` takes raw `double* dst_f2`, `double* dst_vpair`, `int* block_sizes_dst` with no host-vs-device type distinction; the doc-comment contract pins them as caller-provided PINNED HOST destinations, but the bare pointer type can't enforce that a device pointer isn't passed by mistake. Suggested: this is the intended CUDA-free seam shape (device residency crosses via opaque DevicePartial/DeviceF2Blocks handles, not raw pointers), so leave as-is or add a thin host-pointer tag only if a wrapper already exists elsewhere; not worth introducing here. Note: no narrowing/overflow — `slab_off = (size_t)P*P*b0` (line 467) is documented with the correct size_t cast, and all index math lives in the .cu/.cpp, not this header.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

## Group 3 — Dead / commented-out code

No Group 3 issues found.
<!-- 3.1 commented-out blocks: every `//` is documentation (Doxygen contracts, AT2/§12 parity rationale, design notes); no code commented "just in case". 3.2 unreachable: only preprocessor is the include guard (25-26 / 763-764); no #if 0; the inline default virtual bodies (e.g. 430-435, 452-460, 485-494, 512-516, 578-582, 592-595, 608-611, 622-625, 645-648, 660-663, 679-682, 721-725) end at the throw with nothing after it. 3.3 unused symbols: all 7 system includes are live (cstddef→size_t 208-211/284-285/759; cstdint→uint8_t 207; span→std::span 320/576-577/...; stdexcept→runtime_error 432+; string→pat 181; vector→64/73/...) and all 8 steppe includes are used by declared symbols (config→Precision 383; error→Status 125/137; fstats→F2BlockTensor 589 param; qpadm→QpAdmOptions/Model/Result 318-320/717-721; views→core::MatView 380; device_partial→DevicePartial 452; device_f2_blocks→DeviceF2Blocks 319/428/575/718; stream_f2_blocks→StreamTarget 512); the fwd `class ComputeBackend;` (308) is used by the free-fn decl (319). 3.4 computed-but-unread: declarations/POD defaults/inline throw bodies only; the `(void)param;` discards are the intentional unused-parameter idiom for default-throwing virtuals whose signatures are the frozen contract used by overrides. -->

## Group 5 — Hardcoded values / magic numbers

No Group 5 issues found.
<!-- This is a pure CUDA-free interface header: declarations, PODs, value-init defaults, doc-comments, and inline default-throwing virtual bodies. 5.1 unnamed literals: every numeric literal is a POD value-init default with clear zero/identity semantics (P=0, M=0, n_block=0, m=0, r=0, chisq=0.0, wt=0/dof=0/p=0.0 in PodDropRow, device_count=0, compute_major/minor=0, total/free_vram_bytes=0, *=nullptr, *=false); none is an unexplained magic constant. The only non-zero literal `int ploidy = 2;` (218) is a documented domain metadata default ("Default 2 (diploid), documented", lines 216-218) — semantically named by its field + comment, not magic. The svd_path report codes 0/1/2 (165-170) are documented dispatch enum-like values. 5.2 hardcoded sizes/bounds: the small-path envelope (nl<=5, nr<=10, r<=4 line 327) and cuSOLVER routing cap (nl,nr<=32 lines 168, 639) and als_iterations default 20 (654) appear ONLY in descriptive doc-comments; the actual values live in model_in_small_path / QpAdmOptions / the .cu, not as code literals here — correctly NOT hardcoded in this header. 5.3 duplicated constants / drift: no launch-config or shared-mem array sizes (CUDA-free header); the 32 appears only twice in prose (168, 639), consistently, with no code-level duplication that could drift. 5.4 hardcoded paths/IDs/device ids: "conventionally GPU 0, the combine root" (286) is doc prose describing the combine convention, not a hardcoded device id in code; no paths/IDs. 5.5 ambiguous 32: the 32 (168, 639) is the documented gesvdjBatched batched-SVD size cap (nl,nr<=32), explicitly named in context — NOT an unlabeled warp-size literal, and only in prose. -->

## Group 6 — Naming

- [6.3][LOW] backend.hpp:73 — `F2Result` field is spelled lowercase `vpair`, but every doc-comment naming the quantity (lines 19, 66, 70, 376–377, 386, 407) and the matching `compute_f2` doc all capitalize it `Vpair`, while the sibling field one line below is capital `P` (line 76). Within this one struct the same datum is `vpair` (field) vs `Vpair` (its own docs) vs the capital `P` neighbor — an inconsistent casing convention in a single file. Suggested: pick one (either rename the field to match the documented `Vpair`, or lowercase the doc usages) so field-name and contract-name agree; cosmetic, no behavior change.
- [6.3][LOW] backend.hpp:228-232 vs 380-383 — the Q/V/N contract is stored as lowercase fields `q`/`v`/`n` in `DecodeResult` but is referred to (and the `compute_f2` parameters are named) uppercase `Q`/`V`/`N`; the same three quantities thus appear in two casings within this header. This is documented ("the Q/V/N contract") and the field-lowercase / contract-uppercase split is a deliberate convention, so it is intentional, not a bug. Suggested: leave as-is (documented intent); flagged only for completeness of the consistency pass.

Note: the single-letter struct fields `Q`/`Qinv`/`A`/`B`/`w`/`r`/`m`/`P`/`M`/`p` (lines 117–134, 146–148, 76, 234–236) are AT2 / linear-algebra canonical names, each documented inline (e.g. `Q` = jackknife covariance, `A`/`B` = SVD factors, `m = nl*nr`, `r` = rank, `P` = populations, `M` = SNPs); they are parity-load-bearing per §12 (they mirror the AT2 source), NOT cryptic — deliberately NOT flagged. No 6.1 (cryptic), 6.2 (misleading), or 6.4 (nonstandard-abbreviation) issues found: abbreviations (`nl`/`nr`/`dof`/`vpair`/`Qinv`/`loo`/`chisq`/`als`) are standard domain/stats terms, and no name's referent contradicts it (counts are counts, `block_id` is an id, `pop_offsets` are offsets).

## Group 7 — Duplication

- [7.4][LOW] backend.hpp:428-435, 452-460, 485-494, 509-517, 575-582, 589-596, 605-612, 619-626, 641-649, 656-664, 677-683, 717-726 — twelve inline default virtual bodies are the SAME copy-pasted boilerplate differing only by a string constant: discard every parameter with a run of `(void)param;` casts, then `throw std::runtime_error("ComputeBackend::<name>: not …")`. The pattern is mechanically identical across compute_f2_blocks_device / _resident / _into / _streamed, both assemble_f4 overloads, jackknife_cov, rank_test, rank_sweep, gls_weights, gls_weights_loo_batched, and fit_models_batched. Suggested: this is the deliberate, documented "the established backend.hpp pattern" for non-pure base virtuals that overrides must implement (each message is a contract-specific sentinel), and a header macro folding it would HURT the readability/grep-ability of the frozen interface contract more than it helps — leave as-is. Flagged only for completeness; not worth a macro/helper here.
- [7.2][LOW] backend.hpp:573, 89-91, 327-330, 574 — the same flatten/shape conventions are re-stated in prose at several sites: `m = nl*nr` (89, 573), the row-major `k = j + nr*i` flatten (90, 574), and `nl = left.size()`/`nr = right.size()-1`/`r = (opts.rank<0 ? nl-1 : opts.rank)` (109-110, 329-330). These are DOC-COMMENT restatements of a single pinned contract, not duplicated runtime expressions (no recompute; the actual arithmetic lives in the .cu/.cpp), and the repetition is intentional so each virtual's contract is self-contained at its declaration. Suggested: leave as-is — repeating the binding convention next to each method is a feature for a frozen interface, not loop-invariant recompute. No code-level 7.2 issue.

Note (7.1/7.3): No 7.1 (copy-pasted code blocks differing by a constant) beyond the doc-pattern 7.4 above — this is a declarations-only interface header (PODs + pure/inline-throwing virtuals), so there are no duplicated compute blocks to extract. No 7.3 (repeated sizeof/casts): the only cast in the header is `slab_off = (size_t)P*P*b0` and it appears ONLY in the compute_f2_blocks_into doc-comment (line 467 prose), not as repeated code; the `(void)param;` casts are the unused-parameter idiom, covered under 7.4, not redundant sizeof/cast duplication.

## Group 8 — Comments

- [8.2][LOW] backend.hpp:166,168 — stale/contradictory milestone tag inside the SAME `RankSweep::svd_path` doc block: line 166 says the on-device Jacobi is "the path EXECUTED at these sizes by M(fit-4)" but line 168-169 says "the executed SVD is the deterministic on-device Jacobi at all sizes in M(fit-2)". Same fact (the executed Jacobi SVD), two different milestones in adjacent sentences; line 639's matching note correctly says "(M(fit-4))", and the rest of the file uses M(fit-4) for the executed GPU path, so the "M(fit-2)" on line 168 is a leftover from before the M(fit-4) merge. Suggested: change "in M(fit-2)" (line 168) to "in M(fit-4)" to match line 166 and line 639.

Note (8.1/8.3/8.4): No 8.1 (restating code) — this is a contract/interface header; every `//` and `///` is a Doxygen contract, an AT2/§12 parity rationale, a flatten-order pin, or a design-doc cross-reference, none merely narrates an adjacent statement (there is no executable logic here beyond the inline `throw` sentinels, which are themselves explained). No 8.3 (missing rationale) — the non-obvious choices are all justified in place: the row-major `k = j + nr*i` flatten (89-91, "Using i + nl*j would silently transpose Q"), `ploidy = 2` default (216-218, "METADATA parameter, NEVER auto-detected"), every NON-PURE base-throw virtual states WHY it throws and which backend overrides it (e.g. 425-427, 450-451, 483-484, 506-508, 702-716), `set_solve_precision` documents the native-default/promotion rationale (549-561), and the value-init "unknown" defaults are explained (262-264, 296, 304-305). I spot-checked the load-bearing claims against the implementation — small-path envelope nl<=5/nr<=10/r<=4 (matches model_search.cpp:73 and cuda_backend.cu:1490), als_iterations default 20 (matches qpadm.hpp:64), the af6a8c2 golden tag (matches cpu_backend.cpp / cuda_backend.cu / fit-engine.md), and fit_models_batched_default living in core (matches model_search.cpp:55) — all current, so beyond the 8.2 milestone slip the comments are not stale. No 8.4 (orphan TODO/FIXME/HACK) — grep finds no TODO/FIXME/HACK/XXX token in the file; pending work is described as documented contract seams with design-doc references (e.g. the "PENDING seam" at 168-169, the combine "next workflow, not implemented here" at 349-350), each with context and an owning milestone/design section, not as anonymous markers.

## Group 9 — Constants & configuration

No Group 9 issues found.
<!-- This is a declarations-only CUDA-free interface header (PODs + pure/inline-throwing virtuals + doc-comments); it contains no executable logic, no file/function-scope computed variables, and no call sites. 9.1 should-be-const/constexpr: nothing here is a mutable variable that should be const/constexpr — the only "values" are POD struct fields (F2Result/F4Blocks/JackknifeCov/GlsWeights/RankSweep/PopDropRow/DecodeTileView/DecodeResult/BackendCapabilities) and their value-init defaults (P=0, M=0, m=0, r=0, chisq=0.0, status=Status::Ok, device_count=0, ploidy=2, *=nullptr, *=false). Those fields are MUTABLE by design — backends fill them and they cross the seam by value (architecture.md §4); making them const would break the contract. There is no namespace/static constant, no local that is assigned once then never mutated, and no magic literal that should be a named constexpr (the small-path envelope nl<=5/nr<=10/r<=4 at 327, the cuSOLVER cap 32 at 168/639, and als_iterations 20 at 654 appear ONLY in prose; the real values live in model_in_small_path / QpAdmOptions / the .cu, correctly NOT as literals here). 9.2 tangled config: the tunable knobs are surfaced, not buried in logic — `set_solve_precision` (562) is an EXPLICIT documented per-stage promotion seam (ROADMAP §6, native-FP64 default), the matmul `Precision&` is an explicit parameter on every compute virtual (default EmulatedFp64{40} documented at 369-373/406), and `ploidy`/`fudge`/`alpha`/`als_iterations`/the small-path envelope are owned by DecodeTileView / the method params / QpAdmOptions / model_in_small_path, not hidden inside a method body here (there are no method bodies beyond the throw sentinels). No knob is hardcoded inline where it should be a config field. 9.3 positional booleans: there are NO function CALLS in this header, so no foo(true,false,true) call-site smell exists. The bool DATA does appear (BackendCapabilities::can_access_peer 296, ::emulated_fp64_honorable 305; PopDropRow::feasible 188; DecodeTileView pointers default nullptr) but each is a NAMED struct field with a default and inline doc — aggregate/field initialization is self-documenting by the member name, not a positional anonymous bool. No multi-bool positional parameter list on any declared virtual either (the only adjacent same-type run is the two bools in BackendCapabilities, which are named fields, not call args). -->

## Group 10 — Initialization

No Group 10 issues found.
<!-- This is a declarations-only CUDA-free interface header (PODs + value-init defaults + abstract/inline-throwing virtuals + doc-comments); it has NO executable logic with locals. 10.1 late/distant or uninitialized-then-assigned: the only "code" bodies are the inline default-throwing virtuals (430-435, 452-460, 485-494, 512-516, 578-582, 592-595, 608-611, 622-625, 645-648, 660-663, 679-682, 721-725), each just `(void)param;` discards then a `throw` — they declare NO local variables, so no local can be declared-far-from-use or uninitialized-then-assigned. Struct members are declarations, not locals. 10.2 zero-init assumptions: every POD scalar/bool/pointer field carries an EXPLICIT in-class default initializer — F2Result P=0 (76); F4Blocks nl/nr/n_block=0 (109-112); JackknifeCov m=0 (123), status=Status::Ok (125); GlsWeights chisq=0.0 (133), r=0 (134), status=Status::Ok (137); RankSweep f4rank=0/rank_Q=0/svd_path=0 (162-170), status=Status::Ok (172); PopDropRow wt/dof=0, chisq/p=0.0 (183-186), f4rank=0 (186), feasible=false (188), status=Status::Ok (189); DecodeTileView packed=nullptr, bytes_per_record/n_snp/n_individuals=0, pop_offsets=nullptr, n_pop=0, ploidy=2 (207-218); DecodeResult P=0 (234), M=0 (236); BackendCapabilities device_count=0, compute_major/minor=0, total/free_vram_bytes=0, can_access_peer=false, emulated_fp64_honorable=false (270-305). The std::vector / std::string members self-construct empty. Nothing relies on implicit zero-init that does not hold, and there is no partial-init trap: the capabilities() base `return BackendCapabilities{};` (748) value-initializes a fully-defaulted POD (the documented "all-zero/false ⇒ unknown" contract, 262-264) — deterministic, no uninitialized read. -->

## Group 13 — Error handling

No Group 13 issues found.
<!-- This is the CUDA-FREE-BY-CONTRACT interface header (line 6: "THIS HEADER IS CUDA-FREE BY CONTRACT") — declarations-only: PODs, value-init defaults, abstract pure virtuals, and inline default-throwing base virtuals. There are ZERO cuda* runtime API calls, ZERO cuBLAS/cuSOLVER calls, and ZERO kernel launches (no <<<>>>) in the file; all such calls live PRIVATE to steppe_device in src/device/cuda/*.cu. So none of the Group 13 tasks have a subject here. 13.1 unchecked cuda* return: none — no cuda* call exists to check. 13.2 unchecked launch (cudaGetLastError + sync): none — no launch exists. 13.3 inconsistent checking: vacuous — nothing to check inconsistently. 13.4 error-swallowing homegrown macro: none — the file defines no macro at all (the only preprocessor is the STEPPE_DEVICE_BACKEND_HPP include guard, 25-26/763-764). The only error-emitting constructs are the inline base-virtual `throw std::runtime_error(<contract-specific message>)` sentinels (432-434, 456-459, 492-493, 514-516, 580-581, 593-595, 610-611, 623-625, 646-648, 661-663, 681-682, 723-725); these are the documented NON-PURE "the established backend.hpp pattern" — they SURFACE a contract violation loudly (a backend that did not override a non-pure virtual was invoked), the opposite of swallowing. Domain outcomes that must NOT throw are correctly modeled as VALUE status instead (Status fields on JackknifeCov 125, GlsWeights 137, RankSweep 172, PopDropRow 189; fit_models_batched doc 698 "Domain outcomes are per-result status ... NEVER a throw"), which is the right record-and-continue policy for a batched search — not an error-handling defect. -->

## Group 14 — Memory: allocation & lifetime

No Group 14 issues found.
<!-- This is the CUDA-FREE-BY-CONTRACT interface header (line 6: "THIS HEADER IS CUDA-FREE BY CONTRACT") — declarations-only: PODs, value-init defaults, abstract pure virtuals, and inline default-throwing base virtuals. There are ZERO cudaMalloc/cudaMallocAsync/cudaFree/cudaFreeAsync/cudaMemPool*/new[]/malloc/free calls in the file; all device allocation & lifetime lives PRIVATE to steppe_device in src/device/cuda/*.cu, never crossing this CUDA-free seam. 14.1 alloc/free mismatch: none — no allocation or free call of any kind exists. The owned memory in this header is all std::vector<double>/std::vector<int>/std::string struct members (F2Result.f2/vpair, F4Blocks.x_blocks/x_total/x_loo, JackknifeCov.Q/Qinv, GlsWeights.w/A/B, RankSweep.* arrays, PopDropRow.weight/pat, DecodeResult.q/v/n) — RAII containers that self-allocate and self-free; no manual matching to worry about. 14.2 stream-ordered alloc on hot paths: none — no cudaMalloc/cudaFree to convert to cudaMallocAsync/FreeAsync; the hot-path device allocators are in the .cu, not here. 14.3 async/sync free pairing: vacuous — no alloc/free calls to mispair. 14.4 free before async work completes (use-after-free across streams): none — this header manages no device buffer; device residency crosses the seam ONLY via opaque, move-only handles (DevicePartial line 452, DeviceF2Blocks lines 319/428/575/718, StreamTarget line 512) whose own lifetime/free contracts live in device/device_partial.hpp / device_f2_blocks.hpp / stream_f2_blocks.hpp, not here. The raw pointers present — DecodeTileView::packed/pop_offsets (lines 207/211, documented "All pointers are borrowed (the caller owns the storage for the call's duration)", lines 198-199) and compute_f2_blocks_into's dst_f2/dst_vpair/block_sizes_dst (lines 488, documented "caller-provided PINNED host destination ... ONE shared result owned by the orchestrator", lines 466-481) — are explicitly NON-OWNING / caller-owned; the header neither allocates nor frees them, and the disjoint-slice contract (lines 470-471) is the orchestrator's concern. 14.5 missing frees on error paths: none — the only error paths are the twelve inline base-virtual `throw std::runtime_error(...)` sentinels (432-434, 456-459, 492-493, 514-516, 580-581, 593-595, 610-611, 623-625, 646-648, 661-663, 681-682, 723-725), each of which discards its params with `(void)` and throws immediately holding NO resource — nothing to leak. -->

## Group 15 — Memory: transfers

No Group 15 issues found.
<!-- This is the CUDA-FREE-BY-CONTRACT interface header (line 6: "THIS HEADER IS CUDA-FREE BY CONTRACT") — declarations-only: PODs, value-init defaults, abstract pure virtuals, and inline default-throwing base virtuals. There are ZERO cudaMemcpy/cudaMemcpyAsync/cudaMemcpyPeer calls, ZERO cudaMemcpyKind enum tokens (no cudaMemcpyHostToDevice/DeviceToHost/...), and ZERO host-pinning calls (no cudaHostAlloc/cudaHostRegister/cudaMallocHost) in the file; every H<->D transfer lives PRIVATE to steppe_device in src/device/cuda/*.cu, never crossing this seam. 15.1 cudaMemcpy in a loop (hoist/batch/keep-resident): none — no transfer call of any kind exists here. The transfer SEMANTICS are only DESCRIBED in doc-comments, and they describe exactly the resident/batched design the task wants, not a per-iteration copy: assemble_f4 reads "device-resident f2 (zero D2H on the CUDA path)" (line 567); compute_f2_blocks_device leaves the result "RESIDENT in VRAM ... NO forced D2H, NO host alloc" (lines 419-423); compute_f2_blocks_resident "LEAVE the f2/Vpair tensors RESIDENT on this backend's device (NO D2H, NO free)" (lines 439-444); the BATCHED virtuals (compute_f2_blocks over n_block 409-414, gls_weights_loo_batched over n_block 677-683, fit_models_batched over the model axis 717-726) carry a batch axis SO the .cu can do one batched dispatch, NOT a host transfer loop — the header's design is anti-15.1 by construction. 15.2 direction enum mismatch: vacuous — no cudaMemcpyKind argument appears (no CUDA type is allowed across this seam, line 6); the textual "D2H"/"H2D" mentions (e.g. lines 421/443/466/473/475) are prose describing the .cu behavior, not a code-level direction enum that could disagree with its pointers. 15.3 pageable host memory for frequent transfers: none to flag — and where the header DOES pin down host-transfer policy it prescribes PINNED, the desired answer: compute_f2_blocks_into documents the destination is a "caller-provided PINNED host destination" (line 466) and "The backend page-locks [dst_f2+slab_off, ... +P*P*n_block) and the vpair slice for the D2H window (RegisteredHostRegion, graceful pageable degrade) so the two devices' D2Hs run as concurrent pinned DMAs" (lines 472-475) — i.e. the contract explicitly requires pinned staging for the concurrent multi-GPU D2H, the opposite of a pageable-transfer defect. The raw double* dst_f2/dst_vpair/block_sizes_dst (line 488) are caller-owned host destinations whose pinning is the orchestrator's/`.cu`'s job per that contract; the actual cudaHostRegister/cudaMemcpy lives in cuda_backend.cu, not here. NB the Group-4 [4.7][LOW] note already records that these pinned-host destinations are passed as bare double* with no host-vs-device type tag — a type-seam observation, not a transfer defect, so it is not re-raised here. -->

## Group 17 — RAII: lifetime & deleter pitfalls (CUDA-specific)

No Group 17 issues found.
<!-- This is the CUDA-FREE-BY-CONTRACT interface header (line 6: "THIS HEADER IS CUDA-FREE BY CONTRACT") — declarations-only: PODs, value-init defaults, abstract pure virtuals, and inline default-throwing base virtuals. Every Group 17 task is vacuous here because the header neither allocates, frees, nor destroys any CUDA resource; all such lifetime logic lives PRIVATE to steppe_device in src/device/cuda/*.cu, behind this seam, and is reviewed in those units. 17.1 non-throwing destructor: the ONLY destructor in the file is `virtual ~ComputeBackend() = default;` (line 358) on the abstract base that owns NO resource directly (Group 16 confirmed: copy/move deleted 354-357, no freeing dtor body) — it issues no cudaFree/cudaStreamDestroy/handle-destroy, so there is no in-dtor CUDA-error-during-unwinding hazard and no teardown-order/context-already-gone-at-exit concern to guard; the defaulted dtor only runs the (empty) member-destruction of std::vector/std::string POD fields in derived results, which is noexcept-correct. The POD result structs (F2Result/F4Blocks/JackknifeCov/GlsWeights/RankSweep/PopDropRow/DecodeResult/BackendCapabilities) declare NO user destructor — implicitly-noexcept value semantics over std::vector/std::string/scalars only, nothing to swallow. 17.2 deleter matches allocator: there are ZERO frees of any kind (no cudaFree, cudaFreeHost, cudaFreeAsync, cudaFreeArray) and ZERO allocators (no cudaMalloc*/cudaMallocHost/HostAlloc/cudaMallocAsync/cudaMallocArray) in the file — no allocator/deleter pair exists to mismatch. The host-pinned destinations described for compute_f2_blocks_into (lines 466-481, "caller-provided PINNED host destination ... page-locks ... for the D2H window (RegisteredHostRegion, graceful pageable degrade)") are PROSE describing the .cu's RegisteredHostRegion wrapper; the actual cudaHostRegister/cudaHostUnregister (not Malloc/FreeHost) pairing lives in cuda_backend.cu and is reviewed there, not at this CUDA-free seam (no CUDA type may appear here, line 6). 17.3 unique_ptr<T[]> on cudaMalloc: the only smart pointer named in the file is `std::unique_ptr<ComputeBackend>` (prose, lines 338-339, "Move-only ownership of concrete backends ... in Resources") — a single-object HOST owner of a polymorphic backend with the correct virtual dtor (358), NOT a `unique_ptr<T[]>` over a device pointer; there is no array-form unique_ptr and no cudaMalloc'd buffer wrapped here, so the default-delete[]-on-device-pointer UB is not present. 17.4 RAII vs async lifetime (free-at-scope-exit != async-work-finished): the header manages NO device buffer and issues NO async work — there is no stream, no cudaMallocAsync/FreeAsync, no kernel launch, and no RAII guard whose dtor could free memory still in flight. Device residency crosses the seam ONLY via opaque, move-only HANDLE types (DevicePartial line 452, DeviceF2Blocks lines 319/428/575/718, StreamTarget line 512) returned BY VALUE from producer virtuals and consumed BY const-ref by reader virtuals (Group 16 confirmed the value categories); the sync-before-free / tie-free-to-stream obligation for the buffers those handles own lives in device/device_partial.hpp / device_f2_blocks.hpp / stream_f2_blocks.hpp and the .cu, not in this header. The borrowed raw views (DecodeTileView::packed/pop_offsets 207/211, "caller owns the storage for the call's duration" 198-199; compute_f2_blocks_into's dst_f2/dst_vpair/block_sizes_dst 488) are NON-OWNING and never freed here, so no scope-exit-vs-async race is introduced. 17.5 multi-GPU device-correct free (deleter must cudaSetDevice to the alloc device, record+restore): there is NO deleter in this file to make device-correct. The per-device-instance contract IS documented here (lines 341-350: "One backend instance is bound to exactly ONE CUDA device ... cudaSetDevice-bound to it"), which is exactly the invariant a device-correct deleter would rely on, but the deleter that must record-the-current-device / cudaSetDevice(alloc-device) / restore before cudaFree lives in the CUDA backend and the opaque-handle headers (where the buffer was allocated under that device binding), not at this CUDA-free seam — no in-file subject. -->

## Group 16 — RAII: ownership & wrapper hygiene

No Group 16 issues found.
<!-- This is the CUDA-FREE-BY-CONTRACT interface header (line 6: "THIS HEADER IS CUDA-FREE BY CONTRACT") — declarations-only: PODs, value-init defaults, abstract pure virtuals, and inline default-throwing base virtuals. It OWNS no raw resource of any kind. 16.1 wrap every resource: there are ZERO streams/events/graphs/texture-or-surface objects/memory-pools/CUDA-arrays/pinned-host-memory/library-handles (no cuStream*, cudaEvent_t, cudaGraph*, cublasHandle_t, cusolverDnHandle_t, cudaArray, cudaMallocHost) declared or held in this file; every such resource lives PRIVATE to steppe_device in src/device/cuda/*.cu, behind this seam (line 6-9). Device residency crosses the seam ONLY via opaque, move-only HANDLE types that are themselves RAII wrappers defined in their own headers — DevicePartial (device/device_partial.hpp, line 40/452), DeviceF2Blocks (device/device_f2_blocks.hpp, lines 41/319/428/575/718), StreamTarget (device/stream_f2_blocks.hpp, lines 42/512); their *Create/*Destroy/free hygiene is reviewed in THOSE units, not here. So 16.1 has no in-file subject. 16.2 move-only + null-on-move and 16.3 rule-of-five: the ONE type with declared special members is the abstract base ComputeBackend (351-358), and it is correct — copy ctor + copy assign DELETED (354-355), move ctor + move assign DELETED (356-357), virtual ~ComputeBackend() = default (358). A polymorphic abstract base that owns NO resource directly is right to be non-copyable AND non-movable with a virtual defaulted dtor: it prevents object slicing, and concrete-backend ownership is held one level up via std::unique_ptr<ComputeBackend> in Resources (documented lines 337-339) — there is no freeing destructor here that a moved-from null-handle invariant (16.2) or a hand-written rule-of-five (16.3) would need to protect, because the base frees nothing. The POD result structs (F2Result/F4Blocks/JackknifeCov/GlsWeights/RankSweep/PopDropRow/DecodeResult/BackendCapabilities) hold only std::vector/std::string/scalars and correctly rely on the implicitly-generated, value-semantics special members (no user dtor ⇒ no rule-of-five obligation, no double-free risk). 16.4 single clear ownership / non-owning views never freed / no owning-wrapper-by-value: the move-only owning handles are produced and consumed with the right value categories — they are RETURNED BY VALUE from the producer virtuals (compute_f2_blocks_device returns DeviceF2Blocks, 428; compute_f2_blocks_resident returns DevicePartial, 452 — move-out of a freshly-owned resource, the unique_ptr-like single-owner model), and CONSUMED BY const-reference where a method only reads them (assemble_f4 takes `const steppe::device::DeviceF2Blocks& f2`, 575; fit_models_batched takes `const steppe::device::DeviceF2Blocks& f2`, 718; fit_models_batched_default likewise const-ref, 319) — NO owning wrapper is passed BY VALUE into a method (which would force a move/copy of ownership across the call), and none is returned by raw pointer/reference-to-temporary. The raw views in the file are explicitly NON-OWNING and the header never frees them: DecodeTileView::packed/pop_offsets (207/211, "All pointers are borrowed (the caller owns the storage for the call's duration)", 198-199) and compute_f2_blocks_into's double* dst_f2/dst_vpair/block_sizes_dst (488, "caller-provided ... ONE shared result owned by the orchestrator", 466-481) — borrowed kernel-style views, correctly never freed here. 16.5 don't reinvent the wrapper: the header introduces NO new ad-hoc resource wrapper; the move-only device handles are the project's deliberate opaque-handle seam (CUDA-free by contract, so std::unique_ptr<…, cudaDeleter> can't appear here — a device deleter would drag CUDA across the seam), and host ownership is std::vector/std::string + std::unique_ptr<ComputeBackend> (338) — standard library RAII, not reinvented. Nothing to flag. -->



