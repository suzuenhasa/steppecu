# src__device__cuda__ratio_block_jackknife_kernel
Files: /home/suzunik/steppe/src/device/cuda/ratio_block_jackknife_kernel.cu, /home/suzunik/steppe/src/device/cuda/ratio_block_jackknife_kernel.cuh
Subsystem: device-cuda

## Findings

### G7
- [G7.src__device__cuda__ratio_block_jackknife_kernel][LOW] ratio_block_jackknife_kernel.cu:149-178 — The dstat branch recomputes the identical 6-line LOO chain (`cn`, `est_num`, `est_den`, `rel`, `loo_num`, `loo_den`, `R`) in BOTH Pass 2b (lines 152-157) and Pass 2c (lines 171-176), then a 3rd nearly-identical copy in Pass 3 (lines 186-191). The line 165-166 comment documents that the recompute is deliberate (bit-identical from the same `tot_num`/`tot_den`, mirroring host pass structure), so this is parity-load-bearing, not a correctness bug — but the three copies differ by zero constants and could collapse into one helper without changing FP operand order. Suggested: extract a `__device__` `loo_R(k,b,...)` helper returning `R` (and the operands Pass 3 needs) to state the chain once; verify the emitted FP ops are unchanged before/after under the goldens.

### G11
- [G11.src__device__cuda__ratio_block_jackknife_kernel][LOW] ratio_block_jackknife_kernel.cu:48-50 — The five input descriptors (`num`, `den`, `weight`, `xblk_num`, `xblk_den`) are passed by value; each holds a `const double* data` (struct field, line 22 of .cuh) that is NOT `__restrict__`, unlike the output pointers `d_est`/`d_se`/`d_z`/`d_p` which correctly carry `__restrict__` (lines 52-53). The five read-only data pointers and the four write pointers are guaranteed non-aliasing by the caller, so a `__restrict__` qualifier on the descriptor `data` field could let the compiler hoist/reuse the loaded values. Small-struct by-value (5 × 32 B = 160 B) is fine for param space; this is purely a missed read-only-aliasing hint. Suggested: consider `const double* __restrict__ data` in `DRatioJackArray` (verify it does not perturb the §12-fixed FP operand order before adopting).

No other issues found (groups checked: G2-G10,G11-G22). Notes on items deliberately NOT flagged: the redundant-looking `(diffsum / nb) * nb` at lines 98 and 179 is the §12 cancellation-carve-out operand order matching the host long-double reference (comment line 98), NOT a dead computation; the `est = term1 + term2` split (f4ratio) vs inlined `est` (dstat) is the faithful host shape per branch; native FP64 / `double` math is FP64-by-design; the grid-stride loop (lines 56-58), block dim 128 (≡0 mod 32, line 217), `kMaxGridX` clamp (line 220), `long` item index `k` and `long` `rj_at` arithmetic (lines 40, 56-58), and the post-launch `STEPPE_CUDA_CHECK_KERNEL()` (line 224, which does `cudaGetLastError()` + a debug-gated sync) are all correct.
