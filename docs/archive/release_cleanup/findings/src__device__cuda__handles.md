# src__device__cuda__handles
Files: /home/suzunik/steppe/src/device/cuda/handles.hpp
Subsystem: device-cuda

## Findings

### G2
- [G2.device-cuda-handles][LOW] handles.hpp:481 — `static std::atomic_flag emitted = ATOMIC_FLAG_INIT;` uses the `ATOMIC_FLAG_INIT` macro, which is deprecated in C++20 (the project compiles `-std=c++20 -Wall -Wextra -Werror`; since C++20 `std::atomic_flag` is value-initialized to the clear state and the macro is no longer needed). GCC's default does not currently warn on it, but it is a deprecated-API forward-compat smell under warnings-as-errors. Suggested: replace with `static std::atomic_flag emitted;` (default-initialized = clear in C++20).

### G16
- [G16.device-cuda-handles][LOW] handles.hpp:268-271 — `MathModeScope` is documented "Move-only (so it can be returned/held)" but its move ctor (273-274) and move assign (276-283) leave the moved-from object with `prev_` copied (not reset). This is harmless because `restore()` gates on `h_ == nullptr` (set via `std::exchange`), so a moved-from scope is inert; noting only that the moved-from `prev_` is a stale value never read. Same benign pattern in `CusolverMathModeScope` (547-548, 550-557) where `prev_` and `promoted_` are likewise carried/left stale on the moved-from object. No defect — null-on-move is correctly enforced via `h_`. Suggested: none required; the `h_`-null guard is the real invariant.

## Notes (verified clean, not findings)
- G11 const/`__restrict__`/`__host__`/`__device__`: this is a host-side RAII header (no `__global__`/`__device__` kernels, no raw kernel pointer params); G11 does not apply. `get()`/`device_id()`/`promoted()` are correctly `[[nodiscard]] ... const noexcept`.
- G12-G15, G17-G22: no kernel launches, no indexing, no `cudaMalloc`/transfers, no shared memory, no atomics-vs-reduction tradeoffs in this header (the lone `std::atomic_flag` one-shot guard is correct and thread-safe per its stated M4.5 intent). The `set_workspace`/`set_stream` re-apply ordering (146, 159) correctly defends the cuBLAS §2.4.7 workspace-reset hazard.
- G13 error handling: every cuBLAS/cuSOLVER/CUDA call in ctors and setters is wrapped (`CUBLAS_CHECK`/`CUSOLVER_CHECK`/`STEPPE_CUDA_CHECK`); dtor/`restore`/`destroy` paths are `noexcept` and route nonzero status to `STEPPE_LOG_WARN` (never throw on unwind). Correct per architecture.md §7.
- G14/G16/G17 RAII: all four wrappers (`CublasHandle`, `CusolverDnHandle`, `GesvdjInfo`) and both math-mode scopes are move-only with copy deleted, `std::exchange`-based null-on-move, non-throwing freeing dtors, and matched create/destroy pairs (`cublasCreate`/`cublasDestroy`, `cusolverDnCreate`/`cusolverDnDestroy`, `cusolverDnCreateGesvdjInfo`/`cusolverDnDestroyGesvdjInfo`). Workspace `ws_` is correctly documented and treated as non-owning (cleared, not freed). `assert_on_creation_device()` is `noexcept` and its only body is `STEPPE_DEBUG_ONLY(... STEPPE_ASSERT ...)` (assert, not throwing CHECK), so the `noexcept` holds in both debug and NDEBUG.
- G3/G4/G5/G6/G7/G8/G9/G10: includes are all used (`<utility>` exchange, `<cstddef>` size_t, `<atomic>` atomic_flag, the three CUDA headers, the four internal headers). `device_id()` is non-dead public M4.5 scaffold (referenced in cuda_backend.cu:260). No magic numbers, no signed/unsigned or index-width issues (no large-index arithmetic here; P/M/n_block scale does not reach this header). The `STEPPE_HAVE_CUSOLVER_FP64_EMULATED` capability macro (84-88) is a single point of truth with documented forward-compat rationale; `-1` device-ordinal sentinels are documented defensive-never-observed. Comments are accurate and rationale-bearing, no stale/orphan TODO. Member initializers (224-232, 313-314, 400-401, 463, 612-614) are at declaration. Paren balance in the `STEPPE_DEBUG_ONLY`/`STEPPE_ASSERT` block (180-190) verified correct.
