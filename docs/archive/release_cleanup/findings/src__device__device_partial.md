# src__device__device_partial
Files: /home/suzunik/steppe/src/device/device_partial.hpp, /home/suzunik/steppe/src/device/cuda/device_partial.cu, /home/suzunik/steppe/src/device/cuda/device_partial_impl.cuh
Subsystem: device-cuda

## Findings

### G4
- [G4.type & numeric BUGS][LOW] device_partial.hpp:40-42, device_partial_impl.cuh:17-18 — The shape fields `P`, `n_block_local`, `b0` are `int`, and the Impl documents the resident buffers as `[P*P*n_block_local]`. At production scale (P~2500 -> P*P = 6.25M; times n_block ~757 -> ~4.7e9) the element extent exceeds 2^31, so any `int` (or `int*int*int`) product of these fields overflows before widening. The actual `P*P*n_block_local` product that sizes the `DeviceBuffer<double>` is formed in the CONSUMER (cuda_backend.cu, per the header comment at lines 19-22 and impl comment at line 17), not in this unit, and `DeviceBuffer`'s ctor takes a `std::size_t n` — so the overflow-relevant arithmetic lives outside these three files. Within this unit the only concern is that these `int` shape fields invite an `int`-domain product at the call site. Suggested: verify the cuda_backend.cu site widens (e.g. `std::size_t(P)*P*n_block_local`) before constructing the buffers; consider documenting the widening requirement at device_partial_impl.cuh:17-18 since that is where the `[P*P*n_block_local]` extent is named.

No further issues found (groups checked: G2-G3, G5-G10, G11-G22). G11-G22 device-specific checks (launch config, indexing, kernel error handling, syncthreads, memory access, occupancy) are N/A: this unit contains no kernels, launches, memcpy, or device allocation logic — it is an opaque CUDA-free handle (device_partial.hpp), its out-of-line defaulted special members (device_partial.cu), and the Impl struct holding two `DeviceBuffer<double>` members (device_partial_impl.cuh). Ownership/RAII (G16/G17) is correctly delegated to `DeviceBuffer` (move-only, non-throwing device-agnostic-free dtor); `DevicePartial` is properly move-only with deleted copy and a defaulted dtor instantiated against a complete `Impl` in the CUDA TU (device_partial.cu:6,10-13) — no double-free or incomplete-type-deletion trap.
