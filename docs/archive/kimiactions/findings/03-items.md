## Group 6 — comment-hygiene scrub (external reader)

- [6.D1][HIGH] include/steppe/config.hpp, src/device/backend.hpp, src/core/qpadm/model_search.cpp, src/device/cuda/qpgraph_fit_kernels.cu, src/core/internal/log.hpp, src/device/cuda/check.cuh, src/device/cuda/device_buffer.cuh, src/device/cuda/stream.hpp, src/device/cuda/handles.hpp — Comment-only pass purging ~252 ticket/milestone/spike provenance tokens, PUBLIC surface first (config.hpp + backend.hpp); strip the cleanup/ROADMAP/Mx/spike tag, keep the rationale; reword the G14 self-changelog one-liners and down-case the G15 charged vocab off non-§12 lines. Gate: grep. Suggested: see docs/kimiactions/03-low-polish.md §D1.

## Group 7 — tooling polish

- [7.A1][HIGH] cmake/SteppeOptions.cmake, cmake/SteppeWarnings.cmake, CMakeLists.txt, CMakePresets.json — Add option(STEPPE_WERROR ON) gating the three -Werror tokens + option(STEPPE_CCACHE) with find_program(ccache) compiler-launcher; pin WERROR ON in the ci preset. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §A1.
- [7.A2][HIGH] tests/CMakeLists.txt, CMakePresets.json — Add CTest LABELS (unit/reference/cli/python + orthogonal gpu/slow) across the 74-test tree via a steppe_add_test wrapper; add a host preset that excludes the gpu label (the ctest -LE gpu CI lane). Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §A2.
- [7.A3][MED] pyproject.toml, bindings/steppe/__init__.py, tests/python/conftest.py, tests/CMakeLists.txt — Add [tool.pytest.ini_options]/[tool.ruff]/[tool.mypy] + a lint extra to pyproject, a steppe._core mypy ignore-missing override, and ruff --fix the facade (bump line-length over mass-rewrite). Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §A3.
- [7.A4][MED] src/app/f2_dir_writer.cpp, src/app/f2_dir_writer.hpp, src/app/f2_dir_io.cpp, src/device/f2_disk_format.hpp — Stamp meta_schema_version:1 + pops_sha256 (sha256_file of pops.txt) into the writer's meta.json, add kF2MetaSchemaVersion (distinct from kF2DiskVersion), optional back-compat-tolerant reader check. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §A4.
- [7.A5][LOW] build_m0.sh, scripts/regenerate_goldens.sh, tests/reference/goldens/at2/scripts/*.R, tests/reference/goldens/at2/README.md, docs/architecture.md, tests/CMakeLists.txt, docs/RELEASE-SCOPE.md — Author one version-asserting scripts/regenerate_goldens.sh (preflight toolchain + convertf TGENO→PA + R generators in order, mandatory parity re-run reminder), delete build_m0.sh and scrub its architecture.md:115 / tests/CMakeLists.txt:12 refs. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §A5.
- [7.A6][MED] docs/architecture.md, src/core/qpadm/qpadm_fit.hpp, src/core/CMakeLists.txt — Decide the src/core/qpadm/ grab-bag misnomer (rename→fit/, split→qpgraph/+existing fstats/, or DEFER) and record it as an architecture.md module-map note + a qpadm_fit.hpp header pointer; DEFER-and-document is the XS floor. Gate: doc. Suggested: see docs/kimiactions/03-low-polish.md §A6.
- [7.A7][MED] include/steppe/config.hpp — Delete or demote-to-forward-reserved-note the dead DeviceConfig knobs stream_count/search_streams/use_mem_pool (+ kDefaultSearchStreams); strip the live-knob doc framing, keep the "stream_count forced to 1" §12 sentence. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §A7.
- [7.A8][LOW] src/core/CMakeLists.txt, src/device/CMakeLists.txt — Add target_compile_features(steppe_core PUBLIC cxx_std_20) + the same on steppe_device so the C++20 floor is explicit on the two STATIC compute libs (matching the existing 7 targets). Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §A8.

## Group 8 — robustness polish

- [8.B1][HIGH] cmake/SteppeOptions.cmake, src/device/CMakeLists.txt, src/core/internal/nvtx.hpp, src/device/cuda/cuda_backend.cu, src/device/cuda/dates_kernel.cu, src/device/cuda/qpfstats_kernel.cu — Wire NVTX behind STEPPE_NVTX: new CUDA-free nvtx.hpp RAII macro (empty when undefined), target_compile_definitions gate mirroring STEPPE_HAVE_EMU_TUNING, ~6-12 coarse phase-boundary markers (decode/f2 GEMM/jackknife/qpadm_fit), keep them out of hot inner loops. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §B1.
- [8.B2][MED] src/app/f2_dir_io.cpp — Forward std::error_code::message()/errno into the read_f2_dir fault strings (the dir-check at 62-65 plus the two ifstream opens at 41 and 71, capturing errno immediately) while keeping Status::InvalidConfig. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B2.
- [8.B3][MED] src/app/cmd_extract_f2.cpp — Wrap the geno_hash_thread lambda (292-294) in try/catch capturing a std::exception_ptr and degrade to empty geno_sha + source_hash_computed:false on throw, so an escaping bad_alloc can't call std::terminate. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B3.
- [8.B4][LOW] src/core/config/config_builder.hpp, src/core/config/config_builder.cpp — Drop mutable on ConfigBuilder::error_message_ by making build() non-const (no const ConfigBuilder caller exists); leave error_message() const. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §B4.
- [8.B5][LOW] src/device/cuda/cuda_backend.cu — Pool the per-call cuSOLVER potrf/potri/gesvd workspace (2099/2364/4044/4055/4200-4221) into persistent monotonic-grow CudaBackend members distinct from the f2 staging, and add a cap/assert on the pinned stage_f2_/stage_vpair_ growth; hygiene, not a measured win. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B5.
- [8.B6][HIGH] src/device/cuda/dates_kernel.cu, src/device/cuda/qpgraph_fit_kernels.cu, src/device/cuda/dstat_kernel.cu — Replace the 14 bare post-launch STEPPE_CUDA_CHECK(cudaGetLastError()) sites (dates×9, qpgraph_fit×3, dstat×2) with the canonical STEPPE_CUDA_CHECK_KERNEL(), leaving the check.cuh macro body untouched, so the compute-sanitizer pass keeps launch-site attribution. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B6.
- [8.B7][MED] src/device/backend.hpp, src/device/cuda/cuda_backend.cu, src/device/cpu/cpu_backend.cpp, src/core/qpadm/gls_solve.hpp — Delete the vestigial rank_test virtual: the throwing base decl (1692-1698), both backend overrides, and the zero-caller gls_solve.hpp free-function wrapper (17-20); the live rank_sweep/gls_weights path is untouched. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B7.
- [8.B8][LOW] src/device/cuda/qpgraph_fit_kernels.cu, src/device/cuda/qpgraph_fit_kernels.cuh, src/device/cuda/qpadm_fit_kernels.cuh, src/device/cuda/qpadm_fit_kernels.cu — Re-align the two device-convention deviators: qpgraph_fit_kernels namespace steppe::device::cuda→steppe::device + raw __host__ __device__→STEPPE_HD, and add the d_ prefix to qpadm_fit_kernels launch_assemble_* device-pointer params (f2→d_f2, vpair→d_vpair). Gate: grep. Suggested: see docs/kimiactions/03-low-polish.md §B8.
- [8.B9][LOW] src/device/cuda/f2_block_kernel.cu, src/device/cuda/f2_block_kernel.cuh, src/device/cuda/f2_blocks_kernel.cu, src/device/cuda/f2_blocks_kernel.cuh, src/device/CMakeLists.txt — git mv the confusable sibling pair (rename the batched f2_blocks_kernel → f2_batched_kernel, or the primitive → f2_single_block_kernel) and update the CMake source list + every dependent #include. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §B9.

## Group 9 — api / dx polish

- [9.C1][MED] include/steppe/config.hpp, bindings/module.cpp — Add inline static Precision factories fp64()/emulated_fp64(bits=kDefaultMantissaBits)/tf32() (struct stays a default-constructible aggregate) + a static_assert; optional separate-commit call-site migration preserving each mantissa value. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C1.
- [9.C2][MED] include/steppe/fstats.hpp, src/app/f2_dir_io.hpp — Add size_t-safe inline F2BlockTensor::f2_at/vpair_at(i,j,b) const+mutable accessors + a block(b) std::span over the i+P·j+P·P·b layout (the single canonical home for the convention); requires <span>. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C2.
- [9.C3][MED] bindings/module.cpp — Replace the f2_to_numpy element-by-element loop (1031-1032) with std::memcpy(buf, src.data(), n*sizeof(double)) (add <cstring>); serves both the f2 and vpair exports. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C3.
- [9.C4][LOW] examples/python/quickstart_qpadm.py, examples/cpp/quickstart_qpadm.cpp, examples/CMakeLists.txt, examples/README.md, CMakeLists.txt, cmake/SteppeOptions.cmake, src/app/cmd_qpadm.cpp, src/app/f2_dir_io.hpp — Create examples/ read_f2→qpadm→inspect quick-starts (trivial Python facade call + a C++ one mirroring run_qpadm_command) behind a new STEPPE_BUILD_EXAMPLES OFF option, pointed at a committed golden f2-dir, print-only. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §C4.
- [9.C5][LOW] docs/Doxyfile.in, cmake/Docs.cmake, CMakeLists.txt, cmake/SteppeOptions.cmake, include/steppe/qpadm.hpp, bindings/steppe/__init__.py — Add a Doxygen target over include/steppe (find_package(Doxygen) soft no-op, runs locally without CUDA) + a pdoc docs-python target over the facade, behind STEPPE_BUILD_DOCS; single-source PROJECT_NUMBER from project(VERSION). Gate: doc. Suggested: see docs/kimiactions/03-low-polish.md §C5.
- [9.C6][MED] bindings/module.cpp, src/core/config/config_builder.cpp, docs/cli-bindings.md, bindings/steppe/__init__.py — Add the emu32 branch to the Python parse_precision (170-178) so 32-bit emulation is selectable (mirror the CLI's emu32→{EmulatedFp64,32}), reconcile the CLI/Python/emit token spellings + aliases, and document the canonical set. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C6.
- [9.C7][LOW] bindings/steppe/__init__.py — Wrap dates() (855-881) in a typed DatesResult facade class matching the sibling result classes (typed attrs over the dict + optional curve DataFrame + __repr__), annotate -> DatesResult, keep a dict/.raw back-compat accessor. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C7.
- [9.C8][LOW] bindings/module.cpp, bindings/bind_qpadm.cpp, bindings/internal/bind_common.hpp, bindings/CMakeLists.txt — Split the 1170-LOC module.cpp into per-tool register_<tool>(py::module_&) TUs (qpadm/dates/fstats/qpgraph/f2handle) with shared helpers (parse_precision/f2_to_numpy/converters) in bind_common.hpp, reducing module.cpp to a thin assembler preserving registration order; coordinate the C3 memcpy change into the moved home. Gate: golden. Suggested: see docs/kimiactions/03-low-polish.md §C8.
- [9.C9][LOW] src/device/cuda/device_buffer.cuh, src/device/cuda/pinned_buffer.cuh — Add a C++20 requires std::is_trivially_copyable_v<T> constraint to the DeviceBuffer and PinnedBuffer class templates (+ <type_traits>) so the raw-byte cudaMemcpy contract is compiler-enforced. Gate: build. Suggested: see docs/kimiactions/03-low-polish.md §C9.
