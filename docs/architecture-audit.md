# architecture.md ↔ codebase alignment audit

Read-only audit — one agent per spec section (§0–§18) checked whether the code still matches the section AND whether code comments citing the section still line up. Evidence is `file:line`.

**114 findings** over 19 sections — **30 high**, 47 medium, 37 low.

## Section summary

| § | section | code | comments | findings |
|--:|---|---|---|--:|
| 0 | Purpose & how to use this document | aligned | partial | 2 |
| 1 | Project vision & non-goals | partial | none | 2 |
| 2 | Engineering principles | partial | partial | 6 |
| 3 | "Tech stack & pinned versions" | stale | partial | 8 |
| 4 | "Repository layout" (docs/archive/architecture.md, l | partial | aligned | 9 |
| 5 | "Layered architecture mapped onto the qpAdm pipeline | partial | partial | 3 |
| 6 | Build system | partial | partial | 9 |
| 7 | CUDA coding standards & idioms (lines 454-565) | partial | aligned | 4 |
| 8 | DRY &amp; shared utilities | partial | partial | 8 |
| 9 | Configuration management | partial | partial | 7 |
| 10 | Error handling, logging &amp; observability (lines 6 | partial | partial | 7 |
| 11 | Memory &amp; performance strategy (docs/archive/arch | partial | aligned | 3 |
| 12 | Numerical correctness &amp; reproducibility (lines 7 | partial | partial | 5 |
| 13 | "Testing strategy" (docs/archive/architecture.md lin | partial | aligned | 5 |
| 14 | CI/CD &amp; tooling | stale | none | 6 |
| 15 | Python bindings & packaging | partial | aligned | 4 |
| 16 | Documentation & ABI/versioning | partial | aligned | 6 |
| 17 | Scaffold manifest (ordered) (docs/archive/architectu | stale | aligned | 14 |
| 18 | Definition of Done / quality bar (lines 971-988) | partial | partial | 6 |

## High-severity (fix first)

- **§1** · _spec-vs-code_ — §1's 'It is NOT' bullet (architecture.md line 46) carries a STALE-correction bracket asserting: '[STALE vs as-built — the shipped decoder is TGENO-only (src/io/geno_reader.cpp throws a clear error on GENO/PACKEDANCESTRYMAP); PLINK / EIGENSTRAT / PACKEDANCESTRYMAP readers ... are PLANNED Phase-2 work, not current. Transcode with convertf first.]'. This is now false: the code implements and wires up FIVE formats. src/io/geno_reader.cpp auto-detects TGENO/GENO/EIGENSTRAT/PLINK/ANCESTRYMAP in the ctor (lines 283-349) and provides dedicated gathers read_snp_major_tile (line 612, GENO/PACKEDANCESTRYMAP), read_eigenstrat_snp_major_tile (line 692), read_plink_snp_major_tile (line 795), read_ancestrymap_snp_major_tile (line 895). The old throw the bracket cites is gone (geno_reader.cpp line 81 is now inside parse_eigenstrat_geometry). src/core/stats/read_canonical_tile.cpp dispatches all five (switch at lines 63-112). This is not dead code: it is reached from the real f-stat/qpAdm/f2 precompute via read_genotype_front_end (qpfstats.cpp:236, app/extract_f2_core.cpp:105, dstat.cpp:134, dates.cpp:136). Even the cross-ref the bracket itself cites, RELEASE-SCOPE.md §2 #10, now reads 'RESOLVED (Phase-2 format readers, main @ e4a5387) ... geno_reader.cpp:81 no longer throws' (docs/archive/RELEASE-SCOPE.md line 35). The section actively tells a reader the code cannot read PLINK/EIGENSTRAT/PACKEDANCESTRYMAP and to 'Transcode with convertf first', when the code reads them directly. [CONFIRMED]
  - **evidence:** docs/archive/architecture.md:46 (STALE bracket) vs src/io/geno_reader.cpp:283-349,612,692,795,895 + src/core/stats/read_canonical_tile.cpp:63-112 + src/core/stats/qpfstats.cpp:236 + docs/archive/RELEASE-SCOPE.md:35

- **§3** · _spec-vs-code_ — Section's foundational claim (architecture.md:79) that '`third_party/CMakeLists.txt` is the single place FetchContent_Declare/CPM pins live' is DIVERGENT. No `third_party/` directory exists anywhere in the repo. The CPM machinery lives at cmake/CPM.cmake, and dependency pins are DISTRIBUTED per-subtree, not centralized: CLI11 pinned in src/app/CMakeLists.txt:24-28, nanobind in bindings/CMakeLists.txt:47-48. Each uses find_package-first / CPM-fallback (not FetchContent), scoped PRIVATE to its subtree — the opposite of a single central FetchContent file.
  - **evidence:** `find third_party` => empty; cmake/CPM.cmake; src/app/CMakeLists.txt:17-28 and bindings/CMakeLists.txt:30-48 (distributed find_package-then-CPM pins); contradicts architecture.md:79

- **§3** · _spec-vs-code_ — Row 'sccache >= 0.8' (architecture.md:99) is DIVERGENT: the build's compiler cache is ccache, not sccache. CMakeLists.txt:71-85 wires CMAKE_{CXX,CUDA}_COMPILER_LAUNCHER to find_program(CCACHE_PROGRAM ccache) behind option STEPPE_CCACHE (cmake/SteppeOptions.cmake:61). Grep for 'sccache' across build files returns nothing.
  - **evidence:** CMakeLists.txt:71-85 (ccache launcher); cmake/SteppeOptions.cmake:56-61 (STEPPE_CCACHE); no 'sccache' hits in cmake/CMakeLists/toml; contradicts architecture.md:99

- **§3** · _spec-vs-code_ — Row 'Eigen 3.4.x | Host-only small dense LA in the CPU reference backend' (architecture.md:92) is DIVERGENT. No Eigen library is present or linked: no `#include <Eigen>`, no find_package(Eigen), no Eigen3:: anywhere. The CPU reference backend (src/device/cpu/cpu_backend.cpp, 'REFERENCE backend: scalar host oracle') uses a hand-rolled dense-LA header src/core/internal/small_linalg.hpp, not Eigen.
  - **evidence:** grep '#include <Eigen>|find_package(Eigen)|Eigen3::' => empty; src/device/CMakeLists.txt:51 (cpu_backend.cpp); src/core/internal/small_linalg.hpp; contradicts architecture.md:92

- **§4** · _spec-vs-code_ — The STATE preamble declares that 'Still planned (step 2+): app/ CLI, Python bindings, standalone f3/f4/D-stat entry points' — all three are now fully BUILT (with public API + tests), so the section's milestone snapshot is stale. It even self-contradicts: the same tree marks app/ [BUILT, P3] (line 219) and bindings/ [BUILT, M(py-1)] (line 221).
  - **evidence:** Spec docs/archive/architecture.md:111 says these are planned. Reality: src/app/ has main.cpp + full cmd_*.cpp CLI; bindings/ has module.cpp + bind_qpadm/fstats/qpgraph/dates/f2handle.cpp; standalone entry points exist as public headers include/steppe/f3.hpp (run_f3), f4.hpp (run_f4), f4ratio.hpp (run_f4ratio), dstat.hpp (run_dstat), fstat_sweep.hpp (run_f4_sweep/run_f3_sweep), implemented in src/core/qpadm/{f3,f4,f4ratio,fstat_sweep}.cpp + src/core/stats/dstat.cpp, with tests/cli/test_cli_{f3,f4,f4ratio,qpdstat}.cpp and tests/python/test_py_{f3,f4,f4ratio,dstat_geno}.py.

- **§4** · _spec-vs-code_ — The fstats.hpp tree annotation claims 'Standalone f3/f4/D-stat entry points still planned (P2); the f4 math is currently INTERNAL to the fit (qpadm.hpp)'. This is stale — the f4/f3/D-stat math is now exposed as first-class public CUDA-free entry points. The mirrored NOTE under fstats/ (lines 157-158, 'standalone f3/f4/D-stat contractions are still planned (P2)') is stale for the same reason.
  - **evidence:** Spec docs/archive/architecture.md:132-133 and 157-158. Reality: include/steppe/f4.hpp:70 '[[nodiscard]] F4Result run_f4(const device::DeviceF2Blocks&...)', include/steppe/f3.hpp:70 'run_f3', include/steppe/dstat.hpp:83 'run_dstat'; f3.hpp:1 header explicitly self-describes as 'PUBLIC, CUDA-FREE standalone f3-statistic entry point'.

- **§5** · _spec-vs-code_ — §5 line 295 claims the CLI, Python bindings, and standalone f3/f4/D-stat entry points are NOT yet built: 'NO CLI, NO Python bindings, NO standalone f3/f4/D-stat entry points (no app/ or bindings/ dir, no nanobind/CLI11 in CMake) — these stay (planned) below.' Every clause is now false. This whole 'honest NEXT' paragraph is stale.
  - **evidence:** bindings/ dir exists as the nanobind module steppe._core (M(py-1)): bindings/CMakeLists.txt:1 + nanobind_add_module(_core ...) at :64, with bind_qpadm.cpp/bind_dates.cpp/bind_fstats.cpp/bind_qpgraph.cpp/bind_f2handle.cpp/module.cpp. src/app/ is a full CLI11 app (src/app/CMakeLists.txt:17 find_package(CLI11 2.4); src/app/main.cpp, cmd_qpadm.cpp, cmd_qpdstat.cpp, cmd_extract_f2.cpp). Standalone stat entry points exist: include/steppe/f3.hpp, f4.hpp, dstat.hpp, extract.hpp plus src/app/cmd_f3.cpp/cmd_f4.cpp and src/core/qpadm/f3.cpp/f4.cpp.

- **§6** · _spec-vs-code_ — The entire 'Dependency fetching (CPM)' block is stale/never-existed. Spec lines 402-414 say 'third_party/CMakeLists.txt — ALL pins here' and show find_package(CCCL) + CPM fetches of CCCL 3.1 (the 'determinism API' floor), Eigen3 3.4.0, spdlog 1.14.1, CLI11 2.4.2, googletest 1.15.2. Reality: there is NO third_party/ dir (ls fails), and CCCL / Eigen3 / spdlog appear NOWHERE in the build (grep across all *.cmake/CMakeLists.txt returns nothing). The only CPM fetches are CLI11 (src/app, behind STEPPE_BUILD_CLI) and nanobind (bindings, behind STEPPE_BUILD_PYTHON); GoogleTest is located via find_package(GTest CONFIG) with a self-checking-executable fallback, never CPM-fetched. The section even contradicts itself: its own top-level snippet at spec lines 331-332 says 'no third_party/ dir: deps are bootstrapped via cmake/CPM.cmake inside the opt-in src/app + bindings subtrees' — which is what the code actually does.
  - **evidence:** no third_party/ (ls fails); cmake/CPM.cmake:11-16 ('included only from src/app ... and bindings/'); src/app/CMakeLists.txt:17-30 (CLI11 find_package+CPM v2.4.2); bindings/CMakeLists.txt:30-51 (nanobind find_package+CPM v2.4.0); tests/CMakeLists.txt:14-22 (GTest find_package + self-check fallback) — contradicts architecture.md:402-414

- **§6** · _spec-vs-code_ — Compiler launcher is ccache, not sccache, and cmake/CompilerLauncher.cmake does not exist. Spec line 329 does include(cmake/CompilerLauncher.cmake); spec preset 'base' (lines 430-431) sets CMAKE_CUDA_COMPILER_LAUNCHER / CMAKE_CXX_COMPILER_LAUNCHER = sccache; spec prose line 450 says 'sccache caches host TUs reliably; nvcc caching is best-effort'. Reality: no cmake/CompilerLauncher.cmake (ls fails); the string 'sccache' occurs nowhere in the repo (grep empty); the launcher is ccache, wired inline in the top-level CMakeLists behind STEPPE_CCACHE (default ON) via find_program(ccache); and CMakePresets.json sets no compiler launcher at all.
  - **evidence:** no cmake/CompilerLauncher.cmake (ls fails); grep 'sccache' -> empty; CMakeLists.txt:71-85 (ccache via STEPPE_CCACHE) + cmake/SteppeOptions.cmake:56-61; CMakePresets.json:6-15 (base preset, no launcher) — contradicts architecture.md:329,430-431,450

- **§7** · _spec-vs-code_ — The entire "Async memory pools" mechanism and the third allocation home `allocator.cu` do not exist. §7 states application memory is allocated "only inside DeviceBuffer<T> / PinnedBuffer<T> / allocator.cu (pool)" (line 456) and that "A pool-backed allocator variant takes a cudaStream_t and uses cudaMallocAsync/cudaFreeAsync (both inside allocator.cu, allowlisted); set the pool release threshold high (cudaMemPoolAttrReleaseThreshold = UINT64_MAX)" (line 558). No such file or code path exists: the real allocation homes are DeviceBuffer (synchronous cudaMalloc, device_buffer.cuh:83) and PinnedBuffer (cudaHostAlloc, pinned_buffer.cuh:104). The device path never uses stream-ordered pools.
  - **evidence:** `find` for allocator* (excl docs) returns nothing. grep of src/include for cudaMallocAsync/cudaFreeAsync/cudaMemPoolAttrReleaseThreshold matches ONLY comments (device_buffer.cuh:143-144 a 'future switch' warning, cuda_backend.cu:58, config.hpp:422). include/steppe/config.hpp:420-425 marks `use_mem_pool` "FORWARD-RESERVED (NOT wired) ... gates no compute" while its own comment claims the device path uses the cudaMallocAsync allocator "UNCONDITIONALLY" — self-contradictory. The DRY grep-gate allowlist comments (device_buffer.cuh:6, pinned_buffer.cuh:7) list `allocator.cu` as one of three permitted TUs, but only two of the three exist.

- **§8** · _spec-vs-code_ — §8 table names `internal/expected.hpp` as the single home for internal error propagation, exposing `STEPPE_TRY(expr)` over `std::expected<T,Error>`. No such file exists anywhere in the repo, and `STEPPE_TRY` appears in zero source/header files. Internal error propagation is actually done via `std::expected`-based BuildResult in src/core/config/build_result.hpp + config_builder.hpp, with no shared expected.hpp/STEPPE_TRY facade.
  - **evidence:** architecture.md:593 claims `internal/expected.hpp` + `STEPPE_TRY`; `find -name 'expected*.hpp'` = empty; `grep -rn STEPPE_TRY src include` = empty; std::expected lives in src/core/config/build_result.hpp, config_builder.hpp

- **§8** · _spec-vs-code_ — §8 table names `internal/type_traits.hpp` as the single home for precision/traits, exposing `real_t<P>`, `is_fp64`, and index aliases ('the precision switch lives here'). No `type_traits.hpp` file exists and neither `real_t` nor `is_fp64` is defined anywhere in src/include.
  - **evidence:** architecture.md:598 claims `internal/type_traits.hpp` + `real_t<P>`/`is_fp64`; `find -name 'type_traits*.hpp'` = empty; `grep real_t|is_fp64 src include` = empty

- **§9** · _spec-vs-code_ — §9 claims ConfigBuilder::build() itself validates the device-memory budget and other device-dependent constraints. The real build() is CUDA-free and does STATIC token/range validation only; the VRAM budget, precision-honorability, the deterministic+EmulatedFp64 workspace rule, the deterministic+stream_count>1 rule, and device-presence are NOT checked there — they live in the device/app layer at run time (device/vram_budget.hpp, device/resources.cpp build_resources), a split the code documents explicitly.
  - **evidence:** Spec L609 '.build() validates once — including the device-memory budget (§11.2)' and L692 build() 'rejects ... any config whose estimated peak VRAM exceeds device free memory (§11.2)'; 'a precision the selected backend cannot honor'; 'deterministic == true with precision == EmulatedFp64 unless an explicit cublasSetWorkspace workspace is configured'; 'deterministic == true with stream_count > 1'. Reality: config_builder.cpp:210-354 (only device-ordinal token/precision/format/range checks); config_builder.cpp:206-207 'the LIVE device/VRAM probe is the app's job through build_resources ... not this CUDA-free layer'; config_builder.hpp:26-31; §11.2 budget lives in device/vram_budget.hpp:59-172.

- **§9** · _spec-vs-code_ — §9 describes a resampling model that does not exist: enum JackknifeMode{Delete1,Bootstrap}, a RunConfig::resampling() accessor, and RunConfig::bootstrap_reps() ('0 ⇒ delete-1 jackknife'). The real jackknife enum is JackknifePolicy{None,FeasibleOnly,All} — a policy over WHICH models pay for the (always delete-1/LOO) SE, not a delete-1-vs-bootstrap choice. There is no Bootstrap mode, no bootstrap_reps, and no resampling() accessor anywhere.
  - **evidence:** Spec L628 'enum class JackknifeMode { Delete1, Bootstrap };', L654 'JackknifeMode resampling()', L655 'int bootstrap_reps(); // 0 ⇒ delete-1 jackknife'. Reality: include/steppe/qpadm.hpp:54-58 'enum class JackknifePolicy : int { None=0, FeasibleOnly=1, All=2 }'; config_builder.cpp:349-352 maps --jackknife 0|1|2 to JackknifePolicy; grep for bootstrap_reps/resampling()/seed() on RunConfig returns nothing.

- **§10** · _spec-vs-code_ — §10 (architecture.md:715) asserts, present tense, that `internal/log.hpp` "wraps spdlog behind STEPPE_LOG_* so sinks/levels/async are swappable; levels and sinks come from RunConfig, not globals" and that "The Python binding installs a sink forwarding to Python's logging." None of this is built. src/core/internal/log.hpp:44-54 is a bare printf-style `std::fprintf(stderr, "[steppe][warn] " ...)` sink; only ONE level (STEPPE_LOG_WARN) is realized and it is compiled out entirely under NDEBUG. The header itself states (lines 16-17) the fuller levels are "reserved for the structured-logging backend." spdlog appears nowhere in src/ (grep hits only docs). There is no RunConfig-driven sink/level wiring, and no Python logging forwarder in bindings/ (grep for logging/sink across bindings/ returns nothing).
  - **evidence:** src/core/internal/log.hpp:44-54 (fprintf sink, WARN-only, NDEBUG-stripped); log.hpp:16-17 spdlog "reserved"; grep spdlog -> only docs/archive/architecture.md; bindings/ has no logging sink. Contradicts architecture.md:715.

- **§10** · _spec-vs-code_ — §10 observability (architecture.md:717) claims the NVTX helper emits "named, colored ranges" with a color palette "nvtx::Color ... defined once in that header — no per-call-site color literals," and the code example (architecture.md:721) calls `STEPPE_NVTX_SCOPE("f2_block_kernel", nvtx::Color::Green)`. The real macro is `STEPPE_NVTX_RANGE(name)` — a SINGLE string-literal argument with NO color (src/core/internal/nvtx.hpp:47-48, 52). There is no `nvtx::Color` type anywhere (grep for nvtx::Color / Color:: returns nothing), and no color palette. All 8 call sites use STEPPE_NVTX_RANGE(name-only). Both the macro name and the entire color-palette concept in §10 are divergent.
  - **evidence:** src/core/internal/nvtx.hpp:47-52 defines STEPPE_NVTX_RANGE(name) (no color); grep nvtx::Color -> empty; call sites e.g. cuda_backend_f2_blocks.cu:380 STEPPE_NVTX_RANGE("f2_gemm"). Contradicts architecture.md:717 and :721.

- **§11** · _spec-vs-code_ — docs/archive/architecture.md:768 (§11.2, plus the §11.2 header 'validated at build()' and §11.1:751). Spec: 'ConfigBuilder::build() sums the resident terms for the configured K/T, compares against cudaMemGetInfo free memory ... rejects over-budget configs up front (STEPPE_ERR_DEVICE_OOM) ... The single expression validated is total_vram(P, B, m, K, T) <= budget * free.' No such build()-time VRAM gate exists. ConfigBuilder::build() DOES exist but is CUDA-FREE BY CONTRACT: it does only STATIC validation (token legality, ranges, GPU-only) and returns Status::InvalidConfig, never a cudaMemGetInfo sum or DeviceOom. No total_vram(P,B,m,K,T) function exists (only the total_vram_bytes capability field), and the m^2 (Q), m*B (X), and K-batch table terms are summed nowhere. The actual DeviceOom is produced MID-RUN by mapping cudaErrorMemoryAllocation, or by select_output_tier when a streamed chunk cannot fit — not by an up-front build() rejection.
  - **evidence:** config_builder.hpp:25-31 ('CUDA-FREE BY CONTRACT ... makes no CUDA call ... device/VRAM check ... is the APP's job at run time'), :106 build()->BuildResult<RunConfig> (InvalidConfig only); resources.hpp:194 'until ConfigBuilder::build() exists.'; block_partition_rule.hpp:64 'if/when it lands.' and :189 'as ConfigBuilder::build() does not exist.'; block_partition_rule.cpp:109 same; mid-run OOM mapping cuda_backend.cu:289-308 (cudaErrorMemoryAllocation -> Status::DeviceOom); grep: no total_vram(...) function.

- **§12** · _spec-vs-code_ — The §12 cuSOLVER-scope bullet cites `large_svd_V` in `cuda_backend.cu` and the orientation rule at `cuda_backend.cu:4221-4226`. That file was split by the cuda-backend-split refactor and is now only 314 lines total, so lines 4221-4226 do not exist. `large_svd_V` and the `rows=max(nl,nr) >= cols=min(nl,nr)` orientation rule now live in `cuda_backend_qpadm_fit.cu` (orientation at :318 / :351 / :378, gesvdj/gesvd dispatch at :311-404). The described behavior is correct and present, but the file:line citation is stale.
  - **evidence:** wc -l cuda_backend.cu = 314; grep found large_svd_V + orientation only in src/device/cuda/cuda_backend_qpadm_fit.cu:318 (`rows>=cols ✓ (the §1.3 orientation)`), :351, :378; spec line 808 says `cuda_backend.cu:4221-4226`.

- **§13** · _spec-vs-code_ — The section cites the regression-golden directory as `tests/golden/`, but no such directory exists. The ADMIXTOOLS 2 est/se/z/p regression goldens with the pinned-environment metadata actually live under `tests/reference/goldens/at2/` (e.g. golden_fit0.json, golden_fit1_NRBIG.json, golden_fitNA.json, golden_qpwave.json). There is no top-level tests/golden split from tests/reference — regression goldens are nested inside the reference tree. The only occurrences of the `tests/golden/` path in the repo are in docs (architecture.md itself and docs/archive/design/fit-engine.md, which describe an aspirational tests/golden/f2/... and tests/golden/qpadm/... layout that was never created).
  - **evidence:** docs/archive/architecture.md:834 says '**Regression (`tests/golden/`).**'; `ls tests/golden` -> 'No such file or directory'; actual goldens: tests/reference/goldens/at2/golden_fit0.json etc.; README at tests/reference/goldens/at2/README.md:1-6 ties them to §12/§13.

- **§14** · _spec-vs-code_ — The entire CI pipeline described in §14 has no implementation in the repo. Spec lines 845-868 describe a 5-stage GitHub Actions pipeline (PR no-GPU ubuntu-latest lane, PR GPU runner, PR scoped compute-sanitizer lane, nightly sanitizer/nvbench sweep, on-demand wheels), a self-hosted GPU runner matrix YAML (lines 854-864), sccache with the GHA backend SCCACHE_GHA_ENABLED=true (line 866), and format/tidy/IWYU gates (line 868). None of it exists: there is no .github/ directory and no *.yml/*.yaml file anywhere in the tree (outside .git).
  - **evidence:** `ls .github` -> 'No such file or directory'; `find . -path ./.git -prune -o \( -name '*.yml' -o -name '*.yaml' \) -print` returns nothing. The only build automation present is CMake/CMakePresets + pyproject; even the repo's own pyproject.toml:56-58 calls the lint lane 'the future CUDA-free CI lane'.

- **§14** · _spec-vs-code_ — The §14 wheel dependency model is DELIBERATELY REVERSED by the actual pyproject.toml. Spec line 872 says to 'declare a runtime dependency on NVIDIA's pip packages' and line 881 says 'Pin the unsuffixed runtime packages in pyproject.toml [project.dependencies]' and 'The dependency must be declared, or the wheel is broken at import.' The real pyproject.toml declares numpy as the ONLY hard dep (42-44), explicitly documents the CUDA-13 runtime as a SYSTEM requirement resolved at load and NOT a pip dep (lines 7-18, 38-41), and the only NVIDIA pins are the SUFFIXED nvidia-*-cu13 packages inside an OPTIONAL `cuda` extra (65-70) — not unsuffixed, not in [project.dependencies]. The file even states pinning the cu13/unsuffixed wheels 'makes the wheel pip-uninstallable' because they are not yet published with a real >=13 version (lines 12-16).
  - **evidence:** pyproject.toml:42-44 (`dependencies = ["numpy>=1.22"]`), :59-70 (optional `cuda` extra with suffixed nvidia-*-cu13), :7-18 header note explicitly rejecting the pip-dep model. Contradicts architecture.md:872 and :881.

- **§14** · _spec-vs-code_ — The auditwheel-repair exclude list in §14 names the wrong CUDA library set, and no cibuildwheel/auditwheel config exists at all. Spec lines 875-876 exclude libcudart.so.13, libcublas.so.13, libcusolver.so.13, libcurand.so.13. The actual link set is libcudart.so.13, libcublas.so.13, libcusolver.so.12, libcufft.so.12: (a) curand is NOT linked anywhere, (b) cusolver's SONAME major is 12, not 13, and (c) cufft (the DATES autocorrelation LD engine) IS linked but is absent from the spec's exclude list. There is also no cibuildwheel over scikit-build-core wiring (no CIBW_*, no CIBW_MANYLINUX_X86_64_IMAGE, no auditwheel invocation) despite the section header claiming '(verified)'.
  - **evidence:** src/device/CMakeLists.txt:73 links `CUDA::cudart CUDA::cublas CUDA::cusolver CUDA::cufft` (no curand); README.md:26 and pyproject.toml:8-9 both state DT_NEEDED = libcudart.so.13 / libcublas.so.13 / libcusolver.so.12 / libcufft.so.12; grep for cibuildwheel|auditwheel|CIBW_|manylinux outside docs/ finds only one comment ('a future auditwheel-repair', pyproject.toml:63).

- **§15** · _spec-vs-code_ — §15 (line 889) claims: "Because the backend is injected and `CpuBackend` always exists, `import steppe` and smoke tests pass on GPU-free CI runners and laptops; `pytest` asserts GPU-vs-CPU equivalence wherever a device is present." The current bindings wire NO CpuBackend at all — `grep CpuBackend bindings/` returns NONE. Instead the binding is a hard GPU-product fail-fast: ensure_resources() raises `raise_no_device()` ("no CUDA device available (steppe is a GPU product...)") when no GPU is found. The Python suite does NOT assert GPU-vs-CPU equivalence; it asserts GPU output against committed AT2 CSV/binary goldens (rtol 1e-6 / 1e-12) and SKIPS cleanly when no CUDA device is visible (maybe_skip_no_gpu). So the CpuBackend-fallback mechanism the section credits for CI/laptop passing does not exist in the wheel.
  - **evidence:** bindings/internal/bind_common.hpp:90-113 (raise_no_device / ensure_resources fail-fast); grep 'CpuBackend' over bindings/ => NONE; tests/python/conftest.py:161-166 (maybe_skip_no_gpu skips on no device); tests/python/test_py_f4.py:2-13 (compares to AT2 golden, not CPU); contradicts architecture.md:889

- **§15** · _spec-vs-code_ — The §15 footnote (line 930) states "a CPU-only wheel variant omits them [CUDA runtime deps] (the loader resolves the CUDA libs lazily and `CpuBackend` needs none)." The real packaging has no such variant: pyproject.toml's header comment declares "ONE GPU wheel, NO CPU-only variant (memory cpu-is-test-only): steppe is a GPU product." There is a single GPU wheel; the CPU backend is test-only and never shipped as a wheel flavor.
  - **evidence:** pyproject.toml:3 ("ONE GPU wheel, NO CPU-only variant ... steppe is a GPU product"); contradicts architecture.md:930

- **§16** · _spec-vs-code_ — §16 opens with a present-tense claim that ADRs live in docs/adr/ in Michael Nygard format with an "Initial set: 0000 ... 0011" (12 records). No such directory or files exist anywhere in the repo.
  - **evidence:** architecture.md:936 lists ADRs 0000-0011 in docs/adr/. `find /home/suzunik/steppe/docs/adr` => 'No such file or directory'; no 0000-0011 ADR files exist outside docs/archive. docs/ contains only archive/, cheatsheet.md, commands.md, commentdump/, examples/, featuredesign/.

- **§17** · _spec-vs-code_ — Step 10 (io leaf) names files that do not exist and whole subdirs that were never created. Spec: `reader.hpp`, `plink_bed.cpp`, `merge/{merge_plan,allele_harmonize}.cpp`, `filter/{snp_filter,sample_filter,prepass}.cpp`, `impute/missing_policy.cpp`, `genetic_positions.cpp`, `precomputed_f2.cpp`. Actual src/io/ has geno_reader/snp_reader/ind_reader/plink_reader/genotype_source/eigenstrat_format, filter/{include_exclude,mind_prepass,snp_filter}. There is NO merge/ or impute/ subdir, no reader.hpp, no plink_bed.cpp, no sample_filter.cpp, no prepass.cpp (it is mind_prepass.cpp), no genetic_positions.cpp, no precomputed_f2.cpp. A repo-wide grep for allele_harmonize/missing_policy/merge_plan/imputation returns zero hits in src — the merge/impute preprocessing named here appears unimplemented.
  - **evidence:** docs/archive/architecture.md:961 vs `find src/io -type f` (no merge/, no impute/, filter has include_exclude/mind_prepass/snp_filter); grep for allele_harmonize|missing_policy|merge_plan|imputation in src/include = 0 hits

- **§17** · _spec-vs-code_ — Step 4 names `third_party/CMakeLists.txt` as the single place pinning CCCL/NCCL/Eigen/spdlog/CLI11/GoogleTest/nvbench/nanobind. There is NO third_party/ directory at all. Dependencies are declared per-target instead: CUDAToolkit (CCCL/NCCL) at top-level CMakeLists.txt:60, CLI11 in src/app/CMakeLists.txt:17, GoogleTest in tests/CMakeLists.txt:14, nanobind in bindings/CMakeLists.txt:30-50, CPM shim in cmake/CPM.cmake.
  - **evidence:** docs/archive/architecture.md:955 vs `ls third_party` -> 'No such file or directory'

- **§17** · _spec-vs-code_ — Step 6 names two internal DRY primitives that do not exist anywhere. `expected.hpp` (`STEPPE_TRY`, internal-only): no such file and grep for STEPPE_TRY / expected.hpp across src+include returns zero hits — the internal error-propagation macro described here is entirely absent. `type_traits.hpp` (`real_t<P>`): no such file and grep for real_t returns zero hits.
  - **evidence:** docs/archive/architecture.md:957 vs `ls src/core/internal/type_traits.hpp`->missing; grep STEPPE_TRY|expected.hpp = 0; grep real_t = 0

- **§17** · _spec-vs-code_ — Step 15 (CI) claims `.github/workflows/` with lint/build-test/sanitizer/nightly jobs. There is NO .github/ directory in the repo at all.
  - **evidence:** docs/archive/architecture.md:966 vs `ls .github` -> 'No such file or directory'

- **§17** · _spec-vs-code_ — Step 16 claims 'seed docs/adr/0000–0011' and 'copy this document to docs/architecture.md'. There is NO docs/adr/ directory (ADRs not seeded), and no docs/architecture.md — the document lives only at docs/archive/architecture.md. (§16 line 944 also claims the mirror at docs/architecture.md.)
  - **evidence:** docs/archive/architecture.md:967 vs `ls docs/adr`->missing and `ls docs/architecture.md`->missing (docs top-level: archive/, cheatsheet.md, commands.md, commentdump/, examples/, featuredesign/)


## Per-section detail

### §0 — Purpose & how to use this document
code: **aligned** · comments: **partial**

§0 is mostly meta/philosophy, but its 'Assumptions & chosen defaults' table carries concrete, code-verifiable claims. All of those check out against the tree: the CMake 3.28 floor (CMakeLists.txt:14), the ComputeBackend seam (src/device/backend.hpp), the M5 DeviceF2Blocks + opt-in .to_host() + host F2BlockTensor (src/device/device_f2_blocks.hpp:30/74, include/steppe/fstats.hpp:48), EmulatedFp64{40}, and TODO(multigpu-host-bounce). The one stale item — the '13.1+ determinism-API' CUDA floor that the build does not actually enforce (CMakeLists.txt:60 has no version constraint) — is already explicitly marked [STALE] inline with a correct pointer to RELEASE-SCOPE.md §2 #5, so the section is accurate as written (code_alignment=aligned). comment_alignment is partial: two shipped comments (src/device/backend.hpp:552 and tests/reference/test_backend_capabilities_probe.cu:147) cite 'architecture.md §0' for the 'one sm_120 build serves both boxes' rationale, but that content is not in §0 at all — it belongs to §11.4 (architecture.md:786). Other §0 citations (include/steppe/fstats.hpp:11 for the cacheable interchange-artifact concept) are supported by the section text.

- `[medium/comment-vs-spec]` Two shipped code comments cite architecture.md §0 as the source for the claim that the project ships ONE sm_120 build serving 'both boxes', but §0 (lines 3-34) says nothing of the kind. §0 only mentions the 'consumer 5090s' (line 29) and 'box toolkit 13.0.88' (line 18); it never discusses a single sm_120 build covering two hardware tiers. That two-box / one-build capability-tiering concept actually lives in §11.4 (architecture.md:786: 'the budget consumer box' vs 'RTX PRO 6000 stock driver'). So the §0 citation is a wrong-section reference — a reader following it to §0 finds no supporting text.
  - src/device/backend.hpp:552 'One build (sm_120) serves both boxes (architecture.md §0)'; tests/reference/test_backend_capabilities_probe.cu:147 'ships ONE sm_120 build for both boxes (architecture.md §0)'. §0 has no 'both boxes'/'one build' text (grep over lines 3-34 hits only line 18 '13.0.88' and line 29 'consumer 5090s'); the concept is in architecture.md:786 (§11.4).
- `[low/spec-vs-code]` The CUDA Toolkit row's headline value still reads '13.x (13.1+ for the CCCL-3.1 determinism API)', a floor the build does not enforce: CMakeLists.txt:60 is find_package(CUDAToolkit REQUIRED) with no version constraint at all (not even 13.0). This is NOT a live defect in the section, however — §0 already carries an inline [STALE] annotation stating the actual floor is CUDA 13 (green on 13.0.88), that the determinism-API floor was not enforced, and pointing to RELEASE-SCOPE.md §2 #5. The section is therefore self-corrected and accurate as written; noting only because the un-annotated headline still shows 13.1. (Minor knock-on: the referenced RELEASE-SCOPE.md §2 #5 cites CMakeLists.txt:56 for that find_package line, which is actually at line 60 — a stale line-ref in the referenced doc, not in §0 itself.)
  - docs/archive/architecture.md:18 (CUDA row with [STALE] note); CMakeLists.txt:60 'find_package(CUDAToolkit REQUIRED)' (no version floor); RELEASE-SCOPE.md:26 §2 #5 confirms 'no version floor' and cites CMakeLists.txt:56 (actual line is 60).

### §1 — Project vision & non-goals
code: **partial** · comments: **none**

§1 is mostly high-level vision, and its concrete BUILT claims hold: device-resident DeviceF2Blocks with opt-in .to_host() and Resident/HostRam/Disk tiering (src/device/f2_blocks_out.hpp), an always-present CpuBackend reference backend (src/device/cpu/cpu_backend.cpp), and multi-GPU rotation deferred via TODO(multigpu-host-bounce) (device_f2_blocks.hpp:92, model_search.cpp:264, cmd_rotate.cpp:157). The one real divergence is the line-46 STALE bracket, which is itself now stale: it insists the decoder is 'TGENO-only' and that PLINK/EIGENSTRAT/PACKEDANCESTRYMAP readers are un-shipped Phase-2 work, but src/io/geno_reader.cpp implements all five format gathers, src/core/stats/read_canonical_tile.cpp dispatches them, and they are reached from the real precompute path (qpfstats.cpp, extract_f2_core.cpp) — a state RELEASE-SCOPE.md §2 #10 already marks RESOLVED. Its 'merge/harmonize front-end' sub-claim, however, still holds (no multi-dataset merge exists). No code comment cites architecture.md §1; the numerous §1.x citations in code all reference other documents (fit-engine.md, ROADMAP, the FROZEN CONTRACT, cli-bindings.md), so there is no comment-vs-spec drift to report for this section.

- `[high/spec-vs-code]` §1's 'It is NOT' bullet (architecture.md line 46) carries a STALE-correction bracket asserting: '[STALE vs as-built — the shipped decoder is TGENO-only (src/io/geno_reader.cpp throws a clear error on GENO/PACKEDANCESTRYMAP); PLINK / EIGENSTRAT / PACKEDANCESTRYMAP readers ... are PLANNED Phase-2 work, not current. Transcode with convertf first.]'. This is now false: the code implements and wires up FIVE formats. src/io/geno_reader.cpp auto-detects TGENO/GENO/EIGENSTRAT/PLINK/ANCESTRYMAP in the ctor (lines 283-349) and provides dedicated gathers read_snp_major_tile (line 612, GENO/PACKEDANCESTRYMAP), read_eigenstrat_snp_major_tile (line 692), read_plink_snp_major_tile (line 795), read_ancestrymap_snp_major_tile (line 895). The old throw the bracket cites is gone (geno_reader.cpp line 81 is now inside parse_eigenstrat_geometry). src/core/stats/read_canonical_tile.cpp dispatches all five (switch at lines 63-112). This is not dead code: it is reached from the real f-stat/qpAdm/f2 precompute via read_genotype_front_end (qpfstats.cpp:236, app/extract_f2_core.cpp:105, dstat.cpp:134, dates.cpp:136). Even the cross-ref the bracket itself cites, RELEASE-SCOPE.md §2 #10, now reads 'RESOLVED (Phase-2 format readers, main @ e4a5387) ... geno_reader.cpp:81 no longer throws' (docs/archive/RELEASE-SCOPE.md line 35). The section actively tells a reader the code cannot read PLINK/EIGENSTRAT/PACKEDANCESTRYMAP and to 'Transcode with convertf first', when the code reads them directly. [CONFIRMED]
  - docs/archive/architecture.md:46 (STALE bracket) vs src/io/geno_reader.cpp:283-349,612,692,795,895 + src/core/stats/read_canonical_tile.cpp:63-112 + src/core/stats/qpfstats.cpp:236 + docs/archive/RELEASE-SCOPE.md:35
- `[low/spec-vs-code]` The same line-46 bracket lumps 'the merge/harmonize front-end' together with the format readers as 'PLANNED Phase-2 work, not current.' The reader half is now built (finding above), but the multi-dataset MERGE front-end half still appears absent: there is no multi-dataset merge implementation — the only 'merge' symbols are the unrelated config-precedence chain (ConfigBuilder::merge_file/merge_cli/merge_env, src/app/cli_parse.cpp:67-70) and single-dataset harmonization filters (strand-ambiguous drop in include/steppe/config.hpp:612; per-tile filters in src/io/filter/: snp_filter, mind_prepass, include_exclude). So the bracket is half-stale: it should be split, because as written it under-claims the shipped readers while its merge-front-end statement remains roughly accurate. [PLAUSIBLE]
  - docs/archive/architecture.md:46 ('merge/harmonize front-end ... PLANNED Phase-2') vs src/io/filter/ (only single-dataset filters) + src/app/cli_parse.cpp:67-70 (config-merge, not dataset-merge)

### §2 — Engineering principles
code: **partial** · comments: **partial**

§2 is largely code-verifiable and mostly holds: cdiv() is genuinely single-source in core/internal/launch_config.hpp and consumed (not re-derived) by device kernels; core is CUDA-free and drives the GPU through ComputeBackend (two impls); the RAII owners DeviceBuffer/PinnedBuffer/CublasHandle/CusolverDnHandle exist as move-only types with debug-warn (non-silent) teardown; RunConfig is an immutable value object and Resources bundles backend/streams/allocator with no singletons. But three concrete named artifacts in the section are stale/divergent: (1) the device-memory/VRAM budget check is NOT at ConfigBuilder::build() — that layer is CUDA-free static-only and a code comment there explicitly defers the live VRAM probe to the device layer's build_resources; (2) the \"one real_t<P> precision switch\" does not exist — precision is a runtime struct Precision/enum Kind, not a type-level template; (3) the allowlisted TU allocator.cu does not exist, and the allowlist is described inconsistently across the spec, device_buffer.cuh, and src/device/CMakeLists.txt. Lower-severity: the \"enforced in CI\" grep gate has no CI/gate/allowlist artifact in-repo, and \"block_assignment()\" is really assign_blocks(). Comments that cite §2 are mostly accurate; the exceptions are device_buffer.cuh (repeats the nonexistent allocator.cu) and src/device/CMakeLists.txt (a conflicting allowlist).

- `[medium/spec-vs-code]` §2 Fail-fast claims the device-memory / VRAM budget check happens at ConfigBuilder::build(): "Config is validated once at ConfigBuilder::build() and frozen — including a device-memory budget check (§9, §11.2): invalid arch lists, missing devices ... and configs that exceed VRAM surface immediately." The code explicitly disclaims this. ConfigBuilder::build() is the CUDA-free static-validation layer and the VRAM/device probe is done LATER in the device layer, not here.
  - src/core/config/config_builder.cpp:206-208 comment: "the LIVE device/VRAM probe is the app's job through build_resources (cli-bindings.md §4.5), not this CUDA-free layer." build() (config_builder.cpp:210+) does only static string/ordinal validation. The actual budget check lives in src/device/vram_budget.hpp, src/device/tier_select.hpp, src/device/decode_budget.hpp, src/device/resources.cpp — not in ConfigBuilder::build().
- `[medium/spec-vs-code]` §2 DRY lists "one real_t<P> precision switch" as one of the concrete single-source-of-truth artifacts (alongside STEPPE_CUDA_CHECK, cdiv(), block_assignment()). No real_t exists anywhere in the codebase. Precision is not a compile-time type-level template switch at all; it is a runtime enum + mantissa field, a fundamentally different representation than the spec's real_t<P>.
  - grep for real_t across src/ include/ (all .hpp/.h/.cuh/.cu/.cpp) returns zero hits. The real precision model is struct Precision { enum class Kind { Fp64, EmulatedFp64, Tf32 }; int mantissa_bits; } at include/steppe/config.hpp:296-338.
- `[medium/spec-vs-code]` §2 names the DRY-gate allocation allowlist as exactly three TUs: "(device_buffer.cuh, allocator.cu, pinned_buffer.cuh)". allocator.cu does not exist. The only allocation-family call sites in src are pinned_buffer.cuh (cudaHostAlloc/cudaFreeHost) and device_buffer.cuh (cudaMalloc/cudaFree); none of the wider family the spec enumerates (cudaMallocAsync/Managed/FreeAsync) is used at all. The allowlist description is also internally inconsistent: src/device/CMakeLists.txt states a DIFFERENT allowlist (device_buffer.cuh + handles.hpp), omitting allocator.cu and pinned_buffer.cuh.
  - find . -name 'allocator*' returns nothing; grep 'allocator' in src/device/CMakeLists.txt, cmake/, CMakeLists.txt returns nothing. Allocation calls only at src/device/cuda/pinned_buffer.cuh:105 and src/device/cuda/device_buffer.cuh (free at :164). src/device/CMakeLists.txt:9-10 lists "RAII DeviceBuffer (cuda/device_buffer.cuh), RAII cuBLAS handle (cuda/handles.hpp)" as the allowlisted homes.
- `[low/comment-vs-spec]` The device_buffer.cuh header comment faithfully repeats §2's stale three-file allowlist including the nonexistent allocator.cu, so the comment agrees with the spec but both are stale vs the actual file set; meanwhile src/device/CMakeLists.txt:9-10 gives a conflicting allowlist. These two §2-citing comments disagree with each other about what the allowlist is.
  - src/device/cuda/device_buffer.cuh:4-6: "one of the THREE allowlisted translation units ... (architecture.md §2 DRY grep gate: device_buffer.cuh, allocator.cu, pinned_buffer.cuh)" vs src/device/CMakeLists.txt:9-10 which names device_buffer.cuh + handles.hpp instead.
- `[low/spec-vs-code]` §2 asserts the DRY grep gate is "enforced in CI" and that "The allowlist is a checked-in list of translation units, not a pattern; adding a TU to it requires review." No CI configuration, no grep-gate test/ctest target, and no checked-in allowlist file exist in the repository — the allowlist survives only as prose in header comments and the spec. (Marked lower-confidence: CI could live outside this repo.)
  - No .github/ directory; find for *.yml/*.yaml/Jenkinsfile returns none (only pyproject.toml). No grep/allowlist target in tests/CMakeLists.txt or cmake/*.cmake. grep 'allowlist' across .txt/.cmake/.md finds only src/device/CMakeLists.txt and the archived spec — no enforcement artifact.
- `[low/spec-vs-code]` §2 names "one block_assignment() rule" as a concrete DRY single-source example. There is no function named block_assignment(); the actual single-source function is assign_blocks(). The design intent the sentence states (the SNP→block rule lives host-pure in core, never re-derived in io) is fully honored — only the function name is stale.
  - src/core/domain/block_partition_rule.hpp/.cpp exports assign_blocks() (declared block_partition_rule.hpp:190), block_of, block_ranges, block_size_cm_to_morgans; the header even cites "architecture.md §2" for the not-in-io rule. grep 'block_assignment' returns zero code hits.

### §3 — "Tech stack & pinned versions"
code: **stale** · comments: **partial**

§3 is a code-verifiable pins table and is materially STALE. Its foundational claim — that third_party/CMakeLists.txt is the single central place FetchContent/CPM pins live — is wrong: no third_party/ dir exists, CPM machinery is cmake/CPM.cmake, and pins are distributed per-subtree (CLI11 in src/app, nanobind in bindings) via find_package-first/CPM-fallback, not FetchContent. Five rows diverge from as-built code: sccache (code uses ccache), Eigen (CPU reference backend uses hand-rolled src/core/internal/small_linalg.hpp), spdlog (facade is a fprintf sink; spdlog is future-only), nvbench (benches hand-rolled with <chrono>), and the CCCL 'CI asserts CCCL_VERSION>=3.1 + CPM-fetch' guard (does not exist). Minor: the pyproject.toml:59 line citation for the CMake floor drifted to line 81. Aligned rows: CUDA 13.x, CMake >=3.28, C++20, Ninja, CLI11 2.4.2, nanobind >=2, scikit-build-core >=0.10. Comment check: only tests/CMakeLists.txt:8-13 directly cites §3, and it repeats the stale third_party/CPM-GTest assumption (GTest is actually find_package + self-check fallback); no other code comments cite §3.

- `[high/spec-vs-code]` Section's foundational claim (architecture.md:79) that '`third_party/CMakeLists.txt` is the single place FetchContent_Declare/CPM pins live' is DIVERGENT. No `third_party/` directory exists anywhere in the repo. The CPM machinery lives at cmake/CPM.cmake, and dependency pins are DISTRIBUTED per-subtree, not centralized: CLI11 pinned in src/app/CMakeLists.txt:24-28, nanobind in bindings/CMakeLists.txt:47-48. Each uses find_package-first / CPM-fallback (not FetchContent), scoped PRIVATE to its subtree — the opposite of a single central FetchContent file.
  - `find third_party` => empty; cmake/CPM.cmake; src/app/CMakeLists.txt:17-28 and bindings/CMakeLists.txt:30-48 (distributed find_package-then-CPM pins); contradicts architecture.md:79
- `[high/spec-vs-code]` Row 'sccache >= 0.8' (architecture.md:99) is DIVERGENT: the build's compiler cache is ccache, not sccache. CMakeLists.txt:71-85 wires CMAKE_{CXX,CUDA}_COMPILER_LAUNCHER to find_program(CCACHE_PROGRAM ccache) behind option STEPPE_CCACHE (cmake/SteppeOptions.cmake:61). Grep for 'sccache' across build files returns nothing.
  - CMakeLists.txt:71-85 (ccache launcher); cmake/SteppeOptions.cmake:56-61 (STEPPE_CCACHE); no 'sccache' hits in cmake/CMakeLists/toml; contradicts architecture.md:99
- `[high/spec-vs-code]` Row 'Eigen 3.4.x | Host-only small dense LA in the CPU reference backend' (architecture.md:92) is DIVERGENT. No Eigen library is present or linked: no `#include <Eigen>`, no find_package(Eigen), no Eigen3:: anywhere. The CPU reference backend (src/device/cpu/cpu_backend.cpp, 'REFERENCE backend: scalar host oracle') uses a hand-rolled dense-LA header src/core/internal/small_linalg.hpp, not Eigen.
  - grep '#include <Eigen>|find_package(Eigen)|Eigen3::' => empty; src/device/CMakeLists.txt:51 (cpu_backend.cpp); src/core/internal/small_linalg.hpp; contradicts architecture.md:92
- `[medium/spec-vs-code]` Row 'spdlog 1.14.x | Logging backend behind our facade' (architecture.md:93) is STALE vs as-built. The facade exists (src/core/internal/log.hpp) but is a hand-rolled std::fprintf(stderr, ...) printf-style sink; spdlog appears nowhere in src/include/bindings. log.hpp itself states the structured-logging backend is only 'reserved' and its format string 'is the single seam a structured-logging backend would swap' — spdlog is intended-future, not a wired/pinned dependency.
  - grep 'spdlog' in src/include/bindings => empty; src/core/internal/log.hpp:16-17,25,47-52 (fprintf sink; spdlog described as reserved future backend); contradicts architecture.md:93
- `[medium/spec-vs-code]` Row 'nvbench | pinned commit | CUDA microbenchmark + regression JSON' (architecture.md:98) is DIVERGENT: nvbench is not used or pinned. The benchmarks (tests/reference/bench_*.cu) are hand-rolled — bench_f2_multigpu.cu times with <chrono> and includes no nvbench header; grep 'nvbench' across tests returns nothing.
  - tests/reference/bench_f2_multigpu.cu:40 (#include <chrono>), no nvbench include; grep 'nvbench' in tests => empty; contradicts architecture.md:98
- `[medium/spec-vs-code]` Row 'CCCL >= 3.1.0' (architecture.md:87) claims 'CI asserts CCCL_VERSION >= 3.1 and CPM-fetches 3.1 if the toolkit copy is older'. No such gate exists: no find_package(CCCL), no CCCL_VERSION check, no CPM fetch of CCCL anywhere (CCCL is used only as toolkit-bundled CUB/Thrust). The asserted CI guard is unimplemented.
  - grep 'CCCL_VERSION|find_package(CCCL)|VERSION_GREATER 3.1' across cmake/CMakeLists/cu/cuh/hpp => empty; contradicts architecture.md:87
- `[low/comment-vs-spec]` Comment at tests/CMakeLists.txt:8-9 cites 'architecture.md §3' and says the harness works 'even before third_party/ CPM-fetches GTest'. This mirrors the section's stale premise: there is no third_party/ and GoogleTest is never CPM-fetched — tests/CMakeLists.txt:14-21 use find_package(GTest CONFIG QUIET) with a self-checking-main fallback (no CPMAddPackage for GTest). The comment (and the §3 row it cites) describe a CPM-fetch that does not exist.
  - tests/CMakeLists.txt:8-9,13-21 (cites §3; 'third_party/ CPM-fetches GTest'; actual = find_package + self-check fallback, no CPM)
- `[low/spec-vs-code]` CMake floor row (architecture.md:84) cites `pyproject.toml:59` as evidence for the >=3.28 floor, but line 59 is the `test = [...]` pytest extra; the actual `cmake.version = ">=3.28"` is at pyproject.toml:81. Only the line-number citation drifted — the >=3.28 claim itself is correct (also at CMakeLists.txt:14 and CMakePresets.json).
  - pyproject.toml:81 (cmake.version=">=3.28"); pyproject.toml:59 is `test = [...]`; architecture.md:84 cites pyproject.toml:59

### §4 — "Repository layout" (docs/archive/architecture.md, lines 104-257)
code: **partial** · comments: **aligned**

§4's normative content — the dependency-direction rule and 'How CMake enforces it' (lines 252-254: app/bindings→api→core→{device,io}, io as a leaf, the deliberate core→io PRIVATE front-end for stats/{dstat,qpfstats,dates,read_canonical_tile}.cpp, and CUDA PRIVATE to steppe_device) — is still ACCURATE against the code: those stats/*.cpp files exist under src/core/stats/, and the access/extract/device CMakeLists enforce the CUDA-free/plain-C++20 layering exactly as described. Every genuine code comment citing 'architecture.md §4' matches that still-current layering (comment_alignment: aligned; the only wrinkle is p2p_combine.hpp's unrelated '§4 gate' shorthand). But the section's inventory and STATE snapshot are substantially STALE (code_alignment: partial): the STATE preamble calls the app/ CLI, Python bindings, and standalone f3/f4/D-stat entry points 'still planned' when all are built with public headers, CLI commands, and C++/CLI/Python tests; the fstats.hpp/fstats-NOTE 'planned (P2)' annotations are likewise stale; the docs/ paths point at the pre-archive location of this very document; and whole subsystems (qpgraph, qpfstats, dates, PLINK io, src/core/config + stats, the split cuda_backend TUs, tests/cli|python|r) are missing from the tree. Correctly-still-planned items (io/merge, io/impute, precomputed_f2.cpp) were verified absent and are not flagged.

- `[high/spec-vs-code]` The STATE preamble declares that 'Still planned (step 2+): app/ CLI, Python bindings, standalone f3/f4/D-stat entry points' — all three are now fully BUILT (with public API + tests), so the section's milestone snapshot is stale. It even self-contradicts: the same tree marks app/ [BUILT, P3] (line 219) and bindings/ [BUILT, M(py-1)] (line 221).
  - Spec docs/archive/architecture.md:111 says these are planned. Reality: src/app/ has main.cpp + full cmd_*.cpp CLI; bindings/ has module.cpp + bind_qpadm/fstats/qpgraph/dates/f2handle.cpp; standalone entry points exist as public headers include/steppe/f3.hpp (run_f3), f4.hpp (run_f4), f4ratio.hpp (run_f4ratio), dstat.hpp (run_dstat), fstat_sweep.hpp (run_f4_sweep/run_f3_sweep), implemented in src/core/qpadm/{f3,f4,f4ratio,fstat_sweep}.cpp + src/core/stats/dstat.cpp, with tests/cli/test_cli_{f3,f4,f4ratio,qpdstat}.cpp and tests/python/test_py_{f3,f4,f4ratio,dstat_geno}.py.
- `[high/spec-vs-code]` The fstats.hpp tree annotation claims 'Standalone f3/f4/D-stat entry points still planned (P2); the f4 math is currently INTERNAL to the fit (qpadm.hpp)'. This is stale — the f4/f3/D-stat math is now exposed as first-class public CUDA-free entry points. The mirrored NOTE under fstats/ (lines 157-158, 'standalone f3/f4/D-stat contractions are still planned (P2)') is stale for the same reason.
  - Spec docs/archive/architecture.md:132-133 and 157-158. Reality: include/steppe/f4.hpp:70 '[[nodiscard]] F4Result run_f4(const device::DeviceF2Blocks&...)', include/steppe/f3.hpp:70 'run_f3', include/steppe/dstat.hpp:83 'run_dstat'; f3.hpp:1 header explicitly self-describes as 'PUBLIC, CUDA-FREE standalone f3-statistic entry point'.
- `[medium/spec-vs-code]` The docs/ block lists 'docs/architecture.md # [BUILT] this document' and 'docs/ROADMAP.md', but the document has been moved to docs/archive/ and neither file exists at the path the tree claims. The section's own self-citation ('this document') points at a non-existent location.
  - Spec docs/archive/architecture.md:241-242. Reality: `ls docs/architecture.md` and `ls docs/ROADMAP.md` both fail; actual paths are docs/archive/architecture.md and docs/archive/ROADMAP.md.
- `[medium/spec-vs-code]` Entire subsystems are absent from the tree inventory. include/steppe/ (spec lists only config/error/fstats/qpadm.hpp) actually ships 14 public headers including qpgraph.hpp, qpgraph_search.hpp, qpfstats.hpp, dates.hpp, extract.hpp (plus f3/f4/f4ratio/dstat/fstat_sweep). src/core/ has two subdirs the tree never lists: config/ (cli_args, config_builder, run_config, exit_code, build_result) and stats/ (the qpgraph/qpfstats/dates/dstat host orchestration). The qpGraph family + standalone f-stats under qpadm/ are acknowledged only in the MISNOMER note (lines 170-179), not the tree body.
  - Reality: ls include/steppe/ -> 14 headers; ls src/core/ -> config, stats present; ls src/core/qpadm/ -> qpgraph_{enumerate,fit,model,objective,opt_constants,search}, f3.cpp, f4.cpp, f4ratio.cpp, fstat_sweep.cpp. Spec include/steppe tree docs/archive/architecture.md:123-135 and src/core tree 137-179 omit all of these subdirs/headers.
- `[medium/spec-vs-code]` The tree presents src/access/ and src/extract/ as directories that house the f2-dir reader / pop-resolver and the run_extract_f2 chain + STPF2BK1 writer. In reality each dir contains ONLY a CMakeLists.txt; the actual .cpp sources live in src/app/ and are pulled in by the sublib. Functionality matches, but the physical location the tree implies is wrong (the CMakeLists themselves say the sources 'still live in src/app/').
  - Spec docs/archive/architecture.md:217-218. Reality: `ls src/access src/extract` -> only CMakeLists.txt; src/access/CMakeLists.txt adds ../app/f2_dir_io.cpp + ../app/pop_resolver.cpp; src/extract/CMakeLists.txt adds ../app/extract_f2_core.cpp + ../app/f2_dir_writer.cpp.
- `[medium/spec-vs-code]` The src/io leaf tree omits PLINK support and several new front-end files. (The planned merge/, impute/, precomputed_f2.cpp are correctly still absent, so those 'planned' labels are fine.)
  - Spec docs/archive/architecture.md:209-216 lists only eigenstrat_format, {geno,snp,ind}_reader, genotype_tile, filter/. Reality adds src/io/plink_reader.{hpp,cpp}, genotype_source.{hpp,cpp}, ploidy_detect.{hpp,cpp}, snp_major_tile.hpp (with tests/reference/test_plink_decode_equivalence.cu, test_pa_decode_equivalence.cu).
- `[low/spec-vs-code]` The src/device/cuda tree describes a single monolithic cuda_backend.cu implementing decode + f2 + all fit overrides, and lists ~9 device files. The backend is now split into cuda_backend.cu + .cuh + eight cuda_backend_*.cu translation units, plus many new kernels and device/ top-level files. tests/ also grew cli/, python/, and r/ subdirs the tree never mentions.
  - Spec docs/archive/architecture.md:194-207. Reality: src/device/cuda/ has cuda_backend_{decode,f2_blocks,qpadm_fit,dates,dstat,fstats_assemble,qpfstats,qpgraph}.cu plus dstat_kernel, dates_kernel, qpfstats_kernel, qpgraph_fit_kernels, ratio_block_jackknife_kernel, detect_ploidy_kernel, transpose_canonical_kernel, p2p_combine.cu, etc.; src/device/ adds backend_factory, resources, shard_plan, vram_budget, device_partial, f2_blocks_out; tests/ has cli/, python/, r/ dirs.
- `[low/spec-vs-code]` The repo-root annotation states 'git: HEAD on branch phase2-fit-engine == main'. HEAD is now on branch main directly (phase2-fit-engine no longer the checked-out branch).
  - Spec docs/archive/architecture.md:112. Reality: `git rev-parse --abbrev-ref HEAD` -> main.
- `[low/comment-vs-spec]` p2p_combine.hpp uses '§4' as a local shorthand for a four-term peer-access combine gate ('THE §4 COMBINE GATE', 'doc §4 Item 1', 'the four-term §4 gate') and once writes 'architecture.md §11.4 §4'. This is NOT architecture.md §4 (Repository layout); the shorthand can mislead a reader into resolving it against the Repository-layout section. All other §4 citations in the codebase are correct.
  - src/device/p2p_combine.hpp:27,48-50. Contrast with the accurate §4 citations: src/device/CMakeLists.txt:13 'architecture.md §4 "How CMake enforces it"', src/access/CMakeLists.txt:17 and src/extract/CMakeLists.txt:18-19 'PLAIN C++20 CXX target (architecture.md §4)', backend_factory.hpp:9 'CUDA is PRIVATE to steppe_device; architecture.md §4', include/steppe/f3.hpp:'CUDA-FREE BY CONTRACT (architecture.md §4)' — all match §4's still-current layering text.

### §5 — "Layered architecture mapped onto the qpAdm pipeline" (lines 258-310)
code: **partial** · comments: **partial**

§5's core substance is accurate and code-verifiable: the S0-S8 stage-to-file mapping, the 3-GEMM f2 reformulation, Vpair retention as the S4 jackknife weight, the EmulatedFp64{40} default, and the 'fit engine BUILT / golden-gated' narrative all match the code (files present; §5 S2/S4 comments in f2_block_kernel.cu, backend.hpp, f2_from_blocks.cpp track the spec). Two spec-vs-code divergences: (1) HIGH — the 'What is NOT yet built' paragraph (line 295) is fully stale; the CLI (src/app + CLI11), Python bindings (bindings/ nanobind steppe._core), and standalone f3/f4/D-stat entry points all now exist. (2) MEDIUM — S3-S8 name cuda_backend.cu for the fit overrides, but they were split into cuda_backend_qpadm_fit.cu and cuda_backend_fstats_assemble.cu (none remain in cuda_backend.cu). Comment-vs-spec: LOW — a cluster of comments cite stale §5 line numbers (SPEC §5:231, §5 S2 line 236-240) that no longer land in §5, though their content is still correct.

- `[high/spec-vs-code]` §5 line 295 claims the CLI, Python bindings, and standalone f3/f4/D-stat entry points are NOT yet built: 'NO CLI, NO Python bindings, NO standalone f3/f4/D-stat entry points (no app/ or bindings/ dir, no nanobind/CLI11 in CMake) — these stay (planned) below.' Every clause is now false. This whole 'honest NEXT' paragraph is stale.
  - bindings/ dir exists as the nanobind module steppe._core (M(py-1)): bindings/CMakeLists.txt:1 + nanobind_add_module(_core ...) at :64, with bind_qpadm.cpp/bind_dates.cpp/bind_fstats.cpp/bind_qpgraph.cpp/bind_f2handle.cpp/module.cpp. src/app/ is a full CLI11 app (src/app/CMakeLists.txt:17 find_package(CLI11 2.4); src/app/main.cpp, cmd_qpadm.cpp, cmd_qpdstat.cpp, cmd_extract_f2.cpp). Standalone stat entry points exist: include/steppe/f3.hpp, f4.hpp, dstat.hpp, extract.hpp plus src/app/cmd_f3.cpp/cmd_f4.cpp and src/core/qpadm/f3.cpp/f4.cpp.
- `[medium/spec-vs-code]` §5 rows S3–S6/S8 and the note at line 295 name src/device/cuda/cuda_backend.cu as the file holding the CudaBackend fit overrides (assemble_f4/jackknife_cov/rank_sweep/gls_weights/fit_models_batched). Those definitions were split out of cuda_backend.cu into separate TUs; cuda_backend.cu now contains none of them, so every 'device/cuda/cuda_backend.cu' cite in S3–S8 is a stale file location. (The co-named kernels file qpadm_fit_kernels.cu is still correct.)
  - grep for the override symbol definitions: jackknife_cov/rank_sweep/gls_weights/fit_models_batched -> src/device/cuda/cuda_backend_qpadm_fit.cu; assemble_f4 -> src/device/cuda/cuda_backend_fstats_assemble.cu; grep of cuda_backend.cu returns NONE. CMakeLists confirms the split: src/device/CMakeLists.txt:44 'cuda/cuda_backend_qpadm_fit.cu # qpAdm fit engine ... (split T8 / §2.3 TU-I ...)'.
- `[low/comment-vs-spec]` Several code comments cite §5 by absolute spec line number, and those anchors have drifted. §5 now begins at line 258 and its 3-GEMM block is at lines 278-285, but comments still point at pre-258 lines. The comments' CONTENT still matches §5 (svd_path dispatch report, R_diag rows, fused numerator/divide); only the cited line numbers are stale.
  - 'SPEC §5:231' in src/device/backend.hpp:345 and :1714 and src/core/cpu/cpu_backend.cpp:1649 — line 231 is now a tests/ file-tree entry (test_qpadm_domain.cu), not §5. '§5 S2 line 236-240 / 238-240 / 240' in src/device/cuda/f2_block_kernel.cu:159,404, f2_block_kernel.cuh:75, and src/core/internal/f2_estimator.hpp:98 — the S2 3-GEMM block is now at 278-285, not 236-240.

### §6 — Build system
code: **partial** · comments: **partial**

§6 is PARTIALLY aligned. The load-bearing structure holds and verifies against the code: target-based CMake with one target per src/ subdirectory, cmake_minimum_required 3.28 top-level, the CXX/CUDA-20 language policy, the add_subdirectory order (include -> io -> core -> device -> access/extract/app/bindings/tests), CUDA PRIVATE layering, the STEPPE_CUDA_ARCH_RELEASE ship list (75..120 + 120-virtual), CUDA_SEPARABLE_COMPILATION ON, the STEPPE_COMPRESS_FATBIN Release-only --compress-mode=size opt-in, and the steppe::warnings INTERFACE flag policy all match. But three prose subsections are stale/never-existed and are the high-severity hits: (1) the 'Dependency fetching (CPM)' block invents a third_party/CMakeLists.txt and CCCL-3.1/Eigen3/spdlog fetches that appear nowhere (only CLI11 and nanobind are CPM-fetched, each behind its own option; GTest is find_package-with-fallback) — and the section even contradicts itself on this; (2) the compiler launcher is ccache wired inline behind STEPPE_CCACHE, not sccache via a non-existent cmake/CompilerLauncher.cmake; (3) CMakePresets.json diverges (min 3.28 not 3.30; presets base/dev/dev-asan/dev-compute/release/ci, no debug/cuda-debug/asan; base pins arch 120 not native, no launchers). Secondary: the arch default is 120 not 'native', and device LTO (-dlto/lto_<arch>) is described but explicitly deferred in code. On the comment side, most §6 citations are accurate, but two comments mis-attribute Doxygen to §6 (it is §16), several absolute line-number citations into §6 have drifted (~20 lines), and a tests comment still references the phantom third_party/ CPM-fetch of GTest.

- `[high/spec-vs-code]` The entire 'Dependency fetching (CPM)' block is stale/never-existed. Spec lines 402-414 say 'third_party/CMakeLists.txt — ALL pins here' and show find_package(CCCL) + CPM fetches of CCCL 3.1 (the 'determinism API' floor), Eigen3 3.4.0, spdlog 1.14.1, CLI11 2.4.2, googletest 1.15.2. Reality: there is NO third_party/ dir (ls fails), and CCCL / Eigen3 / spdlog appear NOWHERE in the build (grep across all *.cmake/CMakeLists.txt returns nothing). The only CPM fetches are CLI11 (src/app, behind STEPPE_BUILD_CLI) and nanobind (bindings, behind STEPPE_BUILD_PYTHON); GoogleTest is located via find_package(GTest CONFIG) with a self-checking-executable fallback, never CPM-fetched. The section even contradicts itself: its own top-level snippet at spec lines 331-332 says 'no third_party/ dir: deps are bootstrapped via cmake/CPM.cmake inside the opt-in src/app + bindings subtrees' — which is what the code actually does.
  - no third_party/ (ls fails); cmake/CPM.cmake:11-16 ('included only from src/app ... and bindings/'); src/app/CMakeLists.txt:17-30 (CLI11 find_package+CPM v2.4.2); bindings/CMakeLists.txt:30-51 (nanobind find_package+CPM v2.4.0); tests/CMakeLists.txt:14-22 (GTest find_package + self-check fallback) — contradicts architecture.md:402-414
- `[high/spec-vs-code]` Compiler launcher is ccache, not sccache, and cmake/CompilerLauncher.cmake does not exist. Spec line 329 does include(cmake/CompilerLauncher.cmake); spec preset 'base' (lines 430-431) sets CMAKE_CUDA_COMPILER_LAUNCHER / CMAKE_CXX_COMPILER_LAUNCHER = sccache; spec prose line 450 says 'sccache caches host TUs reliably; nvcc caching is best-effort'. Reality: no cmake/CompilerLauncher.cmake (ls fails); the string 'sccache' occurs nowhere in the repo (grep empty); the launcher is ccache, wired inline in the top-level CMakeLists behind STEPPE_CCACHE (default ON) via find_program(ccache); and CMakePresets.json sets no compiler launcher at all.
  - no cmake/CompilerLauncher.cmake (ls fails); grep 'sccache' -> empty; CMakeLists.txt:71-85 (ccache via STEPPE_CCACHE) + cmake/SteppeOptions.cmake:56-61; CMakePresets.json:6-15 (base preset, no launcher) — contradicts architecture.md:329,430-431,450
- `[medium/spec-vs-code]` CMakePresets.json diverges from the spec's preset snippet. Spec line 418 names presets dev/debug/release/cuda-debug/asan/ci and the JSON minimum is 3.30 (line 423) with a 'base' preset carrying sccache launchers and CMAKE_CUDA_ARCHITECTURES=native (lines 425-431). Actual presets are base/dev/dev-asan/dev-compute/release/ci — there is NO 'debug', NO 'cuda-debug', and 'asan' is renamed 'dev-asan' (compute lane is 'dev-compute'); cmakeMinimumRequired is 3.28; and 'base' sets CMAKE_CUDA_ARCHITECTURES=120 + STEPPE_HAVE_EMU_TUNING=ON with no compiler launcher. The 'release' preset's arch list does match the spec.
  - CMakePresets.json:3 (min 3.28 vs spec 3.30), :5-15 (base: arch 120, no sccache), :16-40 (dev/dev-asan/dev-compute — no debug/cuda-debug/asan) — contradicts architecture.md:418,423,425-436
- `[medium/spec-vs-code]` The CMAKE_CUDA_ARCHITECTURES default differs. Spec lines 349-351 show `if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES) set(CMAKE_CUDA_ARCHITECTURES native)` ('native for dev'). Actual cmake/CUDAArch.cmake defaults to STEPPE_CUDA_ARCH="120" (the M0 sm_120 box) and additionally treats an already-defined "75"/""/"OFF" as 'unset' before forcing 120 — never 'native'. The STEPPE_CUDA_ARCH_RELEASE list matches the spec exactly.
  - cmake/CUDAArch.cmake:42-56 (default 120, not native) vs matching release list :61-63 — contradicts architecture.md:349-351
- `[low/spec-vs-code]` Device LTO is described but not implemented. Spec lines 363-371 show steppe_device with a per-arch `-gencode=arch=compute_90,code=lto_90 -dlto` compile option and a `-dlto` link option on Release. Actual src/device/CMakeLists.txt sets CUDA_SEPARABLE_COMPILATION ON (the RDC half, which matches spec line 364) but contains NO -dlto / lto_<arch> options anywhere; grep for 'dlto'/'lto_' is empty. The in-file comment states the multi-arch device-LTO wiring 'lands with the release matrix' — i.e., deferred. Spec itself hedges this snippet as [UNCERTAIN]/illustrative, hence low severity, but the code does not yet do it.
  - src/device/CMakeLists.txt:99-105 (separable comp ON, LTO deferred), grep 'dlto|lto_' -> empty — vs architecture.md:363-371
- `[low/spec-vs-code]` Warnings-as-errors is now conditional. Spec lines 381-388 show unconditional `-Wall;-Wextra;-Werror` (host), `--Werror;all-warnings` (device), and `-Xcompiler=-Wall,-Wextra,-Werror`. Actual cmake/SteppeWarnings.cmake gates the three -Werror tokens behind the STEPPE_WERROR option (default ON, so the default build matches the spec; OFF is a dev escape hatch that keeps -Wall/-Wextra but drops -Werror). Default behavior aligns; the toggle is an unreflected enhancement. The steppe_warnings INTERFACE target, steppe::warnings alias, the CXX/CUDA generator-expression split, and the RelWithDebInfo -lineinfo all match.
  - cmake/SteppeWarnings.cmake:25-50 (STEPPE_WERROR gate) — vs architecture.md:381-388
- `[medium/comment-vs-spec]` Two build comments cite §6 for Doxygen, but §6 contains zero Doxygen content — Doxygen/API-docs live in §16 (architecture.md:944 'API docs via Doxygen + Sphinx/Breathe'). CMakeLists.txt:156 says 'architecture.md §6 names Doxygen as the API-doc generator' and cmake/SteppeOptions.cmake:41 says 'architecture.md §6 lists Doxygen as the API-doc generator'. Wrong section citation.
  - CMakeLists.txt:156 and cmake/SteppeOptions.cmake:41 cite '§6' for Doxygen; §6 (architecture.md:311-453) has no Doxygen — it is in §16 (architecture.md:944)
- `[low/comment-vs-spec]` Stale absolute line-number citations into §6. cmake/CPM.cmake:4 cites 'architecture.md:384 include(.../cmake/CPM.cmake)', but that include statement is now at line 404. src/app/CMakeLists.txt:10,13,23 cite 'line 392' for the `gh:CLIUtils/CLI11@2.4.2` pin / CPM include and attribute it to §15, but the pin actually sits in §6 at line 412 (and the include at line 404). The section text has grown ~20 lines, leaving these hard-coded citations off-by-position (and the §15-vs-§6 attribution imprecise).
  - cmake/CPM.cmake:4 ('architecture.md:384', now 404); src/app/CMakeLists.txt:10,13,23 ('§15 line 392', pin actually §6 line 412) — vs architecture.md:404,412
- `[low/comment-vs-spec]` tests/CMakeLists.txt:9 comment ('to keep the M0 slice buildable even before third_party/ CPM-fetches GTest') perpetuates the stale third_party/ dependency model. It happens to agree with the current (stale) §6 third_party/googletest block, but contradicts the real build, where GTest is resolved by find_package(GTest) with a self-check fallback and no third_party/ or CPM path for GTest exists. Flagged because it reinforces the same stale model as the spec's dependency-fetch subsection.
  - tests/CMakeLists.txt:9 ('before third_party/ CPM-fetches GTest') vs tests/CMakeLists.txt:14-22 (actual find_package(GTest)+fallback); no third_party/ dir

### §7 — CUDA coding standards & idioms (lines 454-565)
code: **partial** · comments: **aligned**

§7 is largely accurate and code-verifiable — the RAII wrappers (DeviceBuffer, PinnedBuffer, CublasHandle, CusolverDnHandle, Stream, Event) are all present, fully move-only with move-assign and debug-log destructors, and the load-bearing claims verify exactly: the CublasHandle (stream, workspace) invariant with set_stream re-applying the workspace after cublasSetStream (handles.hpp:143-161), GEMM routines never calling raw cublasSetStream (confirmed across f2_block/f2_batched/cuda_backend), the throwing source_location-based STEPPE_CUDA_CHECK/CUBLAS_CHECK/CUSOLVER_CHECK and STEPPE_CUDA_CHECK_KERNEL (check.cuh), and the grid-limit story (grid_for/grid_z_extent guards, kMaxGridZ folded into vram_budget max_blocks_per_chunk). Four divergences found, most-severe first: (1) HIGH — allocator.cu and the entire \"Async memory pools\" cudaMallocAsync/cudaFreeAsync mechanism do not exist; the device path uses only synchronous cudaMalloc/cudaHostAlloc, and the config knob that would gate it is explicitly \"NOT wired\"; (2) MEDIUM — CurandGenerator is a phantom (no cuRAND wrapper or usage in production); (3) LOW — no \"occupancy-aware block sizing\" helper in launch_config.hpp; (4) LOW/plausible — the CCCL determinism execution-environment reduction idiom is used nowhere. COMMENTS vs SPEC: every §7-citing code comment matches the section text — none contradict it. Note that the allocator.cu allowlist comments (device_buffer.cuh:6, pinned_buffer.cuh:7) and the config.hpp:421 pool-allocator comment faithfully echo §7, so they are stale relative to the CODE (finding 1) but not relative to the SECTION, hence comment_alignment=aligned.

- `[high/spec-vs-code]` The entire "Async memory pools" mechanism and the third allocation home `allocator.cu` do not exist. §7 states application memory is allocated "only inside DeviceBuffer<T> / PinnedBuffer<T> / allocator.cu (pool)" (line 456) and that "A pool-backed allocator variant takes a cudaStream_t and uses cudaMallocAsync/cudaFreeAsync (both inside allocator.cu, allowlisted); set the pool release threshold high (cudaMemPoolAttrReleaseThreshold = UINT64_MAX)" (line 558). No such file or code path exists: the real allocation homes are DeviceBuffer (synchronous cudaMalloc, device_buffer.cuh:83) and PinnedBuffer (cudaHostAlloc, pinned_buffer.cuh:104). The device path never uses stream-ordered pools.
  - `find` for allocator* (excl docs) returns nothing. grep of src/include for cudaMallocAsync/cudaFreeAsync/cudaMemPoolAttrReleaseThreshold matches ONLY comments (device_buffer.cuh:143-144 a 'future switch' warning, cuda_backend.cu:58, config.hpp:422). include/steppe/config.hpp:420-425 marks `use_mem_pool` "FORWARD-RESERVED (NOT wired) ... gates no compute" while its own comment claims the device path uses the cudaMallocAsync allocator "UNCONDITIONALLY" — self-contradictory. The DRY grep-gate allowlist comments (device_buffer.cuh:6, pinned_buffer.cuh:7) list `allocator.cu` as one of three permitted TUs, but only two of the three exist.
- `[medium/spec-vs-code]` `CurandGenerator` is a phantom RAII wrapper. §7 line 518 asserts "Stream, Event, CusolverDnHandle, CurandGenerator, PinnedBuffer<T> all follow this exact shape: move-construct + move-assign + dtor->destroy()-with-debug-log." Four of the five exist and match the shape exactly; CurandGenerator does not exist and there is no cuRAND anywhere in production src/include. The only cuRAND in the repo is raw device-side Philox (curand_kernel.h) inside a standalone benchmark, not owned by any RAII wrapper.
  - grep for `class Stream/Event/CusolverDnHandle/PinnedBuffer/CublasHandle` confirms Stream (stream.hpp:51), Event (stream.hpp:108), CusolverDnHandle (handles.hpp:332), PinnedBuffer (pinned_buffer.cuh:84) all present and move-only with debug-log destroy. grep for `CurandGenerator` in src/include = 0 hits. grep for `curand` in src/include = 0; the only curand usage is tests/reference/bench_optimizers.cu (curand_init / curandStatePhilox4_32_10_t, lines 96-722), a test bench with no wrapper class.
- `[low/spec-vs-code]` The "occupancy-aware block sizing" helper listed for launch_config.hpp does not exist. §7 line 537: "Launch-config helpers live once in core/internal/launch_config.hpp: cdiv(n,b), grid_for(n, block, max_grid), occupancy-aware block sizing." The file provides cdiv, grid_for, grid_z_extent, the kMaxGridX/Y/Z limits, and fixed named block dims (kDecodeBlockX/Y) — but no occupancy-driven sizing routine (no cudaOccupancyMaxPotentialBlockSize or equivalent); block dims are hard-coded named constants. The other two named helpers (cdiv, grid_for) and the surrounding grid-limit claims (kMaxGridZ folded into vram_budget max_blocks_per_chunk; grid_z_extent guard; SNP axis rides gridDim.x) all verify correctly.
  - launch_config.hpp full read: cdiv (85/92), grid_for (117), grid_z_extent (146), kMaxGridX/Y/Z (65/68/76), kDecodeBlockX/Y (169/174). grep 'occupanc' in launch_config.hpp hits only a doc-comment 'occupancy-friendly default' (172), not a function. vram_budget.hpp:169-185 confirms kMaxGridZ is folded into max_blocks_per_chunk exactly as §7 line 537 claims.
- `[low/spec-vs-code]` The CCCL determinism execution-environment idiom presented as the statistic-path default is used nowhere. §7 lines 541-546 give `namespace det = cuda::execution::determinism; auto env = cuda::execution::require(det::run_to_run); cub::DeviceReduce::Sum(d_in,d_out,n,env);` as "default for the statistic path." No cuda::execution / determinism-env usage exists, and cub::DeviceReduce is not used at all; statistic-path reductions run through cuBLAS GEMM. The actual CUB usage (DeviceSelect::Flagged, DeviceRadixSort, DeviceScan::ExclusiveSum) all uses the classic two-call temp-storage idiom on off-statistic-path (sweep/decode) work — consistent with §7 point 2, but point 1's concrete example has no counterpart in code. Marked low/plausible: it may be forward-looking policy rather than a claim about existing code.
  - grep of src/include for `cuda::execution` / `run_to_run` / `require(` returns no determinism-env usage (all 'determinism' hits are cuBLAS-workspace / cuSOLVER-deterministic-mode, e.g. handles.hpp:328, cuda_backend_f2_blocks.cu:208). CUB two-call idiom sites: cuda_backend_fstats_assemble.cu:237-360 and cuda_backend_decode.cu:261-310. No cub::DeviceReduce match anywhere.

### §8 — DRY &amp; shared utilities
code: **partial** · comments: **partial**

§8 is largely accurate for the helpers that shipped (check.cuh, log.hpp, launch_config.hpp with cdiv/grid_for/kDecodeBlockX-Y/kMaxGridX-Y-Z/grid_z_extent, host_device.hpp, block_partition_rule.hpp, backend.hpp/CpuBackend), but three named single-home files in the DRY table were never built or were renamed: internal/expected.hpp+STEPPE_TRY and internal/type_traits.hpp+real_t/is_fp64 do not exist at all, and internal/span_view.hpp is actually internal/views.hpp with a plain MatView (no cuda::std::span/mdspan, self-admittedly aspirational). The NVTX row is stale (real macro is STEPPE_NVTX_RANGE(name), no color arg, no nvtx::Color palette). The illustrative CMake block cites link libs the real build omits (CCCL::CCCL, CUDA::curand→actually cufft, Eigen3::Eigen not linked to core). The AT2 validation path tools/compare_against_admixtools2.R does not exist (scripts live under tests/reference/goldens/at2/). Comments citing §8 are broadly consistent with the DRY theme, except check.cuh cites a '§8 table' cuFFT row that the table lacks. Most-severe first; two low-severity notes included.

- `[high/spec-vs-code]` §8 table names `internal/expected.hpp` as the single home for internal error propagation, exposing `STEPPE_TRY(expr)` over `std::expected<T,Error>`. No such file exists anywhere in the repo, and `STEPPE_TRY` appears in zero source/header files. Internal error propagation is actually done via `std::expected`-based BuildResult in src/core/config/build_result.hpp + config_builder.hpp, with no shared expected.hpp/STEPPE_TRY facade.
  - architecture.md:593 claims `internal/expected.hpp` + `STEPPE_TRY`; `find -name 'expected*.hpp'` = empty; `grep -rn STEPPE_TRY src include` = empty; std::expected lives in src/core/config/build_result.hpp, config_builder.hpp
- `[high/spec-vs-code]` §8 table names `internal/type_traits.hpp` as the single home for precision/traits, exposing `real_t<P>`, `is_fp64`, and index aliases ('the precision switch lives here'). No `type_traits.hpp` file exists and neither `real_t` nor `is_fp64` is defined anywhere in src/include.
  - architecture.md:598 claims `internal/type_traits.hpp` + `real_t<P>`/`is_fp64`; `find -name 'type_traits*.hpp'` = empty; `grep real_t|is_fp64 src include` = empty
- `[medium/spec-vs-code]` §8 table names `internal/span_view.hpp` for 'non-owning matrix/tensor views over cuda::std::span/mdspan'. That file does not exist; the actual views home is src/core/internal/views.hpp, which defines a plain `struct MatView` (no cuda::std::span, no mdspan). The views.hpp header itself flags span_view.hpp as aspirational: 'The full span_view.hpp (architecture.md §8) generalizes this over cuda::std::span/mdspan ... this is the minimal host-side anchor.'
  - architecture.md:599 vs src/core/internal/views.hpp:51 (`struct MatView`) and views.hpp:8-10 (self-describes span_view.hpp as not-yet-built); `find -name 'span_view*'` = empty
- `[medium/spec-vs-code]` §8 table's NVTX row is stale on both the macro name and the palette. Spec: `STEPPE_NVTX_SCOPE("name", color)` RAII with the color palette `nvtx::Color` 'defined here, once'. Actual macro is `STEPPE_NVTX_RANGE(name)` — one arg (name only), no color parameter — and there is no `nvtx::Color` enum/namespace/palette in nvtx.hpp at all. The device CMakeLists also refers to STEPPE_NVTX_RANGE.
  - architecture.md:595 vs src/core/internal/nvtx.hpp:47 (`#define STEPPE_NVTX_RANGE(name)`); `grep Color|palette|namespace nvtx nvtx.hpp` = empty; src/device/CMakeLists.txt:89 references STEPPE_NVTX_RANGE
- `[medium/spec-vs-code]` The §8 CMake wiring block lists link libraries the real CMake does not use. (1) `steppe_device ... PUBLIC steppe::core_internal CCCL::CCCL` — CCCL::CCCL is not linked in any CMakeLists; actual PUBLIC is `steppe::api steppe::core_internal`. (2) `steppe_device ... PRIVATE CUDA::curand` — curand is linked nowhere and used nowhere in code; the actual PRIVATE list has `CUDA::cufft` (DATES engine) instead. (3) `steppe_core ... PRIVATE ... Eigen3::Eigen` — Eigen3::Eigen is not linked to steppe_core; actual PRIVATE is `steppe::device steppe::warnings steppe::io Threads::Threads` (Eigen is used in src/io, not core). The illustrative block also uses `add_library(steppe_device)` + `target_sources(... cuda/jackknife_kernel.cu ...)`, but the real target is `add_library(steppe_device STATIC ...)` and no `jackknife_kernel.cu` exists (real names: qpfstats_jackknife_kernel.cu / ratio_block_jackknife_kernel.cu).
  - architecture.md:577-578,586 vs src/device/CMakeLists.txt:22,72-73 and src/core/CMakeLists.txt:160-162; `grep -r CCCL/Eigen/curand CMakeLists` = none in device/core link lists
- `[medium/spec-vs-code]` §8's closing paragraph says both backends 'are validated against ADMIXTOOLS 2 via tools/compare_against_admixtools2.R.' There is no tools/ directory and no compare_against_admixtools2.R anywhere. AT2 validation actually lives under tests/reference/goldens/at2/scripts/ (golden_*_generate.R, verify_bitexact.R) and tests/r/.
  - architecture.md:603; `ls tools` = No such file or directory; `find -iname '*admixtools*'` = empty; actual R scripts at tests/reference/goldens/at2/scripts/*.R
- `[low/comment-vs-spec]` check.cuh documents CUFFT_CHECK as 'The ONE cuFFT error check in the codebase (architecture.md §8 table)', but the §8 table has no cuFFT row — it only lists a CUDA error check and a cuBLAS/cuSOLVER check. The code grew a CUFFT_CHECK (for the DATES cuFFT engine) that the §8 table never added, so the comment cites a table entry that does not exist.
  - src/device/cuda/check.cuh:317 ('§8 table') vs architecture.md:591-592 (table lists only CUDA and cuBLAS/cuSOLVER checks, no cuFFT)
- `[low/spec-vs-code]` §8 table presents `block_of` as an active part of the forward SNP→block rule ('the forward int block_of(genetic_pos, blgsize) + assign_blocks'). Per the header, block_of is now a vestigial primitive: assign_blocks no longer calls it (the AT2 anchored cumulative walk does not bin to a fixed grid). block_of still compiles and is documented, so this is a mild staleness, not an outright contradiction.
  - architecture.md:600 vs src/core/domain/block_partition_rule.hpp:20-24 ('assign_blocks no longer calls it ... the AT2 walk does not bin to a fixed grid')

### §9 — Configuration management
code: **partial** · comments: **partial**

§9's design philosophy holds (typed/immutable/layered config, precedence 'defaults < TOML < env(STEPPE_*) < CLI', DI of resources) and the Block-size-unit paragraph is fully accurate: build() converts Morgans→cM via kCentimorgansPerMorgan (config_builder.cpp:455) and block_size_cm_to_morgans is the single cM→Morgans site next to block_partition_rule.hpp feeding assign_blocks. But multiple concrete code claims are stale/divergent. HIGH: (1) build() does NOT validate the device-memory budget/precision-honorability/workspace/stream_count — those are runtime device-layer concerns (vram_budget.hpp, build_resources), contradicting the bolded L609/L692 claims; (2) the JackknifeMode{Delete1,Bootstrap}+bootstrap_reps()+resampling() resampling model does not exist — the real enum is JackknifePolicy{None,FeasibleOnly,All} (qpadm.hpp:54). MEDIUM: Resources/PerGpuResources fields differ and NcclComm is intentionally absent (resources.hpp:122-148); run_qpadm takes f2-blocks+QpAdmOptions+Resources, not (GenotypeDataset,QpAdmModel,RunConfig,Resources) and GenotypeDataset does not exist (qpadm_fit.cpp:297-307); Precision is a struct-with-Kind and mantissa_bits lives on Precision, not DeviceConfig (config.hpp:296-342). LOW: accessor is blgsize_cm() not block_size_cm(), no seed() accessor, and build() returns BuildResult (documented C++20 std::expected stand-in). Comment citations of §9 are mostly accurate for the device-ordinal/Resources contracts, but block_partition_rule.hpp's 'RunConfig::block_size_cm' name is stale and resources.hpp:118 cites '§9 PerGpuResources' as authority for a struct that deliberately omits the §9-listed fields.

- `[high/spec-vs-code]` §9 claims ConfigBuilder::build() itself validates the device-memory budget and other device-dependent constraints. The real build() is CUDA-free and does STATIC token/range validation only; the VRAM budget, precision-honorability, the deterministic+EmulatedFp64 workspace rule, the deterministic+stream_count>1 rule, and device-presence are NOT checked there — they live in the device/app layer at run time (device/vram_budget.hpp, device/resources.cpp build_resources), a split the code documents explicitly.
  - Spec L609 '.build() validates once — including the device-memory budget (§11.2)' and L692 build() 'rejects ... any config whose estimated peak VRAM exceeds device free memory (§11.2)'; 'a precision the selected backend cannot honor'; 'deterministic == true with precision == EmulatedFp64 unless an explicit cublasSetWorkspace workspace is configured'; 'deterministic == true with stream_count > 1'. Reality: config_builder.cpp:210-354 (only device-ordinal token/precision/format/range checks); config_builder.cpp:206-207 'the LIVE device/VRAM probe is the app's job through build_resources ... not this CUDA-free layer'; config_builder.hpp:26-31; §11.2 budget lives in device/vram_budget.hpp:59-172.
- `[high/spec-vs-code]` §9 describes a resampling model that does not exist: enum JackknifeMode{Delete1,Bootstrap}, a RunConfig::resampling() accessor, and RunConfig::bootstrap_reps() ('0 ⇒ delete-1 jackknife'). The real jackknife enum is JackknifePolicy{None,FeasibleOnly,All} — a policy over WHICH models pay for the (always delete-1/LOO) SE, not a delete-1-vs-bootstrap choice. There is no Bootstrap mode, no bootstrap_reps, and no resampling() accessor anywhere.
  - Spec L628 'enum class JackknifeMode { Delete1, Bootstrap };', L654 'JackknifeMode resampling()', L655 'int bootstrap_reps(); // 0 ⇒ delete-1 jackknife'. Reality: include/steppe/qpadm.hpp:54-58 'enum class JackknifePolicy : int { None=0, FeasibleOnly=1, All=2 }'; config_builder.cpp:349-352 maps --jackknife 0|1|2 to JackknifePolicy; grep for bootstrap_reps/resampling()/seed() on RunConfig returns nothing.
- `[medium/spec-vs-code]` The §9 Resources/PerGpuResources sketch does not match the real structs. Spec PerGpuResources carries StreamPool/DeviceAllocator*/CublasHandle/CusolverDnHandle fields plus 'NcclComm comm; // RAII-owned NCCL communicator', and Resources holds a SINGLE top-level ComputeBackend. Reality: PerGpuResources holds {device_id, unique_ptr<ComputeBackend>, BackendCapabilities} — the stream/blas/solver are owned one layer down inside CudaBackend, and NCCL is INTENTIONALLY ABSENT; the backend is PER-DEVICE (there is no single Resources::backend).
  - Spec L676-687 (PerGpuResources with NcclComm comm; Resources{unique_ptr<ComputeBackend> backend; vector<PerGpuResources> gpus;}). Reality: resources.hpp:134-148 'struct PerGpuResources { int device_id; std::unique_ptr<ComputeBackend> backend; BackendCapabilities caps; }'; resources.hpp:122-124 'NCCL is intentionally absent ... NEVER an NCCL AllReduce'; resources.hpp:154-181 Resources has gpus/config/last_combine_path/last_multigpu_timings, no single backend field.
- `[medium/spec-vs-code]` The §9 run_qpadm entry-point signature is stale. Spec: run_qpadm(const GenotypeDataset&, const QpAdmModel&, const RunConfig&, Resources&). Reality: it takes precomputed f2 blocks + QpAdmOptions + Resources — there is no GenotypeDataset type in the tree and run_qpadm takes QpAdmOptions, not RunConfig. The RunConfig header itself acknowledges the real entries take DeviceConfig/QpAdmOptions/PopSelection/FilterConfig directly.
  - Spec L688-689 'QpAdmResult run_qpadm(const GenotypeDataset&, const QpAdmModel&, const RunConfig&, Resources&)'. Reality: qpadm_fit.cpp:297-307 'run_qpadm(const device::DeviceF2Blocks& f2, const QpAdmModel& model, const QpAdmOptions& opts, device::Resources& resources)' (+ F2BlockTensor overload); grep 'GenotypeDataset' finds only run_config.hpp:8-13 noting it as a 'future' entry that the real code bypasses.
- `[medium/spec-vs-code]` Precision shape and the home of mantissa_bits are stale. Spec shows 'enum class Precision { EmulatedFp64, Fp64, Tf32 }' and a separate 'int mantissa_bits = 40;' field ON DeviceConfig (comments cite 'DeviceConfig::mantissa_bits'). Reality: Precision is a struct wrapping 'enum class Kind {Fp64,EmulatedFp64,Tf32}' plus its own mantissa_bits field and named factories; DeviceConfig has a Precision member but NO mantissa_bits field, so DeviceConfig::mantissa_bits does not exist.
  - Spec L613-627 'enum class Precision {...}', L615/L638 'DeviceConfig::mantissa_bits, default 40' / 'int mantissa_bits = 40;'. Reality: config.hpp:296-342 'struct Precision { enum class Kind {...}; Kind kind = Kind::EmulatedFp64; int mantissa_bits = kDefaultMantissaBits; ... };'; DeviceConfig config.hpp:396-534 has 'Precision precision;' but no mantissa_bits member.
- `[low/comment-vs-spec]` block_partition_rule.hpp comments cite the RunConfig accessor as 'RunConfig::block_size_cm' — matching the §9 sketch's block_size_cm() (L653) — but the real accessor is blgsize_cm(). The spec is itself internally inconsistent (sketch L653 'block_size_cm()' vs prose L671 'RunConfig::blgsize_cm()'); the code implements blgsize_cm(), so the sketch name and these comments are both stale. Spec L656 'seed()' accessor also has no counterpart on RunConfig.
  - block_partition_rule.hpp:16,85,172 reference 'RunConfig::block_size_cm'; run_config.hpp:97 defines 'double blgsize_cm() const noexcept'; no block_size_cm()/seed() accessor exists. Spec L653 'double block_size_cm()' vs L671 'RunConfig::blgsize_cm()'.
- `[low/spec-vs-code]` build() return type/signature is stale but the code documents the substitution. Spec: 'std::expected<RunConfig, Error> build() const'. Reality: non-const 'BuildResult<RunConfig> build()' returning unexpected(Status::InvalidConfig), because the toolchain is C++20 (no std::expected) and the layer's error type is Status, not Error — explicitly explained in the header.
  - Spec L665 'std::expected<RunConfig, Error> build() const'. Reality: config_builder.hpp:106 '[[nodiscard]] BuildResult<RunConfig> build();' and config_builder.hpp:97-105 explaining the C++20 BuildResult stand-in + Status/InvalidConfig; config_builder.cpp:214 'return unexpected(Status::InvalidConfig)'.

### §10 — Error handling, logging &amp; observability (lines 696-731)
code: **partial** · comments: **partial**

§10 splits cleanly into a solidly-implemented half and a substantially-stale half. ACCURATE: the domain-outcome error philosophy (RankDeficient/NonSpdCovariance/ChisqUndefined surfaced as ordinary statuses, error.hpp:31-45), and the entire CUDA fault mechanism (typed CudaError/CublasError/CusolverError thrown with file:line:name:string via STEPPE_CUDA_CHECK/CUBLAS_CHECK/CUSOLVER_CHECK, post-launch cudaGetLastError + debug forced-sync in STEPPE_CUDA_CHECK_KERNEL, RAII warn-on-teardown) all match check.cuh. STALE/DIVERGENT: (1) Logging — log.hpp does NOT wrap spdlog, has no RunConfig-driven sinks/levels, and there is no Python logging forwarder; only a WARN-only NDEBUG-stripped fprintf sink exists. (2) NVTX — the macro is STEPPE_NVTX_RANGE(name) with no color; the spec's STEPPE_NVTX_SCOPE + nvtx::Color palette do not exist. (3) The \"Standard ranges\" list matches zero actual range labels. (4) STEPPE_TRY and std::expected<T,Error> propagation do not exist (a BuildResult<T> over Status is used); there is no Error type. (5) The taxonomy table lists IO_FORMAT and CUDA_RUNTIME codes absent from the as-built 5-value Status enum. §10-referencing code comments (error.hpp, check.cuh, log.hpp, nvtx.hpp) are accurate to the code and in the log/nvtx cases actively flag that the spec is the stale side. code_alignment=partial, comment_alignment=partial (comments track code, not the stale spec). All file paths cited are under /home/suzunik/steppe.

- `[high/spec-vs-code]` §10 (architecture.md:715) asserts, present tense, that `internal/log.hpp` "wraps spdlog behind STEPPE_LOG_* so sinks/levels/async are swappable; levels and sinks come from RunConfig, not globals" and that "The Python binding installs a sink forwarding to Python's logging." None of this is built. src/core/internal/log.hpp:44-54 is a bare printf-style `std::fprintf(stderr, "[steppe][warn] " ...)` sink; only ONE level (STEPPE_LOG_WARN) is realized and it is compiled out entirely under NDEBUG. The header itself states (lines 16-17) the fuller levels are "reserved for the structured-logging backend." spdlog appears nowhere in src/ (grep hits only docs). There is no RunConfig-driven sink/level wiring, and no Python logging forwarder in bindings/ (grep for logging/sink across bindings/ returns nothing).
  - src/core/internal/log.hpp:44-54 (fprintf sink, WARN-only, NDEBUG-stripped); log.hpp:16-17 spdlog "reserved"; grep spdlog -> only docs/archive/architecture.md; bindings/ has no logging sink. Contradicts architecture.md:715.
- `[high/spec-vs-code]` §10 observability (architecture.md:717) claims the NVTX helper emits "named, colored ranges" with a color palette "nvtx::Color ... defined once in that header — no per-call-site color literals," and the code example (architecture.md:721) calls `STEPPE_NVTX_SCOPE("f2_block_kernel", nvtx::Color::Green)`. The real macro is `STEPPE_NVTX_RANGE(name)` — a SINGLE string-literal argument with NO color (src/core/internal/nvtx.hpp:47-48, 52). There is no `nvtx::Color` type anywhere (grep for nvtx::Color / Color:: returns nothing), and no color palette. All 8 call sites use STEPPE_NVTX_RANGE(name-only). Both the macro name and the entire color-palette concept in §10 are divergent.
  - src/core/internal/nvtx.hpp:47-52 defines STEPPE_NVTX_RANGE(name) (no color); grep nvtx::Color -> empty; call sites e.g. cuda_backend_f2_blocks.cu:380 STEPPE_NVTX_RANGE("f2_gemm"). Contradicts architecture.md:717 and :721.
- `[medium/spec-vs-code]` §10 "Standard ranges" (architecture.md:728) enumerates `read_genotypes, decode_af, f2_blocks, f4_contract, jackknife_cov, rank_test, gls_fit`. NONE of these labels exist in the code. The actual NVTX range labels are: decode, f2_gemm, jackknife, qpfstats, qpadm_fit, qpfstats_smooth_prep, dates_curve, dates_fit_curves. So `decode_af`→`decode`, `f2_blocks`→`f2_gemm`, `jackknife_cov`→`jackknife`, and `read_genotypes`/`f4_contract`/`rank_test`/`gls_fit` are absent entirely. The list is stale.
  - grep STEPPE_NVTX_RANGE across src/device/cuda/*.cu: decode(cuda_backend_decode.cu:112), f2_gemm(cuda_backend_f2_blocks.cu:380), jackknife(cuda_backend_dstat.cu:123), qpadm_fit(cuda_backend_qpadm_fit.cu:876), qpfstats(cuda_backend_qpfstats.cu:229), dates_curve(cuda_backend_dates.cu:43). Contradicts architecture.md:728.
- `[medium/spec-vs-code]` §10 "Errors mechanism" (architecture.md:713) states "Internal code propagates `std::expected<T, Error>` via `STEPPE_TRY`." Neither exists in the tree. `STEPPE_TRY` is defined/used nowhere (grep hits only docs). `std::expected` is not used for propagation; the config layer instead uses a hand-rolled `BuildResult<T>` documented as a "stand-in for std::expected<T, Status>" — over `Status`, not `Error` (src/core/config/build_result.hpp:3-12). There is no `Error` type at all (include/steppe/error.hpp defines only `enum class Status`); config_builder.hpp:98-103 explicitly notes the arch's `std::expected<RunConfig, Error>` was realized as Status instead. The CUDA half of the same paragraph (CudaError thrown via STEPPE_CUDA_CHECK with file/line/name/string; CUBLAS_CHECK/CUSOLVER_CHECK translating status enums; post-launch cudaGetLastError; debug forced-sync; RAII warn-on-teardown) IS accurate (src/device/cuda/check.cuh:68-82,256-266,332-337).
  - grep STEPPE_TRY -> only architecture.md; src/core/config/build_result.hpp:3-12 (BuildResult = stand-in over Status); config_builder.hpp:98-103; no Error type in include/steppe/error.hpp. Contradicts architecture.md:713.
- `[medium/spec-vs-code]` The §10 taxonomy table (architecture.md:700-709) lists 8 rows including `STEPPE_ERR_IO_FORMAT` (704) and `STEPPE_ERR_CUDA_RUNTIME` (706), and the text (698) says internal code "carries the richer Error with a matching category and a message." The as-built enum (include/steppe/error.hpp:23-50) has only 5 error values — DeviceOom, RankDeficient, NonSpdCovariance, ChisqUndefined, InvalidConfig — with NO IoFormat and NO CudaRuntime, and there is no `Error` type or `category` field (the header calls the richer Error "DEFERRED to M(abi-1)"). Consequently the public surface cannot actually return a CUDA-runtime or IO-format status even though §10's mechanism text and check.cuh:65-67 describe mapping to them; IO-format failures are surfaced as `throw std::runtime_error` in the io leaf, not a Status. §10 partially self-caveats this (698: "as built the same-toolchain C++ surface returns the Status enum directly"), so this is partial rather than outright wrong, but the table still overstates the as-built taxonomy.
  - include/steppe/error.hpp:23-50 (5 error values, no IoFormat/CudaRuntime, no Error/category); check.cuh:65-67 maps to STEPPE_ERR_CUDA_RUNTIME that has no enum home; io leaf throws std::runtime_error (src/io/geno_reader.hpp:158). Contradicts architecture.md:698,704,706.
- `[low/comment-vs-spec]` The log.hpp file header (src/core/internal/log.hpp:16-17) says "The fuller STEPPE_LOG_INFO/ERROR/... levels are reserved for the structured-logging backend (§10)" — i.e. spdlog is future/unbuilt — which directly contradicts §10's present-tense claim (architecture.md:715) that log.hpp already "wraps spdlog." The comment matches the code and reality; the spec section is the stale side. (Note the spec is even internally inconsistent: its own component table at architecture.md:594 says "a spdlog backend lands with §10," i.e. future.)
  - src/core/internal/log.hpp:16-17 vs architecture.md:715; corroborated by architecture.md:594.
- `[low/comment-vs-spec]` The nvtx.hpp file header (src/core/internal/nvtx.hpp:4,29-31) documents the helper as `STEPPE_NVTX_RANGE(name)` at "COARSE phase boundaries" with no mention of color — matching the code but contradicting §10's `STEPPE_NVTX_SCOPE(name, nvtx::Color::...)` and its "color palette defined once" claim (architecture.md:717,721). The comment is accurate; the section text is stale.
  - src/core/internal/nvtx.hpp:4,29-31,47-48 vs architecture.md:717,721.

### §11 — Memory &amp; performance strategy (docs/archive/architecture.md:732-793)
code: **partial** · comments: **aligned**

Most of §11 accurately matches the M5 code: the adaptive OutputTier / select_output_tier policy (src/device/tier_select.hpp), the co-resident 2·P²·B f2+Vpair reservation (src/device/vram_budget.hpp, kResidentTensorCount=2), DeviceF2Blocks, PinnedBuffer, block_partition_rule, the STPF2BK1 disk format, the fixed-order cudaMemcpyPeerAsync P2P combine (src/device/cuda/p2p_combine.cu), and the deferred-to-single-GPU S8 rotation (TODO(multigpu-host-bounce)). All §11-citing code comments track the section correctly (comments-vs-spec: aligned). Three specific mechanism claims are stale/never-built: (1) §11.2 line 768's central 'validated at build()' budget-reject via ConfigBuilder::build()+cudaMemGetInfo+STEPPE_ERR_DEVICE_OOM (build() is CUDA-free static validation returning InvalidConfig; the VRAM-enforcing build() is explicitly documented as not existing; DeviceOom is a mid-run mapping at cuda_backend.cu:289-308); (2) §11.2 line 768's 'set the pool release threshold to UINT64_MAX' (no CUDA memory pool exists — the sole allocator is synchronous cudaMalloc/cudaFree in device_buffer.cuh:83,154); (3) §11.1 line 751's claim that extract-f2 --dry-run prints T_max / tile count / largest-dataset-that-fits (cmd_extract_f2.cpp:242-274 prints the adaptive tier verdict, result size, and free VRAM/host RAM instead; no bytes_per_tile/T_max symbol exists).

- `[high/spec-vs-code]` docs/archive/architecture.md:768 (§11.2, plus the §11.2 header 'validated at build()' and §11.1:751). Spec: 'ConfigBuilder::build() sums the resident terms for the configured K/T, compares against cudaMemGetInfo free memory ... rejects over-budget configs up front (STEPPE_ERR_DEVICE_OOM) ... The single expression validated is total_vram(P, B, m, K, T) <= budget * free.' No such build()-time VRAM gate exists. ConfigBuilder::build() DOES exist but is CUDA-FREE BY CONTRACT: it does only STATIC validation (token legality, ranges, GPU-only) and returns Status::InvalidConfig, never a cudaMemGetInfo sum or DeviceOom. No total_vram(P,B,m,K,T) function exists (only the total_vram_bytes capability field), and the m^2 (Q), m*B (X), and K-batch table terms are summed nowhere. The actual DeviceOom is produced MID-RUN by mapping cudaErrorMemoryAllocation, or by select_output_tier when a streamed chunk cannot fit — not by an up-front build() rejection.
  - config_builder.hpp:25-31 ('CUDA-FREE BY CONTRACT ... makes no CUDA call ... device/VRAM check ... is the APP's job at run time'), :106 build()->BuildResult<RunConfig> (InvalidConfig only); resources.hpp:194 'until ConfigBuilder::build() exists.'; block_partition_rule.hpp:64 'if/when it lands.' and :189 'as ConfigBuilder::build() does not exist.'; block_partition_rule.cpp:109 same; mid-run OOM mapping cuda_backend.cu:289-308 (cudaErrorMemoryAllocation -> Status::DeviceOom); grep: no total_vram(...) function.
- `[medium/spec-vs-code]` docs/archive/architecture.md:768 (§11.2). Spec asserts a device memory pool: 'Set the pool release threshold to UINT64_MAX so variable-size per-replicate allocations recycle from cache instead of round-tripping the OS.' No CUDA memory pool exists anywhere in src/: the sole allowlisted device allocator uses synchronous cudaMalloc/cudaFree, and there is no cudaMallocAsync, cudaMemPoolCreate, cudaMemPoolSetAttribute, cudaMemPoolAttrReleaseThreshold, or UINT64_MAX in the source. device_buffer.cuh treats a memory pool as a hypothetical 'future switch'; the per-chunk cudaMalloc/cudaFree churn the pool would avoid is instead mitigated by pre-sizing chunk slabs ONCE. Corroborating: config.hpp:421-425 (which cites §11.2) doubles down on the same non-existent pool ('the device path uses the pool-backed (cudaMallocAsync) allocator UNCONDITIONALLY'), yet that field is explicitly 'FORWARD-RESERVED (NOT wired)'.
  - device_buffer.cuh:5 (only TU permitted to call cudaMalloc/cudaFree), :83 STEPPE_CUDA_CHECK(cudaMalloc(...)), :154 cudaFree(ptr_), :143-148 'RE-TRIGGER WARNING: a future switch to cudaMallocAsync / a per-device memory pool ...'; cuda_backend_f2_blocks.cu:493-499 (per-chunk cudaMalloc/cudaFree churn mitigated by pre-sizing slabs, not a pool); config.hpp:421-425 (use_mem_pool FORWARD-RESERVED, NOT wired); grep: no cudaMemPool*/ReleaseThreshold/UINT64_MAX in src/.
- `[medium/spec-vs-code]` docs/archive/architecture.md:751 (§11.1 'Chunk-size derivation' box). Spec: 'extract-f2 --dry-run prints T_max, the number of tiles, and the largest dataset (in SNPs x samples) that fits a given VRAM at the chosen budget.' The actual --dry-run prints none of these: geno/snp/ind paths, P, precision, blgsize->n_block, filters, free VRAM + free host RAM, f2 slab bytes/block and result MiB, and the adaptive OutputTier verdict (resident/host/disk). No T_max, no tile count, no 'largest dataset that fits' line. No bytes_per_tile(T)/T_max symbol exists — the M5 streamed path tiles per-block-chunk via max_blocks_per_chunk, not by a single global SNP tile T. This box describes the pre-M5 SNP-tiling/dry-run model that the rest of §11.1 (points 1-4, updated to the DeviceF2Blocks/OutputTier M5 design) supersedes.
  - cmd_extract_f2.cpp:242-274 (full --dry-run printf block; fields as listed; returns kExitOk at :281 so no other dry-run output exists); vram_budget.hpp:169-186 max_blocks_per_chunk is the real per-block-chunk tiling; grep found no bytes_per_tile / T_max symbol in code.

### §12 — Numerical correctness &amp; reproducibility (lines 794-827)
code: **partial** · comments: **partial**

§12's precision/emulation policy and cuSOLVER-determinism claims are faithfully implemented and verifiable in code: emulation_honorable is the single honorability predicate consulted by both f2_compute_type and engage_f2_precision (f2_block_kernel.cu), CUSOLVER_DETERMINISTIC_RESULTS is defensively pinned in the CusolverDnHandle ctor with the exact gesvd/gesvdj scope and a test_handles.cu assertion, large_svd_V uses gesvdj-when-both-dims<=32-else-gesvd under a rows>=cols orientation rule (kGesvdjMaxDim=32), the custom fixed-order Jacobi (dev_jacobi_svd_V) exists, and the blgsize block_of rule is present. However several concrete claims are stale or unbuilt: (1) HIGH — the cuda_backend.cu:4221-4226 / 'large_svd_V in cuda_backend.cu' citation is stale after the cuda-backend-split (file is now 314 lines; code moved to cuda_backend_qpadm_fit.cu); (2) MED — the CCCL run_to_run / cub::DeviceReduce reduction API the section names is used nowhere (reductions are cub::BlockReduce/DeviceScan/DeviceSelect); (3) MED — 'stream_count=1 enforced by build() when deterministic' has no enforcing code (field defaults to 1 but nothing reads/forces it); (4) LOW — the cuRAND Philox seed-control paragraph is entirely absent from production code (only seedless delete-1 jackknife is built). Two code comments (handles.hpp:350, test_handles.cu:222) echo the same stale cuda_backend.cu citation.

- `[high/spec-vs-code]` The §12 cuSOLVER-scope bullet cites `large_svd_V` in `cuda_backend.cu` and the orientation rule at `cuda_backend.cu:4221-4226`. That file was split by the cuda-backend-split refactor and is now only 314 lines total, so lines 4221-4226 do not exist. `large_svd_V` and the `rows=max(nl,nr) >= cols=min(nl,nr)` orientation rule now live in `cuda_backend_qpadm_fit.cu` (orientation at :318 / :351 / :378, gesvdj/gesvd dispatch at :311-404). The described behavior is correct and present, but the file:line citation is stale.
  - wc -l cuda_backend.cu = 314; grep found large_svd_V + orientation only in src/device/cuda/cuda_backend_qpadm_fit.cu:318 (`rows>=cols ✓ (the §1.3 orientation)`), :351, :378; spec line 808 says `cuda_backend.cu:4221-4226`.
- `[medium/spec-vs-code]` §12 bullet 1 states reductions use the CCCL `run_to_run` single-phase API — `cub::DeviceReduce::Sum(..., cuda::execution::require(det::run_to_run)) // default`. No `cub::DeviceReduce`, `cuda::execution::require`, or `determinism::` symbol appears anywhere in src/ or include/. The statistic-path reductions are actually `cub::BlockReduce` (hand-rolled per-block) plus `cub::DeviceScan::ExclusiveSum` / `cub::DeviceSelect::Flagged` / `cub::DeviceRadixSort`. The named run_to_run reduction-determinism mechanism the section leans on is not wired into the code.
  - grep of cub:: over src yields only BlockReduce(1), DeviceRadixSort::SortPairsDescending(4), DeviceScan::ExclusiveSum(6), DeviceSelect::Flagged(24); `run_to_run` occurs only in include/steppe/config.hpp comment + tests/reference/test_f2_multigpu_parity.cu comment; BlockReduce itself only in a comment at qpfstats_jackknife_kernel.cu:19.
- `[medium/spec-vs-code]` §12 bullet 2 claims the statistic-bearing GEMM/SYRK/Cholesky/solve run on one stream with `stream_count = 1` `enforced by build() when deterministic`. No code reads or forces `stream_count` anywhere (src/app/include/bindings) — only the struct field (default 1) and its own doc-comment exist. ConfigBuilder::build() (config_builder.hpp:84-106) documents its validations and never mentions stream_count or deterministic enforcement. The claimed build() enforcement is not implemented; determinism currently holds only because the default value happens to be 1.
  - grep stream_count across src=none; app/include/bindings only include/steppe/config.hpp:413 (field=1) and :502/:512 (its own comment). config_builder.hpp build() validation list omits it entirely.
- `[low/spec-vs-code]` The Seed-control paragraph describes cuRAND Philox (`CURAND_RNG_PSEUDO_PHILOX4_32_10`), independent substream via `subsequence = rep`, and a seed threaded through the API + recorded in golden metadata, with a curandCreate/SetSeed/SetGeneratorOffset code snippet. None of this exists in production code — curand/Philox appear nowhere in src/include/app; the only curand hit is a benchmark (tests/reference/bench_optimizers.cu). The built resampling path is the seedless delete-1 block jackknife; bootstrap RNG resampling is unimplemented, so this paragraph describes an unbuilt mechanism in present tense.
  - grep curand/PHILOX/curandCreateGenerator over src+include+app+tests = only tests/reference/bench_optimizers.cu and tests/CMakeLists.txt; statistic resampling is qpfstats_jackknife_kernel.cu (delete-1 jackknife, no RNG).
- `[low/comment-vs-spec]` Two code comments repeat the section's stale `cuda_backend.cu` citation for the rank-test SVD: handles.hpp:350-351 says `(large_svd_V, cuda_backend.cu)` and test_handles.cu:222 says `(the §1.3 orientation rule, cuda_backend.cu:4221-4226)`. They match the section text but, like the section, are stale relative to the real location (cuda_backend_qpadm_fit.cu). Other §12-referencing comments (handles.hpp ctor, config.hpp:500-502) accurately mirror the section.
  - src/device/cuda/handles.hpp:350-351 `large/NRBIG rank-test SVD (\`large_svd_V\`, cuda_backend.cu)`; tests/reference/test_handles.cu:222 `cuda_backend.cu:4221-4226`; actual impl in cuda_backend_qpadm_fit.cu:342/407.

### §13 — "Testing strategy" (docs/archive/architecture.md lines 828-842)
code: **partial** · comments: **aligned**

§13 is code-verifiable and largely present, but has several concrete stale/divergent claims. Strongest: the section cites the regression-golden directory as `tests/golden/`, which does not exist — the AT2 est/se/z/p goldens with pinned-environment metadata actually live under `tests/reference/goldens/at2/` (docs/archive/architecture.md:834). Second, the described `CudaTest` fixture with per-suite device reset and a `TearDown` clean-error assertion is nowhere in the code (zero hits for CudaTest/TearDown/SetUp; test_cuda_check.cu is a plain main()). Third, of the four numerics claimed as host-.cpp tested, only f2 estimator (test_f2.cpp) and block_of (test_block_partition.cpp) actually are — f4 contraction and jackknife weighting have no host .cpp unit test. Fourth, the enumerated algebraic 'property tests' (f4 half-sum identity, f3==f4, Q PSD, f4 antisymmetry) were not found as dedicated tests. Fifth, compute-sanitizer wires only memcheck (SteppeSanitizers.cmake), not all four tools, and no CI YAML exists. Aligned parts: GoogleTest harness, CpuBackend/CudaBackend equivalence seam under tests/reference/, and all three BUILT tests exist with exactly the cited commit hashes (c8fe397 domain, 2496a14 missing-block, 6481dfa qpwave). Code comments that cite '§13' reference the accurate parts of the section ('Unit tests', 'exercisable GPU-free') and are aligned; none reference the stale CudaTest fixture or tests/golden path.

- `[high/spec-vs-code]` The section cites the regression-golden directory as `tests/golden/`, but no such directory exists. The ADMIXTOOLS 2 est/se/z/p regression goldens with the pinned-environment metadata actually live under `tests/reference/goldens/at2/` (e.g. golden_fit0.json, golden_fit1_NRBIG.json, golden_fitNA.json, golden_qpwave.json). There is no top-level tests/golden split from tests/reference — regression goldens are nested inside the reference tree. The only occurrences of the `tests/golden/` path in the repo are in docs (architecture.md itself and docs/archive/design/fit-engine.md, which describe an aspirational tests/golden/f2/... and tests/golden/qpadm/... layout that was never created).
  - docs/archive/architecture.md:834 says '**Regression (`tests/golden/`).**'; `ls tests/golden` -> 'No such file or directory'; actual goldens: tests/reference/goldens/at2/golden_fit0.json etc.; README at tests/reference/goldens/at2/README.md:1-6 ties them to §12/§13.
- `[medium/spec-vs-code]` The section claims 'A `CudaTest` fixture resets the device per suite and asserts a clean error state in `TearDown` so a leaked sticky error can't cascade.' No such fixture exists. A repo-wide grep for `CudaTest`, `TearDown`, and `SetUp` (over .cu/.cuh/.cpp/.h/.hpp) returns zero hits. The representative CUDA control-flow test (tests/reference/test_cuda_check.cu) is a plain `int main()` with printf, not a GoogleTest fixture, and does no per-suite device reset or TearDown error-state assertion.
  - docs/archive/architecture.md:832 ('A `CudaTest` fixture ... asserts a clean error state in `TearDown`'); grep -rl 'TearDown|CudaTest' over source = no matches; tests/reference/test_cuda_check.cu:123 is `int main()` with no fixture.
- `[medium/spec-vs-code]` The section states the pure numerics '(f2 estimator, f4 contraction, jackknife weighting, `block_of`) tested in plain `.cpp` on the host.' Only two of the four are actually host-.cpp unit-tested: f2 estimator (tests/unit/test_f2.cpp) and block_of (tests/unit/test_block_partition.cpp). There is NO host `.cpp` test for f4 contraction (grep for f4/compute_f4/f4_contract in tests/unit/ returns nothing) and NO host `.cpp` numeric test for jackknife weighting — the jackknife-weighting code lives in .cu kernels (src/device/cuda/ratio_block_jackknife_kernel.cu, qpfstats_jackknife_kernel.cu, f2_block_kernel.cu) and is exercised only via .cu reference/parity tests; the only 'jackknife' mentions in tests/unit are CliArgs/JackknifePolicy plumbing (test_config_builder.cpp), not the weighting numeric.
  - docs/archive/architecture.md:832; grep 'f4' tests/unit/ = no files; grep 'jackknife' tests/unit/ = test_config_builder.cpp:75/160/247/253 (config plumbing) + test_block_partition.cpp:6 (comment about block id), not weighting numerics; jackknife impl in src/device/cuda/*.cu.
- `[low/spec-vs-code]` The 'Property tests' bullet enumerates value-independent algebraic identities as tests: f4(A,B;C,D)==½(f2(A,D)+f2(B,C)-f2(A,C)-f2(B,D)), f3(A;B,C)==f4(A,B;A,C), Q symmetric PSD, and f4 antisymmetry under index swaps. No dedicated property/identity test asserting any of these was found. The closest match is a descriptive comment on the f3-triple gather identity inside a benchmark (tests/reference/bench_optimizers.cu:25), not a property assertion; the shipped tests are reference-equivalence and AT2 goldens (value-dependent), not identity/property tests.
  - docs/archive/architecture.md:835; grep for the half-sum identity / 'antisymmet' / 'f3 == f4' across tests/ finds only bench_optimizers.cu:25 comment ('assemble_f3_triples_gather identity f3(C;i,j)=0.5*(...)'), no EXPECT/ASSERT-style property test.
- `[low/spec-vs-code]` The compute-sanitizer bullet claims 'All four tools ... memcheck --leak-check full, racecheck --racecheck-report analysis, initcheck --track-unused-memory, synccheck' plus '--track-stream-ordered-races all'. In the code only memcheck is wired: cmake/SteppeSanitizers.cmake documents/legalises just `compute-sanitizer --tool memcheck ctest ...` and sets the STEPPE_SANITIZER_COMPUTE marker; racecheck/initcheck/synccheck and the stream-ordered-races flag are not wired anywhere (grep over .cmake/.yml/.yaml/.sh/CMakeLists returns nothing). There is also no CI workflow file in the repo (.github/workflows does not exist); the CI runner is deferred to docs/archive/kimiactions/02-ci-plan.md. Note this bullet is partly CI-scope (overlaps §14), so it is only partially code-verifiable, but the code-visible portion implements 1 of the 4 named tools.
  - docs/archive/architecture.md:839; cmake/SteppeSanitizers.cmake comment 'compute-sanitizer --tool memcheck ctest --test-dir build-rel' (memcheck only); grep 'racecheck|initcheck|synccheck' over build files = no matches; no .github/workflows directory.

### §14 — CI/CD &amp; tooling
code: **stale** · comments: **none**

architecture.md §14 (CI/CD & tooling) is largely STALE/aspirational relative to the code. The entire GitHub Actions pipeline it specifies — the 5 stages, the self-hosted GPU runner matrix YAML, sccache/GHA, the pre-commit hooks, and the CCCL_VERSION>=3.1 assertion — has NO implementation: there is no .github/ directory and no CI/pre-commit config anywhere in the repo. The one concrete, code-touching prescription (the CUDA-13 wheel model) is not merely stale but deliberately REVERSED by pyproject.toml: the spec says to declare unsuffixed NVIDIA pip packages in [project.dependencies], while the code treats the CUDA-13 runtime as an unbundled SYSTEM requirement and pins only suffixed nvidia-*-cu13 in an optional extra, explaining the spec's approach would make the wheel pip-uninstallable. The spec's auditwheel exclude list also names the wrong libs (curand + cusolver.so.13, omitting the actually-linked cufft; real cusolver SONAME major is 12). What DOES have backing is peripheral: scikit-build-core + nanobind (pyproject/bindings) and the compute-sanitizer lane machinery (cmake/SteppeSanitizers.cmake), though nothing invokes the sanitizer in CI. comment_alignment is 'none': no code comment cites architecture.md §14 — the numerous '§14.2' comments in src/device/cuda/ refer to an unrelated performance/SVD-arena findings doc, not this CI/CD section.

- `[high/spec-vs-code]` The entire CI pipeline described in §14 has no implementation in the repo. Spec lines 845-868 describe a 5-stage GitHub Actions pipeline (PR no-GPU ubuntu-latest lane, PR GPU runner, PR scoped compute-sanitizer lane, nightly sanitizer/nvbench sweep, on-demand wheels), a self-hosted GPU runner matrix YAML (lines 854-864), sccache with the GHA backend SCCACHE_GHA_ENABLED=true (line 866), and format/tidy/IWYU gates (line 868). None of it exists: there is no .github/ directory and no *.yml/*.yaml file anywhere in the tree (outside .git).
  - `ls .github` -> 'No such file or directory'; `find . -path ./.git -prune -o \( -name '*.yml' -o -name '*.yaml' \) -print` returns nothing. The only build automation present is CMake/CMakePresets + pyproject; even the repo's own pyproject.toml:56-58 calls the lint lane 'the future CUDA-free CI lane'.
- `[high/spec-vs-code]` The §14 wheel dependency model is DELIBERATELY REVERSED by the actual pyproject.toml. Spec line 872 says to 'declare a runtime dependency on NVIDIA's pip packages' and line 881 says 'Pin the unsuffixed runtime packages in pyproject.toml [project.dependencies]' and 'The dependency must be declared, or the wheel is broken at import.' The real pyproject.toml declares numpy as the ONLY hard dep (42-44), explicitly documents the CUDA-13 runtime as a SYSTEM requirement resolved at load and NOT a pip dep (lines 7-18, 38-41), and the only NVIDIA pins are the SUFFIXED nvidia-*-cu13 packages inside an OPTIONAL `cuda` extra (65-70) — not unsuffixed, not in [project.dependencies]. The file even states pinning the cu13/unsuffixed wheels 'makes the wheel pip-uninstallable' because they are not yet published with a real >=13 version (lines 12-16).
  - pyproject.toml:42-44 (`dependencies = ["numpy>=1.22"]`), :59-70 (optional `cuda` extra with suffixed nvidia-*-cu13), :7-18 header note explicitly rejecting the pip-dep model. Contradicts architecture.md:872 and :881.
- `[high/spec-vs-code]` The auditwheel-repair exclude list in §14 names the wrong CUDA library set, and no cibuildwheel/auditwheel config exists at all. Spec lines 875-876 exclude libcudart.so.13, libcublas.so.13, libcusolver.so.13, libcurand.so.13. The actual link set is libcudart.so.13, libcublas.so.13, libcusolver.so.12, libcufft.so.12: (a) curand is NOT linked anywhere, (b) cusolver's SONAME major is 12, not 13, and (c) cufft (the DATES autocorrelation LD engine) IS linked but is absent from the spec's exclude list. There is also no cibuildwheel over scikit-build-core wiring (no CIBW_*, no CIBW_MANYLINUX_X86_64_IMAGE, no auditwheel invocation) despite the section header claiming '(verified)'.
  - src/device/CMakeLists.txt:73 links `CUDA::cudart CUDA::cublas CUDA::cusolver CUDA::cufft` (no curand); README.md:26 and pyproject.toml:8-9 both state DT_NEEDED = libcudart.so.13 / libcublas.so.13 / libcusolver.so.12 / libcufft.so.12; grep for cibuildwheel|auditwheel|CIBW_|manylinux outside docs/ finds only one comment ('a future auditwheel-repair', pyproject.toml:63).
- `[medium/spec-vs-code]` No pre-commit configuration exists. Spec line 870 describes a .pre-commit-config.yaml wiring mirrors-clang-format (types_or: [c++, c, cuda]) and cmake-pre-commit-hooks clang-tidy. There is no .pre-commit-config.yaml in the repo; only bare .clang-format and .clang-tidy config files exist, with nothing that invokes them as pre-commit hooks.
  - `ls .pre-commit-config.yaml` -> 'No such file or directory'; root contains .clang-format and .clang-tidy but no pre-commit wiring.
- `[medium/spec-vs-code]` No `CCCL_VERSION >= 3.1` assertion exists. Spec line 846 claims 'CI asserts CCCL_VERSION >= 3.1 (§3).' No build file performs any CCCL version check — grepping CCCL across all *.cmake/*.txt/*.cu/*.cuh/*.cpp/*.hpp turns up only descriptive comments about CCCL CUB usage in kernels, never a version assertion. (Compounded by finding 1: there is no CI to assert it in.)
  - grep -rn 'CCCL_VERSION|CCCL' over build + source files: only src/device/cuda/cuda_backend*.cu comments referencing 'CCCL CUB'; zero version guards in CMakeLists.txt, CMakePresets.json, or cmake/*.cmake.
- `[low/spec-vs-code]` The GPU runner matrix and cache tool diverge from the real target. Spec lines 858-861 pin CUDA 13.1/13.3 and arches sm_80 (Ampere)/sm_90 (Hopper); the actual build targets Blackwell sm_120 by default (CUDAArch.cmake:42, pyproject.toml:93) as a fatbinary covering sm_75-sm_120 (README.md:25), with CUDA verified on 13.0 and 13.1 (README.md:26), not 13.3. Spec line 846/866 specify sccache with the GHA backend; the repo instead configures ccache as a local build-iteration launcher (CMakeLists.txt:71, STEPPE_CCACHE default ON) and has no sccache anywhere.
  - cmake/CUDAArch.cmake:42 `set(STEPPE_CUDA_ARCH "120" ...)`, pyproject.toml:93 `-DSTEPPE_CUDA_ARCH=120`, README.md:25-26; CMakeLists.txt:71 ccache launcher; no sccache reference in the tree.

### §15 — Python bindings & packaging
code: **partial** · comments: **aligned**

§15's toml-level facts are accurate (nanobind over pybind11 / ADR-0002, scikit-build-core PEP517 backend, numpy-only hard dep, the exact cuda optional-extra set, build.targets=[\"_core\"], wheel.packages, requires-python>=3.9, and the CMakeLists version regex all match). But the section's prose is materially STALE on the CPU story: it claims a CpuBackend fallback makes import/smoke tests pass GPU-free and that pytest asserts GPU-vs-CPU equivalence, and it references a CPU-only wheel variant. The real build is a single GPU-only wheel (pyproject.toml:3) with NO CpuBackend in the bindings — GPU calls fail fast via raise_no_device (bind_common.hpp:90-113) and the Python tests skip on no-device and compare against AT2 goldens, not CPU. Minor: module.cpp is now just the NB_MODULE assembler (marshalling moved to bind_common.hpp/bind_*.cpp; no `span_view` type), and the real pyproject uses cmake.args + mandatory Release + abi3 stable-ABI packaging not shown in §15. COMMENTS: the only §15-citing comments (bindings/CMakeLists.txt:14,46 crediting nanobind to \"ADR-0002, architecture §15\") are consistent with the section.

- `[high/spec-vs-code]` §15 (line 889) claims: "Because the backend is injected and `CpuBackend` always exists, `import steppe` and smoke tests pass on GPU-free CI runners and laptops; `pytest` asserts GPU-vs-CPU equivalence wherever a device is present." The current bindings wire NO CpuBackend at all — `grep CpuBackend bindings/` returns NONE. Instead the binding is a hard GPU-product fail-fast: ensure_resources() raises `raise_no_device()` ("no CUDA device available (steppe is a GPU product...)") when no GPU is found. The Python suite does NOT assert GPU-vs-CPU equivalence; it asserts GPU output against committed AT2 CSV/binary goldens (rtol 1e-6 / 1e-12) and SKIPS cleanly when no CUDA device is visible (maybe_skip_no_gpu). So the CpuBackend-fallback mechanism the section credits for CI/laptop passing does not exist in the wheel.
  - bindings/internal/bind_common.hpp:90-113 (raise_no_device / ensure_resources fail-fast); grep 'CpuBackend' over bindings/ => NONE; tests/python/conftest.py:161-166 (maybe_skip_no_gpu skips on no device); tests/python/test_py_f4.py:2-13 (compares to AT2 golden, not CPU); contradicts architecture.md:889
- `[high/spec-vs-code]` The §15 footnote (line 930) states "a CPU-only wheel variant omits them [CUDA runtime deps] (the loader resolves the CUDA libs lazily and `CpuBackend` needs none)." The real packaging has no such variant: pyproject.toml's header comment declares "ONE GPU wheel, NO CPU-only variant (memory cpu-is-test-only): steppe is a GPU product." There is a single GPU wheel; the CPU backend is test-only and never shipped as a wheel flavor.
  - pyproject.toml:3 ("ONE GPU wheel, NO CPU-only variant ... steppe is a GPU product"); contradicts architecture.md:930
- `[medium/spec-vs-code]` §15 (line 887) says "The binding (`bindings/module.cpp`) is thin — it converts NumPy arrays to/from `span_view`s and forwards into the public API." bindings/module.cpp no longer does any conversion: it is only the NB_MODULE assembler shell that calls register_f2handle/register_qpadm/register_qpgraph/register_fstats/register_dates. The numpy<->span marshalling now lives in bindings/internal/bind_common.hpp plus the per-tool bind_*.cpp TUs, not in module.cpp. Also the type is std::span / nanobind ndarray, not a `span_view` (grep 'span_view' over bindings/ => none). The 'thin / no compute logic' characterization still holds, but the named file no longer performs the described marshalling.
  - bindings/module.cpp:22-40 (NB_MODULE shell delegating to register_*); bindings/internal/bind_common.hpp:4 & includes <span> (marshalling home); grep 'span_view' bindings/ => none; contradicts architecture.md:887
- `[low/spec-vs-code]` The §15 pyproject excerpt drives the CMake defines via a `[tool.scikit-build.cmake.define]` table with only STEPPE_BUILD_PYTHON=ON / STEPPE_BUILD_CLI=OFF. The real pyproject instead uses `cmake.args = [-DSTEPPE_BUILD_PYTHON=ON, -DSTEPPE_BUILD_CLI=OFF, -DSTEPPE_BUILD_TESTS=OFF, -DSTEPPE_CUDA_ARCH=120]`, mandates `cmake.build-type = "Release"`, and adds a STABLE-ABI single-wheel strategy `wheel.py-api = "cp312"` (abi3) — none of which the section mentions. Functionally the two defines match, but the Release-mandatory build, the sm_120 arch pin, and the abi3 packaging are undocumented in §15. (The numpy-only hard dep, the cuda optional-extra set, build.targets=["_core"], wheel.packages=["bindings/steppe"], requires-python>=3.9, cmake.version>=3.28, and the version regex all match exactly.)
  - pyproject.toml:79-101 (cmake.args/build-type/py-api) vs architecture.md:915-927

### §16 — Documentation & ABI/versioning
code: **partial** · comments: **aligned**

§16's core ABI narrative is accurate at HEAD — there is no C ABI, include/steppe/ is a full-C++ convenience layer (error.hpp defines `enum class Status`, no steppe_status_t/extern \"C\"/src/c_api), and version single-sourcing via project(VERSION) -> STEPPE_VERSION compile-def is exactly as described (src/app/CMakeLists.txt:72, src/extract/CMakeLists.txt:42). But several concrete claims are stale or never-built: (1) docs/adr/ with ADRs 0000-0011 does not exist; (2) the 'as built' claim that std::expected/STEPPE_TRY are used internally is wrong — a C++20 BuildResult<T> stand-in is used and STEPPE_TRY/expected.hpp don't exist; (3) doc tooling is Doxygen + pdoc, not Doxygen + Sphinx/Breathe (cmake/Docs.cmake); (4) no SteppeConfig.cmake / install-export of steppe::api/steppe::core; (5) no CHANGELOG.md; (6) the 'mirrored at docs/architecture.md' self-reference is stale (doc lives at docs/archive/). All four code comments citing §16 are consistent with the current section text, so comment alignment is clean.

- `[high/spec-vs-code]` §16 opens with a present-tense claim that ADRs live in docs/adr/ in Michael Nygard format with an "Initial set: 0000 ... 0011" (12 records). No such directory or files exist anywhere in the repo.
  - architecture.md:936 lists ADRs 0000-0011 in docs/adr/. `find /home/suzunik/steppe/docs/adr` => 'No such file or directory'; no 0000-0011 ADR files exist outside docs/archive. docs/ contains only archive/, cheatsheet.md, commands.md, commentdump/, examples/, featuredesign/.
- `[medium/spec-vs-code]` §16's explicit "AS BUILT (HEAD)" bracket and the internal-error bullets assert the internal/convenience layer uses `std::expected` internally and mention `STEPPE_TRY`. At HEAD std::expected is NOT used (toolchain is C++20; std::expected is C++23) — the code uses a hand-rolled `BuildResult<T>` stand-in, and no `STEPPE_TRY` macro or expected.hpp exists.
  - architecture.md:938/941/942 ('...std::expected internally'; 'std::expected/STEPPE_TRY are internal only'). src/core/config/build_result.hpp:3-12 documents BuildResult<T> as 'a minimal, CUDA-FREE, C++20 stand-in for std::expected<T, Status>' retired 'when the toolchain moves to C++23'. Grep for `std::expected`/`STEPPE_TRY` finds only comments in config_builder.hpp/build_result.hpp; no actual std::expected instantiation and no STEPPE_TRY anywhere in src/include.
- `[medium/spec-vs-code]` §16 says API docs are produced 'via Doxygen + Sphinx/Breathe from include/steppe/'. The build uses Doxygen for C++ (matches) but pdoc for the Python facade — there is no Sphinx or Breathe anywhere in the tree.
  - architecture.md:944 ('API docs via Doxygen + Sphinx/Breathe'). cmake/Docs.cmake:48-56 defines the `docs-python` target with pdoc ('pdoc -o <outdir> <package>'); Doxygen target at :23-40. Grep for sphinx|breathe over *.cmake/CMakeLists.txt/*.py/*.in (non-archive) returns no hits.
- `[medium/spec-vs-code]` §16 states 'Installed SteppeConfig.cmake exports steppe::api/steppe::core.' No SteppeConfig.cmake exists and there is no install(EXPORT)/install(TARGETS) for the C++ libraries; the only install() in the tree ships the Python `_core` extension into the wheel. steppe::api/steppe::core exist only as in-tree ALIAS targets, never installed or exported.
  - architecture.md:944. `find -iname 'SteppeConfig*'` (non-archive) => none. Only install() in tree: bindings/CMakeLists.txt:91 `install(TARGETS _core LIBRARY DESTINATION steppe)`. include/CMakeLists.txt:11-12 defines steppe_api INTERFACE + steppe::api alias with no install/export.
- `[medium/spec-vs-code]` §16 says 'CHANGELOG.md in Keep-a-Changelog.' No CHANGELOG.md exists anywhere in the repo.
  - architecture.md:944. `find -iname 'CHANGELOG*'` (excluding .git) => no results; `ls CHANGELOG.md` => 'No such file or directory'.
- `[low/spec-vs-code]` The closing sentence 'This living document is mirrored at docs/architecture.md' is stale — no docs/architecture.md exists; the living document itself now lives at docs/archive/architecture.md.
  - architecture.md:944. `ls /home/suzunik/steppe/docs/architecture.md` => 'No such file or directory'; the audited file is docs/archive/architecture.md.

### §17 — Scaffold manifest (ordered) (docs/archive/architecture.md, lines 948-967)
code: **stale** · comments: **aligned**

§17 is a concrete, ordered file-by-file scaffold manifest (16 numbered steps), so almost every claim is code-verifiable — and the majority of the exact paths it names are now STALE. The high-level shape (steppe_api INTERFACE, steppe::core_internal INTERFACE, CPM.cmake, steppe::warnings, top-level CMakeLists, backend seam, cpu/cuda backends, f2_block_kernel.cu, tests/CMakeLists.txt + tests/unit/test_f2.cpp, bindings) is intact, but many named artifacts do not exist or were relocated/renamed: entire directories third_party/ (step 4), .github/ (step 15), docs/adr/ (step 16), src/io/merge/ and src/io/impute/ (step 10), and top-level benchmarks/ (step 14) are absent; named files expected.hpp/STEPPE_TRY and type_traits.hpp/real_t (step 6), allocator.hpp (step 7), multi_gpu.cu/.hpp (step 8), version.hpp.in/io/genotype.hpp/steppe.hpp (step 5), reader.hpp/plink_bed.cpp/genetic_positions.cpp/precomputed_f2.cpp (step 10), .editorconfig/.pre-commit-config.yaml/CHANGELOG.md (step 1), and CompilerLauncher.cmake (step 2) do not exist. The merge/impute preprocessing named in step 10 appears unimplemented (0 grep hits). Step 5's 'C-ABI / opaque handles' framing is contradicted by the as-built C++ headers, though §16 already flags that deferral. Comment alignment is fine: the two comments that cite architecture.md §17 (CPM.cmake:1, include/CMakeLists.txt:3) accurately describe what they annotate. Overall the section reads as a day-one bootstrap recipe that the evolved codebase has substantially outgrown.

- `[high/spec-vs-code]` Step 10 (io leaf) names files that do not exist and whole subdirs that were never created. Spec: `reader.hpp`, `plink_bed.cpp`, `merge/{merge_plan,allele_harmonize}.cpp`, `filter/{snp_filter,sample_filter,prepass}.cpp`, `impute/missing_policy.cpp`, `genetic_positions.cpp`, `precomputed_f2.cpp`. Actual src/io/ has geno_reader/snp_reader/ind_reader/plink_reader/genotype_source/eigenstrat_format, filter/{include_exclude,mind_prepass,snp_filter}. There is NO merge/ or impute/ subdir, no reader.hpp, no plink_bed.cpp, no sample_filter.cpp, no prepass.cpp (it is mind_prepass.cpp), no genetic_positions.cpp, no precomputed_f2.cpp. A repo-wide grep for allele_harmonize/missing_policy/merge_plan/imputation returns zero hits in src — the merge/impute preprocessing named here appears unimplemented.
  - docs/archive/architecture.md:961 vs `find src/io -type f` (no merge/, no impute/, filter has include_exclude/mind_prepass/snp_filter); grep for allele_harmonize|missing_policy|merge_plan|imputation in src/include = 0 hits
- `[high/spec-vs-code]` Step 4 names `third_party/CMakeLists.txt` as the single place pinning CCCL/NCCL/Eigen/spdlog/CLI11/GoogleTest/nvbench/nanobind. There is NO third_party/ directory at all. Dependencies are declared per-target instead: CUDAToolkit (CCCL/NCCL) at top-level CMakeLists.txt:60, CLI11 in src/app/CMakeLists.txt:17, GoogleTest in tests/CMakeLists.txt:14, nanobind in bindings/CMakeLists.txt:30-50, CPM shim in cmake/CPM.cmake.
  - docs/archive/architecture.md:955 vs `ls third_party` -> 'No such file or directory'
- `[high/spec-vs-code]` Step 6 names two internal DRY primitives that do not exist anywhere. `expected.hpp` (`STEPPE_TRY`, internal-only): no such file and grep for STEPPE_TRY / expected.hpp across src+include returns zero hits — the internal error-propagation macro described here is entirely absent. `type_traits.hpp` (`real_t<P>`): no such file and grep for real_t returns zero hits.
  - docs/archive/architecture.md:957 vs `ls src/core/internal/type_traits.hpp`->missing; grep STEPPE_TRY|expected.hpp = 0; grep real_t = 0
- `[high/spec-vs-code]` Step 15 (CI) claims `.github/workflows/` with lint/build-test/sanitizer/nightly jobs. There is NO .github/ directory in the repo at all.
  - docs/archive/architecture.md:966 vs `ls .github` -> 'No such file or directory'
- `[high/spec-vs-code]` Step 16 claims 'seed docs/adr/0000–0011' and 'copy this document to docs/architecture.md'. There is NO docs/adr/ directory (ADRs not seeded), and no docs/architecture.md — the document lives only at docs/archive/architecture.md. (§16 line 944 also claims the mirror at docs/architecture.md.)
  - docs/archive/architecture.md:967 vs `ls docs/adr`->missing and `ls docs/architecture.md`->missing (docs top-level: archive/, cheatsheet.md, commands.md, commentdump/, examples/, featuredesign/)
- `[medium/spec-vs-code]` Step 14 claims `benchmarks/bench_f2_kernel.cu` (nvbench) in a top-level benchmarks/ dir. There is no benchmarks/ directory and no bench_f2_kernel.cu. nvbench benchmarks live under tests/reference/ (bench_f2_multigpu.cu, bench_fstats_1240k.cu, bench_optimizers.cu, bench_rotation_1240k.cu).
  - docs/archive/architecture.md:965 vs `ls benchmarks`->missing; benches at tests/reference/bench_*.cu
- `[medium/spec-vs-code]` Step 8 names `cuda/multi_gpu.cu/.hpp` (device enumeration, per-device streams/handles, NCCL comm, SNP-tile sharding, fixed-order f2_blocks combine). No file named multi_gpu.* exists. That responsibility is split across src/device/cuda/p2p_combine.cu, device_partial.cu, src/device/shard_plan.cpp/.hpp, and src/core/fstats/f2_blocks_multigpu*.cpp.
  - docs/archive/architecture.md:959 vs `find src -name 'multi_gpu*'` -> empty
- `[medium/spec-vs-code]` Step 7 names `allocator.hpp` (pool) as one of the allowlisted allocation TUs. No allocator.hpp/.cuh exists under src/device. Device allocation is done via device_buffer.cuh / pinned_buffer.cuh plus inline cudaMallocAsync in cuda_backend*.cu; the named pool-allocator file is absent.
  - docs/archive/architecture.md:958 vs `find src -name 'allocator*'` -> empty
- `[medium/spec-vs-code]` Step 5 names public headers that do not exist: `version.hpp.in` (no version.hpp.in or generated version.hpp anywhere), `io/genotype.hpp` (no include/steppe/io/ subdir), and `steppe.hpp` (no umbrella header). Additionally the step titles them 'Public C-ABI headers ... (opaque handles + status returns)', but the actual headers are ordinary C++: error.hpp exposes `enum class Status` (no steppe_status_t) and fstats.hpp exposes `struct F2BlockTensor` holding std::vector/std::span. (§16 lines 938-942 do acknowledge the C ABI is DEFERRED, so this is documented drift rather than silent.)
  - docs/archive/architecture.md:956 vs `find . -name version.hpp*`->empty, `ls include/steppe/{steppe.hpp,io}`->missing, include/steppe/error.hpp (enum class Status), include/steppe/fstats.hpp:48 (struct F2BlockTensor, std::vector<double>)
- `[medium/spec-vs-code]` Step 1 (repo/tooling roots) lists `.editorconfig`, `.pre-commit-config.yaml`, and `CHANGELOG.md` among the files to lay down. None of the three exist in the repo root.
  - docs/archive/architecture.md:952 vs `ls .editorconfig .pre-commit-config.yaml CHANGELOG.md` -> all 'No such file or directory'
- `[low/spec-vs-code]` Step 11 names `src/app/{main,cli_args}.cpp` + a `commands/` subdir. main.cpp exists, but there is no src/app/cli_args.cpp (arg parsing is src/app/cli_parse.cpp, with the arg struct at src/core/config/cli_args.hpp), and there is no commands/ subdir — commands are flat cmd_*.cpp files directly in src/app/. Also f4_matrix and jackknife (step 11) are header-only files under src/core/qpadm/ (f4_matrix.hpp, jackknife.hpp), not .cpp files under core/fstats/, and gls_solve is gls_solve.hpp (header) not gls_solve.cpp.
  - docs/archive/architecture.md:962 vs `find src/app`: cli_parse.cpp + flat cmd_*.cpp, no commands/ dir, no cli_args.cpp; src/core/qpadm/{f4_matrix.hpp,jackknife.hpp,gls_solve.hpp}
- `[low/spec-vs-code]` Step 2 lists `cmake/CompilerLauncher.cmake` in the cmake module set. That module does not exist; the cmake/ dir has CPM/CUDAArch/Docs/SteppeOptions/SteppeSanitizers/SteppeWarnings. The compiler-launcher (ccache) logic lives inline in the top-level CMakeLists.txt:71-83 and cmake/SteppeOptions.cmake:56-61, not in a dedicated module.
  - docs/archive/architecture.md:953 vs `ls cmake` (no CompilerLauncher.cmake); CMakeLists.txt:71-83 inline ccache
- `[low/spec-vs-code]` Step 6 says `log.hpp/.cpp` and `span_view.hpp`. Only log.hpp exists (no log.cpp anywhere), and there is no span_view.hpp — its role is filled by a reduced src/core/internal/views.hpp (struct MatView). views.hpp:8 itself flags this: it points to 'The full span_view.hpp (architecture.md §8)' as generalizing this minimal anchor.
  - docs/archive/architecture.md:957 vs `find src -name log.cpp`->empty, `ls src/core/internal/span_view.hpp`->missing; src/core/internal/views.hpp:6-10
- `[low/comment-vs-spec]` The only two code comments that cite architecture.md §17 are accurate to the steps they annotate: cmake/CPM.cmake:1 ('CPM.cmake bootstrap shim (architecture.md §6, §17 step 2)') matches step 2's cmake module list, and include/CMakeLists.txt:3 ('steppe_api ... (architecture.md §4, §17 step 5)') matches step 5's 'Add include/CMakeLists.txt defining steppe_api INTERFACE'. Note the include comment calls it a 'header-only API contract', which matches the as-built C++ surface but not step 5's stale 'Public C-ABI headers / opaque handles' wording — the comment is correct and the spec is the stale side. (The '§17.4' token at src/device/cuda/cuda_backend_qpadm_fit.cu:821 is an internal use-after-free audit note, not an architecture.md §17 reference.)
  - cmake/CPM.cmake:1; include/CMakeLists.txt:3; src/device/cuda/cuda_backend_qpadm_fit.cu:821

### §18 — Definition of Done / quality bar (lines 971-988)
code: **partial** · comments: **partial**

§18 is a Definition-of-Done checklist that mixes process/meta requirements with code-verifiable claims. The substantive technical claims I could check against code are aligned: the single-source `__host__ __device__` f2 feeder (src/core/internal/f2_estimator.hpp), the `q(1-q)/max(N-1,1)` denominator (f2_estimator.hpp:60,77), the `Vpair==0 ⇒ 0` guard (f2_estimator.hpp:119,126), S8 pre-sized-slot re-sort by model_index (src/core/qpadm/model_search.cpp), single-stream cuBLAS + `cublasSetWorkspace` (handles.hpp:147), fixed host-order + cudaMemcpyPeer P2P combine (p2p_combine.hpp), Ozaki 40-bit default (config.hpp:44), and `-Werror`/`--Werror all-warnings` (SteppeWarnings.cmake). However several DoD criteria reference artifacts/mechanisms that don't exist as built: (1) `steppe_status_t` — the type doesn't exist; the code uses `enum class Status` and §16 itself flags the C ABI as deferred; (2) the {sm_80, sm_90} build matrix — the project actually defaults to/validates on sm_120 (RTX 5090); (3) CI (`.github/`, CCCL≥3.1 assert) and the \"architecture grep test\"/\"allocation-allowlist grep\" don't exist as automated gates; (4) the CCCL `run_to_run`/`gpu_to_gpu` determinism API is never called (determinism is via workspace+single-stream); (5) `CHANGELOG.md` and `docs/adr/` don't exist. Comment check: comments citing §18 carry a stale line anchor (\"§18 DoD:939\"; §18 is at 971-988), and check.cuh:14 references the non-existent `steppe_status_t`.

- `[medium/spec-vs-code]` §18 line 984 states as a Done criterion: "new failure modes map to a `steppe_status_t` category." `steppe_status_t` does not exist in the codebase. The as-built public error type is `enum class Status` (include/steppe/error.hpp:23). error.hpp:7 says the C ABI `steppe_status_t` is "DEFERRED to M(abi-1)", and architecture.md's own §16 as-built note (line 939) confirms "there is no `steppe_status_t`, no `extern "C"`, no `src/c_api/`." The intent (domain outcomes RankDeficient/NonSpdCovariance/ChisqUndefined returned as values, covered by test_qpadm_domain) IS implemented via `Status`, but §18's present-tense `steppe_status_t` wording is stale/self-contradicted by §16. Supporting stale comment: src/device/cuda/check.cuh:14 also refers to translating to `steppe_status_t`.
  - docs/archive/architecture.md:984 vs include/steppe/error.hpp:7,23; architecture.md:939 ("there is no steppe_status_t"); src/device/cuda/check.cuh:14
- `[medium/spec-vs-code]` §18 line 975 defines the Done build matrix as "(CUDA 13.1+ × {sm_80, sm_90})". The as-built default compute arch is sm_120 (Blackwell / RTX 5090): CMakePresets.json:12 sets CMAKE_CUDA_ARCHITECTURES "120" and cmake/CUDAArch.cmake documents the "M0 sm_120 default" / "RTX 5090 validation box"; the release arch list (CMakePresets.json:50) is 75/80/86/89/90/100/120. The narrow {sm_80, sm_90} DoD matrix does not match the hardware the project actually targets and validates on.
  - docs/archive/architecture.md:975 vs CMakePresets.json:12,50; cmake/CUDAArch.cmake:39-44
- `[medium/spec-vs-code]` §18 lines 975-976 make CI a Done gate: "CI confirms CCCL ≥ 3.1" and "the architecture grep test and the allocation-allowlist grep both pass." No CI exists — there is no `.github/` directory and zero `*.yml`/`*.yaml` files anywhere in the repo — and there is no CCCL version guard in cmake/CMakeLists (grep for CCCL_VERSION/CCCL_MAJOR finds nothing). The allocation allowlist is enforced only by convention/comments (the THREE allowlisted TUs device_buffer.cuh / pinned_buffer.cuh / block_sink.cuh), not by any automated "allowlist grep" or "architecture grep" test (no such add_test/execute_process exists). These CI-gated Done criteria are unverifiable as built.
  - docs/archive/architecture.md:975-976; `.github` absent; find *.yml/*.yaml => 0; no CCCL_VERSION guard; src/device/cuda/device_buffer.cuh:83 (allowlist by comment only)
- `[low/spec-vs-code]` §18 lines 979 and 981 name specific CCCL determinism mechanisms: "run_to_run reductions via the single-phase API" and "on-device partials use CCCL `gpu_to_gpu`." Neither CCCL API is invoked in the source: grep finds no `cuda::execution::require`, no `determinism::`, and no `gpu_to_gpu` anywhere under src/ (only in docs/archive/architecture.md). Determinism is instead achieved by single-stream cuBLAS + `cublasSetWorkspace` (src/device/cuda/handles.hpp:147) and the fixed host-order / cudaMemcpyPeer combine (src/device/p2p_combine.hpp). The named CCCL determinism-execution-environment mechanism is not present; config.hpp:500 only describes reductions as "run_to_run-deterministic" as a property, not via that API.
  - docs/archive/architecture.md:979,981 vs no `gpu_to_gpu`/`cuda::execution` in src/; determinism via src/device/cuda/handles.hpp:147, include/steppe/config.hpp:500-511
- `[low/spec-vs-code]` §18 line 986 requires Done changes to "update headers, Doxygen, `CHANGELOG.md`, and an ADR." Neither artifact exists: there is no CHANGELOG file anywhere in the repo (find changelog* => 0) and no `docs/adr/` directory (the §16-seeded ADRs 0000-0011 were never created on disk). So the CHANGELOG/ADR portions of the DoD reference non-existent artifacts.
  - docs/archive/architecture.md:986; no CHANGELOG*; `docs/adr` absent
- `[low/comment-vs-spec]` Two comments cite §18 with a stale line anchor "§18 DoD:939": tests/reference/test_qpadm_domain.cu:4 and tests/CMakeLists.txt:743. §18 (Definition of Done) is at architecture.md lines 971-988, and its domain-outcome-returned-as-value bullet is line 984; line 939 is inside §16 (the ADR paragraph). The comments' content (a degenerate model returns a per-model status, not a fault) matches §18's substance, but the ":939" line pointer is wrong/stale for §18.
  - tests/reference/test_qpadm_domain.cu:4 and tests/CMakeLists.txt:743 ("§18 DoD:939") vs architecture.md §18 at lines 971-988 / bullet at 984

