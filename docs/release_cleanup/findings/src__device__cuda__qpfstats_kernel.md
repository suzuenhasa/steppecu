# src__device__cuda__qpfstats_kernel
Files: /home/suzunik/steppe/src/device/cuda/qpfstats_kernel.cu, /home/suzunik/steppe/src/device/cuda/qpfstats_kernel.cuh
Subsystem: device-cuda

## Findings

### G12
- [G12.device-cuda][LOW] qpfstats_kernel.cu:56-57 — `qpfstats_zero_nan_ymat_kernel` is a strict one-element-per-thread kernel (guarded by `if (cell >= total) return;`) with the grid sized exactly to cover `total = npopcomb*n_block`. This is correct because `blocks` is derived from `total`, but it carries no grid-stride loop, so it relies on `blocks` fitting the grid x-dim. At qpfstats scale (P~2500 ⇒ npopcomb ~ P*(P-1)/2 ~ 3.1M, n_block~757) `total` ~ 2.36e9 and `blocks` ~ 9.2M, which is well under the 2^31-1 x-dim cap and the `unsigned` cast at line 57 (`static_cast<unsigned>(blocks)`) is safe; no bug today. Suggested: optionally add a grid-stride loop to make the kernel input-size-agnostic and remove the implicit "grid always covers total" coupling, or leave as-is given the comfortable margin.

### G5
- [G5.device-cuda][LOW] qpfstats_kernel.cu:55 vs :64 — two different `constexpr int kThreads` values (256 for the zero-nan launch, 64 for the ridge-diag launch) are both named `kThreads` in sibling functions in the same TU. They are independent and locally `constexpr` so this is not a drift bug, but the identical name for two distinct block-size choices is mildly confusing on a skim. Suggested: name them distinctly (e.g. `kZeroNanThreads` / `kRidgeThreads`) if clarity is wanted; otherwise acceptable.

No other issues found (groups checked: G2-G10, G11-G22). Notes on items deliberately judged clean: G4 — `cell`/`total` are `long` (line 29-30) and the ridge diagonal index is widened to `long` before the multiply (line 44), so no int-index overflow at P~2500/M~584k scale; `cell / npopcomb` (long/int) and the `b` derivation are correct. G11 — both kernel pointer params are `const __restrict__`-qualified where read-only intent allows (`ymat`/`A` are in-place so non-const is correct; `nan_per_block` is an output accumulator). G13 — every CUDA API call is wrapped (`STEPPE_CUDA_CHECK` on `cudaMemsetAsync`, `STEPPE_CUDA_CHECK_KERNEL()` after each launch, giving both `cudaGetLastError()` and the debug-gated sync). G18 — no shared memory, no `__syncthreads`, no warp intrinsics; `atomicAdd` into a pre-zeroed per-block counter is correct and bounds-guarded; `isfinite`/`= 0.0` zeroing matches the documented AT2 ymat_chunk[nan]=0 policy. G14/G16/G17 — kernels own no resources (caller-owned device pointers, non-owning views); nothing to RAII. The empty-launch guards (`if (total <= 0) return;` line 52, `if (n <= 0) return;` line 63) correctly short-circuit before the memset/launch.
