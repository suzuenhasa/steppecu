# src__device__tier_select
Files: /home/suzunik/steppe/src/device/tier_select.hpp
Subsystem: backend

## Findings

### G8
- [G8.src__device__tier_select][LOW] tier_select.hpp:102 — The trailing comment `// (vram_budget.hpp)` is attached to the `max_tile_z` initializer line, but it is a stray continuation of line 101's note that `nonneg` is "the shared helper (7.4)" from vram_budget.hpp. On the `max_tile_z` line it reads as if vram_budget.hpp owns `max_tile`, which it does not. Minor placement ambiguity, not a behavior issue. Suggested: move the `(vram_budget.hpp)` provenance note onto line 101 next to the `nonneg` call it describes, or drop it.
- [G8.src__device__tier_select][LOW] tier_select.hpp:94 — `@param M  UNUSED (kept for call-site symmetry...)` correctly documents the unused parameter (also marked `/*M*/` at line 99), so this is intentional and well-justified, but the doc block at lines 78-92 still narrates a full GPU footprint formula for a host-pure policy helper that `select_output_tier` explicitly does not call (line 88-90 says "NOT used by select_output_tier"). The rationale (bench feasibility narrative / future select) is given, so it is defensible; noting only that the helper is dead with respect to the live policy path. Suggested: none required; keep if the bench narrative still consumes it, otherwise consider deleting with the bench.

(Verified clean for the remaining groups: G2 — no CUDA APIs (CUDA-free header by design, no device calls); G3 — no dead/commented-out code, every declared symbol (`OutputTier`, the three `kForceTierToken*`, all four helper/declarations) is referenced by the documented call sites; G4 — all width-sensitive products are widened before multiply: `pm` cast to size_t (line 70) before the `7·P·M·8` product, `streamed_working_set_bytes` widens `P/max_tile/max_nb` via `nonneg` and explicitly casts `kStreamDeviceChunks` (line 109), `result_bytes`/`resident_need` are size_t (lines 140-141) — no int-index overflow at P~2500/M~584k/n_block~757; the `unsigned` config coefficients (`kFeederRawBufsPerPop` etc.) promote to size_t in every product; `M`/`free_*` guards precede the casts; G5 — every literal (7·P·M, 0.70, 0.60, ring count) is sourced from a named config.hpp constant, no duplicated/magic value, no hardcoded path/id; G6 — names are clear and consistent (`resident_working_set_bytes`, `streamed_working_set_bytes`, `resolve_output_tier`); G7 — the floor(fraction·free) idiom is correctly delegated to the shared `budget_bytes` helper (lines 144,146) rather than re-inlined, and the `7·P·M` coefficient is shared via config.hpp; G8 — comments otherwise carry rationale, no STALE/orphan TODO; G9 — all policy values are `inline constexpr`, the `OutputTier`/`ForceTier` selection is enum-based not positional-bool; G10 — all locals are initialized at declaration at first use.)
