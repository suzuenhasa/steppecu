# src__app__cli_parse
Files: /home/suzunik/steppe/src/app/cli_parse.cpp, /home/suzunik/steppe/src/app/cli_parse.hpp
Subsystem: app-cli

## Findings

### G7
- [G7.src__app__cli_parse][LOW] cli_parse.cpp:276-311 — `add_sweep_mode_flags` and `add_sweep_flags` are near-duplicates: the `--min-z` / `--top-k` / `--sure` bindings are copy-pasted (same lambdas into `a.sweep_min_z` / `a.sweep_top_k` / `a.sweep_sure`), differing only by the help-string wording and the extra `enable_flag` + `--shard-dir` in the mode variant. The three shared bindings drift independently (the `--min-z` help text already diverges: "the on-device filter; default 3.0" appears in both but `--top-k`'s wording is identical, so any future edit must be made in two places). Suggested: extract a private `add_sweep_filter_flags(sub, a)` helper that binds `--min-z`/`--top-k`/`--sure` once and have both callers invoke it, then add the variant-specific flags on top.
- [G7.src__app__cli_parse][LOW] cli_parse.cpp:369-370, 402-403 — the `--fudge` option is bound inline (not via `add_qpadm_option_flags`) for both `qpgraph` and `qpgraph-search` with essentially the same lambda/help ("AT2 cc edge-solve ridge (diag; default 1e-4)"). Two copies that must stay in sync. Suggested: a tiny `add_fudge_flag(sub, a, help)` helper (mirrors the existing `add_f2_dir_flag(..., help)` pattern) since the two help strings differ only slightly.
- [G7.src__app__cli_parse][LOW] cli_parse.cpp:391-394 vs 626-629 vs 298-301 — the `--pops` `std::vector<std::string>` + `->delimiter(',')` binding into `a.pops` is hand-written inline in `qpgraph-search` (391) and `qpfstats` (626) while `add_sweep_flags` (298) and the f4/f3/f4ratio helpers already wrap the identical pattern. A shared `add_pops_flag(sub, a, help)` helper would collapse these. Suggested: factor the bare `--pops` → `a.pops` binding into one parametrized-help helper and reuse it in the two inline sites.

### G8
- [G8.src__app__cli_parse][LOW] cli_parse.hpp:9-12 — header comment still describes the old M(cli-0) SCAFFOLD behavior ("the fit subcommands ... and extract-f2 print a 'not yet implemented' notice and exit cleanly ... No GPU work happens in this milestone"). The .cpp top comment (lines 12-13) and the qpadm callback comment (lines 355-357) state the opposite — all subcommands are now wired to real GPU compute. The header comment is STALE and contradicts the implementation. Suggested: update the header block to reflect that subcommands dispatch to real GPU compute (matching cli_parse.cpp:12-13).
- [G8.src__app__cli_parse][LOW] cli_parse.cpp:355-356, 459-461 — inline comments still carry milestone framing ("only qpwave (M(cli-2)) remains a scaffold no-op" at 355-356; "M(cli-2): the real GPU qpWave rank sweep" at 459). The qpadm-callback comment at 355-356 claiming qpwave is "a scaffold no-op" is now false (the qpwave callback at 456-463 calls the real `run_qpwave_command`). Suggested: drop the obsolete "qpwave remains a scaffold no-op" clause.
