# Review findings — src__device__cuda__p2p_combine

Files: /home/suzunik/steppe/src/device/cuda/p2p_combine.cu, /home/suzunik/steppe/src/device/p2p_combine.hpp

## Group 4 — Type & numeric

- [4.7][LOW] src/device/cuda/p2p_combine.cu:138-141,188-191,264-267 — Raw `double*` are used for both host (`out.f2.data()`) and device (`dResult_f2.data()`/peer `src_f2`) pointers with no host-vs-device type distinction; a swapped space relies on the explicit `cudaMemcpy*` direction enum / peer device ids to fault rather than the type system. Consistent with the project-wide backend seam (DeviceBuffer is the owner). Suggested: optional — if the codebase later adopts a thin DevicePtr/HostPtr wrapper, route these through it; no functional change needed now.

Note (4.2/4.6 — the scale-critical path, verified CLEAN): every global index into the up-to-~10^10-element f2/vpair tensor is widened to `std::size_t` BEFORE the multiply — `slab = (size_t)P * (size_t)P` (cu:94-95, 227-228), `total = slab * (size_t)n_block_full` (cu:97, 229), `part_elems = slab * (size_t)part.n_block_local` (cu:134, 260), `dst_off = slab * (size_t)part.b0` (cu:136, 262). No `int` product overflows before widening. The only in-`int` arithmetic, `part.b0 + lb` (cu:129, 255), is bounded by n_block_full (~757), far below 2^31.
Note (4.3 — CLEAN): `DeviceBuffer<double>(total)` / `.resize(total)` / `.assign(n_block_full,0)` are element-count APIs; raw `cudaMemcpy*` byte counts correctly carry `* sizeof(double)` (cu:135, 185, 261).
Note (4.1/4.4/4.5 — CLEAN/N-A): all math is FP64 by design (no wrong narrowing); loops are ascending `size_t<size_t` (cu:122,249) and `int<int` (cu:128,254) — no unsigned countdown, no signed/unsigned mismatch.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

Note (2.1 — N/A): this unit is a host-side translation unit only — no `__global__`/`__device__` kernels, no `<<<>>>` launches, and no per-arch code; it contains no `sm_*` flags or CMake arch lists (those live in the build files, not here).
Note (2.2 — CLEAN): no texture/surface usage. No `texture<...>`, `surface<...>`, `cudaBindTexture*`, `tex1Dfetch`, or any CUDA-12-removed texture-reference API appears; the unit moves bytes only via `cudaMemcpyAsync` / `cudaMemcpyPeerAsync` (cu:145-148, 166-169, 188-191, 271-273, 283-286).
Note (2.3 — N/A): no warp intrinsics at all (no kernels). No `__shfl*`/`__ballot`/`__any`/`__all`/`__activemask`, deprecated non-`_sync` or otherwise.
Note (2.4 — CLEAN): synchronization uses the current `cudaStreamSynchronize` (cu:183, 192, 297) and `cudaGetDevice`/`cudaSetDevice` (cu:81, 86, 216, 221); no `cudaThreadSynchronize` (or any other `cudaThread*` removed alias). The comments at cu:171-174, 288-290 reference `cudaDeviceSynchronize` only in prose explaining its deliberate absence — no removed-API call. The .hpp (p2p_combine.hpp) is CUDA-free by contract and contains no CUDA API at all.

## Group 3 — Dead / commented-out code

No Group 3 issues found.

Note (3.1 — CLEAN): the large comment blocks (cu:1-41, cu:65-105, cu:197-203; the doc-comments in p2p_combine.hpp:1-110) are design/parity rationale (architecture.md §11.4/§12 references), NOT commented-out code "kept just in case" — no disabled statements exist.
Note (3.2 — CLEAN): no `#if 0`, no `#ifdef`-disabled regions, no code after `return`/`break`. The two `if (part.empty()) continue;` guards (cu:132, 258) skip only the empty-shard case; the following copy code is reachable for non-empty shards.
Note (3.3 — CLEAN): every parameter is read — `shards` (cu:63, 206) feeds `validate_resident_partials` (cu:70-71, 209-210); `partials`, `P`, `n_block_full`, `root_device_id` all consumed. Every `#include` is used: `<cstddef>`/`<span>` (std::size_t, std::span); check.cuh (STEPPE_CUDA_CHECK/WARN); device_buffer.cuh (DeviceBuffer); device_partial_impl.cuh (`part.impl->f2/vpair`); device_f2_blocks_impl.cuh (`DeviceF2Blocks::Impl`, cu:242); pinned_buffer.cuh (RegisteredHostRegion, cu:186-187); stream.hpp (Stream, cu:91, 224); steppe/fstats.hpp (F2BlockTensor); shard_plan.hpp (DeviceShard); f2_partials_validate.hpp (validate_resident_partials). No unused local/helper.
Note (3.4 — CLEAN): every assigned local is read — `prev_device` (cu:80) → `restore{prev_device}` (cu:85, 220); `part_elems` (cu:134, 260) → `part_bytes` (cu:135, 261); `slab`/`total`/`dst_off`/`bytes` and the dst/src pointers all consumed by the copies/D2H. Nothing computed-but-unread.

## Group 5 — Hardcoded values / magic numbers

No Group 5 issues found.

Note (5.1 — CLEAN): the only numeric literals are trivial `0`s — loop inits (`g=0` cu:122,249; `lb=0` cu:128,254), `prev_device=0` (cu:80,215, overwritten by cudaGetDevice), zero-init `.assign(..., 0)` (cu:119,241), and the `total > 0` D2H guard (cu:184). None are tunable thresholds or magic numbers (no `0.001f`/`1024`/`0.5`). No FP literals at all (the FP64 math lives upstream in the GEMM; this TU only MOVES bytes).
Note (5.1/API — CLEAN): `cudaDeviceEnablePeerAccess(part.device_id, 0)` (cu:159,279) — the `0` is the CUDA-Runtime-mandated reserved `flags` argument (the API requires it to be 0); not a tunable constant, so naming it would not improve it.
Note (5.2 — CLEAN): no hardcoded sizes/bounds — `P`, `n_block_full`, `root_device_id` are all parameters; `slab = (size_t)P*(size_t)P` (cu:94-95,227-228) and `total = slab*(size_t)n_block_full` (cu:97,229) are DERIVED, not literal. Allocation sizes flow from these. No fixed buffer/grid/block size.
Note (5.3 — CLEAN): no correctness-coupled duplicated constant that could DRIFT (no block-dim-in-launch-AND-shared-mem-size; this TU has no kernels/`<<<>>>`). The function-name string passed to `validate_resident_partials` ("...combine_f2_partials_resident" cu:71; "...combine_f2_partials_resident_device" cu:210) is a diagnostic label, not a numeric constant.
Note (5.4 — CLEAN): no hardcoded device ids — every device id is a parameter (`root_device_id`) or a handle field (`part.device_id`); no `cudaSetDevice(0)`-style literal. No hardcoded paths/IDs.
Note (5.5 — N/A): no `32` anywhere — no warp-size usage (no kernels), so no ambiguous-32 to name.

## Group 6 — Naming

- [6.3][LOW] src/device/cuda/p2p_combine.cu:106-107,245-246 — Inconsistent naming for the structurally identical full-result device buffers across the two sibling functions in this one file: the host-returning `combine_f2_partials_resident` calls them `dResult_f2`/`dResult_vp` (camelCase with a Hungarian `d`-for-device prefix), while `combine_f2_partials_resident_device` calls the same-purpose buffers `result_f2`/`result_vp` (snake_case, no prefix). Same role, two conventions in the same TU. Suggested: pick one (e.g. `result_f2`/`result_vp` to match the file's otherwise-snake_case locals) and apply to both functions.
- [6.4][LOW] src/device/cuda/p2p_combine.cu:107,139,141,147-148,168-169,187,190-191,246,265,267,273,285-286 — The local/buffer abbreviation `vp` is used pervasively (`dResult_vp`, `dst_vp`, `src_vp`, `pin_vp`, `result_vp`) for the quantity the public struct field spells out as `vpair` (`out.vpair`, `part.impl->vpair`, `out.impl->vpair`). The `vp` ↔ `vpair` split is an inconsistent abbreviation within the file; `vp` is not a standard term outside this code. Suggested: spell the locals `..._vpair` (or accept `vp` but document it once) so the local names match the struct field they mirror — note this is paired with the matching `..._f2` locals, so the rename should keep the pair symmetric.

Note (6.1 — CLEAN): no opaque single-letter/`tmp`/`arr`/`flag`/`data2` names. The only single-letter names are tight-loop counters — `g` (cu:122,249, the fixed g=0..G-1 device/partial index, the project's documented combine-order convention) and `lb` (cu:128,254, the local-block index inside the per-partial block_sizes copy) — both conventional bounded counters. `part`, `slab`, `total`, `part_elems`, `part_bytes`, `dst_off`, and the `dst_*`/`src_*`/`pin_*` pointers are descriptive. `out`/`prev_device`/`restore`/`dev` are conventional.
Note (6.2 — CLEAN): no misleading names — `total`/`part_elems` are element counts and are used as such (byte counts are derived separately as `part_bytes`/`bytes` with `* sizeof(double)`); `dst_off` is a real offset; `n_block_local`/`n_block_full`/`b0` mean exactly what they say; `partials`/`shards` are spans, not maps. The `d` prefix on `dResult_*` honestly marks a device pointer.
Note (6.3/6.4 — the .hpp is CLEAN): p2p_combine.hpp declares the same parameter names (`partials`, `shards`, `P`, `n_block_full`, `root_device_id`) consistently with the .cu definitions; the doc-comments spell `vpair`/`Vpair` in prose. The `vp`/`dResult` issues are .cu-local only.

## Group 7 — Duplication

- [7.1][MED] src/device/cuda/p2p_combine.cu:121-175 vs 248-291 — The entire fixed-order g=0..G-1 placement loop is copy-pasted between `combine_f2_partials_resident` and `combine_f2_partials_resident_device`, differing ONLY by the destination base pointers (`dResult_f2.data()`/`dResult_vp.data()` vs `result_f2`/`result_vp`). The block_sizes inner copy (cu:128-131 / 254-257), the `part.empty()` early-continue, the `part_elems`/`part_bytes`/`dst_off` computation (cu:134-136 / 260-262), the dst/src pointer setup (cu:138-141 / 264-267), the root D2D branch (cu:145-148 / 271-273), and the peer-enable + cudaMemcpyPeerAsync branch (cu:159-169 / 279-286) are byte-for-byte identical. A divergent edit to one (e.g. a peer-access fix) would silently skip the other — a real maintenance hazard at this complexity. Suggested: extract a private helper `place_partials_into(double* dst_f2, double* dst_vp, span partials, slab, root_device_id, root_stream, out.block_sizes)` and call it from both functions; the comment headers cu:197-203 already assert the two are "structurally identical", which a shared helper would enforce mechanically.
- [7.2][LOW] src/device/cuda/p2p_combine.cu:80-86 vs 215-221 — The device-bind preamble is duplicated verbatim: the local `struct DeviceGuard { int dev; ~DeviceGuard()... }`, the `cudaGetDevice(&prev_device)`, the `restore{prev_device}` instantiation, and the `cudaSetDevice(root_device_id)` are re-declared identically in both functions (the guard struct definition itself is repeated, not just used). Suggested: lift `DeviceGuard` to one file-scope (anonymous-namespace) struct and wrap the bind-and-restore in a small helper or a single RAII type constructed from `root_device_id`, so both functions share one definition.
- [7.2][LOW] src/device/cuda/p2p_combine.cu:94-97 vs 227-229 — The `slab`/`total` derivation (`slab = (size_t)P*(size_t)P`; `total = slab*(size_t)n_block_full`) plus the `Stream root_stream_owner; root_stream = ...get()` setup (cu:91-92 / 224-225) and the `validate_resident_partials(...)` precondition call (cu:70-71 / 209-210) are repeated between the siblings. These would naturally fold into the same shared-helper extraction as 7.1. Suggested: compute `slab`/`total` once in a shared prologue helper alongside the validate + stream setup.

Note (7.3 — CLEAN): no repeated `sizeof`/cast that warrants hoisting beyond what 7.1 already covers — `* sizeof(double)` appears at cu:135/185/261 but each is a distinct byte-count at a different scope (per-partial vs full-result D2H); folding the loop body (7.1) removes the cu:135↔261 pair, and the cu:185 D2H is unique to the host-returning sibling. The `(size_t)` widenings are correct per-use and not redundantly recomputed within a function.

Note (7.4 — CLEAN): no macro/boilerplate beyond the DeviceGuard (covered by 7.2). The `STEPPE_CUDA_CHECK`/`STEPPE_CUDA_WARN` wrappers are already the shared error-handling macros; the repeated `cudaMemcpyAsync(...DeviceToDevice...)` / `cudaMemcpyPeerAsync(...)` pairs (f2 then vpair) are inherent to carrying the two parallel tensors and would collapse naturally inside the 7.1 helper rather than needing their own abstraction.

## Group 8 — Comments

- [8.2][MED] src/device/p2p_combine.hpp:6-7,10-11,25-28 — The header's top-of-file prose describes the combine as a SUM/accumulate ("pulls each peer's partial ... and SUMS them in the same fixed g = 0..G-1 order on-device", ".6-7; "sums on-device in the SAME FIXED g=0..G-1 device order", :10-11; "the on-device add SUMS THE SAME FIXED g=0..G-1 ORDER onto a zero-initialized full tensor, the identical arithmetic combine_f2_partials_host performs", :25-28). The actual implementation is a DISJOINT raw byte PLACEMENT, NOT a sum onto a zeroed accumulator — as the .cu (cu:99-105: "NO place-add kernel, NO accumulator zeroing"; cu:36-37) and even this SAME header's own later doc-comment state (`.hpp:51-56`: "a verbatim PLACEMENT (a raw D2D/peer byte copy into the disjoint slice), NOT a sum onto a zeroed accumulator ... there is NO cudaMemset and NO place-add"). The header's lead paragraphs describe behavior the code no longer has (the pre-cleanup-B7 memset+`+=` accumulate path) and directly contradict the implemented semantics documented 40 lines lower in the same file. Suggested: rewrite the :6-7/:10-11/:25-28 "sums on-device" / "zero-initialized full tensor" / "on-device add" phrasing to the disjoint-placement (raw byte copy, written exactly once, no sum/no memset) language already used at .hpp:51-56 and cu:99-105.

Note (8.1 — CLEAN): no restating-the-code comments. The terse inline notes carry real semantics, not echoes of the syntax — `// disjoint slice base` (cu:136,262) names the placement role of `dst_off`, `// resident on part.device_id` (cu:140,266) records cross-device OWNERSHIP of the source pointer (load-bearing for the peer-copy direction), `// empty shard: no resident buffers, nothing to copy` (cu:132,258) gives the WHY of the early-continue, and `// no peer hop` (cu:144,270) explains the root-branch choice. None merely re-say the line.
Note (8.2 — the .cu is otherwise CLEAN): the .cu's large rationale blocks (cu:1-41, 99-105, 197-203) accurately match the implemented PLACEMENT/no-memset/no-place-add semantics and the cleanup-B7 −0.0 story; the §-references and "the transport only moves bytes" prose track the code. The stale prose is confined to the .hpp lead paragraphs (above).
Note (8.3 — CLEAN): non-obvious choices carry their rationale. The `cudaDeviceEnablePeerAccess(part.device_id, 0)` reserved `0` flag and the directional from-the-issuing-device reasoning are explained (cu:153-159,276-279); the `(void)cudaGetLastError()` sticky-error clear after the WARN'd already-enabled status is justified (cu:155-160,280); the absent per-peer `cudaDeviceSynchronize` has an explicit lifetime rationale (cu:171-174,288-290); the NO-memset / single-D2H / RAII-restore / pin-kept-alive-across-DMA choices are all motivated (cu:99-105,177-182,76-79). The intentional FP64/parity deviations from a naive AllReduce are documented with §11.4/§12 citations (cu:39-41) — not silently deviating.
Note (8.4 — CLEAN): no orphan TODO/FIXME/HACK/XXX/WIP/BUG markers in either file (grep-verified). No undated, unowned, contextless deferral comments.

## Group 9 — Constants & configuration

No Group 9 issues found.

Note (9.1 — CLEAN): all derived/read-only locals are already `const` — `slab` (cu:94,227), `total` (cu:97,229), `root_stream` (cu:92,225), `part_elems`/`part_bytes`/`dst_off` (cu:134-136,260-262), `bytes` (cu:185). The src pointers are `const double*` (cu:140-141,266-267); the dst pointers are non-const `double*` because they are DMA write targets (cu:138-139,264-265). The only mutable-by-necessity locals are write-targets or out-params: `prev_device` (cu:80,215) is a `cudaGetDevice` out-param then read; `out` (cu:114,237) is the result being assembled; `DevicePartial& part` (cu:123,250) is intentionally a non-const ref over the non-const `partials` span (the documented consume-in-place contract, hpp:92-98). The `DeviceGuard::dev` member (cu:83,218) is aggregate-initialized once and only read in the dtor — could be `const int dev`, but it is a function-local one-shot RAII struct, so this is negligible. No should-be-const left mutable.
Note (9.2 — CLEAN): no tangled config — this TU has NO tunable knobs. It only MOVES bytes; every size is DERIVED from the parameters (`slab` from `P`, `total`/`part_elems`/`dst_off` from `slab`×block counts), not a buried threshold. The peer-access POLICY (the §4 four-term gate incl. `config.enable_peer_access`) is surfaced ONCE in the caller `f2_blocks_multigpu.cpp` and documented as such (hpp:68-78); this routine deliberately does not re-probe or re-decide. No magic tuning constant is buried in the copy logic.
Note (9.3 — CLEAN): no positional-boolean call. `cudaDeviceEnablePeerAccess(part.device_id, 0)` (cu:159,279) — the `0` is the CUDA-Runtime-mandated RESERVED `flags` argument (the API contract requires 0), not a steppe-chosen bool flag, and is documented as such (cu:153,277). Copy direction is passed as the self-describing enum `cudaMemcpyDeviceToDevice` / `cudaMemcpyDeviceToHost` (cu:146,148,189,191,272-273,290 region) — not a bare true/false. No steppe-defined function in this unit takes positional booleans (both public entry points take only spans + named int params; hpp:111-114,124-127).

## Group 10 — Initialization

No Group 10 issues found.

Note (10.1 — CLEAN): no late/distant or read-before-write declarations. `prev_device` (cu:80,215) is declared `int prev_device = 0;` immediately above its `cudaGetDevice(&prev_device)` out-param write — defensively initialized at declaration, never read while indeterminate. `restore{prev_device}` (cu:85,220) is aggregate-initialized at declaration; its `DeviceGuard::dev` member is set once and only read in the dtor. `slab`/`total` (cu:94-97,227-229) are `const`, initialized at declaration adjacent to first use. The result aggregates `out` (cu:114-119 F2BlockTensor; cu:237-244 DeviceF2Blocks) have every member explicitly set right after declaration (P, n_block, [device_id,] f2/vpair resize/buffers, block_sizes, [impl]) — contiguous build, not distant. All loop-body locals (`part`, `part_elems`, `part_bytes`, `dst_off`, `dst_f2`/`dst_vp`/`src_f2`/`src_vp`, cu:123-141,250-267) and the D2H `bytes`/`pin_f2`/`pin_vp` (cu:185-187) are declared at first use and initialized in the same statement.
Note (10.2 — CLEAN): no broken zero-init assumption. The full-result device buffers `dResult_f2`/`dResult_vp` (cu:106-107) and `out.impl->f2`/`out.impl->vpair` (cu:243-244) are DELIBERATELY un-memset, and this is correctness-validated, NOT a missing init: `validate_resident_partials` (cu:70-71,209-210) enforces that the disjoint shards tile `[0, n_block_full)` exactly (covered == n_block_full), so every slab is overwritten exactly once by its owner's D2D/peer copy — the code relies on the COVERAGE, not on any prior buffer value (cu:99-105 documents this; intentional cleanup-B7 −0.0-faithful placement). The host `out.f2`/`out.vpair.resize(total)` (cu:117-118) value-initialize to 0 via std::vector, but the code does NOT depend on those zeros — the single final D2H overwrites every element (cu:188-191; "resized, NOT zero-assigned", cu:109-110). `out.block_sizes.assign(n_block_full, 0)` (cu:119,241) is an EXPLICIT zero-fill (not relying on default), then the per-partial loop (cu:128-131,254-257) writes each block's count; the exact tiling makes the explicit 0 belt-and-suspenders, not load-bearing-default. Both result structs (F2BlockTensor, DeviceF2Blocks) carry in-class default member initializers (P=0, n_block=0, device_id=-1, empty vectors, null impl) and every field downstream reads is explicitly assigned here, so no default value is silently consumed.

## Group 11 — Qualifiers & const-correctness

No Group 11 issues found.

Note (11.1 — N/A): no kernel exists in this unit, so there is no kernel pointer parameter to mark `const __restrict__`. Both entry points are HOST functions that only orchestrate runtime DMAs; the read-only source pointers handed to `cudaMemcpyAsync`/`cudaMemcpyPeerAsync` are already correctly `const double*` (cu:140-141, 266-267), and `__restrict__` confers no aliasing-optimization benefit on a `cudaMemcpy*` call (the Runtime API takes `void*`/`const void*` and copies opaquely). The write-target pointers are correctly non-const `double*` (cu:138-139, 264-265).
Note (11.2 — N/A): no `__global__`/`__device__` symbol to qualify. `combine_f2_partials_resident` (cu:61, hpp:111) and `combine_f2_partials_resident_device` (cu:204, hpp:124) are implicitly `__host__` (the default) and CONSISTENT between the .cu definitions and the .hpp declarations. The .hpp is CUDA-free BY CONTRACT (architecture.md §4; hpp:13-22) and must NOT carry `__host__`/`__device__` macros — its qualifier-free decl is intentional and correct, not an inconsistency.
Note (11.3 — N/A): no host/device helper duplicated as separate `__host__` and `__device__` copies. The only duplication in this TU (the placement loop cu:121-175 vs 248-291; the DeviceGuard preamble cu:80-86 vs 215-221) is HOST-to-HOST and is already captured under Group 7 (7.1/7.2) — there is no device-side counterpart that would warrant a single `__host__ __device__` definition.
Note (11.4 — N/A): no kernel, so no kernel-param-space (`__grid_constant__` / pass-by-pointer) concern. Both entry points take only `std::span` (a 16-byte ptr+size pair, passed by value) and `int` scalars (cu:62-64, 205-207) — these are host-call parameters where the 256 B/4 KB kernel constant-bank limit does not apply, and they are already lightweight. No large by-value struct is passed into any launch (there is no launch).
Note (11.x — host const-correctness already covered): the one mildly-improvable host qualifier — `DeviceGuard::dev` (cu:83, 218) could be `const int dev` since it is aggregate-initialized once and only read in the dtor — is host code and was already noted under Group 9 (9.1) as negligible; not re-flagged here.

## Group 12 — Launch config & indexing

No Group 12 issues found.

Note (12.1/12.2/12.3/12.4 — N/A): this unit is a pure HOST-side orchestration TU — it contains NO `__global__`/`__device__` kernels and NO `<<<grid,block>>>` launches anywhere; all device work is runtime DMA (`cudaMemcpyAsync`/`cudaMemcpyPeerAsync`/D2H, cu:145-148,166-169,188-191,271-273,283-286) plus device-bind (`cudaSetDevice` cu:86,221) and peer-enable (cu:159,279). So there is no block dim to check against the warp size (12.1), no grid dim to compare to `(n+block-1)/block` (12.2), no one-elem-per-thread kernel that could miss a grid-stride loop (12.3), and no baked-in launch config that should derive from `cudaOccupancyMaxPotentialBlockSize` (12.4). The two `for` loops (cu:122,249 over the G partials; cu:128,254 over each partial's local blocks) are HOST loops that traverse their full ranges by construction (`g < partials.size()`, `lb < part.n_block_local`) — they are not GPU grids and cannot under-cover their input.
Note (12.5 — CLEAN): no hardcoded compute-cap / device-property assumption. No `sm_*` literal, no hardcoded warp size (32), no hardcoded SM/multiprocessor count, max-threads, or shared-mem capacity, and no `cudaGetDeviceProperties` field read against a baked-in constant. Every device id is a parameter (`root_device_id`, cu:64,207) or a handle field (`part.device_id`, cu:143,159,166-169,269,279,283-286) — no `cudaSetDevice(0)`-style literal device. The P2P/peer-reachability CAPABILITY is NOT assumed here: the four-term §4 peer-access gate (incl. `cudaDeviceCanAccessPeer`) is computed ONCE in the caller `f2_blocks_multigpu.cpp` and this routine deliberately does not re-probe it (documented hpp:68-78); reaching this code means the capability was already queried + permitted upstream. `cudaErrorPeerAccessAlreadyEnabled` is handled as an expected status (cu:159-160,279-280), not a hardcoded device assumption.

## Group 13 — Error handling

No Group 13 issues found.

Note (13.1 — CLEAN): every `cuda*` runtime call whose failure is a FAULT routes through the throwing `STEPPE_CUDA_CHECK` (which throws `CudaError` carrying file:line:function via std::source_location, check.cuh:151-155,214-215). Inventory verified complete in both functions: `cudaGetDevice` (cu:81,216), `cudaSetDevice` bind (cu:86,221), the D2D `cudaMemcpyAsync` pairs (cu:145-148,271-273), the `cudaMemcpyPeerAsync` pairs (cu:166-169,283-286), the final D2H `cudaMemcpyAsync` pair (cu:188-191), and every `cudaStreamSynchronize` drain (cu:183,192,297). No bare/unchecked `cuda*` return anywhere.
Note (13.2 — N/A as a LAUNCH check, and the async analogue is CLEAN): this TU has NO `<<<grid,block>>>` kernel launch, so there is no `cudaGetLastError()`-after-launch requirement (the post-launch `STEPPE_CUDA_CHECK_KERNEL` macro, check.cuh:251-256, is for kernel TUs and is correctly absent here). The async-DMA analogue of 13.2 IS satisfied: the async `cudaMemcpyAsync`/`cudaMemcpyPeerAsync` enqueues are each wrapped in `STEPPE_CUDA_CHECK` (surfaces the SYNCHRONOUS submission error), and a later `STEPPE_CUDA_CHECK(cudaStreamSynchronize(root_stream))` (cu:183,192,297) surfaces any DEFERRED async copy fault — both stages present, exactly the two-stage guarantee 13.2 asks for.
Note (13.3 — CLEAN, the WARN vs CHECK split is principled and consistent): the only non-throwing sites are the two documented RECOVERABLE statuses, both routed through the non-throwing `STEPPE_CUDA_WARN` (check.cuh:174-189,227-228): `cudaDeviceEnablePeerAccess` (cu:159,279), where `cudaErrorPeerAccessAlreadyEnabled` is the EXPECTED 2nd+-combine status (a genuine enable failure still surfaces downstream on the throwing `cudaMemcpyPeerAsync`, hpp:75-78), and the `cudaSetDevice` teardown in `~DeviceGuard` (cu:84,219), which must not throw from a destructor. The `(void)cudaGetLastError()` (cu:160,280) is a DELIBERATE sticky-error CLEAR of the already-handled WARN'd status, documented cu:155-160 — not an unchecked call. So fault calls → CHECK (throw), recoverable degrade/teardown → WARN; the split is uniform across both sibling functions, no guarded-here-but-not-there inconsistency.
Note (13.4 — CLEAN, no error-swallowing macro): `STEPPE_CUDA_WARN` does NOT hide errors — it emits one `STEPPE_LOG_WARN` line (debug) AND, build-mode-INDEPENDENTLY, RETURNS the `cudaError_t` (`[[nodiscard]]`, check.cuh:174,188) so a caller can branch; it is the §11.4 capability-tier sibling, not a swallow. At this unit's two call sites the return IS intentionally `(void)`-discarded, but each is a documented recoverable case (peer-enable AlreadyEnabled; non-throwing dtor restore), not a swallowed fault — the fault path is the separate throwing `STEPPE_CUDA_CHECK`. No homegrown macro returns/ignores a status silently; no `cudaGetLastError()` is called only to drop a real error (the one clear at cu:160,280 is by-design after a handled WARN).

## Group 14 — Memory: allocation & lifetime

No Group 14 issues found.

Note (14.1 — CLEAN, no alloc/free mismatch): this TU calls NO raw `cudaMalloc`/`cudaFree`/`malloc`/`new[]` directly — it is not one of the three allowlisted allocation TUs (device_buffer.cuh, allocator.cu, pinned_buffer.cuh). Every device allocation is a `DeviceBuffer<double>` (cu:106-107 `dResult_f2`/`dResult_vp`; cu:243-244 `out.impl->f2`/`out.impl->vpair`) whose ctor `cudaMalloc`s and whose dtor `cudaFree`s the SAME pointer (device_buffer.cuh:74,117) — matched family. Pinned host memory is a `RegisteredHostRegion` (cu:186-187 `pin_f2`/`pin_vp`) pairing `cudaHostRegister`↔`cudaHostUnregister` (pinned_buffer.cuh:164,201), NOT alloc/free — correctly paired and never crossed with `cudaFree`/`free`. The stream is the RAII `Stream` (cu:91,224) pairing `cudaStreamCreateWithFlags`↔`cudaStreamDestroy` (stream.hpp:59,88). The host result `F2BlockTensor::f2/vpair`/`block_sizes` are `std::vector` (cu:117-119) — ordinary host RAII, never handed to a CUDA free. No `cudaMalloc`-then-`free` or `new[]`-then-`cudaFree` cross.

Note (14.3 — CLEAN, no async/sync free cross): there is NO `cudaMallocAsync`/`cudaFreeAsync` (nor `cudaMallocFromPoolAsync`) anywhere in this unit or in its allocation owner `DeviceBuffer` (device_buffer.cuh uses synchronous `cudaMalloc`/`cudaFree` only). So there is no stream-ordered allocation that could be wrongly freed with the plain (or async) counterpart — the pairing question is moot. The async calls present (`cudaMemcpyAsync`/`cudaMemcpyPeerAsync`, cu:145-148,166-169,188-191,271-273,283-286) are DATA MOVEMENT on `root_stream`, not allocations, so they pair with a stream drain (below), not a free.

Note (14.4 — CLEAN, no free-before-async-work / cross-stream use-after-free): every buffer that an enqueued async copy reads or writes outlives that copy's completion, enforced by an explicit drain BEFORE the owner is destroyed (the §7 lifetime story). (a) `dResult_f2`/`dResult_vp` are the D2D/peer WRITE targets (cu:138-139,145-148,166-169) and then the final D2H SOURCE (cu:188-189); the `cudaStreamSynchronize(root_stream)` at cu:183 drains the placement copies and the one at cu:192 drains the D2H, and BOTH fire before the function returns at cu:194 (where the DeviceBuffer dtors run) — the result buffers are never freed while a copy on them is in flight. (b) The pinned D2H destinations `pin_f2`/`pin_vp` wrap `out.f2/vpair.data()`; the trailing `cudaStreamSynchronize` at cu:192 sits INSIDE the `if (total>0)` scope, so it completes the D2H BEFORE the RegisteredHostRegion dtors unregister those pages at the if-scope close (the cu:179-182 "keeps the pin alive across the DMA" rationale — correct ordering, verified). (c) The peer/root SOURCE partials `part.impl->f2/vpair` are owned by the caller's `partials` vector and are documented to be freed by the caller AFTER this returns (§7, hpp:80-83); the cu:183 drain (and cu:297 in the device sibling) guarantees the D2D/peer DMA reading them has completed before the function returns, so no read outlives a freed source — the deliberately-absent per-peer `cudaDeviceSynchronize` (cu:171-174,288-290) is safe precisely because nothing is freed mid-loop. (d) The device-resident sibling RETURNS `out.impl` (moved out, cu:298), so its result buffers survive the function; the cu:297 drain completes every DMA writing them before `root_stream_owner` is destroyed at scope exit. The `root_stream_owner` (created on root, cu:91/224) is destroyed before the `DeviceGuard restore` rebinds `prev_device` (reverse declaration order: restore declared first at cu:85/220, destroyed last), so the stream is destroyed while root is still current — correct.

Note (14.5 — CLEAN, no missing frees on error paths): every owned resource is RAII, so a `STEPPE_CUDA_CHECK` throw anywhere (the D2D/peer copies, the D2H, or a stream sync) unwinds the stack and destroys `dResult_f2`/`dResult_vp`, `root_stream_owner`, and (if reached) `pin_f2`/`pin_vp` — no manual free to skip, no leak. The `DeviceGuard restore` dtor (cu:82-85,217-220) fires on the throw path too (exception-safe device rebind, cu:76-79). The early-throw from `validate_resident_partials` (cu:70-71,209-210) happens BEFORE any allocation, so nothing is owned yet. No raw resource is held across a throwable call without an owner.

Note (14.2 — observational, NOT flagged): the full-shape result buffers (`DeviceBuffer` at cu:106-107,243-244) use synchronous `cudaMalloc`/`cudaFree` (in the owner), which globally synchronize. This is NOT flagged as a hot-path issue because (i) the combine runs ONCE per multi-GPU f2-tensor assembly (a coarse, full-tensor DMA operation), not per-solve in the S8 inner loop, so the alloc is amortized over a multi-GB transfer; and (ii) steppe's architecture (§2 DRY allocation grep-gate) deliberately confines the allocation family to THREE allowlisted owner TUs and does not adopt `cudaMallocAsync`/pools — switching this TU to `cudaMallocAsync` would violate that single-source owner policy. So the synchronous alloc here is an intentional design choice, consistent with the project's RAII-owner model, not a memory-lifetime defect.

## Group 15 — Memory: transfers

No Group 15 issues found.

Note (15.1 — CLEAN, the per-partial loop copies are essential, not hoistable): the placement loop (cu:122-175, 249-291) issues a `cudaMemcpyAsync(...DeviceToDevice)` for the root's own partial (cu:145-148, 271-273) and a `cudaMemcpyPeerAsync` for each peer's partial (cu:166-169, 283-286) ONCE per g. These are NOT a copy that should be hoisted: each iteration moves a DISTINCT source partial (`part.impl->f2/vpair`, resident on a DIFFERENT `part.device_id`) into a DISJOINT result slice at a per-partial offset (`dst_off = slab*(size_t)part.b0`, cu:136,262) — different src AND different dst every iteration, no loop-invariant transfer to lift out. They are all enqueued ASYNC on the single `root_stream` (cu:92,225) so they PIPELINE, with the per-peer `cudaDeviceSynchronize` deliberately ABSENT (cu:171-174,288-290) and exactly ONE drain (cu:183,297). This IS the M4.5 cure (doc §4 Item 1, cu:13-20): the resident partials are consumed in place — NO host bounce, NO H2D re-upload, NO staging buffer — i.e. the very kept-resident/batched discipline task 15.1 asks for. The final full-tensor D2H (cu:188-191) is a SINGLE transfer OUTSIDE the loop (one `cudaMemcpyAsync` per tensor), already batched; the device-resident sibling omits the D2H entirely (stays in VRAM, cu:293-298).
Note (15.2 — CLEAN, every direction enum matches the actual src/dst spaces): D2D `cudaMemcpyDeviceToDevice` (cu:146,148,272-273) — both src (`part.impl->f2/vpair`, device-resident on the root, cu:140-141,266-267) and dst (`dResult_f2/vp` / `result_f2/vp`, root device buffer, cu:138-139,264-265) are DEVICE pointers; correct. `cudaMemcpyPeerAsync(dst, root_device_id, src, part.device_id, ...)` (cu:166-169,283-286) — the explicit dst-device/src-device ids match the true ownership (dst on root, src resident on `part.device_id` the peer); correct, and it carries no direction enum to mismatch (peer-copy uses device ids, not the kind enum). The single D2H `cudaMemcpyDeviceToHost` (cu:189,191) — src `dResult_f2/vp.data()` (device) → dst `out.f2/vpair.data()` (host std::vector); direction correct. No reversed/wrong-kind enum.
Note (15.3 — CLEAN, the only H<->D transfer is already pinned): the host-returning sibling's single final D2H (cu:188-191) — the ONLY transfer touching host memory in either function — is explicitly PINNED for the transfer window via `RegisteredHostRegion pin_f2`/`pin_vp` over `out.f2/vpair.data()` (cu:186-187), the documented M4.5 d2h-speed pinning with graceful pageable degrade (cu:177-182), and the trailing `cudaStreamSynchronize` (cu:192) keeps the pin alive across the DMA before the RAII unregister — so the highest-bandwidth case is already taken. Every other transfer in both functions is D2D or peer-to-peer (cu:145-148,166-169,271-273,283-286): no pageable HOST buffer participates, so pinning is N/A there. The device-resident sibling performs NO host transfer at all (result stays in VRAM). No frequent pageable H<->D path left un-pinned.

## Group 16 — RAII: ownership & wrapper hygiene

- [16.3][LOW] src/device/cuda/p2p_combine.cu:82-85,217-220 — The local `struct DeviceGuard { int dev; ~DeviceGuard()... }` has a user-declared, side-effecting destructor (it issues `cudaSetDevice(dev)` to restore the device binding) but declares NO copy/move members, so it implicitly remains COPYABLE. A copy would re-fire the restoring `cudaSetDevice` once per copy (the action-on-dtor twice). This is not currently triggered — `restore{prev_device}` is aggregate-initialized in place and never copied/moved — so it is pure hygiene (latent), and `DeviceGuard` is a scope-restore guard, not a freeing/owning wrapper, so a "double" here is a redundant rebind, not a double-free. Suggested: delete the copy/move members (`DeviceGuard(const DeviceGuard&) = delete;` etc.) or lift it to one file-scope move-only scope-guard type (this would also resolve the verbatim duplication already flagged under Group 7.2); a one-line `const int dev` is also fine since it is set once.

Note (16.1 — CLEAN, every resource is wrapped): this TU is a host-side orchestration unit that calls NO raw resource-acquire/free family directly — there is no bare `cudaStreamCreate`/`cudaEventCreate`/`cudaMalloc`/`cudaHostAlloc`/`cudaHostRegister`/graph/graph-exec/texture/surface/memory-pool/CUDA-array/cuBLAS/cuSOLVER `*Create`. Every resource it uses is held by an existing RAII owner: the combine's stream is `Stream root_stream_owner` (cu:91,224; `cudaStreamCreateWithFlags`↔`cudaStreamDestroy`, stream.hpp:59,88), the full-result device memory is `DeviceBuffer<double>` (cu:106-107,243-244; `cudaMalloc`↔`cudaFree`, device_buffer.cuh:74,117), the device-resident sibling's result `Impl` is held via `std::make_unique<DeviceF2Blocks::Impl>()` (cu:242, owning the two `DeviceBuffer<double>` members, device_f2_blocks_impl.cuh:15-16), and the D2H host destinations are page-locked by `RegisteredHostRegion pin_f2`/`pin_vp` (cu:186-187; `cudaHostRegister`↔`cudaHostUnregister`, pinned_buffer.cuh:164,201). The `cudaDeviceEnablePeerAccess` (cu:159,279) is NOT a freeable handle — it sets process-global directional peer-access state deliberately left enabled across combine calls (the `AlreadyEnabled` 2nd-call status is expected; hpp:75-78) — so its absence of a matching disable is by design (the §11.4 capability tier), not a leaked resource. No un-wrapped resource exists.

Note (16.2 — N/A in this TU; the wrappers it consumes are correctly move-only + null-on-move): this unit DEFINES no resource wrapper class (only the local non-owning `DeviceGuard` scope guard, covered under 16.3). The owners it consumes all implement copy=deleted + null-on-move correctly: `Stream` (copy deleted stream.hpp:72-73; move-ctor/assign `std::exchange(o.s_, nullptr)` stream.hpp:62,67), `DeviceBuffer` (copy deleted device_buffer.cuh:90-91; move `std::exchange(o.ptr_, nullptr)`/`size_,0` device_buffer.cuh:78-79,84-85), and `RegisteredHostRegion` (copy deleted pinned_buffer.cuh:188-189; move `std::exchange(o.ptr_, nullptr)` pinned_buffer.cuh:178,183) — so no copyable-freeing-wrapper double-free and no un-nulled moved-from is reachable through this TU. The device-resident sibling RETURNS `out` by value (cu:298), relying on those move semantics — sound.

Note (16.3 — the consumed freeing wrappers have a complete rule-of-five): `Stream`/`DeviceBuffer`/`RegisteredHostRegion`/`PinnedBuffer` each declare the freeing destructor + deleted copy + defaulted-out move-ctor and move-assign with a self-assignment guard and a `reset()`/`destroy()` before taking the moved-from handle (stream.hpp:64-75, device_buffer.cuh:81-93, pinned_buffer.cuh:88-100,180-191) — the full five. The only rule-of-five gap is the LOCAL `DeviceGuard` (above, 16.3 LOW), whose dtor performs an action rather than a free.

Note (16.4 — CLEAN, single clear ownership; raw pointers are non-owning views; no owning wrapper passed by value): the two entry points take their inputs as non-owning `std::span<DevicePartial>` / `std::span<const DeviceShard>` (cu:62-63,205-206) — views, never owners-by-value — and the routine explicitly does NOT take ownership of or free the resident source partials (it reads `part.impl->f2/vpair.data()` in place; the caller frees them AFTER return, §7, hpp:80-83). Every raw `double*` (`dst_f2`/`dst_vp`/`src_f2`/`src_vp` cu:138-141,264-267; `dResult_*.data()`/`result_*` cu:106-107,245-246; `out.f2/vpair.data()` cu:188-191) is a non-owning view into a `DeviceBuffer`/`std::vector`/`Impl` owner handed to `cudaMemcpy*` — none is `cudaFree`'d here. The result `out` (`F2BlockTensor` cu:114 / `DeviceF2Blocks` cu:237) is the single owner, built locally and returned by value (move). No owning wrapper (`Stream`/`DeviceBuffer`/`RegisteredHostRegion`) is ever passed or returned by value into a sink that would copy-free it.

Note (16.5 — CLEAN, no reinvented wrapper): the unit uses the project's canonical owners (`DeviceBuffer<double>`, `Stream`, `RegisteredHostRegion`) and `std::make_unique` for the `Impl` — it does not hand-roll a device-memory or stream RAII type. The only home-grown local type, `DeviceGuard` (cu:82-85,217-220), is a device-BINDING scope-restore guard (save/restore `cudaSetDevice`), a distinct idiom for which the codebase has no existing wrapper and which is NOT a resource-freeing wrapper that `unique_ptr`+deleter or `thrust::device_vector` would replace — so it is not a reinvention of an existing owner (its copyability is the 16.3 LOW, and its verbatim duplication across the two functions is already Group 7.2). No bespoke reimplementation of an allocator/owner that an off-the-shelf or existing-steppe wrapper already covers.

## Group 17 — RAII: lifetime & deleter pitfalls (CUDA-specific)

No Group 17 issues found.

Note (17.1 — CLEAN, the only dtor in this TU is non-throwing): the single user-declared destructor in this unit is the local `DeviceGuard::~DeviceGuard()` (cu:84, 219), which restores the device via `(void)STEPPE_CUDA_WARN(cudaSetDevice(dev))` — routed through the NON-throwing WARN (check.cuh), never the throwing CHECK, so a failed restore at teardown is logged-and-swallowed, never thrown during unwinding (the cu:76-79/82-85 "teardown WARN, never throw" rationale; correct §7 behavior, fires on the early-throw/fault paths too). Teardown-order at process exit is safe: even if the context were gone, `cudaSetDevice` would WARN-degrade, not crash. The owners this TU consumes all carry non-throwing dtors that log-and-swallow a nonzero teardown status: `DeviceBuffer::reset()` (device_buffer.cuh:111-122, `cudaFree` -> STEPPE_LOG_WARN), `Stream::destroy()` (stream.hpp:84-95, `cudaStreamDestroy` -> WARN), `RegisteredHostRegion::reset()` (pinned_buffer.cuh:197-208, `cudaHostUnregister` -> WARN). No dtor in or reachable from this TU can throw.
Note (17.2 — CLEAN, every deleter matches its allocator): this TU performs NO raw allocation, so all alloc/free pairing lives in the consumed owners and is correctly matched — `DeviceBuffer` `cudaMalloc` (device_buffer.cuh:74) <-> `cudaFree` (device_buffer.cuh:117) for `dResult_f2/vp` (cu:106-107) and `out.impl->f2/vpair` (cu:243-244); `RegisteredHostRegion` `cudaHostRegister` (pinned_buffer.cuh:164) <-> `cudaHostUnregister` (pinned_buffer.cuh:201) for `pin_f2/vp` (cu:186-187); `Stream` `cudaStreamCreateWithFlags` (stream.hpp:59) <-> `cudaStreamDestroy` (stream.hpp:88) for `root_stream_owner` (cu:91,224). No `cudaFreeAsync`/`cudaMallocAsync`/`cudaFreeArray`/`cudaMallocArray` anywhere (the unit only DMAs bytes), so no async- or array-deleter cross is possible. The `cudaDeviceEnablePeerAccess` (cu:159,279) sets process-global directional state, not a freeable handle, so it has no deleter to mismatch (the absent disable is by design; §11.4, hpp:75-78).
Note (17.3 — CLEAN, no unique_ptr<T[]> over device memory): the only `std::make_unique` (cu:242) is `std::make_unique<DeviceF2Blocks::Impl>()` — a SINGLE host heap object (default `delete`, not `delete[]`) that itself owns two `DeviceBuffer<double>` members (device_f2_blocks_impl.cuh), each of which `cudaFree`s through its own dtor. There is no `unique_ptr<T[]>`, and no raw `cudaMalloc` pointer is ever handed to a default `delete`/`delete[]`; device memory is exclusively owned by `DeviceBuffer` (whose dtor calls `cudaFree`, not `delete[]`). So the `unique_ptr<T[]>`-over-cudaMalloc UB does not arise.
Note (17.4 — CLEAN, async work is drained before any RAII free): every async copy on `root_stream` (the D2D `cudaMemcpyAsync`, the `cudaMemcpyPeerAsync`, the final D2H) is fenced by a `cudaStreamSynchronize(root_stream)` BEFORE the owner that backs its src/dst is destroyed — so no free-at-scope-exit races in-flight async work. Host-returning sibling: the placement copies are drained at cu:183 and the D2H at cu:192, both BEFORE the `dResult_f2/vp` `DeviceBuffer` dtors and the `RegisteredHostRegion` pin dtors run (the pins specifically unregister at the cu:184 if-scope close, AFTER the cu:192 sync — the "keeps the pin alive across the DMA" ordering, cu:179-182, verified correct). Device-resident sibling: the cu:297 drain completes every D2D/peer DMA writing `out.impl->f2/vpair` BEFORE the function returns; `out` is RETURNED BY VALUE (move, cu:298), so its result `DeviceBuffer`s are moved out and survive the call rather than being freed here. The peer/root SOURCE partials (`part.impl->f2/vpair`, cu:140-141,266-267) are owned by the caller and freed AFTER return (§7, hpp:80-83); the pre-return drain (cu:183 / cu:297) guarantees the reading DMA has completed before any source is freed — which is exactly why the deliberately-absent per-peer `cudaDeviceSynchronize` (cu:171-174,288-290) is safe (nothing is freed mid-loop). The `root_stream_owner` is destroyed (reverse declaration order) AFTER `out`/`dResult_*` but BEFORE the `DeviceGuard restore` rebinds the caller's device, so `cudaStreamDestroy` runs while the root is still current — correct teardown order.
Note (17.5 — CLEAN, device-correct free does not require a per-free cudaSetDevice here): the result `DeviceBuffer`s (`dResult_f2/vp` cu:106-107; `out.impl->f2/vpair` cu:243-244) are all allocated on the ROOT (the routine `cudaSetDevice(root_device_id)` at cu:86/221 before any allocation), so they are single-device root-resident — there is no multi-device alloc-set whose frees would need to fan out to different devices. `DeviceBuffer::~DeviceBuffer` calls `cudaFree(ptr_)` WITHOUT a `cudaSetDevice` (device_buffer.cuh:111-122), which is correct because `cudaFree` is pointer-device-aware (it frees the allocation on whatever device it was allocated on, independent of the current device) — the unit's own cu:77-79 comment asserts exactly this ("cudaFree on the resident handles ... is pointer-device-aware, so the restored device does not matter for their teardown, §7"). Consequently the `DeviceGuard` restoring the CALLER's device at scope exit (cu:84,219) cannot cause a wrong-device free of either the local result buffers (freed before/at return on the root path) or the caller-owned source partials (freed after return): cudaFree ignores the current device. The device-resident sibling's returned `out.impl` buffers are likewise root-allocated and free correctly later under any current device. No per-deleter `cudaSetDevice`-to-alloc-device is needed because no owner in play relies on the current device for its free.

## Group 18 — Correctness traps (wrong numbers, not crashes)

No Group 18 issues found.

Note (18.1/18.2/18.3/18.4 — N/A, no kernels): this unit is a pure HOST-side orchestration TU — it contains NO `__global__`/`__device__` kernels and NO `<<<grid,block>>>` launches (already established under Group 12.1-12.4). So there is no `__syncthreads()` at all (no divergent barrier reached by only some threads, 18.1; no shared-mem write-then-read missing a barrier, 18.2 — there is NO shared memory and NO block-level cooperation in this TU), no warp-synchronous-execution assumption (18.3), and no warp intrinsic to require a `_sync` variant + explicit mask (18.4 — no `__shfl*`/`__ballot`/`__any`/`__all`/`__activemask`, grep-confirmed under Group 2.3). All device work is runtime DMA (`cudaMemcpyAsync`/`cudaMemcpyPeerAsync`, cu:145-148,166-169,188-191,271-273,283-286) ordered on one `root_stream`, device-bind (`cudaSetDevice` cu:86,221), and peer-enable (cu:159,279).
Note (18.5 — N/A, no GPU thread index to bound): there is no kernel thread-index pattern (`idx = blockIdx*blockDim + threadIdx`) that could run OOB without an `if(idx<n)` guard. The only iteration is the two HOST `for` loops — `g < partials.size()` (cu:122,249) over the G partials and `lb < part.n_block_local` (cu:128,254) over each partial's local blocks — which traverse their full ranges by construction and cannot over-index. The destination writes are bounded by the validated disjoint tiling: `validate_resident_partials` (cu:70-71,209-210) enforces that the shards tile `[0,n_block_full)` exactly, so each per-partial slab-stack of `part_bytes` lands at `dst_off = slab*(size_t)part.b0` strictly inside `total = slab*(size_t)n_block_full` (every offset widened to size_t before the multiply, Group 4.2/4.6 note). The `if(total>0)` guard (cu:184) gates the final D2H against a zero-size transfer. No silent OOB.
Note (18.6 — N/A as a thread-race; the cross-DEVICE read is correctly DMA-ordered): there is no shared/global cross-THREAD read without a barrier (no kernel threads). The analogous cross-DEVICE read — the root pulling each peer's resident partial via `cudaMemcpyPeerAsync` (cu:166-169,283-286) — is correctly serialized: all copies are enqueued on the single `root_stream` (cu:92,225) and one `cudaStreamSynchronize` (cu:183 host-returning / cu:297 device-resident) drains every DMA BEFORE any source partial is freed by the caller (§7, hpp:80-83) and before the result is read downstream. The peer source buffers are NOT freed mid-loop (cu:171-174,288-290 rationale), so the deliberately-absent per-peer sync is safe — no read of a stale/freed peer buffer (already cross-checked under Groups 14.4 / 17.4).
Note (18.7 — N/A, this combine is a PLACEMENT, not a reduction): there is NO float reduction / accumulation anywhere — the combine is a DISJOINT raw byte PLACEMENT (each result slab written EXACTLY ONCE by its owning device's D2D/peer copy into a non-overlapping slice, cu:99-105,143-170), NOT a sum onto an accumulator (the pre-cleanup-B7 memset+`+=` path was deleted; cu:36-37). So there is no float-add whose result depends on summation order, and §12 bit-identity here rests on byte-identity of the moved doubles, not on reduction ordering. The one fixed-order traversal — the `g=0..G-1` loop placing `block_sizes` (cu:128-131,254-257) — is an INTEGER copy into disjoint `[b0..b0+n_block_local)` slots on a deterministic HOST loop (no float, no cross-thread order dependence), so it is the correct deterministic-order kind, not an order-assuming reduction.

## Group 19 — Performance: debug leftovers

No Group 19 issues found.

Note (19.1 — CLEAN, no stray cudaDeviceSynchronize): there is NO `cudaDeviceSynchronize()` call anywhere in this TU. The ONLY synchronization is `cudaStreamSynchronize(root_stream)` (cu:183, 192, 297) — the deliberate minimal drains: one fence before the final D2H and one after it (host-returning sibling), and one fence after the placement loop (device-resident sibling). These are scoped to the combine's own non-default `root_stream` (cu:92,225), not a process-wide device serialize, and each has a documented lifetime rationale (drain before any source partial is freed / before the result is read; cu:177-182, 293-296). The DELIBERATELY-ABSENT per-peer `cudaDeviceSynchronize` is documented as such (cu:171-174, 288-290: "NO per-peer cudaDeviceSynchronize here") — its name appears only in prose explaining why it is omitted, NOT as a call. No debug-leftover full-device sync that would serialize the pipelined peer copies. (STEPPE_CUDA_CHECK_KERNEL's NDEBUG-gated sync is N/A here — this TU has no kernels.)
Note (19.2 — CLEAN, no leftover printf / #if 0): no `printf`, `fprintf`, `std::cout`/`std::cerr`, `puts`, or any stray stdout/stderr write in either file. The only diagnostic emission is via the project's `STEPPE_CUDA_WARN` logging macro (cu:159,279 peer-enable; the `~DeviceGuard` restore cu:84,219), which is intentional recoverable-status logging, not a debug printf. No `#if 0` / `#ifdef`-disabled region and no code-after-`return` (already cross-confirmed CLEAN under Group 3.2). The large comment blocks (cu:1-41, 99-105, 197-203) are design/parity rationale, not disabled code.
Note (19.3 — N/A, no kernels so no __syncthreads): this unit is a pure HOST-side orchestration TU with NO `__global__`/`__device__` kernels and NO `<<<grid,block>>>` launches (established under Groups 12.1-12.4, 18.1). There is therefore NO `__syncthreads()` (nor `__syncwarp`/`__threadfence`) anywhere — no redundant or no-longer-guarding block barrier can exist. The only ordering primitive is the stream-level `cudaStreamSynchronize` drain (cu:183,192,297), which IS load-bearing (see 19.1), not a redundant barrier.

## Group 20 — Performance: memory access

No Group 20 issues found.

Note (20.1 — N/A, no kernel = no thread-level coalescing to assess): all three Group 20 tasks are GPU-kernel memory-access concerns; this unit is a pure HOST-side orchestration TU with NO `__global__`/`__device__` kernels and NO `<<<grid,block>>>` launches (established under Groups 12.1-12.4, 18.1, 19.3), so there are NO consecutive threads issuing global loads/stores whose addresses could be uncoalesced. The actual device data movement is the contiguous DMAs `cudaMemcpyAsync(...DeviceToDevice)` (cu:145-148,271-273), `cudaMemcpyPeerAsync` (cu:166-169,283-286), and the final D2H (cu:188-191) — each moves a SINGLE contiguous byte range (a source slab-stack of `part_bytes` from `part.impl->f2/vpair.data()` into a disjoint contiguous result slice at `dst_off = slab*(size_t)part.b0`, cu:134-141,260-267). The copy engine streams contiguous bytes; there is no per-thread stride to coalesce, and the source and destination are both densely contiguous (no gather/scatter), so the transport is already the bandwidth-optimal access pattern.
Note (20.2 — N/A, no shared memory): there is NO `__shared__` declaration and NO kernel-block cooperation anywhere in this TU (no kernels at all), so there is no shared-memory access pattern that could serialize on bank conflicts. All ordering is stream-level (`cudaStreamSynchronize`, cu:183,192,297); all storage is global device buffers (`DeviceBuffer<double>`, cu:106-107,243-244) and host `std::vector`/pinned regions — none of which involves a shared-memory bank.
Note (20.3 — CLEAN, no repeated global re-read; every value is cached in a const local): in the host placement loops (cu:122-175,249-291) each per-partial quantity is read ONCE and reused: `part.impl->f2.data()`/`vpair.data()` are cached in `src_f2`/`src_vp` (cu:140-141,266-267), the result base `dResult_f2.data()`/`dResult_vp.data()` (or `result_f2`/`result_vp`) plus `dst_off` are folded once into `dst_f2`/`dst_vp` (cu:138-139,264-265), and the loop-invariant `slab`/`total`/`root_device_id`/`root_stream` are hoisted to `const` locals before the loop (cu:94-97,227-229,92,225) — none is recomputed or re-fetched per use. NOTE this is host pointer/scalar bookkeeping, NOT GPU global-memory re-reads (the task's true target, which requires a kernel); the device-side bytes are touched exactly once each by the contiguous copy engine (the disjoint tiling writes every result slab EXACTLY ONCE, cu:99-105), so there is no value read from device global memory multiple times that should be staged in a register/shared. The inner `block_sizes` copy (cu:128-131,254-257) reads host struct fields (`part.b0`, `part.n_block_local`, `part.block_sizes[lb]`), not device global memory.

## Group 21 — Performance: occupancy & registers

No Group 21 issues found.

Note (21.1/21.2/21.3/21.4 — N/A, no kernels = no occupancy/register surface): all four Group 21 tasks are GPU-kernel concerns (warp divergence, per-block shared memory, register spills, register hints), but this unit is a pure HOST-side orchestration TU with NO `__global__`/`__device__` kernels and NO `<<<grid,block>>>` launches anywhere — established under Groups 12.1-12.4, 18.1, 19.3, 20.1, and grep-confirmed (no `__global__`/`__device__`/`<<<`/`__shared__`/`__launch_bounds__`/`#pragma unroll`/`__forceinline__`/`__restrict__`). Both entry points (`combine_f2_partials_resident` cu:61, `combine_f2_partials_resident_device` cu:204) are implicitly-`__host__` functions whose ONLY device work is runtime DMA (`cudaMemcpyAsync` cu:145-148,188-191,271-273; `cudaMemcpyPeerAsync` cu:166-169,283-286) plus device-bind (cu:86,221) and peer-enable (cu:159,279). The copy engine executes the transfers, not a steppe-authored kernel grid, so there is NO warp to serialize on a branch (21.1), NO `__shared__` per-block allocation to crater occupancy (21.2), NO monolithic kernel whose live-range pressure could spill to local memory (21.3), and NO tight device loop / kernel signature where `#pragma unroll`, `__launch_bounds__`, or `__forceinline__` could help (21.4).
Note (21.1 — the only branching is HOST control flow, not warp divergence): the `if (part.device_id == root_device_id)` root-vs-peer split (cu:143,269), the `if (part.empty()) continue;` early-out (cu:132,258), and the `if (total > 0)` D2H guard (cu:184) are HOST `if`s evaluated once per partial / once per call on the CPU — there is no SIMT lane that could diverge. The two `for` loops (`g < partials.size()` cu:122,249; `lb < part.n_block_local` cu:128,254) are sequential host loops over small counts (G devices ~ single digits; n_block per partial <= n_block_full ~757), not thread grids; they carry no per-lane predication and no `__forceinline__`/`#pragma unroll` would change a device-side instruction stream (there is none). Any unroll of these tiny host loops is a compiler decision on ordinary CPU code, immaterial to GPU occupancy.
Note (21.4 — host-call params are correctly lightweight; no kernel-attribute opportunity missed): both entry points take only `std::span` (16-byte ptr+size, by value) and `int` scalars (cu:62-64,205-207) — a host calling convention, NOT a kernel parameter bank, so `__launch_bounds__`/`__grid_constant__` are inapplicable (no launch) and would be wrong to add. The src pointers are already `const double*` (cu:140-141,266-267); `__restrict__` confers nothing on opaque `cudaMemcpy*`/`cudaMemcpyPeerAsync` calls (Runtime API copies via `void*`, no aliasing-optimizable kernel body), so its deliberate absence is correct here, not a missing hint. Adding any of these reflexively would be noise on a kernel-free TU.

## Group 22 — Performance: compute & launch

No Group 22 issues found.

Note (22.1 — CLEAN, no atomics; the combine is by-design a placement, not a reduction): there is NO atomic anywhere in this TU — no `atomicAdd`/`atomicCAS`/`atomicExch`/`atomicMax` (grep-confirmed; this is also a host-only TU with no kernels in which a device atomic could appear). The combine is a DISJOINT raw byte PLACEMENT: each result slab is written EXACTLY ONCE by its owning device's `cudaMemcpyAsync`(D2D)/`cudaMemcpyPeerAsync` into a non-overlapping slice (cu:99-105,143-170,269-287), so there is NO cross-source accumulation that an atomic or a reduction/scan would implement — the pre-cleanup-B7 memset+`+=` accumulate path was deleted (cu:36-37). So the "atomics where a proper reduction would be cheaper" trap cannot arise: there is neither an atomic to replace nor a reduction to perform. The `block_sizes` placement (cu:128-131,254-257) is a disjoint integer host-side copy into `[b0..b0+n_block_local)` slots, not a contended accumulate.
Note (22.2 — CLEAN, no integer div/mod in either loop): neither placement loop (cu:122-175 / 249-291) nor the inner block_sizes loop (cu:128-131 / 254-257) contains a `/` or `%`. Every per-partial offset/size is a MULTIPLY on widened operands — `part_elems = slab * (size_t)part.n_block_local` (cu:134,260), `part_bytes = part_elems * sizeof(double)` (cu:135,261), `dst_off = slab * (size_t)part.b0` (cu:136,262) — with `slab = (size_t)P*(size_t)P` and `total = slab*(size_t)n_block_full` hoisted before the loop (cu:94-97,227-229). No division-by-stride, no modulo-decompose-index pattern, so there is nothing to convert to shifts/masks (and these are host loops over single-digit-G / <=~757-block ranges anyway — even were there a div, it would not be a GPU-thread cost). The only `+` in-loop is `part.b0 + lb` (cu:129,255), a cheap int add.
Note (22.3 — CLEAN, all loop-invariant work already hoisted; no repeated index recompute): the loop-invariant scalars/handles — `slab`, `total`, `root_device_id`, `root_stream` (cu:94-97,92,225,227-229) — are computed ONCE before the placement loop and only read inside it. Per-iteration, each derived quantity is computed once into a `const` local and reused: `part_elems`/`part_bytes`/`dst_off` (cu:134-136,260-262), the result-base+offset folded once into `dst_f2`/`dst_vp` (cu:138-139,264-265), and the sources cached once in `src_f2`/`src_vp` (cu:140-141,266-267). No index or pointer base is recomputed across the f2-then-vpair copy pair or re-fetched per use (this overlaps the Group 20.3 finding, here re-checked from the compute-hoisting angle). The inner block_sizes loop reads `part.b0`/`part.block_sizes[lb]` directly — trivial host indexing, no hoistable invariant beyond what is already `const`.
Note (22.4 — CLEAN, no kernel launches; DMAs already pipelined on one stream with a single drain): this TU issues NO `<<<grid,block>>>` kernel launches at all (established Groups 12.1-12.4, 21.x), so there is no "many small kernel launches dominated by per-launch cost" to fuse or capture into a CUDA graph. The device work is runtime DMA: per partial, a D2D `cudaMemcpyAsync` pair or a `cudaMemcpyPeerAsync` pair, ALL enqueued ASYNC on the single non-default `root_stream` (cu:92,225) so they PIPELINE, with the per-peer `cudaDeviceSynchronize` deliberately ABSENT (cu:171-174,288-290) and exactly ONE drain (cu:183/192 host-returning, cu:297 device-resident). This is already the minimal-launch / batched-transfer shape task 22.4 asks for; introducing a CUDA graph would be unjustified — the combine runs ONCE per multi-GPU f2-tensor assembly (a coarse, multi-GB transfer), NOT per-solve in the S8 inner loop, and the spec's own criterion ("only where the profiler confirms it") is not met. Observational (NOT flagged): the per-peer `cudaDeviceEnablePeerAccess(part.device_id,0)` + `(void)cudaGetLastError()` pair (cu:159-160,279-280) re-runs every combine call (returning the expected `AlreadyEnabled` on 2nd+ calls), a small host-side runtime call per peer per call — but G is single-digit and the call is amortized over the multi-GB DMA, so it is not a per-launch-cost hot spot; moving the enable to a once-per-process gate would belong to the caller's §4 peer-access gate (f2_blocks_multigpu.cpp), not this transport routine.
