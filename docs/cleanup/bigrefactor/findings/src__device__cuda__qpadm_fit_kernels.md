# Review findings — src__device__cuda__qpadm_fit_kernels

Files: /home/suzunik/steppe/src/device/cuda/qpadm_fit_kernels.cu, /home/suzunik/steppe/src/device/cuda/qpadm_fit_kernels.cuh

## Group 4 — Type & numeric

- [4.2][LOW] qpadm_fit_kernels.cu:394,468,701,842,957-959,989,1031-1032,1074-1075,1203,1231,1264-1266 and launch wrappers 1291,1305,1319,1340,1371,1383,1395,1545 — All global indices/offsets into the resident f2 tensor (P*P*nb, up to ~10^10 elems), the dLoo/dQinv/dX/dXtau/dWmat strided model arenas, and the per-thread VRAM scratch slices are computed in 64-bit `long` (the multiplicand is `static_cast<long>(...)`-widened BEFORE the product, e.g. f2 slab at :405 `static_cast<long>(P)*static_cast<long>(P)*b`, model bases at :1055/:1108 `static_cast<long>(model)*m*m`, scratch slice at :851 `gid*dbl_refit`). No int-before-widen overflow found on any large-arena path. The only hygiene note: `long` (not `int64_t`/`size_t`) is the index type throughout — correct on Linux LP64 (64-bit), but `long` is 32-bit on LLP64/Windows; the project is Linux/CUDA-only so this is hygiene, not a bug. Suggested: optionally standardize on `std::int64_t`/`size_t` for the global-index type to make the 64-bit intent platform-independent; no functional change needed.
- [4.6][LOW] qpadm_fit_kernels.cu:1153-1157,1115,1150,933,861,1214 — Thread-local index arithmetic that stays in `int` (e.g. fit_reduced `ia = surv[a/nr]*nr + (a%nr)` and `qinv[ia + m*ib]`; xmat reshapes `total[j + nr*i]`, `loo[(j+nr*i) + (long)m*b]`) is bounded by m = nl*nr <= kQpMaxM = 50 in the SMALL bucket (the host bucketer enforces nl<=5, nr<=10, r<=4 before launch), so the products max ~50*49 — far under 2^31. Correctly NOT widened (no overflow). The block-offset factor `(long)m*b` in the loo reshapes IS widened. Suggested: none — bound is enforced by the documented small-path guard; leave as-is.

No further Group 4 issues found. (4.1 float-vs-double: every kernel is intentional native FP64 — literals 1.0/0.5, sqrt/fabs in double, the cancellation-sensitive 4-slab f4 combine at :409/:978, and the `0.0/0.0` NaN-on-singular at :1176 are all parity-load-bearing by §12; no wrong float narrowing. 4.3 allocation sizing: no cudaMalloc/new/DeviceBuffer here — all allocation lives in cuda_backend.cu. 4.4 unsigned countdown: the only descending loop dev_solve :105 `for (int i=n-1; i>=0; --i)` is signed — terminates. 4.5 signed/unsigned compares: loop bounds are int-vs-int; the lone `static_cast<unsigned>(n)` at :1446-1447 is guarded n>0 at :1444. 4.7 host/device pointer typing: raw `const double*`/`int*` device pointers with no host/device wrapper, but these are PRIVATE steppe_device launch-wrapper params consumed only by cuda_backend.cu — consistent with the existing f2_blocks_kernel seam; not a new finding for this unit.)

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found. (2.1 dropped archs: this unit is .cu/.cuh source only — no CMake arch lists, no `-arch=sm_XX`/`-gencode`/`compute_XX` flags, no sm_50/60/70 references or arch-targeted inline PTX; arch selection lives in the build system, not here. 2.2 texture/surface references: no `texture<...>`/`surface<...>` declarations, no `cudaBindTexture*`, no `tex1Dfetch`/`tex2D`/`surf*` — all f2/dLoo/dQinv/dX/scratch access is via direct `const double* __restrict__` global loads (e.g. the f4 4-slab `at()` lambda :406-408/:975-977), so nothing to port to texture objects. 2.3 non-_sync warp intrinsics: no `__shfl*`/`__ballot`/`__any`/`__all`/`__activemask` of any kind — all reductions are single-thread sequential loops (the §12 deterministic-order requirement), so there is no warp-level primitive to migrate to the `_sync` variants; the only synchronization is the block-level `__syncthreads()` at :1063 in add_fudge_diag_models_kernel, which is not deprecated. 2.4 cudaThreadSynchronize: not used — the launch wrappers post-launch error-check via `STEPPE_CUDA_CHECK_KERNEL()` and never call `cudaThreadSynchronize`/`cudaDeviceSynchronize`.)

## Group 3 — Dead / commented-out code

- [3.4][LOW] qpadm_fit_kernels.cu:1185-1186 — In qpadm_fit_models_kernel's single-source popdrop loop, `wr` is filled by `fit_reduced` (the reduced-fit normalized weights, written into the `w_red` out-param) but never read — only `cr` (the reduced-fit chisq) is consumed at :1188. The per-drop weights are intentionally discarded (popdrop reports only the drop chisqs; only the FULL row's weights at :1174-1176 feed d_pop_wfull), so `wr` is a required-but-unused scratch buffer of the lambda's signature, not removable dead code. The lambda's `int` return (status) is likewise discarded here (vs. consumed as `s0` at :1172 for the full row). Suggested: optional `(void)wr;`/comment to document the intentional discard; no functional change.
- [3.3][LOW] qpadm_fit_kernels.cu:1109 — qpadm_fit_models_kernel takes `dLoo` and `d_se` params that it no longer uses (the LOO/SE moved to the separate qpadm_loo_models_kernel + qpadm_se_from_wmat_kernel); both are explicitly silenced with `(void)dLoo; (void)d_se;` and the launcher (:1349-1363) still threads them through. Intentional, already documented — not removable without changing the launch_qpadm_fit_models_batched signature/callsite. Suggested: optionally drop `dLoo`/`d_se` from this kernel + its launcher (and the .cuh decl :274-281) once the caller no longer needs the symmetry; leave as-is otherwise.

No further Group 3 issues found. (3.1 commented-out blocks: none — every `//` line is an explanatory comment or a contract/parity note, no stashed-out code. 3.2 unreachable code: no `#if 0`/`#if 1`; all `return;` are guard/early-exits (block-bounds `idx>=total`, single-thread `threadIdx!=0||blockIdx!=0`, `r<=0`, `r==0` trivial path, total<=0 launch guards) with nothing after them in scope. 3.3 unused helpers/includes: both `#include`s (`cuda_runtime.h`, `check.cuh`) are used; every anonymous-namespace device helper is live — dev_lu_factor←dev_solve; dev_solve←opt/weights/large; dev_jacobi_svd_V←dev_seed_ab; dev_seed_ab←dev_als_weights+seed_ab_kernel; dev_opt_A/B+dev_chisq_of←dev_als_weights+als_kernel+weights_chisq_kernel; dev_als_weights←loo_batched/qpadm_fit_models/qpadm_loo_models; the `*_large` helpers←als_large/weights_chisq_large/loo_large_batched; every `__global__` has a matching `launch_*` wrapper and a .cuh decl. 3.4 computed-but-unread: only the :1185 `wr` above; the `loo_large_batched_kernel` correctly SKIPS the dead chisq (it never computes it — :822-823), so no wasted compute there.)
