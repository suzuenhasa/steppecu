# Review findings — src__device__cuda__cuda_backend

Files: /home/suzunik/steppe/src/device/cuda/cuda_backend.cu

## Group 4 — Type & numeric

- [4.7][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1504,1628,2165,2465 — The large-path / batched helpers (`large_svd_V`, `large_fit_one`, `fit_chunk`, `assemble_result`) pass raw `double*`/`int*` with no host-vs-device space distinction; both host pointers (e.g. `weight`/`se` host arrays in `assemble_result`) and device pointers (e.g. `dXmat`, `dQinv` in `large_fit_one`) travel as bare `T*`, so the wrong space could be passed without a compile error. NOT a bug (every call site is correct and this matches the codebase-wide `DeviceBuffer<T>::data()` -> raw `T*` kernel-launch idiom). Suggested: optional hygiene only — a thin device-pointer wrapper (or naming convention already followed via the `d`-prefix) so host/device spaces cannot be transposed at a call site.

No further Group 4 issues found. Notes on the tasks that are clean (verified, not omitted):
- [4.1] No `float` anywhere in the TU; all math is `double`/FP64 by design (the §12 parity law). N/A, no wrong narrowing found.
- [4.2] Every global index/offset into the f2 tensor (P*P*n_block) and genotype matrix (P*M) is computed in `std::size_t` with an explicit leading cast (e.g. lines 201-204, 359-361, 446-447, 514-521, 710, 768, 978-982, 1058-1059); the S8 strided-batched GEMM strides use `long long` casts (lines 2230-2232). No 32-bit `int` global index.
- [4.3] Allocations are element-count `DeviceBuffer<T>` ctors (sizeof handled by the RAII type) and every `cudaMemcpyAsync` byte count multiplies `* sizeof(T)` (e.g. lines 224, 255, 362, 1078-1081, 1271-1278); no element/byte confusion.
- [4.4] No descending unsigned loop; no `for (unsigned i=n-1; i>=0; --i)` form present.
- [4.5] Loop bounds are consistently `std::size_t < .size()` or `int < int`; no signed/unsigned comparison hazard.
- [4.6] The one genuine narrowing — whole-matrix GEMM contraction extent `k = static_cast<int>(M)` (run_f2_gemms) — is guarded by an explicit fail-fast `if (M > INT_MAX) throw` BEFORE any allocation or narrowing (lines 191-199); all scale products are widened to `std::size_t`/`long long` before multiply, so no int overflow before assignment to a wider type.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found. Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [2.1] No GPU arch flags / CMake arch lists in this .cu (build flags live in CMake, outside this TU); no `sm_50/60/70` or `compute_50/60/70` references anywhere. Compute capability is read only as observability (`cudaGetDeviceProperties` -> caps.compute_major/minor, lines 1139-1142) and explicitly NOT used as a dispatch key (comment lines 1136-1138). N/A.
- [2.2] No texture/surface REFERENCES (`texture<...>`, `surface<...>`, `cudaBindTexture*`, `tex1D/2D/3D`) — none present (this TU does no texture sampling; it is host-side orchestration + cuBLAS/cuSOLVER calls). N/A.
- [2.3] No warp intrinsics at all in this TU (`__shfl*`, `__ballot*`, `__any`, `__all` live in the kernel `.cu` files, not this host-orchestration backend); so no non-`_sync` deprecated form here. N/A.
- [2.4] No `cudaThreadSynchronize`/`cudaThreadExit`; synchronization uses the current `cudaStreamSynchronize` (e.g. lines 260, 379, 569, 679, 1095, 2266, 2285, 2327) and `cudaEventSynchronize` (line 970). All other runtime calls used (`cudaMemcpyAsync`, `cudaEventCreateWithFlags`/`Record`/`Destroy`, `cudaSetDevice`/`cudaGetDevice`, `cudaGetDeviceCount`, `cudaMemGetInfo`, `cudaDeviceCanAccessPeer`, `cudaGetDeviceProperties`) and the cuBLAS/cuSOLVER routines (`cublasGemmEx`/`cublasDsyrk`/`cublasDgemmStridedBatched`, `cusolverDnDpotrf`/`Dpotri`/`DgesvdJ`/`Dgesvd`/`Dpotrf|Dpotrs Batched`) are all current CUDA 13 / Blackwell sm_120-supported APIs — no removed/deprecated entry point.

## Group 3 — Dead / commented-out code

- [3.4][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2276,2279 — `int solve_info = 0;` is passed to `cusolverDnDpotrsBatched` as the host `&solve_info` out-arg on every column-`c` iteration but is NEVER read; the per-system SPD status is taken solely from the earlier `potrfBatched`'s `dInfo` (lines 2257-2264), so a singular/failed per-column solve status is silently discarded. The pointer is a required non-null cuSOLVER arg (cannot be dropped), but the value is computed-unread. Suggested: leave the arg, but either check `solve_info != 0` (record-and-continue like the potrf path) or add a brief `// solve_info: required out-arg; SPD status already gated by potrfBatched dInfo` note so the discard is intentional, not an oversight.
- [3.4][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1508 — `DeviceBuffer<int> dInfo(1);` in `large_svd_V` is passed to every `cusolverDnDgesvdj`/`cusolverDnDgesvd` call (lines 1528,1538,1570,1579) as the convergence/`devInfo` out-arg but is never copied back or checked, so a non-converged Jacobi SVD or an illegal gesvd parameter (devInfo!=0) passes silently into the seed/ALS on the large (NRBIG) path. Required non-null API arg, but the status is computed-unread. Suggested: optionally D2H+check `dInfo` once after the SVD on the large path (it is already off the bit-parity small path), or note the intentional discard.

No further Group 3 issues found. Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [3.1] No commented-out code blocks "kept just in case" — every `//`/`///` line in the TU is genuine descriptive prose (the lines grep flags as starting with `// cuda…`/`// d…`/`// launch…` are mid-sentence comment continuations, e.g. 158, 760, 898, 976, 1021, 1540, 1583, 2142, 2284), not disabled statements.
- [3.2] No unreachable code: no `#if 0`/`#if false`/`#ifdef NEVER`; no statements after a `return`/`break` in any block (the early-return guards at 171, 449, 714, 1063, 1264, 1363 etc. are all followed only by the function's normal continuation, never orphaned code).
- [3.3] No unused symbols. All 16 standard-library includes are referenced (`<span>`/`<memory>`/`<cstring>`/`<algorithm>`/`<climits>`/`<cmath>`/`<cstddef>`/`<cstdint>`/`<string>`/`<limits>`/`<stdexcept>`/`<vector>` all have live uses). All project-local includes are used: `launch_decode_af` (1085), `DiskF2Blocks` (complete type needed to deref `*target.disk_dst` at 417), `DevicePartial::Impl`/`DeviceF2Blocks::Impl` (302,324), `jacobi_svd`/`SvdResult` (1849), the f2/f4/qpadm-fit launch wrappers, `DiskSink::take_descriptor` (417). `kStreamStagingSlots` is named only in the block_sink.cuh include comment (line 70) and is not referenced in the TU — but the include is still required for `BlockSink`/`HostRamSink`/`DiskSink`, so this is an over-broad comment, not an unused include (not flagged). No unused function params (every `(void)precision;` cast at 1247,1325,1648,1734,1873,1951 is a deliberate carve-out suppression, documented as native-FP64-by-§12, not dead).
- [3.4] Beyond the two cuSOLVER status out-args above, no other computed-but-unread values. `prop` (cudaDeviceProp, line 1139) is fully filled by `cudaGetDeviceProperties` but only `.major`/`.minor` read (1141-1142) — idiomatic single-struct fill, not a dead store (the API has no field-selective form). `tile_elems` (978) is read at 980; `max_psp_nb`/`bucket_max_blocks` (609-610,862-865) feed the slab allocations and loop; `n_models`/`int_refit` (1987,2000) flow into `launch_qpadm_loo_large_batched` (2027-2029).

## Group 5 — Hardcoded values / magic numbers

- [5.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2511 — `assemble_result` (the S8 batched-rotation post-process) selects f4rank with a HARDCODED `if (rankp[...] > 0.05)`, while the single-model path `rank_sweep` selects the identical f4rank from the PASSED `alpha` parameter (line 1840, `rs.p[r] > alpha`). The comment at line 2508 even names it "p(r) > rank_alpha" as if a variable existed. So the batched rotation IGNORES a caller-supplied non-default rank alpha and silently uses 0.05 — a wrong f4rank for any alpha != 0.05, and a divergence between the batched and single-model paths. Suggested: thread the rank alpha (the same value `rank_sweep` receives) into `fit_chunk`/`assemble_result` (or a single named `kDefaultRankAlpha` shared with the oracle), do not inline 0.05.
- [5.5][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1515,1558,1739 — the bare `32` in `nl <= 32 && nr <= 32` is the cuSOLVER gesvdj (one-sided Jacobi) per-dimension limit (gesvdj for both dims <=32, gesvd otherwise; comment line 1501), NOT a warp size — exactly the "ambiguous 32, name it" case. It is repeated unnamed in BOTH large_svd_V dispatch branches (1515, 1558) AND in the svd_path observability report (1739). Suggested: name it once (e.g. `kGesvdjMaxDim = 32`) and use it at all three sites so the meaning is explicit.
- [5.3][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1739 vs 1515/1558 — DRIFT hazard: `rs.svd_path = (nl<=32 && nr<=32) ? 1 : 2` (1739) is the REPORTED routine, but the routine actually executed is decided by the SAME predicate duplicated in large_svd_V (1515, 1558). They are physically separate literals in separate functions; if one threshold is edited and the other is not, svd_path mis-reports the executed path (and the parity test asserting svd_path==2 for NRBIG / ==1 for the 9-pop silently passes on a wrong dispatch). Suggested: derive svd_path from the SAME named constant / a shared `gesvdj_applicable(nl,nr)` helper that large_svd_V also calls, so the report cannot diverge from the dispatch.
- [5.3][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1490 vs 1723-1724,2040 — the bit-parity small-path envelope `nl <= 5 && nr <= 10 && r <= 4` is a single literal in `model_fits_small_path` (good, one source for the dispatch), but its comment ("the kQpMax* bit-parity envelope") plus the doc comments at 1723-1724 and 2039-2040 assert it MUST equal the `kQpMaxNl/Nr/R` constants in qpadm_fit_kernels.cu. Those device-side kernel constants are the actual buffer/loop bounds; if they and this host predicate drift, a model the host routes to the "small" path overruns the kernel's fixed envelope (a correctness/overflow bug, not cosmetic). Suggested: source `model_fits_small_path` from the SAME `kQpMaxNl/Nr/R` symbols the kernels use (include them) rather than re-typing 5/10/4 here.
- [5.1][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1937 — `if (status_i == 6)` maps a magic kernel status code `6` to `Status::RankDeficient` with no named constant; the meaning of `6` lives only in the kernel side. A drift between the kernel's emitted code and this `6` silently mis-classifies the fit outcome. Suggested: a shared named enum/constant (e.g. `kQpStatusRankDeficient`) defined once with the kernel, used here instead of the literal `6`.
- [5.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2150,2151 — the VRAM-budget fallbacks `4 GB` (`static_cast<std::size_t>(4) << 30`) and `512 MB` headroom (`static_cast<std::size_t>(512) << 20`) are inline magic sizes in fit_one_bucket (the only chunk-budget tuning knobs in the fit path). Each has a clarifying comment, so this is hygiene only; they are NOT derived from the device/VRAM-budget policy the f2 path centralizes in vram_budget.hpp / kMaxVramUtilizationFraction. Suggested: lift to named constants (e.g. `kFitBudgetFreeVramFallbackBytes`, `kFitBudgetHeadroomBytes`) near the other steppe::config VRAM knobs so the fit and f2 budgets are tunable in one place.

Notes on the tasks that are clean (verified, not omitted):
- [5.1] The other recurring scalars are NOT unexplained magic: the rank tolerance `smax * m * epsilon` (1851-1852) is the standard SVD rank tolerance, documented as bit-identical to the CpuBackend oracle; `alpha = 1.0/nb`, `beta = 0.0` (1406-1407, 2224-2225) are the SYRK/GEMM scaling identities; the FP64 `0.0`/`1.0`/`0.5`-style literals are parity-load-bearing (§12) and intentional.
- [5.2] Hardcoded shapes are derived or documented, not arbitrary: `n_models = 1` (1987) is the explicitly-documented single-model S8 seam; the `kStreamDeviceChunks = 2` device ring (934) is already a named constant; the tile-budget `envelope/4` split (809) is documented as a NO-OP at parity sizes. The S8 column-by-column inverse loop is bounded by `m` (a derived shape), not a literal.
- [5.4] No hardcoded filesystem paths, no hardcoded device IDs (device_id is a ctor parameter defaulting to 0, threaded through guard_device/capabilities), no hardcoded stream/handle IDs. All paths come from `target.disk_path` (the caller's StreamTarget).

## Group 6 — Naming

- [6.1][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2381 — `const double s = (nb - 1) / std::sqrt(nb)` is the block-jackknife SE scale factor, an opaque single-letter name used OUTSIDE any loop (it is read 60+ lines away at the two `launch_qpadm_loo_models_batched(... s ...)` call sites, 2391 and 2422). Unlike the documented math-dimension single letters, `s` here is a derived statistical coefficient with no comment naming it. Also collides locally in spirit with `s`/`Sn`/`surv` (the survivor set) nearby. Suggested: rename to a self-describing name (e.g. `jackknife_scale` / `se_scale`) so the SE formula is legible.
- [6.3][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:150,441,1053 vs 1359,1364,1877,1953,2120,2125,2173 — the symbol `M` is used for TWO unrelated quantities across this TU: in compute_f2/run_f2_blocks_resident/decode_af it is the SNP count (`const long M = Q.M`), while in the qpAdm-fit virtuals it is the widened f4-matrix dimension `static_cast<std::size_t>(m)` (m = nl*nr). A reader scanning across functions meets `M` meaning "SNPs" then "nl*nr". Within any one function only one meaning is live, and the lowercase `m`/uppercase `M` widening pair is consistent locally, so this is hygiene, not a bug. Suggested: in the fit functions name the widened dimension `mz`/`m_sz` (reserve `M` for the SNP-axis extent it denotes on the f2 path), or document the dual use.
- [6.4][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1601,1605 — `wc` in `large_dbl_scratch` is a nonstandard abbreviation for the weight/chisq scratch-layout size; it pairs with the equally terse `als` (line 1600, the ALS layout size). `als` reads as the well-known acronym, but `wc` is opaque without the doc comment. Suggested: spell it `weight_chisq` (or `wc_bytes`-style) to match the descriptive `als`/`als_*` naming used elsewhere in the same file.
- [6.1][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1598,1608,1616-1621 — single-letter `t` (max(nl,nr)*r), `a` (nl*r), `bb` (r*nr), `rr` (clamped r) in the `large_*_scratch` size helpers are scratch-layout dimensions; each is explained by the surrounding doc comment, but `bb`/`a`/`t` are opaque at the use site (e.g. `m + a + bb + large_dbl_scratch(...)`). Borderline — these are local arithmetic temporaries in tightly-commented size formulas (the kind the task tolerates). Suggested: optional only — if touched, name them `t_dim`/`a_elems`/`b_elems` to match the verbose naming the rest of the TU favors.

Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [6.1] The device/host pointer convention is applied with rare consistency: every device buffer/pointer is `d`-prefixed (`dQ`,`dV`,`dN`,`dQ_raw`,`dXmat`,`dQinv`,`dQf`,`dLoo`,`dXtau`,`dVout`,`dXt`,`dScratch`,`dIntScratch`,`dAptr`,`dBptr`,`dAseed`,`dBseed`,`dWmat`,`dSurv`,`dLooS`,`dQinvS`, …) and every host staging vector is `h_`-prefixed (`h_left`,`h_right`,`h_info`,`h_se`,`h_weight`,`h_Aptr`,`h_Bptr`,`h_surv`,`h_seS`,`h_offsets_tile`,`h_sizes_tile`). The remaining single letters are genuine loop counters (`b`,`c`,`i`,`j`,`k`,`kk`,`r`,`rr`,`mi`,`bi`,`drop`,`peer`,`start`,`off`) — the explicitly exempt case. No `tmp`/`data2`/`arr`/`flag`-style sludge anywhere.
- [6.2] No misleading names found: `block_sizes`/`block_offsets` are genuinely sizes/offsets; `bucket_members` is a list-of-index-lists (named accurately); `surv`/`se_computed`/`se_eligible`-style flags match their semantics; `batched_dispatch_count_` is a count and is used as a count. `n_eligible`/`n_models`/`n_refit`/`n_block`/`n_off` are all genuine counts. `lp`/`rp` (2182,2185) are terse but are honestly left/right row POINTERS into the arena, not mislabeled.
- [6.3] Member-variable convention (trailing underscore: `device_id_`,`blas_`,`solver_`,`stream_`,`workspace_`,`pinned_in_`,`stage_f2_`,`stage_vpair_`,`tot_line_`,`batched_dispatch_count_`,`solve_precision_`) is uniform. The `n_block`/`nb` pair is used consistently (the API/struct field is `n_block`, the local working copy `nb`); `n_models`/`n_block` style is consistent. No `nElements` vs `num_elements` vs `n` drift within the file. The lowercase-`m`/uppercase-`M` "same value, wider type" pair (e.g. 1359/1364, 2120/2125) is applied the same way every fit function — a deliberate convention, not inconsistency (the only wrinkle is `M`'s dual meaning, flagged at 6.3 above).
- [6.4] Abbreviations are otherwise standard/domain-justified: `nl`/`nr`/`r`/`rmax`/`dof`/`chisq`/`xtau`/`loo`/`Qinv`/`Qf`/`syrk`/`gemm`/`potrf`/`potri`/`gesvd`/`gesvdj` are the AT2/cuSOLVER/cuBLAS domain vocabulary; `psp_nb` (=P·s_pad·nb), `pp_nb`, `tile`, `slab`, `s_pad`, `s_lo`/`s_hi` are documented at their declaration. Only `wc` (6.4 above) reads as truly nonstandard.

## Group 7 — Duplication

- [7.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:474-490 vs 730-746 — the `ceil_bucket` lambda + `struct Bucket { int s_pad; std::vector<int> blocks; }` + the bucket-assignment loop + the `std::sort` by `s_pad` are BYTE-IDENTICAL copy-paste across `run_f2_blocks_resident` and `stream_f2_blocks_impl` (the 730-746 comment literally reads "IDENTICAL to run_f2_blocks_resident"). Two independent definitions of the SAME size-bucketing rule + the SAME local `Bucket` type: a change to the spike's grouping rule (or a `kBlockGroupPadBase` semantics tweak) must be edited in both or the two paths silently diverge (and the §12 claim "bit-identical to the device-resident path" rests on them staying equal). Suggested: extract one `static`/file-local helper (e.g. `size_buckets(block_sizes, n_block) -> std::vector<Bucket>`) with `Bucket` hoisted to a private struct, called by both.
- [7.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:460-468 vs 719-727 — the block-layout derivation (`core::block_ranges(...)` then the `for (b)` loop filling `block_offsets[b] = ranges[b].begin` and `block_sizes[b] = ranges[b].size()`) is duplicated verbatim between `run_f2_blocks_resident` and `stream_f2_blocks_impl` (the 712 comment says "IDENTICAL to run_f2_blocks_resident"). Same drift risk as 7.1 above; both pieces are the prologue the doc comments promise is shared. Suggested: fold the block_ranges→offsets/sizes derivation into the same shared helper as the bucketing (one prologue producing {block_offsets, block_sizes, buckets}).
- [7.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1812-1835 vs 2513-2537 — the AT2 res$rankdrop nested-table build is copy-pasted: `rank_sweep` fills `rs.rd_*` and `assemble_result` fills `res.rankdrop_*`, both with the same descending `r = rmax - k` loop, the same `dd`/`cd` nested-diff + `pchisq_upper(cd, dd)`, and the same `INT_MIN` / `quiet_NaN()` NA tail for rank 0. The two differ only by the destination field names (and `rs.rd_p`/`rs.rd_chisq` are pre-stored vs `assemble_result` re-derives `rankp`). The comments cross-reference each other ("Mirrors ranktest.cpp ... EXACTLY"), confirming intent to keep them equal. Suggested: extract a `fill_rankdrop(rmax, dof[], chisq[], p[], out...)` helper writing into the common field set, shared by both (and ideally the CPU ranktest path it mirrors).
- [7.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1838-1841 vs 2509-2511 — the f4rank selection ("smallest non-rejected rank, ascending") is the same loop twice: `rank_sweep` does `for r: if p[r] > alpha { f4rank = r; break; }` (fallback `rmax`) and `assemble_result` does the identical loop with the inlined `> 0.05`. (The `0.05` vs `alpha` divergence is already flagged at 5.1; the loop SHAPE is the duplication.) Suggested: a tiny shared `first_nonrejected_rank(p, rmax, alpha)` returning the f4rank, used by both — which also closes the 5.1 alpha-vs-0.05 drift in one stroke.
- [7.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1291-1293, 1367-1369, 2129-2131 — the jackknife normalizer `n = Σ block_sizes` is open-coded three times with the same `long long n_ll = 0; for (...) n_ll += ...; double n = static_cast<double>(n_ll);` idiom (two iterate `f2.block_sizes` / one iterates the `block_sizes` span). Loop-invariant, identical reduction, identical widening. Suggested: a one-line `static double total_block_n(span<const int>)` (or reuse a core helper) computing the long-long sum→double once.
- [7.4][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:224-233 vs 553-562 vs 777-780 — the AMORTIZED-pin prologue `pinned_in_.ensure(Q.data, raw_bytes); pinned_in_.ensure(V.data, raw_bytes); pinned_in_.ensure(N.data, raw_bytes);` (with `raw_bytes = pm * sizeof(double)`) is repeated in three entry points, and in two of them (224-233, 553-562) it is immediately followed by the SAME three `cudaMemcpyAsync(d*_raw.data(), {Q,V,N}.data, raw_bytes, H2D, stream_)` H2D triple. Boilerplate a helper folds (e.g. `pin_and_upload_qvn(dQ_raw, dV_raw, dN_raw, Q, V, N, raw_bytes)`); 777-780 pins only (no upload) and would call the pin-only half. Suggested: a small private `ensure_pinned_qvn(Q,V,N,bytes)` (+ optional upload sibling) to fold the triplets.
- [7.4][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1658-1672, 1750-1772, 1884-1898 — `rank_test`, `rank_sweep`, and `gls_weights` each open with the same device-buffer set + H2D + xmat prologue: allocate `dTotal[m]`, `dXmat[m]`, `dQinv[m*m]`, `dA`, `dB`, `dW[nl]`, `dchisq(1)`, `dStatus(1)`; `cudaMemcpyAsync` x_total→dTotal and Qinv→dQinv; then `launch_qpadm_xmat_from_rowmajor(dTotal, nl, nr, dXmat, stream_)`. The three then diverge into the small/large fit dispatch (also near-duplicated: 1673-1692 vs 1899-1917 share the small `seed_ab`/`als`/`weights_chisq` and large `large_fit_one` branch shapes). Loop-invariant scaffolding repeated three times. Suggested: a shared `upload_fit_inputs(x, cov, r, ...) -> {dXmat, dQinv, ...}` prologue struct + a `dispatch_single_fit(small, ...)` helper so the three S5/S6 virtuals share the alloc/upload/xmat and the small-vs-large branch.
- [7.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1408-1413 vs 2223-2234 — the precision-engaged covariance matmul appears twice with the same scoped pattern: `{ MathModeScope(blas_, CUBLAS_PEDANTIC_MATH); engage_f2_precision(blas_, precision); <one cublas covariance call with alpha=1/nb, beta=0>; }`. `jackknife_cov` uses `cublasDsyrk`, `fit_chunk` uses `cublasDgemmStridedBatched`, so the inner call genuinely differs — but the scope-bracket + `alpha=1.0/nb, beta=0.0` setup is identical boilerplate. Borderline (the two cuBLAS calls are distinct). Suggested: optional only — a `ScopedCovariancePrecision` RAII (or a `with_engaged_precision(blas_, precision, fn)` wrapper) to fold the bracket; or leave as-is given the differing inner call.
- [7.3][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1364/1422-1424 vs 2125-2126 — the `m*m` element count is recomputed inline in `jackknife_cov` (`M*M` with `M = (size_t)m`, used at 1405/1415-1416/1422-1424/1470-1471) whereas `fit_one_bucket`/`fit_chunk` introduce the named `Mm = M*M` (2126, 2174) and reuse it. The square-of-dimension idiom + the `static_cast<std::size_t>(m)` widening recur in `rank_test`/`gls_weights`/`rank_sweep` (e.g. 1660, 1670-1671, 1752, 1770, 1886, 1896) as `(size_t)m * (size_t)m`. Repeated cast/product. Suggested: hoist the `Mm = M*M` local (as `fit_chunk` already does) in the fit virtuals that recompute it, so the m² extent is computed once per function.

No further Group 7 issues found. The whole-file feeder→GEMM→assemble body is correctly factored ONCE into `run_f2_blocks_resident` and shared by all four `compute_f2_blocks*` overrides (278-390) — that is the opposite of duplication and is good. The `large_*_scratch` size helpers (1596-1622) are already single-source (no inline re-derivation of the ALS/weight-chisq layout). The `M`/`Mm` naming in `fit_chunk` shows the intended hoist pattern; the findings above are where it was not applied consistently.

## Group 8 — Comments

- [8.2][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1502 — The `large_svd_V` doc says "`dXt` is the nr×nl transpose scratch (only used in the nr>=nl branch)", but `dXt` is written AND read in BOTH branches: the nr>=nl branch writes Xt=transpose(xmat) into it (1511) and reads it through gesvd/gesvdj (1523/1536), and the nl>nr branch writes V=transpose(dVt) into it (1582) and copies its leading r cols to dVout (1584-1585). The "only used in the nr>=nl branch" clause is wrong — it understates the buffer's role on the nl>nr path. A maintainer trusting it could mis-size or drop the buffer for the nl>nr case (the sizing at 1681/1763 happens to cover both as `nr*nl`, so no live bug). Suggested: correct to "transpose scratch used in BOTH branches (holds Xt for nr>=nl, V for nl>nr)".
- [8.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1866 — The `gls_weights` doc hardcodes "20 ALS opt_A/opt_B iters", but the code passes the runtime-configurable `opts.als_iterations` (1903, 1914), whose default is 20 (qpadm.hpp:64). The comment bakes the DEFAULT into the prose as if it were fixed, so a caller overriding `als_iterations` makes the comment describe behavior the code no longer has. Suggested: say "`opts.als_iterations` (default 20) ALS opt_A/opt_B iters", not the bare "20".

Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [8.1] No restating-the-code comments. Every `//`/`///` line is substantive design/parity/perf rationale (the §11.4/§12/perf-discovery prose), not mechanical narration of the statement it precedes; no `i++; // increment` style noise anywhere.
- [8.3] Non-obvious constants generally carry their rationale at the use site: the ctor member-init/teardown ordering (94-127, 2612-2660), the M0 INT_MAX fail-fast (173-199), the VRAM-budget split (783-814), the kStreamDeviceChunks=2 ring (924-934), the per-chunk reuse syncs (663-679), the trailing potrsBatched drain "MEASURED on box5090" (2282-2285) are all explained. The two genuinely under-justified literals (the inline `0.05` rank-alpha at 2511 vs the named `alpha`/comment "p(r) > rank_alpha" at 2508-2511, and the magic kernel status `6` at 1937) are bare magic numbers whose meaning lives only off-file — already captured as Group 5 findings [5.1] (2511) and [5.1] (1937), so not re-raised here.
- [8.4] No orphan TODO/FIXME/HACK/XXX/WIP/TBD markers in the TU (grep-verified, none present). The forward-looking "the NEXT workflow, not implemented here" (20), "a future per-stage policy can set" (1437-1442), and "the model axis is the S8 seam" (1987) are scoped, owned design notes tied to ROADMAP/§ references, not dangling task markers.

## Group 9 — Constants & configuration

- [9.3][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2450-2458 — `assemble_result(mdl, nl, nr, r_fit, rmax, tag, h_info[j] != 0, h_status[j], se_computed[j] != 0, h_weight.data()+j*nl, h_se.data()+j*nl, h_chisq[j], ...)` passes TWO bare positional booleans (`nonspd = h_info[j] != 0` and `se_computed = se_computed[j] != 0`) interleaved with an `int fit_status` and a long run of `double*` args. The two bools sit at non-adjacent positions in an 18-arg call; a caller transposing the `nonspd` and `se_computed` flags (or slipping one past `fit_status`) compiles silently and mis-classifies the model outcome / the SE-presence sentinel. Compounded because the very next sibling pattern (`fit_status` as bare `int`) is adjacent. Suggested: pass small named enums/flag structs (e.g. `SpdStatus::NonSpd`, `SeComputed::Yes`) or an aggregate `AssembleFlags{bool nonspd; bool se_computed;}` so the two booleans cannot be transposed at the call site.
- [9.2][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:809 — the M5 streamed VRAM split divisor `const std::size_t tile_budget_b = envelope_b / 4u;` ("reserve a quarter of the envelope for [the tile feeder]") is a tunable budget-partition knob hardcoded as a bare `/ 4u` buried mid-function, while every OTHER VRAM lever (`kMaxVramUtilizationFraction`, `kCublasWorkspaceBytes`) is surfaced in steppe::config.hpp. A future re-balance of feeder-vs-slab budget edits a literal here rather than a named config. Suggested: lift to a named constant (e.g. `kStreamTileBudgetFraction` near the other VRAM knobs in config.hpp) instead of the inline `/ 4u`.
- [9.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2150-2151 — the fit-path chunk-budget knobs `4 GB` free-VRAM fallback (`static_cast<std::size_t>(4) << 30`) and `512 MB` headroom (`static_cast<std::size_t>(512) << 20`) are inline magic sizes in `fit_one_bucket`, not surfaced alongside the f2 path's centralized VRAM policy (vram_budget.hpp / kMaxVramUtilizationFraction); the fit and f2 budgets are tuned in two unrelated places. (Already noted from the magic-number lens as [5.2]; raised here as the config-surfacing angle.) Suggested: lift to named `kFitBudgetFreeVramFallbackBytes` / `kFitBudgetHeadroomBytes` near the steppe::config VRAM knobs so all VRAM tuning lives in one place.
- [9.2][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1490 — the bit-parity small-path dispatch envelope `nl <= 5 && nr <= 10 && r <= 4` is hardcoded inside `model_fits_small_path` rather than sourced from the kQpMaxNl/Nr/R config constants the device kernels actually use as buffer/loop bounds (qpadm_fit_kernels.cu). This is a config value (the dispatch threshold) re-typed in logic instead of shared from the one config source — a drift between the two overruns the kernel's fixed envelope. (The correctness/overflow angle is captured as [5.3]; raised here as the config-not-surfaced angle.) Suggested: include and use the kQpMaxNl/Nr/R symbols directly here rather than re-typing 5/10/4.

No further Group 9 issues found. Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [9.1] No should-be-const/constexpr left mutable that matters: locals that look fixed are genuinely written (`int gi` 483/739 reassigned in the bucket-assignment scan, `bool can_peer` 1164, `bool degenerate` 1779, `int rk` 1853, `std::size_t bk` 2088, `int chunk_idx` 952 incremented, the `Ring ring[]`/`RingGuard`/`r.used` 940-950 mutated by the loop). `const` is already applied with rare discipline to every fixed local (the `const std::size_t pm/two_pm/pp` extents 201-204/514-515, `const double alpha/beta` 1406-1407/2224-2225, `const int rows/cols` 1512/1550, etc.). `kStreamDeviceChunks` (934) is already `constexpr`. The two `ceil_bucket` lambdas (474, 730) capture nothing and are pure, but a non-capturing local lambda is not a "should-be-constexpr" defect (it is the duplication already flagged as [7.1]). Members are correctly mutable: `solve_precision_` (2683), `batched_dispatch_count_` (2689), `tot_line_` (2695), `stage_f2_`/`stage_vpair_` (2723-2724), `pinned_in_` (2709) are all written after construction by design; `device_id_` (2620) is set once in the init-list and never const-qualified only because the move-only ComputeBackend base needs the member assignable.
- [9.2] The remaining buried scalars are either already-named config (`kBlockGroupPadBase` used at 476/732/477, `kMaxVramUtilizationFraction`/`kCublasWorkspaceBytes`/`kDefaultMantissaBits` from config.hpp, `core::kMaxGridZ` at 828) or documented derived/no-op values (`n_models = 1` 1987 the documented single-model seam, the `envelope/4` and `kStreamDeviceChunks=2` ring depth which are config-shaped but the latter is already constexpr). The `als_iterations` default (20) is correctly threaded from `opts.als_iterations` (1787/1903/1970/2027/2297), NOT hardcoded in logic — the only place "20" appears is the stale doc-comment already flagged as [8.2]. The hardcoded `0.05` rank-alpha at 2511 (the batched path ignoring the caller's alpha) is a genuine buried-config bug already captured as [5.1]/[7.2]; not re-raised here.
- [9.3] No other positional-boolean anti-pattern: the cuSOLVER `/*econ=*/1` (1522,1564) is an `int` econ flag already self-documented with an inline `/*name=*/` comment; the `'S'`/`'N'` job characters (1536,1577) are the standard cuSOLVER gesvd jobu/jobvt char API (not booleans); `cudaEventCreateWithFlags(&r.reuse, cudaEventDisableTiming)` (945) uses a named runtime enum flag; the cuBLAS `CUBLAS_OP_N`/`CUBLAS_OP_T`/`CUBLAS_FILL_MODE_LOWER` args are named enums. The remaining `bool small`/`bool survivor`/`bool ok_fit` are locals consumed in-function (named at their declaration), not positional call args.

## Group 10 — Initialization

- [10.1][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2354 — `bool survivor;` is the ONLY uninitialized scalar local in the whole TU (grep-verified; every other scalar is zero/`{}`-initialized at declaration). It is assigned in all three `switch (opts.jackknife)` arms — `None`(2357), `FeasibleOnly`(2362), `All`/`default`(2367) — before its sole read at 2370, and the `default` makes the switch exhaustive, so there is NO path that reads it uninitialized (correct, not a live bug). Borderline 10.1 hygiene: declared uninitialized-then-assigned where a `const bool survivor = [&]{ switch... }();` or an in-place initializer would remove the window and let the compiler enforce the all-paths-assigned guarantee if a future enum case is added without a `default`. Suggested: initialize at declaration (e.g. `bool survivor = false;`) or compute it as a `const` from an IIFE/helper so the no-uninitialized-read property is structural, not switch-coverage-dependent.

Notes on the tasks that are clean (verified across the full 2756-line TU, not omitted):
- [10.1] No late/distant declarations: locals are declared at or immediately before first use (the `d*` DeviceBuffers right before their H2D/launch; `int gi = -1` at 483/739 inside the bucket scan; `int rk = 0` 1853, `double tr = 0.0` 1421, `long long n_ll = 0` 1291/1367/2129 each just before the accumulation that fills them; `std::size_t bk` 2088 at the bucket-search use; `int chunk_idx = 0` 952 just before the loop). No declare-far-from-use across function-length gaps. All API out-locals (`int info = 0` 1452, `int lwork = 0` 1520/1532/1562/1573, `int lwork_f = 0` 1446, `int count = 0` 1126/2750, `std::size_t free_b = 0, total_b = 0` 503/752/1149, `int access = 0` 1167, `int entry_device = 0` 1132, `int status_i = 0`/`double chisq = 0.0` 1798/1799/1918/1919) are zero-initialized BEFORE the cuBLAS/cuSOLVER/runtime call that fills them — the correct pattern (the API may leave the out-arg untouched on a non-success path), not an uninitialized-then-assigned defect.
- [10.2] No unsound zero-init assumption. Every result/output vector is EXPLICITLY value-filled, not relying on container default-zero coincidence: `out.q/v/n.assign(pm, 0.0)` (1060-1062), `out.f2/vpair.resize(pp)` then fully written by the D2H (253-259), `out.x_blocks/x_total/x_loo.assign(...,0.0)` (1260-1263), `gw.A/B/w.assign(...,0.0)` (1654-1655,1879-1881), `wmat(...,0.0)` (1955), `rs.chisq/dof/p.assign(n,0.0/0)` (1776-1778), `out.Q/Qinv.assign(M*M,0.0)` (1415,1470), `tot_line_.assign(m,0.0)` (1263). The `h_se(B*nl, 0.0)` zero-fill (2313) for NON-survivor models is never consumed as a value — `assemble_result` reads `se` only when `se_computed[j]` is true (2483), so the zero is defensive, not a relied-upon default. Device buffers are not assumed zeroed: `dQinv` is filled by `launch_fill_identity_batched` (2249) before use, gather/feeder/assemble kernels fully write their `s_pad`/`P²·nb` outputs before any read (the L4b over-sizing comment 588-595 makes this explicit), and the cuSOLVER-overwritten `dA2` is a fresh D2D copy of `dXmat` (1551-1555). Aggregate/struct locals are properly initialized: `cudaDeviceProp prop{}` (1139) value-init then API-filled; `Ring` (935-940) has in-class member initializers (`reuse=nullptr`, `used=false`) and its `DeviceBuffer` members default-construct empty then are assigned in the 942-946 loop; `Bucket`(479)/`Key`(2080) use member initializers / aggregate init; `gesvdjInfo_t params = nullptr` (1517,1559) is null-init then created. Members (2620-2724) all carry in-class initializers (`device_id_=0`, `Stream{}`, `CublasHandle{}`, `solve_precision_{Fp64}`, `batched_dispatch_count_=0`, `tot_line_{}`, `pinned_in_{}`, `stage_*{}`), and `device_id_` is set in the init-list before any dependent member constructs — no member left to chance.

## Group 11 — Qualifiers & const-correctness

No Group 11 issues found. This TU is PURE host-side orchestration: it contains ZERO CUDA function/pointer qualifiers (grep-verified across the full 2756-line file — no `__global__`, `__device__`, `__host__`, `__restrict__`, `__grid_constant__`, `__launch_bounds__`). Every kernel reaches the device through a `launch_*` wrapper (e.g. `launch_f2_feeder` 236, `launch_gather_group` 649/1014, `launch_assemble_f4_gather` 1287, `launch_qpadm_fit_models_batched` 2295) or through cuBLAS/cuSOLVER, all DEFINED in other TUs — so all four Group 11 tasks (which target kernel/`__host__ __device__`/`__global__`-param surfaces) have NO surface in this file. Notes on the tasks verified clean (not omitted):
- [11.1] N/A — no `__global__`/`__device__` kernel is DEFINED here, so there are no read-only kernel pointer params to mark `const __restrict__`. The kernel pointer qualifiers live in the `launch_*` wrappers' `.cu`/.cuh files (f2_block_kernel.cu, f2_blocks_kernel.cu, qpadm_fit_kernels.cu, decode_af_kernel.cu), out of this unit. The cuBLAS/cuSOLVER routines (`cublasGemmEx`/`Dsyrk`/`DgemmStridedBatched`, `cusolverDnDpotrf`/`Dpotri`/`DgesvdJ`/`Dgesvd`/`Dpotrf|DpotrsBatched`) take raw `double*`/`int*` per their own fixed C API — not steppe-authored signatures to re-qualify.
- [11.2] N/A — no host/device/global qualifier appears anywhere, so no inconsistency is possible. All member functions (`compute_f2`, `run_f2_blocks_resident`, `large_svd_V`, `large_fit_one`, `rank_sweep`, `gls_weights`, `fit_chunk`, `assemble_result`, …) and the two free factory functions (`make_cuda_backend`, `visible_device_count`) are plain host functions; the `static` helpers (`set_and_return_device` 2607, `model_fits_small_path` 1489, `large_dbl_scratch` 1596, `large_int_scratch` 1607, `large_loo_dbl_refit` 1616) are host-only by design.
- [11.3] No host/device helper duplicated-instead-of-`__host__ __device__` in this TU. The pure-arithmetic size/dispatch helpers (`large_dbl_scratch`/`large_int_scratch`/`large_loo_dbl_refit`/`model_fits_small_path`, the two `ceil_bucket` lambdas 474/730) are consumed ONLY host-side here — to size `DeviceBuffer`s and pick the small/large dispatch — and the resulting sizes/strides are passed to the kernels as scalar args (e.g. `static_cast<long>(dbl_refit)` 2028, `s_pad`/`nb`). The kernels do their OWN device-side indexing in their own TUs; there is no copy of the same body needing a shared `__host__ __device__` definition inside this file. (The two `ceil_bucket` lambdas being byte-identical is a host-host duplication already captured under Group 7 [7.1], not a Group 11 qualifier issue.)
- [11.4] No large by-value struct passed as a kernel param (no `__global__` here to receive one; nothing to mark `__grid_constant__` or repass by pointer). The domain structs that ARE large — `core::MatView`, `Precision`, `QpAdmOptions`, `DecodeTileView`, `StreamTarget`, `F4Blocks`, `JackknifeCov`, `DeviceF2Blocks` — all cross the host-function boundary by `const&` (e.g. `compute_f2(const MatView&, …, const Precision&)` 144-147, `fit_models_batched(const DeviceF2Blocks&, span, const QpAdmOptions&, const Precision&)` 2061-2065, `stream_f2_blocks_impl(…, StreamTarget&)` 401-404); only small scalars/enums (`Precision::Kind tag`, `int`/`long`/`double`) and raw pointers travel by value into the `launch_*` wrappers — within the host C++ param-passing norm, not a CUDA param-space concern.

## Group 12 — Launch config & indexing

No Group 12 issues found. This TU is PURE host-side orchestration: it contains ZERO triple-chevron `<<<...>>>` kernel launches and ZERO `__global__` kernel definitions (grep-verified across the full 2756-line file). Every device launch goes through a `launch_*` wrapper (e.g. `launch_f2_feeder` 236/564/992, `launch_gather_group` 649/1014, `launch_assemble_blocks_group` 660/1023, `launch_decode_af` 1085, `launch_assemble_f4_gather` 1287, the `launch_qpadm_*` fit wrappers, `launch_f4_*_models_batched` 2207-2216) or through cuBLAS/cuSOLVER — all DEFINED in OTHER TUs (f2_block_kernel.cu, f2_blocks_kernel.cu, decode_af_kernel.cu, qpadm_fit_kernels.cu). Tasks 12.1-12.4 (block dim multiple-of-32, grid dim hardcoded vs cdiv, missing grid-stride loop, baked-in vs occupancy-derived config) all target the `<<<grid,block>>>` site itself, which has NO surface in this file — the launch math lives in `core/internal/launch_config.hpp` + the kernel `.cu` files (verified: the fit kernels at qpadm_fit_kernels.cu:395-396/469-470/702-703/843-844/960-961/990-991/1032-1033 use proper `idx += gridDim.x*blockDim.x` grid-stride loops, so the batch axes the backend passes — `B`, `nb` — ride gridDim.x without an overflow, and the M4 gather/scatter batch `nb` rides gridDim.z). Notes on the one task with surface here, verified clean (not omitted):
- [12.5] Device-property / compute-cap assumptions are QUERIED, not hardcoded. `capabilities()` (1119-1196) probes every datum it reports: `cudaGetDeviceCount` (1127), `cudaGetDeviceProperties`→`prop.major/minor` (1140-1142), `cudaMemGetInfo`→free/total VRAM (1150), `cudaDeviceCanAccessPeer` per peer (1172). Compute capability is OBSERVABILITY ONLY and explicitly NOT a dispatch key (comment 1136-1138: "one sm_120 build serves both boxes … not a dispatch key"), so no per-arch branch keys off a hardcoded SM. The VRAM budgets derive the per-chunk block cap from the live `cudaMemGetInfo` free figure (`free_b` 503/752, `capabilities().free_vram_bytes` 2149) through the host-pure budget helpers (`max_blocks_per_chunk` 614, the inline streamed `streamed_max_blocks` 820-830, `fit_one_bucket` 2143-2155) — none assumes a fixed VRAM size (the 4 GB/512 MB at 2150-2151 are FALLBACKS for a 0 query, already flagged as [5.2]/[9.2], not an unconditional assumption). The one hardware constant that IS a literal — `core::kMaxGridZ = 65535` (launch_config.hpp:73), used at 828 and inside `max_blocks_per_chunk` to cap the gridDim.z batch — is the documented, compute-capability-INVARIANT z-grid ceiling (65535 on every CC; vram_budget.hpp:126), so it is correctly a build-time constant rather than a `cudaGetDeviceProperties().maxGridSize[2]` query; flagging it would be noise. The grid-z cap is genuinely enforced: the resident path clamps `nb` via `max_blocks_per_chunk`→`min(…, kMaxGridZ)` and the streamed path via `streamed_max_blocks`→`min(…, kMaxGridZ)` (828), and the kernel-side `STEPPE_ASSERT(1 <= n_in_group <= kMaxGridZ)` (launch_config.hpp:144) backstops it — so no batch ever exceeds the z-grid limit.

## Group 15 — Memory: transfers

- [15.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2270-2280 — In `fit_chunk` the column-by-column batched inverse loop runs `m` (= nl·nr, up to ~50 small-path) iterations and EACH iteration does a fresh H2D `cudaMemcpyAsync(dBptr.data(), h_Bptr.data(), B*sizeof(double*), H2D)` (2273) of the B per-model RHS pointers for column c. The pointers are fully deterministic (`dQinv.data() + j*Mm + c*M`, 2272), so all `m` pointer arrays could be precomputed into ONE `[m × B]` device pointer arena and uploaded with a SINGLE H2D before the loop, leaving only the `cusolverDnDpotrsBatched` per column. The loop-local comment at 2267 even claims "precompute all m, H2D once each lazily" — but the code uploads per-iteration, not once, so the comment over-promises what the code does. PARITY-NEUTRAL (same pointers, same solves), so this is a scale/latency cost (m small H2Ds + the implicit per-column ordering on the one stream), not a wrong result; it bites the S8 rotation's hot per-chunk path. Suggested: hoist to a single `DeviceBuffer<double*> dBptrAll(m*B)` filled once on the host and uploaded with one `cudaMemcpyAsync` before the `for c` loop; index `dBptrAll.data() + c*B` per column (mirrors the single dAptr upload at 2254).

- [15.3][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1962-1967, 2031-2033 (and the other fit-path H2D/D2H: 1666-1702, 1766-1771, 1892-1897, 1377-1388, 1416-1471, 1270-1278, 2190-2195) — The qpAdm-fit intermediates cross the seam from/into PAGEABLE `std::vector`s (x_total m, Qinv m², x_loo m·nb, wmat nb·nl, the B-strided fit arenas). For the small-LA per-call fits these are KB-scale and inherent to the CUDA-free F4Blocks/JackknifeCov/GlsWeights host-vector seam, so pinning would not pay (correctly left pageable, mirroring the documented pageable-result decision at 246-252). BUT `dLoo`/`x_loo` at S7 (1962-1964) and the S8 `dLoo`/`dQinv` arenas scale as m·nb (nb up to ~757) × the model batch B, so at the rotation/large-model envelope these specific transfers grow into the MB-to-low-GB range yet ride pageable host memory (host-blocking staging, no overlap with the concurrent peer device under the §11.4 fan-out — the exact wall the f2 path's `pinned_in_` was built to remove). Latent / scale-dependent: today's single-model fit and small rotation buckets keep these small; only the large-nb · large-B envelope makes them bandwidth-relevant. Suggested: if profiling at the S8/large-model envelope shows these H2D/D2Hs on the critical path, amortize-pin the x_loo/Qinv source (or stage through a persistent `PinnedBuffer`) exactly like the f2 `pinned_in_`/`stage_*_` seam — otherwise leave pageable (the small-fit case is correctly not worth pinning).

Notes on the tasks verified clean (with the §12/FP64/scale context applied; not omitted):
- [15.1] The DOMINANT data movement is already correctly hoisted / kept-resident: the M0/M4 Q/V/N H2D uses the amortized `pinned_in_` registry registered ONCE per (ptr,bytes) and reused (224-227, 553-556, 777-780); the M4/M5 per-chunk slabs are pre-sized once and reused (no per-chunk copy churn, 622-626, 885-922); the resident f2/Vpair tensors are MOVED out (no D2H at all on the resident path, 303-305, 325-326); the fit virtuals upload x_total/Qinv ONCE before the per-rank sweep (1766-1772 then the `for r` loop reuses dXmat/dQinv) and S7 uploads dLoo/dQinv ONCE before the batched launch (1962-1967). The per-rank D2H of chisq/status in `rank_sweep` (1800-1804) and the per-(model,block) Stage-A SVD launches in `gls_weights_loo_batched` (2006-2022) are LAUNCHES with NO H2D/D2H inside the loop — the dLoo H2D is hoisted above (1962), so no in-loop transfer there. The S8 SE compaction gather (2414-2416) is ONE kernel (device-side D2D scatter), not a per-survivor copy loop. So 2273 is the one genuine in-loop H2D.
- [15.2] Every transfer's direction enum matches its actual src/dst space (full-file audit). All `cudaMemcpyHostToDevice` sites have a host source (`std::vector`/`MatView::data`/`&scalar`) → `DeviceBuffer::data()` dest (228-233, 525-530, 557-562, 770-772, 920-922, 983-988, 1006-1011, 1077-1082, 1270-1278, 1377-1388, 1386, 1666-1671, 1766-1771, 1892-1897, 1962-1967, 2136-2138, 2190-2195, 2254-2256, 2273, 2409-2411). All `cudaMemcpyDeviceToHost` have a device source → host `std::vector`/`&scalar` dest (255-259, 1089-1094, 1301-1312, 1416, 1453, 1462, 1471, 1694-1702, 1800-1803, 1920-1933, 2031-2033, 2264, 2317-2326, 2396, 2428-2430). The three `cudaMemcpyDeviceToDevice` are genuinely device→device (1424 dQf←dQ; 1542/1552/1584 the large-SVD scratch D2D), correctly tagged. No direction enum is transposed.
- [15.3] The intentional pinned/pageable split is sound and applied with the §11.4 perf rationale: the stable, REUSED H2D inputs (Q/V/N) are amortized-pinned via `PinnedRegistryCache pinned_in_` (a true async DMA precondition; comment 215-223, 535-552, 773-776), and the large compute_f2_blocks_into D2H stages through PERSISTENT per-backend `PinnedBuffer` (`stage_f2_`/`stage_vpair_`, 369-387, 2711-2724) so the two devices D2H concurrently without the device-wide cudaHostRegister lock. The FRESHLY-allocated per-call M0 result vectors (out.f2/vpair) are DELIBERATELY left pageable — a per-call cudaHostRegister would pay the page-lock tax with zero amortization (documented MEASURED loss, 246-252). The decode_af tile (1077-1094) is one-shot tile-shaped, not a repeated bandwidth path. Only the scale-dependent fit-path x_loo/Qinv (flagged [15.3] above) are a future pinning candidate.

## Group 13 — Error handling

No Group 13 issues found. Verified across the full 2756-line TU + the shared check.cuh macro home (read in full); the §12/FP64/capability-tier context applied so the intentional non-throwing degrade and the destructor cleanup-discards are NOT flagged. Notes on each task (verified, not omitted):
- [13.1] EVERY `cuda*` runtime API return in executable code is wrapped in `STEPPE_CUDA_CHECK` (the throwing, file:line-carrying `cuda_check` via std::source_location): all `cudaMemcpyAsync` H2D/D2H/D2D (228-233, 255-259, 525-530, 557-562, 770-772, 920-922, 983-988, 1006-1011, 1077-1082, 1089-1094, 1270-1278, 1301-1312, 1377-1388, 1416-1424, 1453-1471, 1542-1545, 1552-1555, 1584-1587, 1666-1702, 1766-1803, 1892-1933, 1962-2034, 2136-2138, 2190-2195, 2254-2273, 2317-2430), every `cudaStreamSynchronize` (260, 379, 569, 679, 1095, 1313, 1418, 1455, 1464, 1473, 1704, 1804, 1935, 2034, 2266, 2285, 2327, 2397, 2431), `cudaMemGetInfo` (504, 752, 1150), `cudaEventCreateWithFlags`/`cudaEventRecord`/`cudaEventSynchronize` (945, 1039, 970), and the device-selection calls (`cudaSetDevice` 1134/1194/2608/`guard_device` 2599, `cudaGetDevice` 1133, `cudaGetDeviceCount` 1127/2751, `cudaGetDeviceProperties` 1140). The ONLY un-CHECKed `cuda*` call is `(void)cudaEventDestroy(...)` in the `RingGuard` destructor (949) — a DELIBERATE, correct discard: a destructor must not throw and the event is being torn down on scope exit (matches the std-library noexcept-cleanup idiom). NOT a finding.
- [13.2] No triple-chevron `<<<...>>>` and no `__global__` in this TU (it is pure host orchestration); every device launch goes through a `launch_*` wrapper defined in the kernel TUs. The launch-error + async-fault check (the BOTH that 13.2 requires) is therefore the wrapper's responsibility, and it is discharged: each wrapper calls `STEPPE_CUDA_CHECK_KERNEL()` immediately after its `<<<>>>` (verified: f2_block_kernel.cu:324/383, f2_blocks_kernel.cu:203/285, decode_af_kernel.cu:111, qpadm_fit_kernels.cu ×25), which does `STEPPE_CUDA_CHECK(cudaGetLastError())` (launch-config error, synchronous) AND a `STEPPE_DEBUG_ONLY(cudaDeviceSynchronize())` (async-fault attribution under compute-sanitizer); release relies on the next runtime call surfacing the sticky async error (check.cuh:244-256). On THIS TU's side that "next runtime call" is always reached: every entry point that issues launches ends in a `STEPPE_CUDA_CHECK`-guarded `cudaStreamSynchronize` (or a CHECKed D2H) — e.g. compute_f2 260, run_f2_blocks_resident 569/668/679, decode_af 1095, the fit virtuals 1313/1418/1704/1804/1935/2034/2266/2285/2327/2397/2431 — so an async kernel fault is surfaced as a thrown `CudaError`, never silently dropped. Both halves covered.
- [13.3] Checking is CONSISTENT, not mixed: every FAULT call uses the matching throwing macro (`STEPPE_CUDA_CHECK` / `CUBLAS_CHECK` 1411/2228 / `CUSOLVER_CHECK` 1447-1579/2260-2280), and the SINGLE non-throwing call is the P2P probe `STEPPE_CUDA_WARN(cudaDeviceCanAccessPeer(...))` at 1172 — the EXPECTED capability-degrade on the budget GeForce tier (architecture.md §11.4; check.cuh CAP-1/CAP-2), branched on at 1173, never a swallow. No call site is left bare while a sibling is guarded. (The cuSOLVER per-system `devInfo`/`solve_info` out-args at 1508/2276 are computed-but-unread VALUES, already captured as Group 3 [3.4]; they are NOT an unchecked API return — the wrapping `CUSOLVER_CHECK` does check the API status. Not re-raised here as a Group 13 issue.)
- [13.4] No error-swallowing homegrown macro. The check.cuh macros are the OPPOSITE of swallowing: `STEPPE_CUDA_CHECK`/`CUBLAS_CHECK`/`CUSOLVER_CHECK` THROW typed exceptions (`CudaError`/`CublasError`/`CusolverError`) carrying file:line:function (std::source_location) + the stringized expr + the error name/string (check.cuh:51-201), explicitly replacing the spike's `std::exit`-on-failure macros (check.cuh:10-14). `STEPPE_CUDA_WARN` is non-throwing BY DESIGN for recoverable statuses but does NOT hide them — it logs one STEPPE_LOG_WARN line AND `[[nodiscard]]`-returns the status so the caller must branch (check.cuh:174-189). The only non-throwing FAULT-path discards are in destructors (`RingGuard::~RingGuard` 949; the `MathModeScope`/`CusolverMathModeScope` restore() in handles.hpp log-and-continue at scope exit) — correct noexcept-cleanup, not concealment.

## Group 14 — Memory: allocation & lifetime

- [14.2][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2006-2022 (large_svd_V at 1504-1589) — The PARALLEL large-LOO Stage A loops `large_svd_V` over all `nb` blocks; each `large_svd_V` invocation allocates ~6 fresh `DeviceBuffer`s (`dInfo` 1508, `dS`/`dU` 1513-1514, `dVt` 1519/1557, `dWork` 1524/1534/1566/1575, `dA2` 1551) and FREES them at its scope exit. `cudaFree` (DeviceBuffer::reset, device_buffer.cuh:117) is DEVICE-WIDE-SYNCHRONIZING (the same global-allocator-lock + full-device sync the L4b slab pre-sizing comment at 577-607 went to lengths to hoist out of the chunk loop). So the loop incurs `nb`×~6 device-wide syncs, DEFEATING the documented design intent at 1976-1979 ("all async-enqueued on stream_ with NO per-block cudaStreamSynchronize ... the dominant 701× host round-trip is gone") — there is no explicit `cudaStreamSynchronize`, but every `cudaFree` is an implicit full-device sync that is at least as serializing. PARITY-NEUTRAL (the sync moves no bits), so this is a perf-correctness/scale issue, not a wrong-result bug; it bites exactly the NRBIG/large-nb wall this stage was rewritten to fix. Suggested: hoist the gesvd scratch (`dS`/`dU`/`dVt`/`dWork`/`dA2`/`dInfo`) to per-block-strided arenas allocated ONCE before the loop and pass slices into a scratch-taking `large_svd_V` overload (mirror the Stage-B `dScratch`/`dIntScratch` pre-sizing), so Stage A allocates/frees once, not nb× inside the loop.

- [14.5][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1517-1529, 1559-1571 — `gesvdjInfo_t params` is a RAW (non-RAII) cuSOLVER handle: created via `cusolverDnCreateGesvdjInfo(&params)` (1518/1560) and freed via `cusolverDnDestroyGesvdjInfo(params)` (1529/1571), with the throwing `CUSOLVER_CHECK`-wrapped `cusolverDnDgesvdj_bufferSize`/`cusolverDnDgesvdj` (and the intervening `DeviceBuffer dWork(...)` ctor, which can throw on OOM) BETWEEN them. If any of those throws, the stack unwinds PAST the `cusolverDnDestroyGesvdjInfo` line and the gesvdj-info handle LEAKS (the only resource in this TU not under RAII — `DeviceBuffer`/`PinnedBuffer`/`RingGuard` all clean up on unwind). Latent: triggers only on a cuSOLVER fault / scratch OOM, and the run typically aborts on such an unrecoverable error, but it violates the TU's own "RAII for ALL ... handles" standard (header comment 23-25). Suggested: wrap `params` in a tiny move-only RAII guard (ctor `cusolverDnCreateGesvdjInfo`, dtor `cusolverDnDestroyGesvdjInfo`) like the existing `RingGuard`, so an exception between create and destroy still frees it.

- [14.5][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:942-950 — The ring-buffer event creation loop `cudaEventCreateWithFlags(&r.reuse, ...)` (945) runs BEFORE `RingGuard ring_guard{ring, kStreamDeviceChunks}` is constructed (950). If the 2nd iteration's `cudaEventCreateWithFlags` throws (STEPPE_CUDA_CHECK), the already-created `ring[0].reuse` event has no guard yet (`ring_guard` is not constructed until after the loop), so it LEAKS on unwind (the `Ring` struct holds the raw `cudaEvent_t` but has no destructor — only `RingGuard` destroys them). Latent: event creation rarely fails. Suggested: construct `ring_guard` (or move event creation into a guard whose dtor iterates the array) BEFORE / as part of the creation loop so a partial-creation failure still tears down the events already made.

No further Group 14 issues found. Notes on the tasks verified clean (with the §12/FP64/scale context applied; not omitted):
- [14.1] No alloc/free mismatch. ALL device memory is `DeviceBuffer<T>` (cudaMalloc paired with cudaFree in its own dtor, device_buffer.cuh:74/117); pinned host staging is `PinnedBuffer<T>` (cudaHostAlloc/cudaFreeHost, pinned_buffer.cuh:79/112) and the H2D-source pins are `PinnedRegistryCache`→`RegisteredHostRegion` (cudaHostRegister/cudaHostUnregister, pinned_buffer.cuh:164/201). No raw `cudaMalloc`/`new[]`/`malloc`/`free`/`delete` anywhere in this .cu — it is NOT on the allocation allowlist (header 22-25), and every host buffer is a `std::vector`. The cuSOLVER `gesvdjInfo_t` (1518/1560) pairs Create with Destroy (1529/1571), and the ring `cudaEventCreateWithFlags` (945) pairs with `cudaEventDestroy` (949) — matching create/destroy families (the throw-window gaps are the 14.5 findings above, not a wrong-family mismatch). The host result vectors freed across the seam are `std::vector` (out.f2/vpair/q/v/n, gw.A/B/w, h_left/h_right/h_info/...), never device-allocated then host-freed.
- [14.2] (See the [14.2] finding above for the one hot-path miss.) The OTHER hot paths are already correctly designed against the plain-cudaMalloc/cudaFree device-wide-sync cost: the M4/M5 per-chunk slabs (dQg…dRg, dIds, the streamed ring, the tile feeder bufs) are PRE-SIZED ONCE at the max footprint and REUSED across every chunk (577-626, 885-922, 941-946) — the documented L4b/P3 fix that removed the 645 cudaMalloc + 648 cudaFree per run. The fit virtuals (rank_test/rank_sweep/gls_weights/jackknife_cov/assemble_f4/fit_chunk) allocate fresh `DeviceBuffer`s per CALL, but these are single-shot small-LA solves (m≤~50, KB-scale), one allocation burst per fit, not a per-block inner loop — within the design (the f2/streamed hot loops are the ones that were hoisted; a stream-ordered allocator/pool here would be hygiene, not a measured wall). steppe deliberately does NOT use cudaMallocAsync/pools (the RAII DeviceBuffer is the single-source owner, header 22-25); the §12 single-stream-per-device contract makes a pool's cross-stream benefit moot.
- [14.3] N/A — no `cudaMallocAsync`/`cudaFreeAsync` is used in the TU (or anywhere via DeviceBuffer); all device frees are the plain `cudaFree` in `DeviceBuffer::reset`, so there is no async/sync free-pairing surface to mismatch.
- [14.4] No free-before-async-completion (use-after-free across streams). The §12 single-stream-per-device model serializes every launch/copy on `stream_`, and each public entry point ENDS in a CHECKed `cudaStreamSynchronize(stream_.get())` before the owning `DeviceBuffer`s leave scope (compute_f2 260, run_f2_blocks_resident 679, decode_af 1095, assemble_f4 1313, jackknife_cov 1418/1455/1464/1473, rank_test 1704, rank_sweep 1804, gls_weights 1935, gls_weights_loo_batched 2034, fit_chunk 2266/2285/2327/2397/2431) — so the buffers' work has drained before their cudaFree. The RESIDENT f2/vpair buffers (rb.f2/rb.vpair) are intentionally NOT freed at the producer's scope exit: they MOVE OUT into DeviceF2Blocks/DevicePartial (303-305, 325-326, 680) and the trailing sync at 679 guarantees they are fully written before the caller (the combine / the fit gather) reads them — the documented "buffers ESCAPE, survive the jthread join" cure (309-329). The streamed ring reuses a device buffer only after its prior chunk's spill D2Hs drained via the per-buffer `cudaEventSynchronize(r.reuse)` (969-970), and `sink.finish()` (1047) drains all in-flight D2Hs before the ring buffers free at scope exit — correct cross-buffer overlap, no early free. The intra-`large_svd_V` D2D `cudaMemcpyAsync(dVout, dU/dXt)` (1542/1584) reads its scratch before scope exit, and the scope-exit cudaFree's device-wide sync drains it before the memory is released.

## Group 16 — RAII: ownership & wrapper hygiene

- [16.1][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1517-1529, 1559-1571 — `gesvdjInfo_t params` is the one cuSOLVER library handle in the TU NOT wrapped: it is created by `cusolverDnCreateGesvdjInfo(&params)` (1518/1560) and torn down by `cusolverDnDestroyGesvdjInfo(params)` (1529/1571), but the intervening `cusolverDnDgesvdj_bufferSize`/`cusolverDnDgesvdj` are throwing `CUSOLVER_CHECK`s and the `DeviceBuffer<double> dWork(...)` ctor (1524/1566) can throw on OOM — any of which unwinds PAST the Destroy line and LEAKS the gesvdj-info handle. This violates the TU's own "RAII for ALL ... handles" standard (header 22-25). It is the SAME defect Group 14 captured as [14.5][MED] (allocation-lifetime lens); re-raised here as the squarely-Group-16 wrapper-hygiene angle: every other resource in this TU (`DeviceBuffer`, `PinnedBuffer`, `Stream`, the library handles `blas_`/`solver_`) is under a move-only freeing wrapper, and this one is the lone raw Create/Destroy pair with an exception window between them. Suggested: a tiny move-only RAII guard (ctor `cusolverDnCreateGesvdjInfo`, dtor `cusolverDnDestroyGesvdjInfo`, copy deleted, null-on-move) mirroring `CublasHandle`/the `Event` wrapper, so an exception between create and destroy still frees it (16.1/16.3).
- [16.5][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:935-950 — The streamed ring REINVENTS the existing `Event` RAII wrapper: `struct Ring` holds a RAW `cudaEvent_t reuse = nullptr` (938) created by hand with `cudaEventCreateWithFlags(&r.reuse, cudaEventDisableTiming)` (945) and torn down by a hand-rolled `struct RingGuard { ... ~RingGuard() { ... cudaEventDestroy(...) } }` (947-950). steppe ALREADY provides exactly this in `device/cuda/stream.hpp` — `Event` (line 105) is a move-only owning wrapper whose default ctor creates a `cudaEventDisableTiming` event (the precise flag used here) and whose dtor `cudaEventDestroy`s it through the §7 teardown-warning sink. Replacing `cudaEvent_t reuse` with an `Event reuse;` member would delete the raw create/destroy AND the entire `RingGuard` scaffold, and would also close the partial-construction leak window (the 2nd `Ring`'s `cudaEventCreateWithFlags` throwing before `ring_guard` is constructed leaks `ring[0].reuse`) that Group 14 flagged as [14.5][LOW] — per-element RAII makes that structural, not guard-coverage-dependent. The TU does not include `Event` only because it currently hand-rolls the equivalent; `stream.hpp` is already on the include list (line 76). Suggested: hold an `Event` (or an `Event[kStreamDeviceChunks]`) in `Ring` and delete `RingGuard` (16.5) — reuse the wrapper rather than a parallel raw-handle + bespoke-guard pair.

No further Group 16 issues found. Notes on the tasks verified clean (with the §12/FP64/scale context applied; not omitted):
- [16.1] EVERY OTHER resource in the TU is wrapped. Device memory is `DeviceBuffer<T>` (no raw `cudaMalloc`/`cudaFree`); pinned host D2H staging is `PinnedBuffer<double>` (`stage_f2_`/`stage_vpair_`, 2723-2724); the amortized H2D source pins are `PinnedRegistryCache pinned_in_` (2709, owning the `cudaHostRegister`/`Unregister`); the library handles are `CublasHandle blas_` (2662) + `CusolverDnHandle solver_` (2671); the per-device statistic stream is `Stream stream_` (2661); the math-mode stickiness is the scoped `MathModeScope`/`CusolverMathModeScope` (1409, 1443, 2226, 2258). The TU declares NO raw stream/graph/graph-exec/texture/surface/memory-pool/CUDA-array; the only raw library handle is the `gesvdjInfo_t` flagged at [16.1] above.
- [16.2] Move-only + null-on-move is correctly delegated to the wrappers (their defns are in stream.hpp/handles.hpp/device_buffer.cuh/pinned_buffer.cuh, OUTSIDE this unit — verified in the consumed headers stream.hpp + handles.hpp: copy deleted, move `std::exchange`es the handle to null, dtor null-checks). Within THIS TU the move discipline is exercised correctly: `rb.f2`/`rb.vpair` `DeviceBuffer`s are `std::move`d OUT into the `DeviceF2Blocks`/`DevicePartial` impl (303-305, 325-326), the producer's moved-from `rb` then leaves scope owning nothing (no double-free); `block_sizes`/`impl` are `std::move`d into the handles (300, 322). The hand-rolled `Ring` (the one un-wrapped event owner) does NOT support move at all — it is only ever held in a fixed local array and torn down by `RingGuard`, so there is no moved-from-event double-free path here, but it is also why [16.5] recommends the `Event` wrapper (which DOES null-on-move).
- [16.3] Rule-of-five applies only to the wrapper TYPES (defined elsewhere); this TU defines `CudaBackend` (move-only via the `ComputeBackend` base, header 84-86) whose members are all RAII wrappers, so it needs NO user-declared destructor — member teardown is automatic in reverse declaration order, and the ordering is deliberately documented as load-bearing (2647-2660: `workspace_` freed before `blas_`'s non-owning pointer into it; `stream_` outlives `blas_`/`solver_`). The two local helper structs `Ring`/`RingGuard` (935-950) are the only in-TU types with destructor-relevant state: `Ring` has a freeing resource (`cudaEvent_t`) but NO destructor (the freeing dtor lives in the separate `RingGuard`), which is exactly the rule-of-five smell [16.5] addresses; `RingGuard` has the freeing dtor but is non-copyable/non-movable only by accident (it holds a raw `Ring*`+`int`, never copied), correct for its one fixed use.
- [16.4] Single clear ownership holds. Raw kernel pointers are non-owning views obtained via `DeviceBuffer::data()` / `PinnedBuffer::data()` and passed to `launch_*`/cuBLAS/cuSOLVER — NEVER freed at the call site (the wrapper frees on scope exit). The large-path helpers (`large_svd_V` 1504, `large_fit_one` 1628) take bare `double*`/`int*` SCRATCH that the CALLER owns as `DeviceBuffer`s (rank_test 1680-1683, rank_sweep 1762-1765, gls_weights 1909-1912) and slices into — a clean borrow, the scratch never freed inside the helper. No owning wrapper is passed BY VALUE: `DeviceF2Blocks`/`DevicePartial`/`F4Blocks`/`JackknifeCov`/`GlsWeights`/`MatView`/`Precision`/`QpAdmOptions` all cross by `const&` (e.g. 1237-1240, 1338-1341, 2061-2065); the only by-value owning transfers are the deliberate RETURN-by-move of `ResidentBlocks`/`DeviceF2Blocks`/`DevicePartial` (the escape cure, 295-329, 433-680). The `h_Aptr`/`h_Bptr`/`dAptr`/`dBptr` device-pointer arrays (2251-2280) hold raw `double*` VIEWS into `dQf`/`dQinv` (those buffers own the VRAM), correctly non-owning.
- [16.5] (See the [16.5] finding above for the one reinvented wrapper.) Everywhere else the TU REUSES the project wrappers rather than rolling its own: `DeviceBuffer`/`PinnedBuffer`/`PinnedRegistryCache`/`CublasHandle`/`CusolverDnHandle`/`Stream`/`MathModeScope`/`CusolverMathModeScope` are all the single-source steppe types, not bespoke re-implementations, and the TU does NOT reach for `thrust::device_vector` or a raw `unique_ptr`+deleter where the existing `DeviceBuffer` already serves (header 22-25 documents the deliberate single-owner `DeviceBuffer` choice over pools/thrust).

## Group 17 — RAII: lifetime & deleter pitfalls (CUDA-specific)

- [17.5][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2599 (guard_device), 936-950 (Ring/RingGuard), 2661-2724 (member dtors) — DEVICE-CORRECT-FREE is NOT enforced on the teardown path of a backend bound to a NON-default device. Every COMPUTE entry runs `guard_device()` (cudaSetDevice device_id_, 2599) so allocations land on device_id_, but DESTRUCTION runs under whatever device is ambient at the destruction site: `~CudaBackend` (member dtors → `DeviceBuffer::reset`/`Stream`/`CublasHandle`/`CusolverDnHandle`/`PinnedBuffer` destroy), the per-call `DeviceBuffer`/`RingGuard` scope-exit frees, and — critically for the M4.5 scaffold — the resident `rb.f2`/`rb.vpair` buffers that MOVE OUT into `DeviceF2Blocks`/`DevicePartial` (303-305, 325-326, 680) and are freed LATER by the host-side combine, which may run with a different (or device-0) ambient. None of these free sites re-selects device_id_. This is SAFE TODAY only because `cudaFree`/`cudaFreeHost`/`cudaStreamDestroy`/`cudaEventDestroy`/`cublasDestroy`/`cusolverDnDestroy` are device-association-agnostic (the pointer/object carries its own device/context, so the free works regardless of the current device) — i.e. the whole multi-GPU escape-the-buffer design rests on an UNSTATED invariant that the wrappers never need the alloc device current to free. The 17.5 task's "deleter must cudaSetDevice to the alloc device (record+restore)" is therefore deliberately NOT done; that is defensible for `cudaFree`, but it is an undocumented load-bearing assumption at exactly the multi-GPU seam this file is scaffolding (a future switch to `cudaMallocAsync`/a per-device memory pool, or any free API that IS device-current-sensitive, would silently free on the wrong device). Suggested: document in the header `STANDARDS` block (and/or at the `rb` escape comment 303-329) that device-resident buffers are freed device-agnostically by design (cudaFree carries the device), so the cross-device combine free is sound and the wrappers intentionally omit a record-and-restore cudaSetDevice — making the invariant explicit rather than implicit.

No further Group 17 issues found. Notes on the tasks verified clean (with the §12/FP64/scale/multi-GPU context applied; the wrapper headers device_buffer.cuh / pinned_buffer.cuh / handles.hpp / stream.hpp read in full):
- [17.1] NON-THROWING DESTRUCTOR is satisfied everywhere. The TU declares no user destructor for `CudaBackend` — teardown is member RAII, and every wrapper dtor is non-throwing log-and-swallow: `DeviceBuffer::reset` (device_buffer.cuh:111-125, `cudaFree`→STEPPE_LOG_WARN), `PinnedBuffer::reset` (pinned_buffer.cuh:108-120, `cudaFreeHost`→WARN), `RegisteredHostRegion::reset` (pinned_buffer.cuh:197-208, `cudaHostUnregister`→WARN), `Stream::destroy` (stream.hpp:84-95, `cudaStreamDestroy`→WARN), `Event::destroy` (stream.hpp:156-167), `CublasHandle::destroy`/`CusolverDnHandle::destroy` (handles.hpp:193-209/359-368, `cublasDestroy`/`cusolverDnDestroy`→WARN), and the `MathModeScope`/`CusolverMathModeScope` restore()s. The ONE in-TU destructor — `RingGuard::~RingGuard` (949) — does `(void)cudaEventDestroy(...)` (status DELIBERATELY discarded, never thrown), the correct noexcept-cleanup form. CONTEXT-MAY-BE-GONE-AT-EXIT: all teardown frees route their nonzero status (which is what `cudaErrorCudartUnloading`/a torn-down context returns) to STEPPE_LOG_WARN and continue — a backend destroyed during process exit after the driver has begun unloading warns but never throws/aborts, so the "context gone at exit" hazard is handled. (The two THROW-WINDOW gaps where an exception BETWEEN a manual create and its destroy leaks — the raw `gesvdjInfo_t` 1517-1571 and the `Ring` event partial-construction 942-950 — are LIFETIME leaks already captured as Group 14 [14.5][MED]/[14.5][LOW] and Group 16 [16.1]/[16.5]; they are not destructor-throws, so not re-raised under 17.1.)
- [17.2] DELETER MATCHES ALLOCATOR for every owner. `DeviceBuffer` pairs `cudaMalloc` (device_buffer.cuh:74) ↔ `cudaFree` (117); `PinnedBuffer` pairs `cudaHostAlloc(...cudaHostAllocDefault)` (pinned_buffer.cuh:80) ↔ `cudaFreeHost` (112); `RegisteredHostRegion` pairs `cudaHostRegister` (164) ↔ `cudaHostUnregister` (201); the in-TU `RingGuard` pairs `cudaEventCreateWithFlags` (945) ↔ `cudaEventDestroy` (949); the raw `gesvdjInfo_t` pairs `cusolverDnCreateGesvdjInfo` (1518/1560) ↔ `cusolverDnDestroyGesvdjInfo` (1529/1571); the library handles pair `cublasCreate`/`cusolverDnCreate` ↔ `cublasDestroy`/`cusolverDnDestroy`. No cross-family mismatch (no `cudaFree`-on-`cudaMallocHost`, no `cudaFreeAsync`-on-`cudaMalloc`, no `delete[]`-on-`cudaMalloc`, no `cudaFreeArray`-without-`cudaMallocArray`). The TU uses NO `cudaMallocAsync`/`cudaFreeAsync`/`cudaMallocArray`/`cudaMallocHost`, so the async-pair and array-pair pitfalls have no surface here (grep-verified).
- [17.3] NO `unique_ptr<T[]>` (or `make_unique<T[]>`) over device memory — the UB default-`delete[]`-on-`cudaMalloc` trap has zero surface (grep-verified: no `unique_ptr<...[]>`/`new[]`/`delete[]` anywhere). Device memory is exclusively `DeviceBuffer<T>` (custom `cudaFree` deleter in its own dtor). The only `std::unique_ptr`/`make_unique` uses are `std::make_unique<DeviceF2Blocks::Impl>()` (302) / `std::make_unique<DevicePartial::Impl>()` (324) — HOST objects (the Impl structs that themselves hold `DeviceBuffer`s), single-object form with the default scalar `delete`, correct.
- [17.4] RAII-vs-ASYNC-LIFETIME (free at scope exit ≠ async work done) is correctly closed — this is the SAME guarantee Group 14 [14.4] audited in full and confirmed clean: the §12 single-stream-per-device model serializes all launches/copies on `stream_`, and every entry point ENDS in a CHECKed `cudaStreamSynchronize(stream_.get())` before its owning `DeviceBuffer`s leave scope (e.g. 260, 379, 569, 668, 679, 1095, 1313, 1418/1455/1464/1473, 1704, 1804, 1935, 2034, 2266/2285/2327/2397/2431), so no buffer is freed while its async work is still in flight. The streamed ring reuses a device buffer only after its prior chunk's spill D2Hs drained via `cudaEventSynchronize(r.reuse)` (969-970), and `sink.finish()` (1047) drains all in-flight D2Hs before the ring buffers free at scope exit. The resident `rb.f2`/`rb.vpair` deliberately do NOT free at producer scope exit: they move out (303-305, 325-326) and the trailing sync (679) guarantees they are fully written before the combine reads/frees them ("buffers ESCAPE, survive the jthread join"). The intra-`large_svd_V` D2D copies (1542/1584) read their scratch before that scratch's scope-exit `cudaFree` (whose device-wide sync also drains the stream). No use-after-free across the async boundary.

## Group 18 — Correctness traps (wrong numbers, not crashes)

No Group 18 issues found. This TU is PURE host-side orchestration: it defines ZERO `__global__` kernels, has ZERO triple-chevron `<<<...>>>` launches, and uses ZERO barrier / warp primitives (`__syncthreads`, `__syncwarp`, `__shfl*`, `__ballot*`, `__any`, `__all`) — grep-verified across the full 2756-line file and consistent with the Group 11/12/13 passes already on record. Every device-side parallelism reaches the GPU through a `launch_*` wrapper (f2_block_kernel.cu, f2_blocks_kernel.cu, decode_af_kernel.cu, qpadm_fit_kernels.cu) or through cuBLAS/cuSOLVER, all DEFINED in OTHER TUs. Tasks 18.1–18.7 all target the INSIDE of a kernel body (the divergent/missing `__syncthreads`, the warp-synchronous / non-`_sync` intrinsic, the missing `if(idx<n)` bounds guard, the cross-thread shared-mem read, the order-assuming reduction) — none of which has any surface in this host file; they belong to the kernel `.cu` units. Notes on the tasks, verified clean (not omitted):
- [18.1/18.2/18.3/18.4/18.6] N/A — no `__syncthreads()`, no `__syncwarp()`, no warp shuffle/vote intrinsics, and no `__shared__` declarations exist in this TU, so there is no in-kernel barrier-placement, warp-scheduling, or shared-memory cross-thread-read surface here. The shared-memory / barrier / warp-intrinsic correctness is the responsibility of the kernel TUs.
- [18.5] N/A here — the missing-`if(idx<n)` OOB trap is a kernel-thread-index concern; this file launches no kernels directly. The HOST bounds it controls are sound: the batch/grid extents it passes (`nb`, `B`, `s_pad`, `m`) are clamped before launch (`max_blocks_per_chunk`/`streamed_max_blocks` → `min(…, kMaxGridZ)` at 614/820-830, the `B_max` VRAM clamp at 2153-2155) and the host index loops are all bounded by `.size()`/the derived extent (e.g. the bucket scans 481-490/737-744, the column-by-column inverse `for c < m` 2270, the survivor filter `for j < B` 2350, the rankdrop `for k < nrk` 2518), with the partition contract validated once in `core::block_ranges` (460-468/719-727) — no host index runs past its array.
- [18.7] The only reductions in this TU are SEQUENTIAL HOST loops with a fixed, deterministic iteration order — the jackknife normalizer `n = Σ block_sizes` (1291-1293, 1367-1368, 2129-2130, accumulated in `long long` then widened to `double`) and `tr(Q) = Σ Q[k,k]` (1421-1422). These do NOT assume a GPU thread-execution order; they ARE the §12 fixed-order form (a single host thread summing in index order), so they are correct, not order-assuming. The order-dependent-float-reduction trap (a GPU reduction relying on an unenforced thread order) has no surface here — the device-side reductions (the f4 loo/total, the SE-from-wmat, the SVD/ALS) all live in the kernel TUs.

## Group 19 — Performance: debug leftovers

No Group 19 issues found. Verified across the full 2756-line TU (grep-confirmed for `cudaDeviceSynchronize`, `printf`/`fprintf`/`std::cout`/`std::cerr`/`puts`, `#if 0`/`#if false`, `__syncthreads`/`cudaThreadSynchronize` — ZERO hits for every one). Notes on each task (verified, not omitted):
- [19.1] No stray `cudaDeviceSynchronize()` anywhere in the TU. ALL synchronization is the targeted per-device statistic-stream `cudaStreamSynchronize(stream_.get())` (260, 379, 569, 668, 679, 1095, 1313, 1418, 1455, 1464, 1473, 1704, 1804, 1935, 2034, 2266, 2285, 2327, 2397, 2431) or `cudaEventSynchronize(r.reuse)` (970) — never the device-wide barrier. Each stream sync is a load-bearing correctness barrier, NOT a debug leftover: drain before a D2H result read (260, 1095, 1313), before freeing the feeder inputs whose VRAM the bucket slabs reuse (569), before the next chunk overwrites the reused slabs (668, the documented §12 single-stream serialization), the trailing "resident tensors fully written before the caller reads" drain (679), and the MEASURED cuSOLVER-potrs-lane hazard ("an undrained batched-potrs lane returns cudaErrorInvalidValue on the following cudaMemcpyAsync — MEASURED on box5090", 2282-2285). The hot paths went to documented lengths to REMOVE per-block/per-chunk syncs (the "dominant 701× host round-trip is gone" Stage-A loop 1976-1979 has NO per-block sync; the streamed spill loop explicitly notes "NO cudaStreamSynchronize here" 1029-1030). The NDEBUG-gated debug sync in `STEPPE_CUDA_CHECK_KERNEL` (check.cuh, the per-launch async-fault attribution) is the intentional one the task exempts and is NOT in this TU (this file launches via `launch_*` wrappers, never `<<<>>>`). N/A — no leftover serializing sync.
- [19.2] No leftover `printf`/`fprintf`/iostream debug prints and no `#if 0`/`#if false`/`#ifdef NEVER` blocks (grep-verified zero hits; consistent with the Group 3 [3.2] dead-code pass). The only "diag/trace" prose is the comment "S4 fudge diag (per-model trace)" at 2236, which labels a genuine COMPUTE launch (`launch_add_fudge_diag_models_batched`, the S4 fudge-diagonal kernel), not a debug print — the word "trace" there means the matrix trace term, not logging. Diagnostic OUTPUT in this TU goes only through the structured `STEPPE_LOG_WARN`/`STEPPE_CUDA_WARN` capability-degrade path (the non-throwing P2P probe, check.cuh), which is intentional observability, not a stray debug print. N/A.
- [19.3] No `__syncthreads()` at all — this TU is PURE host-side orchestration (zero `__global__` kernels, zero `<<<...>>>` launches; consistent with the Group 11/12/18 passes). The barrier-placement concern (a `__syncthreads` no longer guarding anything) is a kernel-body matter that belongs to the kernel `.cu` units (f2_block_kernel.cu, f2_blocks_kernel.cu, decode_af_kernel.cu, qpadm_fit_kernels.cu) — it has NO surface in this file. N/A.

## Group 20 — Performance: memory access

No Group 20 issues found. This TU is PURE host-side orchestration: grep-verified across the full 2756-line file there are ZERO `__global__` kernel definitions, ZERO triple-chevron `<<<...>>>` launches, ZERO `__device__`/`__shared__` declarations, and ZERO `threadIdx`/`blockIdx`/`blockDim`/`gridDim`/`__ldg`/texture uses (consistent with the Group 11/12/18/19 passes already on record). All three Group 20 tasks are device-kernel-INTERIOR memory-access concerns — global-memory coalescing, shared-memory bank patterns, and per-thread register caching — and have NO surface in a file that issues every device computation through `launch_*` wrappers (f2_block_kernel.cu, f2_blocks_kernel.cu, decode_af_kernel.cu, qpadm_fit_kernels.cu) or through cuBLAS/cuSOLVER, all defined in OTHER TUs. Notes on each task (verified, not omitted):
- [20.1] N/A — uncoalesced global access (consecutive threads not hitting consecutive global addresses) is a within-kernel thread→address mapping property. This file declares no kernel and maps no thread index to a global address; the f2 feeder/gather/assemble and the qpAdm-fit kernels own their own access patterns in their `.cu` units, and the dense LA goes through cuBLAS/cuSOLVER (NVIDIA-tuned coalescing). The host-side memory the TU DOES touch is `std::vector`/scalar reads on the CPU (the bucket scans 481-490/737-744, the block-layout fill 463-468/722-727, the per-model index arenas 2180-2187, the column-by-column B-pointer fill 2270-2272, the survivor filter 2350-2371, the SE scatter 2432-2436) — host DRAM accesses, not a GPU global-coalescing surface. The column-major [P×M] H2D slices it issues (e.g. 974-988) are SINGLE contiguous `cudaMemcpyAsync` runs (the slice "is ONE cudaMemcpyAsync per matrix", 974-977), i.e. fully contiguous DMA, not a strided kernel gather.
- [20.2] N/A — shared-memory bank conflicts require a `__shared__` array and a thread-indexed access into it; there is no `__shared__` declaration anywhere in this TU (grep-verified). The determinism `workspace_` (2672, a `DeviceBuffer<std::byte>` of kCublasWorkspaceBytes) is cuBLAS's GLOBAL-memory scratch, not CUDA `__shared__`/smem, and its internal use is cuBLAS's own. Bank-conflict analysis belongs to the kernel TUs that declare shared memory.
- [20.3] N/A here as a register/shared caching concern (that is a kernel-interior optimization). The CLOSEST host analogue — recomputing a loop-invariant value instead of hoisting it — was already surveyed under Group 7 (the `m*m`/`Mm` extent recomputation [7.3], the `n = Σ block_sizes` reductions [7.2]) and Group 15 (the per-column H2D of the deterministic B-pointer array at 2273, which re-uploads a recomputable pointer set every iteration [15.1]); those are host-side hoist/caching observations, captured in their proper groups, not a GPU re-read-global-into-register defect. No device-side repeated global load exists in this file to flag.

## Group 21 — Performance: occupancy & registers

No Group 21 issues found. This TU is PURE host-side orchestration: grep-verified across the full 2756-line file there are ZERO `__global__` kernel definitions, ZERO triple-chevron `<<<...>>>` launches, ZERO `__device__`/`__shared__` declarations, and ZERO occupancy/register hints to evaluate (`__launch_bounds__`, `__forceinline__`, `#pragma unroll`) — all return zero hits, consistent with the Group 11/12/18/19/20 passes already on record. All four Group 21 tasks are device-kernel-INTERIOR occupancy/register concerns and have NO surface in a file that issues every device computation through `launch_*` wrappers (f2_block_kernel.cu, f2_blocks_kernel.cu, decode_af_kernel.cu, qpadm_fit_kernels.cu) or through cuBLAS/cuSOLVER, all defined in OTHER TUs. The §12 single-statistic-stream serialization is an intentional parity requirement, not an occupancy lever, and is not flagged. Notes on each task (verified, not omitted):
- [21.1] N/A — warp divergence (heavy branching serializing a warp) is a within-kernel thread-execution property. This file declares no kernel and maps no thread to a divergent branch; the per-warp control flow of the f2 feeder/gather/assemble and the qpAdm-fit kernels lives in their own `.cu` units, and the dense LA goes through cuBLAS/cuSOLVER (NVIDIA-tuned). The HOST branching this TU does contain — the size-bucket scans (481-490/737-744), the small-vs-large fit dispatch (`model_fits_small_path` 1489, the gesvdj-vs-gesvd branch 1515/1558), the `switch (opts.jackknife)` (2354-2367), the survivor filter (2350-2371) — runs on a single CPU thread, NOT a GPU warp, so there is no warp to serialize.
- [21.2] N/A — excessive shared memory per block cratering occupancy requires a `__shared__` allocation (static or the dynamic `<<<…, smem_bytes>>>` third launch arg); there is NO `__shared__` declaration and NO `<<<>>>` launch in this TU (grep-verified). The cuBLAS determinism `workspace_` (2672, a `DeviceBuffer<std::byte>` of kCublasWorkspaceBytes) is GLOBAL-memory scratch handed to cuBLAS, not CUDA `__shared__`/smem, so it has no bearing on per-block shared-memory occupancy. Smem sizing is the kernel TUs' concern.
- [21.3] N/A — register spills (a monolithic kernel spilling to local memory, the split candidate) is a per-`__global__` register-pressure property visible only in a kernel body. No kernel is DEFINED here. The large HOST member functions (e.g. `run_f2_blocks_resident` 433, `fit_chunk` ~2100, `gls_weights_loo_batched` ~1950, `capabilities` 1119) are plain CPU functions whose locals live on the CPU stack / in `std::vector`s — there is no GPU register file or local-memory spill surface in host code. The split-the-kernel direction belongs to the kernel `.cu` units.
- [21.4] N/A — missing register hints (`#pragma unroll` on a tight device loop, `__launch_bounds__` to cap registers/raise occupancy, `__forceinline__` on a hot device helper) all attach to `__global__`/`__device__` code, of which this TU has none (grep-verified zero `__launch_bounds__`/`__forceinline__`/`#pragma unroll`/`__global__`/`__device__`). The host loops here (the bucket scans, the column-by-column inverse `for c < m` 2270, the rankdrop sweep) are CPU loops the C++ compiler optimizes under the project's host flags; a CUDA register hint would be meaningless on them. The hints belong with the kernel definitions in the device `.cu` units, where the prior groups already directed the kernel-interior findings.

## Group 22 — Performance: compute & launch

- [22.4][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2270-2281 — The S8 batched-inverse column loop runs `m` (= nl·nr) iterations, and EACH iteration issues a per-column H2D of the B RHS pointers (2273) followed by ONE `cusolverDnDpotrsBatched` (2277) on the single statistic stream — i.e. `m` separate small batched-solve launches plus `m` small H2Ds where the whole inverse is fully determined up front. The B-pointer arrays for all `m` columns are computable before the loop (`dQinv.data()+j*Mm + c*M`), so this is the classic many-small-repeated-launches-dominated-by-per-launch-cost pattern on the hot per-chunk rotation path; cuSOLVER exposes no nrhs>1 potrsBatched, but the per-column launches could be issued back-to-back from ONE pre-uploaded `[m×B]` pointer arena (removing the m interleaved H2Ds), and the m solves are independent enough to consider a CUDA-graph capture of the column loop (profiler-confirmed). The per-column H2D half of this is already captured as Group 15 [15.1]; the LAUNCH-COUNT half (m potrsBatched dispatches + the per-launch overhead) is the Group 22 angle. PARITY-NEUTRAL. Suggested: pre-upload one `[m×B]` pointer arena before the loop (closes the 15.1 H2D), then either fire the m potrsBatched calls without interleaved copies or, if the profiler shows per-launch cost dominating at the rotation envelope, capture the column loop into a CUDA graph replayed per chunk.

- [22.4][MED] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:1780-1810 — The S5 rank sweep loop runs `rmax+1` iterations and each iteration issues the per-rank fit launches (seed/ALS/weights-chisq or `large_fit_one`) AND THEN forces a full `cudaStreamSynchronize` (1804) to read back the 1-int status + 1-double chisq (1800-1803). The trailing sync per rank turns the sweep into a launch→sync→launch ladder with no overlap between consecutive ranks: rank r+1's seed cannot begin until rank r's tiny D2H has drained. The chisq/status of all ranks could be written into `[rmax+1]`-length device arrays (one slot per rank) and D2H'd + synced ONCE after the loop, so the rmax+1 fits enqueue back-to-back and the host pays a single round-trip. This is the same per-iteration-host-round-trip the LOO Stage-A rewrite explicitly removed ("the dominant 701× host round-trip is gone", 1976-1979) — the rank sweep still has it. rmax is small (min(nl,nr)-1, ≤ ~9 small-path) so the absolute cost is modest, but the S8 rotation already proved the batched form is the design direction. PARITY-NEUTRAL (same per-rank math; only WHEN the host reads moves). Suggested: accumulate chisq[r]/status[r] into device arrays indexed by r and do ONE D2H + one sync after the `for r` loop, removing the rmax per-rank syncs.

- [22.3][LOW] /home/suzunik/steppe/src/device/cuda/cuda_backend.cu:2270-2272 — Inside the column loop the inner `for j` recomputes the loop-invariant per-model base `dQinv.data() + j*Mm` on every column `c` (only the `+ c*M` term varies with the outer loop). The `j*Mm` products (B of them) are recomputed `m` times — `m·B` multiplies where `B` per-model bases computed once (e.g. an `h_QinvBase[j] = dQinv.data()+j*Mm` vector, like the `h_Aptr` array already built once at 2251-2252) would leave only the `+ c*M` add per column. Minor host arithmetic, dominated by the H2D/launch cost above; hygiene. Suggested: hoist the B per-model `dQinv.data()+j*Mm` bases into a vector once before the `for c` loop and add `c*M` per column (mirrors the single `h_Aptr` precompute at 2252).

- [22.1] N/A — no atomics anywhere in this TU (grep-verified: no `atomicAdd`/`atomicCAS`/`atomic*`/`<cuda/atomic>`/`std::atomic`). It is pure host orchestration; every device-side reduction (f4 loo/total, SE-from-wmat, the SVD/ALS) lives in the kernel TUs, and the host reductions (`n = Σ block_sizes` 1291-1293/1367-1368/2129-2130, `tr(Q)` 1421-1422) are single-thread sequential sums in fixed index order — the §12 deterministic form, not an atomic-vs-reduction tradeoff. No surface for 22.1.

- [22.2] N/A — no expensive integer div/mod in any loop. The only `%` in the file is `chunk_idx % kStreamDeviceChunks` (965) and `ring[chunk_idx % kStreamDeviceChunks]` is the same; `kStreamDeviceChunks` is `constexpr int = 2` (934), a power of two, so the compiler lowers it to a mask — not a runtime division. The only `/` in loops are the FP `alpha = 1.0/nb` covariance scale (1406/2224, computed ONCE outside the inner work) and the SE scale `(nb-1)/sqrt(nb)` (2381-2382, computed once per chunk). The bucket/arena offset arithmetic (`j*(nl+1)`, `j*Mm`, `static_cast<std::size_t>(nr)*r*ob`, etc.) is multiplies/adds, never per-iteration integer division. No power-of-two-stride div/mod to convert.
