# Review findings — src__device__cuda__device_partial

Files: /home/suzunik/steppe/src/device/cuda/device_partial.cu, /home/suzunik/steppe/src/device/cuda/device_partial_impl.cuh, /home/suzunik/steppe/src/device/device_partial.hpp

## Group 4 — Type & numeric

- [4.2][LOW] src/device/device_partial.hpp:39,40 — The shape contract this handle carries (`int P`, `int n_block_local`) describes a `[P*P*n_block_local]` resident buffer (impl comment, device_partial_impl.cuh:17-18). No index arithmetic or allocation happens in this unit, so there is no overflow here; but the `int` shape fields are the *width source* for the consumers (cuda_backend.cu/p2p_combine.cu) that do compute `P*P*n_block_local`, where the product exceeds 2^31 at scale (P≈2500, n_block≈757). Suggested: leave as-is for this unit (correctly delegates to DeviceBuffer); ensure the consuming TUs widen to size_t/int64_t before the `P*P*n_block_local` product — flag belongs to those units, not this one.

## Group 21 — Performance: occupancy & registers

No Group 21 issues found. This is a declaration-only unit with NO `__global__`/`__device__` kernels and no launch sites anywhere in the three files, so every Group 21 task (a per-kernel occupancy/register concern) is structurally out of scope. device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members (host code); device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate; device_partial.hpp is the CUDA-free opaque-handle declaration with one host `const noexcept` accessor (`empty()`, device_partial.hpp:50). 21.1: there is no kernel and no branching at all (no `if`/`switch` in any thread context — `empty()` at device_partial.hpp:50 is a host predicate), so no warp divergence is possible. 21.2: no `__shared__` memory is declared anywhere, so there is no per-block shared-memory footprint to crater occupancy. 21.3: there are no kernels to spill registers — no monolithic device function exists, and the data members are device pointers owned by `DeviceBuffer<double>` (device_partial_impl.cuh:17-18) plus plain host scalars/`std::vector<int>` (device_partial.hpp:39-46). 21.4: there is correctly NO `#pragma unroll`/`__launch_bounds__`/`__forceinline__` here — those hints belong on the kernels that actually iterate this resident f2/vpair tensor, which live in the consuming TUs (cuda_backend.cu/p2p_combine.cu); the out-of-line `= default` host special members at device_partial.cu:10-13 want no register hints. Any genuine occupancy/register finding belongs to those consuming units, not this declaration-only handle.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

## Group 3 — Dead / commented-out code

- [3.3][LOW] src/device/device_partial.hpp:8 — `#include <cstddef>` is unused: the header declares only `int`/`bool` scalars, `std::vector<int>` (`<vector>`), and `std::unique_ptr<Impl>` (`<memory>`); no `std::size_t`/`ptrdiff_t`/`offsetof`/`std::byte` etc. appears (grep over the file is empty). Harmless, but it's a stray include. Suggested: drop `#include <cstddef>` (or, if kept deliberately as a self-sufficiency safety net, leave it — pure hygiene, no correctness impact).

## Group 5 — Hardcoded values / magic numbers

- [5.1][LOW] src/device/device_partial.hpp:42 — `int device_id = -1` is an unnamed sentinel literal for "no device / unset CUDA ordinal". The intent is clear from the member name + comment ("the physical CUDA ordinal"), and `-1` is the conventional invalid-ordinal value, so this is not a correctness risk; it is only a hygiene note that the same sentinel recurs across the CUDA seam. Suggested: optionally hoist a shared `constexpr int kInvalidDeviceId = -1` (or reuse an existing project constant) so the empty/moved-from sentinel is named in one place rather than spelled `-1` inline — pure hygiene, no behavior change.

## Group 6 — Naming

No Group 6 issues found. The unit's names are domain-canonical or self-documenting: `P` (device_partial.hpp:39) is the project/AT2-canonical population-count symbol, not a cryptic single letter; `f2`/`vpair` (device_partial_impl.cuh:17-18) are the established f2-statistic / variance-pair domain terms matching the header's "f2/Vpair" comment; `n_block_local`, `b0`, `device_id`, `block_sizes`, `impl`, `empty()` are accurate and each documented inline. No misleading names (6.2), the `P` (uppercase domain symbol) + snake_case scalars mix is an intentional domain convention rather than an in-file inconsistency (6.3), and no nonstandard abbreviations beyond documented domain terms (6.4). device_partial.cu defines only out-of-line special members with no local identifiers.

## Group 7 — Duplication

No Group 7 issues found. The unit is declaration-only and intentionally minimal: device_partial.cu:10-13 is the four-line out-of-line pimpl special-member idiom (`= default` ctor/dtor/move-ctor/move-assign), which exists *because* the CUDA-free header cannot default them with an incomplete `Impl` — it is the minimum boilerplate the pimpl pattern requires, not collapsible duplication (a macro would only obscure it) (7.4). The two `DeviceBuffer<double>` members `f2`/`vpair` (device_partial_impl.cuh:17-18) are distinct domain tensors (the f2 statistic and its variance pair), a legitimate two-field aggregate, not a copy-pasted block differing by a constant (7.1). There are no loops, expressions, or computed values anywhere in the unit, so there is nothing loop-invariant to hoist (7.2) and no repeated `sizeof`/cast to fold (7.3) — the `[P*P*n_block_local]` shape appears only in twin comments, not as live arithmetic.

## Group 8 — Comments

- [8.2][LOW] src/device/device_partial.hpp:16-18 — Stale comment: the doc comment says "the DeviceBuffer<double> f2/vpair owners + the resident device pointers live in `Impl`, defined only in cuda/device_partial.cu", but `struct DevicePartial::Impl` is actually defined in cuda/device_partial_impl.cuh:16-19; device_partial.cu only includes that .cuh and defines the out-of-line special members. The sibling reference at device_partial.hpp:53 already correctly points to `cuda/device_partial_impl.cuh`, so this comment is internally inconsistent and predates the .cuh split. Suggested: change "defined only in cuda/device_partial.cu" to "defined in cuda/device_partial_impl.cuh" to match line 53 and the actual location.

## Group 9 — Constants & configuration

No Group 9 issues found. This is a declaration-only unit with no tunable knobs and no executable logic. 9.1: the public shape fields (device_partial.hpp:39-46 `P`, `n_block_local`, `b0`, `device_id`, `block_sizes`) are intentionally mutable — DevicePartial is a move-only data-carrier handle populated incrementally after construction by the producing worker and read by the combine, so they cannot be `const`/`constexpr`; their sentinel defaults are in-class initializers (the `-1`/`0` literals are already covered as a Group 5 hygiene note). 9.2: no configuration knobs are buried in logic anywhere in the three files — there is no logic, only the pimpl special-member declarations (device_partial.cu:10-13) and the two-field Impl aggregate (device_partial_impl.cuh:16-19). 9.3: no positional-boolean call sites exist; device_partial.cu defines only `= default` special members and `empty()` (device_partial.hpp:50) takes no arguments.

## Group 10 — Initialization

No Group 10 issues found. 10.1: there are no local variables anywhere in the unit — device_partial.cu:10-13 is only `= default` special members, and device_partial_impl.cuh / device_partial.hpp are declaration-only, so there is no "declared far from first use" or "uninitialized-then-assigned" pattern to flag. 10.2: every scalar data member carries an explicit in-class initializer (device_partial.hpp:39-42: `int P = 0`, `int n_block_local = 0`, `int b0 = 0`, `int device_id = -1`), so none of them rely on implicit zero-init; the class-type members (`std::vector<int> block_sizes` at device_partial.hpp:46, `std::unique_ptr<Impl> impl` at device_partial.hpp:54, and the two `DeviceBuffer<double>` f2/vpair at device_partial_impl.cuh:17-18) are default-constructed to their well-defined empty/null states by their own default ctors, not by any zero-init assumption. The DeviceBuffer default-ctor semantics live outside this unit; from this unit's vantage the members are correctly default-constructed.

## Group 11 — Qualifiers & const-correctness

No Group 11 issues found. This is a declaration-only unit with no kernels, no `__global__`/`__device__`/`__host__` functions, and no pointer parameters anywhere in the three files. 11.1: there are no read-only kernel pointers to annotate with `const __restrict__` — the resident `double*` data lives inside the `DeviceBuffer<double>` members (device_partial_impl.cuh:17-18) and is consumed by kernels in other TUs (cuda_backend.cu/p2p_combine.cu), where any `const __restrict__` finding belongs. 11.2: no execution-space qualifier inconsistency — the only member function, `empty()` (device_partial.hpp:50), is correctly an un-annotated host-only `const noexcept` accessor (DevicePartial is the CUDA-free orchestrator handle, never referenced from device code), and device_partial.cu:10-13 are plain host `= default` special members, all consistent with the CUDA-free seam design (architecture.md §4). 11.3: there are no host/device helper functions at all, so nothing is duplicated that should be folded into a single `__host__ __device__` definition. 11.4: no kernel takes any by-value parameter — `Impl` is owned via `std::unique_ptr<Impl>` (device_partial.hpp:54) and DevicePartial is move-only (copy deleted, device_partial.hpp:35-36), so neither is ever passed by value into a kernel; `__grid_constant__`/by-pointer concerns do not apply here.

## Group 12 — Launch config & indexing

No Group 12 issues found. This is a declaration-only unit with no `__global__` kernels and no `<<<grid, block>>>` launch sites anywhere in the three files, so every Group 12 task is out of scope here. device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members (host code); device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate; device_partial.hpp is the CUDA-free opaque-handle declaration. 12.1: no block dimension is specified anywhere — no warp-size multiple to check. 12.2: no grid dimension is computed or hardcoded — the `[P*P*n_block_local]` shape (device_partial_impl.cuh:17-18) appears only in comments, never as a launch extent. 12.3: there is no one-elem-per-thread kernel and no thread indexing, so the grid-stride-loop concern does not apply; the kernels that actually iterate this resident f2/vpair tensor live in the consuming TUs (cuda_backend.cu/p2p_combine.cu), where any grid-stride finding belongs. 12.4: no launch config exists to derive vs bake in — no `cudaOccupancyMaxPotentialBlockSize` decision point here. 12.5: no compute-capability or device-property assumptions — `device_id` (device_partial.hpp:42) is a plain carried ordinal, queried/set by the producing worker in another TU, not a hardcoded cap.

## Group 13 — Error handling

No Group 13 issues found. This is a declaration-only unit with no CUDA runtime calls, no kernel launches, and no error macros anywhere in the three files. 13.1: there are no `cuda*` API calls to wrap in a CHECK — device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members; the actual `cudaMalloc`/`cudaFree` of the resident `DeviceBuffer<double>` f2/vpair pair (device_partial_impl.cuh:17-18) happens inside `DeviceBuffer`'s own ctor/dtor (a different unit), and the destructor-frees this handle's lifetime comment (device_partial.hpp:24-28) describes are delegated entirely to those `DeviceBuffer` members, so any cudaFree-return check belongs to the DeviceBuffer unit, not here. 13.2: there are no `<<< >>>` launches in this unit, so there is no `cudaGetLastError()` + later-sync pair to require. 13.3: nothing is "inconsistently" checked because nothing is checked at all here — the unit issues zero CUDA calls. 13.4: no homegrown error-handling macro is defined or used anywhere in the three files.

## Group 14 — Memory: allocation & lifetime

No Group 14 issues found. This unit performs NO direct device allocation or free — all CUDA memory ownership is delegated to the two `DeviceBuffer<double>` members (`f2`/`vpair`, device_partial_impl.cuh:17-18), which own their own `cudaMalloc`/`cudaFree` lifetime in a different unit (device_buffer.cuh). 14.1: no alloc/free-API mismatch is possible here — there is no raw `cudaMalloc`/`new[]`/`malloc` and no matching free anywhere; device_partial.cu:10-13 is only `= default` special members, and the buffer pair is freed exclusively through `DeviceBuffer`'s own dtor (RAII), so the alloc and free are guaranteed to use the same API by construction (the pairing concern, if any, belongs to the DeviceBuffer unit). 14.2/14.3: no stream-ordered (`cudaMallocAsync`/`FreeAsync`) vs plain (`cudaMalloc`/`cudaFree`) decision is made or visible in this unit — there are zero allocator calls, so there is no sync-across-all-streams hot-path concern and no async/sync free-pairing mismatch to flag here; those belong wherever `DeviceBuffer` chooses its allocator. 14.4: the header documents a real cross-stream free-after-combine ordering contract (device_partial.hpp:24-28: the handle "frees in its destructor AFTER the combine has consumed it"), but this unit only *declares* and delegates that contract — the destructor is `= default` (device_partial.cu:11) and merely runs the `DeviceBuffer` dtors; the actual guarantee that the combine's async work on these buffers has completed before the handle is destroyed is enforced by the orchestrator's handle-vector lifetime (it must outlive the combine) and by `DeviceBuffer`'s own dtor synchronization, neither of which lives here, so there is no use-after-free bug to assign to this unit (the move-only design at device_partial.hpp:33-36 correctly prevents accidental double-ownership/early-free via copy). 14.5: there are no error paths to leak on — `unique_ptr<Impl> impl` (device_partial.hpp:54) and the two `DeviceBuffer` members are RAII and exception-safe; the `= default` move ctor/assign (device_partial.cu:12-13) transfer the `unique_ptr` with no intermediate raw-pointer window, and the empty-shard path (`impl == nullptr`, `empty()` at device_partial.hpp:50) frees nothing, so no construction/move/error path can leak the resident pair.

## Group 15 — Memory: transfers

No Group 15 issues found. This is a declaration-only unit with zero `cudaMemcpy`/`cudaMemcpyAsync` calls and no H<->D transfer logic anywhere in the three files. 15.1: there is no transfer-in-a-loop to hoist/batch — device_partial.cu:10-13 is only `= default` pimpl special members, and the entire reason this unit exists is the OPPOSITE of a transfer: the M4.5 device-resident combine keeps each device's f2/vpair partial RESIDENT on the device that computed it ("NO D2H, NO free", device_partial.hpp:14, reinforced at device_partial.hpp:16-18), so by design no copy occurs here. 15.2: there is no `cudaMemcpyKind` direction enum used anywhere, so no direction-vs-actual-transfer mismatch is possible. 15.3: no host-pinned-vs-pageable decision is made in this unit — the `std::vector<int> block_sizes` (device_partial.hpp:46) is a plain host-side per-block SNP-count vector that is never the source/destination of a frequent device transfer in this TU (the combine that places it lives in cuda_backend.cu/p2p_combine.cu), and the resident `double` data is owned by the `DeviceBuffer<double>` f2/vpair members (device_partial_impl.cuh:17-18), whose any-transfer behavior lives in device_buffer.cuh / the consuming TUs. Any genuine transfer/pinned-memory finding belongs to those units, not this declaration-only handle.

## Group 16 — RAII: ownership & wrapper hygiene

No Group 16 issues found. This unit is an exemplary pimpl-RAII handle; every Group 16 task is satisfied. 16.1: the ONLY owned resource in the unit is device memory, and it is fully wrapped — the two `DeviceBuffer<double>` f2/vpair members (device_partial_impl.cuh:17-18) are the project's canonical move-only `cudaMalloc`/`cudaFree` RAII owner (device_buffer.cuh:78-93, one of the three allowlisted alloc TUs); there are NO unwrapped streams, events, graphs/graph-execs, texture/surface objects, memory pools, CUDA arrays, pinned host memory, or cuBLAS/cuSOLVER library handles anywhere in the three files (device_partial.cu:10-13 is only `= default` special members, device_partial_impl.cuh is a two-field aggregate, device_partial.hpp is the CUDA-free declaration). 16.2: `DevicePartial` is correctly move-only with null-on-move — copy is `= delete` (device_partial.hpp:35-36), move-ctor/assign are declared `noexcept` (device_partial.hpp:33-34) and defined `= default` out-of-line (device_partial.cu:12-13); the defaulted moves transfer the `std::unique_ptr<Impl> impl` member (device_partial.hpp:54), which nulls the moved-from to a valid empty handle (impl == nullptr, `empty()` device_partial.hpp:50), so a moved-from DevicePartial frees nothing — no double-free, no un-nulled stale handle; the underlying `DeviceBuffer` likewise nulls its moved-from with `std::exchange(o.ptr_, nullptr)` (device_buffer.cuh:78-79,84-85). 16.3: rule of five is complete and explicit — dtor (device_partial.hpp:32 / device_partial.cu:11), deleted copy-ctor + copy-assign (device_partial.hpp:35-36), move-ctor + move-assign (device_partial.hpp:33-34 / device_partial.cu:12-13); the out-of-line `= default` definitions exist precisely so the special members are instantiated where `Impl` is a COMPLETE type (the required CUDA-free-header pimpl idiom, file-header comment device_partial.cu:1-5), not omitted or copyable. 16.4: single, clear ownership — `std::unique_ptr<Impl>` (device_partial.hpp:54) is the one owner of the Impl, which owns the buffer pair; the public `int` shape scalars / `std::vector<int> block_sizes` are non-owning host metadata, and the resident `double*` is owned by `DeviceBuffer` and handed to consumer kernels (cuda_backend.cu/p2p_combine.cu) only as a non-owning view via `data()` (device_buffer.cuh:95-96) which those TUs never free; DevicePartial is never passed by value (move-only), so no owning wrapper is copied across an API boundary. 16.5: the wrapper is NOT reinvented — it composes `std::unique_ptr` + the canonical `DeviceBuffer<double>` rather than hand-rolling a raw-pointer owner; the four out-of-line special members (device_partial.cu:10-13) are the minimum the pimpl pattern requires for an incomplete-type member, not a redundant freeing wrapper.

## Group 17 — RAII: lifetime & deleter pitfalls (CUDA-specific)

- [17.5][LOW] src/device/device_partial.hpp:27-28 — Multi-GPU teardown correctness rests on the documented assumption "cudaFree is pointer-device-aware — it frees on the buffer's owning device from any current-device context", NOT on a device-setting deleter. The handle carries `device_id` (the alloc device, device_partial.hpp:42) but the actual free path is `DeviceBuffer::reset()` → bare `cudaFree(ptr_)` (device_buffer.cuh:117) with NO `cudaSetDevice(device_id)` and no record/restore. For plain `cudaMalloc`/`cudaFree` pointers this is CORRECT (the runtime resolves the pointer's owning context), so task 17.5's "deleter must cudaSetDevice to the alloc device" does NOT apply here — it would only apply to a pool/async free (`cudaFreeAsync`) or to stream/event destroy, none of which this unit uses. So this is correct-by-design; the only note is that the correctness is asserted in this unit's comment while the free lives in `DeviceBuffer`, where no comment reaffirms the cross-device-free guarantee. Suggested: no code change for `cudaMalloc`/`cudaFree`; optionally note in DeviceBuffer that `cudaFree` is current-device-independent so a future switch to `cudaMallocAsync`/pool memory (which is NOT device-independent on free) re-triggers a 17.5 deleter requirement.
- [17.1][LOW] src/device/device_partial.cu:11 — `~DevicePartial() = default` is the right non-throwing teardown: it runs `~Impl()` (defaulted) → the two `DeviceBuffer<double>` dtors, whose `reset()` is `noexcept` and routes a nonzero `cudaFree` status to a debug warning rather than throwing (device_buffer.cuh:111-125), so no exception can escape during unwinding. The defaulted `~DevicePartial`/`~Impl` are themselves implicitly `noexcept` and invoke nothing throwing. No teardown-order concern is assignable to this unit (the "context may be gone at exit" guard, if needed, belongs to DeviceBuffer's swallow-and-warn path). Recorded as satisfied, not a defect.

Group 17 tasks not applicable to this declaration-only unit: 17.2 (deleter↔allocator match) — no deleter or allocator named here; the `cudaMalloc`↔`cudaFree` pairing lives entirely in DeviceBuffer (device_buffer.cuh:74,117). 17.3 (`unique_ptr<T[]>` on cudaMalloc) — the sole smart pointer is a single-object `std::unique_ptr<Impl>` (device_partial.hpp:54) over a host struct freed by host `delete`, never an array and never cudaMalloc-backed. 17.4 (RAII-frees-vs-async-done use-after-free) — no stream or async work is created in this unit; the "free in dtor AFTER the combine has consumed it" ordering (device_partial.hpp:24-28) is an external precondition enforced by the orchestrator's handle-vector lifetime + DeviceBuffer dtor, already captured under Group 14.4, so no Group 17 use-after-free bug is assignable here.

## Group 18 — Correctness traps (wrong numbers, not crashes)

No Group 18 issues found. This is a declaration-only unit with NO `__global__`/`__device__` kernels, no shared memory, no warp intrinsics, no thread indexing, and no reductions anywhere in the three files, so every Group 18 task is structurally out of scope. device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members (host code); device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate; device_partial.hpp is the CUDA-free opaque-handle declaration with one host `const noexcept` accessor (`empty()`, device_partial.hpp:50). 18.1/18.2: no `__syncthreads()` and no shared-memory write-then-read exists, so neither a divergent barrier nor a missing RAW/WAR barrier is possible. 18.3/18.4: no warp-synchronous code and no `__shfl`/`__ballot`/`__any`/`__all` (or their `_sync` forms) appear, so there is no independent-thread-scheduling assumption and no missing-mask intrinsic. 18.5: no kernel and no thread/index arithmetic exists (the `[P*P*n_block_local]` shape at device_partial_impl.cuh:17-18 appears only in comments, never as a live bounds-guarded access), so no missing `if(idx<n)` OOB is assignable here — the kernels that iterate this resident f2/vpair tensor live in the consuming TUs (cuda_backend.cu/p2p_combine.cu), where any bounds-guard finding belongs. 18.6: no cross-thread shared/global read without a barrier or atomic — there are no threads at all in this host-side handle. 18.7: no float reduction exists, so there is no order-dependent reduction to check against the §12 fixed-order/deterministic requirement (the deterministic reductions over this resident data live in the consuming TUs).

## Group 19 — Performance: debug leftovers

No Group 19 issues found. This is a declaration-only unit with NO `__global__`/`__device__` kernels and NO CUDA runtime calls anywhere in the three files, so every Group 19 task is structurally out of scope. 19.1: there is no `cudaDeviceSynchronize()` (stray or otherwise) and no `STEPPE_CUDA_CHECK_KERNEL` use — device_partial.cu:10-13 is only out-of-line `= default` pimpl special members, device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate, and device_partial.hpp is the CUDA-free opaque-handle declaration; the synchronizing/teardown work is delegated to `DeviceBuffer`'s own dtor in another TU. 19.2: no leftover `printf`/`std::printf` and no `#if 0` (or any preprocessor-disabled block) appears in any of the three files — the only `#if`/`#ifndef` are the legitimate include guards (device_partial_impl.cuh:8-9,23; device_partial.hpp:5-6,58). 19.3: there is no `__syncthreads()` anywhere (no kernel exists), so there is no redundant barrier to flag.

## Group 20 — Performance: memory access

No Group 20 issues found. This is a declaration-only unit with NO `__global__`/`__device__` kernels, no shared memory, no thread indexing, and no global-memory access patterns anywhere in the three files, so every Group 20 task is structurally out of scope. device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members (host code); device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate; device_partial.hpp is the CUDA-free opaque-handle declaration with one host `const noexcept` accessor (`empty()`, device_partial.hpp:50). 20.1: there is no kernel and no `threadIdx`/`blockIdx`-indexed global load/store, so there is no coalescing pattern to assess — the resident `[P*P*n_block_local]` f2/vpair tensor (device_partial_impl.cuh:17-18) is named only in comments here; the kernels that actually stride it live in the consuming TUs (cuda_backend.cu/p2p_combine.cu), where any uncoalesced-access finding belongs. 20.2: no `__shared__` memory is declared or accessed anywhere in the unit, so there is no bank-conflict (serializing stride/padding) pattern to flag. 20.3: there is no kernel loop and no repeated global-memory read — the only data members are device pointers owned by `DeviceBuffer<double>` and plain host scalars/`std::vector<int> block_sizes` (device_partial.hpp:39-46), none of them re-read in any hot loop here (there are no loops at all in this unit), so there is no value to cache in a register/shared.

## Group 22 — Performance: compute & launch

No Group 22 issues found. This is a declaration-only unit with NO `__global__`/`__device__` kernels, no `<<<...>>>` launch sites, no loops, and no arithmetic anywhere in the three files, so every Group 22 task is structurally out of scope. device_partial.cu:10-13 defines only out-of-line `= default` pimpl special members (host code); device_partial_impl.cuh:16-19 is a two-field `DeviceBuffer<double>` aggregate; device_partial.hpp is the CUDA-free opaque-handle declaration with one host `const noexcept` accessor (`empty()`, device_partial.hpp:50). 22.1: there are no atomics — no `atomicAdd`/`atomicCAS`/etc. and no kernel to host them; the deterministic reductions over this resident f2/vpair tensor live in the consuming TUs (cuda_backend.cu/p2p_combine.cu), where any atomics-vs-reduction finding belongs. 22.2: there is no integer div/mod at all — the `[P*P*n_block_local]` index arithmetic (device_partial_impl.cuh:17-18) appears only in comments, never as live in-loop division/modulo; no loop exists here to host one. 22.3: there is no loop-invariant work or repeated index recomputation to hoist — there are zero loops and zero computed expressions in the unit (device_partial.cu:10-13 is only `= default` special members; the shape product is never evaluated here). 22.4: there is no launch overhead to fuse or capture into a CUDA graph — this unit issues no kernel launches and no CUDA runtime calls; it is the M4.5 device-resident handle that the orchestrator forwards, and any per-launch-cost / graph-capture finding belongs to the consuming TUs that actually launch over this resident data, not to this declaration-only handle.
