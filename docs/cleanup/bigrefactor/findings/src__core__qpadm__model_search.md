# Review findings — src__core__qpadm__model_search

Files: /home/suzunik/steppe/src/core/qpadm/model_search.cpp, /home/suzunik/steppe/src/core/qpadm/model_search.hpp

## Group 4 — Type & numeric

No Group 4 issues found.

Notes (context, not findings): This unit is a host-pure orchestrator (model-list partition, multi-GPU jthread fan-out, pre-sized-slot re-sort) — it computes NO global index into the f2 tensor (P*P*n_block) or the genotype matrix, so the §SCALE 2^31 concern does not apply here. 4.1: no float math; the only numeric construct is the intentional `Precision{EmulatedFp64, kDefaultMantissaBits}` (cpp:31,86,294), which is FP64-by-design — N/A. 4.2/4.6: all indices are `std::size_t` loop counters over `models`/`results`/`gpus` (thousands of models, G devices) and `int model_index` values that are range-checked (`mi < 0 || static_cast<std::size_t>(mi) >= n`) before any cast (cpp:223-226, 263-266, 305-306) — no array offset can exceed `int` range and no widening-after-overflow arithmetic exists. 4.3: only `std::vector`/`reserve` (element-counted); no `cudaMalloc`/`new[]` byte-sizing. 4.4: every loop counts up (`++i`/`++g`/`++k`); no unsigned countdown. 4.5: loop-bound comparisons are `std::size_t` vs `.size()`/`G` throughout (cpp:94, 111, 117, 175, 182, 185, 247, 277, 295) — no signed/unsigned mismatch. 4.7: borrowed device handles are the typed wrapper `const device::DeviceF2Blocks*` (cpp:165, 191-192, 253), not raw untyped `T*`; no host/device pointer-space confusion.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

Notes (context, not findings): This is a host-pure, CUDA-FREE orchestrator (is_cuda=false) — no `.cu` code, no CUDA runtime calls, no kernels. 2.1: no CMake/arch flags or `sm_*` references in a source/header file (arch lists live in CMake, not here). 2.2: no texture/surface references (`texture<...>`, `cudaBindTexture*`, `surface<...>`) — no device kernel constructs at all. 2.3: no warp intrinsics (non-`_sync` or otherwise — `__shfl`/`__ballot`/`__any`/`__all`/`__activemask`); there is no device code. 2.4: no `cudaThreadSynchronize` (or any `cuda*` runtime call) — the only synchronization is the `std::jthread` join barrier (cpp:244-273), which is standard C++, not CUDA. The CUDA-adjacent identifiers present (`device::DeviceF2Blocks`, `ComputeBackend`, `device::Resources`, `upload_f2_blocks_to_device`, `to_host`) are all CUDA-free seam abstractions declared in other units; nothing in this unit touches a deprecated/removed CUDA API or platform flag.

## Group 3 — Dead / commented-out code

- [3.3][LOW] src/core/qpadm/model_search.cpp:19 — `#include "steppe/error.hpp"  // Status` is an unused direct include: `Status` is the only symbol `error.hpp` exports, and it is never referenced anywhere in this translation unit (the token `Status` appears ONLY in this include comment; the `worker_errors`/`exception_ptr` machinery uses `<exception>`, not `Status`). It is also already pulled transitively via `model_search.hpp` → `steppe/qpadm.hpp:31`. Suggested: drop the line (or, if kept defensively, remove the misleading `// Status` comment) — hygiene only, no behavior change.

Notes (context, not findings): 3.1: no commented-out executable code — the large comment blocks (cpp:28-38, 47-54, 66-68, 76-82, 88-92, the TODO(multigpu-host-bounce) at 139-163, and the `// ----` section dividers) are active design rationale / TODOs, not code parked "just in case". 3.2: no `#if 0` and no code after `return`/`break`; the lone bare `return;` (cpp:251, empty-shard guard inside the worker lambda) and all function-final `return`s correctly terminate their scopes. 3.3 (rest): all other includes are used — `<cstddef>` (std::size_t, 21 uses), `<exception>` (exception_ptr/current_exception/rethrow_exception), `<span>`, `<stdexcept>` (runtime_error), `<thread>` (jthread), `<vector>`, `model_search_core.hpp` (plan_model_shards/ModelShard), `qpadm_fit.hpp` (run_impl/left_with_target), `config.hpp` (Precision/kDefaultMantissaBits), `fstats.hpp` (F2BlockTensor). The file-internal helpers `fit_models_batched_default`/`model_in_small_path` ARE used (declared in device/backend.hpp:318,331 as the base ComputeBackend body's delegates) and `fit_shard` is used within this TU (cpp:220,257); none are dead. 3.4: every assigned local is read — `left_idx`→assemble_f4, `X`→moved into run_impl, `res`/`r`→returned, `prec`→passed to backend calls, `need_host`/`n_owned`→drive the host-materialize/reserve, `slot`/`mi`→index `results` (cpp:305-307); no computed-but-unread values.

