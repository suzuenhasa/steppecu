# Review findings — src__core__fstats__f2_blocks_multigpu

Files: src/core/fstats/f2_blocks_multigpu.cpp, src/core/fstats/f2_blocks_multigpu.hpp

## Group 4 — Type & numeric

No Group 4 issues found.

Notes (verified clean, not findings):
- 4.2/4.6 (index width / overflow at scale): the only at-scale index arithmetic is the full-tensor sizing in the host-staged arm. `slab = (std::size_t)P * (std::size_t)P` (line 204) widens BOTH operands to size_t before the multiply, and `total = slab * (std::size_t)n_block` (line 205) is computed in size_t (slab is already size_t). For the max case P=2500, n_block=757 the product ~4.7e9 exceeds 2^31 but is held in size_t — correct. The `for (int b=0; b<n_block; ++b)` loop (line 372) keeps `b` tiny and widens via `(std::size_t)b` before indexing.
- 4.1 (float/double): orchestration-only file, no numeric literals or float math — N/A.
- 4.3 (allocation sizing): no cudaMalloc/new/DeviceBuffer; only std::vector resize/assign (element-counted, no sizeof confusion) — N/A.
- 4.4/4.5 (countdown / signed-unsigned compare): single ascending `int b < int n_block` loop; no unsigned countdown, no signed/unsigned mismatch.
- 4.7 (host/device pointer typing): host `.data()` pointers (line 212) flow only into the host-pure core combine; the device-resident path uses opaque typed handles (DevicePartial / DeviceF2Blocks). Space discipline is clean.

## Group 2 — Deprecated / removed APIs & platform support

No Group 2 issues found.

Notes (verified clean, not findings):
- This is a CUDA-FREE, host-pure orchestration unit in `steppe::core` (is_cuda=false): it includes NO CUDA header (`<cuda_runtime.h>` etc. absent; confirmed in .cpp lines 29-51 and .hpp lines 22-28) and issues no CUDA API call — all device interaction is through the CUDA-free `ComputeBackend` seam and opaque handles (DevicePartial / DeviceF2Blocks). None of the Group 2 patterns can occur here by construction.
- 2.1 (dropped archs Maxwell/Pascal/Volta, sm_50/60/70, CUDA_ARCH): no arch flags, no CMake arch lists, no `__CUDA_ARCH__` guards in either file — N/A (those live in CMake/.cu units, not this host TU).
- 2.2 (texture/surface references): no `texture<>`/`surface<>`/`cudaBindTexture*`/`cudaBindSurface*` — none; this unit touches no texture memory.
- 2.3 (non-`_sync` warp intrinsics): no warp intrinsics at all (no `__shfl`/`__any`/`__all`/`__ballot`); host-only code, no `__device__`/`__global__` kernels.
- 2.4 (cudaThreadSynchronize -> cudaDeviceSynchronize): no `cudaThreadSynchronize` (or any cuda* runtime call). The `cudaMemcpyPeer`/`cudaMemcpyPeerAsync`/`cudaDeviceEnablePeerAccess` tokens at .cpp lines 22, 109, 176 and .hpp line 17 are DESCRIPTIVE COMMENTS only, referencing the P2P transport implemented in the separate `device/cuda/p2p_combine.cu` unit — not API calls in this file, so not in scope here.

## Group 3 — Dead / commented-out code

- [3.4][LOW] src/core/fstats/f2_blocks_multigpu.cpp:358 — `out.P = P;` in compute_f2_blocks_multigpu_tiered is computed but never read: all three switch arms overwrite it unconditionally before any read (Resident `out.P = out.resident.P` line 386, HostRam `out.P = out.host.P` line 399, Disk `out.P = out.disk.P` line 419). The companion `out.n_block = ...` (line 359) is NOT dead — the HostRam arm only overwrites it under the `if (out.host.n_block >= 0)` guard (line 398), so its initial value is a live default. Suggested: drop the line 358 `out.P = P` (or keep both as intentional defensive defaults and leave as-is — it is harmless, parity-neutral hygiene only).

Notes (verified clean, not findings):
- 3.1 (commented-out code kept "just in case"): the file is heavily commented, but every `//` block is design/rationale PROSE (the §4 gate banner, parity argument, doc cross-references) — no statements or expressions are commented out anywhere in either file. None.
- 3.2 (unreachable code): no `#if 0`. Each `if (G == 1)` early-returns but the trailing code is reached for G != 1; the `if (use_p2p)` arm returns and the host-staged code below is reached when `!use_p2p`; the tiered `switch` covers all OutputTier enumerators and the trailing `return out` (line 424) is reachable. The `G >= 2` term in `use_p2p` (lines 163, 272) is documented as "dead-true" (the G==1 fast-path returned above), but that is an INTENTIONAL, parity-neutral predicate-completeness choice (cleanup X6, so the code matches the four-term gate documented across the seam) — not dead code, explicitly justified at lines 149-156; NOT flagged.
- 3.3 (unused symbols): all includes are used — `<cstddef>`(size_t), `<cstdlib>`(std::getenv lines 354/409), `<span>`(std::span), `<stdexcept>`(std::runtime_error), `<vector>`(std::vector); `block_partition_rule.hpp`(block_ranges/BlockRange line 367), `tier_select.hpp`(resolve_output_tier/free_host_ram_bytes/OutputTier lines 351-355), `stream_f2_blocks.hpp`(StreamTarget lines 393/412), `p2p_combine.hpp`(combine_f2_partials_resident_device), and every device handle/header has a use. All three entries use all of their params (resources, Q, V, N, partition, precision). No unused locals — `P`/`M`/`n_block`/`G`/`use_p2p`/`shards`/`shards_span`/`slab`/`total`/`free_vram`/`free_host`/`tier`/`path`/`ranges` are each read after assignment.

## Group 5 — Hardcoded values / magic numbers

- [5.3][MED] src/core/fstats/f2_blocks_multigpu.cpp:161-163, 270-272 — the four-term `use_p2p` combine gate predicate (`prefer_p2p_combine && enable_peer_access && gpus[0].caps.can_access_peer && G >= 2`) is duplicated VERBATIM in two functions (host entry and device entry). The file's own banner (lines 101-160) declares this the "SINGLE AUTHORITATIVE HOME" of the gate so it "has ONE place to drift from", yet the predicate is physically copy-pasted into the second function (line 268 even calls it "copied VERBATIM"). If a fifth term or a term reorder ever lands in one copy and not the other, the two host/device entries silently disagree on transport selection — a real correctness/parity divergence risk. Suggested: extract a single `bool select_p2p_combine(const Resources&, std::size_t G)` helper (or a `Resources::should_use_p2p_combine()`) and call it from both sites so the predicate exists once. (Note: the X6-documented dead-true `G >= 2` term itself is intentional predicate-completeness, not flagged.)
- [5.3][MED] src/core/fstats/f2_blocks_multigpu.cpp:219-220 — the genuine-degrade detection predicate (`prefer_p2p_combine && enable_peer_access && !can_access_peer && G >= 2`) is a third near-copy of the same four-term gate with only the third term negated. It is logically coupled to `use_p2p` (it must fire exactly when "P2P was requested+permitted but unavailable"), so it must stay in lockstep with the gate at 161-163; if the gate gains/changes a term this copy will not. Suggested: derive it from the same factored helper (e.g. `requested_p2p && !use_p2p`), so the degrade WARN cannot drift from the gate it describes.
- [5.4][LOW] src/core/fstats/f2_blocks_multigpu.cpp:410 — the disk-cache fallback path `"./steppe_f2_blocks.cache"` is a hardcoded relative-path string literal inlined at the only use site (the env `STEPPE_F2_CACHE_PATH` name at line 409 is the documented escape hatch, so the value is overridable, but the default itself is an unnamed magic string). Suggested: hoist to a named constant (e.g. `constexpr char kDefaultF2CachePath[] = "./steppe_f2_blocks.cache";`) near the tier-select includes so the default has one named home shared with any future reader/cache-eviction site.

Notes (verified clean, not findings):
- 5.1 (unnamed literals): the only bare integer literals are the device-count bounds `G < 1` (lines 82, 255, 340), `G == 1` (lines 93, 263), and `G >= 2` (gate term) — each is a structural, self-documenting count threshold (≥1 device required; the 1-device fast path), explained inline; not magic numbers. There are NO float/precision literals, NO block/grid dims, NO tile sizes, NO `0.5`/`1024`/`0.001f` — this is a CUDA-free orchestration TU (the precision/Ozaki-mantissa literals live in the device/Ozaki units, per §12/FP64 context).
- 5.2 (hardcoded sizes/bounds that should be derived): the tier-select bounds are explicitly RUNTIME-probed, not hardcoded — `free_vram` from `caps.free_vram_bytes` (line 351) and `free_host` from `free_host_ram_bytes()`/sysinfo (line 352), with the comment stressing "never hardcoded" (lines 312, 346). The shard count is `G` (device count, derived). `slab = P*P` (line 204) and `total = slab*n_block` (line 205) are derived from the inputs, not constants. Nothing to parameterize.
- 5.4 (device ids): `resources.gpus[0]` / `gpus[0].device_id` (lines 94, 163, 187, 264, 288, 304, 351, 382, 396, 416) — index `0` is the structural ROOT/lone device (the fixed g=0..G-1 combine root, documented at .hpp lines 59, 105 and .cpp lines 92, 187), a domain invariant, not a hardcoded board id; not flagged. No `cudaSetDevice(0)`-style literal device ids, no PCI ids, no hostnames.
- 5.5 (ambiguous 32): no `32` (or any warp-size / power-of-two block-dim literal) appears in either file — host-pure orchestration, no kernel launch geometry. N/A.

## Group 6 — Naming

- [6.3][LOW] src/core/fstats/f2_blocks_multigpu.cpp:351-352 — the two sibling free-memory probes are named asymmetrically: `free_vram` (VRAM bytes) vs `free_host` (host RAM bytes). `free_vram` carries the memory-class suffix while `free_host` drops the `_ram`/`_bytes` suffix that its own source `free_host_ram_bytes()` (line 352) and the device-side `caps.free_vram_bytes` (line 351) both use. The pair reads as "vram" paired with "host" rather than two parallel byte counts. Harmless (both feed `resolve_output_tier` positionally) and meaning is clear from the initializer, but inconsistent within the same two lines. Suggested: name the pair in parallel (e.g. `free_vram_bytes` / `free_host_bytes`) so both probes share one convention.

Notes (verified clean, not findings):
- 6.1 (cryptic names): the single-letter names are all DOMAIN-CANONICAL and consistent codebase-wide — `Q`/`V`/`N` are the architecture.md per-SNP Q/V/N contract (.hpp lines 22, 110), `P`/`M` are pop-count / SNP-count, `G` is device count (`resources.device_count()`, line 81), and `b` (line 372) is a tight block-index loop counter widened via `(std::size_t)b` before indexing. No `tmp`/`data2`/`arr`/`flag`-style opaque names anywhere. Locals `slab`/`total`/`tier`/`path`/`ranges`/`out`/`target`/`env`/`host`/`partials`/`shards`/`shards_span` are all self-describing.
- 6.2 (misleading names): every container/flag name matches its contents — `block_sizes` holds per-block sizes, `block_id` holds block ids, `partials` holds DevicePartials, `shards` holds DeviceShards, `use_p2p` is the P2P-gate bool, `n_block` is a block COUNT used as a count/loop bound (lines 205, 208, 372). No count-that-is-an-index, no list-that-is-a-map.
- 6.3 (inconsistent conventions in one file): locals are uniformly snake_case (`n_block`, `use_p2p`, `shards_span`, `free_vram`, `free_host`, `disk_path`); the single-cap domain symbols `P`/`M`/`G` are the established convention shared with views.hpp/the seam, not a competing style. No `nElements` vs `num_elements` vs `n` drift within the unit (the `free_vram`/`free_host` suffix asymmetry is flagged separately as 6.3 LOW above).
- 6.4 (nonstandard abbreviations): all abbreviations are established/documented — `Vpair`/`vpair` is the AT2 variance-pair domain term (.hpp line 78), `D2H`/`D2D` are CUDA-standard transfer directions, `SPMG` is defined in the header banner (.hpp line 3), `p2p` is peer-to-peer (CUDA-standard), `VRAM`/`RAM` are conventional. No invented or obscure abbreviations.

## Group 7 — Duplication

- [7.1][MED] src/core/fstats/f2_blocks_multigpu.cpp:161-163, 270-272 — the four-term `use_p2p` gate block (`prefer_p2p_combine && enable_peer_access && gpus[0].caps.can_access_peer && G >= 2`) is copy-pasted verbatim across the host entry and the device entry (line 268 explicitly labels the second copy "copied VERBATIM"). The file's own banner (lines 101-160) calls this the "SINGLE AUTHORITATIVE HOME" so the predicate "has ONE place to drift from", yet it physically exists twice. A term add/reorder in one copy silently diverges transport selection between the host/device entries. Suggested: extract one `bool select_p2p_combine(const Resources&, std::size_t G)` (or `Resources::should_use_p2p_combine(G)`) and call it from both sites. (Cross-refs Group 5 5.3 from the magic-number angle; the same defect under task 7.1 copy-pasted-block.)
- [7.1][MED] src/core/fstats/f2_blocks_multigpu.cpp:71-78, 244-251, 330-337 — the four-line "Shared contract" STEPPE_ASSERT block (Q/V/N P agree, M agree, P/M non-negative, block_id length == M) is copy-pasted three times, differing ONLY by the function-name string literal embedded in each message (lines 268/329 comments even say "identical to the host entry" / "identical to the other entries"). Suggested: fold into one `validate_multigpu_inputs(Q, V, N, partition, M, const char* fn)` helper (or a STEPPE_ASSERT-bearing inline) so the four guards live once and only the fn tag varies.
- [7.1][MED] src/core/fstats/f2_blocks_multigpu.cpp:81-86, 254-259, 339-344 — the `G = device_count(); if (G < 1) throw runtime_error(...)` fail-fast is copy-pasted three times, differing only by the function-name prefix in the throw message. Suggested: a `require_at_least_one_device(resources, const char* fn)` returning G, or fold into the same input-validation helper as the asserts above.
- [7.2][LOW] src/core/fstats/f2_blocks_multigpu.cpp:60-62, 239-241, 327 — the `const int P = Q.P; const long M = Q.M; const int n_block = partition.n_block;` prologue is repeated verbatim at the top of all three entries. Minor, but it travels with the assert/fail-fast blocks above and would naturally fold into the same shared prelude. Suggested: extract alongside the validation helper, or leave if the asserts are factored (the trio is cheap once the larger blocks move).
- [7.1][MED] src/core/fstats/f2_blocks_multigpu.cpp:178-187, 282-288 — the P2P resident arm is near-duplicated: both call `compute_multigpu_partials_resident(...)`, set `last_combine_path = P2pDeviceResident`, then `combine_f2_partials_resident_device(span(partials), shards_span, P, n_block, gpus[0].device_id)`; the ONLY difference is the host entry appends `.to_host()` (line 187). Suggested: have the host entry call the device entry's resident arm and append `.to_host()` once (the device-entry no-peer fallback at line 302-304 already delegates the OTHER way — host entry -> device upload — so a single shared resident helper would let both directions reuse one body).
- [7.2][LOW] src/core/fstats/f2_blocks_multigpu.cpp:168-170, 275-277 — the shard-plan pair (`plan_multigpu_shards(partition, M, n_block, G)` then build `shards_span` over `shards.data()/.size()`) is duplicated verbatim in the host and device entries. If the resident arms are merged (7.1 above) this folds out for free; otherwise a `make_shard_plan(partition, M, n_block, G)` returning `{shards, span}` (or just the vector, with the span built at the one use) removes the repeat.
- [7.2][LOW] src/core/fstats/f2_blocks_multigpu.cpp:77,205,208,250,336,359,369,371,387,419 — the negative-clamp idiom `X < 0 ? 0 : X` (with `static_cast<std::size_t>(...)` on the count-to-size variants) appears ~10 times for `M` and `n_block`. Hand-repeated ternaries are easy to copy with one operand wrong. Suggested: a tiny `clamp_nonneg(long)` / `nonneg_count(int)` (or `static_cast<std::size_t>` wrapper) so the "treat a defensive negative as 0" rule is written once. Parity-neutral hygiene.
- [7.4][LOW] src/core/fstats/f2_blocks_multigpu.cpp:391-401, 403-421 — the HostRam and Disk switch arms share a fold-able skeleton: set `out.tier`, build a `StreamTarget`, set `target.tier`, point its dst, call `backend->compute_f2_blocks_streamed(Q,V,N,block_id,n_block,precision,target)`, then mirror P/n_block/block_sizes back from the per-tier handle (lines 398-401 vs 419-420). The Disk arm adds path resolution; otherwise the streamed-call + mirror-back is the same with the target field and source handle swapped. Suggested: factor a `finish_streamed_tier(out, target, handle)` (or a small helper taking the tier + dst + path) that runs the streamed call and the mirror-back once, so the two arms only differ in the target setup.

## Group 8 — Comments

- [8.2][MED] src/core/fstats/f2_blocks_multigpu.hpp:45-47 — STALE comment: the `compute_f2_blocks_multigpu_tiered` docstring claims "this is the single-GPU (G==1) arm (G>=2 delegates to the existing device entry for now)", but the function body (.cpp:339-425) has ONLY the `if (G < 1) throw` guard — no `G==1`/`G>=2` branch and no delegation to `compute_f2_blocks_multigpu_device`. For G>=2 it unconditionally runs the single-`gpus[0]` Resident/HostRam/Disk path (verified: the only G-test in the body is `G < 1`). A reader relying on the docstring would assume the tiered path delegates to multi-GPU sharding when G>=2; it does not (it silently uses only the root device). Suggested: correct the docstring to state the tiered path always drives gpus[0] regardless of G (multi-GPU tiered sharding is the unimplemented follow-on), or implement the documented G>=2 delegation.
- [8.2][LOW] src/core/fstats/f2_blocks_multigpu.hpp:90-92 — STALE/inaccurate combine name: the `compute_f2_blocks_multigpu` docstring describes the G>=2 path as "plan_block_shards → per-device sub-view compute → host-staged fixed-order combine (combine_f2_partials_host, the portable parity baseline)". The host-staged arm was rewritten (the M4.5 direct path, .cpp:190-212) to `compute_multigpu_partials_into` which D2Hs each partial DIRECTLY into the pre-allocated result; the .cpp's own comments (lines 196-198) state this replaced "the deleted host-staged combine ... NO combine_f2_partials_host alloc + copy_n". `combine_f2_partials_host` still exists as a symbol (f2_combine.cpp) but is no longer the combine this entry uses, so naming it as THE G>=2 combine in the header contract is stale. Suggested: update the docstring to name the current direct sharded-D2H path (compute_multigpu_partials_into) and keep the "bit-identical to combine_f2_partials_host" note only as a parity reference.
- [8.3][LOW] src/core/fstats/f2_blocks_multigpu.cpp:351 — missing rationale: `free_vram` is read from `resources.gpus[0].caps.free_vram_bytes` (the ROOT device only) while the docstring frames the tier as fitting the multi-GPU result; the inline comment (lines 347-350) explains WHEN the probe was captured but not WHY only gpus[0]'s VRAM gates the tier when G>=2 (it is correct given the tiered path is single-device, but that single-device constraint is the unstated premise). Suggested: add a one-line note that the tier is gated on the root device because the tiered path is single-GPU (cross-ref the G-handling note from 8.2).

## Group 9 — Constants & configuration

- [9.2][LOW] src/core/fstats/f2_blocks_multigpu.cpp:410 — tunable config default buried in logic: the disk-cache fallback path `"./steppe_f2_blocks.cache"` is an inline string literal inside the Disk switch arm. It is a genuine configurable default (the config.disk_cache_path knob and STEPPE_F2_CACHE_PATH env both override it), yet the baseline value is hidden mid-function rather than surfaced at the file top / next to the tier-select config so a reader/operator can see the cache default in one place. Suggested: hoist to a file-top `constexpr char kDefaultF2CachePath[] = "./steppe_f2_blocks.cache";` (this is the same line as Group 5 5.4 viewed from the tangled-config angle — one fix resolves both).
- [9.2][LOW] src/core/fstats/f2_blocks_multigpu.cpp:354,409 — the override-hatch env-var names `"STEPPE_FORCE_TIER"` (line 354) and `"STEPPE_F2_CACHE_PATH"` (line 409) are inline string literals at their `std::getenv(...)` call sites. These are configuration keys (the documented test/operator override surface, cited in the file header at lines 32 and 405-406) but exist only as bare literals in logic; a typo in one would silently disable the override, and there is no single place listing the unit's env knobs. Suggested: name them as file-top `constexpr char kEnvForceTier[]` / `kEnvF2CachePath[]` constants beside the tier-select includes so the env config surface is enumerated in one place.

No other Group 9 issues found.

Notes (verified clean, not findings):
- 9.1 (should-be-const left mutable): all eligible locals ARE already declared `const` — `P`/`M`/`n_block` (lines 60-62, 239-241, 326-327), `G` (lines 81, 254, 339), `use_p2p` (lines 161, 270), `shards`/`shards_span` (lines 168-170, 275-277), `slab`/`total` (lines 204-205), `free_vram`/`free_host`/`tier` (lines 351-353), and `ranges` (line 367). The genuinely-mutable locals must mutate: `out` is field-assigned across switch arms, `target`/`partials`/`host`/`path` are built/moved/conditionally overwritten. The function params that are intentionally non-const-ref (`Resources& resources`) are documented as mutated (the backend scratch / last_combine_path; .hpp lines 107-109). Nothing const-able is left mutable.
- 9.1 (constexpr opportunities): the only compile-time-constant value in the unit is the disk-default string (flagged under 9.2 above); there are no other inline integer/string constants that could be `constexpr` (the `G < 1`/`G == 1`/`G >= 2` thresholds are structural device-count bounds, intentionally inline per Group 5 5.1, not constexpr-worthy named constants).
- 9.2 (tunable knobs buried vs surfaced): the runtime tunables ARE surfaced — `force_tier` / `disk_cache_path` / `prefer_p2p_combine` / `enable_peer_access` all live on the `Config` struct (include/steppe/config.hpp:284,316,349,356) and are read through `resources.config.*` (lines 162, 271, 351-354, 407), not hardcoded. The tier bounds are runtime-probed (free_vram / free_host_ram_bytes), explicitly "never hardcoded" (lines 312, 346). Only the two default/key literals above (the cache default string and the two env-var names) are buried in logic rather than named at the file top.
- 9.3 (positional booleans): NO positional-boolean calls anywhere — `foo(true, false, ...)` does not occur. The backend seam calls (`compute_f2_blocks`, `compute_f2_blocks_device`, `compute_f2_blocks_streamed`, lines 94-95, 264-265, 382-383, 396-397, 416-417) and the core helpers (`plan_multigpu_shards`, `compute_multigpu_partials*`, `combine_f2_partials_resident_device`) pass only typed values/handles/spans, no bare bool flags. The four-term `use_p2p` gate (lines 161-163, 270-272) and the degrade predicate (lines 219-220) AND together NAMED bool members (`prefer_p2p_combine`, `enable_peer_access`, `can_access_peer`), never anonymous literals. `StreamTarget` (lines 393-394, 412-414) is configured by named field assignment, not a positional-bool ctor. Clean.

## Group 10 — Initialization

No Group 10 issues found.

Notes (verified clean, not findings):
- 10.1 (late/distant decls; uninitialized-then-assigned): every local in all three entries is declared AT first use WITH an initializer — `P`/`M`/`n_block` (lines 60-62, 239-241, 326-327), `G` (lines 81, 254, 339), `use_p2p` (lines 161, 270), `shards`/`shards_span` (lines 168-170, 275-277), `slab`/`total` (lines 204-205), `free_vram`/`free_host`/`tier` (lines 351-353), `ranges` (line 367), and `path` (line 407, initialized from `resources.config.disk_cache_path` then conditionally reassigned — no uninitialized window). The two result objects are declared exactly where they begin being populated: `F2BlockTensor out` (line 201) immediately followed by the field assignments (lines 202-208), and `F2BlocksOut out` (line 357) immediately followed by lines 358-359 and the per-arm fills. `StreamTarget target` is declared inside its own switch arm at its point of use (lines 393, 412). There is no declare-now/assign-far-below pattern and no read-before-write.
- 10.2 (zero-init assumptions that do not hold): the two result types both carry IN-CLASS member initializers, so no path relies on implicit zero-init. `F2BlockTensor` (include/steppe/fstats.hpp:68,71) initializes `P = 0` / `n_block = 0` and its vectors default-construct empty; in the host-staged arm `out.P`/`out.n_block` are explicitly assigned (lines 202-203) and the three vectors are resize/assign'd (lines 206-208) before the worker fan-out, so nothing leans on the zero default. `F2BlocksOut` (src/device/f2_blocks_out.hpp:63-66) initializes `tier = OutputTier::Resident`, `P = 0`, `n_block = 0`, and the three tier members default-construct; `out.tier` is explicitly set in every switch arm (lines 381, 392, 404) and the switch is exhaustive over all three OutputTier enumerators. The one default-relying READ — `if (out.host.n_block >= 0)` (line 398) — is well-defined because `out.host` is a default-constructed `F2BlockTensor` whose `n_block` is in-class-initialized to 0 (not implicit zero-init), and similarly the `StreamTarget` (src/device/stream_f2_blocks.hpp:36-39) fields `tier`/`host_dst`/`disk_dst`/`disk_path` are all in-class-initialized, with the relevant ones explicitly set before each backend call. The `out.P = P` default ahead of the exhaustive switch (line 358) is overwritten by every arm; it is harmless and already noted under Group 3.4 (dead store), not a zero-init hazard.

